#![forbid(unsafe_code)]

use anyhow::{bail, Result};

fn main() -> Result<()> {
    bail!(
        "generate_zk_fixtures is not implemented in this workspace snapshot; restore the original implementation before using this binary"
    )
}
