{
  "name": "system_health_check",
  "description": "Performs a standard system health check sequence (CPU, Memory).",
  "steps": [
    {
      "tool": "shell__run",
      "params": {
        "command": "echo",
        "args": ["[HealthCheck] CPU: OK"]
      }
    },
    {
      "tool": "shell__run",
      "params": {
        "command": "echo",
        "args": ["[HealthCheck] RAM: OK"]
      }
    },
    {
      "tool": "agent__complete",
      "params": {
        "result": "System health check passed."
      }
    }
  ]
}