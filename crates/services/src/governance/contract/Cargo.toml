# Path: crates/services/governance/contract/Cargo.toml
[package]
name = "governance-service-contract"
version = "0.1.0"
edition = "2021"
description = "On-chain WASM component for the IOI SDK Governance service"
license = "MIT OR Apache-2.0"

[lib]
crate-type = ["cdylib"]

[dependencies]
ioi-contract-sdk = { path = "../../../../contract-sdk", features = [
    "wee_alloc",
] }
# DO NOT inherit from workspace. Specify version and features directly for no_std compatibility.
parity-scale-codec = { version = "3.7.5", default-features = false, features = [
    "derive",
] }
