#![allow(unused_imports)]

use super::*;

// Slot definitions and defaults are still co-located with component specs in
// `components` for this no-behavior-change split because action-frame assembly
// depends on both. This module reserves the slot boundary for future extraction.
