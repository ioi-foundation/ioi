[package]
name = "fee-calculator-service"
version = "0.1.0"
edition = "2021"

[workspace]

[lib]
crate-type = ["cdylib"]

[dependencies]
ioi-contract-sdk = { path = "../../../../contract-sdk" }
parity-scale-codec = { version = "3.7.5", default-features = false, features = [
    "derive",
] }

# ❌ REMOVE this whole block (it’s what causes the error)
# [package.metadata.component]
# package = "ioi:fee-calculator"
# target = "../../../../types/wit/ioi.wit"
# world  = "service"

[profile.release]
codegen-units = 1
opt-level = "z"
lto = true
panic = "abort"
strip = true
