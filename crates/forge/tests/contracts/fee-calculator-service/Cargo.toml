# Path: crates/forge/tests/contracts/fee-calculator-service/Cargo.toml

[package]
name = "fee-calculator-service"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"] # Compile to a dynamic system library for WASM

[dependencies]
# FIX: The `workspace = true` shorthand is no longer valid.
# Provide the full dependency details.
depin-sdk-contract = { path = "../../../../contract", features = ["wee_alloc"] }
parity-scale-codec = { workspace = true }
