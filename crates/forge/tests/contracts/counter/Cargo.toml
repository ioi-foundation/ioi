# Path: crates/forge/tests/contracts/counter/Cargo.toml
[package]
name = "counter-contract"
version = "0.1.0"
edition = "2021"

# This crate is a separate project and should not inherit workspace properties.
[workspace]

[lib]
crate-type = ["cdylib"]

[dependencies]
ioi-contract-sdk = { path = "../../../../contract-sdk" }
# REMOVED: wee_alloc = { version = "0.4.5", default-features = false }

[profile.release]
codegen-units = 1
opt-level = "z"
lto = true
panic = "abort"
strip = true
