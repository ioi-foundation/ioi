{
  "format": "ioi.program.canonical_estate.v1",
  "role": "Identifies THE ONE canonical implementation estate, declares how its evidence is stored, and states the rules a non-canonical copy must carry. Every other copy of this directory tree is a reference, never an authority.",
  "frozen_on": "2026-07-29",
  "frozen_at_commit": "7ee7acc2e43ddfc3488f560a81c7a2d33f9d34eb",
  "frozen_by": "owner-directed program-architecture repair under _archive/holds/certification-hold.v1.json",
  "estate_id": "estate://ioi/private-implementation-estate/1",
  "canonical": {
    "repo_root_realpath": "/home/heathledger/Documents/ioi/repos/ioi",
    "estate_relative_path": "internal-docs/implementation",
    "role": "canonical",
    "authority": "The single writable estate. Records, evidence, ledgers, and generated projections are authored here and nowhere else.",
    "path_is_host_state": "The absolute path is host state, not identity. Identity is estate_id plus the role marker; the path is recorded so a second writable estate on this host is detectable, not so the path becomes doctrine."
  },
  "reference_copies": {
    "rule": "A checkout that carries a copy of this tree and is not the canonical estate MUST declare itself with an estate-role.v1.json at the copy's root, naming role `reference_readonly`, the canonical estate_id, and a private staging directory OUTSIDE the canonical estate tree. A copy with no marker is INDISTINGUISHABLE from the canonical estate and fails closed. CORRECTED 2026-07-29: the copy set is DERIVED from `git worktree list`, not from a hand-listed pair of directories. The previous discovery looked in two places and found one copy; git finds FIVE, and four of them carried no marker at all.",
    "marker_file": "estate-role.v1.json",
    "marker_format": "ioi.program.estate_role.v1",
    "roles": {
      "canonical": "The one writable estate. Exactly one may exist per estate_id on a host.",
      "reference_readonly": "A read-only reference. It may be read and diffed. It may not be the source of a record, an evidence object, a ledger append, or a generated projection, and nothing here may be copied back over the canonical estate except through an explicit, reviewed promotion.",
      "private_staging": "A worktree's own scratch area for work that is not yet promoted. It is never scanned as estate content and never referenced by a record."
    },
    "worktree_contract": "A worktree gets a READ-ONLY reference plus its own private staging directory. It never gets a second writable estate.",
    "copy_discovery": {
      "derivation": "git worktree list --porcelain, plus any additional_search_roots below. Every worktree path is checked for internal-docs/implementation.",
      "why": "A hand-maintained list of places to look is a coverage claim nobody checks. On 2026-07-29 it was checking two directories while five copies existed.",
      "additional_search_roots": [
        "/home/heathledger/Documents/ioi/worktrees",
        "/home/heathledger/Documents/ioi/quarantine"
      ],
      "additional_search_roots_are_host_state": "These are recorded so a copy placed outside a git worktree is still discovered on this host. They are not identity and not doctrine."
    },
    "read_only_is_enforced_by_mode_bits": {
      "rule": "A copy declaring `reference_readonly` MUST carry no write bit on any of its files. The declaration is a promise; the mode bits are the control.",
      "why": "A writable mirror is a second canonical estate that has not admitted it yet. A tool run inside it, or a stray editor, authors into something indistinguishable from the original and nothing records that it happened.",
      "enforced_by": "internal-docs/implementation/tools/check-canonical-estate.mjs (check: reference-copy-writable)"
    },
    "staging_lives_outside": {
      "rule": "A private staging directory MUST be outside the canonical estate tree.",
      "why": "Staging held inside the estate is one boundary-file edit away from being scanned as authoritative content, and nothing about that edit would look like a promotion. The nested worktree's marker declared exactly that until 2026-07-29.",
      "enforced_by": "internal-docs/implementation/tools/check-canonical-estate.mjs (check: staging-inside-canonical-estate)"
    }
  },
  "byte_aliasing_prohibition": {
    "rule": "No file inside the canonical estate's digest scope may have a hard-link count greater than 1.",
    "why": "A hard link aliases the same inode across two working trees. Writing through the alias mutates the canonical estate with no event in the canonical checkout, no git status, and no ledger entry — the estate would change while every projection still described the old bytes.",
    "explicitly_not_doctrine": "The hard-link workaround previously used to share estate bytes between worktrees is REJECTED as doctrine. It is recorded here as a prohibited technique, not as an approved one. Sharing is by read-only reference and content address, never by aliasing bytes.",
    "enforced_by": "internal-docs/implementation/tools/check-canonical-estate.mjs (check: hardlink-alias)",
    "scope_corrected_on": "2026-07-29",
    "scope": "The declared digest scope PLUS `_archive/`. The prohibition always said 'no canonical or evidence byte may be aliased'; the enforcement covered the canonical half only, leaving the attestation ledgers, the holds, the freeze manifest and the content-addressed evidence store outside every inode check. The check now also names the ALIAS PAIR: for each discovered estate copy it looks for a file sharing an inode with a canonical file, so a report says which copy can write through where, not merely that something is aliased."
  },
  "immutable_evidence_store": {
    "root": "_archive/evidence/cas",
    "addressing": "An object is stored at _archive/evidence/cas/<sha256-of-its-bytes> and its name IS its digest. An object is written once and never rewritten; a body that must change becomes a NEW object with a new address, and the superseding relationship is recorded by the referring manifest.",
    "reference_form": "cas://sha256/<hex>",
    "precedent": "_archive/attestations/review-manifests/<sha256>.json already stores reviewed canon-review manifests by content address. This generalises that store rather than adding a second one.",
    "enforced_by": "internal-docs/implementation/tools/check-canonical-estate.mjs (check: cas-object-mismatch)",
    "nonclaim": "Content addressing makes an object immutable and citable. It does not make the object true, current, or sufficient."
  },
  "canon_baseline_binding": {
    "rule": "A canon baseline binds an EXACT GIT TREE plus an EXPLICIT DURABLE OVERLAY MANIFEST. It never binds the ambient filesystem.",
    "required_bindings": [
      "bound_git_tree: a commit and the docs/ tree sha it resolves, so the tracked subject set is reproducible from git alone.",
      "overlay_manifest: program/canon-overlay-manifest.v1.json, enumerating every canon subject that is NOT in the bound tree, with its content address and a classification from the manifest's closed vocabulary."
    ],
    "absence_rule": "MISSING IGNORED BYTES ARE NOT DELETIONS. A declared overlay member absent from disk is an overlay defect (overlay_member_absent). It is never a canon removal, never advances a baseline, and never retires an identity.",
    "retirement_rule": "Retirement is recorded in the disposition/tombstone ledger and carried into the next immutable manifest version. A frozen manifest is never edited. Silent disappearance retires nothing: a retirement still requires a tombstone, an inbound-reference proof, and a named successor or rationale — all of them recorded in program/canon-overlay-dispositions.v1.json, where they can be derived and checked.",
    "retirement_rule_corrected_on": "2026-07-29",
    "retirement_rule_superseded_text": "Retiring an overlay member requires an explicit tombstone appended to the manifest's retired_members plus a named successor disposition. Silent disappearance retires nothing.",
    "why_the_previous_rule_was_contradictory": "It directed an edit into a body this same estate FREEZES. program/canon-overlay-manifest.v1.json is frozen subject frz-0002 and a freeze discharged by editing what it froze is not a freeze, so the append rule and the freeze rule could not both be obeyed. Meanwhile two identities WERE retired, in the disposition ledger, and the manifest and the ledger therefore disagreed by construction while the rule told you to resolve it in the one place you may not write.",
    "the_frozen_empty_list_is_true": "The v1 manifest's `retired_members: []` TRUTHFULLY RECORDS THE STATE FROZEN AT THAT DIGEST. Nothing was retired when it froze, so nothing belonged in that list. Later retirement decisions do not rewrite history; they are carried into the next immutable version.",
    "successor_manifest": {
      "path": "program/canon-overlay-manifest.successor.candidate.v2.json",
      "state": "CANDIDATE, NOT ADMITTED",
      "consumed_by_no_tool": true,
      "binds": "The frozen v1 manifest by self-digest and by file digest, all 220 identities at their ORIGINAL digests, the two retired identities with their tombstones and retained bytes, and its own self-digest.",
      "reports": "218 active, 2 retired, 220 identities. A retired identity is carried, never dropped: dropping it would make a retirement indistinguishable from a subject that was never enumerated.",
      "admission_blocked_by": "_archive/holds/certification-hold.v1.json held action `canon-impact --accept (no further baseline advancement)`. Binding this successor into a canon baseline IS a baseline advance and is therefore held; until then v1 remains the manifest a baseline binds.",
      "generated_by": "internal-docs/implementation/tools/derive-successor-manifest.mjs --write --out <path outside the estate>",
      "enforced_by": "internal-docs/implementation/tools/check-successor-manifest.mjs (checks: successor-manifest-missing, successor-manifest-drift, predecessor-unbound, v1-mutated, identity-removed, identity-added, identity-digest-changed, retired-bytes-absent, tombstone-manifest-disagreement, silent-resurrection, successor-counts, successor-admitted, retirement-model-superseded)",
      "owning_record": "m0-overlay-member-custody-and-disposition-successor"
    },
    "historical_baseline": {
      "path": "generated/canon-baseline.v1.json",
      "subject_count": 990,
      "state": "PRESERVED HISTORICALLY, CONTAMINATED",
      "defect": "It was accepted over the ambient filesystem: 770 of its 990 subjects come from the git tree and 220 are host-local ignored bytes that were never committed to any ref. The acceptance ledger recorded them under a SKIP describing them as unmerged branch canon, which is false for all 220.",
      "never_rewritten": "This artifact is not edited, refreshed, or regenerated. It is the evidence of what was accepted, and the acceptance ledger entry that advanced it stands."
    },
    "successor_baseline": {
      "path": "program/canon-baseline-successor.candidate.v1.json",
      "state": "CANDIDATE, NOT ADMITTED",
      "consumed_by_no_tool": true,
      "admission_blocked_by": "_archive/holds/certification-hold.v1.json held action `canon-impact --accept (no further baseline advancement)`. Wiring this candidate into tools/canon-impact.mjs IS a baseline advance and is therefore held.",
      "owning_record": "m0-canonical-estate-and-baseline-binding-successor"
    },
    "overlay_disposition_layer": {
      "path": "program/canon-overlay-dispositions.v1.json",
      "input": "program/canon-overlay-producer-evidence.v1.json",
      "role": "The manifest ENUMERATES the overlay, the binding layer BINDS each member with a producer of record derived from the bound tree, and this layer says who is ACCOUNTABLE for each one: exactly one disposition per member from a closed set — an existing owner, a named bounded successor, retained non-claim evidence under a named custodian and an explicit retention rule, or a retirement carrying a tombstone, an inbound-reference proof, and a successor rationale.",
      "why_it_is_a_separate_layer": "AN INDEX CANNOT OWN WHAT IT ENUMERATES. The binding layer answers `is there a producer of record in the bound git tree`, and for 194 of 220 members its honest answer is no. This layer answers a different question against different evidence — the git history of the producer, its rebind chain, the commit that terminally removed it, the schema the bytes declare about themselves, and a live inbound-reference proof. The two are kept apart deliberately: this ledger and its input are listed in the binding layer's self-enumeration exclusions, so recording a disposition CANNOT change one member's producer attribution. Verified when the layer was added: 194 unattributed producers before, 194 after, zero member bindings changed.",
      "explicitly_not_attribution": "A wildcard owner; a baseline or index self-reference; directory-default ownership; and `preserved in CAS`. Each is refused BY NAME and exercised against synthetic bad input on every run. CAS membership proves byte identity and retention ONLY — not provenance quality, not claim validity, not review, not ownership, not coverage.",
      "unattributed_is_not_a_disposition": "A member for which no disposition is derivable stays unattributed, is reported individually with the reason, and remains a certification blocker. The count is expected to fall and is never engineered to zero.",
      "retirement_is_of_an_identity": "A retirement recorded here disposes of a subject identity. It deletes nothing and it removes nothing from the enumeration. THIS LEDGER IS WHERE A RETIREMENT IS RECORDED; the next immutable manifest version is where it is CARRIED. A frozen manifest is never edited.",
      "decision_source_is_recorded": "Every disposition carries `decision_source`, from the closed pair {derived, explicit_owner_ruling}. `derived` means an executable predicate over real bytes and real git facts produced it and nobody chose it; `explicit_owner_ruling` means the owner chose it, at a named path and a named digest, because no derivation was available. The mislabel is refused in BOTH directions, and an owner-ruled member carries family_rule: null and is enumerated by no family rule — a ruling stated as a predicate is one inference, not a set of individual decisions.",
      "owner_ruling_ledger": {
        "path": "_archive/attestations/owner-rulings.v1.json",
        "format": "ioi.program.owner_rulings.v1",
        "role": "The append-only record of explicit owner rulings. Each decision binds EXACTLY ONE path and EXACTLY ONE sha256, and carries a structured evidentiary restriction stating what the ruled bytes may and may not establish. A decision whose bound digest no longer matches the bytes is NOT applied; the member returns to a withheld attribution rather than inheriting a decision about different bytes.",
        "nonclaim": "A ruling records that the owner decided. It validates nothing, verifies no custody record, admits no claim the ruled artifacts make, and discharges no hold."
      },
      "generated_by": "internal-docs/implementation/tools/derive-overlay-dispositions.mjs (--scan writes the git-history input; --write [--out <staging path>] derives the ledger; --check re-derives and diffs byte-for-byte)",
      "enforced_by": "internal-docs/implementation/tools/check-overlay-dispositions.mjs (checks: ledger-missing, ledger-drift, producer-evidence-drift, binding-drift, disposition-uncovered, disposition-orphan, disposition-ambiguous, disposition-vocabulary, disposition-fields, attribution-wildcard, attribution-baseline-self-reference, attribution-directory-default, attribution-cas-membership, successor-record-absent, successor-stage-certified, successor-not-proposed, custodian-record-absent, retired-inbound-live, retired-successor-absent, retired-rebind-as-retirement, family-rule-unowned, family-rule-nondeterministic, family-rule-exception-hidden, decision-source-missing, decision-source-vocabulary, owner-ruling-reported-as-derived, derived-reported-as-owner-ruling, owner-ruling-digest-mismatch, owner-ruling-as-family-rule, owner-ruling-uncovered, owner-ruling-restriction-absent, owner-ruling-restriction-weakened, owner-ruling-ledger-drift)",
      "owning_record": "m0-overlay-member-custody-and-disposition-successor"
    },
    "overlay_binding_layer": {
      "path": "program/canon-overlay-bindings.v1.json",
      "role": "The overlay manifest ENUMERATES the host-local durable evidence a baseline must bind; this layer BINDS each enumerated member with the axes an enumeration cannot carry — content address, provenance, owner, retention/disposition.",
      "generated_by": "internal-docs/implementation/tools/bind-overlay-members.mjs --write",
      "manifest_never_edited": "program/canon-overlay-manifest.v1.json is a frozen subject. The binding layer is additive and changes not one byte of the enumeration.",
      "omission_rule": "Deduplication by digest is allowed; OMISSION IS NOT. An enumerated member with no binding, a binding with no retained object, or a binding that re-decides a classification each fail closed.",
      "enforced_by": "internal-docs/implementation/tools/check-canonical-estate.mjs (checks: overlay-bindings-missing, overlay-bindings-drift, overlay-binding-omitted, overlay-binding-digest-mismatch, overlay-binding-reclassified, overlay-binding-address, overlay-binding-unretained, overlay-binding-vocabulary, overlay-binding-unverified, overlay-binding-orphan)"
    }
  },
  "consumed_by": [
    "internal-docs/implementation/tools/check-canonical-estate.mjs",
    "internal-docs/implementation/tools/promote-staged.mjs"
  ],
  "nonclaim": "This file identifies an estate and states rules about copies, byte aliasing, evidence storage, and baseline binding. It certifies nothing, closes no stage, changes no status, and grants no coverage to any canon subject.",
  "promotion_events": {
    "rule": "PRIVATE STAGING HAS NO AUTHORITY UNTIL IT IS PROMOTED, and a promotion is a recorded, digest-bound event — never a copy command.",
    "ledger": "_archive/attestations/estate-promotions.v1.json",
    "written_by": "internal-docs/implementation/tools/promote-staged.mjs",
    "event_binds": [
      "source path (which must be OUTSIDE the canonical estate) and its digest",
      "target estate-relative path, the digest it carried before, and the digest it now carries",
      "an accountable reviewer_ref and a stated reason",
      "the resulting body retained content-addressed under _archive/evidence/cas/ BEFORE the event is appended"
    ],
    "post_promotion_drift": "On every run the checker re-digests each promoted target. Bytes that moved after their promotion with no later promotion event are reported as an unrecorded mutation of authoritative bytes.",
    "enforced_by": "internal-docs/implementation/tools/check-canonical-estate.mjs (checks: promotion-ledger-missing, promotion-event-unsourced, promotion-sequence-broken, promotion-body-unretained, promotion-result-drift, promotion-target-absent)"
  }
}
