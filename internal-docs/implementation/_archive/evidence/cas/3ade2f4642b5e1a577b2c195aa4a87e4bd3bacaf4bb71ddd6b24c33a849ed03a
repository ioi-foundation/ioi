{
  "evidence_format": "ioi.program.work_item.v1",
  "work_item_id": "m0-literal-exit-evidence-contract-successor",
  "stage_id": "M9",
  "record_role": "implementation_cut",
  "status": "evidence_ready",
  "canon_owners": [
    "docs/architecture/_meta/current-canon-defaults.md",
    "docs/architecture/_meta/source-of-truth-map.md"
  ],
  "canon_snapshot": {
    "captured_at": "2026-07-29",
    "captured_at_commit": "7ee7acc2e43ddfc3488f560a81c7a2d33f9d34eb",
    "snapshot_role": "Provenance for the canon revision this record was authored against. Drift detection is owned by generated/canon-baseline.v1.json via tools/canon-impact.mjs.",
    "aggregate_sha256": "533ee42eae62733886986ecda771f425a47f86ad4349c67095e86230f12d8421",
    "owners": [
      {
        "path": "docs/architecture/_meta/current-canon-defaults.md",
        "sha256": "8ca50a056184538edd8a00ed4fbc251ff2afe1a1cbeb840fc2535eb962254e53"
      },
      {
        "path": "docs/architecture/_meta/source-of-truth-map.md",
        "sha256": "8a8e48f7685046e0f66ccff127559c53182e92d18bb2d45df49ec930120ba60d"
      }
    ]
  },
  "contract_families": [],
  "dependency_work_item_ids": [],
  "external_gates": [],
  "aggregate_child_ids": [],
  "aggregate_child_dispositions": [],
  "aggregate_verification_binding": null,
  "implementation_actions": [],
  "consequential_effects_and_final_invokers": [],
  "applicable_pg_ids": [],
  "pg_gate_states": [],
  "product_journey_and_states": [],
  "ordered_product_operator_journey": [],
  "metrics_and_frozen_thresholds": [],
  "compatibility_and_migration": [],
  "evidence_index": {
    "literal_exit": "M0_LITERAL_EXIT_EVIDENCE_CONTRACT_SUCCESSOR_EXIT=0",
    "retained_refs": [],
    "expected_output_paths": [
      "internal-docs/implementation/evidence/M9/m0-literal-exit-evidence-contract-successor.exit.v1.txt"
    ],
    "historical_unavailable_refs": [],
    "negative_and_inconclusive_retention": true,
    "producer_independence_required": true,
    "checkout_validation": "not_applicable_until_evidence_ready",
    "historical_status_preserved": true,
    "task_exit_code_is_proof": false
  },
  "evidence_refs": [],
  "pr": null,
  "last_status_transaction": {
    "from": "proposed",
    "to": "evidence_ready",
    "at_commit": "7ee7acc2e43ddfc3488f560a81c7a2d33f9d34eb",
    "certification_result_sha256": null,
    "rule": "Derived by tools/transition.mjs from one retained certification result. No downstream artifact was hand-edited."
  },
  "required_work": [
    "Successor to the verified closure m0-literal-exit-evidence-contract, required because the contract it proved no longer reproduces: check-literal-exit-contract reports 95 errors over three retained logs, and the gate that was supposed to make the contract unavoidable was not asking the validator."
  ],
  "objective": "Re-prove the retained literal-exit CONTRACT, not the hole that let it be bypassed. The unowned defect beneath this predecessor is FIXED — tools/transition.mjs now imports refuseUnlessConformingLiteralExitLog from tools/lib/literal-exit.mjs, so the gate and the bar read the same validator. That fix closes a hole; it proves nothing about the contract. The contract's claim is that every retained stage exit log is machine-checkable, byte-bound to the artifact it certifies, and separate from narrative — and today three retained logs falsify that claim. Re-prove it end to end: one validator, one conforming shape, admission refused at the gate and reported at the bar, over the population as it actually stands. M0_LITERAL_EXIT_EVIDENCE_CONTRACT_SUCCESSOR_EXIT=0.",
  "falsifiable_claim": "Every retained stage exit log either conforms to ioi.program.literal_exit.v1 and binds its artifact at a digest the artifact currently carries, or is reported by name with the reason it does not; the gate and the bar reach that verdict through ONE validator and are shown to agree on the same inputs; and a non-conforming log cannot be admitted by the gate even when its record is otherwise complete.",
  "selected_profile": "The retained literal-exit evidence contract for the selected deployment profile: machine-checkable stage exit logs, one validator, gate and bar agreeing.",
  "private_artifacts": [
    {
      "artifact_id": "work-item-record:m0-literal-exit-evidence-contract-successor",
      "artifact_class": "private_implementation_plan_and_status_record",
      "path": "internal-docs/implementation/work-items/active/m0-literal-exit-evidence-contract-successor.v1.json",
      "product_authority": false,
      "architecture_canon": false
    },
    {
      "artifact_id": "future-exit-contract:M0_LITERAL_EXIT_EVIDENCE_CONTRACT_SUCCESSOR_EXIT=0",
      "artifact_class": "private_future_literal_exit_contract",
      "path": null,
      "product_authority": false,
      "architecture_canon": false
    }
  ],
  "current_implementation_evidence": [
    "THE UNOWNED DEFECT IS FIXED, AND THAT IS NOT THIS RECORD'S PROOF. tools/transition.mjs line 65 now imports refuseUnlessConformingLiteralExitLog from tools/lib/literal-exit.mjs and calls it before admitting an artifact as a retained literal-exit log. Before that, the gate accepted any file the record named and only the bar ever looked at its shape, which is how free-form prose was admitted as a conforming log. A closed hole is a precondition for re-proving the contract; on its own it is a diff.",
    "THE CONTRACT STILL DOES NOT REPRODUCE. tools/check-literal-exit-contract.mjs is RED with 95 errors over exactly three artifacts: 93 from evidence/M9/route-final-invoker-census-successor-after-publication-rebuild.exit.v1.txt, which is narrative prose in a log's filename; 1 from evidence/M0/m0-program-control-selected-profile-exit-proof.exit.v1.txt, whose pinned m0-exit-report.json digest the artifact no longer carries; and 1 from evidence/M0/m0-unsigned-review-anchor.exit.v1.txt, whose anchor chain grew. Three artifacts, three different reasons, one falsified claim.",
    "THE THREE ARE NOT ONE DEFECT. The prose log is a SHAPE failure and is owned by route-final-invoker-census-corrective-successor under osh-0016. The two stale digests are BINDING failures against artifacts that legitimately changed, and are owned by m0-unsigned-review-anchor-successor and by the aggregate's direct re-proof of m0-program-control-selected-profile-exit-proof. This successor owns the CONTRACT: that the validator is single, that the gate and the bar agree, and that the population is reported completely.",
    "THE WAIVED SKIP IS PART OF THE CONTRACT, NOT AN ASIDE. The bar reports 31 validated stage logs alongside a SKIP: legacy top-level evidence files predate the contract and are validated by nothing here. A contract that silently excludes a population is a contract over the population it chose. The successor must state that boundary in machine-readable form or bring the legacy files inside it.",
    "NO PREDECESSOR ARTIFACT IS REFRESHED. All three failing logs stay byte-intact. Re-pinning a digest in place to clear a bar is the exact move the certification hold forbids, and it would destroy the only evidence that the artifacts moved.",
    "DEMONSTRATION PENDING. This record is proposed; the `verified` transition that would admit it is a HELD ACTION."
  ],
  "in_scope": [
    "A conforming retained log for this successor, produced through the same validator it certifies.",
    "A demonstration that the gate (tools/transition.mjs) and the bar (tools/check-literal-exit-contract.mjs) reach the SAME verdict on the same inputs, including at least one input each refuses.",
    "Complete, by-name reporting of the current population: which retained logs conform, which do not, and for each non-conforming log which of the three failure classes it is in.",
    "An explicit, machine-readable statement of the contract's population boundary, so the legacy top-level files are either inside the contract or declared outside it by rule rather than by a prose SKIP.",
    "The distinction the contract must carry: a SHAPE failure is a malformed log; a BINDING failure is a correct log whose artifact moved. Reporting them identically is what let a prose file sit next to two honest staleness reports."
  ],
  "out_of_scope": [
    "Editing, re-pinning, or regenerating any of the three failing retained logs. They are the baseline.",
    "Proving the shape defect in the route-census prose log; that belongs to route-final-invoker-census-corrective-successor under osh-0016.",
    "Re-binding the review-epoch anchor or the m0-exit-report digest; those belong to their own successors.",
    "Re-certifying stage M0.",
    "Any claim that a conforming log is a true log. The contract is about checkability, never about correctness."
  ],
  "positive_proof": [
    "The bar reports every retained log by name with a conforming/non-conforming verdict and a failure class; the gate refuses a non-conforming log on an otherwise complete record; the gate and the bar are shown to call one validator; and this record's own retained literal appears exactly once."
  ],
  "adversarial_or_fault_proof": [
    "Predeclared refusals: a log that is valid prose and invalid KEY=VALUE; a conforming log whose ARTIFACT_SHA256 does not match the artifact on disk; a conforming log naming an artifact that does not exist; a log admitted by the gate that the bar then rejects, and the reverse, which is the divergence that made the original defect possible; a record whose declared literal_exit token does not appear in its own retained log; and a duplicated literal in one log.",
    "The retained baseline is the three failing artifacts, kept byte-intact, plus the pre-fix transition.mjs behaviour: a gate that accepted any named file, so that the difference between 'the hole is closed' and 'the contract reproduces' is itself in evidence."
  ],
  "code_anchors": [
    {
      "path": "internal-docs/implementation/tools/lib/literal-exit.mjs",
      "present_when": "current_precedent",
      "evidence_scope": "the single validator the gate and the bar must both call; its presence is not proof of this work item"
    },
    {
      "path": "internal-docs/implementation/tools/transition.mjs",
      "present_when": "current_precedent",
      "evidence_scope": "the gate, now calling that validator; the fix is this record's precondition, not its proof"
    },
    {
      "path": "internal-docs/implementation/tools/check-literal-exit-contract.mjs",
      "present_when": "current_precedent",
      "evidence_scope": "the bar, RED with 95 errors over three artifacts at the time of authoring"
    }
  ],
  "remaining_nonclaims": [
    "A conforming log is a checkable log. It is never evidence that what the log asserts is true.",
    "Closing the gate/validator hole is not re-proving the contract, and this record does not present it as one.",
    "This successor proves nothing about the three failing artifacts' own repairs; each is owned elsewhere and named here only so the population is complete.",
    "The predecessor closure is not reopened, weakened, or restated. It was proven against the revision it was proven against.",
    "Until the certification hold releases and the `verified` transition is taken, this record closes nothing and discharges no hold."
  ],
  "rollback_or_stop_rule": "Stop if the only way to make the bar green is to edit a retained predecessor log, to widen the conforming shape until prose passes, or to move the population boundary so the failing artifacts fall outside it. All three are green-by-suppression. If the contract genuinely cannot cover the legacy population, say so in the contract and report the exclusion by name on every run.",
  "exit_criteria": [
    "Every retained log carries a by-name verdict and a failure class; gate and bar demonstrably share one validator and agree on a common input set including mutual refusals; the population boundary is machine-readable; every predeclared refusal fires by name; and the retained literal appears exactly once."
  ],
  "source_provenance": "Authored 2026-07-29 under an owner-directed repair pass while _archive/holds/certification-hold.v1.json is OPEN. Ordered explicitly by the owner as a subordinate of m0-closure-successor-aggregate, with the instruction that the successor must prove the contract REPRODUCES rather than merely that the unowned defect beneath it is closed. Its predecessor is qualified by osh-0001 and osh-0011."
}
