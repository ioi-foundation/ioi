{
  "evidence_format": "ioi.program.m3_workrun_resource_isolation_contract.v1",
  "work_item_id": "m3-workrun-resource-isolation-contract",
  "proof_profile": "local process-scoped WorkRun admission with fail-closed stronger-floor refusal",
  "implementation_anchors": [
    {
      "path": "crates/services/src/agentic/runtime/kernel/runtime_goal_pursuit.rs",
      "sha256": "2943b5bf389f76ccb8df3faa091259aa47bf64e417b6eeee6105f2b1812d0c46",
      "symbols": [
        "GoalPursuitCore::admit_workrun_isolation",
        "GoalPursuitCore::preserve_workrun_isolation"
      ]
    },
    {
      "path": "crates/node/src/bin/hypervisor_daemon_routes/environment_routes.rs",
      "sha256": "d7969514e9a61b7caf49c54d0425fbd708b0b93f77a6294fda6244001c200bf7",
      "symbols": [
        "admit_workrun_isolation_contract",
        "handle_workrun_create",
        "handle_workrun_execute"
      ]
    }
  ],
  "contract_bindings": [
    {
      "contract_id": "schema://ioi/components/hypervisor/workload-isolation-requirements/v1",
      "schema_path": "docs/architecture/_meta/schemas/hypervisor-workload-isolation-requirements.v1.schema.json",
      "schema_sha256": "009f38134cc33243d781c5161604c4fd73e6136b34192646dcbe41c8a641a80f"
    },
    {
      "contract_id": "schema://ioi/components/hypervisor/workload-isolation-binding/v1",
      "schema_path": "docs/architecture/_meta/schemas/hypervisor-workload-isolation-binding.v1.schema.json",
      "schema_sha256": "7a4c878a0664ae0ec2d8d965f81ce809e02adda1aaa17e2b838646de47b32897"
    }
  ],
  "checks": [
    {
      "command": "cargo test -p ioi-services runtime_goal_pursuit --lib",
      "exit_code": 0,
      "result": "18 passed; includes immutable binding and downgrade, cross-run replay, and tamper refusals"
    },
    {
      "command": "cargo test -p ioi-node containment_tests --bin hypervisor-daemon",
      "exit_code": 0,
      "result": "20 passed; includes pre-effect local admission and missing/stronger-floor refusals"
    },
    {
      "command": "cargo fmt --all -- --check",
      "exit_code": 0
    }
  ],
  "positive_proof": [
    "The daemon assigns the WorkRun identity, recomputes the compiled-requirements hash, mints the binding reference and binding hash, and validates both registered generated contract projections before creating the WorkRun workspace.",
    "The WorkRun record persists the exact RuntimeAssignment reference, requirements commitment, immutable binding, and terminal cleanup disposition.",
    "Execute and the reusable replay, cancel, replacement, and terminal preservation seam require the exact persisted binding."
  ],
  "adversarial_or_fault_proof": [
    "Missing compiled requirements or binding inputs refuse before workspace creation.",
    "A vm_kernel requirement presented to the process-scoped local route refuses rather than falling back or recording a stronger isolation claim.",
    "Environment mismatch, cross-WorkRun replay, binding downgrade, and post-persistence tamper refuse.",
    "Legacy WorkRuns without an admitted binding cannot execute."
  ],
  "nonclaims": [
    "Contract admission and preservation do not prove escape resistance, live microVM containment, HypervisorOS, or cross-tenant suitability.",
    "The local route admits only the honest process_scoped floor; the existing microVM route remains unavailable for this host-mutating WorkRun path.",
    "This evidence closes only M3 binding propagation and grants no M9 compute-profile or product-release claim."
  ]
}
