--- a/internal-docs/implementation/ioi-target-end-state-master-implementation-guide.md
+++ b/internal-docs/implementation/ioi-target-end-state-master-implementation-guide.md
@@ -102,14 +102,13 @@
    `program-state.json` only as its derived session-orientation projection.
 5. Current code and retained evidence for runtime truth and the exact proof
    scope they actually establish.
-6. The active
-   [`Hypervisor kernel migration matrix`](../../docs/architecture/_meta/hypervisor-kernel-substrate-migration-matrix.md)
-   for the current route-family migration lane.
-7. This guide for program activation, dependencies, proof aggregation, and
+6. This guide for program activation, dependencies, proof aggregation, and
    source disposition.
-8. A specialist ledger or work package explicitly activated by this guide.
-9. Preserved plans, prompt packs, captures, comparative research, and historical
-   evidence.
+7. A subordinate implementation module explicitly activated by one stage or
+   work package in this guide. A module owns reusable detail only; it cannot
+   order work or carry live status.
+8. Archived terminal records, preserved plans, prompt packs, captures,
+   comparative research, and historical evidence.
 
 Publication date, document length, “master” in a title, or an
 `AUTHORITATIVE` label in an ignored file never outranks that order.
@@ -128,24 +127,31 @@
 6. resolve sequencing in execution horizons;
 7. update this guide only when doctrine, sequencing, an exit-proof definition,
    or source disposition actually changes through the applicable review;
-8. preserve the superseded source and its evidence.
+8. preserve the superseded source and its evidence without deletion: move the
+   full body to the approved audit/archive class, retain its digest and
+   provenance, and leave a one-screen tombstone at any stable path still
+   referenced. Preservation does not require an obsolete body to remain in the
+   active reader path.
 
 This guide may narrow a claim immediately when evidence fails. It may widen a
 claim only after owner doctrine, implementation truth, and proof agree.
 
-### 2.3 Active specialist authorities
+### 2.3 Subordinate implementation modules
 
-Three specialist documents remain active without becoming peer roadmaps:
+Subordinate implementation modules may remain active only for the exact detail
+this guide delegates. They are not sequencers, status owners, or architecture
+canon.
 
-| Document | Sole active responsibility |
+| Module | Sole subordinate responsibility |
 | --- | --- |
-| [`hypervisor-kernel-substrate-unification-master-guide.md`](../../docs/architecture/_meta/hypervisor-kernel-substrate-unification-master-guide.md) | Canonical target and terminal contract for convergence on one Rust daemon/Core execution fabric. |
-| [`hypervisor-kernel-substrate-migration-matrix.md`](../../docs/architecture/_meta/hypervisor-kernel-substrate-migration-matrix.md) | Current macro route-family ownership, remaining terminal blockers, and do-not-recreate ledger. |
-| [`canon-mechanism-hardening-action-plan.md`](./canon-mechanism-hardening-action-plan.md) | Definitions, evidence, and closure status for the 58 `PG-*` production-integration gates. |
-
-The first two are canonical implementation authorities. The hardening plan is
-an internal specialist ledger. This guide activates and aggregates their work
-but does not reproduce their detailed rows.
+| [`stage-guides/m0/runtime-trust-boundary.md`](./stage-guides/m0/runtime-trust-boundary.md) | Reusable M0 runtime trust-boundary/PEP method; dated inventories remain audit evidence. |
+| [`stage-guides/m6/product-surface-and-ux-proof.md`](./stage-guides/m6/product-surface-and-ux-proof.md) | M6 compiler and UX-proof method; no phase order or live status. |
+| [`stage-guides/m8/campaign-experiment-method.md`](./stage-guides/m8/campaign-experiment-method.md) | M8 experiment/evaluator/fault method only when the master pulls it. |
+| [`proof-gates/mechanism-gate-registry.md`](./proof-gates/mechanism-gate-registry.md) | Status-free PG definitions, applicability semantics, and proof shapes. |
+
+Archived kernel migration records are terminal evidence and own no current
+direction. Gate applicability and status live only in work-item records and
+generated program state.
 
 ## 3. Target Product And Proof Profile
 
@@ -264,14 +270,20 @@
 ```text
 work_item_id
 stage_id
+record_role: implementation_cut | aggregate_exit | private_verifier |
+             conditional_future
 status: proposed | scoped | active | evidence_ready | verified | blocked |
         superseded | rejected
 objective
 falsifiable_claim
 selected_profile
 canon_owners
+canon_snapshot
+contract_families
+private_artifacts
 current_implementation_evidence
-dependencies
+dependency_work_item_ids
+aggregate_child_ids
 in_scope
 out_of_scope
 implementation_actions
@@ -283,6 +295,8 @@
 metrics_and_frozen_thresholds
 compatibility_and_migration
 evidence_index
+code_anchors
+evidence_refs
 remaining_nonclaims
 rollback_or_stop_rule
 source_provenance
@@ -296,6 +310,11 @@
 
 - its claim can fail;
 - its owner documents and current work-item records have been read;
+- the current architecture-coverage projection maps every in-scope canonical
+  owner/contract to this stage and a bounded record, and no changed owner digest
+  remains unreviewed;
+- every dependency is another work-item ID or an explicitly approved external
+  gate; prose placeholders do not form the dependency graph;
 - its selected deployment profile and nonclaims are explicit;
 - upstream contracts are verified or explicitly simulated;
 - metrics and thresholds are frozen before observation;
@@ -377,15 +396,17 @@
 
 ### 6.1 Runtime-convergence rail
 
-The canonical kernel guide and migration matrix own this rail. A macro cut is
-valid only when it:
+Current daemon, Agentgres, wallet, contract, and domain owners own their
+respective runtime facts. This master owns rail ordering; no archived guide or
+matrix owns current direction. A macro cut is valid only when it:
 
 1. replaces a fail-closed or split JS surface with a positive Rust daemon API;
 2. moves the selected route family's admission, authority, receipt,
    Agentgres commit, projection, replay, and protocol shape together;
 3. deletes or demotes the old authoring path in the same cut;
 4. adds positive and negative conformance;
-5. updates the migration matrix once at the macro boundary.
+5. regenerates the private route/final-invoker and Hypervisor-surface coverage
+   projections once at the macro boundary.
 
 Green current-tier conformance is necessary but does not prove terminal
 ownership. M9 requires terminal ownership for every route used by the selected
@@ -393,8 +414,11 @@
 
 ### 6.2 Production-gate rail
 
-The hardening plan remains the sole definition and closure ledger for the 58
-`PG-*` gates. This guide records only applicability:
+The status-free
+[`proof-gates/mechanism-gate-registry.md`](./proof-gates/mechanism-gate-registry.md)
+owns reusable definitions and proof shapes for the 58 `PG-*` gates. Owning
+work-item records carry applicability and closure status; generated program
+state projects it:
 
 - `required_now`: blocks the selected stage or release profile;
 - `conditional`: blocks only when the named posture is selected;
@@ -452,6 +476,12 @@
 - the selected profile, thresholds, nonclaims, and rollback rules are frozen;
 - no ignored guide still functions as a competing sequencer.
 
+The M0 exit is owned by one aggregate work-item record joining M0.1–M0.9, all
+existing and approved M0 records, selected PG applicability, the exact runtime-
+trust/route/final-invoker censuses, frozen profile/baselines/nonclaims, source
+dispositions, and the single-sequencer check. `M0_EXIT=0` closes no
+architecture or production capability.
+
 Nonclaim: M0 does not close any architecture or production capability.
 
 ### M1 — Bounded-System Constitutional Core
@@ -500,6 +530,10 @@
 - the package remains reusable and contains no live lease, credential, gateway,
   run, node, or System identity.
 
+The M1 exit is owned by one aggregate work-item record joining M1.1–M1.7 and
+all protected-transition children, including migration/succession, residual
+disposition, dissolution, and local-only enrollment.
+
 Pulled product surfaces: Studio package/genesis composition, Governance
 preview, Packages lifecycle, provisional System detail.
 
@@ -540,6 +574,11 @@
 - `M2.10` make activation a forward-only compare-and-swap and persist resource
   cleanup obligations across parent deletion, provider loss, rollback,
   supersession, and ambiguous effects.
+- own `project-discovery-startup-and-session-chain` and
+  `storage-profile-repair-and-availability` as M2 mechanism records; later
+  stages may aggregate their evidence but may not redefine their truth;
+- require the existing M2 selected-profile aggregate to join both new M2
+  records; neither may remain outside its child graph.
 
 Exit proof:
 
@@ -588,6 +627,13 @@
   archive-only replay at each owner's legal mutation boundary;
 - `M3.8` preserve negative, invalid, inconclusive, superseded, and challenged
   outputs.
+- own `production-information-flow-and-declassification`,
+  `production-receipt-checkpoint-and-offline-proof`, and
+  `agentgres-production-readiness-and-branch-effects` as M3 mechanism records.
+  M9 must aggregate their selected-profile evidence before the first sovereign-
+  product claim;
+- require the existing M3 direct-path/exit aggregate to join all three new M3
+  records while retaining the direct-path differential.
 
 Exit proof:
 
@@ -725,6 +771,11 @@
   System-interface packages and projections; neither one UI instance nor
   multiple UI replicas becomes the System, its runtime truth, or its
   failover/coordination layer.
+- own `sdk-cli-adk-odk-builder-journey` for supported interface and maturity-
+  labeled builder parity; WP-DOCS supplies runnable documentation but does not
+  become a second sequence;
+- require the existing M6 aggregate to join the builder record; runnable
+  interface evidence does not grant client-side authority.
 
 Exit proof:
 
@@ -739,6 +790,25 @@
   recovery, and completed states;
 - product-usability thresholds in the selected cohort are frozen and met.
 
+The M6 exit requires separate bounded records for the normalized compiler;
+source/serve convergence; production fixture-fallback retirement; shell/alias
+migration and accessibility; Systems and Work; consequential-action authority/
+receipt convergence; current-source surface disposition; and honest
+registration/routing/loading/empty/denied/unavailable/degraded states for every
+pulled core/owner/substrate surface. It also requires a closed projection from
+each admitted surface to the exact later-stage operational-journey owner listed
+in the approved work-item map.
+
+Route presence, registration, reference capture, pixel parity, or an M6 shell-
+state exit cannot establish operational depth. Preserve the existing nonclaim:
+operational depth for every registered surface is proved only by its applicable
+later-stage owner record and aggregate.
+
+M6 owns the operational-journey records for Home, Systems, Projects,
+Applications, Work, Automations, Packages, and Developer Workspace because
+their selected backend prerequisites are M1–M5. Its aggregate joins those eight
+records.
+
 Pulled product surfaces: all five core workspaces, twelve owner applications,
 two substrate applications, conditional Embodied Systems registration, and
 extension/tool projections.
@@ -774,6 +844,8 @@
 - `M7.8` render mapping uncertainty and disputes without pretending one global
   ontology exists.
 
+M7 owns the Studio, Ontology, and Data operational-journey records.
+
 Exit proof:
 
 - the same signed package or recipe cannot silently execute against a later
@@ -784,6 +856,9 @@
   authority, truth, or acceptance;
 - contradictory and uncertain assertions remain inspectable.
 
+The M7 aggregate joins the Studio, Ontology, and Data literal exits after the
+semantic/data mechanism exits.
+
 Pulled product surfaces: Ontology, Data, Studio, Provenance, Domain/extension
 interfaces, Developer Console federation configuration.
 
@@ -823,6 +898,18 @@
   epoch, and emit only a target-owner UpgradeProposal;
 - `M8.9` preserve the direct one-shot proposal path for work that does not need
   a Campaign.
+- own `foundry-evaluations-production-loop`,
+  `worker-training-and-local-marketplace-supply`,
+  `managed-billing-work-credits-and-supplier-reconciliation`, and
+  `marketplace-neutral-routing-contribution-accounting` as reusable local/
+  managed mechanisms. These record IDs contain no federation or connected-
+  service work;
+- require the M8 aggregate to join all four distinct literal exits. Local
+  worker supply depends on the M5 acceptance/challenge boundary and billing;
+  neutrality depends on the local-worker and billing exits. Neither depends on
+  the M12 dispute kernel, preventing an M8↔M12 cycle.
+
+M8 owns the Evaluations, Improvement, and Foundry operational-journey records.
 
 Exit proof:
 
@@ -835,6 +922,10 @@
 - all promotion, activation, rollback, recall, and kill decisions cross the
   target owner's ordinary governance path.
 
+The M8 aggregate joins the Evaluations, Improvement, and Foundry literal exits;
+shell presence, suite declaration, proposal cards, or run plans are not
+execution proof.
+
 Pulled product surfaces: Governance Enterprise Learning Boundary, Foundry,
 Evaluations, Improvement, Models/routes, Provenance, Packages.
 
@@ -891,6 +982,19 @@
   cleanup reconciliation, archive, uninstall-without-wipe, and retirement
   through the product;
 - `M9.9` publish only the claim level supported by the retained evidence.
+- own `platform-operability-observability-and-incidents` and
+  `hypervisoros-ctee-task-capsule-attestation` for the selected sovereign
+  profile, and aggregate the selected M3 information-flow/receipt/Agentgres
+  mechanisms. This does not establish multi-node continuity.
+
+M9 owns the Governance, Provenance, Developer Console, and Environments
+operational-journey records for the selected sovereign/managed profile. Its
+aggregate joins their literal exits without inferring federation or multi-node
+continuity. M9.1 is owned by an explicit compact-versus-advanced identical-
+object/hash parity record. The stage exit is owned by one aggregate record
+joining the complete 21-step journey, sovereign lane, optional managed overlay,
+Authority Gateway coverage, lifecycle/offline evidence, release supply chain,
+applicable PG gates, metrics, stop rules, and permitted claim.
 
 #### M9 Authority Gateway attach proof
 
@@ -939,6 +1043,11 @@
 enforcement point, applicable `AuthorityGrant`/`CapabilityLease` crossing,
 final invoker, and receipt spine.
 
+Use daemon doctrine for the IOI Authority Gateway and the current HypervisorOS/
+daemon contract ownership for `EnforcementCoverageDeclaration`; keep any route
+census, comparison manifest, or retained exit log explicitly private and non-
+canonical.
+
 For every selected action class, the retained
 `EnforcementCoverageDeclaration` assesses six capability facts independently:
 
@@ -1014,6 +1123,8 @@
 - `M10.7` expose desired/observed topology, root, epoch/fence, degraded state,
   recovery, and continuity receipts.
 
+M10 owns the Operations operational-journey record.
+
 Exit proof:
 
 - one stable `system_id`, constitution, owner set, and finality profile survive;
@@ -1022,6 +1133,8 @@
   consensus or authority expansion;
 - automated failover remains unclaimed unless separately proven.
 
+The M10 continuity aggregate joins the Operations literal exit.
+
 Pulled product surfaces: Systems, Operations, Environments, Provenance.
 
 Nonclaim: useful distributed work or sovereign federation.
@@ -1068,6 +1181,9 @@
   partition, reservation, stale allocation, takeover, and ambiguous-effect
   faults.
 
+M11 conditionally owns the Embodied Systems non-live operational-journey
+record only when its registration is pulled.
+
 Exit proof:
 
 - complementary roles complete useful work across nodes under unchanged
@@ -1079,6 +1195,9 @@
   control veto or emergency stop;
 - simulation/SIL/HIL/shadow evidence remains labeled at its actual stage.
 
+The M11 aggregate may join only the Embodied Systems non-live literal exit;
+live actuator/E1+ promotion remains a separately approved conditional future.
+
 Pulled product surfaces: Systems, Work, Operations, Provenance, Foundry, and
 conditional Embodied Systems.
 
@@ -1111,7 +1230,14 @@
 - `M12.7` implement hosted and federated admission modes with explicit ordering,
   merge, conflict, adjudication, and failover behavior;
 - `M12.8` preserve independent challenges, acceptance, contribution lineage,
-  dispute state, revocation, and portable participant exit.
+  dispute state, revocation, and portable participant exit;
+- own `dispute-adjudication-remedy-kernel` for AIIP and cross-rail admission/
+  appeal/remedy binding after the M5 acceptance/challenge and M8 billing/
+  neutrality exits. M13–M14 may aggregate its evidence but cannot settle
+  before its literal exit;
+- require the existing M12 selected-profile aggregate to join the dispute-
+  kernel record and prevent it from emitting its literal exit while that child
+  remains open.
 
 Exit proof:
 
@@ -1162,6 +1288,9 @@
 - no proof step requires shared runtime, database, administrator, or continued
   hosted trust.
 
+The M13 exit requires an explicit aggregate exit record distinct from the
+trial records it joins.
+
 Pulled product: invited cross-organization and Network/Open Goal Space modes,
 aiagent/sas participation surfaces, cross-domain Governance and Provenance.
 
@@ -1191,7 +1320,19 @@
 - `M14.5` authorize a sovereign L1 only when neutral ordering/security,
   performance, governance, or economics beats the compatible alternative;
 - `M14.6` keep Work Credits, provider tokens, cash, worker payouts, bonds, and
-  any native asset distinct.
+  any native asset distinct;
+- own `connected-worker-capability-supply-and-hiring`,
+  `decentralized-profile-admission-and-exit`, and
+  `ecosystem-assurance-and-public-claim-estate` as M14 records. The connected-
+  worker record owns external digital-worker discovery, capability hiring,
+  lifecycle, and portable exit only;
+- keep `m14-service-family-owner-contract-and-product-surfaces` as the sole
+  sas service registry/order/delivery/acceptance/payment/service-dispute-
+  binding/suspension/exit journey owner, while the M12 dispute kernel remains
+  the sole adjudication/appeal/remedy mechanism owner. These records depend on
+  the distinct M8 billing, local-worker, and neutrality exits and the M12
+  dispute exit; M14 does not co-own or append scope to those earlier record
+  IDs. Preserve the valid no-L1 branch and all demand/mainnet nonclaims.
 
 Exit proof:
 
@@ -1205,6 +1346,9 @@
 - exit, dispute, and failure paths work before mainnet or native-asset
   commitment.
 
+The M14 exit requires an explicit aggregate exit record distinct from the
+demand, service, and L1-decision records it joins.
+
 Nonclaims: compulsory settlement, token demand from L0 adoption, generic model-
 call currency, or one token per worker, GoalRun, room, node, or DAS.
 
@@ -1217,7 +1361,9 @@
 
 Stages: M0–M14, with selected-route terminality required by M9.
 
-Owner: canonical kernel master guide and migration matrix.
+Owners: current daemon, Agentgres, wallet, contract, connector/tool,
+Hypervisor-surface, and applicable domain-owner canon. This master alone orders
+their runtime-convergence work.
 
 Retained obligations:
 
@@ -1246,8 +1392,8 @@
   not return;
 - macro cuts retire complete authority boundaries, not one helper at a time.
 
-Terminal evidence: the migration matrix's terminal conditions and full
-conformance, the exact method-inventory audit verifier, extraction-specific
+Terminal evidence: the current repository's full applicable conformance
+aggregate, the exact method-inventory audit verifier, extraction-specific
 negative tests proving the single-gateway property, and stage-specific live
 probes for every selected effect family.
 
@@ -1463,6 +1609,11 @@
 join through bounded adapters; IOI does not require HypervisorOS on every
 servo, sensor, controller, drone, robot, or certified local safety loop.
 
+`live-embodied-promotion` remains a conditional-future record outside the
+active M0–M14 closure path. It may be assigned and activated only by a later
+explicit sequencer amendment after the M11 non-live exit; M11 evidence cannot
+prove live physical action.
+
 ### WP-COOP — Conditional sovereign cooperation
 
 Stages: contracts in M12, proof in M13.
@@ -1499,6 +1650,23 @@
 than copying it. A documentation site, screenshot, or static demo never
 upgrades implementation status.
 
+### 8.12 Architecture coverage disposition
+
+Every build-relevant canonical owner/contract must resolve to exactly one of:
+
+1. a named M0–M14 requirement and owning work-item record;
+2. a named work package with one owning stage and activation condition; or
+3. a conditional-future record whose activation requires a later explicit
+   sequencer amendment.
+
+Information-flow enforcement, production receipt proofs, environment
+construction, model/provider rights, managed economics/disputes,
+Foundry/Evaluations, worker/service supply, platform operability,
+private/measured substrate, Agentgres production breadth, ecosystem
+assurance/public claims, storage breadth, and live embodied promotion may not
+remain as unassigned prose. Planning them does not activate them or widen
+M9–M14 claims.
+
 ## 9. Application And UX Pull Map
 
 Application work is contract-pulled. The canonical object owner must exist
@@ -1698,8 +1866,10 @@
 npm run hypervisor-conformance:negative
 ```
 
-Use the kernel migration matrix's full `hypervisor-conformance` bar for macro
-runtime cuts. Use focused Rust, TypeScript, schema, SDK, CLI, browser,
+Use the current repository `hypervisor-conformance` aggregate for macro runtime
+cuts and record the exact selected command set in the owning work item; the
+archived kernel matrix is historical evidence only. Use focused Rust,
+TypeScript, schema, SDK, CLI, browser,
 usability, chaos, provider-exit, interoperability, and hardware tests when
 their profiles are selected. A green command proves only its actual scope.
 
@@ -1713,8 +1883,11 @@
    code anchors, retained evidence, exit lines, and nonclaims;
 4. refresh the machine-verified `canon-to-code-delta.md` projection when its
    declared owner, target, code-anchor, or crossing facts changed;
-5. update the kernel migration matrix if a macro route-family boundary changed;
-6. update the hardening ledger if a `PG-*` gate actually closed;
+5. regenerate the private route/final-invoker and Hypervisor-surface coverage
+   projections if a macro route-family boundary changed;
+6. update the owning work-item evidence and regenerate program state if a
+   `PG-*` gate actually closed; the status-free mechanism-gate registry changes
+   only when a gate definition or proof shape changes;
 7. update this guide only if approved sequencing, stage doctrine, exit-proof
    definition, or source disposition changed; never add a live status story;
 8. narrow product copy and public documentation to the resulting claim, then
@@ -1724,6 +1897,9 @@
    `npm run generate:program-state` and pass
    `node internal-docs/implementation/check-program-state.mjs` so the local
    projection never goes stale.
+10. regenerate and validate `generated/architecture-coverage.v1.json`; a canon
+    change with no implementation disposition blocks admission but changes no
+    existing status by itself.
 
 Do not mark an internal plan complete as a substitute for those updates.
 
@@ -1807,31 +1983,37 @@
 | `compatibility_pointer` | Stable path retained to route old links. |
 | `rejected_conflict` | Conflicting target/owner is not executable; useful evidence remains. |
 
-### 14.1 `internal-docs/implementation/` — complete pre-consolidation inventory
+This section owns source-disposition policy and approvals. Its status-free
+machine attachment, [`source-dispositions.v1.json`](./source-dispositions.v1.json),
+owns each exact approved path/role/destination fact under that delegation. The
+generated private source manifest deterministically joins that registry with
+every current path, digest, and inbound-link census; it owns no disposition.
+Archives, projections, and tombstones schedule nothing.
+
+### 14.1 `internal-docs/implementation/` — approved active roles and generated source manifest
+
+[`generated/source-manifest.v1.json`](./generated/source-manifest.v1.json)
+projects the complete path/digest/class/disposition inventory from
+`source-dispositions.v1.json` and the current filesystem/link census. The
+compact table below defines active-role policy and exceptions; the attachment
+is the exact per-path registry, and the generated manifest is never an approval
+source.
 
-The inventory below covers all twelve files that existed in this directory
-before this master and README were created.
-
-| Preserved source | Disposition | Master mapping | Retained value / conflict resolution |
-| --- | --- | --- | --- |
-| [`bounded-recursive-improvement-campaign-discovery-plan.md`](./bounded-recursive-improvement-campaign-discovery-plan.md) | `conditional_satellite` | M8, WP-IMPROVE | Statistical, evaluator-integrity, exposure, order, fault, and experiment detail retained for M8. Canon owns Campaign meaning; this source's phase list does not execute independently. |
-| [`canon-mechanism-hardening-action-plan.md`](./canon-mechanism-hardening-action-plan.md) | `active_satellite` | all stages, `R-OPS` | Sole detailed 58-gate definition/closure ledger and mechanism evidence. This guide never duplicates or closes its gates. |
-| [`canon-sota-improvement-review.md`](./canon-sota-improvement-review.md) | `historical_evidence` | M0, all rails | Dated 2026-07-16 advisory/research provenance. Adopted lessons are in canon, rails, and work packages; recommendations do not schedule work, and the comparative goal prompt owns the reusable method. |
-| [`hypervisor-bounded-das-application-taxonomy-winning-state-plan.md`](./hypervisor-bounded-das-application-taxonomy-winning-state-plan.md) | `active_satellite` | M6, M9, WP-UX | Detailed taxonomy migration, `UX-00`, seed coverage, pressure tests, and usability measures. Canon controls current names/owners. |
-| [`hypervisor-model-mount-rust-consolidation-and-deadcode-retirement.md`](./hypervisor-model-mount-rust-consolidation-and-deadcode-retirement.md) | `absorbed_complete` | WP-RUNTIME | Completed JS-daemon/model-mount retirement checkpoint. Remaining work belongs to the canonical migration matrix; do not replay its phases. |
-| [`ioi-design-system-portable-package-plan.md`](./ioi-design-system-portable-package-plan.md) | `absorbed_open` | M6, M9, WP-UX | Track 1 is retained as package-adoption history, not current implementation truth. Package presence and consumer adoption require an owning work-item record and committed anchors. Broader-consumer and ESM-native obligations are conditional. |
-| [`ioi-undeniable-product-proof-implementation-guide.md`](./ioi-undeniable-product-proof-implementation-guide.md) | `absorbed_open` | M0–M14, especially M9 | First-proof profile, goal-pass discipline, repair obligations, scorecard, release ladder, and stop rules are consolidated here; its discovery/canon-feedback method now lives in the comparative goal prompt. Its old dependency graph is superseded. |
-| [`low-level-implementation-milestones.md`](./low-level-implementation-milestones.md) | `compatibility_pointer` | this guide | Stable historical link target; now routes sequencing here rather than to the stale refine guide. |
-| [`refine-architecture.md`](./refine-architecture.md) | `historical_evidence` | M0, M3, M6, M9 | June-era architecture pressure audit. Later canon resolved most owner gaps; stale Missions, old app taxonomy, old paths, and its phase order are not executable. Path stays for ADR/test backlinks. |
-| [`runtime-action-schema.json`](./runtime-action-schema.json) | `reference_guardrail` | `R-CONTRACT` | Byte-identical mirror of the canonical schema at inventory time. Canonical schema is the only owner; this copy is not a plan. |
-| [`runtime-module-map.md`](./runtime-module-map.md) | `rejected_conflict` | WP-RUNTIME | Names deleted `packages/runtime-daemon` and `ioi-runtime-bridge.rs`; do not use as current layout until refreshed against Rust daemon reality. |
-| [`runtime-package-boundaries.md`](./runtime-package-boundaries.md) | `reference_guardrail` | WP-RUNTIME | Ownership checklist remains useful; paths/taxonomy require refresh before implementation use. |
-
-### 14.1A Post-consolidation activated specialist guides
-
-| Preserved source | Disposition | Master mapping | Retained value / conflict resolution |
+| Approved active source | Disposition | Master mapping | Retained value / conflict resolution |
 | --- | --- | --- | --- |
-| [`runtime-kernel-service-trust-boundary-audit.md`](./runtime-kernel-service-trust-boundary-audit.md) | `active_satellite` | M0.9, WP-RUNTIME, M9 | Complete method-by-method classification of the current Rust aggregation facade. Its inventory verifier is binding for coverage, but its proposed extractions activate only as master-pulled owner cuts and may not weaken the single authority/admission/final-invoker/receipt gateway. |
+| [`source-dispositions.v1.json`](./source-dispositions.v1.json) | `source_registry` | §14 | Status-free exact per-path role/destination registry approved under this section; owns no order or status. |
+| [`stage-guides/m0/runtime-trust-boundary.md`](./stage-guides/m0/runtime-trust-boundary.md) | `active_satellite` | M0.9, WP-RUNTIME, M9 | Reusable trust-boundary/PEP method only; dated census is audit evidence. |
+| [`stage-guides/m6/product-surface-and-ux-proof.md`](./stage-guides/m6/product-surface-and-ux-proof.md) | `active_satellite` | M6, WP-UX | Compiler and UX-proof method only; no phase order/status. |
+| [`stage-guides/m8/campaign-experiment-method.md`](./stage-guides/m8/campaign-experiment-method.md) | `conditional_satellite` | M8, WP-IMPROVE | Experiment/evaluator/fault method only; no independent activation. |
+| [`proof-gates/mechanism-gate-registry.md`](./proof-gates/mechanism-gate-registry.md) | `active_satellite` | all stages, `R-OPS` | Status-free PG definitions and proof shapes only. |
+| [`check-program-state.mjs`](./check-program-state.mjs) | `compatibility_pointer` | M0, §13.3 | Stable wrapper for the master-required validation command. |
+| [`low-level-implementation-milestones.md`](./low-level-implementation-milestones.md) | `compatibility_pointer` | this guide | Stable sequencing pointer. |
+
+All prior bodies, audits, generated mirrors, logs, reconciliation transactions,
+and work-item paths retain their exact approved role/destination in
+`source-dispositions.v1.json`. `generated/source-manifest.v1.json` records and
+verifies their SHA-256 values, inbound links, and projected dispositions; their
+archive/audit/pointer classes schedule nothing.
 
 ### 14.2 `.internal/plans/` — complete current inventory
 
@@ -1888,8 +2070,8 @@
 | [`canon-to-code-delta.md`](../../docs/architecture/_meta/canon-to-code-delta.md) | Machine-verified authority for stable object-to-owner/code-anchor crossing identities and the deferred contract-pulled UX backlog; it owns no cut status and is validated with `npm run check:canon-to-code-delta`. |
 | [`implementation-matrix.md`](../../docs/architecture/_meta/implementation-matrix.md) | Owns concept-to-owner and durable-form doctrine; it carries no work queue or cut-status pointer. |
 | [`work-items/`](./work-items/README.md) | Ignored, machine-local durable cut-status records; validate with `npm run check:work-items`. They are implementation workflow, not architecture canon. |
-| [`hypervisor-kernel-substrate-unification-master-guide.md`](../../docs/architecture/_meta/hypervisor-kernel-substrate-unification-master-guide.md) | Owns the canonical runtime convergence target and terminal contract. |
-| [`hypervisor-kernel-substrate-migration-matrix.md`](../../docs/architecture/_meta/hypervisor-kernel-substrate-migration-matrix.md) | Owns current macro migration status. |
+| [`hypervisor-kernel-substrate-unification-master-guide.md`](../../docs/architecture/_meta/hypervisor-kernel-substrate-unification-master-guide.md) | Archived terminal convergence record; useful evidence, not a current owner or sequencer. |
+| [`hypervisor-kernel-substrate-migration-matrix.md`](../../docs/architecture/_meta/hypervisor-kernel-substrate-migration-matrix.md) | Archived terminal migration record; useful evidence, not current status. |
 | [`wallet-protocol-sdk-packaging-plan.md`](../../docs/architecture/_archive/specs/wallet-protocol-sdk-packaging-plan.md) | Archived packaging detail; conditional work is pulled by WP-WALLET and current wallet owners. |
 
 ### 14.6 Consolidation control files
@@ -1909,7 +2091,7 @@
 | The autonomous-systems suite says `AUTHORITATIVE` and has hundreds of inherited backlinks. | Its front door is reclassified as historical UX/product-discovery evidence and routes here. |
 | Old application surface indexes say to begin with an archived graft master. | Implementation begins here; graft material is evidence reached through WP-UX/M6. |
 | `.internal/plans` files call themselves canonical, living, ready, or master guides. | Their exact dispositions are in section 14.2; none is a peer sequencer. |
-| `runtime-module-map.md` names deleted JS-daemon/bridge roots. | It is `rejected_conflict` until refreshed; current Rust daemon and migration matrix win. |
+| `runtime-module-map.md` names deleted JS-daemon/bridge roots. | It is `rejected_conflict` until refreshed; current Rust daemon owners, repository layout, and conformance aggregate win. The archived migration matrix is evidence only. |
 | The `RuntimeKernelService` method count is treated as proof of a monolithic or over-trusted kernel. | Size is only an inventory signal. The method audit and extraction-specific negative proofs decide whether each method belongs in the trusted core, an owner service, a projection boundary, or retirement, while preserving one authority and final-invoker path. |
 | FQF plan proposes a second canonical state/projection fabric. | Agentgres, domain owners, and MemorySpace retain their canonical responsibilities; only wiki UX/promotion lessons survive. |
 | Old UX plans use Missions and a pre-refactor rail/application taxonomy. | Generic Mission is retired; current Systems/Work and owner/substrate application taxonomy win. |
