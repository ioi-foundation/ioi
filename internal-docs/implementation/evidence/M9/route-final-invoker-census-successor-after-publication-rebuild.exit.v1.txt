ROUTE_FINAL_INVOKER_CENSUS_SUCCESSOR_EXIT=0

Work item : route-final-invoker-census-successor-after-publication-rebuild (M9)
Branch    : safety/provider-bars-and-census (base master 98d4f2281)
Date      : 2026-07-29

WHY THIS RECORD EXISTED
  docs/evidence/m0-program-control/effect-census.json named
  lifecycle_routes.rs#handle_scm_publish four times and its replacement zero times;
  discovery found 1576 identities against a committed lock of 1572;
  tools/check-route-census-maintenance.mjs reported
  "census, lock, and anchors do not cohere".

BYPASS / RETIREMENT DIFF — BEFORE (literal, tools/check-route-census-maintenance.mjs --write)
  CENSUS_MAINT_BEFORE_EXIT=1
  [ERROR] currency: scripts/m0-program-control.mjs --check FAILED; census, lock, and anchors do not cohere
  census_entry_count 1572 / lock_entry_count 1572 / bypass_candidates [] / unretired_ghosts []
  The census-vs-lock diff was 0/0 because census AND lock were stale TOGETHER. The live
  drift was discovery-vs-lock, recorded below.

DISCOVERY-vs-LOCK DIFF — BEFORE (the drift the census re-entry had to disposition)
  discovered 1576, reviewed lock 1572
  ADMITTED (in discovery, absent from the lock)  = 4
    http:hypervisor-daemon:GET  /v1/hypervisor/scm-destination-bindings
    http:hypervisor-daemon:GET  /v1/hypervisor/scm-publication-effects
    http:hypervisor-daemon:POST /v1/hypervisor/scm-destination-bindings
    http:hypervisor-daemon:POST /v1/hypervisor/scm-publication-proposals
  RETIRED  (in the lock, absent from discovery) = 0
  The stop rule ("stop if coherence can only be reached by deleting a census entry without
  recording its retirement through the bypass diff") did NOT fire: no census entry had to be
  deleted, because no route identity was retired.

REBIND IS NOT RETIREMENT
  PR #127/#128 deleted lifecycle_routes.rs#handle_scm_publish, but the ROUTE identity
  http:hypervisor-daemon:POST /v1/hypervisor/environments/:id/scm/publish SURVIVES. Its
  handler, handler source file, and final-invoker locator re-bind onto
  crates/node/src/bin/hypervisor_daemon_routes/scm_publication_routes.rs#handle_scm_publish.
  The two kinds are mechanically distinct and cannot be confused:
    - a RETIREMENT removes the identity from discovery and surfaces as an
      "unretired ghost" ERROR in tools/check-route-census-maintenance.mjs;
    - a REBIND keeps the identity and surfaces as a "stale handler anchor" /
      "stale or mismatched handler_source_file" ERROR in scripts/m0-program-control.mjs --check.
  A formatting pass produces only the second kind and can therefore never read as a retirement.

CENSUS DELTA
  admitted            4  (named above; each reviewed against its own handler source)
  retired             0
  locator rebound  1327  (registration/handler/symbol/final-invoker locators moved)
  unchanged         245  (byte-identical to the immutable 2026-07-18 branch-point baseline)
  judgment re-reviews 6 entries, EVERY ONE a claim reduction or relocation:
    POST /v1/hypervisor/exec, POST /v1/hypervisor/terminals,
    POST /v1/hypervisor/workruns/:id/execute, POST /v1/hypervisor/environments/:id/:action
      -> durable evidence and policy/revocation gate symbols drop to
         not_identified / not_established_at_final_invoker, because containment withdrew the
         host-execution fallback those symbols were observed on
    POST /v1/hypervisor/maintenance/idle-sweep -> candidate leaf re-binds within environment_routes.rs
    POST /v1/hypervisor/environments/:id/scm/publish -> re-binds onto the rebuilt publication
         handler and gains the durability symbols that handler actually observes
  terminal_claim_count 0, unresolved_placeholder_count 0 (unchanged; nothing was promoted).

DISCOVERY FAIL-CLOSED FINDING (fixed in this cut)
  Fresh discovery REFUSED to close four handlers: environment_routes.rs and binding_routes.rs
  now call admit_isolated_execution / admit_cache_scope / admit_cache_path, which live in
  crates/services/.../kernel/emergency_containment.rs, outside the daemon route function index.
  They are name-matched as effect-relevant, so discovery failed closed with
  effect_reachability_error. All three are pure total functions with no I/O, so they are
  classified in RUST_EXTERNAL_PURE_CALLS with that argument written at the definition site.

ANCHOR SEQUENCE
  sequence 23  epoch_id route-final-invoker-census-successor-after-publication-rebuild-review-2026-07-29
               reviewer_id claude-opus-5-publication-rebuild-census-successor
               authorship_binding self_declared_unsigned
               total_reviewed_entry_count 1576
               latest_epoch_reviewed_entry_count 1331
               predecessor: sequence 22 proof-infra-chain-resume-census-reentry-review-2026-07-28
  Epochs after the re-entry: 2026-07-18 baseline 245 + 2026-07-29 successor 1331 = 1576.

BYPASS / RETIREMENT DIFF — AFTER (literal)
  CENSUS_MAINT_AFTER_EXIT=0
  check-route-census-maintenance: PASS (0 error, 0 warn, 1 skip)
  currency_check_pass true / census_entry_count 1576 / lock_entry_count 1576
  bypass_candidates [] / unretired_ghosts [] / consequential_without_invoker_or_blocker 0

GATES (literal)
  node scripts/m0-program-control.mjs --check      exit 0
    "M0 supplied-snapshot check passed: 1576 entries, exit verified,
     fingerprint 3f129042da5e14dda8c97156b0a46756a9f93dfb78c9025d0035e8415b4cbc45;
     currentness not established."
  npm run check:m0-program-control                 exit 0
  npm run check:pre-next-leg                       PNL_EXIT=0, 1..60 pass 60 fail 0
  node internal-docs/implementation/tools/check-program.mjs   exit 0, PASS (0 error)

REMAINING NON-CLAIMS
  A passing census proves closure and coherence, never that any final-invoker claim is
  order-proven. Restoring coherence is not a claim that the publication plane is correct.
