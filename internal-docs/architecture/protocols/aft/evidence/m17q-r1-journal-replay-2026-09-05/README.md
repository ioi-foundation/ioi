# Typed member transitions and journal replay — 2026-09-05

The existing production member now stages candidate insertion, preparation-attempt reservation and accepted-head advancement through one typed transition function. Its live admission checks and final expiry fence remain required; typed deltas have no constructor for online grants. The storage backend remains authenticated full snapshots (member schema 6, handoff schema 3).

Before changing production staging, four tests compared typed journal replay to the preceding snapshot implementation. They exercised three consecutive slots in both authority modes, two preparation reservations per slot, accepted-head advancement from actual local query grants, and journal reopening after every slot. Separate tests cover exact generation/scope, retained candidate requirements, duplicate insertion, counter sequence, historical reservation refusal, owned conflict preservation and typed semantic refusal before pending-anchor advancement. The preceding implementation and source hashes are retained under `before-production-staging`.

After the refactor, production staging and replay share the transition function. This is regression coverage, not independent implementation evidence or a transition refinement proof. Removed-retained-candidate and removed-counter-sequence controls each fail by assertion; source is restored afterward. Final check commands and outcomes are retained in `restored-checks.json`. The mandatory M16Q runner now includes all four exact typed tests and twelve exact journal component tests, with all new source files included in its hash inputs. Runner syntax validation is not a full M16Q run.

Outstanding integration: replace full-state persistence with typed journal append; avoid cloning/encoding retained history on each transition; preserve final continuation checks and quarantine semantics; implement the explicit format break and update recovery qualification; bind finite production lifetime and byte/rate/slot quotas; provide safe retention/compaction and aggregate timing/fairness evidence. No whole R1 critical/high finding is closed, and no R2/M17Q/M18Q admission is claimed.

## Prepared transitions and replacement backend

Production staging now separates borrowed transition validation and exact projected logical size from mutation. The replacement `JournalMemberStore` component uses the same preparation, a cached logical byte count, bounded delta serialization, journal append with the final caller check, and in-memory apply only after the record and anchor commit. Its bootstrap uses member schema 7 inside the authenticated journal; this is not selected by production yet. Production remains schema 6 full snapshots.

Six integration tests now include replacement-backend parity across three slots in both modes, actual local query grants with final expiry checks, reopening after every slot, final-check refusal without memory publication, quarantine after anchor I/O failure, pending-record recovery, and exact cached SCALE size across 65 accepted positions. The first and 65th records have equal lengths for each transition kind in that fixture. This is concrete incremental record-size evidence, not an aggregate latency or memory bound.

Final retained source and check manifests are `prepared-backend-started.json` and `prepared-backend-completed.json`. The exact revision passes 59 focused QUV tests with one existing ignored test, 20 runtime tests, CLI compilation, formatting and runner syntax. Both transition controls were repeated on the prepared implementation in `prepared-controls`, then restored. Earlier intermediate logs outside these manifests are diagnostic history, not qualification of the final revision. The mandatory runner includes all six exact integration tests and all twelve journal primitive tests. No full M16Q R2 run or fresh independent review has occurred.
