# Handoff final continuation and recovery reachability

Scoped R1 remediation; no whole finding, R2, M17Q or M18Q closure.
`started.json`, source copies, `results.json` and `completed.json` retain exact
selected dirty-worktree bindings. All selected sources remained unchanged.
The subsequent handoff storage-reservation integration postdates this evidence.

83 core tests, 21 runtime tests, CLI compilation, formatting/syntax and focused
record/anchor/lifetime/continuation formal plus record/anchor syscall gates pass.
The benchmark remains a separately mandatory final R2 gate, not executed here.
The continuation kernel passes 10 TLAPS obligations and 145 positive states;
removing the final fence or rehydrating a grant on recovery violates the named
invariants. This conditions on own live QUV, exact scope, authenticated durable
recovery and filesystem primitives, not full transition refinement or liveness.

The production deadline regression exercises before/equal/after expiry with no
state/anchor mutation or quarantine on expiry. The final outcome-based removed-
fence control fails the expired-install assertion; code is restored exactly.
The earlier count-based control is retained as development history and superseded.
Recovery validates the one-shot generation/predecessor/install shape even for
synthetic correctly authenticated state with a matching authenticated anchor.
Removing that guard fails the semantic-refusal regression; code is restored.

The initial formal attempt failed one inductive obligation. The strengthened
proof records that Synced/Installed phases imply durable state; positive and
both negative models now pass. No claim or deadline was weakened. Full production
resource/service/retention bounds, full refinement, clean R2, immutable candidate,
fresh independent review and owner release admission remain outstanding.
