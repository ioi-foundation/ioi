# Scoped continuation ownership evidence

Selected dirty-worktree source; not immutable R2 qualification. run_checks.py
records exact selected source and commands: 15 runtime-finality, 22 QUV runtime,
two executor tests, CLI compile, formatting, syntax and formal checks pass.
QuvContinuationAdmission has nine TLAPS obligations and eight states; both
expected countermodels violate Ownership. run_mutations.py releases admission
before the consuming callback; the production regression fails and source is
restored. Initial formatting failure is retained with its corrected passing run.

The ownership theorem assumes the callback finishes the synchronous transition
and started workers retain the wrapper until return/unwind. Physical resource,
preparation admission, fairness and complete transition composition remain open.

run_process.py is running the process campaign; consult process-completed.json
for its eventual disposition. This driver captures stdout but lacks the full
runner component sink, so it cannot satisfy strict component timing evidence.
The dynamic mock-verifier fixture skip is unrelated to this identity-hub/QUV
campaign; the four release node executables are built from changed source.

All whole R1 findings OPEN; M17Q REPAIR_REQUIRED; M18Q NOT_ADMITTED.
