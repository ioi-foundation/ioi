# Consequence representation evidence — 25 test revision

Borrowed hash view and direct canonical hashing added. Receipt-budget changes postdate this source.

This scoped collector passed 25 consequence tests, 7 manifest-type tests,
21 runtime tests, 2 executor refusal/lock tests, CLI compilation, formatting,
runner syntax, the consequence trace/format formal gate and the existing T10
formal gate. Read results.json for exact commands and completed.json for source
stability. Source copies and hashes are selected dirty-worktree evidence, not a
clean immutable qualification checkout. No benchmark or full process campaign
was run for this revision; all remain mandatory at complete M16Q R2.

The latest receipt-budget revision's trace/format gate discharges 17 TLAPS
obligations, a 44-state positive model and the required reused-lookup
countermodel. Its four restored production negative controls reject missing
live/recovery trace limits, noncanonical endpoint evidence and oversized named-
profile resource evidence. Both baseline and mutant source files are retained.
Older revision results are bound separately and are not substituted for them.

The first untyped receipt-charge lemma failed one obligation. The corrected
lemma explicitly requires the TypeOK invariant already proved for reachable
states; no target or numeric bound was weakened. Its failed output is retained
under receipt-budget/formal-development.log and the successful typed proof under
formal-typed.log. Serializer charges and nonrollback lookup primitives remain
explicit conditional antecedents; this is not complete production refinement.

The trace allowance is 4 plus the manifest's complete reconciliation budget.
The fixed ML-DSA-44 adapter has <=16 KiB canonical evidence and <=80 KiB outer
record bounds, with bounded file reading. For that named online profile, the
encoded receipt ceiling is exact manifest bytes + 4*16 MiB + 512*(4+M) + 80 KiB
+ 32 KiB. Other adapters do not inherit the bound. The hash-view revision preserves
canonical bytes/optional field omission without an explicit audit/trace clone
or extra complete hash-input Vec; JCS still buffers object values for key sorting.

These are representation and hash-memory repairs. They do not reserve physical
receipt/endpoint storage, bound total heap/service/recovery costs, qualify cross-
configuration retention, discharge full transition refinement or close any whole
R1 finding. M15Q reopened; M16Q R2 unqualified; M17Q REPAIR_REQUIRED; M18Q
NOT_ADMITTED. M12a/M12b and portable_final_receipt=false remain fixed.
