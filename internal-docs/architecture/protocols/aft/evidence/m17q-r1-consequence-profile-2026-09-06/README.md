# Scoped v6 consequence storage profile binding

84 core tests passed (one benchmark deliberately ignored in this focused run),
30 consequence, 8 type, 21 runtime and 2 executor tests passed, plus CLI,
format/syntax, both storage syscall gates and the selected formal obligations.
All checks bind unchanged selected sources archived in sources/. This is not
full M16Q qualification; the ignored benchmark and all other mandatory gates
remain required on the integrated clean candidate.

policy_vector.py independently assembles SCALE bytes for one v6 fixture. The
production missing-storage-field mutation produced exactly its predicted different
root and failed the golden assertion (exit 101). Exact mutant/baseline sources
are retained; baseline was restored and its source hashes rechecked.

The twelve shared coefficients root receipt/endpoint representation and allocated
receipt charges. They do not establish global admission/capacity, RAM, metadata,
service/fairness, retention or complete transition refinement. No whole R1 finding
is closed and no immutable candidate, review or M18Q admission exists.

Run run_checks.py to reproduce this scoped campaign; run_mutations.py applies
one local defensive mutant with exact restoration. Do not run other source
writers concurrently. Preserve every failed artifact.
