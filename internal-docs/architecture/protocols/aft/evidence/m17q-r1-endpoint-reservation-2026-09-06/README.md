# Scoped endpoint reservation repair evidence

All scoped checks passed on the selected source hashes in started.json; source
archive matches after the production capacity-guard mutation was restored.
28 consequence, 7 type, 21 runtime and 2 executor tests; CLI; formatting/syntax;
17 trace/format TLAPS obligations; 9 generic record-reservation obligations;
existing T10 proof/model checks and their required negative models passed.
final-syscalls binds the final source to issued KEEP_SIZE allocation, full
ancestry sync before invocation, and write/fsync/rename/directory-sync. Removed
allocation/sync and injected live allocation trace controls were rejected.
The production removed-capacity regression failed its intended assertion
(exit 101); exact baseline/mutant source is retained. Earlier syscalls is the
pre-active-signature-validation revision, not final-source evidence.

Reproduce: python3 run_checks.py from this evidence directory or repository;
run_mutations.py temporarily applies one local defensive mutant and restores
its exact source in finally. No other source writers may run during these checks.

These are selected dirty-worktree source checks, not a clean immutable M16Q
candidate. Generic reservation proof lifting remains conditional on the documented
filesystem/custody assumptions. Physical metadata, receipt reservation, aggregate
capacity, RAM, fair service, full refinement and complete qualification remain
open. All 13 whole R1 findings remain OPEN; M18Q NOT_ADMITTED.
