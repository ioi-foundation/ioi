# Incremental journal component qualification — 2026-09-05

The test-only member journal primitive writes one authenticated immutable record per transition and updates a separate authenticated head anchor. Recovery authenticates the complete bounded chain before invoking semantic replay into caller-owned temporary memory, then advances a pending anchor only after replay succeeds. Records never confer a process-local online grant. Independent nonrollback anchor custody remains an external assumption; lexical path separation does not establish that custody.

Seven component tests pass. The complete focused QUV unit selection passes 48 tests with one existing ignored test. Raw logs, commands, toolchain, HEAD, and source-subset hashes are retained. An initial compile check ran zero journal tests before tests existed and is not qualification evidence.

This component adds parent-directory fsync before healthy recovery and removes an unacknowledged next temporary record only after chain validation and semantic replay. A pending final record plus a further temporary record is refused. Prior record bytes and inode remain unchanged across subsequent appends in the positive regression. Tests also cover semantic replay refusal before anchor advance, interrupted bootstrap, exact provisioning, exclusive locking, corruption refusal before any replay callback, temporary-record cleanup, and pre-write record/count/total capacity refusals with disk non-mutation.

The module is gated by `cfg(test)` and does not replace production member schema 6 or handoff schema 3. Generic journal limits are committed to the component scope but are not yet rooted production authority/rate/slot quotas. Recovery remains linear in retained records; safe retention/compaction, uncertain-write coverage, typed production transition replay, lifetime bounds, formal refinement, sustained process qualification, and clean full R2 remain outstanding. No whole R1 finding is closed, and no M17Q/M18Q admission follows from these tests.

## Continuation fence and uncertain-write qualification

The next source revision adds `append_with_prewrite_check`. It finishes encoding, authentication, hashing and headroom admission before running the caller's final continuation check immediately before persistence. A refused check preserves disk, generation and healthy state. A write error quarantines the instance; clearing an I/O obstruction does not permit another append until authenticated reopening. Record-write failure recovers the old generation; anchor-write failure recovers the authenticated pending record after successful semantic replay. A semantic refusal preserves partial temporary state for inspection.

Twelve component tests pass, and the focused QUV suite passes 53 tests with one existing ignored test. Removing the final check and removing the quarantine guard each cause the corresponding regression to fail by assertion. The temporary changes were restored. Raw control logs and source copies are retained separately from the earlier seven-test source subset. The restored suite result and hashes are in `commit-boundary-completed.json`.

Production integration remains outstanding. Inspection confirms three existing member mutations: candidate insertion, preparation-attempt reservation, and accepted-head advancement. Their future journal payloads must encode typed deltas and replay their state invariants, preserve exact independently provisioned enrollment, and apply in-memory state only after durable record and anchor completion. The live accepted-head path must use the final continuation fence; recovery must never fabricate a live grant from stored acceptance bytes. Replacing file snapshots with a directory requires an explicit clean format break, updated recovery tests and qualification. Handoff persistence remains separate.
