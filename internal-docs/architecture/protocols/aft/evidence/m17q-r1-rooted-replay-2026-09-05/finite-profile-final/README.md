# Schema-9 finite-profile scoped checks

All eight commands in `results.json` passed with all 70 selected source hashes
unchanged: 70 QUV tests (one benchmark ignored), 21 runtime tests, the rooted
configuration regression, CLI compilation, formatting, runner syntax, first-two
summary proof (27 obligations), and lifetime proof (36 obligations) with its
positive and required event-reuse negative models. The positive lifetime model
explored 6,144 distinct states; the mutation violated `ChargedRecordBytes`.

`started.json` and `sources/` bind the selected production/formal sources;
narrative documents are excluded. This is a dirty-worktree source subset at
HEAD 24a9888e3b88383c18dfbfea0f2e7fa44b99fa64, not an immutable qualified checkout.
Use `run_checks.py` for exact commands. Running it again requires an empty new
evidence directory to preserve these results. No process, restart, performance,
physical storage or complete M16Q qualification is claimed by this campaign.
The schema-7 process/benchmark evidence remains historical and was not rerun.

The sibling finite-profile-final campaign repeated these checks following an
overly broad source-drift concern: the narrative edits were outside the selected
source set, so the initial campaign had no recorded drift. Both runs are retained;
the repeated results do not expand coverage or replace independent review.

The slot horizon, first-two summaries and preparation counters bound encoded
retention. They do not reserve physical disk, qualify total memory or timing,
establish cross-configuration retention, or complete production refinement.
All 13 whole R1 findings remain open; M16Q R2, fresh M17Q and M18Q are outstanding.
