\* AFT-CB P2.7 — the succession race at the MHA corner on both rings:
\* 2 old members (1 honest), 2 standby members (1 honest), two roots, a
\* short clock.  The adversary schedules everything including whether
\* submissions ever become observations (A10 is NOT enforced — safety
\* must hold on every schedule).
CONSTANTS
  OldRing = {o1, o2}
  HonestOld = {o1}
  Standby = {s1, s2}
  HonestStandby = {s1}
  Roots = {rX, rY}
  NoRoot = NoRoot
  T_halt = 2
  G = 2
  MaxTick = 6

INIT Init
NEXT Next

INVARIANT TypeOK
INVARIANT Inv
INVARIANT NoBindingRivalReleased
INVARIANT ActivationJustified
