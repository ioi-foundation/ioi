# Hypervisor Kernel Substrate Migration Matrix

Status: compact implementation migration matrix.
Canonical owner: this file tracks live/current/final ownership for the
Hypervisor kernel substrate unification migration; doctrine remains owned by
the subject docs and the master guide.
Supersedes: ad hoc split-brain status notes for this migration when they
conflict with the route-family owner map below.
Superseded by: none.
Last alignment pass: 2026-06-13.
Last matrix pruning pass: 2026-06-13, after the direct-invoker-only runner cuts,
coding-tool approval-satisfaction JS gate retirement, the Rust approval
satisfaction/projection positive API cut, the Rust-owned coding-tool patch
snapshot capture cut, and the Rust-planned/Agentgres-admitted coding-tool
budget-block governance cut, the public coding-tool budget recovery retry
Rust-planning/Agentgres-commit cut, the budget-recovery request/override
Rust-control and wallet-authority cut, the diagnostics repair retry-run
Rust-planning cut, the coding-tool result envelope/context
Rust planning cut, the Rust-owned coding-tool artifact draft planning plus
Agentgres artifact-state commit cut, and the public thread runtime-control Rust planner
cut, the generic runtime thread-event Rust Agentgres admission cut, and the
runtime thread-event projection Rust Agentgres cut, and the public runtime
thread-event replay positive API cut, and the public runtime thread/turn
projection positive API cut, and the public agent status-control Rust
planning/Agentgres commit cut, and the public agent delete Rust tombstone
planning/Agentgres commit cut, and the public agent create Rust
planning/Agentgres commit cut, and the public run create Rust
planning/Agentgres commit cut, and the public top-level thread create Rust
planning/Agentgres commit/projection cut, and the public non-runtime
resume/turn-create Rust lifecycle/projection cut, and the public task/job
cancel Rust planning/Agentgres commit cut, and the public task/job read
projection positive API cut, and the public task create Rust
planning/projection/Agentgres commit cut, and the workspace-trust
warning/ack Rust planning plus runtime-event Agentgres admission cut, and the
public runtime account/node/tool catalog Rust projection positive API cut, and
the public skill/hook registry Rust projection positive API cut, the public
model_mount catalog-status Rust read-projection positive API cut, the public
memory route projection Rust positive API cut, the public conversation artifact
read projection Rust positive API cut, the public conversation-artifact control
Rust planning plus Agentgres artifact-state commit cut, and the runtime subagent
read projection Rust positive API cut, and the public subagent wait control Rust planning plus
Agentgres commit cut, the public subagent input/resume Rust run-create/control
planning plus Agentgres commit cut, the public subagent assign/cancel
direct-control Rust planning plus Agentgres commit cut, and the public subagent
spawn Rust agent-create/run-create/control planning plus Agentgres commit cut,
and the public subagent cancellation propagation Rust propagated-cancel/control
planning plus Agentgres commit cut, and the public runtime memory
write/edit/delete/policy Rust planning plus Agentgres memory-state commit cut,
and the public managed-session inspection/control Rust projection/planning plus
runtime-event admission cut, and the public workspace-change inspection/control
Rust projection/planning plus runtime-event admission cut, and the public
thread-fork Rust planning plus Agentgres commit/projection/runtime-event
admission cut, the public model load/unload Rust instance-lifecycle planning
plus Agentgres model-instance commit cut, and the public model-instance
maintenance Rust planning plus Agentgres model-instance commit cut, and the
public provider inventory Rust positive API cut, the public model tokenizer
record replay Rust positive API cut, and the public catalog-search Rust
projection cut, the public model-capability projection positive API cut, the
public runtime-engine control/projection positive API cut, the public
runtime-survey capture Rust planning plus Agentgres receipt-state commit cut, the public
backend-lifecycle control positive API cut, the public
provider-lifecycle record-state positive API cut, the public model
artifact/endpoint record-state positive API cut, the public model
storage/download control positive API cut, the model_mount receipt-replay
read-projection state_dir authority cut, the public backend log read Rust
read-projection positive API cut, the public runtime-survey capture Rust
positive API cut, the public catalog-provider OAuth session/state Rust
read-projection replay cut, the OpenAI-compatible stream cancellation Rust
positive API cut, and the public computer-use request-lease Rust StepModule
positive API cut.

## Purpose

This matrix is the Phase 0 inventory for
[`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md).
It keeps each route family honest about current live authority, target owner,
truth path, conformance tier, and cleanup condition.

This file is no longer a per-slice evidence archive. Per-slice archaeology made
the migration optimize for token-maxing iteration instead of whole authority
cuts. The matrix now records macro boundaries only: what has changed ownership,
what remains temporary, and what must not be recreated.

Terminal status is not claimed here. The migration is open until
`hypervisor-conformance` passes and the terminal conditions in the master guide
are all true.

## Current Sprint Lane

The current implementation lane is Rust substrate migration plus JS facade
retirement. Current tier conformance is green, but the terminal migration is not
claimed. Future work must prioritize macro authority cuts over additional
bridge/fallback polish:

- replace a fail-closed JS facade with a positive Rust daemon-core API;
- move one route-family truth path into Rust, including authority, admission,
  receipts, projection, replay, and stable protocol shape;
- delete the JS facade or reduce it to product/API/IDE/SDK adapter behavior in
  the same cut;
- update conformance and this matrix once per macro cut, not once per helper.

The current `ioi-step-module-bridge` command path is acceptable only as
migration transport. It is not the terminal architecture. It must collapse into
the Rust daemon core API or remain narrow transport with no independent
authority, accepted-truth mutation, fallback execution, or compatibility-shim
semantics.

## Route-Family Owner Map

| Route family | Current live status | Terminal owner | Next macro cut |
| --- | --- | --- | --- |
| `model-mounting/artifact-endpoints` | Public `importModel()`, `mountEndpoint()`, and `unmountEndpoint()` now call Rust daemon-core `plan_model_mount_artifact_endpoint`, receive Rust-authored `model-artifacts` or `model-endpoints` records with artifact/endpoint hashes, authority hashes, wallet/cTEE boundary facts, and Agentgres truth evidence, then require Rust Agentgres model_mount record-state commit before returning public artifact or endpoint truth. The old fail-closed artifact/endpoint JS facade, JS artifact/endpoint map mutation, JS lifecycle receipts, `writeMap("model-artifacts")`, `writeMap("model-endpoints")`, local materialization, and no-commit planner success remain retired for these public mutation edges. | Rust daemon core `model_mount` plus Agentgres record-state admission/projection/replay, wallet.network authority, cTEE custody, and stable protocol APIs. | Move hosted/provider endpoint discovery/materialization and richer artifact/endpoint replay/projection into Rust, then replace the temporary JS runner/client transport with stable direct Rust daemon-core APIs. |
| `model-mounting/storage-download-controls` | Public `catalogImportUrl()`, `downloadModel()`, `cancelDownload()`, `deleteModelArtifact()`, and `cleanupModelStorage()` now call Rust daemon-core `plan_model_mount_storage_control`, receive Rust-authored `model-catalog-imports`, `model-downloads`, or `model-storage-controls` records with storage-control hashes, authority hashes, wallet/cTEE boundary facts, and Agentgres truth evidence, then require Rust Agentgres model_mount record-state commit before returning public storage/download truth. `storageSummary()`, `listDownloads()`, and `downloadStatus()` now call Rust model_mount read-projection kinds over runtime `state_dir`, replay admitted storage-control records from `model-catalog-imports`, `model-downloads`, and `model-storage-controls`, and filter out JS-authored storage/download truth. The old fail-closed catalog/download/storage JS facade, JS lifecycle receipts, JS download/artifact map mutation/readback, `writeMap("model-downloads")`, `writeMap("model-artifacts")`, fixture/live download materialization, local filesystem mutation/scanning, and no-commit planner success remain retired for these edges. | Rust daemon core `model_mount` plus Agentgres record-state admission/projection/replay, wallet.network authority, cTEE custody, and stable protocol APIs. | Move richer filesystem custody, richer hosted catalog/download materialization, command-transport retirement, and stable direct Rust daemon-core storage/download APIs. |
| `model-mounting/mcp-workflow` | Public `importMcpJson()`, `compileEphemeralMcpIntegrations()`, `invokeMcpTool()`, and `executeWorkflowNode()` now call Rust daemon-core `plan_model_mount_mcp_workflow`, receive Rust-authored `mcp-servers` or `mcp-workflow-controls` records with workflow hashes, authority hashes, receipt refs, custody facts, no-JS-execution flags, and Agentgres truth evidence, then require Rust Agentgres model_mount record-state commit before returning public workflow truth. Public `listMcpServers()` now calls Rust read-projection kind `mcp_servers` with runtime `state_dir`; Rust replays persisted `mcp-servers/*.json`, filters to Rust-authored MCP workflow records, returns only redacted server projections, and keeps JS `mcpServers` maps, old `publicMcpServer()` readback, direct JS receipt synthesis, registry mutation, route tests, receipt-gate dispatch, model invocation, and projection-required refusal shims retired. | Rust daemon core `model_mount` plus Agentgres record-state admission/projection/replay, receipt/state-root binding, wallet.network authority, cTEE custody, StepModuleRouter dispatch planning, and stable protocol APIs. | Move actual MCP transport execution, wallet/containment exits, content receipts, richer workflow replay/projection, command-transport retirement, and stable direct Rust daemon-core MCP workflow APIs. |
| `coding-tools` | Rust-live StepModule execution exists for migrated tools; Rust daemon-core now constructs coding-tool `StepModuleInvocation` envelopes from canonical request facts before workload dispatch/admission; normal success/failure execution now calls Rust `plan_coding_tool_result_envelope` before dispatch for the StepModule context and again after workload observation for the result `payload_summary` and runtime-event candidate before Agentgres result-event admission; approval-required execution projects approval request/decision/lease truth through Rust `project_coding_tool_approval_satisfaction` and then asks Rust `plan_coding_tool_approval_satisfaction` before the StepModule path; unsatisfied approval block result/event materialization is Rust-planned by `plan_coding_tool_approval_block`; budget-block governance is Rust-planned by `plan_coding_tool_budget_block` and the blocked result event is Rust Agentgres-admitted through `admit_coding_tool_result_event` before JS returns the policy error; run-level coding-tool budget recovery `retry_approved` now requires Rust `plan_coding_tool_budget_recovery_state_update`, commits only the Rust-planned run projection through Agentgres-backed `writeRun`, and `request_approval`/`approve_override` now require Rust `plan_coding_tool_budget_recovery_control`; request approval commits only the Rust-planned waiting run projection, approve override requires wallet.network grant plus authority receipt refs and binds a Rust-authored authority hash into the run projection, and the standalone JS budget-recovery policy/result helper remains deleted; accepted coding-tool result events and command-stream events are admitted by Rust before JS projection registration; coding-tool artifact drafts now call Rust daemon-core `plan_runtime_coding_tool_artifact_drafts`, require Rust-authored artifact records plus Rust Agentgres artifact-state commit before JS updates the temporary artifact projection cache, and reject no-planner/no-commit/no-record success; artifact read/retrieve now calls Rust daemon-core `project_runtime_coding_tool_artifact_read` with runtime `state_dir`, where Rust replays committed `artifacts/*.json` Agentgres records, filters canonical Rust-authored coding-tool artifacts, enforces thread ownership and canonical target/range aliases, shapes byte ranges, result metadata, receipt refs, and available-artifact projection, rejects retired `artifact_records` candidate transport, and fails closed when the Rust projector or `state_dir` is absent; `file.apply_patch` patch snapshot capture now consumes Rust `workspace_restore.rs` snapshot record/event output and no longer calls the JS snapshot-event appender; public workspace snapshot list/content-package and restore preview/apply APIs now call Rust `workspace_restore.rs` projection/restore command responses through the direct-invoker runner instead of JS runtime-event/artifact readback; Rust `coding_tool_workspace.rs` emits canonical snake_case workload observations for migrated workspace, patch, test, and diagnostics tools; `plan_post_edit_diagnostics_feedback` now owns post-edit diagnostics skip/path selection, repair policy normalization, rollback refs, auto diagnostics `tool_call_id`, and `diagnostics_repair_context` authoring before JS forwards the Rust-authored `lsp.diagnostics` request; diagnostics repair policy projection now sends runtime `state_dir` plus diagnostic event ids to Rust, where Rust replays admitted Agentgres runtime events to derive policy inputs and deterministic injection ids before returning rollback repair policy truth; diagnostics operator override state update now sends raw operator request, decision, repair-policy context, and canonical wallet authority refs to Rust, where Rust derives `approval_required`, `approval_satisfied`, and `approval_source`, rejects retired JS verdict/authority transport, requires wallet.network grant and authority receipt refs for approval-required overrides, binds a Rust-authored authority hash into the operator-control run projection, and direct operator-override event planning now applies the same wallet authority gate before runtime-event admission; the JS approval satisfaction projection callback, JS approval manifest matcher, JS approval block facade, JS budget-block result/event facade, JS budget-recovery policy/result helper, JS diagnostics operator-override approval helper/verdict transport, JS result-event admission hook, JS command-stream append facade, JS artifact draft record materializer, JS artifact read/retrieve selector/slicer/candidate transport, JS-created coding-tool StepModule envelope, JS-authored StepModule context, JS-authored result payload summary/source-event kind/result-event candidate, JS post-edit diagnostics planner, public workspace snapshot JS projection/readback facade, and successful-without-Rust-snapshot compatibility path are deleted or demoted to Rust-client scaffolding. | Rust daemon core `step_router` plus workload/WASM backend, Agentgres admission, receipt/state-root binding, projection, and replay. | Move remaining diagnostics repair approval authority and broader orchestration into the Rust-owned execution spine, now that approval-required operator override issuance is wallet-gated by Rust and budget-recovery request/override issuance is Rust-planned; then collapse the temporary JS runner transport into stable Rust protocol/API calls. |
| `approvals-gates` | Approval manifest planning, coding-tool approval satisfaction projection, approval satisfaction planning, approval block shaping, approval state-update command response shaping, public approval request/decision/revoke control, and public approval queue/read projection now route through Rust `approval.rs` via direct-invoker runners; public approval decision/revoke first calls Rust `authorize_approval_decision`, requires wallet.network grant refs and authority receipts, binds the Rust authority hash into the state update, and the Rust decision/revoke state planners reject missing wallet authority. JS governance no longer scans approval events/leases, JS approval request/decision readback is retired, JS approval satisfaction projection is retired, and the public approval surface persists only Rust-authored run/agent projections through Agentgres-gated state commits while `GET /v1/threads/:thread_id/approvals` returns the Rust `project_approval_queue` projection. | Rust daemon core `authority` backed by wallet.network grants, Agentgres expected heads, receipts, replay, and stable approval read APIs. | Move remaining approval authority issuance/consumption and approval-state runner transport into direct Rust authority/projection APIs. |
| `runtime-events-replay-trace` | Generic runtime thread-event append now calls Rust `admit_runtime_thread_event` through the direct-invoker Agentgres runner, requires receipt refs, expected heads, state roots, storage admission, and projection watermarks before JS can register the Rust-returned event in the local replay cache; synthetic `thread.started` projection and legacy run-event-to-runtime-event projection now call Rust `project_runtime_thread_events`, which authors thread/run event envelopes, skips known idempotency keys, admits each event through the same Rust Agentgres admission core, and returns only Rust-admitted events for JS replay registration. Public stream/turn replay readback now calls Rust `project_runtime_thread_event_replay`; Rust owns stream/turn selection, canonical cursor evaluation, required Agentgres admission refs, and the returned event set while JS only supplies temporary cache candidates. The runner transport remains adapter scaffolding. | Rust projection/replay core over Agentgres-admitted truth and receipt-bound state roots. | Replace the temporary runner/cache transport with direct Rust projection/replay APIs consumed by IDE/SDK. |
| `model-mounting` | Major route-decision, invocation, provider-execution, lifecycle, required-boundary, read-projection, and receipt-binding planning is Rust-owned behind migration transport; public provider upsert now calls Rust daemon-core `plan_model_mount_provider_control`, receives a Rust-authored `model-providers` record with provider-control and authority hashes, wallet.network/cTEE no-plaintext custody facts, and Agentgres provider-control truth evidence, then requires Rust Agentgres model_mount record-state commit before returning public provider truth. Rust read-projection kind `providers` now replays admitted `model-providers/*.json` provider-control records alongside admitted `model-provider-inventory/*.json` materialization records, filters JS-authored provider truth, and the mounted provider lookup path consumes that Rust projection instead of `state.providers` map truth. The old fail-closed provider-upsert JS facade, provider-map mutation/lookup truth, vault-material resolution, `writeMap("model-providers")`, and no-commit planner success remain retired for this public provider write and lookup edge; public local/fixture provider health and native-local provider start/stop now call the Rust `plan_model_mount_provider_lifecycle` boundary, receive Rust-authored `model-provider-lifecycle-controls` records, require Rust Agentgres model_mount record-state commit before returning public lifecycle truth, and still avoid JS driver execution, lifecycle receipts, provider-map writes, and projection writes; public load/unload now asks Rust for provider lifecycle, asks Rust `plan_model_mount_instance_lifecycle` for the model-instance transition record, commits only that Rust-authored record through Rust Agentgres model_mount record-state admission, and returns public instance truth after commit without JS provider drivers, JS lifecycle receipts, `writeMap("model-instances")`, or legacy instance-record helpers; loaded-instance idle eviction, duplicate coalescing, and explicit supersede now ask Rust `plan_model_mount_instance_lifecycle` for evict/supersede transition records, including `reason` and `superseded_by` where required, commit only those Rust-authored records through Rust Agentgres model_mount record-state admission, and update local instance projection cache only after commit; public fixture/local-folder, native-local, and hosted metadata provider model/list-loaded inventory now call Rust `plan_model_mount_provider_inventory`, receive Rust-authored `model-provider-inventory` records, commit only those records through Rust Agentgres model_mount record-state admission before inventory truth can return, and expose `providerInventoryRecords()` as Rust read-projection kind `provider_inventory_records` over runtime `state_dir` replay; public `catalogSearch()` now calls Rust read-projection kind `catalog_search` with canonical query state and runtime `state_dir`, and Rust replays admitted `model-provider-inventory/*.json` list-model records into Rust-authored catalog-search results while filtering out JS-authored inventory and loaded-instance inventory; public `listArtifacts()`, `listProductArtifacts()`, `runtimeModelCatalogList()`, and `openAiModelList()` now call Rust read-projection kinds with runtime `state_dir`, materialize artifact/runtime-catalog/OpenAI-list records from admitted provider inventory, and keep JS topology maps out of dedicated request truth; public `listModelCapabilities()` now calls Rust read-projection kind `model_capabilities` with empty request state plus runtime `state_dir`, replays admitted route, route-endpoint-resolution, provider-inventory, and instance records into canonical capability readiness contracts, and no longer returns the default empty placeholder; public `catalogStatus()` now calls Rust read-projection kind `catalog_status` with runtime `state_dir`, replays admitted provider-inventory records into Rust-authored catalog provider status, storage status, last-search summary, and result rows, filters JS-authored inventory, and keeps `catalog_status_input` out of request truth; public `listOAuthSessions()` and `listOAuthStates()` now call Rust read-projection kinds `oauth_sessions` and `oauth_states` with empty request state plus runtime `state_dir`, replay admitted `model-catalog-provider-controls/*.json` records in Rust, filter legacy JS OAuth truth, and return redacted wallet/cTEE custody rows without reading `oauthSessions`/`oauthStates` maps or translating retired OAuth refusals; migrated provider upsert, provider lookup, provider inventory, catalog search, catalog status, and catalog-provider OAuth readback no longer use JS driver execution, JS inventory receipts, JS provider-port iteration, JS search/status aggregation, JS OAuth map readback, `lastCatalogSearch`, local artifact/instance fallback reads, artifact or instance map mutation, provider map mutation/readback, JS vault resolution, or no-commit planner success, while hosted/nonlocal provider transport execution and dynamic hosted catalog materialization remain non-terminal but public hosted inventory no longer returns through JS driver execution; public route write/test, mounted route selection, explicit-model endpoint resolution, and runtime explicit/run-override model-route selection now call Rust `plan_model_mount_route_control`, commit or forward only Rust-authored route, route-test, route-selection, or endpoint-resolution records through Rust Agentgres record-state admission, and no longer return through the route-facing JS refusal/fallback facade for those edges; public tokenize/count/context-fit utility facades now call Rust `plan_model_mount_tokenizer`, bind the Rust route-selection record/receipt, commit only Rust-authored tokenizer/context-fit records through Rust Agentgres model_mount record-state admission, expose `modelTokenizerRecords()` as Rust read-projection kind `model_tokenizer_records` over persisted `model-tokenizer-utilities/*.json`, and no longer return through the tokenizer required-record refusal shim, JS context-window fallback, JS receipt, truncation, response-envelope path, or JS tokenizer projection path; public backend health/start/stop now call Rust `plan_model_mount_backend_lifecycle`, receive Rust-authored `model-backend-lifecycle-controls` records, commit only those records through Rust Agentgres model_mount record-state admission, return Rust public responses with JS backend-registry reads, process control, and log reads/writes marked false, and no longer return through the backend-lifecycle required-record facade for migrated methods; public backend logs now call Rust read-projection kind `backend_logs` with runtime `state_dir`, replay admitted `model-backend-lifecycle-controls/*.json`, filter JS-authored controls plus retired read-as-mutation `logs_read` records, and no longer return through backend-lifecycle mutation planning; public server start/stop/restart/write, operation recording, and log append now call Rust `plan_model_mount_server_control`, receive Rust-authored `model-server-controls` records, commit only those records through Rust Agentgres model_mount record-state admission, while public server logs/events/log-record reads now call Rust read-projection kinds `server_logs`, `server_events`, and `server_log_records`, replay admitted `model-server-controls/*.json`, filter JS-authored controls plus retired read-as-mutation `logs_read`/`events_read`/`log_projection` records, and no longer return through server-control mutation planning or the server-control required-record facade for migrated methods; public runtime-engine selection/profile/remove mutations now call Rust `plan_model_mount_runtime_engine`, receive Rust-authored `runtime-engine-controls` records, commit only those records through Rust Agentgres model_mount record-state admission, return Rust public responses with JS preference/profile/projection writes marked false, and no longer return through the runtime-engine required-record facade for migrated methods; public fixture/local-folder non-stream invocation and native-local stream invocation now enter the Rust `model_mount` route-selection receipt, provider-execution admission, provider invocation/stream execution, provider-result admission, accepted-receipt transition, StepModule receipt binding, Agentgres operation/state-root binding, and Rust-authored invocation receipt persistence path marked by `model_mount_invocation_positive_rust_path` and `rust_daemon_core_model_invocation_receipt`; those migrated public invocation paths no longer use JS provider drivers, JS invocation receipts, route-state writes, provider-native request-shaping hooks, record-state commits, or stream downgrade fallback; model conversation-state writes now call Rust `plan_model_mount_conversation_state` and commit only Rust-authored `model-conversations` records through Rust Agentgres record-state admission, while native stream-completion finalization now calls Rust `plan_model_mount_stream_completion`, commits only the Rust-authored conversation record, and persists only the Rust-authored `model_invocation_stream_completed` receipt with receipt_binder, accepted-receipt append, StepModule result, Agentgres operation/state-root/resulting-head bindings, and conversation/stream hashes; OpenAI-compatible stream cancellation now calls Rust `plan_model_mount_stream_cancel`, commits only the Rust-authored canceled conversation record, and persists only the Rust-authored `model_invocation_stream_canceled` receipt with receipt_binder, accepted-receipt append, StepModule result, Agentgres operation/state-root/resulting-head bindings, and stream-cancel hash; JS conversation record synthesis, JS stream-completion receipt synthesis, JS stream-cancel receipt synthesis, JS receipt-binding construction, `state.receipt("model_invocation_stream_completed")`, `state.receipt("model_invocation_stream_canceled")`, and `writeMap("model-conversations")` remain retired; public conversation list projection now calls Rust read-projection kind `model_conversation_states` with runtime `state_dir`, replays persisted `model-conversations/*.json` Agentgres records in Rust, emits only Rust-authored conversation records with conversation hashes, Rust conversation/stream evidence, and Agentgres conversation-truth evidence, and deletes the old fail-closed JS list facade plus public-list `conversation_states` request input; public `listInstances()` now calls Rust read-projection kind `instances` with runtime `state_dir`, replays persisted `model-instances` records in Rust, emits only Rust-authored instance lifecycle records with lifecycle hashes and Agentgres registry evidence, and keeps public-list instance request state empty so JS instance maps cannot return as topology truth; public `listRoutes()` now calls Rust read-projection kind `routes` with runtime `state_dir`, replays persisted `model-routes` records in Rust, emits only Rust-authored route-control records with route-control evidence and receipt refs, and keeps public-list route request state empty so JS route maps cannot return as topology truth; public `modelRouteDecisions()` now calls Rust read-projection kind `model_route_decisions` with runtime `state_dir`, replays persisted `model-route-selections/*.json` Agentgres records in Rust, emits only Rust-authored route-selection decisions with route-control evidence and accepted-receipt binding, and keeps JS receipt lists out of route-decision request truth; public `modelRouteEndpointResolutions()` calls Rust read-projection kind `model_route_endpoint_resolutions`, replays persisted `model-route-endpoint-resolutions/*.json` records, and filters endpoint-resolution truth to Rust route-control records; public `listEndpoints()` now calls Rust read-projection kind `endpoints` with runtime `state_dir`, replays persisted `model-route-endpoint-resolutions/*.json` records, materializes canonical endpoint records from Rust route-control endpoint resolutions, filters JS-authored endpoint-resolution records, and keeps public-list endpoint request state empty so JS endpoint maps cannot return as topology truth; public `projectionSummary()`, `receiptReplay()`, `authoritySnapshot()`, `listProviderHealth()`, `latestProviderHealth()`, `latestVaultHealth()`, `latestRuntimeSurvey()`, `snapshot()`, and `projection()` now call Rust read-projection kinds with empty request state plus runtime `state_dir`, Rust replays admitted `receipts/*.json` through the shared receipt projection boundary, emits public provider-health list entries from admitted `provider_health` receipts, and JS receipt or provider-health lists can no longer become public projection, authority, provider-health, vault-health, runtime-survey, receipt-replay, snapshot, or projection truth; public `runtimeSurvey()` now calls Rust daemon-core `plan_model_mount_runtime_survey`, Rust replays runtime-engine state from Agentgres `state_dir`, authors the `runtime_survey` receipt with snake_case details plus Rust hardware facts and no LM Studio CLI execution, requires Rust Agentgres model_mount receipt-state commit before returning public survey truth, and keeps JS hardware probes, JS runtime-engine reads, JS LM Studio probes, `state.receipt("runtime_survey")`, and no-commit success retired; JS model-mounting state still contains other fail-closed or adapter surfaces. | Rust daemon core `model_mount` owns provider control write records and provider-control replay/projection for provider lookups, provider lifecycle, provider inventory records/replay for migrated fixture/native-local/hosted-metadata read APIs, catalog-search, catalog-status, catalog-provider OAuth session/state replay, and provider-inventory topology/catalog materialization projections over provider inventory records, model-capability projection over admitted route/topology records, instance lifecycle transitions, route control, route-selection and endpoint-resolution replay, endpoint-list projection, tokenizer control/replay, backend-lifecycle control record planning, server-control and runtime-engine control record planning, runtime-survey receipt planning, backend/server/runtime projection, invocation, receipt binding, conversation-state write planning, stream-completion receipt/record planning, stream-cancel receipt/record planning, public conversation, instance, route list, route-decision, endpoint-list projection, and receipt-backed projection/authority/health/survey/snapshot envelopes over direct Agentgres replay, and stable APIs. | Move hosted/provider transports, hosted/provider endpoint discovery/materialization, wallet/cTEE invocation authority gates, richer OAuth execution/materialization, richer hosted catalog materialization, stable direct Rust/Agentgres APIs, then delete the corresponding JS adapter scaffolding. |
| `agentgres-admission` | Runtime Agentgres runner is direct-invoker-only and Rust response shaping exists; public agent creation now calls Rust `plan_agent_create_state_update`, agent-scoped run creation calls Rust `plan_run_create_state_update`, public task creation calls Rust `plan_runtime_task_job_create_state_update`, public run cancellation calls Rust `plan_run_cancel_state_update`, public task/job cancellation calls Rust `plan_runtime_task_job_cancel_state_update`, public task/job list/get now calls Rust `project_runtime_task_job_projection` for projection/filter/public-id selection over run candidates, public agent archive/unarchive/resume/close/reload controls call Rust `plan_agent_status_state_update`, permanent delete calls Rust `plan_agent_delete_state_update`, runtime-service thread start calls Rust `plan_runtime_bridge_thread_start_agent_state_update`, runtime-service thread control calls Rust `plan_runtime_bridge_thread_control_agent_state_update`, runtime-service turn submit calls Rust `plan_runtime_bridge_turn_run_state_update`, and those lifecycle controls commit only Rust-authored projections/tombstones/bridge agents/bridge runs through Agentgres-backed `writeAgent`/`writeRun`; JS thread/store persistence facades still stage some other calls. | Rust daemon core `agentgres_admission` is the only admitted truth path for meaningful state transitions and artifact/payload refs. | Move remaining thread lifecycle commit and projection persistence through direct Rust Agentgres APIs; delete JS accepted-truth mutation paths. |
| `receipt-binding` | Receipt binder and many receipt aliases are Rust-owned or retired; JS facades still translate some receipt-required errors. | Rust daemon core `receipt_binder` binds every meaningful operation to expected heads, state roots, ArtifactRefs, PayloadRefs, and replay. | Collapse remaining JS receipt translators into Rust-owned response envelopes for one live route family. |
| `ctee-private-workspace` | cTEE Private Workspace runner is direct-invoker-only and fail-closed without Rust; CLI admission surface exists. | Rust daemon core `ctee` enforces no-plaintext-custody, declassification, leakage failure, and private workspace receipt semantics. | Replace the JS runner with a direct Rust cTEE custody protocol/API and remove the runner facade. |
| `workload-client-wasm` | StepModule ABI and Rust workload client contracts exist; migrated coding-tool invocation envelopes and workload dispatch requests are now constructed in Rust daemon-core from canonical JS client facts, while JS still initiates the temporary runner transport. | Rust/WASM workload backends execute admitted Step/Module work through the shared contract under daemon authority. | Replace temporary runner transport with stable Rust protocol/API calls and extend Rust-owned invocation construction across remaining route families. |
| `workflow-compositor` | IDE/compositor projections have many alias retirements and negative guards; public runtime thread/turn projection records now call Rust `project_runtime_thread_turn_projection`, so Rust owns the thread/turn record shape, seq-range and item-id derivation from admitted replay events, canonical projection hashes, and retired request alias rejection while JS only gathers canonical agent/run/event facts. Public runtime account/node/tool catalog, skill/hook registry, repository workflow, lifecycle, memory list/policy/path/status/validation, diagnostics repair decision resolution, conversation-artifact list/get/revision, subagent list/get/result, workspace-change inspection, and managed-session inspection route projections now call Rust `project_runtime_tool_catalog`, `project_skill_hook_registry`, `project_repository_workflow`, `project_runtime_lifecycle`, `project_runtime_memory_projection`, `project_runtime_diagnostics_repair_projection`, `project_runtime_conversation_artifact_projection`, `project_runtime_subagent_projection`, `project_runtime_workspace_change_projection`, and `project_runtime_managed_session_projection`, so the old JS projection-required catalog/repository/lifecycle facades plus the fail-closed public memory, diagnostics repair decision resolver, conversation-artifact read, subagent read, workspace-change inspection, and managed-session inspection facades are retired; JS only forwards canonical request/environment/workspace/lifecycle/memory/diagnostics/artifact/subagent/workspace-change/session candidates and fails closed if the Rust projector is missing. Public memory write/edit/delete/policy controls now require Rust `plan_runtime_memory_control` and Rust Agentgres memory-state commits before returning route projections; memory status, validation, and direct memory control-event append now require Rust `plan_runtime_memory_control` event planning plus Rust-authored runtime-event admission; public workflow-edit proposal/apply controls now require Rust `plan_runtime_workflow_edit_control` event planning plus Rust-authored runtime-event admission; public workspace-change control now requires Rust `plan_runtime_workspace_change_control` event planning plus Rust-authored runtime-event admission; public managed-session control now requires Rust `plan_runtime_managed_session_control` event planning plus Rust-authored runtime-event admission; public thread-fork control now requires Rust `plan_runtime_thread_fork_control`, commits only the Rust-authored forked agent through Agentgres-backed `writeAgent`, projects the forked thread through the Rust projection boundary, and admits only the Rust-authored `thread.forked` runtime event; public conversation-artifact create/action/export/promote now requires Rust `plan_runtime_conversation_artifact_control` and Rust Agentgres artifact-state commits before returning route truth; public diagnostics repair decision execution and direct decision-event append now require Rust `plan_runtime_diagnostics_repair_control` event planning plus Rust-authored runtime-event admission; diagnostics operator override execution now requires Rust `plan_diagnostics_operator_override_state_update` plus Rust Agentgres run-state commit, and direct operator-override event append now requires Rust diagnostics repair event planning/admission; diagnostics repair retry-turn creation now composes with the Rust run-create state-update surface and Rust diagnostics repair retry-event planning/admission, and direct retry-event append now requires Rust-authored runtime-event admission; public subagent spawn, wait, input, resume, assign, cancel, cancellation propagation, and direct control-event append now also require Rust `plan_runtime_subagent_control` and Rust-authored runtime-event admission; public lifecycle controls still require Rust subagent state-update planning before Agentgres-backed subagent persistence, spawn composes with Rust-owned child-agent and run creation, input/resume compose with the Rust run-create state-update path, and cancel/propagation compose with the Rust run-cancel state-update path. Other IDE/runtime views still consume adapter-shaped runtime surfaces. | IDE consumes Rust projection/replay records and never creates accepted truth. | Wire the next compositor/runtime view or control directly to Rust projection/admission output and remove the corresponding JS facade. |
| `worker-service-packages` | Worker/service package runner is direct-invoker-only and Rust governed-receipt response shaping exists. | Rust daemon core package invocation path with authority, artifacts, receipts, Agentgres truth, and compositor projection. | Replace the JS package runner with direct Rust protocol/API and keep JS as SDK/API adapter only. |
| `meta-improvement` | Governed-improvement runner is direct-invoker-only and Rust governed-admission response shaping exists. | Rust daemon core governs proposal admission, verification receipts, authority gates, and accepted improvement truth. | Move proposal execution/projection/read APIs out of JS facade scaffolding. |
| `rust-daemon-core` | Command envelope identity, typed operation dispatch, public runtime account/node/tool catalog projection, public skill/hook registry projection, public repository workflow projection, public lifecycle projection, runtime bridge thread-start, thread-control, and turn-submit state planning, public memory route projection, public memory write/edit/delete/policy control planning, public memory status/validation/direct control-event planning, public workflow-edit proposal/apply control-event planning, public managed-session inspection projection/control-event planning, public workspace-change inspection projection/control-event planning, public thread-fork control planning, model-mount MCP workflow planning and MCP server list projection, post-edit diagnostics feedback planning, coding-tool budget recovery request/override control planning, diagnostics repair policy projection, public diagnostics repair decision execution/direct event planning, diagnostics repair decision projection, diagnostics operator override state-update/direct-event planning, diagnostics repair retry-run request planning, diagnostics repair retry-event planning, public conversation-artifact read projection and create/action/export/promote control planning, public subagent read projection, public subagent spawn/wait/input/resume/assign/cancel/propagated-cancel control-event planning, public subagent direct control-event append planning, and many response planners are Rust-owned; some temporary command transport remains. Shared JS command-spawn helper is deleted. | Rust daemon core owns hot-path execution semantics, authority gates, StepModuleRouter dispatch, Agentgres admission, receipt binding, replay, projection, and stable APIs. | Replace command-transport-shaped daemon runners with direct in-process or stable daemon protocol APIs. |
| `js-facade-retirement` | Many JS pass-through wrappers, compatibility aliases, and local writers are deleted; public agent create/run create/run cancel/task create/task-job cancel/status/delete controls, task/job list/get projections, runtime-service thread start, thread control, and turn submit, workspace-trust warning/ack event controls, public runtime account/node/tool catalog projections, public skill/hook registry projections, public repository workflow projections, public lifecycle projections, public memory list/policy/path/status/validation route projections, public memory write/edit/delete/policy controls, public memory status/validation/direct control-event append, public workflow-edit proposal/apply controls, public coding-tool budget recovery retry/request/override controls, workspace-change inspection/control, managed-session inspection/control, thread-fork control, post-edit diagnostics feedback planning, diagnostics repair policy projection, public diagnostics repair decision execution/direct decision-event append, diagnostics repair decision resolution, diagnostics operator override execution/direct operator-event append, diagnostics repair retry-run request authoring/turn creation/direct retry-event append, conversation-artifact list/get/revision projections and create/action/export/promote controls, model-mount MCP import/ephemeral/tool/workflow-node planning plus MCP server list readback, subagent list/get/result projections, subagent spawn/wait/input/resume/assign/cancel/propagation controls, and subagent direct control-event append moved from fail-closed JS facade to Rust-owned positive paths; remaining non-test `rust_core_required` markers are honest fail-closed boundaries. | JS facades are non-authoritative clients only, or retired. | Convert fail-closed facades into Rust-backed positive paths in whole route-family batches, then delete the facade. |

## Macro Authority Cut Ledger

This ledger replaces the old per-slice evidence list. It records the meaningful
authority-boundary cuts that should guide future work.

| Macro cut | Current proof | Still temporary |
| --- | --- | --- |
| Rust-live coding-tool StepModule path | Migrated coding tools execute through Rust workload/StepModule contracts, explicit `daemon_js`/shadow/gated/backend selection fails closed, and Rust daemon-core constructs the coding-tool `StepModuleInvocation` envelope before workload dispatch/admission. | JS invocation surface still coordinates temporary runner transport plus fail-closed governance/artifact/snapshot surfaces. |
| Public computer-use request-lease positive API | Public `ioi.computer_use.*` invocation methods now map browser discovery, control, native-browser, visual GUI, sandboxed-hosted, and visual GUI observe calls to canonical `computer_use.request_lease` input and invoke the Rust-live coding-tool StepModule path. Rust `coding_tool_computer_use.rs` authors the lease request, provider registry selection, wallet.network authority boundary, fail-closed provider availability, receipt refs, and canonical result fields; the old guard-only public JS facade is gone and direct computer-use event append remains fail-closed. | Concrete provider execution, direct Rust daemon-core computer-use event materialization, cTEE private-workspace custody, durable Agentgres expected-head/state-root binding, replay/projection, command-transport retirement, and stable IDE/CLI/SDK APIs remain non-terminal. |
| Coding-tool artifact draft Rust planning | `plan_runtime_coding_tool_artifact_drafts` authors canonical artifact records in Rust, rejects retired `artifactDrafts` aliases, requires receipt refs, and the daemon artifact surface commits only those Rust-authored records through Rust Agentgres artifact-state admission before updating its temporary read cache. | Runner transport retirement and stable IDE/CLI/SDK APIs remain non-terminal. |
| Coding-tool artifact read/retrieve projection Rust-owned | `project_runtime_coding_tool_artifact_read` now requires runtime `state_dir`, replays committed `artifacts/*.json` Agentgres records in Rust, filters canonical Rust-authored coding-tool artifacts, enforces thread ownership and retired target/range/candidate-transport alias rejection, shapes byte ranges, result metadata, receipt refs, and available-artifact projection, and the daemon artifact surface fails closed without the Rust projector or `state_dir` instead of selecting/slicing/transporting records in JS. | Diagnostics projection/replay, runner transport retirement, and stable IDE/CLI/SDK APIs remain non-terminal. |
| Rust command-envelope and bridge contraction | Rust owns canonical command-envelope parsing, typed operation identity, and many child command response families; deleted bridge delegates must not return. | `ioi-step-module-bridge` remains migration transport, not terminal API. |
| Direct-invoker-only daemon runners | Worker/service package, L1 settlement, cTEE private workspace, external capability authority, governed improvement, coding-tool approval, approval-state, runtime Agentgres admission, workspace restore, context-policy/state-update, model_mount admission, and StepModule runners reject command/env compatibility and require `daemonCoreInvoker`. | The runner layer is still JS protocol scaffolding until direct Rust daemon-core APIs replace it. |
| Model_mount Rust owner split | Rust owns route decision, public route write/test control planning, invocation admission, provider execution, provider result, lifecycle planning, backend-process planning, accepted-receipt planning, read-projection planning, and receipt-binding response shaping behind migration transport. | JS model_mount state/materialization/read adapters remain broad and must be retired by positive Rust APIs. |
| Model_mount stream-cancel positive API | OpenAI-compatible stream cancellation now calls Rust daemon-core `plan_model_mount_stream_cancel`; Rust authors `model_mount_stream_cancel_rust_owned` plans with `rust_daemon_core_model_stream_cancel` and `agentgres_model_stream_cancel_truth_required` evidence, commits the canceled `model-conversations` record through Agentgres model_mount record-state admission, and persists only the Rust-authored `model_invocation_stream_canceled` receipt. | Command-transport retirement, hosted stream parity, deeper wallet/cTEE invocation authority, and stable SDK/IDE stream lifecycle APIs remain non-terminal. |
| Model_mount catalog-status projection positive API | `catalogStatus()` now calls Rust model_mount read-projection kind `catalog_status` with runtime `state_dir`; Rust replays admitted `model-provider-inventory/*.json` Agentgres records, filters out JS-authored inventory, and emits Rust-authored catalog provider status, storage status, last-search summary, and result rows while the JS facade sends an empty request state without `catalog_status_input`. | Hosted/provider catalog transports, richer hosted catalog metadata/materialization, command-transport retirement, and stable SDK/IDE catalog APIs remain non-terminal. |
| Model_mount receipt-replay read projection positive API | `projectionSummary()`, `receiptReplay()`, `authoritySnapshot()`, `listProviderHealth()`, `latestProviderHealth()`, `latestVaultHealth()`, `latestRuntimeSurvey()`, `snapshot()`, and `projection()` now call Rust model_mount read-projection kinds with empty request state plus runtime `state_dir`; Rust replays admitted `receipts/*.json` records through the shared receipt projection boundary, rejects missing `state_dir`, emits `listProviderHealth()` from admitted `provider_health` receipts instead of a default empty list, and ignores caller-supplied `state.receipts` and `state.provider_health` so JS receipt or telemetry lists cannot become projection, replay, authority, health, survey, snapshot, or broad projection truth. | Richer Rust Agentgres topology joins, direct daemon-core projection APIs, command-transport retirement, and stable SDK/IDE projection APIs remain non-terminal. |
| Model_mount catalog-provider-control positive API | Public catalog-provider config list/get/write, private config/material resolution, and OAuth start/callback/exchange/refresh/revoke now call Rust daemon-core `plan_model_mount_catalog_provider_control`, receive Rust-authored catalog-provider-control records with wallet authority and cTEE custody evidence, commit only those records through Rust Agentgres model_mount record-state admission, and return committed Rust response envelopes. The old JS config/runtime-material/OAuth fail-closed facade no longer executes OAuth credential helpers, resolves vault refs, reads provider config/runtime-material maps as truth, writes OAuth/session/provider maps, or returns plaintext material. | Auth-header materialization, richer hosted catalog transport/materialization, command-transport retirement, and stable SDK/IDE catalog-provider APIs remain non-terminal. |
| Model_mount capability-token control positive API | Public capability-token create/list/authorize/revoke now call Rust daemon-core `plan_model_mount_capability_token_control`, receive Rust-authored `capability-tokens` records with wallet.network authority evidence, commit only those records through Rust Agentgres model_mount record-state admission, and return committed Rust response envelopes. Rust replays admitted `capability-tokens/*.json` records for list/authorize/revoke, while the committed record persists only token hashes and authority facts; the one-time token material is returned outside the persisted record. JS token maps, JS walletAuthority grant helpers, JS permission-token receipts, and direct token-map projection no longer authorize, mutate, or return accepted capability-token truth. | Deeper wallet authority policy, revocation epochs, command-transport retirement, and stable SDK/IDE capability-token APIs remain non-terminal. |
| Model_mount vault control positive API | Public vault bind/list/metadata/status/health/remove now call Rust daemon-core `plan_model_mount_vault_control`, receive Rust-authored `vault-refs` records with wallet.network authority and cTEE custody evidence, commit only those records through Rust Agentgres model_mount record-state admission, and return committed Rust response envelopes. Rust replays admitted `vault-refs/*.json` bind/remove records for list/metadata/status/health; committed records persist vault_ref_hash, material_hash, custody facts, and authority facts without plaintext material, and JS vault-port metadata/write helpers no longer author accepted vault truth. | Deeper cTEE material storage, provider auth materialization, command-transport retirement, and stable SDK/IDE vault APIs remain non-terminal. |
| Model_mount receipt-gate positive API | Receipt-gate validation now calls Rust daemon-core `plan_model_mount_receipt_gate`, passes canonical receipt facts and required tool receipt ids to Rust, receives a Rust-authored `workflow_receipt_gate` or `workflow_receipt_gate_blocked` receipt with receipt-binder and Agentgres gate evidence, and persists only that receipt through Rust Agentgres model_mount receipt-state admission. JS no longer computes gate failures or authors gate receipts. | Broader MCP/workflow execution, workflow policy composition, command-transport retirement, and stable SDK/IDE receipt-gate APIs remain non-terminal. |
| Model_mount runtime-engine control/projection positive API | Public runtime-engine selection/profile/remove mutations now call Rust daemon-core `plan_model_mount_runtime_engine`, receive Rust-authored `runtime-engine-controls` records with receipt refs and Rust runtime-engine evidence, commit only those records through Rust Agentgres model_mount record-state admission, and return Rust public responses that explicitly mark JS preference, profile, and projection writes false. Public runtime-engine list/profile/preference/default-load/detail reads now call Rust read-projection kinds with empty JS state plus runtime `state_dir`; Rust replays admitted `runtime-engine-controls/*.json`, filters out JS-authored controls, materializes runtime-engine projection truth from Rust-owned control records, and fails closed on missing detail only after Agentgres replay. The old runtime-engine required-record facade is removed from the hot path; missing Rust planning still fails closed before JS can write runtime-engine state. | Local runtime materialization, command-transport retirement, and stable SDK/IDE runtime-engine APIs remain non-terminal. |
| Model_mount runtime-survey capture positive API | Public `runtimeSurvey()` now calls Rust daemon-core `plan_model_mount_runtime_survey`; Rust replays runtime-engine state from Agentgres `state_dir`, captures Rust hardware facts, refuses LM Studio CLI execution, authors the canonical `runtime_survey` receipt with snake_case details and no JS-probe flags, and the JS edge must commit that Rust-authored receipt through Rust Agentgres model_mount receipt-state admission before returning public survey truth. The old fail-closed public survey facade, JS hardware probe path, JS runtime-engine read path, JS LM Studio survey path, JS `state.receipt("runtime_survey")` creation, and no-commit success path remain retired. | Local runtime materialization, richer private-workspace/cTEE survey policy, command-transport retirement, and stable SDK/IDE runtime-survey APIs remain non-terminal. |
| Model_mount backend-lifecycle positive API | Public backend health/start/stop now call Rust daemon-core `plan_model_mount_backend_lifecycle`, receive Rust-authored `model-backend-lifecycle-controls` records with lifecycle evidence, commit only those records through Rust Agentgres model_mount record-state admission, and return Rust public responses that explicitly mark JS backend-registry reads, process control, and log reads/writes false. Public backend list and backend log readback now call Rust read-projection kinds `backends` and `backend_logs` with empty topology state plus runtime `state_dir`; Rust replays admitted `model-backend-lifecycle-controls/*.json`, filters out JS-authored lifecycle controls and retired `logs_read` read-as-mutation controls, and materializes backend projection/log truth from Rust-owned control records. The old backend-lifecycle required-record facade, Rust required planner file, command operation, JS runner method, and lifecycle-control `logs_read` read path are removed from the hot path; missing Rust planning/projection still fails closed before JS backend registry, process, or log authority can run. | Actual backend process supervision/transport execution, richer backend process-state materialization, command-transport retirement, and stable SDK/IDE backend lifecycle APIs remain non-terminal. |
| Model_mount server-control positive API | Public server start/stop/restart/write, operation recording, and log append now call Rust daemon-core `plan_model_mount_server_control`, receive Rust-authored `model-server-controls` records with receipt refs and Rust server-control evidence, commit only those records through Rust Agentgres model_mount record-state admission, and return Rust public responses that explicitly mark JS state writes, JS log writes, and JS transport execution false. Public `serverStatus()` now calls Rust read-projection kind `server_status` with empty JS state plus runtime `state_dir`; Rust replays admitted `model-server-controls/*.json`, filters out JS-authored server controls, and materializes server status, last operation, last receipt, topology counts, and backend-state counts from Rust-owned records. Public `serverLogs()`, `serverEvents()`, and `serverLogRecords()` now call Rust read-projection kinds `server_logs`, `server_events`, and `server_log_records` with canonical `server_log_query` plus runtime `state_dir`; Rust replays admitted `model-server-controls/*.json`, filters JS-authored controls and retired read-as-mutation `logs_read`/`events_read`/`log_projection` records, and returns redacted log/event projections without committing a server-control record for reads. The old server-control required-record facade is no longer the hot path for migrated methods; missing Rust planning still fails closed before JS can write state or logs. | Actual process supervision/transport execution, command-transport retirement, and stable SDK/IDE server-control APIs remain non-terminal. |
| Public model storage/download control positive Rust path | `catalogImportUrl()`, `downloadModel()`, `cancelDownload()`, `deleteModelArtifact()`, and `cleanupModelStorage()` now call Rust daemon-core `plan_model_mount_storage_control`, receive Rust-authored `model-catalog-imports`, `model-downloads`, or `model-storage-controls` records with storage/download evidence, authority hashes, wallet/cTEE boundary facts, and Agentgres truth evidence, then commit only those records through Rust Agentgres model_mount record-state admission before public truth returns. `storageSummary()`, `listDownloads()`, and `downloadStatus()` now route through Rust read-projection kinds that replay admitted storage-control records and keep JS storage maps/filesystem scans out of public read truth. The old fail-closed catalog/download/storage JS facade, JS lifecycle receipts, JS download/artifact maps, local network/filesystem materializers, direct `writeMap()` storage truth, and no-commit planner success remain retired. | Richer filesystem custody and catalog/download materialization, command-transport retirement, and stable SDK/IDE storage/download APIs remain non-terminal. |
| Public model route-control positive API | Public route upsert/test, mounted route selection, explicit-model endpoint resolution, and runtime explicit/run-override model-route selection now call Rust daemon-core `plan_model_mount_route_control`, receive Rust-authored route, route-test, route-selection, or endpoint-resolution records plus receipt refs, commit only those records through Rust Agentgres model_mount record-state admission, and return or forward committed Rust route-control truth; Rust route selection reuses the Rust model_mount route-decision admission/accepted receipt authoring path before JS sees a selection. | Public route list and route-selection/endpoint-resolution projection/replay are Rust-owned; model invocation route selection, deeper wallet/cTEE route authority policy, command-transport retirement, and stable SDK/IDE route APIs remain non-terminal. |
| Public model route list projection positive Rust path | `listRoutes()` now calls the Rust model_mount read-projection command with projection kind `routes` and runtime `state_dir`; Rust replays persisted `model-routes/*.json` Agentgres records, filters public list truth to Rust-authored route-control records carrying `model_mount.route_control`, `model_mount_route_control_rust_owned`, `rust_daemon_core_route_control_plan`, `agentgres_route_truth_required`, and receipt refs, then returns the deterministic list. The public-list JS `routes` request state remains empty, so JS route maps cannot return as topology truth. | Command-transport retirement, deeper wallet/cTEE route authority policy, hosted/provider transports, and stable SDK/IDE route APIs remain non-terminal. |
| Public model route decision replay positive Rust path | `modelRouteDecisions()` now calls the Rust model_mount read-projection command with projection kind `model_route_decisions` and runtime `state_dir`; Rust replays persisted `model-route-selections/*.json` Agentgres records, filters public decision truth to Rust-authored route-selection records carrying `model_mount.route_control`, `model_mount.route_selection`, route-control evidence, receipt refs, and accepted-receipt binding, and the JS facade sends an empty request state so JS receipt arrays cannot synthesize route decisions. `modelRouteEndpointResolutions()` similarly replays `model-route-endpoint-resolutions/*.json` and filters endpoint-resolution truth to Rust route-control records. | Command-transport retirement, model invocation route selection, deeper wallet/cTEE route authority policy, hosted/provider transports, and stable SDK/IDE route APIs remain non-terminal. |
| Public model endpoint list projection positive Rust path | `listEndpoints()` now calls the Rust model_mount read-projection command with projection kind `endpoints` and runtime `state_dir`; Rust replays persisted `model-route-endpoint-resolutions/*.json` Agentgres records, filters public endpoint truth to Rust-authored route-control endpoint-resolution records, materializes canonical endpoint records with snake_case `provider_id` and `model_id`, and the JS facade sends an empty request state so JS endpoint maps cannot synthesize endpoint-list truth. | Command-transport retirement, deeper wallet/cTEE route authority policy, hosted/provider endpoint discovery/materialization, and stable SDK/IDE endpoint APIs remain non-terminal. |
| Public model provider lifecycle record positive Rust path | `providerHealth()`, `startProvider()`, and `stopProvider()` for migrated fixture/native-local providers now call Rust daemon-core `plan_model_mount_provider_lifecycle`, receive Rust-authored `model-provider-lifecycle-controls` records with lifecycle hashes, operation kinds, provider-lifecycle evidence, and Agentgres provider-lifecycle truth evidence, then require Rust Agentgres model_mount record-state commit before returning public lifecycle truth. The old plan-only success path, JS provider driver execution, JS lifecycle receipts, provider-map writes, and projection writes remain retired; hosted/custom provider lifecycle still fails closed before JS drivers can execute. | Provider lifecycle replay/projection over admitted records, hosted/provider transports, deeper receipt/state-root binding beyond record-state commit, command-transport retirement, and stable SDK/IDE provider-lifecycle APIs remain non-terminal. |
| Public model artifact/endpoint record positive Rust path | `importModel()`, `mountEndpoint()`, and `unmountEndpoint()` now call Rust daemon-core `plan_model_mount_artifact_endpoint`, receive Rust-authored `model-artifacts` or `model-endpoints` records with artifact/endpoint hashes, authority hashes, wallet/cTEE boundary facts, and Agentgres artifact/endpoint truth evidence, then require Rust Agentgres model_mount record-state commit before returning public artifact or endpoint truth. The old fail-closed artifact/endpoint JS facade, JS artifact/endpoint map mutation, JS lifecycle receipt synthesis, `writeMap("model-artifacts")`, `writeMap("model-endpoints")`, local materialization, and no-commit planner success remain retired. | Hosted/provider endpoint discovery/materialization, artifact/endpoint replay and read projection over admitted records, deeper receipt/state-root binding beyond record-state commit, command-transport retirement, and stable SDK/IDE artifact/endpoint APIs remain non-terminal. |
| Public model provider inventory record/replay positive Rust path | `listProviderModels()` and `listProviderLoaded()` now call Rust daemon-core `plan_model_mount_provider_inventory`, including fixture/local-folder, native-local, and hosted metadata providers, receive Rust-authored `model-provider-inventory` records with inventory hashes, operation kinds, provider-inventory evidence, and Agentgres provider-inventory truth evidence, then require Rust Agentgres model_mount record-state commit before returning public inventory truth. Hosted/nonlocal inventory uses Rust `rust_model_mount_hosted_provider_inventory`, records only canonical provider metadata/item refs, and marks hosted transport as not executed. `providerInventoryRecords()` calls Rust read-projection kind `provider_inventory_records` with runtime `state_dir`; Rust replays persisted `model-provider-inventory/*.json` records and filters truth to Rust-authored provider-inventory records. The old no-commit planner success, hosted fail-closed facade, JS driver execution, and JS artifact/instance materialization paths stay retired. | Hosted/provider transport execution, hosted/provider endpoint discovery/materialization, deeper receipt/state-root binding beyond record-state commit, command-transport retirement, and stable SDK/IDE provider-inventory APIs remain non-terminal. |
| Public model catalog-search projection positive Rust path | Public `catalogSearch()` now calls Rust model_mount read-projection kind `catalog_search` with canonical query state and runtime `state_dir`; Rust replays admitted `model-provider-inventory/*.json` records, filters truth to Rust-authored `list_models` provider-inventory records, and returns Rust-authored catalog-search entries bound to inventory record ids and inventory hashes. The old JS provider-port iteration, JS result aggregation, entry enrichment, and `lastCatalogSearch` write path remain retired. | Hosted/provider catalog transports, richer hosted catalog metadata/materialization, command-transport retirement, and stable SDK/IDE catalog APIs remain non-terminal. |
| Public provider-inventory topology/catalog materialization positive Rust path | `listArtifacts()`, `listProductArtifacts()`, `listProviders()`, `runtimeModelCatalogList()`, and `openAiModelList()` now call Rust model_mount read-projection kinds with runtime `state_dir`; Rust replays admitted `model-provider-inventory/*.json` records, filters out JS-authored inventory, materializes provider/artifact/runtime-catalog/OpenAI-list records from Rust fixture/native-local inventory, and keeps dedicated request state empty so JS topology maps cannot return as provider/catalog truth. | Hosted/provider endpoint discovery/materialization is still not terminal because provider-inventory records do not yet carry hosted endpoint truth; public endpoint lists are Rust-owned from route-control endpoint-resolution replay. Hosted/provider transports, richer catalog metadata/status, command-transport retirement, and stable SDK/IDE provider/catalog APIs remain non-terminal. |
| Public model-capability projection positive Rust path | `listModelCapabilities()` now calls Rust model_mount read-projection kind `model_capabilities` with empty request state plus runtime `state_dir`; Rust replays admitted `model-routes`, `model-route-endpoint-resolutions`, `model-provider-inventory`, and `model-instances` records, filters out JS-authored topology truth, and emits canonical `ioi.model-capability.v1` readiness contracts instead of a default empty placeholder or JS materializer. | Route-derived capability projection is Rust-owned for admitted local topology; richer hosted/provider materialization, wallet/cTEE authority binding, command-transport retirement, and stable SDK/IDE capability APIs remain non-terminal. |
| Public model tokenizer positive API | Public tokenize/count/context-fit utility facades now call Rust daemon-core `plan_model_mount_tokenizer`, consume the Rust route-selection record/accepted receipt from `model_mount.route.select`, receive Rust-authored token/count/context-fit records plus receipt refs, commit only those records through Rust Agentgres model_mount record-state admission, and return committed Rust tokenizer truth. `modelTokenizerRecords()` now calls Rust read-projection kind `model_tokenizer_records` with runtime `state_dir`; Rust replays persisted `model-tokenizer-utilities/*.json` records and filters truth to Rust-authored tokenizer/context-fit records with tokenizer and Agentgres truth evidence. The old required-record shim, JS context-window fallback, JS tokenization/context-fit receipts, route mutation, truncation, response-envelope shaping, and JS tokenizer projection path are retired from the hot path. | Receipt/state-root binding beyond record-state commit, deeper wallet/cTEE tokenizer policy, command-transport retirement, and stable SDK/IDE tokenizer APIs remain non-terminal. |
| Public model invocation positive Rust path | Public fixture/local-folder non-stream invocation and native-local stream invocation now call Rust-owned route selection records/receipts, provider-execution admission, provider invocation/stream execution, provider-result admission, accepted-receipt transition planning, receipt binding, StepModule projection, Agentgres operation/state-root binding, and Rust-authored receipt persistence through `model_mount_invocation_positive_rust_path`; the old public invocation fail-closed facade, JS provider driver execution, JS invocation receipt creation, route-state persistence, non-stream stream fallback, and provider-native request-shaping hook are retired from those migrated paths. | Hosted/OpenAI/Ollama/LM Studio transports, conversation-state and stream-completion projection, durable projection/replay, deeper wallet/cTEE invocation policy, command-transport retirement, and stable SDK/IDE invocation APIs remain non-terminal. |
| Public model conversation/stream-completion positive Rust path | Conversation-state writes call Rust daemon-core `plan_model_mount_conversation_state`, commit only the Rust-authored `model-conversations` record through Rust Agentgres model_mount record-state admission, and update the local continuation cache only after commit. Native stream completion calls Rust daemon-core `plan_model_mount_stream_completion`, commits only the Rust-authored conversation record, and persists only the Rust-authored `model_invocation_stream_completed` receipt with receipt_binder, accepted-receipt append, StepModule result, Agentgres operation/state-root/resulting-head bindings, and conversation/stream hashes. The old JS conversation write refusal path, JS stream-completion receipt synthesis, JS receipt-binding construction, direct `state.receipt("model_invocation_stream_completed")`, and `writeMap("model-conversations")` remain retired for these edges. | Command-transport retirement, hosted stream finalization parity, deeper wallet/cTEE conversation authority policy, and stable SDK/IDE conversation APIs remain non-terminal. |
| Public model conversation list projection positive Rust path | `listConversations()` now calls the Rust model_mount read-projection command with projection kind `model_conversation_states` and runtime `state_dir`; Rust replays persisted `model-conversations/*.json` Agentgres records, filters public list truth to records carrying `model_mount.conversation` boundary, conversation hash, Rust conversation-state or stream-completion evidence, and Agentgres conversation-truth evidence, then sorts and returns the list. The old fail-closed JS list facade, public-list `conversation_states` request input, and broad snapshot/projection `conversation_states: state.listConversations()` input remain retired. | Command-transport retirement, deeper wallet/cTEE conversation authority policy, hosted stream parity, and stable SDK/IDE conversation APIs remain non-terminal. |
| Public model load/unload positive Rust path | Public load/unload now calls Rust provider lifecycle planning, Rust instance lifecycle planning, and Rust Agentgres `model-instances` record-state commit before returning public instance truth. The canonical estimate-only now calls Rust instance-lifecycle estimate planning, skips provider lifecycle/JS sizing/JS driver execution, commits the Rust-authored estimate record through Rust Agentgres, and returns only that committed estimate truth. The old public load/unload edge refusal, JS provider driver execution, JS lifecycle receipt creation, JS estimate facade/sizing path, `commitModelInstanceRecordState`, direct `state.instances.set`, supersede-on-load mutation, and `writeMap("model-instances")` paths remain retired. | Public instance list projection/replay is Rust-owned; deeper receipt/state-root binding, command-transport retirement, richer load sizing/materialization, and stable SDK/IDE load/unload APIs remain non-terminal. |
| Public model instance maintenance positive Rust path | Loaded-instance idle eviction, duplicate coalescing, and explicit supersede now call Rust `plan_model_mount_instance_lifecycle`, require Rust-authored evict/supersede records with canonical snake_case metadata and `superseded_by` binding for supersede transitions, commit only those records through Rust Agentgres `model-instances` record-state admission, and update local projection cache only after commit. The old maintenance fail-closed facade, JS lifecycle receipts, direct pre-commit instance mutation, `writeMap("model-instances")`, and compatibility `supersededBy` metadata remain retired. | Public instance list projection/replay is Rust-owned; deeper receipt/state-root binding, command-transport retirement, and stable SDK/IDE maintenance APIs remain non-terminal. |
| Public model instance list projection positive Rust path | `listInstances()` now calls the Rust model_mount read-projection command with projection kind `instances` and runtime `state_dir`; Rust replays persisted `model-instances/*.json` Agentgres records, filters public list truth to Rust-authored instance lifecycle records carrying lifecycle hashes, `rust_model_mount_instance_lifecycle`, and `agentgres_model_instance_registry_planned` evidence, then returns the deterministic list. The public-list JS `instances` request state remains empty, so JS instance maps cannot return as topology truth. | Command-transport retirement, deeper receipt/state-root binding beyond record-state commit, hosted/provider transports, and stable SDK/IDE instance APIs remain non-terminal. |
| Public runtime memory read projection positive API | Public memory list, policy, path, status, and validation route projections now call Rust daemon-core `project_runtime_memory_projection` through the mounted thread-memory surface; JS only supplies temporary projection candidates and the route fails closed if the Rust projector is absent or mismatched. | Durable memory replay/projection storage, wallet/policy authority, cTEE-coupled private memory custody, direct memory admission/storage APIs, and direct protocol APIs remain non-terminal. |
| Conversation-artifact read/control positive API | Public and thread-scoped conversation-artifact list, get, and revision route projections now call Rust daemon-core `project_runtime_conversation_artifact_projection` through the mounted conversation-artifact surface; create/action/export/promote now call Rust daemon-core `plan_runtime_conversation_artifact_control` and commit only the Rust-authored artifact through Rust Agentgres artifact-state admission. JS only supplies temporary artifact candidates and canonical request fields, fails closed when Rust planning/projection/commit is missing or mismatched, and direct `ConversationArtifactStore` mutation writers stay retired. | Durable ArtifactRef/PayloadRef admission, richer Agentgres-backed artifact replay/projection storage, wallet/cTEE authority where needed, command-transport retirement, and stable SDK/IDE artifact APIs remain non-terminal. |
| Runtime managed-session projection/control positive API | Public managed-session inspection now calls Rust daemon-core `project_runtime_managed_session_projection`, and public managed-session control now calls Rust daemon-core `plan_runtime_managed_session_control` before admitting only the Rust-authored `managed_session.controlled` runtime event. JS supplies temporary canonical session candidates and request facts, validates Rust projection/event identity, ignores retired control aliases, and keeps runtime-bridge inspect/control dispatch and JS result-envelope authorship retired. | Durable Agentgres-backed managed-session storage/replay/projection, wallet/cTEE session authority, command-transport retirement, and stable IDE/CLI/SDK APIs remain non-terminal. |
| Runtime workspace-change projection/control positive API | Public workspace-change inspection now calls Rust daemon-core `project_runtime_workspace_change_projection`, and public workspace-change accept/reject/rollback control now calls Rust daemon-core `plan_runtime_workspace_change_control` before admitting only the Rust-authored `workspace_change.controlled` runtime event. JS supplies temporary canonical workspace-change candidates and request facts, validates Rust projection/event identity, ignores retired control aliases, and keeps runtime-bridge inspect/control dispatch, request-hash/receipt synthesis, fixture fallback projection, and JS result-envelope authorship retired. | Durable Agentgres-backed workspace-change storage/replay/projection, wallet/workspace rollback authority, command-transport retirement, and stable IDE/CLI/SDK APIs remain non-terminal. |
| Runtime thread-fork control positive API | Public thread fork now calls Rust daemon-core `plan_runtime_thread_fork_control`, receives a Rust-authored forked agent, forked thread projection, and `thread.forked` runtime event, commits only the forked agent through Agentgres-backed `writeAgent`, validates the Rust projection, and admits only the Rust-authored event. JS supplies temporary canonical source thread/agent facts and request fields, ignores retired aliases, and keeps `createAgent`, JS event synthesis, runtime-event idempotency lookup, and JS receipt/policy evidence retired. | Durable Agentgres-backed thread-fork storage/replay/projection, wallet/cTEE fork authority, command-transport retirement, and stable IDE/CLI/SDK APIs remain non-terminal. |
| Public approval control Rust-owned | Rust `approval.rs` owns approval manifest planning, coding-tool approval satisfaction projection, approval satisfaction planning, approval block result/event materialization, public approval queue projection, and approval request/decision/revoke command response shaping. Public decision/revoke control now calls Rust `authorize_approval_decision` before state planning; Rust requires wallet.network grant refs and authority receipts, emits an authority hash/record, and the decision/revoke state planners fail closed without that binding before any JS commit can persist. JS command/env fallback, request/decision/lease scanning, approval satisfaction projection, approval readback helpers, direct runtime-event append, and the JS approval block facade are retired. | Approval request/grant issuance semantics and the approval-state runner transport still need direct Rust authority/projection APIs beyond migration scaffolding. |
| Public approval queue/read projection Rust-owned | `project_approval_queue` is a Rust daemon-core operation; Rust now derives pending/resolved approval queue records from run/agent approval projections, filters resolved records unless requested, emits canonical snake_case request/decision/lease/receipt fields, and the public `GET /v1/threads/:thread_id/approvals` route calls `approvalSurface.listThreadApprovals()` through the direct-invoker approval-state runner instead of any JS event/readback helper. | Approval authority projection/replay storage and the approval-state runner transport still need direct Rust ownership beyond migration scaffolding. |
| Coding-tool approval satisfaction positive API | `project_coding_tool_approval_satisfaction` and `plan_coding_tool_approval_satisfaction` are Rust daemon-core operations; Rust now projects approval request/decision/lease state from Agentgres-backed run/agent projections, attaches the manifest for satisfaction evaluation, selects revocation as the latest decision when present, and invocation can proceed past an approval manifest only when Rust returns a satisfied record with approval receipts/policy refs. The JS store projection callback and exported JS manifest matcher are retired. | Remaining approval authority replay/projection APIs still need direct Rust ownership. |
| Coding-tool approval block positive API | `plan_coding_tool_approval_block` is a Rust daemon-core operation; unsatisfied approval now returns a Rust-shaped blocked coding-tool result/event envelope with approval refs instead of calling `blockCodingToolForApproval()`. | Approval-block persistence still needs direct Rust authority/admission ownership beyond the current result-event admission path. |
| Coding-tool budget block positive API | `plan_coding_tool_budget_block` is a Rust daemon-core operation in `policy/context_lifecycle.rs`; blocked budget execution now returns a Rust-shaped coding-tool result/event envelope with budget status, policy refs, receipt refs, and canonical snake_case fields, and the invocation hot path admits that blocked event through Rust `admit_coding_tool_result_event` before returning the policy error. The JS governance surface only forwards canonical request facts to Rust and remains fail-closed when the planner is absent. | The context-policy runner is still temporary command transport, and broader diagnostics/projection/replay work remains outside this cut. |
| Coding-tool budget recovery control positive API | `plan_coding_tool_budget_recovery_state_update` is a Rust daemon-core operation for `retry_approved`, and `plan_coding_tool_budget_recovery_control` is the Rust daemon-core operation for `request_approval` and `approve_override`; the public run route validates the Rust-authored operator control and run projection for each action, commits only that run through Agentgres-backed `writeRun`, requires wallet.network grant plus authority receipt refs for override authority, binds the Rust authority hash into the override projection, and removes the old admission-required command plus standalone JS policy/result helper module. | Retry-event materialization, durable replay/projection, deeper approval authority projection, and command-transport retirement remain non-terminal. |
| Coding-tool result envelope/context planning positive API | `plan_coding_tool_result_envelope` is a Rust daemon-core operation; normal success/failure execution requires a Rust-planned StepModule context before workload dispatch and a Rust-planned result `payload_summary` plus runtime-event candidate before Agentgres result-event admission. JS forwards canonical facts and validates the Rust plan instead of authoring StepModule context, source-event kind, payload summary, or result-event truth. | JS still coordinates artifact/snapshot materialization, diagnostics orchestration, runner transport, and some readback/projection adapter context. |
| Coding-tool result-event admission positive API | `admit_coding_tool_result_event` is a Rust daemon-core operation; successful, failed, and approval-blocked coding-tool result events require Rust Agentgres admission with receipt refs, expected heads, state roots, payload refs, storage admission, and projection watermarks before JS can register them for replay. | JS still coordinates diagnostics orchestration and some readback/projection adapter context. |
| Coding-tool command-stream admission positive API | `admit_coding_tool_command_stream_events` is a Rust daemon-core operation; Rust owns stream request evaluation, stdout/stderr chunking, command-stream event materialization, Agentgres storage admission, receipt/state-root binding, payload refs, and projection watermarks before JS can register stream events for replay. | JS still constructs StepModule/workload request context and coordinates diagnostics follow-up plus projection/readback adapters. |
| Coding-tool StepModule invocation construction Rust-owned | `run_coding_tool_step_module` now accepts canonical coding-tool request facts; Rust daemon-core owns the migrated tool contract table, input hashing, invocation id generation, authority/custody/backend fields, workload dispatch request construction, router admission, receipt binding, Agentgres admission, and projection record creation. JS no longer imports the coding-tool StepModule ABI builder or passes a `StepModuleInvocation` into the command. | JS still initiates temporary runner transport and coordinates diagnostics follow-up plus projection/readback adapters. |
| Coding-tool patch workspace snapshot capture Rust-owned | Rust `workspace_restore.rs` now emits snapshot capture records, snapshot artifact records, and snapshot runtime events with canonical ids, hashes, trigger context, receipt refs, artifact refs, restore metadata, and admission identity; `file.apply_patch` consumes that Rust output through the direct-invoker workspace restore runner, commits only the Rust snapshot artifact through Rust Agentgres artifact-state admission, admits only the Rust snapshot event through Rust Agentgres runtime-event admission, and no longer calls JS snapshot-event authoring or completes successfully when the Rust snapshot record/artifact/event is missing. | Diagnostics follow-up remains JS-coordinated, durable projection/replay storage remains non-terminal, and the runner is temporary migration transport. |
| Public workspace snapshot/restore API Rust-owned | `project_workspace_snapshot_list`, `project_workspace_snapshot_content_package`, `preview_workspace_snapshot_restore`, and `apply_workspace_snapshot_restore` are Rust daemon-core command responses in `workspace_restore.rs`; the public daemon surface now calls the direct-invoker workspace restore runner for list/content/preview/apply, commits only Rust-authored restore preview/apply artifact records through Rust Agentgres artifact-state admission, admits only Rust-authored restore preview/apply runtime events through Rust Agentgres event admission, and no longer derives snapshot projection truth from JS runtime events or `codingArtifacts`. | Agentgres-backed durable projection/replay storage and the temporary runner transport still need direct Rust ownership beyond this public restore admission cut. |
| Coding-tool workload observation contract canonicalized | Rust `coding_tool_workspace.rs` now emits snake_case result observations for `workspace.status`, `git.diff`, `file.inspect`, `file.apply_patch`, `test.run`, and `lsp.diagnostics`; the JS Rust-live result wrapper recursively strips retired camelCase observation keys instead of translating them, and coding-tool result summaries/output contracts read only canonical Rust result fields. | JS still coordinates post-edit diagnostics, projection, replay, and public readback adapters after forwarding canonical observation facts into the Rust result-envelope planner. |
| Post-edit diagnostics feedback plan Rust-owned | `plan_post_edit_diagnostics_feedback` is a Rust daemon-core operation; Rust owns post-edit diagnostics mode normalization, changed-path selection from canonical `changed_files`, repair-policy normalization, workspace snapshot and rollback refs, auto diagnostics `tool_call_id`, `diagnostics_repair_context`, and the `lsp.diagnostics` request envelope. The diagnostics feedback JS surface now fails closed without the Rust planner and only forwards the Rust-authored request to the mounted coding-tool invocation surface. | The actual `lsp.diagnostics` follow-up still runs through temporary JS runner transport, and diagnostics projection/replay/readback remain JS adapter scaffolding until Rust projection APIs replace them. |
| Runtime diagnostics repair policy projection positive API | `project_runtime_diagnostics_repair_policy` is a Rust daemon-core operation for diagnostics rollback repair policy projection; pending diagnostics feedback now sends runtime `state_dir` plus diagnostic event ids to Rust, Rust replays the admitted Agentgres runtime events to derive diagnostic status/count, rollback refs, snapshot refs, source tool-call refs, repair contexts, and deterministic injection id, and retired JS policy-input transport (`diagnostic_status`, `diagnostic_count`, `workspace_snapshot_refs`, `rollback_refs`, `source_tool_call_ids`, `diagnostics_repair_contexts`, `receipt_refs`) fails closed. The JS `diagnosticsRollbackRepairPolicy`, default-decision helper, context-policy aggregation facade, and diagnostics policy candidate transport are retired instead of kept as compatibility shims. | JS still selects temporary diagnostic event ids and coordinates runner transport; wallet-governed override authority, broader diagnostics orchestration, receipt/state-root binding, command-transport retirement, and stable SDK/IDE diagnostics APIs remain non-terminal. |
| Context-policy/state-update Rust planning | Context budget, coding-tool budget, compaction, context-compaction, operator control, run-cancel, thread lifecycle, MCP/memory, bridge thread/subagent, and lifecycle state-update planning use Rust response families and direct invoker only. | Public JS context/state facades remain protocol scaffolding. |
| Public context policy event APIs Rust-owned | Public `compactThread` now uses Rust `planContextCompaction` for the `context.compacted` event plan, admits only the Rust-planned event through Rust Agentgres runtime-event admission, uses Rust `planContextCompactionStateUpdate` bound to the admitted event id/seq, and commits only Rust-planned run or agent projections through Agentgres-backed `writeRun`/`writeAgent`; public thread/run context-budget and thread compaction-policy now require Rust policy planning, Rust-authored runtime-event identity, and Rust Agentgres runtime-event admission before returning route truth, with compaction-policy execution composing through the Rust-owned `compactThread` path when Rust approves compaction. | Direct Rust protocol APIs, durable replay/projection, richer policy receipts/state roots, and command-transport retirement remain non-terminal. |
| Public agent create positive API | Public agent creation now calls Rust `plan_agent_create_state_update`; JS gathers candidate provider/model-route/MCP/runtime-control facts only after the Rust planner boundary exists, requires a Rust-returned `agent.create` projection with complete identity/timestamps, commits only that projection through Agentgres-backed `writeAgent`, and keeps direct `agents` map mutation retired. | Wallet/cTEE policy, replay/projection, and direct Rust lifecycle APIs remain non-terminal beyond the temporary context-policy runner transport. |
| Public top-level thread create positive API | Top-level public thread creation now calls Rust `plan_thread_create_state_update`; JS gathers canonical agent/thread candidate facts only after the Rust planner boundary exists, requires Rust-returned `agent` and `thread` projections with matching identity, commits only the Rust-authored `thread.create` agent projection through Agentgres-backed `writeAgent`, projects the returned thread through the Rust thread/turn projection boundary, and remains fail-closed before route planning when the Rust planner is absent. | Wallet/cTEE policy, direct lifecycle protocol APIs, durable thread-create replay/projection storage, and the context-policy runner transport remain temporary. Runtime-service thread start is now a separate Rust bridge-start positive path. |
| Runtime bridge thread start positive API | Runtime-service thread creation now calls Rust `plan_runtime_bridge_thread_start_agent_state_update`; JS requires the Rust-planned bridge agent to carry canonical `runtime_profile`, `runtime_session_id`, `runtime_bridge_id`, and `runtime_bridge_source`, commits only that agent through Agentgres-backed `writeAgent`, returns the Rust thread projection, and still never dispatches the deleted JS runtime bridge `startThread` path. | Durable managed-session storage/replay/projection, wallet/cTEE runtime-service authority, durable bridge replay/projection, direct protocol APIs, and command-transport retirement remain non-terminal. |
| Runtime bridge thread control positive API | Runtime-service thread resume/control now calls Rust `plan_runtime_bridge_thread_control_agent_state_update`; JS requires the Rust-planned `thread.runtime_bridge.control` agent/control envelope, commits only that bridge-control agent through Agentgres-backed `writeAgent`, returns the Rust thread projection, and still never dispatches the deleted JS runtime bridge `controlThread` path. | Durable managed-session storage/replay/projection, wallet/cTEE runtime-service authority, durable bridge replay/projection, direct protocol APIs, and command-transport retirement remain non-terminal. |
| Runtime bridge turn submit positive API | Runtime-service turn creation now calls Rust `plan_runtime_bridge_turn_run_state_update`; JS builds only the temporary run candidate, obtains the Rust turn projection, requires the Rust-planned `turn.runtime_bridge.submit` run projection, commits only that run through Agentgres-backed `writeRun`, returns the Rust turn projection, and still never dispatches the deleted JS runtime bridge `submitTurn` path. | Durable managed-session storage/replay/projection, wallet/cTEE runtime-service authority, durable bridge replay/projection, direct protocol APIs, and command-transport retirement remain non-terminal. |
| Public run create positive API | Agent-scoped public run creation now calls Rust `plan_run_create_state_update`; JS gathers agent/provider/model-route/memory/run-candidate facts only after the Rust planner boundary exists, requires a Rust-returned `run.create` projection with complete identity/timestamps, commits only that projection through Agentgres-backed `writeRun`, keeps direct `runs` map mutation retired, ignores retired thread/approval and diagnostics request aliases, and remains fail-closed before lookup/route/memory when the Rust planner is absent. | Run replay/projection, wallet/cTEE policy, and direct Rust lifecycle APIs remain non-terminal beyond the temporary context-policy runner transport. |
| Public task create positive API | Public task creation now calls Rust `plan_runtime_task_job_create_state_update`; JS requires canonical `agent_id`, gathers the existing agent, model-route, memory, and run candidate only after the Rust planner boundary exists, and requires Rust-authored `task.create` task/job/checklist plus run projections before committing only the returned run through Agentgres-backed `writeRun`. Direct `createAgent`, `createRun`, JS task/job/checklist projection synthesis, retired request aliases, and projection mismatch compatibility paths remain retired. | Wallet/operator authority, durable task-create replay/projection storage, and direct Rust lifecycle APIs remain non-terminal beyond the temporary context-policy runner transport. |
| Public run cancel positive API | Public run cancellation now calls Rust `plan_run_cancel_state_update`; JS supplies only the current run and cancel timestamp, requires a Rust-returned `run.cancel` projection with canceled status, terminal job/run events, runtime task/job/checklist records, receipts, and artifacts, commits only that Rust-authored projection through Agentgres-backed `writeRun`, and keeps direct run-map mutation plus JS runtime task/job/checklist/event/receipt/artifact materialization retired. | Wallet/operator authority, cancellation replay/projection storage, and direct Rust lifecycle APIs remain non-terminal beyond the temporary context-policy runner transport. |
| Public task/job cancel positive API | Public task and job cancellation now call Rust `plan_runtime_task_job_cancel_state_update`; JS derives only the canonical run id from `task_`/`job_` public ids, supplies the current run and cancel timestamp, requires Rust-returned `task.cancel`/`job.cancel` projections with matching canceled task/job/checklist and run records, terminal events, receipts, and artifacts, then commits only the Rust-authored run through Agentgres-backed `writeRun`. Direct `cancelRun`, public-id fallback, JS task/job/checklist/event/receipt/artifact materialization, and projection mismatch compatibility paths are retired. | Wallet/operator authority, cancellation replay/projection storage, and direct Rust lifecycle APIs remain non-terminal beyond the temporary context-policy runner transport. |
| Public task/job read projection positive API | Public task/job list and get now call Rust `project_runtime_task_job_projection`; JS only gathers raw run candidates after the Rust projector boundary exists, forwards canonical `agent_id`, `status`, `task_id`, and `job_id`, and requires Rust-authored task/job records, filters, and public-id selection before returning public records. The task/job surface no longer receives JS `runtimeTaskRecordForRun`/`runtimeJobRecordForRun` builders, retired `agentId` aliases remain ignored, and missing/mismatched Rust projections fail closed instead of falling back to JS record synthesis. | Wallet/operator authority, durable read projection storage/replay, and direct Rust lifecycle APIs remain non-terminal beyond the temporary context-policy runner transport. |
| Public non-runtime thread resume/turn create positive API | Public non-runtime thread resume now enters the mounted Rust-planned agent status-control path and returns the Rust thread projection; public non-runtime turn creation now enters the mounted Rust-planned run-create path and returns the Rust turn projection. Direct `store.updateAgent()`, `store.createRun()`, JS turn projection composition, and runtime-event append remain retired from the thread-turn surface. | Diagnostics-blocked turn creation, durable managed-session storage/replay/projection, direct lifecycle protocol APIs, durable replay/projection storage, and command-transport retirement remain non-terminal. |
| Public agent status-control positive API | Public archive/unarchive/resume/close/reload now call Rust `plan_agent_status_state_update`; JS supplies the current agent and requested status facts, requires a Rust-returned agent projection plus matching operation kind, and commits only that Rust-authored projection through Agentgres-backed `writeAgent`. Missing planner output or mismatched operation kind fails closed before persistence. | Replay, projection, and broader lifecycle APIs remain temporary or fail-closed. |
| Public agent delete positive API | Public permanent delete now calls Rust `plan_agent_delete_state_update`; JS supplies the current agent fact, requires a Rust-returned `agent.delete` tombstone with `status: deleted` and `deletedAt`, commits only that tombstone through Agentgres-backed `writeAgent`, and keeps local removal/duplicate operation append retired. | Wallet/retention authority, deletion replay/projection, durable cache invalidation, and broader lifecycle APIs remain non-terminal. |
| Public thread runtime-control positive API | Rust `plan_thread_control_agent_state_update` now owns public mode/model/thinking and generic runtime-control state planning. The JS surface gathers canonical agent, control, event-sequence, and model-route facts, fails closed without the Rust planner before lookup, persists only the Rust-authored agent projection through Agentgres-backed `writeAgent`, and leaves direct control-event append retired under `thread-control-js-facade-retired`. | Model route selection still depends on the separate model_mount route authority surface; direct runtime-control event materialization, deeper workspace-trust wallet/cTEE authority enforcement, replay/projection storage, and the context-policy runner transport remain temporary. |
| Workspace-trust warning/ack positive API | Rust `plan_workspace_trust_control_state_update` now owns workspace-trust warning and acknowledgement event envelope planning. Thread mode updates require the Rust workspace-trust planner before lookup/write, persist only the Rust-authored thread-control agent projection, then admit Rust-authored workspace-trust warning events through `admit_runtime_thread_event`; acknowledgement routes replay admitted warning events through the Rust runtime-event replay path, require Rust-authored acknowledgement events with receipts, and admit only those Rust-authored events. The old JS warning/ack payload builders and repository-context warning record path remain retired. | Wallet/cTEE workspace authority is represented as Rust-owned receipt/authority refs but still needs deeper direct cTEE/wallet enforcement and stable protocol APIs beyond the temporary context-policy runner and replay cache transport. |
| Generic runtime thread-event admission positive API | `admit_runtime_thread_event` is a Rust daemon-core operation; generic runtime event append now requires Rust Agentgres admission with receipt refs, expected heads, state roots, storage admission, payload refs, and projection watermarks before JS can register the Rust-returned event for local replay. | JS still constructs the candidate event envelope and only keeps a temporary replay cache; synthetic thread-start projection, legacy run-event projection, public readback APIs, and runner transport still need direct Rust projection/replay ownership. |
| Runtime thread-event projection positive API | `project_runtime_thread_events` is a Rust daemon-core operation; Rust now authors synthetic `thread.started` and run-event projection envelopes from canonical agent/run facts, rejects retired projection request aliases, skips known idempotency keys, requires receipt refs, admits each projected event through Rust Agentgres admission, and returns only Rust-admitted event records for local replay registration. The thread-event surface no longer receives the JS `ttiEnvelopeForRunEvent` builder. | JS still gathers current agent/run facts and maintains a temporary replay cache; runner transport still needs direct Rust projection/replay ownership. |
| Runtime thread-event replay positive API | `project_runtime_thread_event_replay` is a Rust daemon-core operation; public stream/turn readback now sends temporary cache candidates to Rust, and Rust owns replay-kind selection, canonical `since_seq`/`last_event_id` cursor evaluation, required Agentgres admission refs, resulting state/head/watermark projection, and the returned event list. The JS stream/turn helpers no longer call `runtimeCursorSeq()` or filter by stream/turn locally, and they fail closed without the Rust replay API. | JS still maintains and supplies the temporary replay cache, and the runtime Agentgres runner is still migration transport until stable Rust projection/replay protocol APIs replace it. |
| Runtime thread/turn projection positive API | `project_runtime_thread_turn_projection` is a Rust daemon-core operation; public thread and turn records are Rust-authored projections over canonical agent/run facts and Rust replay events, including seq ranges, input/output item ids, usage envelope placement, runtime identity fields, projection hashes, and retired alias rejection. The JS `threadForAgent()` and `turnForRun()` helpers now fail closed without the Rust projection API instead of composing public records locally. | JS still supplies canonical agent/run/usage facts and invokes the temporary runtime Agentgres runner transport until stable Rust projection APIs replace it. |
| Public operator turn-control positive API | Public operator interrupt/steer now call Rust `plan_operator_interrupt_state_update` / `plan_operator_steer_state_update`, validate the Rust-planned operator-control run projection, resolve the live run through the mounted run resolver, persist only the Rust-planned run through Agentgres-backed `writeRun`, and return the Rust operator-control envelope. The old JS runtime-bridge control, JS event append, local run-map mutation, and no-planner success paths remain retired. | Command transport, wallet/runtime-control authority, durable replay/projection storage, deeper receipt/state-root binding, and stable SDK/IDE operator-control APIs remain non-terminal. |
| Public runtime account/node/tool catalog projection positive API | `project_runtime_tool_catalog` is a Rust daemon-core operation; public `/v1/account`, `/v1/runtime/nodes`, and `/v1/tools` now require the Rust projector through the mounted tool surface, return Rust-authored account/node/tool catalog records, keep canonical snake_case tool catalog entries, and reject projection mismatches. The old `RuntimeToolCatalogProjectionRequiredCore` command path is removed from the Rust policy owner, command protocol, command dispatch, JS runner, and mounted tool surface. | JS still supplies temporary request/environment facts and invokes the context-policy runner transport; direct Rust catalog storage/projection APIs and broader Agentgres-backed catalog persistence remain non-terminal. |
| Public skill/hook registry projection positive API | `project_skill_hook_registry` is a Rust daemon-core operation; public `/v1/skills`, `/v1/hooks`, and doctor skill/hook readiness now require the Rust projector through the mounted skill-hook surface, return Rust-authored public catalog records, and reject registry-kind mismatches. The old `SkillHookRegistryProjectionRequiredCore` command path is removed from the Rust policy owner, command protocol, command dispatch, JS runner, and mounted skill-hook surface. | JS still supplies temporary workspace/home request facts and invokes the context-policy runner transport; direct Rust catalog persistence/replay APIs and broader Agentgres-backed governance storage remain non-terminal. |
| Public repository workflow projection positive API | `project_repository_workflow` is a Rust daemon-core operation; public repository workflow routes now require the Rust projector through the mounted repository surface, return Rust-authored repository context, branch policy, GitHub context, issue context, PR attempt, review gate, and GitHub PR create-plan records, and reject projection-kind mismatches. The old `RepositoryWorkflowProjectionRequiredCore` command path is removed from the Rust policy owner, command protocol, command dispatch, JS runner, and mounted repository surface. | JS still supplies temporary workspace request facts and invokes the context-policy runner transport; durable Agentgres-backed repository workflow storage/replay, wallet authority for external exits, receipt/state-root binding, and direct Rust protocol APIs remain non-terminal. |
| Public lifecycle projection positive API | `project_runtime_lifecycle` is a Rust daemon-core operation; public agent, thread, run, agent-run, thread usage, thread turn/detail/event, run wait/conversation/usage/event/replay/trace/inspect/computer-use/scorecard/artifact list/detail routes now require the Rust projector through the mounted lifecycle surface, return Rust-selected lifecycle projections, and reject projection-kind mismatches. The old `RuntimeLifecycleProjectionRequiredCore` command path is removed from the Rust policy owner, command protocol, command dispatch, JS runner, and mounted lifecycle surface. | JS still supplies temporary Agentgres/cache request facts and invokes the context-policy runner transport; direct Rust run-read storage/replay APIs, wallet/cTEE authority on lifecycle exits, receipt/state-root binding, and stable IDE/CLI/SDK protocol APIs remain non-terminal. |
| Public memory projection positive API | `project_runtime_memory_projection` is a Rust daemon-core operation; public memory list, policy, path, status, and validation routes require the Rust projector through the mounted thread-memory surface, return Rust-selected memory projections, and reject projection-kind mismatches. The public read/status/policy/path/validation fail-closed JS facade is removed, while missing Rust projection still fails before JS `AgentMemoryStore` readback. | JS still supplies temporary memory projection candidates from the current store; direct Rust memory admission/storage/replay APIs, wallet/policy authority, receipt/state-root binding, and stable SDK/IDE memory APIs remain non-terminal. |
| Runtime MCP registry/control and serve positive API | `plan_mcp_control_agent_state_update` now owns MCP registry import/add/remove/enable/disable state planning plus status, validation, direct control-event state records, and live MCP transport-admission records for invocation/discovery. Public and thread-scoped MCP controls require the Rust planner, send only canonical request/server/tool/transport fields, receive Rust-authored registry counts/hash/control envelopes plus an agent projection, and persist only that projection through Agentgres-backed `writeAgent`; direct JS registry mutation, event append, `agents.set`, `listMcpServers`, `validateMcp`, JS transport invocation, and old fail-closed mutation/live facade behavior remain retired. MCP serve `tools/call` now requires Rust daemon-core `plan_runtime_mcp_serve_tool_call` to author the coding-tool request envelope before the protocol adapter calls the mounted Rust-owned coding-tool invocation surface; JS no longer derives served tool-call ids, idempotency keys, workflow ids, or `mcp_serve_request`, and the path fails closed when either Rust-owned boundary is missing instead of resolving thread agents or invoking JS thread-tool wrappers. | Actual MCP transport execution, wallet authority for external exits, transport containment, content receipts, replay/projection storage, and stable IDE/CLI/SDK protocol APIs remain non-terminal. |
| Model-mount MCP workflow positive API | `plan_model_mount_mcp_workflow` is a Rust daemon-core operation; model-mount MCP import, ephemeral registration, tool invocation, and workflow-node execution now require Rust-authored workflow/control records plus Rust Agentgres model_mount record-state commit before returning route truth. `mcp_servers` is a Rust model_mount read-projection kind over runtime `state_dir`, so server list readback replays admitted `mcp-servers` records and filters out legacy or JS-authored MCP truth. | Actual MCP transport execution remains non-terminal and still needs Rust wallet/containment authority, content receipts, richer workflow replay/projection, command-transport retirement, and stable IDE/CLI/SDK protocol APIs. |
| Public memory mutation/control positive API | `plan_runtime_memory_control` is a Rust daemon-core operation for public memory write, edit, delete, and policy mutation; public thread/agent memory controls require Rust planning, commit Rust-authored record/policy payloads through Rust Agentgres memory-state admission, refresh the temporary memory projection cache, and then return Rust-projected route output. Direct JS `AgentMemoryStore` `remember`/`updateRecord`/`deleteRecord`/`setPolicy` writers remain retired, missing Rust planning fails closed, and direct memory mutation helper remains fail-closed before JS event/state mutation. | JS still supplies current memory candidates and coordinates temporary runner/commit transport; wallet/policy authority, cTEE private-memory custody, durable replay/projection storage, command-transport retirement, and stable SDK/IDE memory APIs remain non-terminal. |
| Runtime memory status/validation control-event positive API | `recordThreadMemoryStatus()`, `validateThreadMemory()`, and `appendThreadMemoryControlEvent()` now call Rust daemon-core `plan_runtime_memory_control` for event-only `memory.status` and `memory.validate` planning, receive Rust-authored runtime-event payloads, and admit only those events through Rust runtime-event Agentgres admission. The old JS status/validation/direct event authorship facade no longer appends JS events, mutates agents, calls `writeAgent`, or synthesizes duplicate receipt/policy evidence when Rust planning is missing. | JS still supplies temporary current memory/projection/request facts and coordinates runner/admission transport; direct memory mutation helper remains fail-closed, and wallet/policy authority, cTEE private-memory custody, durable replay/projection storage, command-transport retirement, and stable SDK/IDE memory APIs remain non-terminal. |
| Runtime workflow-edit control positive API | `plan_runtime_workflow_edit_control` is a Rust daemon-core operation for public workflow-edit proposal and apply event planning; `proposeWorkflowEdit()` and `applyWorkflowEditProposal()` now require Rust-authored `workflow.edit_proposed`/`workflow.edit.apply` runtime events and admit only those events through Rust runtime-event Agentgres admission. The old JS proposal/apply facade no longer authors event envelopes, persists approval requests, builds proposal/apply truth envelopes, writes workflow JSON, or replays legacy `workflow.edit_applied` as accepted truth when Rust planning is missing. | JS still supplies temporary request/workflow facts and coordinates runner/admission transport; wallet approval authority, workflow mutation custody, durable workflow-edit projection/replay, ArtifactRef/PayloadRef binding where needed, command-transport retirement, and stable SDK/IDE workflow APIs remain non-terminal. |
| Runtime diagnostics repair decision control positive API | `plan_runtime_diagnostics_repair_control` is a Rust daemon-core operation for diagnostics repair decision execution and direct decision-executed event planning; `executeDiagnosticsRepairDecision()` and `appendDiagnosticsRepairDecisionExecutedEvent()` now require Rust-authored diagnostics repair runtime events and admit only those events through Rust runtime-event Agentgres admission. The old JS decision-execution/event facade no longer resolves accepted repair truth, appends JS runtime events, mutates run state, or persists repair execution truth when Rust planning is missing. | JS still supplies temporary request/gate/snapshot facts and coordinates runner/admission transport; wallet-governed repair policy authority, durable diagnostics repair projection/replay, receipt/state-root binding, command-transport retirement, and stable SDK/IDE diagnostics APIs remain non-terminal. |
| Runtime diagnostics repair decision projection positive API | `project_runtime_diagnostics_repair_projection` is a Rust daemon-core operation for diagnostics repair decision resolution; `resolveDiagnosticsRepairDecision()` now sends runtime `state_dir` instead of JS decision candidates, Rust replays admitted Agentgres runtime events for diagnostics repair decisions, validates projection kind/thread/decision/gate identity before returning accepted repair truth, and rejects retired `projection`/`decision`/`decisions`/`repair_decisions` candidate transport. The old fail-closed JS projection-retired facade marker is removed instead of preserved as compatibility scaffolding. | JS still coordinates temporary runner transport and supplies the runtime state-dir handle; wallet-governed repair policy authority, broader diagnostics repair policy/replay, receipt/state-root binding, command-transport retirement, and stable SDK/IDE diagnostics APIs remain non-terminal. |
| Runtime diagnostics operator override state-update/direct-event positive API | Diagnostics operator override execution now requires Rust `plan_diagnostics_operator_override_state_update` to derive operator-override approval state from canonical operator request, decision, and repair-policy context, reject retired JS approval verdict and authority transport, require wallet.network grant plus authority receipt refs for approval-required overrides, bind a Rust-authored diagnostics operator-override authority hash into the operator-control run projection, and then commit only that Rust-planned run through the Rust Agentgres run-state commit path. Direct operator-override event append now requires Rust `plan_runtime_diagnostics_repair_control` to author the `diagnostics.operator_override.event` runtime event, apply the same wallet.network authority gate, bind authority evidence into the payload, and admit only that Rust-authored event through Rust runtime-event Agentgres admission. The old JS operator-override facade no longer resolves accepted repair truth, derives approval verdicts or authority, appends operator override events, mutates run maps, or persists run state when Rust planning, wallet authority, or run-state admission is missing. | JS still supplies temporary canonical run/event/decision/authority-ref facts and coordinates runner/commit/admission transport; durable diagnostics repair projection/replay, command-transport retirement, and stable SDK/IDE diagnostics APIs remain non-terminal. |
| Runtime diagnostics repair retry run/event positive API | Diagnostics repair retry-turn creation now requires Rust `plan_runtime_diagnostics_repair_retry_run` before lookup and run creation, so Rust authors the retry run-create request plus retry-event request seed before JS executes the mounted Rust-owned run-create lifecycle surface and admits the Rust-authored `diagnostics.repair_retry.created` event through Rust runtime-event Agentgres admission. Direct retry-event append still requires Rust diagnostics repair retry-event planning and Rust runtime-event admission. The old JS retry facade no longer authors retry prompts/options/diagnostics feedback, calls `store.createRun()`, appends JS retry events, mutates run maps, or persists retry truth when Rust planning is missing. | JS still supplies temporary agent, request, gate, and snapshot facts and coordinates runner/admission transport; wallet-governed repair policy authority, durable diagnostics repair projection/replay, command-transport retirement, and stable SDK/IDE diagnostics APIs remain non-terminal. |
| Public conversation-artifact read/control positive API | `project_runtime_conversation_artifact_projection` and `plan_runtime_conversation_artifact_control` are Rust daemon-core operations; public and thread-scoped conversation-artifact list/get/revision routes require the Rust projector, and create/action/export/promote routes require the Rust planner plus Rust Agentgres artifact-state commit before returning route truth. The mounted surface sends only canonical request fields, rejects mismatched Rust plans/projections, and never calls direct JS `ConversationArtifactStore` create/action/export/promote writers. | JS still supplies temporary artifact candidates from the current store and coordinates runner/commit transport; durable ArtifactRef/PayloadRef admission, richer Agentgres-backed artifact replay/projection storage, wallet/cTEE authority where needed, command-transport retirement, and stable SDK/IDE artifact APIs remain non-terminal. |
| Public subagent read projection positive API | `project_runtime_subagent_projection` is a Rust daemon-core operation; public subagent list/get/result routes require the Rust projector through the mounted subagent surface, return Rust-selected subagent projections, and reject projection-kind mismatches. The public read fail-closed JS facade is removed, while missing Rust projection still fails before JS `subagents`/`runs` map readback. | JS still supplies temporary subagent/run candidates from the current store; direct Rust subagent admission/storage/replay APIs, StepModuleRouter delegation authority, wallet authority for delegation/cancellation, receipt/state-root binding, and stable SDK/IDE subagent APIs remain non-terminal. |
| Public subagent spawn control positive API | `plan_runtime_subagent_control` now owns `subagent.spawn`; public spawn preflights the Rust subagent control/state planner, composes Rust-owned child-agent creation and Rust-owned child run creation through the mounted agent/run lifecycle surface, admits the Rust-authored `subagent.spawned` runtime event, requires Rust subagent record state-update planning, and persists only the Rust-planned subagent projection through Agentgres-backed `writeSubagent`. The old spawn fail-closed JS facade is removed without restoring `store.createAgent()`, `store.createRun()`, direct subagent-map mutation, or JS-authored spawn event truth. | JS still supplies temporary parent/thread/run candidate facts and coordinates migration transports; direct Rust subagent delegation/execution admission, StepModuleRouter execution, wallet authority, durable replay/projection storage, receipt/state-root binding, command-transport retirement, and stable SDK/IDE subagent APIs remain non-terminal. |
| Public subagent wait control positive API | `plan_runtime_subagent_control` is a Rust daemon-core operation for `subagent.wait`; public wait control now requires the Rust control planner for the lifecycle event, Rust runtime-event Agentgres admission for that event, Rust subagent record state-update planning, and Agentgres-backed `writeSubagent` persistence before returning the Rust-authored result/projection. The old wait fail-closed JS facade is removed, and direct control-event append now enters the Rust-planned event admission path instead of JS event authorship. | JS still supplies temporary current subagent/run facts and coordinates migration transports; direct Rust subagent delegation/execution admission, StepModuleRouter execution, wallet authority, durable replay/projection storage, receipt/state-root binding, command-transport retirement, and stable SDK/IDE subagent APIs remain non-terminal. |
| Public subagent input/resume control positive API | `plan_runtime_subagent_control` now owns `subagent.input` and `subagent.resume` control-event planning; public input and resume controls require the Rust read projection, Rust-owned child-agent run creation through the agent/run lifecycle surface, Rust-authored runtime-event admission, Rust subagent record state-update planning, and Agentgres-backed `writeSubagent` persistence. The old input/resume fail-closed JS facades are removed without restoring `store.createRun()` or direct subagent-map mutation. | JS still supplies temporary current subagent/run facts and coordinates migration transports; direct Rust StepModuleRouter delegation/execution, wallet authority, durable replay/projection storage, command-transport retirement, and stable SDK/IDE subagent APIs remain non-terminal. |
| Public subagent direct assign/cancel control positive API | `plan_runtime_subagent_control` now also owns `subagent.assign` and `subagent.cancel` control-event planning; public assignment and cancellation controls require Rust-authored runtime-event admission, Rust subagent record state-update planning, and Agentgres-backed `writeSubagent` persistence. Public cancellation additionally composes with the Rust run-cancel state-update path before the subagent cancellation projection is committed. The old assign/cancel fail-closed JS facades are removed without restoring JS `store.cancelRun()` or direct subagent-map mutation. | JS still supplies temporary current subagent/run facts and coordinates migration transports; direct Rust StepModuleRouter delegation/execution, wallet authority, durable replay/projection storage, command-transport retirement, and stable SDK/IDE subagent APIs remain non-terminal. |
| Public subagent cancellation propagation positive API | `plan_runtime_subagent_control` now owns `subagent.cancel.propagate`; public parent cancellation propagation requires Rust subagent read projection for candidate selection, Rust propagated-cancel control planning, Rust run-cancel state planning, Rust runtime-event admission, Rust subagent record state-update planning, and Agentgres-backed `writeSubagent` persistence before returning the propagated/noop envelope. The old propagation fail-closed JS facade is removed without restoring JS `store.cancelRun()`, direct subagent-map mutation, or JS-authored propagation event truth. | JS still supplies temporary current subagent/run facts and coordinates migration transports; direct Rust StepModuleRouter delegation/execution, wallet authority, durable replay/projection storage, command-transport retirement, and stable SDK/IDE subagent APIs remain non-terminal. |
| Runtime subagent direct control-event positive API | `appendThreadSubagentControlEvent` now calls Rust daemon-core `plan_runtime_subagent_control` and admits only the Rust-authored event through Rust runtime-event Agentgres admission. The old direct append fail-closed JS facade no longer authors event payloads, synthesizes receipts/policy refs, mutates subagent records, or falls back to JS event append when Rust planning is missing. | JS still supplies the temporary current subagent/control request facts and coordinates runner/admission transport; direct Rust subagent admission/storage/replay APIs, StepModuleRouter delegation/execution authority, wallet authority, durable replay/projection storage, receipt/state-root binding, command-transport retirement, and stable SDK/IDE subagent APIs remain non-terminal. |
| JS facade and alias retirement | Daemon-store pass-through wrappers, many compatibility aliases, local event writers, fixture selectors, provider JS drivers, and stale readback helpers have been deleted or fail closed. | Remaining `rust_core_required` surfaces should be collapsed into macro positive Rust APIs instead of producing more helper-level retirements. |

## Remaining Terminal Blockers

- Rust daemon core does not yet own every hot-path positive API. Many JS surfaces
  fail closed correctly, but fail-closed is not terminal ownership.
- StepModuleRouter dispatch, workload invocation construction, Agentgres
  admission, receipt/state-root binding, projection, and replay are not yet one
  Rust-owned execution spine for every route family.
- Approval-required coding-tool execution has Rust satisfaction, block planning,
  result-event admission, public approval request/decision/revoke control,
  public approval queue/read projection, public decision/revoke wallet authority
  binding, budget-block planning/admission, and coding-tool satisfaction
  projection, but approval request/grant issuance semantics and approval
  authority projection/replay still need direct Rust authority/admission
  ownership beyond temporary runner transport.
- Coding-tool invocation now requires Rust daemon-core result envelope/context
  planning before workload dispatch and result-event admission. Patch snapshot
  capture, public workspace snapshot/restore APIs, public restore
  artifact/event admission, workload observation field ownership, and
  post-edit diagnostics feedback planning are Rust-owned, but diagnostics
  projection, replay, and temporary runner transport still need one Rust-owned
  execution spine.
- Model_mount provider and instance lifecycle still need direct Rust/Agentgres
  projection/replay APIs beyond the migrated local health/start/stop, public
  load/unload, instance-maintenance planning/commit, provider-inventory
  record/replay, provider-inventory topology/catalog materialization edges, and
  positive runtime-engine preference/profile control/projection records, public
  backend-lifecycle control records, public catalog/download/storage-control
  records, storage/download read projection over admitted records,
  runtime-engine read projection over admitted controls, and catalog-provider
  OAuth read projection over admitted controls, plus receipt-backed
  projection/authority/health/survey/snapshot envelopes over `receipts/*.json`
  replay; backend/server projection, conversation state, hosted/provider
  endpoint discovery/materialization, richer hosted catalog materialization, deeper wallet/cTEE route
  authority, hosted/provider transports, and direct protocol/API surfaces still
  need direct Rust daemon-core ownership beyond migration transport.
- Public agent create/thread create/run create/task create/run cancel/task-job cancel/archive/unarchive/resume/close/reload/delete and thread
  mode/model/thinking runtime controls are now Rust-planned and
  Agentgres-backed at the public surface, while generic runtime thread-event
  append plus synthetic thread-start/run-event projection now use Rust
  Agentgres admission/projection spines. Public stream/turn replay readback now
  uses Rust replay selection over Agentgres-admitted event records, and public
  thread/turn projection records are Rust-authored over those replay events.
  Deletion/cancellation replay/projection, direct runtime-control event
  materialization, deeper wallet/cTEE workspace-trust authority enforcement,
  diagnostics-blocked turn creation, durable task/job replay/projection storage,
  durable managed-session storage/replay/projection, wallet/cTEE
  managed-session authority, durable workspace-change storage/replay/projection,
  wallet/workspace rollback authority, durable thread-fork storage/replay/projection,
  wallet/cTEE fork authority, and broader run lifecycle still need Rust
  daemon-core ownership.
  Subagent spawn/wait/input/resume/assign/cancel and cancellation propagation controls now have Rust control-event planning,
  Rust runtime-event admission, Rust subagent state-update planning, and
  Agentgres-backed persistence; spawn also uses the Rust agent-create and run-create
  state-update paths, input/resume also use the Rust run-create
  state-update path, and cancellation/propagation also use the Rust run-cancel
  state-update path, and direct subagent control-event append now uses Rust
  control-event planning plus Rust runtime-event admission. Public memory
  write/edit/delete/policy controls now use Rust memory-control planning and
  Rust Agentgres memory-state commits, and memory status/validation/direct
  control-event append now uses Rust memory-control event planning plus Rust
  runtime-event admission, and workflow-edit proposal/apply now uses Rust
  workflow-edit event planning plus Rust runtime-event admission, and
  diagnostics repair decision execution/direct decision-event append now use Rust
  diagnostics repair event planning plus Rust runtime-event admission,
  diagnostics repair policy projection and decision resolution now use Rust
  diagnostics repair projection APIs, diagnostics operator override execution now
  uses Rust state-update planning plus Rust run-state
  admission, direct operator-override event append now uses Rust diagnostics
  repair event planning/admission, and diagnostics repair retry-turn
  creation/direct retry-event append now compose Rust run-create and diagnostics
  repair event planning/admission, while direct memory mutation, diagnostics
  durable diagnostics repair projection/replay, MCP surfaces, and deeper workflow mutation
  custody still include or need fail-closed Rust-owned boundaries.
- cTEE private workspace, worker/service package, L1 settlement, external
  capability, and governed-improvement runners are direct-invoker-only, but the
  JS runner facades still need replacement by stable Rust daemon-core APIs.
- IDE/CLI/SDK surfaces still need stable protocol APIs over Rust projection and
  replay records instead of depending on route-family JS adapter behavior.
- The temporary command transport must not become the terminal substrate.

## Do Not Recreate

The following paths are retired and must not be restored as compatibility
scaffolding:

- shared JS command-spawn helper:
  `packages/runtime-daemon/src/runtime-daemon-core-command-runner.mjs`
- StepModule command wrapper:
  `packages/runtime-daemon/src/step-module-command-runner.mjs`
- JS coding-tool approval satisfaction gate:
  `codingToolApprovalSatisfaction()`
- JS coding-tool approval satisfaction projection/matcher:
  `codingToolApprovalSatisfactionProjection()`,
  `codingToolApprovalManifestsMatch()`
- JS approval request readback facade:
  `latestApprovalRequestEvent()`
- JS approval block facade:
  `blockCodingToolForApproval()`
- command/env fallback selectors for migrated hot paths:
  `IOI_RUNTIME_DAEMON_CORE_COMMAND`, `IOI_RUNTIME_DAEMON_CORE_COMMAND_ARGS`,
  per-runner `*_COMMAND` envs, constructor `command`, and constructor `args`
- bridge-local command identity/dispatch wrappers deleted from
  `crates/node/src/bin/ioi_step_module_bridge`
- daemon-store pass-through wrappers that re-enter mounted surfaces as duplicate
  authority
- standalone JS fixture-policy compatibility wrapper:
  `packages/runtime-daemon/src/model-mounting/fixture-policy.mjs`;
  `fixture-policy.mjs` is absent, and disabled-internal-fixture cleanup remains
  private inside `default-discovery.mjs`
- retired internal fixture enable selector:
  `IOI_ENABLE_INTERNAL_FIXTURE_MODELS` no longer enables fixture seeding; the
  only explicit selector is `IOI_EXPOSE_INTERNAL_FIXTURE_MODELS`
- JS event scans, local file cache reads, fixture selectors, provider-map
  readbacks, or compatibility aliases that can author accepted truth

## Conformance Posture

Current conformance is proof that the current tier surface is internally
consistent, not proof that terminal migration is done.

Required checks after every macro cut:

| Check | Purpose |
| --- | --- |
| `node --check` on touched JS/conformance files | syntax and module integrity |
| focused `node --test` or Rust tests | local behavior and negative guards |
| `npm run hypervisor-conformance:docs` | docs/source-of-truth consistency |
| `npm run hypervisor-conformance:bridge` | bridge/direct-invoker/facade guards |
| relevant tier commands | receipts, cTEE, compositor, negative, or ABI as touched |
| `npm run hypervisor-conformance` | full current-tier proof |
| `git diff --check` | whitespace and patch hygiene |

Terminal acceptance still requires the master guide terminal conditions, not
just a green current-tier suite.

## Matrix Maintenance Rule

Do not append one section per implementation slice. Add or update a row only
when a macro authority boundary changes:

- a positive Rust daemon-core API replaces a fail-closed JS facade;
- a route-family truth path moves into Rust with admission, receipts,
  projection, replay, and conformance;
- a temporary command transport or JS facade is deleted for an entire family;
- a terminal blocker changes shape.

If future work needs detailed evidence, cite tests, conformance check IDs, and
git commits. Do not rebuild a slice-by-slice narrative inside this matrix.
