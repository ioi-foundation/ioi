# Hypervisor Kernel Substrate Migration Matrix

Status: compact implementation migration matrix.
Canonical owner: this file tracks live/current/final ownership for the
Hypervisor kernel substrate unification migration; doctrine remains owned by
the subject docs and the master guide.
Supersedes: ad hoc split-brain status notes for this migration when they
conflict with the route-family owner map below.
Superseded by: none.
Last alignment pass: 2026-06-15.
Last matrix pruning pass: 2026-06-15, after the direct typed workload API
StepModule runner-facade deletion cut, StepModule command-env selector deletion,
the Rust approval-lease authority cut with JS lease facade deletion,
the Rust Agentgres MCP live-result pending-transport fixture marker retirement,
and Rust approval-request authority issuance cut,
coding-tool approval-satisfaction JS gate retirement, the Rust approval
satisfaction/projection positive API cut, the Rust-owned coding-tool patch
snapshot capture cut, and the Rust-planned/Agentgres-admitted coding-tool
budget-block governance cut, the public coding-tool budget recovery retry
Rust-planning/Agentgres-commit cut, the budget-recovery request/override
Rust-control and wallet-authority cut, the diagnostics repair retry-run
Rust-planning cut, the context lifecycle typed Rust daemon-core API cut for
context-budget, coding-tool budget/block, compaction-policy, and context
compaction plus command-transport retirement, the runtime-control typed Rust
daemon-core API cut for coding-tool budget recovery, diagnostics operator
override, operator interrupt/steer, and run-cancel plus command-transport
retirement, the coding-tool budget recovery/operator-control/run-cancel
command-shaped Rust owner wrapper retirement, the coding-tool result envelope/context
Rust planning cut, the Rust-owned coding-tool artifact draft planning plus
Agentgres artifact-state commit cut, and the public thread runtime-control Rust planner
cut, the generic runtime thread-event Rust Agentgres admission cut, the direct
runtime thread-event admission cache-transport retirement cut, the coding-tool
event admission cache-transport retirement cut, and the runtime
thread-event projection Rust Agentgres cut, and the public runtime
thread-event replay positive API cut, and the public runtime thread/turn
projection positive API cut, and the public agent status-control Rust
planning/Agentgres commit cut, and the public agent delete Rust tombstone
planning/Agentgres commit cut, and the public agent create Rust
planning/Agentgres commit cut, and the public run create Rust
planning/Agentgres commit cut, and the public top-level thread create Rust
planning/Agentgres commit/projection cut, and the public non-runtime
resume/turn-create Rust lifecycle/projection cut, the diagnostics-blocked
turn-create Rust run-planning/projection cut, and the public task/job
cancel Rust planning/Agentgres commit cut, and the public task/job read
projection positive API plus state_dir replay/candidate-transport retirement
cut, and the public task create Rust
planning/projection/Agentgres commit cut, and the workspace-trust
warning/ack Rust planning plus runtime-event Agentgres admission cut, and the
public runtime account/node/tool catalog Rust projection positive API cut, and
the public skill/hook registry Rust projection positive API cut, the public
model_mount catalog-status Rust read-projection positive API cut, the public
memory route projection Rust positive API cut, the public conversation artifact
read projection Rust positive API cut, the public conversation-artifact control
Rust planning plus Agentgres artifact-state commit cut, and the runtime subagent
read projection Rust positive API cut, and the public subagent wait control Rust planning plus
Agentgres commit cut, the public subagent input/resume Rust run-create/control
planning plus Agentgres commit cut, the public subagent assign/cancel
direct-control Rust planning plus Agentgres commit cut, and the public subagent
spawn Rust agent-create/run-create/control planning plus Agentgres commit cut,
and the public subagent cancellation propagation Rust propagated-cancel/control
planning plus Agentgres commit cut, and the public runtime memory
write/edit/delete/policy Rust planning plus Agentgres memory-state commit cut,
and the public memory mutation current-record/policy `state_dir` replay cut,
and the public managed-session inspection/control Rust projection/planning plus
runtime-event admission cut, and the public workspace-change inspection/control
Rust projection/planning plus runtime-event admission cut, and the public
thread-fork Rust planning plus Agentgres commit/projection/runtime-event
admission cut, the public model load/unload Rust instance-lifecycle planning
plus Agentgres model-instance commit cut, and the public model-instance
maintenance Rust planning plus Agentgres model-instance commit cut, and the
public provider inventory Rust positive API cut, the public model tokenizer
record replay Rust positive API cut, and the public catalog-search Rust
projection cut, the public model-capability projection positive API cut, the
public runtime-engine control/projection positive API cut, the public
runtime-survey capture Rust planning plus Agentgres receipt-state commit cut, the public
backend-lifecycle control positive API cut, the public
provider-lifecycle record-state positive API cut, the public model
artifact/endpoint typed daemon-core API command-transport retirement cut, the public model
storage/download control positive API cut, the model_mount receipt-replay
read-projection state_dir authority cut, the public backend log read Rust
read-projection positive API cut, the public runtime-survey capture Rust
positive API cut, the public catalog-provider OAuth session/state Rust
read-projection replay cut, the OpenAI-compatible stream cancellation Rust
positive API cut, the public computer-use request-lease Rust StepModule
positive API cut, the L1 settlement state-root Rust derivation/client-truth
retirement cut, the MCP serve tool-result Rust projection cut, the runtime
MCP live-result Agentgres replay/projection cut, the MCP catalog search/fetch
Rust projection cut, the runtime MCP control/catalog direct daemon-core API
command-transport retirement cut, the MCP serve typed daemon-core API
command-transport retirement cut, the Rust daemon-core command-protocol module
deletion cut, the model_mount
invocation/provider/lifecycle/result typed daemon-core API command-transport
retirement cut, the runtime-service lifecycle agent-alias retirement cut, the
model_mount tokenizer/required-control typed daemon-core
API command-transport retirement cut, the model_mount conversation/stream typed daemon-core API
command-transport retirement cut, and the model_mount MCP workflow typed daemon-core API
command-transport retirement cut, the model_mount server-control typed
daemon-core API command-transport retirement cut, and the model_mount runtime-engine/survey typed daemon-core API command-transport retirement cut, the model_mount catalog/provider/capability/vault/receipt-gate typed daemon-core API command-transport retirement cut, the hosted provider metadata transport Rust materialization cut, the model_mount MCP workflow Rust result-payload materialization cut, the runtime MCP control Rust result-payload materialization cut, the runtime MCP catalog live-discovery Rust materialization cut, the runtime MCP control Rust driver-backend contract cut, the backend registry derivation/seeding JS facade deletion cut, the runtime MCP live/serve fallback-proof protocol deletion cut, the runtime MCP top-level route/client family retirement cut, the runtime-control state-event sequence cache transport retirement cut, the run-memory command Rust projection/control plus daemon-store memory wrapper deletion cut, the runtime task/job runner-injection alias deletion cut, the diagnostics repair runner-injection alias deletion cut, the workflow-edit runner-injection alias deletion cut, the run-cancel runner-injection alias deletion cut, the coding-tool budget recovery runner-injection alias deletion cut, the runtime tool catalog runner-injection alias deletion cut, the skill/hook registry runner-injection alias deletion cut, the repository workflow runner-injection alias deletion cut, the runtime lifecycle projection runner-injection alias deletion cut, the lifecycle admission route-fallback deletion cut, the thread-turn surface runner-alias deletion cut, the runtime subagent runner-wrapper deletion cut, the diagnostics repair surface runner-wrapper deletion cut, the runtime agent/run lifecycle helper runner-fallback deletion cut, the conversation-artifact surface runner-wrapper deletion cut, the runtime MCP serve store-core fallback deletion cut, the coding-tool artifact surface store-core fallback deletion cut, the coding-tool budget recovery surface store-core fallback deletion cut, the runtime task/job surface store-core fallback deletion cut, the runtime MCP single context-policy core mount deletion cut, the runtime context/memory auxiliary self-core plus planner-alias deletion cut, the runtime-service thread-turn bridge-adapter constructor alias deletion cut, and the model_mount read-projection JS facade deletion cut.
Slice 1324 additionally hard-deletes the runtime thread-control existing-model
compatibility fallback: canonical model-control input for Rust route selection
no longer reads persisted camelCase `existingModel` aliases (`routeId`,
`reasoningEffort`, `maxCostUsd`, `workflowGraphId`, or `workflowNodeId`) as
alternate model-route truth. Focused tests and conformance keep canonical
`route_id` / `workflow_node_id` input live while guarding that the retired
existing-control aliases cannot override the Rust-bound request.
Slice 1325 additionally hard-deletes the runtime thread-control top-level alias
truth path: initial/normalized controls, the public thread-control surface, and
the lifecycle fallback seed no longer emit `approvalMode` or `updatedAt`
runtime-control duplicates, runtime-backed turn request shaping scrubs
`threadMode` / `approvalMode` aliases before forwarding canonical
`thread_mode` / `approval_mode`, and focused tests plus conformance keep the
retired aliases poisoned but non-authoritative.

Slice 1267 additionally hard-deletes the model_mount MCP workflow fallback-proof
protocol shape: Rust no longer serializes the old JS proof, command fallback,
binary-bridge fallback, or compatibility fallback false fields for MCP server,
workflow execution, or result payload contracts, and JS/conformance reject
those retired fields if they return.
Slice 1268 additionally hard-deletes the hosted provider lifecycle/inventory
fallback-proof protocol shape: Rust no longer serializes the old JS/command/
binary-bridge/compatibility fallback false fields for provider metadata
transport contracts, and JS/conformance reject those retired fields if they
return.
Slice 1269 additionally hard-deletes the runtime MCP live/serve fallback-proof
protocol shape: Rust no longer serializes the old JS/backend/command/
binary-bridge/compatibility fallback false fields for MCP live-result,
backend-execution, receipt, Agentgres fixture, or served-tool result records,
and JS/conformance reject those retired fields if they return.
Slice 1270 additionally retires the runtime MCP top-level route/client family:
the public daemon no longer handles `/v1/mcp*`, the legacy model-mount daemon no
longer handles `/api/v1/mcp*`, SDK global MCP catalog/control clients are gone,
CLI live MCP aliases are deleted, and MCP status/search/fetch/serve projections
advertise only `/v1/threads/{thread_id}/mcp*` protocol routes.
Slice 1271 additionally retires the runtime doctor/readiness missing-core
compatibility fallback: `/v1/doctor` no longer synthesizes degraded tool,
runtime-node, or skill/hook readiness rows when Rust projection APIs are absent;
the mounted doctor aggregate now fails closed until Rust-authored projections
are available.
Slice 1272 additionally deletes the remaining runtime-service bridge-named
profile helper artifact: live daemon code now imports `runtime-profile.mjs`,
`runtime-api-bridge.mjs` and its test are absent, and conformance guards that
the retired bridge module filename cannot return as compatibility scaffolding.
Slice 1273 hard-cuts runtime doctor/readiness projection ownership into Rust:
`/v1/doctor` now calls typed
`daemonCoreRuntimeProjectionApi.projectRuntimeDoctorReport`, backed by Rust
`RuntimeKernelService::project_runtime_doctor_report`; the
`runtime-doctor-report.mjs` facade and test are absent; and conformance rejects
any return to the JS doctor aggregate, daemon-store doctor wrapper, or mounted
tool/skill surface composition inside the doctor route.
Slice 1274 hard-cuts Studio intent-frame routing ownership into Rust:
`/v1/studio/intent-frame` now calls typed
`daemonCoreRuntimeProjectionApi.projectStudioIntentFrame`, backed by Rust
`RuntimeKernelService::project_studio_intent_frame`; the
`studio-intent-frame.mjs` classifier facade and test are absent; and conformance
rejects any return to the JS resolver, daemon-store route wrapper, or retired
`executionMode` input alias passthrough.
Slice 1275 hard-cuts public computer-use discovery/provisioning projections into
Rust: `/v1/computer-use/providers` and `/v1/computer-use/browser-discovery` now
call typed `daemonCoreRuntimeProjectionApi.projectRuntimeComputerUse`, backed by
Rust `RuntimeKernelService::project_runtime_computer_use`; the
`computer-use-provider-registry.mjs` and `browser-discovery.mjs` facades/tests
are absent; and conformance rejects any return to those JS route dependencies or
retired camelCase request aliases.
Slice 1276 hard-cuts computer-use run materialization into Rust run-create
planning: JS now supplies only a canonical
`computer_use_materialization_request`, Rust `RunCreateStateUpdateCore`
materializes the computer-use trace/events/receipt/artifact during
`plan_run_create_state_update`, the deleted `computer-use-projection.mjs`
facade/test cannot return, and conformance rejects any JS-authored
`computerUse`/`computer_use_projection` candidate on the migrated run-create hot
path.
Slice 1277 retires the root `daemonCoreApi` compatibility mount for the
authority/governed-admission family: external capability authority, cTEE Private
Workspace, worker/service package admission, L1 settlement admission, and
governed-improvement proposal admission now accept only their explicit typed
daemon-core API handles, reject flat or nested `daemonCoreApi` fallbacks before
Rust invocation, and keep command/env fallback plus generic invoker paths
retired.
Slice 1278 retires the remaining root `daemonCoreApi` compatibility mount for
the approval, Agentgres, workspace restore, and context-policy cluster:
coding-tool approval, approval state, runtime Agentgres admission, workspace
restore/snapshot, and runtime context-policy now accept only their explicit typed
daemon-core API handles, reject flat or nested `daemonCoreApi` fallbacks before
Rust invocation, and keep command/env fallback plus generic invoker paths
retired.
Slice 1279 retires the mounted JS runtime bridge turn/control lifecycle facade:
public runtime-service resume and turn submission now call the direct Rust
thread-lifecycle adapter with typed `daemonCoreThreadLifecycleApi` planning,
Agentgres `writeAgent`/`writeRun` commits, and Rust thread/turn projections,
while `agentRunLifecycleSurface.createRuntimeBridgeThreadControl` and
`agentRunLifecycleSurface.createRuntimeBridgeTurn` are absent and conformance
guards that those mounted JS facade calls cannot return.
Slice 1287 hard-cuts runtime thread-event projection/replay cache transport:
projection and replay requests now send runtime `state_dir` and reject
caller-supplied projection `latest_seq`, `expected_head`,
`state_root_before`, `existing_idempotency_keys`, and replay `latest_seq`;
Rust derives head, state root, latest sequence, and idempotency from admitted
`events/*.jsonl` Agentgres records before projection or replay can return.
Slice 1288 hard-cuts runtime thread-event projection fact transport: projection
requests no longer carry JS `workspace_root`, `agent`, or `runs` facts; Rust
derives thread-start and run-event sources from admitted `agents/*.json` and
`runs/*.json` Agentgres records under `state_dir`, and conformance guards the
deleted JS projection helpers plus the rejected fact fields.
Slice 1289 hard-cuts runtime thread-event admission cache transport: direct
generic event admission no longer carries JS-derived `latest_seq`,
`expected_head`, or `state_root_before`; Rust requires runtime `state_dir`, reads
admitted `events/*.jsonl` Agentgres records, derives latest sequence/head/state
root before admission, and conformance guards the rejected cache fields plus the
absence of JS `latestRuntimeEventSeq()` authority in the migrated admission path.
Slice 1290 hard-cuts coding-tool event admission cache transport: result-event
and command-stream admission requests no longer carry JS-derived `latest_seq`,
`expected_head`, or `state_root_before`; Rust requires runtime `state_dir`, reads
admitted `events/*.jsonl` Agentgres records, derives latest sequence/head/state
root before admission, and conformance guards the rejected cache fields plus the
absence of JS `latestRuntimeEventSeq()` authority in both migrated coding-tool
admission paths.
Slice 1291 hard-cuts coding-tool duplicate-result replay out of the JS local
event cache: Rust result-event admission now replays an existing admitted event
with the same idempotency key from Agentgres `events/*.jsonl`, while the
invocation surface no longer reads `runtimeEventStream(...).idempotency` or
wires `codingToolInvocationResultFromEvent` before Rust admission. Conformance
guards the Rust replay path, focused Rust/JS tests, and the absence of the old
JS cache shortcut.
Slice 1292 hard-cuts pending diagnostics feedback off the JS local event cache:
`pendingDiagnosticsFeedbackForNextTurn()` now uses Rust runtime thread-event
replay through `runtimeEventsForStream(..., { since_seq: 0 })` before selecting
diagnostic completion events and fails closed if that Rust replay API is absent;
conformance guards the Rust replay client call and the absence of the old
`runtimeEventStream()` cache read.
Slice 1293 hard-cuts workspace-trust acknowledgement replay and sequencing off
the JS local event cache: workspace-trust warning/ack planning now sends only
runtime `state_dir` and canonical request facts to Rust, Rust replays admitted
`events/*.jsonl` records to resolve warning truth, Rust rejects restored
`events` candidate transport and caller-supplied `seq`, and the JS
workspace-trust state client no longer calls `runtimeEventsForStream()` or
`latestRuntimeEventSeq()` before Rust planning/admission.
Slice 1294 hard-cuts runtime-control state-event sequence cache transport:
thread-control, operator interrupt/steer, context-compaction planning, and
MCP-control state updates now send runtime `state_dir` and event-stream/thread
identity to Rust, Rust replays admitted `events/*.jsonl` records to derive the
latest sequence, Rust rejects caller-supplied `seq` or `previous_latest_seq`,
and the JS thread-control, thread-turn, context-policy, and MCP-control
surfaces no longer call `latestRuntimeEventSeq()` or send sequence authority
fields before Rust planning/admission.
Slice 1295 hard-cuts run-memory command resolution onto the Rust memory
projection/control spine: `resolveRunMemory()` now requires the mounted
thread-memory surface, reads policy/path/record truth through Rust public memory
projection, sends chat/API remember, edit, delete, enable, and disable commands
through Rust `plan_runtime_memory_control` plus Agentgres memory-state commit,
and fails closed before JS memory cache reads if that Rust surface is absent.
The same cut deletes the daemon-store memory pass-through wrappers for remember,
list, policy, path, edit/delete, status/validation, and direct memory
control-event append so migrated memory hot paths cannot re-enter through
store-level compatibility handles.
Slice 1296 hard-cuts runtime task/job runner injection aliases:
`createRuntimeTaskJobSurface()` no longer accepts `taskJobCreateRunner`,
`taskJobCancelRunner`, or `taskJobProjectionRunner`, and task create, task/job
cancel, and task/job read projection moved onto the Rust daemon-core task/job
planners/projector before Agentgres-backed run persistence or route projection.
Daemon construction no longer wires parallel task/job runner handles, and
conformance guards that the retired alias names cannot return; Slice 1314
removes the remaining store-mounted planner/projector fallback.
Slice 1297 hard-cuts diagnostics repair runner injection aliases:
`createRuntimeDiagnosticsRepairSurface()` no longer accepts
`diagnosticsRepairRunner`; diagnostics repair decision execution, direct
repair/override/retry event append, operator override state update, retry-run
planning, retry-result projection, decision projection, and repair policy
projection resolve only `store.contextPolicyCore` before entering the Rust
daemon-core diagnostics repair planners/projectors. Daemon construction no
longer wires a parallel diagnostics repair runner handle, focused tests mount
fake Rust planners/projectors only under `store.contextPolicyCore`, and
conformance guards that the retired alias name cannot return.
Slice 1298 hard-cuts workflow-edit runner injection aliases:
`createRuntimeWorkflowEditSurface()` no longer accepts `workflowEditRunner`;
public workflow-edit proposal and apply controls resolve only
`store.contextPolicyCore` before Rust daemon-core workflow-edit control
planning and Rust runtime-event admission. Daemon construction no longer wires
a parallel workflow-edit runner handle, focused tests mount fake Rust planners
only under `store.contextPolicyCore`, and conformance guards that the retired
alias name cannot return.
Slice 1299 hard-cuts run-cancel runner injection aliases: `cancelRun()` no
longer reads `state.runCancelRunner`; cancellation state planning and
admission-required refusal shaping now resolve through the Rust daemon-core
mount that the auxiliary surface passes explicitly before Agentgres-backed
`writeRun` persistence. Conformance guards that the retired runner alias cannot
return.
Slice 1300 hard-cuts coding-tool budget recovery runner injection aliases:
`createRuntimeCodingToolBudgetRecoverySurface()` no longer accepts
`codingToolBudgetRecoveryRunner`; retry completion, request-approval control,
and approve-override control moved onto the Rust daemon-core budget recovery
planners before wallet authority binding and Agentgres-backed `writeRun`
persistence. Daemon construction no longer wires a parallel budget recovery
runner handle, and conformance guards that the retired alias name cannot
return; Slice 1313 removes the remaining store-mounted planner fallback.
Slice 1301 hard-cuts runtime tool catalog runner injection aliases:
`createRuntimeToolSurface()` no longer accepts `toolCatalogRunner`; account,
runtime-node, and tool catalog projections mount the positive
`contextPolicyCore` API directly before Rust daemon-core catalog projection.
Daemon construction no longer wires a parallel tool catalog runner handle,
focused tests mount fake Rust projectors only through `contextPolicyCore`, and
conformance guards that the retired alias name cannot return.
Slice 1302 hard-cuts skill/hook registry runner injection aliases:
`createRuntimeSkillHookSurface()` no longer accepts `skillHookRunner`;
catalog, skills, and hooks projections mount the positive `contextPolicyCore`
API directly before Rust daemon-core registry projection. Daemon construction
no longer wires a parallel skill/hook runner handle, focused tests mount fake
Rust projectors only through `contextPolicyCore`, and conformance guards that
the retired alias name cannot return.
Slice 1303 hard-cuts repository workflow runner injection aliases:
`createRuntimeRepositorySurface()` no longer accepts `repositoryRunner`;
repository workflow projections mount the positive `contextPolicyCore` API
directly before Rust daemon-core repository projection. Daemon construction no
longer wires a parallel repository runner handle, focused tests mount fake Rust
projectors only through `contextPolicyCore`, and conformance guards that the
retired alias name cannot return.
Slice 1304 hard-cuts runtime lifecycle projection runner injection aliases:
`createRuntimeLifecycleProjectionSurface()` no longer accepts
`lifecycleRunner`; public lifecycle projections mount the positive
`contextPolicyCore` API directly before Rust daemon-core Agentgres replay
projection. Daemon construction no longer wires a parallel lifecycle runner
handle, focused tests mount fake Rust projectors only through
`contextPolicyCore`, and conformance guards that the retired alias name cannot
return.
Slice 1305 hard-cuts the route-level lifecycle admission fallback:
public agent/thread create routes and native agent status/delete/run-create
routes no longer accept a `lifecycleAdmissionRunner` handler option or fall
back through `store.contextPolicyCore ?? lifecycleAdmissionRunner`; those route
families pass only `store.contextPolicyCore` into the direct Rust-backed
lifecycle functions, and conformance guards that the route fallback cannot
return.
Slice 1306 hard-cuts thread-turn surface runner aliases:
`createRuntimeThreadTurnSurface()` no longer accepts
`threadLifecycleRunner`, `threadTurnAdmissionRunner`, or
`operatorTurnControlAdmissionRunner`; runtime-service resume/turn submit,
public non-runtime resume/turn create, and operator interrupt/steer planning
all resolve through the single positive `contextPolicyCore` mount, and
conformance guards that the retired aliases cannot return.
Slice 1307 hard-cuts runtime subagent runner wrappers:
`createRuntimeSubagentSurface()` no longer routes subagent projection/control
through `subagentProjectionRunner`, `subagentControlRunner`, or the
`store.contextPolicyCore ?? contextPolicyCore` fallback shape; subagent
list/get/result, spawn, wait, input, resume, assign, cancel, propagated cancel,
direct control-event append, and child lifecycle composition all resolve
through the single positive `contextPolicyCore` mount injected by daemon
startup, and conformance guards that the retired wrappers and fallback cannot
return.
Slice 1308 hard-cuts diagnostics repair surface runner wrappers:
`createRuntimeDiagnosticsRepairSurface()` no longer routes diagnostics repair
decision control, retry-run planning, retry-result projection, decision
projection, or operator-override state update through
`diagnosticsRepairControlRunner`, `diagnosticsRepairRetryRunRunner`,
`diagnosticsRepairRetryResultProjectionRunner`, `diagnosticsRepairProjectionRunner`,
`diagnosticsOperatorOverrideStateUpdateRunner`, or the
`store.contextPolicyCore ?? null` fallback shape. Decision execution, direct
decision/retry/operator event append, retry turn creation, retry result
projection, decision projection, and operator override execution all resolve
through the single positive `contextPolicyCore` mount injected by daemon
startup; diagnostics retry lifecycle composition passes that same core into the
direct Rust run-create path, and conformance guards that the retired wrappers
and fallback cannot return.
Slice 1309 hard-cuts runtime agent/run lifecycle helper runner fallbacks:
`createAgent()`, `createThread()`, `createRun()`,
`createRuntimeBridgeTurnRun()`, and `createRuntimeBridgeThreadControl()` no
longer accept per-operation state-update runner deps or recover through
`store.contextPolicyCore ?? null`; agent create, thread create, run create,
runtime-service bridge thread start/control, and runtime-service turn submit all
resolve through the explicit `lifecycleAdmissionRunner` dependency supplied by
the daemon route/surface caller, and conformance guards that the retired
per-operation runner deps plus store fallback cannot return.
Slice 1310 hard-cuts conversation-artifact surface runner wrappers:
`createRuntimeConversationArtifactSurface()` no longer routes artifact
create/action/export/promote control or list/get/revision projection through
`conversationArtifactControlRunner`, `conversationArtifactProjectionRunner`, or
the `store.contextPolicyCore ?? contextPolicyCore` fallback shape. Public and
thread-scoped conversation-artifact read/control routes resolve through the
single positive `contextPolicyCore` mount injected by daemon startup before Rust
control planning, Rust projection, and Agentgres artifact-state commit;
conformance guards that the retired wrappers and fallback cannot return.
Slice 1311 hard-cuts runtime MCP serve store-core fallback: MCP serve
`tools/call` planning, result projection, and live-result replay now resolve
only through the single positive `contextPolicyCore` mount supplied to
`createRuntimeMcpServeSurface()` by daemon startup. The surface and focused
tests no longer read or model `store.contextPolicyCore`, so served tool-call
truth cannot return through a store-mounted planner fallback; conformance guards
that the retired store-core fallback cannot return.
Slice 1312 hard-cuts coding-tool artifact surface store-core fallback:
artifact draft materialization and artifact read/retrieve projection now resolve
only through the single positive `contextPolicyCore` mount supplied to
`createRuntimeCodingToolArtifactSurface()` by daemon startup. The surface no
longer reads `store.contextPolicyCore ?? contextPolicyCore`, so draft records,
read projections, and result retrieval cannot return through a store-mounted
artifact planner/projector fallback; conformance guards that the retired
store-core fallback cannot return.
Slice 1313 hard-cuts coding-tool budget recovery surface store-core fallback:
retry-approved state update, request-approval control, and approve-override
control now resolve only through the positive `contextPolicyCore` mount
supplied to `createRuntimeCodingToolBudgetRecoverySurface()` by daemon startup.
The surface and focused tests no longer read or model `store.contextPolicyCore`
or `store.contextPolicyCore ?? null`, so budget-recovery run truth cannot return
through a store-mounted planner fallback; conformance guards that the retired
store-core fallback cannot return.
Slice 1314 hard-cuts runtime task/job surface store-core fallback: task create,
task/job cancel, and task/job list/get projection now resolve only through the
positive `contextPolicyCore` mount supplied to `createRuntimeTaskJobSurface()`
by daemon startup. The surface and focused tests no longer read or model
`store.contextPolicyCore` or `store.contextPolicyCore ?? null`, so task/job run
truth and read projection cannot return through a store-mounted
planner/projector fallback; conformance guards that the retired store-core
fallback cannot return.
Slice 1315 hard-cuts runtime auxiliary compositor store-core fallback:
managed-session projection/control, workspace-change projection/control, and
thread-fork control now resolve only through the positive `contextPolicyCore`
mount supplied to `createRuntimeThreadAuxiliarySurface()` by daemon startup.
The auxiliary surface passes that mount to the helper modules explicitly, and
the helper modules plus focused tests no longer read or model
`deps.contextPolicyCore ?? store.contextPolicyCore`, `store.contextPolicyCore`,
or `store?.contextPolicyCore`; conformance guards that the retired store-core
fallback cannot return.
Slice 1316 hard-cuts runtime context-policy surface store-core fallback:
`compactThread()`, thread/run context-budget event planning, and thread
compaction-policy event planning now resolve only through the positive
`contextPolicyCore` mount supplied to `createRuntimeContextPolicySurface()` by
daemon startup. The surface and focused tests no longer read or model
`store?.contextPolicyCore ?? contextPolicyCore`, `store.contextPolicyCore`, or
`store?.contextPolicyCore`; conformance guards that the retired store-core
fallback cannot return.
Slice 1317 hard-cuts runtime workflow-edit surface store-core fallback:
workflow-edit proposal and apply controls now resolve only through the positive
`contextPolicyCore` mount supplied to `createRuntimeWorkflowEditSurface()` by
daemon startup. The surface and focused tests no longer read or model
`store?.contextPolicyCore ?? null`, `store.contextPolicyCore`, or
`store?.contextPolicyCore`; conformance guards that the retired store-core
fallback cannot return.
Slice 1318 hard-cuts the thread-memory/lifecycle store-core fallback cluster:
the thread-memory surface is now constructed per daemon instance with the
positive `contextPolicyCore` mount supplied by startup, memory projection/control
runners resolve only that constructor mount, and `updateAgent()` / `deleteAgent()`
default their status/delete runners to `null` instead of
`store.contextPolicyCore ?? null`. Focused tests pass the Rust core explicitly,
and conformance guards that the retired memory surface and lifecycle helper
store-core fallbacks cannot return.
Slice 1319 hard-cuts the run-cancel state-core fallback: `cancelRun()` now
receives the positive `contextPolicyCore` mount explicitly from the auxiliary
surface and subagent cancellation composition. The cancellation helper and
focused tests no longer read or model `state.contextPolicyCore` or
`state?.contextPolicyCore`, and conformance guards that the retired state-core
fallback cannot return.
Slice 1320 hard-cuts the runtime MCP single context-policy core mount:
daemon startup now injects one positive `contextPolicyCore` into MCP catalog,
control, and serve surfaces; the MCP catalog/control surfaces no longer
self-create `RuntimeContextPolicyCore`; and `mcp-manager.mjs` requires an
explicit mounted core instead of constructing a duplicate core from
`daemonCoreMcpApi`. Registry, validation, catalog, status, search/fetch,
live-result, and serve truth cannot return through a manager/surface self-core
fallback, and conformance guards the daemon injection plus the retired self-core
defaults.
Slice 1321 hard-cuts runtime context/memory auxiliary self-core and planner
aliases: coding-tool invocation passes the daemon-owned `contextPolicyCore` into
budget-policy preflight; context-budget, coding-tool budget, and
compaction-policy helpers require explicit mounted runners instead of
self-creating `RuntimeContextPolicyCore`; workflow-only context-budget projection
also requires the constructor mount; coding-tool governance budget-block accepts
only `contextPolicyCore` and ignores the retired `codingToolBudgetBlockPlanner`
alias; thread-control defaults the constructor core to `null`; and
memory-manager status/validation helpers require an explicit daemon core.
Conformance guards the retired helper self-core defaults, planner alias, and
daemon injection path.
Slice 1322 hard-cuts runtime-service thread-turn bridge-adapter constructor
aliases: `runtime-thread-turn-surface.mjs` no longer accepts
`runtimeBridgeThreadControl` or `runtimeBridgeTurnRun` overrides; runtime-service
resume and turn submission call the direct Rust lifecycle adapters with the
daemon-mounted `contextPolicyCore`; focused tests mount fake Rust cores only
through `contextPolicyCore` and install throw-if-called retired aliases; and
conformance guards the absent constructor aliases plus direct adapter calls.
Slice 1323 hard-cuts the model_mount read-projection JS facade boundary:
`ModelMountingState` now calls the mounted `modelMountCore.planReadProjection()`
directly through `modelMountReadProjection()` for public model_mount readbacks,
canonical projection persistence, runtime-engine/catalog/server/backend/MCP/
conversation/topology projection reads, and not-found translations. The
standalone helper module is deleted, the focused proof calls mounted state
methods directly, route-family tests mount fake Rust cores only through
`modelMountCore`, and conformance guards the absent helper file/property plus
direct `modelMountReadProjection()` calls.
Slice 1226 additionally retired the runtime compositor/task-job command
transport family: task/job create/cancel/projection, workflow-edit control,
managed-session projection/control, workspace-change projection/control,
thread-fork control, conversation-artifact projection/control, and subagent
projection/control now use typed runtime-control/projection daemon-core APIs,
while Rust command-protocol source is deleted and conformance source-scans keep
the old command operations and dispatch wrappers absent.
Slice 1227 additionally retired the coding-tool result/artifact and
diagnostics-repair command transport family: coding-tool result envelope,
artifact draft/read projection, post-edit diagnostics feedback,
diagnostics-repair control/retry-run, diagnostics-repair decision projection,
and diagnostics repair policy projection now use typed runtime
control/projection daemon-core APIs, while Rust rejects the old command
operations and dispatch wrappers. Slice 1228 retires the remaining
`run_coding_tool_step_module` command transport, and Slice 1262 deletes the
temporary JS StepModule runner facade: the coding-tool invocation surface now
calls typed `daemonCoreWorkloadApi.runCodingToolStepModule` directly, Rust
exposes `RuntimeKernelService::run_coding_tool_step_module`,
`command_protocol.rs` is deleted and `pub mod command_protocol` is absent, and
conformance guards the deleted runner files. Slice 1233 deletes the retired
`ioi-step-module-bridge` binary and the
empty `ioi_step_module_bridge/mod.rs` tombstone; conformance now guards their
absence instead of accepting a fail-closed bridge artifact.
Slice 1234 deletes the remaining Rust service-kernel command-dispatch transport
module: `crates/services/src/agentic/runtime/kernel/command_dispatch.rs` is
absent, `kernel/mod.rs` no longer exports `command_dispatch`, and Slice 1285
deletes `command_protocol.rs` instead of preserving the empty retired-operation
catalog as terminal substrate.
Slice 1235 retires the daemon-wide generic `daemonCoreInvoker` pass-through:
`startRuntimeDaemonServiceWithStore()` and `AgentgresRuntimeStateStore` fail
closed if the top-level option is supplied, and the coding-tool approval policy
factory constructs the default core only from typed `daemonCoreApprovalApi`
instead of forwarding a generic invoker handle.
Slice 1236 binds MCP serve `tools/call` public result truth to Rust-authored
Agentgres live-result replay: Rust emits materialized `runtime.mcp_serve`
`ioi.runtime.mcp-live-result.v1` records with protocol payloads and no-fallback
facts, JS must commit/replay those records before JSON-RPC return, and Rust
replay accepts `runtime.mcp_serve` while continuing to filter JS-authored live
results.
Slice 1237 hard-cuts hosted provider lifecycle/inventory metadata transport out
of the old refusal marker: Rust now emits contained provider metadata transport
contracts with `rust_materialized` execution status, cTEE no-plaintext custody,
wallet.network transport authority, and no retired JS/command/binary-bridge/
compatibility fallback proof fields; the JS protocol edge rejects the retired
`hosted_provider_transport_not_executed` evidence marker before public truth can
return.
Slice 1238 hard-cuts model_mount MCP workflow execution out of the admitted-but-
pending result lane: Rust now materializes deterministic protocol result payloads
and hashes for MCP tool invocation and workflow-node execution receipts,
StepModuleRouter results carry those hashes, JS rejects stale pending
materialization, and receipt/store guards require the Rust materialized result
binding before public execution truth or receipt persistence can return.
Slice 1239 hard-cuts runtime MCP control live invoke/discovery exits out of the
admitted-but-pending transport-result lane: Rust now materializes deterministic
`ioi.runtime.mcp-live-result-payload.v1` protocol payloads and hashes, binds the
hash through the live-exit receipt, control record, Agentgres live-result record,
and replay path, and JS rejects the retired pending Rust transport marker before
result-state commit or public truth can return.
Slice 1240 hard-cuts runtime MCP catalog live-discovery out of the deferred
projection lane: Rust now emits `rust_mcp_live_discovery_materialized` summaries
and `runtime_mcp_live_discovery_rust_materialized` evidence for declared catalog
rows, keeps the retired deferred marker false, and JS/conformance no longer
accept the deferred marker as live-discovery success truth.
Slice 1241 hard-cuts runtime MCP control live results out of the generic backend
materialization lane: Rust now embeds an
`ioi.runtime.mcp-backend-execution.v1` contract in MCP-control live payloads,
binds receipts/results/replay to `ioi_drivers::mcp::McpManager` plus
`ioi_drivers::mcp::transport::McpTransport`, stamps
`rust_driver_contract_bound`, and JS/conformance reject missing driver-backend
contracts before public live-result truth can return.
Slice 1242 hard-cuts runtime MCP control live results out of the planner-direct
terminal-result lane: the surface now requires
`daemonCoreMcpApi.executeRuntimeMcpLiveBackend`, requires
`runtime_mcp_live_backend_rust_driver_executed` evidence and
`rust_driver_executed` backend-execution observation details on the committed
result, and fails closed before receipt/result commit, replay, or agent write
if the backend executor is absent or unbound.
Slice 1243 wires the required MCP live-backend API to real Rust MCP process
I/O: `RuntimeAgentService::execute_runtime_mcp_live_backend()` validates the
typed backend execution request, calls the mounted `McpManager` for
`tools/call` or admitted live `tools/list`, records
`runtime_mcp_live_backend_actual_mcp_manager_io`, and `McpTransport` now
retains its spawned child instead of dropping the `kill_on_drop` process after
pipe extraction.
Slice 1244 closes the runtime MCP live-result receipt-order blocker:
`executeRuntimeMcpLiveBackend()` now runs before receipt/result state commits,
the Rust backend service returns executed control/receipt/result truth, the
actual driver-result hash is bound into the public result payload and recomputed
payload hash, and JS commits only that Rust-returned receipt/result pair before
replay and agent projection.
Slice 1264 additionally removes the stale `admitted_pending_rust_transport`
fixture truth from the Rust Agentgres MCP live-result state commit examples and
protocol tests: runtime MCP live-result commit fixtures now carry
`status: "rust_materialized"`, `result_materialized: true`,
`backend_materialization_status: "rust_driver_contract_bound"`, and no retired
command/binary/compatibility fallback proof fields, while conformance scans
the Agentgres Rust admission/protocol cores to keep the pending-transport marker
out of accepted live-result commit truth.
Slice 1245 closes broader runtime MCP serve admission: Rust
`RuntimeMcpServeToolCallPlanCore` requires wallet authority grant/receipt refs,
cTEE custody refs, and transport containment refs before served `tools/call`
planning; the Rust result projector binds those refs into the
`runtime.mcp_serve` live-result record, and JS commits/replays only that
Rust-authored result.
Slice 1246 closes the SDK/public-route MCP serve protocol gap: the advertised
`/v1/threads/{thread_id}/mcp/serve` route now reaches the mounted Rust-owned
MCP serve surface, stable SDK clients send `ioi.runtime.mcp-serve-client.v1`
protocol bodies with wallet authority refs, cTEE custody refs, and containment
refs, and admission refs no longer ride query-string transport where arrays
could collapse.
Slice 1247 closes the IDE MCP serve client split: React Flow MCP serve state
nodes no longer expose a configurable endpoint override or duplicate camelCase
protocol body fields. The IDE builder now emits the canonical
`/v1/threads/{thread_id}/mcp/serve` daemon protocol request with
`ioi.runtime.mcp-serve-client.v1`, body-carried allowed tools, wallet authority
grant/receipt refs, cTEE custody refs, and containment refs.
Slice 1248 closes the CLI MCP serve client split: the Rust CLI TUI now exposes
`/mcp serve` as a canonical daemon protocol client for
`/v1/threads/{thread_id}/mcp/serve`, emits
`ioi.runtime.mcp-serve-client.v1` bodies with allowed tools, wallet authority
grant/receipt refs, cTEE custody refs, containment refs, and a raw JSON-RPC
`tools/list` message, and carries no endpoint override, top-level
`/v1/mcp/serve`, or query-string admission fallback.
Slice 1249 retires the top-level MCP serve compatibility path: the public
daemon no longer handles `GET` or `POST /v1/mcp/serve`, the SDK no longer
exports the global `serveMcpRpc()` client or `RuntimeMcpServeRpcInput`, and
tests/conformance require MCP serve JSON-RPC to enter through the canonical
thread-scoped `/v1/threads/{thread_id}/mcp/serve` protocol route.
Slice 1250 retires the top-level runtime memory context route family: public
`/v1/memory*` query/body-context routes, daemon-store memory context helpers,
and SDK global memory status/validation clients are gone; explicit
`/v1/threads/{thread_id}/memory*` and `/v1/agents/{agent_id}/memory*` routes are
the protocol surface over Rust-owned memory projection/control records.
Slice 1251 hard-retires the RuntimeAgentService command/binary bridge
substrate and Slice 1272 deletes the bridge-named JS helper module:
`runtime-api-bridge.mjs` is absent, runtime profile normalization lives in
`runtime-profile.mjs`, the `ioi-runtime-bridge` Cargo bin is deleted, daemon startup rejects
`runtimeBridge`, Rust service policy no longer reads bridge command-env
overrides, Agent Studio launch/proof scripts use inference/model-route helper
APIs instead of the deleted helper, and stale retired runtime-service proof
scripts/tests are removed.
Slice 1252 retires the thread/run/subagent lifecycle command-shaped Rust owner
wrapper cluster: `policy/thread_lifecycle.rs` now exposes only direct
`*Core::plan()` request/record APIs for thread control, runtime bridge
thread-start/control/turn, subagent records, and agent/thread/run
create/status/delete; `ThreadLifecycleCommandError`, the lifecycle
`*BridgeRequest` structs, `plan_*_state_update_response` wrappers,
`rust_*_state_update_command` source markers, and bridge-shaped owner tests are
deleted. JS context-policy normalizers are typed API normalizers with
`rust_*_state_update_api` defaults, and conformance forbids the command
wrappers from returning.
Slice 1253 hard-cuts runtime thread-event replay off JS replay candidates:
Rust replay now requires runtime `state_dir`, reads admitted Agentgres
`events/*.jsonl` records itself, rejects caller-supplied replay `events`
transport, and the daemon passes only replay kind, cursor, latest seq, and
`state_dir` into the typed replay API. The old
`runtimeThreadReplayCandidateEvents` collector and public
`replayFromCanonicalState` run-read facade are deleted, and lifecycle run replay
now enters through `eventsForRun` over the mounted Rust thread-event replay
path.
Slice 1254 hard-cuts public lifecycle projection off JS cache candidates:
Rust `project_runtime_lifecycle` now requires runtime `state_dir`, replays
admitted `agents/*.json`, `runs/*.json`, and `events/*.jsonl` records itself,
derives agent/thread/run/turn/event/replay/usage/trace/artifact projections from
those Agentgres records, and rejects caller-supplied lifecycle candidate fields
such as `agents`, `runs`, `events`, `replay`, `usage`, `trace`, and `artifact`.
The daemon lifecycle surface passes only route identifiers plus `state_dir`; it
no longer calls JS agent/run maps, thread/turn helpers, usage helpers, event
streams, replay helpers, trace helpers, or artifact resolvers before invoking
Rust.
Slice 1255 folds the remaining public usage and authority-evidence read edges
into that Rust lifecycle projector: Rust projection kinds `usage_list` and
`authority_evidence_summary` derive top-level usage and authority/preflight
evidence from admitted Agentgres `runs/*.json` and `events/*.jsonl` records,
public `/v1/usage`, `/v1/authority-evidence`, and
`/v1/workflow-capability-preflights` call the lifecycle surface, and the old JS
authority summary helper plus run-read `listUsage` / `authorityEvidenceSummary`
facade are retired.
Slice 1256 retires the migrated authority-evidence native compatibility path:
`/api/v1/authority-evidence`, `/api/v1/authority-evidence-summaries`,
`/api/v1/workflow-capability-preflight-evidence`, and
`/api/v1/workflow-capability-preflight` are no longer routed. The canonical
Rust-owned daemon protocol surface for this family is `/v1/authority-evidence`
and `/v1/workflow-capability-preflights`, and conformance now guards that the
native aliases cannot return.
Slice 1257 retires the diagnostics repair retry result JS facade:
`RuntimeDiagnosticsRepairRetryResultProjectionCore` now projects retry result
envelopes through typed
`daemonCoreRuntimeProjectionApi.projectRuntimeDiagnosticsRepairRetryResult`,
the daemon requires that Rust projection API before JS agent lookup/run
creation, validates the complete Rust projection after event admission, rejects
partial or mismatched projections instead of locally filling fields, and the old
JS retry/operator result helpers plus stale daemon constructor wiring are
deleted. Rust rejects
`project_runtime_diagnostics_repair_retry_result` as command transport, and
conformance guards the positive API, missing/partial projection failure paths,
and absence of the deleted helpers.
Slice 1258 retires the IDE diagnostics repair compatibility body: React Flow
diagnostics repair nodes now send only canonical snake_case daemon protocol
fields for decision execution, the public diagnostics repair surface rejects
retired camelCase request aliases before Rust planning/admission, and
conformance guards both the canonical IDE body and daemon alias rejection.
Slice 1259 retires runtime-service lifecycle agent aliases: Rust bridge
start/control planning scrubs retired camel runtime-service identity fields,
emits only canonical snake_case projections, JS lifecycle normalizers reject any
aliased Rust response, daemon runtime identity/session helpers read snake_case
state only, and conformance guards the retired compatibility path.
Slice 1229 additionally retires the model_mount generic daemon-core invoker
shim: `ModelMountCore` rejects `daemonCoreInvoker`, stores only
`daemonCoreModelMountApi`, deletes `invokeDaemonCore()`, removes the command
schema marker from the mounted core, and `ModelMountingState` no longer forwards
the daemon-wide invoker into model_mount.

## Purpose

This matrix is the Phase 0 inventory for
[`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md).
It keeps each route family honest about current live authority, target owner,
truth path, conformance tier, and cleanup condition.

This file is no longer a per-slice evidence archive. Per-slice archaeology made
the migration optimize for token-maxing iteration instead of whole authority
cuts. The matrix now records macro boundaries only: what has changed ownership,
what remains temporary, and what must not be recreated.

Terminal status is not claimed here. The migration is open until
`hypervisor-conformance` passes and the terminal conditions in the master guide
are all true.

## Current Sprint Lane

The current implementation lane is Rust substrate migration plus JS facade
retirement. Current tier conformance is green, but the terminal migration is not
claimed. Future work must prioritize macro authority cuts over additional
bridge/fallback polish:

- replace a fail-closed JS facade with a positive Rust daemon-core API;
- move one route-family truth path into Rust, including authority, admission,
  receipts, projection, replay, and stable protocol shape;
- delete the JS facade or reduce it to product/API/IDE/SDK adapter behavior in
  the same cut;
- update conformance and this matrix once per macro cut, not once per helper.

The former `ioi-step-module-bridge` command path is retired for daemon hot
paths. It must not exist even as a fail-closed tombstone and must not regain
command transport, fallback execution, or compatibility-shim semantics.

## Route-Family Owner Map

| Route family | Current live status | Terminal owner | Next macro cut |
| --- | --- | --- | --- |
| `model-mounting/artifact-endpoints` | Public `importModel()`, `mountEndpoint()`, and `unmountEndpoint()` now call typed `daemonCoreModelMountApi.planModelMountArtifactEndpoint`, backed by Rust `RuntimeKernelService::plan_model_mount_artifact_endpoint`, receive Rust-authored `model-artifacts` or `model-endpoints` records with artifact/endpoint hashes, authority hashes, wallet/cTEE boundary facts, and Agentgres truth evidence, then require Rust Agentgres model_mount record-state commit before returning public artifact or endpoint truth. The old `plan_model_mount_artifact_endpoint` command operation, command-dispatch arm, bridge request/response wrapper, backend marker, and JS command-envelope builder are retired for this mutation family. Public `listArtifacts()` and `listEndpoints()` now call Rust read-projection kinds over runtime `state_dir`; Rust replays admitted `model-artifacts/*.json` and `model-endpoints/*.json` artifact-endpoint records, merges them with provider-inventory and route endpoint-resolution materializations, applies Rust endpoint unmount records as removal, and filters JS-authored artifact/endpoint truth. The old fail-closed artifact/endpoint JS facade, JS artifact/endpoint map mutation/readback, JS lifecycle receipts, `writeMap("model-artifacts")`, `writeMap("model-endpoints")`, local materialization, no-commit planner success, and JS-authored list truth remain retired for these public mutation/read edges. | Rust daemon core `model_mount` plus Agentgres record-state admission/projection/replay, wallet.network authority, cTEE custody, and stable protocol APIs. | Move hosted/provider endpoint discovery/materialization, deeper receipt/state-root binding, and stable SDK/IDE artifact/endpoint APIs over the Rust-owned artifact/endpoint records and projections. |
| `model-mounting/storage-download-controls` | Public `catalogImportUrl()`, `downloadModel()`, `cancelDownload()`, `deleteModelArtifact()`, and `cleanupModelStorage()` now call typed `daemonCoreModelMountApi.planModelMountStorageControl`, backed by Rust `RuntimeKernel::plan_model_mount_storage_control`, and receive Rust-authored `model-catalog-imports`, `model-downloads`, or `model-storage-controls` records with storage-control hashes, authority hashes, wallet/cTEE boundary facts, and Agentgres truth evidence before Rust Agentgres model_mount record-state commit returns public storage/download truth. `storageSummary()`, `listDownloads()`, and `downloadStatus()` now call Rust model_mount read-projection kinds over runtime `state_dir`, replay admitted storage-control records from `model-catalog-imports`, `model-downloads`, and `model-storage-controls`, and filter out JS-authored storage/download truth. The old fail-closed catalog/download/storage JS facade, JS command-envelope builder, model_mount storage-control command operation/dispatch arm, Rust bridge response wrapper, JS lifecycle receipts, JS download/artifact map mutation/readback, `writeMap("model-downloads")`, `writeMap("model-artifacts")`, fixture/live download materialization, local filesystem mutation/scanning, and no-commit planner success remain retired for these edges. | Rust daemon core `model_mount` typed API plus Agentgres record-state admission/projection/replay, wallet.network authority, cTEE custody, and stable protocol APIs. | Move richer filesystem custody, richer hosted catalog/download materialization, and stable SDK/IDE storage/download APIs over the Rust-owned projection/replay records. |
| `model-mounting/mcp-workflow` | Public `importMcpJson()`, `compileEphemeralMcpIntegrations()`, `invokeMcpTool()`, and `executeWorkflowNode()` now call typed `daemonCoreModelMountApi.planModelMountMcpWorkflow`, backed by Rust `RuntimeKernel::plan_model_mount_mcp_workflow`, receive Rust-authored `mcp-servers` or `mcp-workflow-controls` records with workflow hashes, authority hashes, receipt refs, custody facts, and Agentgres truth evidence, then require Rust Agentgres model_mount record-state commit before returning public workflow truth. MCP tool invocation and workflow-node external exits now require Rust-enforced `wallet.network.mcp_external_exit` grant refs, authority receipt refs, cTEE custody refs, and transport containment refs before planning or commit, bind those refs into the Rust authority hash/control record, and reject no-authority, no-custody, or no-containment requests before JS transport or compatibility behavior can run. MCP tool invocation now returns a Rust-admitted transport execution contract with `transport_execution_status: "rust_admitted"`, content receipt refs, StepModuleRouter owner, and materialized protocol result payload/hash; workflow-node execution returns the matching `execution_status: "rust_admitted"` StepModule dispatch contract with the same materialized result binding. Rust no longer serializes the retired JS proof, command fallback, binary-bridge fallback, or compatibility fallback false fields on MCP server, execution, or result payload contracts, and the JS model-mount core rejects those stale fallback-proof fields, stale `rust_required`, and pending-materialization MCP workflow execution responses instead of normalizing them into public truth. Public `listMcpServers()` now calls Rust read-projection kind `mcp_servers` with runtime `state_dir`; Rust replays persisted `mcp-servers/*.json`, filters to Rust-authored MCP workflow records, returns only redacted server projections, and keeps the model_mount MCP workflow command operation/dispatch arm, bridge request/response wrapper, command-envelope builder, JS `mcpServers` maps, old `publicMcpServer()` readback, direct JS receipt synthesis, registry mutation, route tests, receipt-gate dispatch, model invocation, and projection-required refusal shims retired. | Rust daemon core `model_mount` plus Agentgres record-state admission/projection/replay, receipt/state-root binding, wallet.network authority, cTEE custody, StepModuleRouter dispatch planning, materialized result payload binding, and stable protocol APIs. | Move live external MCP transport backend invocation/discovery, broader runtime containment sandboxing, and stable SDK/IDE/CLI protocol APIs over the typed Rust daemon-core MCP workflow records. |
| `coding-tools` | Rust-live StepModule execution exists for migrated tools; Rust daemon-core now constructs coding-tool `StepModuleInvocation` envelopes from canonical request facts before workload dispatch/admission; normal success/failure execution now calls Rust `plan_coding_tool_result_envelope` before dispatch for the StepModule context and again after workload observation for the result `payload_summary` and runtime-event candidate before Agentgres result-event admission; approval-required execution reaches Rust through mounted `codingToolApprovalCore`, projects approval request/decision/lease truth through Rust `project_coding_tool_approval_satisfaction` by sending runtime `state_dir` for admitted `agents/*.json`/`runs/*.json` replay instead of JS `run`/`agent` candidates, and then asks Rust `plan_coding_tool_approval_satisfaction` before the StepModule path; unsatisfied approval block result/event materialization is Rust-planned by `plan_coding_tool_approval_block`; budget-block governance is Rust-planned by `plan_coding_tool_budget_block` and the blocked result event is Rust Agentgres-admitted through `admit_coding_tool_result_event` before JS returns the policy error; run-level coding-tool budget recovery `retry_approved` now requires Rust `plan_coding_tool_budget_recovery_state_update`, commits only the Rust-planned run projection through Agentgres-backed `writeRun`, and `request_approval`/`approve_override` now require Rust `plan_coding_tool_budget_recovery_control`; request approval commits only the Rust-planned waiting run projection, approve override requires wallet.network grant plus authority receipt refs and binds a Rust-authored authority hash into the run projection, and the standalone JS budget-recovery policy/result helper remains deleted; accepted coding-tool result events and command-stream events are admitted by Rust before JS projection registration; coding-tool artifact drafts now call Rust daemon-core `plan_runtime_coding_tool_artifact_drafts`, require Rust-authored artifact records plus Rust Agentgres artifact-state commit before JS updates the temporary artifact projection cache, and reject no-planner/no-commit/no-record success; artifact read/retrieve now calls Rust daemon-core `project_runtime_coding_tool_artifact_read` with runtime `state_dir`, where Rust replays committed `artifacts/*.json` Agentgres records, filters canonical Rust-authored coding-tool artifacts, enforces thread ownership and canonical target/range aliases, shapes byte ranges, result metadata, receipt refs, and available-artifact projection, rejects retired `artifact_records` candidate transport, and fails closed when the Rust projector or `state_dir` is absent; `file.apply_patch` patch snapshot capture now consumes Rust `workspace_restore.rs` snapshot record/event output and no longer calls the JS snapshot-event appender; public workspace snapshot list/content-package and restore preview/apply APIs now call Rust `workspace_restore.rs` projection/restore protocol responses through mounted `workspaceRestoreCore` instead of JS runtime-event/artifact readback; Rust `coding_tool_workspace.rs` emits canonical snake_case workload observations for migrated workspace, patch, test, and diagnostics tools; `plan_post_edit_diagnostics_feedback` now owns post-edit diagnostics skip/path selection, repair policy normalization, rollback refs, auto diagnostics `tool_call_id`, and `diagnostics_repair_context` authoring before JS forwards the Rust-authored `lsp.diagnostics` request; diagnostics repair policy projection now sends runtime `state_dir` plus diagnostic event ids to Rust, where Rust replays admitted Agentgres runtime events to derive policy inputs and deterministic injection ids before returning rollback repair policy truth; diagnostics operator override state update now sends raw operator request, decision, repair-policy context, and canonical wallet authority refs to Rust, where Rust derives `approval_required`, `approval_satisfied`, and `approval_source`, rejects retired JS verdict/authority transport, requires wallet.network grant and authority receipt refs for approval-required overrides, binds a Rust-authored authority hash into the operator-control run projection, and direct operator-override event planning now applies the same wallet authority gate before runtime-event admission; the JS approval runner facade/normalizer, JS approval satisfaction projection callback, JS approval satisfaction run/agent candidate helper, JS approval manifest matcher, JS approval block facade, JS budget-block result/event facade, JS budget-recovery policy/result helper, JS diagnostics operator-override approval helper/verdict transport, JS result-event admission hook, JS command-stream append facade, JS artifact draft record materializer, JS artifact read/retrieve selector/slicer/candidate transport, JS-created coding-tool StepModule envelope, JS-authored StepModule context, JS-authored result payload summary/source-event kind/result-event candidate, JS post-edit diagnostics planner, public workspace snapshot JS projection/readback facade, workspace restore generic command invoker path, and successful-without-Rust-snapshot compatibility path are deleted or demoted to Rust-client scaffolding. Slice 1227 retires command transport for coding-tool result envelope, coding-tool artifact draft/read projection, post-edit diagnostics feedback, diagnostics-repair control/retry-run, diagnostics-repair decision projection, and diagnostics repair policy projection: `RuntimeContextPolicyCore` now calls typed runtime control/projection APIs, Rust command-protocol source is deleted and conformance source-scans keep the retired command operations absent, and the dispatch arms/response wrappers/source markers are gone. Slice 1228 retires the remaining coding-tool StepModule command transport and Slice 1262 deletes the temporary JS StepModule runner facade: `AgentgresRuntimeStateStore` passes `daemonCoreWorkloadApi` directly to the coding-tool invocation surface, coding-tool invocation surface calls `daemonCoreWorkloadApi.runCodingToolStepModule` directly with canonical request facts and no command `operation`/`backend`/`invocation` fields, Rust `RuntimeKernelService::run_coding_tool_step_module` owns the run, `command_protocol.rs` is deleted and `pub mod command_protocol` is absent, and `packages/runtime-daemon/src/step-module-runner.mjs` plus its focused test are absent. Slice 1233 deletes the retired `ioi-step-module-bridge` binary and empty `ioi_step_module_bridge/mod.rs` tombstone, and conformance now requires both to stay absent. | Rust daemon core `step_router` plus workload/WASM backend, Agentgres admission, receipt/state-root binding, projection, and replay. | Move remaining diagnostics repair approval authority, durable replay/storage, MCP/model_mount materialization, and stable IDE/CLI/SDK protocol APIs into the Rust-owned execution spine without restoring StepModule command transport, bridge binaries, or tombstone modules. |
| `approvals-gates` | Approval manifest planning, coding-tool approval satisfaction projection, approval satisfaction planning, approval block shaping, approval state-update response shaping, public approval request/decision/revoke control, and public approval queue/read projection now call typed Rust daemon-core approval APIs through mounted `daemonCoreApprovalApi` methods; coding-tool approval mounts `codingToolApprovalCore` directly, and public approval-state control/read mounts `approvalStateCore` directly instead of deleted JS runner facades. The old generic `daemonCoreInvoker` command-envelope request builder, root `daemonCoreApi` compatibility mount, nested `daemonCoreApi.approval*` fallbacks, and Rust approval command-protocol operations are retired for this family. Public approval request now first calls Rust `authorize_approval_request`, requires a Rust-issued request-authority record with authority receipt refs, authority hash, and Rust-authored `approval_lease`, and the Rust request state planner rejects missing request-authority or approval-lease binding before any JS commit can persist. Public approval decision/revoke first calls Rust `authorize_approval_decision`; approve, reject, and revoke decisions now require a typed `wallet_approval_grant` object, Rust verifies the grant structure and derives the wallet approval grant artifact hash/ref before authority hashing, and the public JS surface blanks caller `authority_grant_refs` so JS cannot mint the approval wallet grant string for any decision outcome. The Rust decision/revoke state planners bind the Rust authority hash/lease into the state update and reject missing wallet authority or lease authority. `packages/runtime-daemon/src/runtime-approval-lease.mjs` and its helper test are deleted; JS no longer normalizes approval decisions or authors approval lease ids, policy hashes, TTLs, expiry facts, or lease state. Public approval request/decision/revoke state updates now send runtime `state_dir`; Rust replays the target run/agent from admitted `agents/*.json` and `runs/*.json` Agentgres projections, rejects JS-supplied `agent`/`run` candidate transport before state-update truth can return, and the JS surface no longer calls `agentForThread`, `getRun`, `listRuns`, or `runs.get` for public approval control. Public approval queue/read also sends runtime `state_dir`; Rust replays admitted `agents/*.json` and `runs/*.json` Agentgres projections, derives pending/resolved queue truth including Rust-authored lease state, and rejects JS-supplied `agent`/`run`/`runs` candidate transport before queue truth can return. Coding-tool approval satisfaction projection also sends runtime `state_dir`, replays admitted agent/run projections in Rust, and rejects JS-supplied `agent`/`run` candidate transport before request/decision/lease truth can return. JS governance no longer scans approval events/leases, JS approval request/decision readback is retired, JS approval satisfaction projection is retired, JS approval satisfaction run/agent candidate helper is retired, JS approval runner response normalization is retired, JS approval-state response normalization is retired, and the public approval surface persists only Rust-authored run/agent projections through Agentgres-gated state commits while `GET /v1/threads/:thread_id/approvals` returns the Rust `project_approval_queue` projection. | Rust daemon core `authority` backed by wallet.network grants, approval lease authority, Agentgres expected heads, receipts, replay, and stable approval read APIs. | Move remaining wallet.network grant issuance/signature-consumption hardening, richer authority projection/replay storage, and stable IDE/CLI/SDK approval read APIs beyond the thin JS protocol client scaffolding. |
| `external-capability-authority` | External capability exit authorization now calls mounted `externalCapabilityAuthorityCore`; the JS runner facade, store runner option, command/env fallback, JS response normalizer, generic `daemonCoreInvoker` command-envelope request builder, root `daemonCoreApi` compatibility mount, and Rust `authorize_external_capability_exit` command operation are deleted or retired for this family. The core requires typed `daemonCoreAuthorityApi.authorizeExternalCapabilityExit`, rejects retired request aliases/truth fields/options plus `daemonCoreInvoker` and flat `daemonCoreApi` before Rust invocation, and returns the Rust `authority.rs` wallet.network authority protocol envelope as-is; Rust command-protocol tests now prove `authorize_external_capability_exit` has no command schema/operation entry. Product/API/CLI/SDK clients remain protocol clients over the daemon route and cannot receive JS-minted `exit_authorized`, `direct_truth_write_allowed`, wallet grant refs, authority receipts, source/backend, or authority hash truth. | Rust daemon core `authority` backed by wallet.network grants, cTEE custody boundaries where relevant, Agentgres admission evidence, receipt/state-root binding, projection, replay, and stable protocol APIs. | Move authority projection/replay records, richer Agentgres receipt/state-root binding, and stable IDE/CLI/SDK authority read APIs out of JS adapter scaffolding. |
| `runtime-events-replay-trace` | Generic runtime thread-event append now calls Rust `admit_runtime_thread_event` through mounted `runtimeAgentgresAdmissionCore` with the candidate event plus runtime `state_dir`; Rust derives latest sequence, current head, and state root from admitted `events/*.jsonl` Agentgres records before admission, then requires receipt refs, storage admission, payload refs, and projection watermarks before JS can register the Rust-returned event in the local replay cache, while the direct admission request rejects caller-supplied `latest_seq`, `expected_head`, and `state_root_before`. Synthetic `thread.started` projection and legacy run-event-to-runtime-event projection now call Rust `project_runtime_thread_events`, which loads canonical source facts from admitted Agentgres `agents/*.json` and `runs/*.json` records under runtime `state_dir`, authors thread/run event envelopes, derives latest sequence, current head, state root, and idempotency from admitted `events/*.jsonl`, admits each event through the same Rust Agentgres admission core, rejects caller-supplied projection cache transport (`latest_seq`, `expected_head`, `state_root_before`, and `existing_idempotency_keys`) plus JS fact transport (`workspace_root`, `agent`, and `runs`), and returns only Rust-admitted events for JS replay registration. Public stream/turn replay readback now calls Rust `project_runtime_thread_event_replay` with replay kind, cursor, and runtime `state_dir`; Rust reads admitted Agentgres `events/*.jsonl` records itself, owns stream/turn selection, canonical cursor evaluation, required Agentgres admission refs, latest-sequence derivation, state/head/watermark projection, and the returned event set, and rejects caller-supplied replay `events` plus `latest_seq` transport. Public lifecycle projection, including top-level usage and authority-evidence/preflight summaries, now calls Rust `project_runtime_lifecycle` with runtime `state_dir`; Rust replays admitted `agents/*.json`, `runs/*.json`, and `events/*.jsonl` records for those projections and rejects JS candidate transport. The duplicate `replayFromCanonicalState` facade, JS `runtimeThreadReplayCandidateEvents` cache transport, JS `runtimeThreadProjectionAgent`/`runtimeThreadProjectionRun`/`runtimeThreadProjectionRunEvent` fact helpers, JS direct admission cache-head transport, JS authority summary helper, and run-read top-level usage/evidence facade are deleted. The JS runner/env/normalizer path is deleted; mounted request-envelope builders are typed API scaffolding only. | Rust admission/projection/replay core over Agentgres-admitted truth and receipt-bound state roots. | Replace remaining local replay-cache hydration and IDE/SDK read APIs with direct Rust projection/replay protocol APIs. |
| `model-mounting` | The daemon mounts `modelMountCore` directly. Route-decision, invocation admission, provider-execution admission, provider invocation/stream execution, provider lifecycle/inventory, instance lifecycle, provider-result admission, backend-process planning, backend-lifecycle planning, artifact-endpoint planning, storage control, route-control, tokenizer/required-control planning, conversation/stream planning, MCP workflow, server-control, runtime-engine, runtime-survey, read-projection, catalog-provider control, provider control, capability-token control, vault control, receipt-gate planning, accepted-receipt head/transition planning, and invocation receipt-binding now call typed `daemonCoreModelMountApi` methods backed by Rust `RuntimeKernelService`; their migrated command operations, dispatch arms, bridge request/response wrappers, backend/source markers, JS command-envelope builders, command/env runner paths, fail-closed JS facades, JS map mutation/readback truth, provider-map mutation/lookup truth, no-commit planner success, and JS-authored receipt/projection truth remain retired. Public model_mount reads replay Agentgres-admitted records through Rust read-projection kinds over runtime `state_dir`, including provider/config/catalog/OAuth, topology, backend/server/runtime, conversation, route-decision, endpoint, receipt/authority/health/survey/snapshot, storage, custody, and product/catalog projections, while migrated writes commit only Rust-authored records before public truth returns. | Rust daemon core `model_mount` with Agentgres record-state admission/projection/replay, wallet.network authority, cTEE no-plaintext custody, receipt/state-root binding, StepModuleRouter dispatch planning, Rust/WASM workload contracts, and stable protocol APIs. | Move remaining backend process supervision/execution materialization, projection-helper, hosted/provider transport, OAuth execution/materialization, hosted catalog materialization, invocation authority, and local cache scaffolding to Rust daemon-core/protocol APIs; keep JS as protocol/client scaffolding only and do not restore command/env, binary bridge, JS facade, duplicate truth, or fallback paths. |
| `model-mounting/core-api-boundary` | The mounted model_mount core is typed-API-only: constructor `daemonCoreInvoker` is rejected as a retired compatibility option, the stored generic invoker and `invokeDaemonCore()` shim are deleted, the daemon-core command schema marker is gone from `model-mount-core.mjs`, and `ModelMountingState` forwards only `daemonCoreModelMountApi`. Route-decision default source is `rust_model_mount_api`, not a command-transport marker. | Rust daemon core `model_mount` typed protocol/API over Agentgres record-state admission/projection/replay. | Move remaining backend supervision/execution materialization, hosted/provider transport, OAuth/materialization, invocation authority, local cache scaffolding, and stable IDE/CLI/SDK protocol APIs to Rust-owned records without restoring generic invokers, command/env fallback, binary bridge fallback, or JS command-envelope compatibility. |
| `model-mounting/wallet-custody-projection` | Broad `projection()` grants, broad `snapshot()` tokens/vault refs, and `authoritySnapshot()` grants/vault refs now replay admitted Rust `capability-tokens/*.json` and `vault-refs/*.json` records through `model_mount/read_projection/custody.rs`; token material and plaintext vault material stay redacted, revokes/removes are applied, and the JS facade keeps request state empty instead of sending `grants`, `vault_refs`, wallet, vault, or Agentgres adapter-status truth. | Rust daemon core `model_mount` plus Agentgres record replay, wallet.network authority, cTEE no-plaintext custody, and stable protocol APIs. | Move deeper wallet authority policy, revocation epochs, cTEE material storage, provider auth materialization, and stable direct Rust custody projection APIs. |
| `model-mounting/topology-accessors` | Mounted `endpoint()`, `route()`, `instance()`, `getModel()`, provider-direct mount lookup, model-id endpoint resolution, loaded-instance reuse, idle eviction, loaded-instance coalescing, and explicit supersede now consume Rust read-projection lists (`endpoints`, `providers`, `routes`, `instances`, and `artifacts`) instead of reading `state.endpoints`, `state.providers`, `state.routes`, `state.instances`, or `state.artifacts` as accepted truth; map-only topology rows fail not-found or stay untouched, provider-direct artifact creation still fails closed before JS mutation, and load fallback still enters the Rust-planned mount/load surfaces. | Rust daemon-core model_mount read projection over Agentgres-admitted artifact, endpoint, provider, instance, and route records. | Retire remaining model_mount local cache materialization and replace migration transport with stable direct Rust topology lookup APIs. |
| `agentgres-admission` | Runtime Agentgres admission/commit/replay/projection now mounts `runtimeAgentgresAdmissionCore` over typed `daemonCoreAgentgresApi` methods; the JS runner facade, runner tests, command/env fallback, spawn hooks, generic `daemonCoreInvoker` option, root `daemonCoreApi` compatibility mount, nested `daemonCoreApi.agentgres*` fallbacks, JS response normalizers, and Rust Agentgres command-protocol dispatch are retired for this family. The core sends typed Rust daemon-core Agentgres API requests for coding-tool result-event admission, command-stream admission, runtime thread-event admission/projection/replay, thread/turn projection, storage-write admission, and runtime run/agent/memory/subagent/artifact/model_mount state commits, returns Rust envelopes without JS normalization, and public daemon-store event/state helpers call this mounted core. Coding-tool result-event, coding-tool command-stream, and runtime thread-event admission/projection/replay now send runtime `state_dir` instead of JS admission cache head/latest-seq/state-root inputs, projection cache head/idempotency/latest-seq candidates, or JS agent/run fact candidates; Rust derives those facts from admitted `agents/*.json`, `runs/*.json`, and `events/*.jsonl` Agentgres records and rejects caller-supplied admission `latest_seq`, `expected_head`, and `state_root_before`. Public agent/run/task/lifecycle controls still use Rust planners/projectors and commit Rust-authored projections through Agentgres-backed `writeAgent`/`writeRun`; JS thread/store persistence facades still stage some remaining calls as non-authoritative cache scaffolding. | Rust daemon core `agentgres_admission`, `agentgres_protocol`, `runtime_thread_event`, and `coding_tool_event` own admitted truth, storage admission, state-root/receipt binding, projection, replay, and runtime state commits for migrated Agentgres hot paths. | Replace the remaining local replay cache and thread/store persistence scaffolding with stable protocol read APIs; do not restore the JS runner, command-env compatibility, generic command invoker, root daemon-core compatibility mount, Agentgres command-protocol operations, JS admission cache transport, or JS projection fact transport. |
| `receipt-binding` | Receipt binder and many receipt aliases are Rust-owned or retired; model_mount invocation receipt binding now calls typed Rust daemon-core API, while broader JS receipt translators still need collapse where they author live route-family envelopes. | Rust daemon core `receipt_binder` binds every meaningful operation to expected heads, state roots, ArtifactRefs, PayloadRefs, and replay. | Collapse the remaining JS receipt translators outside the migrated model_mount receipt path into Rust-owned response envelopes. |
| `ctee-private-workspace` | cTEE Private Workspace product route now calls a mounted `cteePrivateWorkspaceCore` API; the JS runner facade, runner tests, store runner option, command/env fallback, JS receipt/evidence normalization, generic `daemonCoreInvoker` command-envelope request builder, root `daemonCoreApi` compatibility mount, and Rust `execute_private_workspace_ctee_action` command operation are deleted or retired for this family. The core requires typed `daemonCoreCteeApi.executePrivateWorkspaceCteeAction`, rejects retired request aliases/options plus `daemonCoreInvoker` and flat `daemonCoreApi` before Rust invocation, and returns the Rust `governed_receipt.rs` cTEE custody protocol envelope as-is; Rust command-protocol tests now prove `execute_private_workspace_ctee_action` has no command schema/operation entry. CLI/SDK/IDE clients remain protocol clients over the daemon route. | Rust daemon core `ctee` enforces no-plaintext-custody, declassification, leakage failure, and private workspace receipt semantics. | Move richer cTEE projection/replay records, deeper Agentgres receipt/state-root binding, and stable IDE/CLI/SDK cTEE read APIs out of JS adapter scaffolding. |
| `workload-client-wasm` | StepModule ABI and Rust workload client contracts exist; migrated coding-tool invocation envelopes and workload dispatch requests are now constructed in Rust daemon-core from canonical JS client facts, and JS initiates them through typed `daemonCoreWorkloadApi.runCodingToolStepModule` protocol-client scaffolding. | Rust/WASM workload backends execute admitted Step/Module work through the shared contract under daemon authority. | Extend Rust-owned invocation construction across remaining route families and move stable IDE/CLI/SDK workload APIs over Rust-owned projection/replay records. |
| `workflow-compositor` | IDE/compositor projections have many alias retirements and negative guards; public runtime thread/turn projection records now call Rust `project_runtime_thread_turn_projection`, so Rust owns the thread/turn record shape, seq-range and item-id derivation from admitted replay events, canonical projection hashes, and retired request alias rejection while JS only gathers canonical agent/run/event facts. Public runtime account/node/tool catalog, skill/hook registry, repository workflow, lifecycle, memory list/policy/path/status/validation, diagnostics repair decision resolution, conversation-artifact list/get/revision, subagent list/get/result, workspace-change inspection, and managed-session inspection route projections now call Rust `project_runtime_tool_catalog`, `project_skill_hook_registry`, `project_repository_workflow`, `project_runtime_lifecycle`, `project_runtime_memory_projection`, `project_runtime_diagnostics_repair_projection`, `project_runtime_conversation_artifact_projection`, `project_runtime_subagent_projection`, `project_runtime_workspace_change_projection`, and `project_runtime_managed_session_projection`, so the old JS projection-required catalog/repository/lifecycle facades plus the fail-closed public memory, diagnostics repair decision resolver, conversation-artifact read, subagent read, workspace-change inspection, and managed-session inspection facades are retired; JS forwards canonical request/environment/workspace/lifecycle/memory/diagnostics/artifact route inputs plus runtime `state_dir` for subagent, workspace-change, and managed-session inspection replay, and fails closed if the Rust projector is missing. Public memory write/edit/delete/policy controls now require Rust `plan_runtime_memory_control` and Rust Agentgres memory-state commits before returning route projections; memory status, validation, and direct memory control-event append now require Rust `plan_runtime_memory_control` event planning plus Rust-authored runtime-event admission; public workflow-edit proposal/apply controls now require Rust `plan_runtime_workflow_edit_control` event planning plus Rust-authored runtime-event admission; public workspace-change control now requires Rust `plan_runtime_workspace_change_control` event planning plus Rust-authored runtime-event admission; public managed-session control now requires Rust `plan_runtime_managed_session_control` event planning plus Rust-authored runtime-event admission; public thread-fork control now requires Rust `plan_runtime_thread_fork_control`, commits only the Rust-authored forked agent through Agentgres-backed `writeAgent`, projects the forked thread through the Rust projection boundary, and admits only the Rust-authored `thread.forked` runtime event; public conversation-artifact create/action/export/promote now requires Rust `plan_runtime_conversation_artifact_control` and Rust Agentgres artifact-state commits before returning route truth; public diagnostics repair decision execution and direct decision-event append now require Rust `plan_runtime_diagnostics_repair_control` event planning plus Rust-authored runtime-event admission; diagnostics operator override execution now requires Rust `plan_diagnostics_operator_override_state_update` plus Rust Agentgres run-state commit, and direct operator-override event append now requires Rust diagnostics repair event planning/admission; diagnostics repair retry-turn creation now composes with the Rust run-create state-update surface and Rust diagnostics repair retry-event planning/admission, and direct retry-event append now requires Rust-authored runtime-event admission; public subagent spawn, wait, input, resume, assign, cancel, cancellation propagation, and direct control-event append now also require Rust `plan_runtime_subagent_control` and Rust-authored runtime-event admission; public lifecycle controls still require Rust subagent state-update planning before Agentgres-backed subagent persistence, spawn composes with Rust-owned child-agent and run creation, input/resume compose with the Rust run-create state-update path, and cancel/propagation compose with the Rust run-cancel state-update path. Slice 1226 retires the runtime compositor/task-job command transport for task/job create/cancel/projection, workflow-edit control, managed-session projection/control, workspace-change projection/control, thread-fork control, conversation-artifact projection/control, and subagent projection/control; Slice 1227 retires the diagnostics-repair control/retry/projection/policy command transport and post-edit diagnostics feedback command transport. `RuntimeContextPolicyCore` now calls typed runtime control/projection APIs for those families, `RuntimeKernelService` exposes direct Rust service APIs, and Rust no longer exports `command_protocol.rs`; conformance source-scans keep the old command operations absent with no dispatch wrappers. Other IDE/runtime views still consume adapter-shaped runtime surfaces. | IDE consumes Rust projection/replay records and never creates accepted truth. | Wire the next compositor/runtime view or control directly to Rust projection/admission output and remove the corresponding JS facade. |
| `diagnostics-repair-protocol-clients` | SDK diagnostics restore execution already sends canonical snake_case request bodies; Slice 1258 makes the IDE diagnostics repair node match that stable daemon protocol body and removes the remaining React Flow camelCase compatibility body. The public diagnostics repair decision execution route now rejects retired aliases such as `decisionId`, `snapshotId`, `workflowGraphId`, `workflowNodeId`, `approvalGranted`, `allowConflicts`, `restoreApplyIdempotencyKey`, and `payloadSchemaVersion` before Rust planning or runtime-event admission, and conformance guards the daemon rejection plus the IDE canonical body. | IDE/SDK diagnostics repair clients are non-authoritative protocol clients over the Rust-owned diagnostics repair control/admission surface. | Keep remaining diagnostics read/replay clients on Rust-owned Agentgres records; do not restore camelCase request compatibility bodies, JS decision truth, or local result helper projection. |
| `worker-service-packages` | Worker/service package product-route admission now calls mounted `workerServicePackageCore`; the JS runner facade, runner tests, store runner option, command/env fallback, JS ref/envelope normalizer, generic `daemonCoreInvoker` command-envelope request builder, root `daemonCoreApi` compatibility mount, and Rust `admit_worker_service_package_invocation` command operation are deleted or retired for this family. The core requires typed `daemonCoreWorkerServiceApi.admitWorkerServicePackageInvocation`, rejects retired request aliases/truth fields/options plus `daemonCoreInvoker` and flat `daemonCoreApi` before Rust invocation, and returns the Rust `governed_receipt.rs` admission protocol envelope as-is. CLI/SDK/IDE clients remain protocol clients over the daemon route and cannot receive JS-minted package admission, receipt, artifact, payload, authority, source, or backend truth. | Rust daemon core package invocation path with authority, artifacts, receipts, Agentgres truth, and compositor projection. | Move richer package invocation projection/replay records, deeper Agentgres receipt/state-root binding, and stable IDE/CLI/SDK package admission read APIs out of JS adapter scaffolding. |
| `meta-improvement` | Governed-improvement product-route admission now calls mounted `governedImprovementCore`; the JS runner facade, command/env fallback, JS response normalizer, generic `daemonCoreInvoker` command-envelope request builder, root `daemonCoreApi` compatibility mount, and Rust `admit_governed_runtime_improvement_proposal` command operation are deleted or retired for this family. The core requires typed `daemonCoreGovernedAdmissionApi.admitGovernedRuntimeImprovementProposal`, rejects retired proposal payload aliases/truth fields/options plus `daemonCoreInvoker` and flat `daemonCoreApi` before Rust invocation, and returns the Rust `governed_admission.rs` admission protocol envelope without JS synthesis. | Rust daemon core governs proposal admission, verification receipts, authority gates, and accepted improvement truth. | Move proposal execution/projection/replay records, deeper Agentgres receipt/state-root binding, and stable IDE/CLI/SDK governed-improvement read APIs out of JS adapter scaffolding. |
| `rust-daemon-core` | Command envelope identity, typed operation dispatch, public runtime account/node/tool catalog projection, public skill/hook registry projection, public repository workflow projection, public lifecycle projection, runtime bridge thread-start, thread-control, turn-submit, subagent-record, and public agent/thread/run lifecycle state planning through typed `daemonCoreThreadLifecycleApi`, diagnostics-blocked turn creation through Rust run-create planning and Rust turn projection, workflow-edit, diagnostics-repair, thread-turn, and lifecycle admission-required refusal planning through typed daemon-core APIs, MCP control/catalog/search/fetch/serve planning through typed `daemonCoreMcpApi`, public memory route projection, public memory write/edit/delete/policy control planning, public memory status/validation/direct control-event planning, public workflow-edit proposal/apply control-event planning, public managed-session inspection projection/control-event planning, public workspace-change inspection projection/control-event planning, public thread-fork control planning, model-mount MCP workflow planning and MCP server list projection, post-edit diagnostics feedback planning, coding-tool budget recovery request/override control planning, diagnostics repair policy projection, public diagnostics repair decision execution/direct event planning, diagnostics repair decision projection, diagnostics operator override state-update/direct-event planning, diagnostics repair retry-run request planning, diagnostics repair retry-event planning, diagnostics repair retry-result projection, public conversation-artifact read projection and create/action/export/promote control planning, public subagent read projection, public subagent spawn/wait/input/resume/assign/cancel/propagated-cancel control-event planning, public subagent direct control-event append planning, and many response planners are Rust-owned; task/job create/cancel/projection plus the runtime compositor control/projection family now use typed runtime-control/projection daemon-core APIs instead of command transport, and coding-tool result/artifact plus diagnostics feedback/control/projection/policy now also use typed runtime control/projection APIs instead of command transport. Public lifecycle create/thread/run/status/delete routes, public non-runtime resume/turn creation, diagnostics repair retry-run creation, and subagent spawn/input/resume now call direct Rust-backed lifecycle functions with explicit planner/build/projection dependencies instead of the retired `agentRunLifecycleSurface` facade; production source no longer mounts or exports `createRuntimeAgentRunLifecycleSurface`. Public runtime-service resume and turn submission call the direct Rust thread-lifecycle adapter from `runtime-thread-turn-surface.mjs` with typed planner, Agentgres commit, and Rust projection dependencies instead of mounted `agentRunLifecycleSurface.createRuntimeBridgeThreadControl` / `createRuntimeBridgeTurn` facade methods, which are absent. The old context-lifecycle, runtime-control, thread-lifecycle state-update, admission-required, MCP control/catalog/serve, runtime projection, runtime compositor/task-job, coding-tool result/artifact, diagnostics-repair, diagnostics retry-result, and coding-tool StepModule command operations have no Rust command-protocol substrate: `command_protocol.rs` is deleted and `pub mod command_protocol` is absent; coding-tool budget recovery, diagnostics operator override, operator turn-control, operator interrupt/steer, and run-cancel no longer expose Rust command-shaped owner wrappers, bridge request types, `_command` source markers, or bridge-shaped owner proof tests; `command_dispatch.rs` is deleted, the old bridge binary and root tombstone are deleted, the top-level daemon `daemonCoreInvoker` pass-through is fail-closed instead of stored or forwarded, and conformance requires all of these absence guards to hold. Shared JS command-spawn helper is deleted. Slice 1251 deletes the remaining RuntimeAgentService command/binary bridge substrate: the JS API adapter export, `ioi-runtime-bridge` Cargo bin, bridge env policy override, deleted runtime-service helper, and retired runtime-service proof scripts/tests are gone, while daemon startup rejects `runtimeBridge`. | Rust daemon core owns hot-path execution semantics, authority gates, StepModuleRouter dispatch, Agentgres admission, receipt binding, replay, projection, and stable APIs. | Replace remaining model_mount/materialization transports and deeper MCP/materialization/replay gaps with direct in-process or stable daemon protocol APIs; do not restore StepModule command transport, command-dispatch transport, bridge binaries, root tombstones, daemon-wide generic invoker pass-through, mounted lifecycle facades, or the retired serve command bridge. |
| `js-facade-retirement` | Many JS pass-through wrappers, compatibility aliases, and local writers are deleted; public agent create/run create/run cancel/task create/task-job cancel/status/delete controls, task/job list/get projections, runtime-service thread start, thread control, and turn submit, diagnostics-blocked non-runtime turn creation, workspace-trust warning/ack event controls, public runtime account/node/tool catalog projections, public skill/hook registry projections, public repository workflow projections, public lifecycle projections, public memory list/policy/path/status/validation route projections, public memory write/edit/delete/policy controls, public memory status/validation/direct control-event append, public workflow-edit proposal/apply controls, public coding-tool budget recovery retry/request/override controls, workspace-change inspection/control, managed-session inspection/control, thread-fork control, post-edit diagnostics feedback planning, diagnostics repair policy projection, public diagnostics repair decision execution/direct decision-event append, diagnostics repair decision resolution, diagnostics operator override execution/direct operator-event append, diagnostics repair retry-run request authoring/turn creation/direct retry-event append, conversation-artifact list/get/revision projections and create/action/export/promote controls, model-mount MCP import/ephemeral/tool/workflow-node planning plus MCP server list readback, subagent list/get/result projections, subagent spawn/wait/input/resume/assign/cancel/propagation controls, and subagent direct control-event append moved from fail-closed JS facade to Rust-owned positive paths; the mounted runtime bridge turn/control lifecycle facade methods are now deleted from `agentRunLifecycleSurface`, and public runtime-service resume/turn submission enters the direct Rust lifecycle adapter instead. Remaining non-test `rust_core_required` markers are honest fail-closed boundaries. | JS facades are non-authoritative clients only, or retired. | Convert fail-closed facades into Rust-backed positive paths in whole route-family batches, then delete the facade. |

## Macro Authority Cut Ledger

This ledger replaces the old per-slice evidence list. It records the meaningful
authority-boundary cuts that should guide future work.

| Macro cut | Current proof | Still temporary |
| --- | --- | --- |
| Rust-live coding-tool StepModule path | Migrated coding tools execute through Rust workload/StepModule contracts via typed `daemonCoreWorkloadApi.runCodingToolStepModule`; command-envelope selection fails closed, retired command/backend env selectors and the temporary StepModule runner facade are absent, and Rust daemon-core constructs the coding-tool `StepModuleInvocation` envelope before workload dispatch/admission. | JS invocation surface is protocol-client scaffolding over the typed workload API plus mounted Rust-backed governance, artifact, snapshot, and diagnostics surfaces; it must not restore command transport, command-env selectors, or JS-authored StepModule truth. |
| Public computer-use request-lease positive API | Public `ioi.computer_use.*` invocation methods now map browser discovery, control, native-browser, visual GUI, sandboxed-hosted, and visual GUI observe calls to canonical `computer_use.request_lease` input and invoke the Rust-live coding-tool StepModule path. Rust `coding_tool_computer_use.rs` authors the lease request, provider registry selection, wallet.network authority boundary, fail-closed provider availability, receipt refs, and canonical result fields; the old guard-only public JS facade is gone and direct computer-use event append remains fail-closed. | Concrete provider execution, direct Rust daemon-core computer-use event materialization, cTEE private-workspace custody, durable Agentgres expected-head/state-root binding, replay/projection, and stable IDE/CLI/SDK APIs remain non-terminal. |
| Public computer-use discovery/provider projection positive API | Public `/v1/computer-use/providers` and `/v1/computer-use/browser-discovery` now call typed `daemonCoreRuntimeProjectionApi.projectRuntimeComputerUse` through `RuntimeContextPolicyCore`; Rust `runtime_computer_use.rs` owns provider-registry and browser-discovery projection truth, reuses the StepModule provider registry from `coding_tool_computer_use.rs`, shapes browser process/CDP discovery in Rust, emits Rust receipt/evidence refs, and ignores retired camelCase request aliases. The JS `computer-use-provider-registry.mjs` and `browser-discovery.mjs` facades/tests are deleted, and daemon startup no longer imports or injects those route dependencies. | Concrete provider execution, direct Rust computer-use event materialization, cTEE custody, durable Agentgres expected-head/state-root binding, replay/projection, and stable IDE/CLI/SDK APIs remain non-terminal. |
| Computer-use run materialization Rust run-create owner | Public run creation no longer calls a JS computer-use projection facade. `buildRun()` forwards only canonical request facts through `computer_use_materialization_request`, Rust `plan_run_create_state_update` consumes and removes that request, rejects prebuilt `computerUse` or `computer_use_projection` candidates, and materializes `trace.computerUse`, canonical computer-use events, a `computer_use_trace` receipt, task-state evidence refs, and `computer-use-trace.json` during run-create planning. The old `computer-use-projection.mjs` facade and test are deleted, the JS event-contract module is protocol scaffolding only, and conformance guards the retired JS projection path. | Concrete provider execution beyond run-create materialization, direct provider/backend event admission, cTEE private-workspace custody, durable Agentgres expected-head/state-root binding across replay/projection, and stable IDE/CLI/SDK APIs remain non-terminal. |
| Coding-tool artifact draft Rust planning | `plan_runtime_coding_tool_artifact_drafts` authors canonical artifact records in Rust, rejects retired `artifactDrafts` aliases, requires receipt refs, and the daemon artifact surface commits only those Rust-authored records through Rust Agentgres artifact-state admission before updating its temporary read cache. The artifact surface now resolves draft planning only through its positive `contextPolicyCore` startup mount, with `store.contextPolicyCore ?? contextPolicyCore` fallback retired and guarded. | Stable IDE/CLI/SDK APIs remain non-terminal. |
| Coding-tool artifact read/retrieve projection Rust-owned | `project_runtime_coding_tool_artifact_read` now requires runtime `state_dir`, replays committed `artifacts/*.json` Agentgres records in Rust, filters canonical Rust-authored coding-tool artifacts, enforces thread ownership and retired target/range/candidate-transport alias rejection, shapes byte ranges, result metadata, receipt refs, and available-artifact projection, and the daemon artifact surface fails closed without the Rust projector or `state_dir` instead of selecting/slicing/transporting records in JS. The artifact surface now resolves read/retrieve projection only through its positive `contextPolicyCore` startup mount, with `store.contextPolicyCore ?? contextPolicyCore` fallback retired and guarded. | Diagnostics projection/replay, durable artifact storage, and stable IDE/CLI/SDK APIs remain non-terminal. |
| Coding-tool, diagnostics, and runtime-control command transport retired | Coding-tool result envelope planning, artifact draft planning, artifact read projection, post-edit diagnostics feedback planning, diagnostics-repair control, diagnostics-repair retry-run planning, diagnostics-repair decision projection, diagnostics repair policy projection, coding-tool budget recovery request/override planning, diagnostics operator override state updates, operator turn-control admission refusals, operator interrupt/steer planning, run-cancel state/admission planning, and coding-tool StepModule workload dispatch now call typed Rust daemon-core APIs backed by direct Rust service methods. `RuntimeContextPolicyCore` rejects generic `daemonCoreInvoker` for this family, the coding-tool invocation surface calls the typed Rust workload API directly, the temporary StepModule runner facade is deleted, Rust `command_protocol.rs` is deleted and no longer exported, and `command_dispatch.rs` is deleted instead of carrying dispatch arms or command response wrappers; the former Rust command-shaped owner wrapper types/functions/tests for budget recovery, operator control, and run cancel are deleted. The stale `diagnosticsRepairRunner` and `codingToolBudgetRecoveryRunner` constructor aliases are deleted, diagnostics repair surface runner wrappers are deleted, and diagnostics retry lifecycle composition passes the single daemon-injected `contextPolicyCore` into the direct Rust run-create path instead of reading `store.contextPolicyCore ?? null`. | Durable diagnostics/runtime-control replay/storage, broader orchestration, and stable IDE/CLI/SDK APIs remain non-terminal without StepModule command transport. |
| Diagnostics repair protocol-client compatibility retired | IDE diagnostics repair nodes now emit canonical snake_case daemon protocol bodies for decision execution, SDK diagnostics restore already uses canonical request fields, and the daemon diagnostics repair surface rejects retired camelCase request aliases before Rust planning/admission. Conformance guards the IDE body shape and daemon alias rejection, so the old React Flow compatibility body cannot return as an alternate diagnostics repair client truth path. | Durable diagnostics repair storage/replay, wallet-governed repair policy authority, and remaining diagnostics read APIs still need Rust-owned Agentgres projection/replay coverage. |
| Rust command-envelope and bridge contraction | Rust no longer owns a command-envelope parsing substrate for daemon-core hot paths: `command_protocol.rs` and `command_dispatch.rs` are deleted, deleted bridge delegates must not return, and the old `ioi-step-module-bridge` binary and `ioi_step_module_bridge/mod.rs` tombstone must not exist. | Command transport is no longer a terminal substrate; remaining work is direct Rust materialization/replay/protocol API ownership for non-terminal route families without restoring command-dispatch transport, a bridge binary, tombstone module, or compatibility process path. |
| Daemon-wide generic invoker pass-through retired | `startRuntimeDaemonServiceWithStore()` and `AgentgresRuntimeStateStore` now reject top-level `daemonCoreInvoker` instead of storing or forwarding it, and `createCodingToolApprovalPolicy()` constructs its default core from typed `daemonCoreApprovalApi` only. Conformance forbids `daemonCoreInvoker: options.daemonCoreInvoker`, `this.daemonCoreInvoker = options.daemonCoreInvoker`, and `daemonCoreInvoker: deps.daemonCoreInvoker` on those live daemon surfaces while preserving direct core-level fail-closed tests for retired compatibility options. | Remaining typed cores still fail closed when their Rust API is missing; replace those guard-only route edges with positive Rust materialization/projection APIs rather than reintroducing a generic invoker handle. |
| cTEE Private Workspace core API | Product-route admission now reaches Rust-owned cTEE custody through mounted `cteePrivateWorkspaceCore`; the JS runner facade is deleted, no command/env selection is read, retired request aliases/options fail closed, and the core returns Rust `governed_receipt.rs` cTEE custody protocol envelopes without synthesizing receipt/evidence refs or source/backend defaults. The core now requires typed `daemonCoreCteeApi.executePrivateWorkspaceCteeAction`, rejects generic `daemonCoreInvoker` and root `daemonCoreApi`, and Command-protocol deletion guards keep the old `execute_private_workspace_ctee_action` command operation absent. | Richer cTEE projection/replay records, deeper Agentgres receipt/state-root binding, and stable IDE/CLI/SDK cTEE read APIs remain non-terminal; other route families still carry temporary command transport. |
| Worker/service package core API | Product-route admission now reaches Rust-owned package invocation admission through mounted `workerServicePackageCore`; the JS runner facade is deleted, no command/env selection is read, the core requires typed `daemonCoreWorkerServiceApi.admitWorkerServicePackageInvocation`, rejects generic `daemonCoreInvoker` and root `daemonCoreApi`, and Command-protocol deletion guards keep the old `admit_worker_service_package_invocation` command operation absent. The core returns Rust `governed_receipt.rs` admission protocol envelopes without synthesizing receipt refs, artifact refs, payload refs, authority refs, source, or backend truth. | Richer package projection/replay records, deeper Agentgres receipt/state-root binding, and stable IDE/CLI/SDK package admission read APIs remain non-terminal; other route families still carry temporary command transport. |
| L1 settlement core API | Product-route admission now reaches Rust-owned settlement trigger admission through mounted `l1SettlementCore`; Rust `settlement.rs` derives admitted `state_root_ref` from canonical settlement/domain/trigger/receipt facts, and daemon/SDK/IDE/CLI clients fail closed on retired `state_root_ref`/`stateRootRef` request truth before transport or Rust invocation. The JS runner facade is deleted, no command/env selection is read, the core requires typed `daemonCoreGovernedAdmissionApi.admitL1SettlementAttempt`, rejects generic `daemonCoreInvoker` and root `daemonCoreApi`, and Command-protocol deletion guards keep the old `admit_l1_settlement_attempt` command operation absent. The core returns Rust `governed_admission.rs` admission protocol envelopes without synthesizing trigger refs, receipt refs, source, backend, settlement refs, state-root refs, or admission hashes. | Richer L1 settlement projection/replay records, deeper Agentgres receipt/state-root binding, and stable IDE/CLI/SDK settlement read APIs remain non-terminal; other route families still carry temporary command transport. |
| Governed-improvement core API | Product-route admission now reaches Rust-owned governed proposal admission through mounted `governedImprovementCore`; the JS runner facade is deleted, no command/env selection is read, the core requires typed `daemonCoreGovernedAdmissionApi.admitGovernedRuntimeImprovementProposal`, rejects generic `daemonCoreInvoker` and root `daemonCoreApi`, and Command-protocol deletion guards keep the old `admit_governed_runtime_improvement_proposal` command operation absent. Retired proposal payload aliases fail closed, and the core returns Rust `governed_admission.rs` admission protocol envelopes without synthesizing expected heads, eval/verifier receipt refs, source, backend, proposal refs, admission hashes, state roots, approval refs, or rollback refs. | Richer proposal execution/projection/replay records, deeper Agentgres receipt/state-root binding, and stable IDE/CLI/SDK governed-improvement read APIs remain non-terminal; other route families still carry temporary command transport. |
| External capability authority core API | Product-route authorization now reaches Rust-owned wallet.network authority through mounted `externalCapabilityAuthorityCore`; the JS runner facade is deleted, no command/env selection is read, retired request aliases/truth fields fail closed, and the core returns Rust `authority.rs` authorization envelopes without synthesizing authorization booleans, wallet grant refs, authority receipts, authority hashes, source, or backend truth. The core requires typed `daemonCoreAuthorityApi.authorizeExternalCapabilityExit`, rejects generic `daemonCoreInvoker`, and Command-protocol deletion guards keep the old `authorize_external_capability_exit` command operation absent. | Richer authority projection/replay records, deeper Agentgres receipt/state-root binding, and stable IDE/CLI/SDK authority read APIs remain non-terminal; other route families still carry temporary command transport. |
| Runtime bridge turn/control mounted facade retired | Public runtime-service resume and turn submission no longer call `agentRunLifecycleSurface.createRuntimeBridgeThreadControl` or `agentRunLifecycleSurface.createRuntimeBridgeTurn`. `runtime-thread-turn-surface.mjs` calls the direct Rust lifecycle adapter with typed `daemonCoreThreadLifecycleApi` planning through `RuntimeContextPolicyCore`, carries run-builder/provider dependencies explicitly, commits only Rust-planned agent/run records through Agentgres-backed writes, and returns Rust thread/turn projections. The thread-turn surface no longer accepts `threadLifecycleRunner`, `threadTurnAdmissionRunner`, or `operatorTurnControlAdmissionRunner`; runtime-service resume/turn submit, public non-runtime resume/turn create, and operator interrupt/steer planning resolve only through the positive `contextPolicyCore` mount. Turn submission no longer calls `store.turnForRun(candidateRun)` before Rust planning or sends a JS-authored `projection` candidate into Rust; Rust denies the retired request field, and projection happens only after the Rust-planned `writeRun` commit. The mounted `agentRunLifecycleSurface` facade is now absent entirely, and conformance guards the absent export, absent store property, absent public-surface calls, retired runner aliases, and retired projection-candidate transport. | Broader lifecycle completion still needs deletion/cancellation replay/projection, durable wallet/cTEE authority, and stable IDE/CLI/SDK lifecycle APIs over Rust-owned records. |
| Typed daemon-core APIs | Runtime Agentgres admission, context-policy/state-update, approval-state, coding-tool approval, workspace restore, external capability authority, cTEE private workspace, worker/service package invocation, L1 settlement, governed-improvement proposal admission, model_mount migrated planners, and StepModule execution now use typed Rust daemon-core APIs and reject generic `daemonCoreInvoker` on migrated hot paths. The authority/governed-admission cluster and the approval/Agentgres/workspace/context cluster also reject flat/root `daemonCoreApi` compatibility mounts, so these public admission/control/projection cores can only enter through their explicit typed API handles. The old runtime context-policy runner module/test, `createContextPolicyRunnerFromEnv`, store/service `contextPolicyRunner` option, command/env compatibility, and JS response facade identity are retired. | Remaining model_mount materialization, durable replay/storage, richer MCP/runtime transport, and stable IDE/CLI/SDK APIs must continue as typed protocol/API work rather than command or direct-invoker compatibility. |
| Model_mount Rust owner split | Route-decision admission, invocation admission, provider-execution admission, provider invocation/stream execution, provider lifecycle/inventory planning, instance lifecycle planning, provider-result admission, backend-process planning, backend-lifecycle planning, artifact-endpoint planning, storage control, route-control planning, tokenizer/required-control planning, conversation/stream planning, MCP workflow planning, server-control planning, runtime-engine planning, runtime-survey planning, catalog-provider control planning, provider control planning, capability-token control planning, vault control planning, receipt-gate planning, accepted-receipt head/transition planning, and invocation receipt-binding now call typed `daemonCoreModelMountApi` methods; Rust rejects the retired command operations, command-dispatch arms, and bridge request/response wrappers for that family. Backend-process and backend-lifecycle planning now call `daemonCoreModelMountApi.planModelMountBackendProcess` / `planModelMountBackendLifecycle`, backed by Rust `RuntimeKernelService::plan_model_mount_backend_process` / `plan_model_mount_backend_lifecycle`, with the old command operations, dispatch arms, bridge request wrappers, command source markers, and JS command envelopes absent. Read-projection planning now calls typed `daemonCoreModelMountApi.planModelMountReadProjection`, backed by Rust `RuntimeKernelService::plan_model_mount_read_projection`. Mounted model_mount topology accessors, route-control candidate assembly, provider-lifecycle subject selection, and instance-maintenance selectors consume Rust read-projection lists for endpoint, provider, route, instance, artifact, model-id endpoint, loaded-instance reuse, idle eviction, coalescing, and explicit supersede instead of accepting JS map-only topology truth. | Remaining JS model_mount cache materialization plus backend process supervision/execution materialization and projection migration transport must be retired by stable direct Rust APIs; the backend-process/backend-lifecycle planning, tokenizer/required-control/conversation/stream/catalog-provider/provider/capability-token/vault/receipt-gate/accepted-receipt/receipt-binding command transports are retired. |
| Model_mount generic invoker shim retired | `ModelMountCore` rejects `daemonCoreInvoker`, stores only `daemonCoreModelMountApi`, deletes `invokeDaemonCore()`, removes the command schema marker from the mounted core, and `ModelMountingState` no longer forwards the daemon-wide invoker. Conformance now requires the retired option test and absence of the generic invoker storage, direct-invoker fallback error, command schema marker, and old command source default. | Backend supervision/execution materialization, hosted/provider transport, OAuth/materialization, invocation authority, local cache scaffolding, and stable IDE/CLI/SDK protocol APIs remain non-terminal Rust-owned materialization/protocol work. |
| Model_mount topology and maintenance projection lookup | `state-accessors.mjs` and `loaded-instances.mjs` lookup paths now call `listEndpoints()`, `listProviders()`, `listRoutes()`, `listInstances()`, and `listArtifacts()`; focused tests seed map-only topology and maintenance rows and prove those rows fail not-found or stay untouched while projection rows succeed and drive Rust lifecycle planning. | Local projection caches still exist for other materialization adapters until direct Rust daemon-core topology lookup APIs replace the JS store shell; instance-maintenance no longer refreshes `state.instances` after commit. |
| Model_mount stream-cancel positive API | OpenAI-compatible stream cancellation now calls typed `daemonCoreModelMountApi.planModelMountStreamCancel`, backed by Rust `RuntimeKernelService::plan_model_mount_stream_cancel`; Rust authors `model_mount_stream_cancel_rust_owned` plans with `rust_daemon_core_model_stream_cancel` and `agentgres_model_stream_cancel_truth_required` evidence, commits the canceled `model-conversations` record through Agentgres model_mount record-state admission, persists only the Rust-authored `model_invocation_stream_canceled` receipt, and rejects the old `plan_model_mount_stream_cancel` command operation/dispatch/wrapper/backend path. | hosted stream parity, deeper wallet/cTEE invocation authority, and stable SDK/IDE stream lifecycle APIs remain non-terminal. |
| Model_mount catalog-status projection positive API | `catalogStatus()` now calls Rust model_mount read-projection kind `catalog_status` with runtime `state_dir`; Rust replays admitted `model-provider-inventory/*.json` Agentgres records, filters out JS-authored inventory, and emits Rust-authored catalog provider status, storage status, last-search summary, and result rows while the JS facade sends an empty request state without `catalog_status_input`. | Hosted/provider catalog transports, richer hosted catalog metadata/materialization, and stable SDK/IDE catalog APIs remain non-terminal. |
| Model_mount receipt-replay read projection positive API | `projectionSummary()`, `receiptReplay()`, `authoritySnapshot()`, `listProviderHealth()`, `latestProviderHealth()`, `latestVaultHealth()`, `latestRuntimeSurvey()`, `snapshot()`, and `projection()` now call Rust model_mount read-projection kinds with empty request state plus runtime `state_dir`; Rust replays admitted `receipts/*.json` records through the shared receipt projection boundary for receipt, authority, vault-health, runtime-survey, snapshot, and projection receipt fields, while provider-health list/latest now replay admitted `model-provider-lifecycle-controls/*.json` lifecycle records instead of `provider_health` receipts or JS telemetry files. Rust also replays admitted `capability-tokens/*.json` and `vault-refs/*.json` through the custody read-projection boundary for public authority grants/vault refs and broad snapshot/projection token/vault fields, with token material and plaintext vault material redacted. Rust rejects missing `state_dir` and ignores caller-supplied `state.receipts`, `state.provider_health`, JS grants, and JS vault refs so JS receipt, telemetry, token, or vault lists cannot become projection, replay, authority, health, survey, snapshot, or broad projection truth. | Richer Rust Agentgres topology joins, direct daemon-core projection APIs, and stable SDK/IDE projection APIs remain non-terminal. |
| Model_mount catalog-provider-control positive API | Public catalog-provider config list/get/write, private config/material resolution, and OAuth start/callback/exchange/refresh/revoke now call Rust daemon-core `plan_model_mount_catalog_provider_control`, receive Rust-authored catalog-provider-control records with wallet authority and cTEE custody evidence, commit only those records through Rust Agentgres model_mount record-state admission, and return committed Rust response envelopes. The old JS config/runtime-material/OAuth fail-closed facade no longer executes OAuth credential helpers, resolves vault refs, reads provider config/runtime-material maps as truth, writes OAuth/session/provider maps, or returns plaintext material. | Auth-header materialization, richer hosted catalog transport/materialization, and stable SDK/IDE catalog-provider APIs remain non-terminal. |
| Model_mount capability-token control positive API | Public capability-token create/list/authorize/revoke now call Rust daemon-core `plan_model_mount_capability_token_control`, receive Rust-authored `capability-tokens` records with wallet.network authority evidence, commit only those records through Rust Agentgres model_mount record-state admission, and return committed Rust response envelopes. Rust replays admitted `capability-tokens/*.json` records for list/authorize/revoke and for broad `projection`, `snapshot`, and `authoritySnapshot()` grant/token readbacks, while the committed record persists only token hashes and authority facts; the one-time token material is returned outside the persisted record. JS token maps, JS walletAuthority grant helpers, JS permission-token receipts, and direct token-map projection no longer authorize, mutate, or return accepted capability-token truth. | Deeper wallet authority policy, revocation epochs, and stable SDK/IDE capability-token APIs remain non-terminal. |
| Model_mount vault control positive API | Public vault bind/list/metadata/status/health/remove now call Rust daemon-core `plan_model_mount_vault_control`, receive Rust-authored `vault-refs` records with wallet.network authority and cTEE custody evidence, commit only those records through Rust Agentgres model_mount record-state admission, and return committed Rust response envelopes. Rust replays admitted `vault-refs/*.json` bind/remove records for list/metadata/status/health and for broad `projection`, `snapshot`, and `authoritySnapshot()` vault-ref readbacks; committed records persist vault_ref_hash, material_hash, custody facts, and authority facts without plaintext material, and JS vault-port metadata/write helpers no longer author accepted vault truth. | Deeper cTEE material storage, provider auth materialization, and stable SDK/IDE vault APIs remain non-terminal. |
| Model_mount receipt-gate positive API | Receipt-gate validation now calls Rust daemon-core `plan_model_mount_receipt_gate`, passes canonical receipt facts and required tool receipt ids to Rust, receives a Rust-authored `workflow_receipt_gate` or `workflow_receipt_gate_blocked` receipt with receipt-binder and Agentgres gate evidence, and persists only that receipt through Rust Agentgres model_mount receipt-state admission. JS no longer computes gate failures or authors gate receipts. | Broader MCP/workflow execution, workflow policy composition, and stable SDK/IDE receipt-gate APIs remain non-terminal. |
| Model_mount runtime-engine control/projection positive API | Public runtime-engine selection/profile/remove mutations now call typed `daemonCoreModelMountApi.planModelMountRuntimeEngine`, backed by Rust `RuntimeKernelService::plan_model_mount_runtime_engine`, receive Rust-authored `runtime-engine-controls` records with receipt refs and Rust runtime-engine evidence, commit only those records through Rust Agentgres model_mount record-state admission, and return Rust public responses that explicitly mark JS preference, profile, and projection writes false. Public runtime-engine list/profile/preference/default-load/detail reads now call Rust read-projection kinds with empty JS state plus runtime `state_dir`; Rust replays admitted `runtime-engine-controls/*.json`, filters out JS-authored controls, materializes runtime-engine projection truth from Rust-owned control records, and fails closed on missing detail only after Agentgres replay. The old runtime-engine required-record facade is removed from the hot path; the old `plan_model_mount_runtime_engine` command operation, dispatch arm, bridge response wrapper, backend marker, and JS command-envelope builder are retired; missing Rust planning still fails closed before JS can write runtime-engine state. | Local runtime materialization and stable SDK/IDE runtime-engine APIs remain non-terminal. |
| Model_mount runtime-survey capture positive API | Public `runtimeSurvey()` now calls typed `daemonCoreModelMountApi.planModelMountRuntimeSurvey`, backed by Rust `RuntimeKernelService::plan_model_mount_runtime_survey`; Rust replays runtime-engine state from Agentgres `state_dir`, captures Rust hardware facts, refuses LM Studio CLI execution, authors the canonical `runtime_survey` receipt with snake_case details and no JS-probe flags, and the JS edge must commit that Rust-authored receipt through Rust Agentgres model_mount receipt-state admission before returning public survey truth. The old `plan_model_mount_runtime_survey` command operation, dispatch arm, bridge response wrapper, backend marker, and JS command-envelope builder are retired. The old fail-closed public survey facade, JS hardware probe path, JS runtime-engine read path, JS LM Studio survey path, JS `state.receipt("runtime_survey")` creation, and no-commit success path remain retired. | Local runtime materialization, richer private-workspace/cTEE survey policy, and stable SDK/IDE runtime-survey APIs remain non-terminal. |
| Model_mount backend-lifecycle positive API | Public backend health/start/stop now call Rust daemon-core `plan_model_mount_backend_lifecycle`, receive Rust-authored `model-backend-lifecycle-controls` records with lifecycle evidence, commit only those records through Rust Agentgres model_mount record-state admission, and return Rust public responses that explicitly mark JS backend-registry reads, process control, and log reads/writes false. Backend-process planning now calls typed `daemonCoreModelMountApi.planModelMountBackendProcess`, backed by Rust `RuntimeKernelService::plan_model_mount_backend_process`; backend lifecycle planning calls typed `daemonCoreModelMountApi.planModelMountBackendLifecycle`, backed by `RuntimeKernelService::plan_model_mount_backend_lifecycle`; and Rust command protocol rejects the retired backend-process/backend-lifecycle command operations. Public backend list, mounted `backendRegistry()`/`backend()` lookup, and backend log readback now call Rust read-projection kinds `backends` and `backend_logs` with empty topology state plus runtime `state_dir`; Rust replays admitted `model-backend-lifecycle-controls/*.json`, filters out JS-authored lifecycle controls and retired `logs_read` read-as-mutation controls, and materializes backend projection/log truth from Rust-owned control records. The old JS backend-registry overlay export that merged derived defaults, `state.backends`, and process snapshots is deleted; `deriveBackendRegistry()`, `seedBackends()`, the JS `backendRegistryRecords()` default factory, and the retired `model-backends` local persistence map are also deleted, so backend inventory/projection cannot re-enter as fixture-carried, default-seeded, or fail-closed JS truth. The old backend-lifecycle required-record facade, Rust required planner file, command operations, JS runner methods, JS command-envelope builders, backend/source command markers, and lifecycle-control `logs_read` read path are removed from the hot path; missing Rust planning/projection still fails closed before JS backend registry, process, or log authority can run. | Actual backend process supervision/transport execution, richer backend process-state materialization, and stable SDK/IDE backend lifecycle APIs remain non-terminal. |
| Model_mount server-control positive API | Public server start/stop/restart/write, operation recording, and log append now call Rust daemon-core `plan_model_mount_server_control`, receive Rust-authored `model-server-controls` records with receipt refs and Rust server-control evidence, commit only those records through Rust Agentgres model_mount record-state admission, and return Rust public responses that explicitly mark JS state writes, JS log writes, and JS transport execution false. Public `serverStatus()` now calls Rust read-projection kind `server_status` with empty JS state plus runtime `state_dir`; Rust replays admitted `model-server-controls/*.json`, filters out JS-authored server controls, and materializes server status, last operation, last receipt, topology counts, and backend-state counts from Rust-owned records. Public `serverLogs()`, `serverEvents()`, and `serverLogRecords()` now call Rust read-projection kinds `server_logs`, `server_events`, and `server_log_records` with canonical `server_log_query` plus runtime `state_dir`; Rust replays admitted `model-server-controls/*.json`, filters JS-authored controls and retired read-as-mutation `logs_read`/`events_read`/`log_projection` records, and returns redacted log/event projections without committing a server-control record for reads. The old server-control required-record facade is no longer the hot path for migrated methods; missing Rust planning still fails closed before JS can write state or logs. | Actual process supervision/transport execution and stable SDK/IDE server-control APIs remain non-terminal. |
| Public model storage/download control positive Rust path | `catalogImportUrl()`, `downloadModel()`, `cancelDownload()`, `deleteModelArtifact()`, and `cleanupModelStorage()` now call typed `daemonCoreModelMountApi.planModelMountStorageControl`, backed by Rust `RuntimeKernel::plan_model_mount_storage_control`, receive Rust-authored `model-catalog-imports`, `model-downloads`, or `model-storage-controls` records with storage/download evidence, authority hashes, wallet/cTEE boundary facts, and Agentgres truth evidence, then commit only those records through Rust Agentgres model_mount record-state admission before public truth returns. `storageSummary()`, `listDownloads()`, and `downloadStatus()` now route through Rust read-projection kinds that replay admitted storage-control records and keep JS storage maps/filesystem scans out of public read truth. The old fail-closed catalog/download/storage JS facade, command-envelope request builder, model_mount storage-control command operation/dispatch arm, bridge response wrapper, JS lifecycle receipts, JS download/artifact maps, local network/filesystem materializers, direct `writeMap()` storage truth, and no-commit planner success remain retired. | Richer filesystem custody and catalog/download materialization plus stable SDK/IDE storage/download APIs remain non-terminal. |
| Public model route-control positive API | Public route upsert/test, mounted route selection, explicit-model endpoint resolution, and runtime explicit/run-override model-route selection now call typed `daemonCoreModelMountApi.planModelMountRouteControl`, backed by Rust `RuntimeKernelService::plan_model_mount_route_control`, receive Rust-authored route, route-test, route-selection, or endpoint-resolution records plus receipt refs, commit only those records through Rust Agentgres model_mount record-state admission, and return or forward committed Rust route-control truth; public route upsert/test no longer repopulate `state.routes` or send JS route-map `current_route` candidates into Rust, and mounted route selection now supplies route/endpoints/providers from Rust read-projection list APIs instead of raw JS route, endpoint, or provider maps; Rust route selection calls typed daemonCoreModelMountApi.admitModelMountRouteDecision for Rust model_mount route-decision admission and accepted receipt authoring before JS sees a selection; Rust rejects the retired admit_model_mount_route_decision command operation and the retired plan_model_mount_route_control command operation/dispatch arm/bridge response. | Public route list and route-selection/endpoint-resolution projection/replay are Rust-owned and route-decision plus route-control command transports, route-map writeback, JS current-route candidate transport, and raw mounted topology map candidates are retired for this route-control boundary; model invocation route selection beyond this admission cut, deeper wallet/cTEE route authority policy, hosted/provider materialization, Rust-owned topology joins, and stable SDK/IDE route APIs remain non-terminal. |
| Public model route list projection positive Rust path | `listRoutes()` now calls the Rust model_mount read-projection API with projection kind `routes` and runtime `state_dir`; Rust replays persisted `model-routes/*.json` Agentgres records, filters public list truth to Rust-authored route-control records carrying `model_mount.route_control`, `model_mount_route_control_rust_owned`, `rust_daemon_core_route_control_plan`, `agentgres_route_truth_required`, and receipt refs, then returns the deterministic list. The public-list JS `routes` request state remains empty, so JS route maps cannot return as topology truth. | deeper wallet/cTEE route authority policy, hosted/provider transports, and stable SDK/IDE route APIs remain non-terminal. |
| Public model route decision replay positive Rust path | `modelRouteDecisions()` now calls the Rust model_mount read-projection API with projection kind `model_route_decisions` and runtime `state_dir`; Rust replays persisted `model-route-selections/*.json` Agentgres records, filters public decision truth to Rust-authored route-selection records carrying `model_mount.route_control`, `model_mount.route_selection`, route-control evidence, receipt refs, and accepted-receipt binding, and the JS facade sends an empty request state so JS receipt arrays cannot synthesize route decisions. `modelRouteEndpointResolutions()` similarly replays `model-route-endpoint-resolutions/*.json` and filters endpoint-resolution truth to Rust route-control records. | model invocation route selection, deeper wallet/cTEE route authority policy, hosted/provider transports, and stable SDK/IDE route APIs remain non-terminal. |
| Public model endpoint list projection positive Rust path | `listEndpoints()` now calls the Rust model_mount read-projection API with projection kind `endpoints` and runtime `state_dir`; Rust replays persisted `model-route-endpoint-resolutions/*.json` and `model-endpoints/*.json` Agentgres records, filters public endpoint truth to Rust-authored route-control endpoint-resolution or artifact-endpoint records, applies Rust endpoint unmount records as removal, materializes canonical endpoint records with snake_case `provider_id` and `model_id`, and the JS facade sends an empty request state so JS endpoint maps cannot synthesize endpoint-list truth. | deeper wallet/cTEE route/artifact endpoint authority policy, hosted/provider endpoint discovery/materialization, and stable SDK/IDE endpoint APIs remain non-terminal. |
| Public model provider lifecycle record/projection positive Rust path | `providerHealth()`, `startProvider()`, and `stopProvider()` for migrated fixture/native-local and hosted/custom metadata providers now call Rust daemon-core `plan_model_mount_provider_lifecycle`, receive Rust-authored `model-provider-lifecycle-controls` records with lifecycle hashes, operation kinds, provider-lifecycle evidence, Agentgres provider-lifecycle truth evidence, and a Rust-contained hosted metadata transport contract where applicable, then require Rust Agentgres model_mount record-state commit before returning public lifecycle truth. Public provider-health list/latest and broad snapshot/projection provider-health fields now replay admitted lifecycle records in Rust, emit `agentgres_provider_lifecycle_health` truth with `receipt: null`, and ignore stale `provider_health` receipts plus JS telemetry inputs. Implicit lifecycle endpoint/model subjects now come from the Rust `endpoints` read-projection list instead of `state.endpoints`, and conformance guards the map-only endpoint negative so JS endpoint maps cannot feed provider lifecycle requests before Rust planning. The hosted/custom path uses `rust_model_mount_hosted_provider_lifecycle`, records `rust_hosted_provider_metadata_transport_materialized`, `ctee_hosted_provider_secret_not_exposed`, and `wallet_network_provider_transport_authority_bound`, persists `rust_materialized` transport execution while omitting retired JS/command/binary-bridge/compatibility fallback proof fields, and keeps JS provider driver execution, JS lifecycle receipts, provider-map writes, endpoint-map subject truth, projection writes, and fail-closed refusal truth retired. | Live external hosted API/model payload execution, deeper receipt/state-root binding beyond record-state commit, Rust-owned topology joins, and stable SDK/IDE provider-lifecycle APIs remain non-terminal. |
| Public model artifact/endpoint record positive Rust path | `importModel()`, `mountEndpoint()`, and `unmountEndpoint()` now call typed `daemonCoreModelMountApi.planModelMountArtifactEndpoint`, backed by Rust `RuntimeKernelService::plan_model_mount_artifact_endpoint`, receive Rust-authored `model-artifacts` or `model-endpoints` records with artifact/endpoint hashes, authority hashes, wallet/cTEE boundary facts, and Agentgres artifact/endpoint truth evidence, then require Rust Agentgres model_mount record-state commit before returning public artifact or endpoint truth. The old `plan_model_mount_artifact_endpoint` command operation, command-dispatch arm, bridge request/response wrapper, backend marker, and JS command-envelope builder are retired. `listArtifacts()` and `listEndpoints()` now replay those admitted artifact-endpoint records in Rust, merge them with provider-inventory and route endpoint-resolution materializations, apply Rust unmount records as endpoint removal, and filter JS-authored artifact/endpoint truth. The old fail-closed artifact/endpoint JS facade, JS artifact/endpoint map mutation/readback, JS lifecycle receipt synthesis, `writeMap("model-artifacts")`, `writeMap("model-endpoints")`, local materialization, no-commit planner success, and JS list truth remain retired. | Hosted/provider endpoint discovery/materialization, deeper receipt/state-root binding beyond record-state commit, and stable SDK/IDE artifact/endpoint APIs remain non-terminal. |
| Public model provider inventory record/replay positive Rust path | `listProviderModels()` and `listProviderLoaded()` now call Rust daemon-core `plan_model_mount_provider_inventory`, including fixture/local-folder, native-local, and hosted metadata providers, receive Rust-authored `model-provider-inventory` records with inventory hashes, operation kinds, provider-inventory evidence, Agentgres provider-inventory truth evidence, and a Rust-contained hosted metadata transport contract where applicable, then require Rust Agentgres model_mount record-state commit before returning public inventory truth. Hosted/nonlocal inventory uses Rust `rust_model_mount_hosted_provider_inventory`, records canonical provider metadata/item refs, records `rust_hosted_provider_metadata_transport_materialized`, `ctee_hosted_provider_secret_not_exposed`, and `wallet_network_provider_transport_authority_bound`, and persists `rust_materialized` transport execution while omitting retired JS/command/binary-bridge/compatibility fallback proof fields. `providerInventoryRecords()` calls Rust read-projection kind `provider_inventory_records` with runtime `state_dir`; Rust replays persisted `model-provider-inventory/*.json` records and filters truth to Rust-authored provider-inventory records. The old no-commit planner success, hosted fail-closed facade, JS driver execution, and JS artifact/instance materialization paths stay retired. | Live external hosted catalog API execution, hosted/provider endpoint discovery/materialization, deeper receipt/state-root binding beyond record-state commit, and stable SDK/IDE provider-inventory APIs remain non-terminal. |
| Public model catalog-search projection positive Rust path | Public `catalogSearch()` now calls Rust model_mount read-projection kind `catalog_search` with canonical query state and runtime `state_dir`; Rust replays admitted `model-provider-inventory/*.json` records, filters truth to Rust-authored `list_models` provider-inventory records, and returns Rust-authored catalog-search entries bound to inventory record ids and inventory hashes. The old JS provider-port iteration, JS result aggregation, entry enrichment, and `lastCatalogSearch` write path remain retired. | Hosted/provider catalog transports, richer hosted catalog metadata/materialization, and stable SDK/IDE catalog APIs remain non-terminal. |
| Public provider-inventory topology/catalog materialization positive Rust path | `listArtifacts()`, `listProductArtifacts()`, `listProviders()`, `runtimeModelCatalogList()`, and `openAiModelList()` now call Rust model_mount read-projection kinds with runtime `state_dir`; Rust replays admitted `model-provider-inventory/*.json` records, filters out JS-authored inventory, materializes provider/product-artifact/runtime-catalog/OpenAI-list records from Rust fixture/native-local inventory, merges admitted artifact-endpoint `model-artifacts/*.json` into the public artifact list, and keeps dedicated request state empty so JS topology maps cannot return as provider/catalog/artifact truth. | Hosted/provider endpoint discovery/materialization is still not terminal because provider-inventory records do not yet carry hosted endpoint truth; public endpoint lists are Rust-owned from route-control endpoint-resolution plus artifact-endpoint replay. Hosted/provider transports, richer catalog metadata/status, and stable SDK/IDE provider/catalog APIs remain non-terminal. |
| Public model-capability projection positive Rust path | `listModelCapabilities()` now calls Rust model_mount read-projection kind `model_capabilities` with empty request state plus runtime `state_dir`; Rust replays admitted `model-routes`, `model-route-endpoint-resolutions`, `model-provider-inventory`, `model-artifacts`, `model-endpoints`, and `model-instances` records, filters out JS-authored topology truth, and emits canonical `ioi.model-capability.v1` readiness contracts instead of a default empty placeholder or JS materializer. | Route/artifact-endpoint-derived capability projection is Rust-owned for admitted local topology; richer hosted/provider materialization, wallet/cTEE authority binding, and stable SDK/IDE capability APIs remain non-terminal. |
| Public model tokenizer positive API | Public tokenize/count/context-fit utility facades now call typed `daemonCoreModelMountApi.planModelMountTokenizer`, backed by Rust daemon-core `RuntimeKernelService::plan_model_mount_tokenizer`, consume the Rust route-selection record/accepted receipt from `model_mount.route.select`, receive Rust-authored token/count/context-fit records plus receipt refs, commit only those records through Rust Agentgres model_mount record-state admission, and return committed Rust tokenizer truth. Tokenizer-required refusal records now call typed `daemonCoreModelMountApi.planModelMountTokenizerRequired`, backed by Rust `RuntimeKernelService::plan_model_mount_tokenizer_required`; the old tokenizer and required-control command operations, dispatch arms, bridge request/response wrappers, backend markers, and JS command-envelope builders are retired. `modelTokenizerRecords()` now calls Rust read-projection kind `model_tokenizer_records` with runtime `state_dir`; Rust replays persisted `model-tokenizer-utilities/*.json` records and filters truth to Rust-authored tokenizer/context-fit records with tokenizer and Agentgres truth evidence. The old required-record shim, JS context-window fallback, JS tokenization/context-fit receipts, route mutation, truncation, response-envelope shaping, and JS tokenizer projection path are retired from the hot path. | Receipt/state-root binding beyond record-state commit, deeper wallet/cTEE tokenizer policy, and stable SDK/IDE tokenizer APIs remain non-terminal. |
| Public model invocation positive Rust path | Public fixture/local-folder non-stream invocation and native-local stream invocation now call Rust-owned route selection records/receipts, provider-execution admission, provider invocation/stream execution, provider-result admission, accepted-receipt transition planning, receipt binding, StepModule projection, Agentgres operation/state-root binding, and Rust-authored receipt persistence through `model_mount_invocation_positive_rust_path`; the old public invocation fail-closed facade, JS provider driver execution, JS invocation receipt creation, route-state persistence, non-stream stream fallback, and provider-native request-shaping hook are retired from those migrated paths. | Hosted/OpenAI/Ollama/LM Studio transports, conversation-state and stream-completion projection, durable projection/replay, deeper wallet/cTEE invocation policy, and stable SDK/IDE invocation APIs remain non-terminal. |
| Public model conversation/stream-completion positive Rust path | Conversation-state writes call typed `daemonCoreModelMountApi.planModelMountConversationState`, backed by Rust `RuntimeKernelService::plan_model_mount_conversation_state`, commit only the Rust-authored `model-conversations` record through Rust Agentgres model_mount record-state admission, and update the local continuation cache only after commit. Native stream completion calls typed `daemonCoreModelMountApi.planModelMountStreamCompletion`, backed by Rust `RuntimeKernelService::plan_model_mount_stream_completion`, commits only the Rust-authored conversation record, and persists only the Rust-authored `model_invocation_stream_completed` receipt with receipt_binder, accepted-receipt append, StepModule result, Agentgres operation/state-root/resulting-head bindings, and conversation/stream hashes. The old conversation/stream command operations, dispatch arms, bridge request/response wrappers, backend markers, JS command-envelope builders, JS conversation write refusal path, JS stream-completion receipt synthesis, JS receipt-binding construction, direct `state.receipt("model_invocation_stream_completed")`, and `writeMap("model-conversations")` remain retired for these edges. | hosted stream finalization parity, deeper wallet/cTEE conversation authority policy, and stable SDK/IDE conversation APIs remain non-terminal. |
| Public model conversation list projection positive Rust path | `listConversations()` now calls the Rust model_mount read-projection API with projection kind `model_conversation_states` and runtime `state_dir`; Rust replays persisted `model-conversations/*.json` Agentgres records, filters public list truth to records carrying `model_mount.conversation` boundary, conversation hash, Rust conversation-state or stream-completion evidence, and Agentgres conversation-truth evidence, then sorts and returns the list. The old fail-closed JS list facade, public-list `conversation_states` request input, and broad snapshot/projection `conversation_states: state.listConversations()` input remain retired. | deeper wallet/cTEE conversation authority policy, hosted stream parity, and stable SDK/IDE conversation APIs remain non-terminal. |
| Public model load/unload positive Rust path | Public load/unload now calls Rust provider lifecycle planning, Rust instance lifecycle planning, and Rust Agentgres `model-instances` record-state commit before returning committed Rust instance truth without refreshing `state.instances`. The canonical estimate-only now calls Rust instance-lifecycle estimate planning, skips provider lifecycle/JS sizing/JS driver execution, commits the Rust-authored estimate record through Rust Agentgres, and returns only that committed estimate truth. The old public load/unload edge refusal, JS provider driver execution, JS lifecycle receipt creation, JS estimate facade/sizing path, `commitModelInstanceRecordState`, direct/post-commit `state.instances` mutation, supersede-on-load mutation, and `writeMap("model-instances")` paths remain retired. | Public instance list projection/replay is Rust-owned; deeper receipt/state-root binding, richer load sizing/materialization, and stable SDK/IDE load/unload APIs remain non-terminal. |
| Public model instance maintenance positive Rust path | Loaded-instance idle eviction, duplicate coalescing, and explicit supersede now select candidate instances and endpoint/provider enrichment from Rust read projections, call Rust `plan_model_mount_instance_lifecycle`, require Rust-authored evict/supersede records with canonical snake_case metadata and `superseded_by` binding for supersede transitions, and commit only those records through Rust Agentgres `model-instances` record-state admission without refreshing `state.instances`. Map-only JS instance, endpoint, or provider rows cannot trigger or enrich maintenance planning. The old maintenance fail-closed facade, JS lifecycle receipts, direct pre/post-commit instance mutation, `writeMap("model-instances")`, and compatibility `supersededBy` metadata remain retired. | Public instance list projection/replay is Rust-owned; deeper receipt/state-root binding, and stable SDK/IDE maintenance APIs remain non-terminal. |
| Public model instance list projection positive Rust path | `listInstances()` now calls the Rust model_mount read-projection API with projection kind `instances` and runtime `state_dir`; Rust replays persisted `model-instances/*.json` Agentgres records, filters public list truth to Rust-authored instance lifecycle records carrying lifecycle hashes, `rust_model_mount_instance_lifecycle`, and `agentgres_model_instance_registry_planned` evidence, then returns the deterministic list. The public-list JS `instances` request state remains empty, so JS instance maps cannot return as topology truth. | deeper receipt/state-root binding beyond record-state commit, hosted/provider transports, and stable SDK/IDE instance APIs remain non-terminal. |
| Public runtime memory read projection positive API | Explicit public thread/agent memory list, policy, path, status, and validation route projections now call Rust daemon-core `project_runtime_memory_projection` through typed `daemonCoreThreadMemoryApi.projectRuntimeMemoryProjection`; JS supplies only route-owned thread/agent context, filters, and runtime `state_dir`, while Rust replays admitted `memory-records/*.json` and `memory-policies/*.json`, filters active canonical records, synthesizes effective policy/path/status/validation truth, and rejects retired JS projection candidate transport. Slice 1250 retires the old top-level `/v1/memory`, `/v1/memory/records`, `/v1/memory/policy`, `/v1/memory/path`, and `/v1/memory/validate` context-query/body routes, daemon-store memory context helpers, and SDK global memory status/validation clients; conformance guards that only explicit thread/agent memory protocol routes can reach the Rust projection/control path. Rust rejects the old `project_runtime_memory_projection` command operation. | Wallet/policy authority, cTEE-coupled private memory custody, direct memory admission/storage APIs, and stable IDE memory APIs remain non-terminal. |
| Conversation-artifact read/control positive API | Public and thread-scoped conversation-artifact list, get, and revision route projections now call Rust daemon-core `project_runtime_conversation_artifact_projection` through the mounted conversation-artifact surface; read requests send canonical request fields plus runtime `state_dir`, Rust replays admitted `artifacts/*.json` conversation-artifact records, filters active canonical records, and rejects retired JS artifact candidate transport. Create/action/export/promote now call Rust daemon-core `plan_runtime_conversation_artifact_control` with runtime `state_dir`; Rust replays admitted `artifacts/*.json` records for existing artifact truth, rejects retired control candidate transport, and the daemon commits only the Rust-authored artifact through Rust Agentgres artifact-state admission. JS no longer supplies artifact candidates for control planning, fails closed when Rust planning/projection/commit is missing or mismatched, and direct `ConversationArtifactStore` mutation writers stay retired. The mounted surface now resolves control and projection through the single positive `contextPolicyCore` startup mount without `conversationArtifactControlRunner`, `conversationArtifactProjectionRunner`, or `store.contextPolicyCore ?? contextPolicyCore` fallback wrappers. | Durable ArtifactRef/PayloadRef admission, richer Agentgres-backed mutation replay/projection storage, wallet/cTEE authority where needed, and stable SDK/IDE artifact APIs remain non-terminal. |
| Runtime managed-session projection/control positive API | Public managed-session inspection now calls Rust daemon-core `project_runtime_managed_session_projection` with runtime `state_dir`; Rust replays admitted `events/*.jsonl` records, derives session projection cards in daemon-core, and rejects JS-supplied projection/session candidates. Public managed-session control now also sends runtime `state_dir`; Rust replays the selected current session before planning, rejects JS-supplied session candidate transport, and only then returns the Rust-authored `managed_session.controlled` runtime event for admission. JS supplies request facts, validates Rust event identity, ignores retired control aliases, keeps runtime-bridge inspect/control dispatch and JS result-envelope authorship retired, and now reaches the helper only through the daemon-mounted auxiliary `contextPolicyCore` dependency with no `store.contextPolicyCore` fallback. | Durable managed-session record storage beyond runtime-event replay, wallet/cTEE session authority, and stable IDE/CLI/SDK APIs remain non-terminal. |
| Runtime workspace-change projection/control positive API | Public workspace-change inspection now calls Rust daemon-core `project_runtime_workspace_change_projection` with runtime `state_dir`; Rust replays admitted `events/*.jsonl` records, derives workspace-change review cards in daemon-core, and rejects JS-supplied projection/change candidates. Public workspace-change accept/reject/rollback control now also sends runtime `state_dir`; Rust replays the selected current change before transition validation, rejects JS-supplied change candidate transport, and only then returns the Rust-authored `workspace_change.controlled` runtime event for admission. JS supplies request facts, validates Rust event identity, ignores retired control aliases, keeps runtime-bridge inspect/control dispatch, request-hash/receipt synthesis, fixture fallback projection, and JS result-envelope authorship retired, and now reaches the helper only through the daemon-mounted auxiliary `contextPolicyCore` dependency with no `store.contextPolicyCore` fallback. | Durable workspace-change record storage beyond runtime-event replay, wallet/workspace rollback authority, and stable IDE/CLI/SDK APIs remain non-terminal. |
| Runtime thread-fork control positive API | Public thread fork now calls Rust daemon-core `plan_runtime_thread_fork_control`, receives a Rust-authored forked agent, forked thread projection, and `thread.forked` runtime event, commits only the forked agent through Agentgres-backed `writeAgent`, validates the Rust projection, and admits only the Rust-authored event. JS supplies temporary canonical source thread/agent facts and request fields, ignores retired aliases, keeps `createAgent`, JS event synthesis, runtime-event idempotency lookup, and JS receipt/policy evidence retired, and now reaches the helper only through the daemon-mounted auxiliary `contextPolicyCore` dependency with no `store.contextPolicyCore` fallback. | Durable Agentgres-backed thread-fork storage/replay/projection, wallet/cTEE fork authority, and stable IDE/CLI/SDK APIs remain non-terminal. |
| Public approval control Rust-owned | Rust `approval.rs` owns approval manifest planning, coding-tool approval satisfaction projection, approval satisfaction planning, approval block result/event materialization, public approval request authority issuance, Rust approval-lease authority, public approval queue projection, and approval request/decision/revoke response shaping behind typed `daemonCoreApprovalApi` methods. Public request/decision/revoke state planning now sends runtime `state_dir`; Rust replays the target run/agent from admitted Agentgres state, can resolve the latest run without a JS-supplied run id, and rejects retired JS `run`/`agent` candidate transport. Public request control calls Rust `authorize_approval_request` through the typed API before state planning; Rust requires request authority receipt refs, emits an authority hash/record plus Rust-authored approval lease, and the request state planner fails closed without those bindings. Public decision/revoke control calls Rust `authorize_approval_decision` through the typed API before state planning; Rust requires a typed `wallet_approval_grant` object plus authority receipts for approve, reject, and revoke, derives the wallet approval grant hash/ref, emits an authority hash/record plus approval lease, and the decision/revoke state planners fail closed without those bindings before any JS commit can persist. The daemon now mounts `approvalStateCore`; the JS approval-state runner, generic command invoker, Rust approval command operations, command/env fallback, response normalizer, JS approval lease helper, request/decision/lease scanning, approval satisfaction projection, approval readback helpers, direct runtime-event append, JS run/agent target lookup, and JS approval block facade are retired. | Wallet.network grant issuance semantics, durable projection/replay storage, and stable IDE/CLI/SDK approval APIs remain beyond the thin JS protocol-client scaffolding. |
| Public approval decision grant artifact binding | `authorize_approval_decision` now requires approve, reject, and revoke requests to carry a typed `wallet_approval_grant` artifact; Rust verifies the grant structure, derives `wallet_approval_grant_hash` and `wallet_approval_grant_ref`, uses that derived ref as `wallet_network_grant_refs`, and binds it into the Rust authority hash/record before state planning. The public JS approval surface forwards the grant object but always blanks caller `authority_grant_refs`, and focused Rust/JS tests plus conformance reject the old JS-minted grant-ref path for all decision outcomes. | Broader wallet.network grant issuance/signature-consumption semantics, durable authority projection/replay storage, and stable IDE/CLI/SDK approval APIs remain non-terminal. |
| Public approval queue/read projection Rust-owned | `projectApprovalQueue` is a typed Rust daemon-core approval API method; public queue reads now send runtime `state_dir`, Rust replays admitted `agents/*.json` and `runs/*.json` Agentgres projections, rejects JS `agent`/`run`/`runs` candidate transport, derives pending/resolved approval queue records from replayed run/agent approval projections, filters resolved records unless requested, emits canonical snake_case request/decision/lease/receipt fields, and the public `GET /v1/threads/:thread_id/approvals` route calls `approvalSurface.listThreadApprovals()` through mounted `approvalStateCore` instead of any JS event/readback helper, candidate collector, deleted approval-state runner, generic command invoker, or Rust command operation. | Richer approval authority projection/replay storage and stable direct Rust approval read APIs remain beyond the thin JS protocol-client scaffolding. |
| Coding-tool approval satisfaction positive API | `projectCodingToolApprovalSatisfaction` and `planCodingToolApprovalSatisfaction` are typed Rust daemon-core approval API methods; Rust now requires runtime `state_dir`, replays admitted `agents/*.json` and `runs/*.json` projections to derive approval request/decision/lease state, rejects JS `agent`/`run` candidate transport, attaches the manifest for satisfaction evaluation, selects revocation as the latest decision when present, and invocation can proceed past an approval manifest only when Rust returns a satisfied record with approval receipts/policy refs. The JS store projection callback, projection-context run/agent helper, exported JS manifest matcher, generic command invoker, and Rust command operations are retired. | Remaining approval authority replay/projection APIs still need richer Rust-owned durable storage and stable read APIs. |
| Coding-tool approval block positive API | `planCodingToolApprovalBlock` is a typed Rust daemon-core approval API method; unsatisfied approval now returns a Rust-shaped blocked coding-tool result/event envelope with approval refs instead of calling `blockCodingToolForApproval()`, the generic command invoker, or a Rust approval command operation. | Approval-block persistence still needs direct Rust authority/admission ownership beyond the current result-event admission path. |
| Coding-tool budget block positive API | `planCodingToolBudgetBlock` is a typed `daemonCoreContextLifecycleApi` method backed by Rust `policy/context_lifecycle.rs`; blocked budget execution now returns a Rust-shaped coding-tool result/event envelope with budget status, policy refs, receipt refs, and canonical snake_case fields, and the invocation hot path admits that blocked event through Rust `admit_coding_tool_result_event` before returning the policy error. The JS governance surface only forwards canonical request facts to Rust, sends no generic command `operation`/`backend` envelope, remains fail-closed when the typed context lifecycle API is absent, and Command-protocol deletion guards keep the old `plan_coding_tool_budget_block` command operation absent. | Broader diagnostics/projection/replay work remains outside this cut. |
| Coding-tool budget recovery control positive API | `planCodingToolBudgetRecoveryStateUpdate` and `planCodingToolBudgetRecoveryControl` are typed `daemonCoreRuntimeControlApi` methods backed by Rust `policy/coding_tool_budget_recovery.rs`; the public run route validates the Rust-authored operator control and run projection for retry/request/override actions, commits only that run through Agentgres-backed `writeRun`, requires wallet.network grant plus authority receipt refs for override authority, binds the Rust authority hash into the override projection, sends no generic command `operation`/`backend` envelope, resolves the budget-recovery surface only through the positive `contextPolicyCore` constructor mount, removes the old admission-required command plus standalone JS policy/result helper module, and Command-protocol deletion guards keep the old budget-recovery runtime-control command operations absent. `CodingToolBudgetRecoveryCommandError`, `CodingToolBudgetRecovery*BridgeRequest`, `plan_coding_tool_budget_recovery_*_response`, `rust_coding_tool_budget_recovery_*_command`, the `store.contextPolicyCore` / `store.contextPolicyCore ?? null` surface fallback, and the bridge-shaped owner proof tests are deleted or conformance-guarded, so the direct API path cannot fall back to a command-shaped Rust owner wrapper or store-mounted planner fallback. | Retry-event materialization, durable replay/projection, and deeper approval authority projection remain non-terminal. |
| Coding-tool result envelope/context planning positive API | `plan_coding_tool_result_envelope` is a Rust daemon-core operation; normal success/failure execution requires a Rust-planned StepModule context before workload dispatch and a Rust-planned result `payload_summary` plus runtime-event candidate before Agentgres result-event admission. JS forwards canonical facts and validates the Rust plan instead of authoring StepModule context, source-event kind, payload summary, or result-event truth. | JS still coordinates artifact/snapshot materialization, diagnostics orchestration, typed workload API calls, and some readback/projection adapter context. |
| Coding-tool result-event admission positive API | `admit_coding_tool_result_event` is a Rust daemon-core operation; successful, failed, and approval-blocked coding-tool result events now send only event facts plus runtime `state_dir`, Rust derives latest sequence/head/state root from admitted `events/*.jsonl`, rejects caller-supplied `latest_seq`, `expected_head`, and `state_root_before`, replays an existing admitted event for duplicate idempotency keys from Agentgres truth instead of letting JS read a local idempotency cache, and requires Rust Agentgres admission with receipt refs, payload refs, storage admission, and projection watermarks before JS can register them for replay. | JS still coordinates diagnostics orchestration and some readback/projection adapter context. |
| Coding-tool command-stream admission positive API | `admit_coding_tool_command_stream_events` is a Rust daemon-core operation; command-stream admission now sends only command/request/result facts plus runtime `state_dir`, Rust derives latest sequence/head/state root from admitted `events/*.jsonl`, rejects caller-supplied `latest_seq`, `expected_head`, and `state_root_before`, and owns stream request evaluation, stdout/stderr chunking, command-stream event materialization, Agentgres storage admission, receipt/state-root binding, payload refs, and projection watermarks before JS can register stream events for replay. | JS still forwards StepModule/workload request context through the typed workload API and coordinates diagnostics follow-up plus projection/readback adapters. |
| Coding-tool StepModule invocation construction Rust-owned | `daemonCoreWorkloadApi.runCodingToolStepModule` now calls Rust `RuntimeKernelService::run_coding_tool_step_module` with canonical coding-tool request facts; Rust daemon-core owns the migrated tool contract table, input hashing, invocation id generation, authority/custody/backend fields, workload dispatch request construction, router admission, receipt binding, Agentgres admission, and projection record creation. JS no longer imports the coding-tool StepModule ABI builder, passes a `StepModuleInvocation`, builds a command envelope, or sends `operation`/`backend` transport fields. | JS still coordinates diagnostics follow-up plus projection/readback adapters as protocol-client scaffolding over the typed workload API. |
| Coding-tool patch workspace snapshot capture Rust-owned | Rust `workspace_restore.rs` now emits snapshot capture records, snapshot artifact records, and snapshot runtime events with canonical ids, hashes, trigger context, receipt refs, artifact refs, restore metadata, and admission identity through typed `daemonCoreWorkspaceRestoreApi.captureWorkspaceSnapshotFiles`; `file.apply_patch` consumes that Rust output through the mounted `workspaceRestoreCore`, commits only the Rust snapshot artifact through Rust Agentgres artifact-state admission, admits only the Rust snapshot event through Rust Agentgres runtime-event admission, and no longer calls JS snapshot-event authoring, the generic command invoker, or a Rust command operation before returning snapshot truth. | Diagnostics follow-up remains JS-coordinated and durable projection/replay storage remains non-terminal. |
| Public workspace snapshot/restore API Rust-owned | `projectWorkspaceSnapshotList`, `projectWorkspaceSnapshotContentPackage`, `previewWorkspaceSnapshotRestore`, and `applyWorkspaceSnapshotRestore` are typed Rust daemon-core workspace restore API methods backed by `workspace_restore.rs`; the public daemon surface now calls the mounted `workspaceRestoreCore` for list/content/preview/apply, requires Rust `projection`, `restore_preview`, and `restore_apply` envelopes, commits only Rust-authored restore preview/apply artifact records through Rust Agentgres artifact-state admission, admits only Rust-authored restore preview/apply runtime events through Rust Agentgres event admission, and no longer derives snapshot projection truth from JS runtime events or `codingArtifacts`. The generic command invoker and Rust command operations are retired for this family. | Agentgres-backed durable projection/replay storage and stable IDE/CLI/SDK workspace restore read APIs remain beyond the thin JS protocol-client scaffolding; the JS runner facade/normalizer transport is gone. |
| Coding-tool workload observation contract canonicalized | Rust `coding_tool_workspace.rs` now emits snake_case result observations for `workspace.status`, `git.diff`, `file.inspect`, `file.apply_patch`, `test.run`, and `lsp.diagnostics`; the JS Rust-live result wrapper recursively strips retired camelCase observation keys instead of translating them, and coding-tool result summaries/output contracts read only canonical Rust result fields. | JS still coordinates post-edit diagnostics, projection, replay, and public readback adapters after forwarding canonical observation facts into the Rust result-envelope planner. |
| Post-edit diagnostics feedback plan Rust-owned | `plan_post_edit_diagnostics_feedback` is a Rust daemon-core operation; Rust owns post-edit diagnostics mode normalization, changed-path selection from canonical `changed_files`, repair-policy normalization, workspace snapshot and rollback refs, auto diagnostics `tool_call_id`, `diagnostics_repair_context`, and the `lsp.diagnostics` request envelope. The diagnostics feedback JS surface now fails closed without the Rust planner and only forwards the Rust-authored request to the mounted coding-tool invocation surface. | The actual `lsp.diagnostics` follow-up now runs through the typed workload API, while diagnostics projection/replay/readback remain JS adapter scaffolding until Rust projection APIs replace them. |
| Runtime diagnostics repair policy projection positive API | `project_runtime_diagnostics_repair_policy` is a Rust daemon-core operation for diagnostics rollback repair policy projection; pending diagnostics feedback now reads diagnostic completion candidates through Rust runtime thread-event replay (`runtimeEventsForStream(..., { since_seq: 0 })`) instead of `runtimeEventStream()` local cache truth, then sends runtime `state_dir` plus diagnostic event ids to Rust, Rust replays the admitted Agentgres runtime events to derive diagnostic status/count, rollback refs, snapshot refs, source tool-call refs, repair contexts, and deterministic injection id, and retired JS policy-input transport (`diagnostic_status`, `diagnostic_count`, `workspace_snapshot_refs`, `rollback_refs`, `source_tool_call_ids`, `diagnostics_repair_contexts`, `receipt_refs`) fails closed. The JS `diagnosticsRollbackRepairPolicy`, default-decision helper, context-policy aggregation facade, diagnostics policy candidate transport, and local pending-event cache read are retired instead of kept as compatibility shims. | JS still selects temporary diagnostic event ids from Rust replay records and coordinates typed workload API follow-up; wallet-governed override authority, broader diagnostics orchestration, receipt/state-root binding, and stable SDK/IDE diagnostics APIs remain non-terminal. |
| Context-policy/state-update Rust planning | Context budget, coding-tool budget policy, coding-tool budget block, compaction policy, context-compaction plan, and context-compaction state update now use typed `daemonCoreContextLifecycleApi` methods; the runtime context-policy surface resolves compactThread, thread/run context-budget events, and thread compaction-policy events only through the constructor-mounted positive `contextPolicyCore`, with `store?.contextPolicyCore ?? contextPolicyCore` and `store.contextPolicyCore` fallback retired. Coding-tool budget recovery, workflow-edit admission-required, diagnostics-repair admission-required, diagnostics operator override, operator turn-control admission-required, operator interrupt/steer, and run-cancel state/admission planning now use typed `daemonCoreRuntimeControlApi` methods; thread-control, runtime-bridge thread start/control, runtime-bridge turn submit, subagent record persistence, agent/thread/run creation, agent status/delete state-update, thread-turn admission-required, and lifecycle admission-required planning now use typed `daemonCoreThreadLifecycleApi` methods; workspace-trust warning/ack planning now uses typed `daemonCoreWorkspaceTrustApi.planWorkspaceTrustControlStateUpdate`; MCP control/catalog/search/fetch/serve planning now uses typed `daemonCoreMcpApi` methods. Thread-control, operator interrupt/steer, context-compaction plan, workspace-trust acknowledgement, and MCP-control state updates now send runtime `state_dir` replay handles instead of JS sequence/cache candidates; Rust derives latest event sequence from admitted `events/*.jsonl` records and rejects caller `seq`, `previous_latest_seq`, or event replay candidates. These migrated families send canonical request bodies without generic command `operation`/`backend` envelopes, Command-protocol deletion guards keep the old context lifecycle, runtime-control, thread-lifecycle state-update, admission-required, workspace-trust state-update, MCP control/catalog, and MCP serve command operations absent, `command_dispatch.rs` is deleted, and coding-tool budget recovery, diagnostics operator override, operator turn-control, operator interrupt/steer, and run-cancel have additionally deleted their Rust command-response wrappers, bridge request types, command error wrappers, `_command` source markers, and bridge-shaped owner proof tests. | Public JS context/state facades remain protocol scaffolding; move remaining deeper MCP/memory projection/custody/materialization work to stable Rust daemon-core protocol/API surfaces. |
| Runtime compositor/task-job command transport retirement | Task/job create/cancel planning, task/job read projection, workflow-edit control, managed-session projection/control, workspace-change projection/control, thread-fork control, conversation-artifact projection/control, and subagent projection/control now enter Rust through typed `daemonCoreRuntimeControlApi` or `daemonCoreRuntimeProjectionApi` methods backed by `RuntimeKernelService` direct APIs. Command-protocol deletion guards keep absent the old command operation names, `command_dispatch.rs` is deleted, Rust command-response wrappers/source markers are deleted, JS sends no generic `operation`/`backend` command envelopes for these hot paths, and conformance guards the retired command operations plus source-scan blockers. The runtime task/job surface resolves create/cancel/project only through the constructor-mounted positive `contextPolicyCore`, with `store.contextPolicyCore` and `store.contextPolicyCore ?? null` fallback retired; the auxiliary compositor surface now passes its constructor-mounted `contextPolicyCore` into managed-session, workspace-change, and thread-fork helpers with no `deps.contextPolicyCore ?? store.contextPolicyCore` or `store.contextPolicyCore` fallback; the stale `workflowEditRunner` constructor alias is deleted, and workflow-edit proposal/apply controls now resolve only through the constructor-mounted positive `contextPolicyCore` with `store?.contextPolicyCore ?? null` fallback retired. | Durable route-specific storage/replay, wallet/cTEE authority expansion, StepModuleRouter delegation execution, model_mount materialization, and stable IDE/CLI/SDK protocol APIs remain non-terminal. |
| Public context policy event APIs Rust-owned | Public `compactThread` now uses Rust `planContextCompaction` through `daemonCoreContextLifecycleApi` for the `context.compacted` event plan, sends runtime `state_dir` instead of caller `previous_latest_seq`, admits only the Rust-planned event through Rust Agentgres runtime-event admission, uses Rust `planContextCompactionStateUpdate` through the same typed API bound to the admitted event id/seq, and commits only Rust-planned run or agent projections through Agentgres-backed `writeRun`/`writeAgent`; public thread/run context-budget and thread compaction-policy now require Rust policy planning through typed context lifecycle APIs, Rust-authored runtime-event identity, and Rust Agentgres runtime-event admission before returning route truth, with compaction-policy execution composing through the Rust-owned `compactThread` path when Rust approves compaction. The context-policy surface no longer calls `latestRuntimeEventSeq()`, preassigns event `seq`, or recovers through `store.contextPolicyCore`; daemon startup supplies the only positive core mount. | Durable replay/projection, richer policy receipts/state roots, wallet/cTEE authority, and stable IDE/CLI/SDK APIs remain non-terminal. |
| Public agent create positive API | Public agent creation now calls Rust `plan_agent_create_state_update`; JS gathers candidate provider/model-route/MCP/runtime-control facts only after the Rust planner boundary exists, requires a Rust-returned `agent.create` projection with complete identity/timestamps, commits only that projection through Agentgres-backed `writeAgent`, and keeps direct `agents` map mutation retired. Public route handlers no longer accept or fall back to a route-level `lifecycleAdmissionRunner`; agent create passes only `store.contextPolicyCore` into the direct Rust-backed lifecycle function, and the lifecycle helper no longer accepts `agentCreateStateUpdateRunner` or `store.contextPolicyCore ?? null` as an alternate planner. | Wallet/cTEE policy, replay/projection, and direct Rust lifecycle APIs remain non-terminal beyond the temporary typed context-policy API scaffolding. |
| Public top-level thread create positive API | Top-level public thread creation now calls Rust `plan_thread_create_state_update`; JS gathers canonical agent/thread candidate facts only after the Rust planner boundary exists, requires Rust-returned `agent` and `thread` projections with matching identity, commits only the Rust-authored `thread.create` agent projection through Agentgres-backed `writeAgent`, projects the returned thread through the Rust thread/turn projection boundary, and remains fail-closed before route planning when the Rust planner is absent. Public route handlers no longer accept or fall back to a route-level `lifecycleAdmissionRunner`; thread create passes only `store.contextPolicyCore` into the direct Rust-backed lifecycle function, and the lifecycle helper no longer accepts `threadCreateStateUpdateRunner`, `runtimeBridgeThreadStartStateUpdateRunner`, or `store.contextPolicyCore ?? null` as alternate planners. | Wallet/cTEE policy, direct lifecycle protocol APIs, durable thread-create replay/projection storage, and typed context-policy API scaffolding remains temporary. Runtime-service thread start is now a separate Rust bridge-start positive path. |
| Runtime bridge thread start positive API | Runtime-service thread creation now calls Rust `plan_runtime_bridge_thread_start_agent_state_update`; JS requires the Rust-planned bridge agent to carry canonical `runtime_profile`, `runtime_session_id`, `runtime_bridge_id`, and `runtime_bridge_source`, commits only that agent through Agentgres-backed `writeAgent`, returns the Rust thread projection, and still never dispatches the deleted JS runtime bridge `startThread` path. | Durable managed-session storage/replay/projection, wallet/cTEE runtime-service authority, durable bridge replay/projection, direct protocol APIs, and command-transport retirement remain non-terminal. |
| Runtime bridge thread control positive API | Runtime-service thread resume/control now calls Rust `plan_runtime_bridge_thread_control_agent_state_update`; JS requires the Rust-planned `thread.runtime_bridge.control` agent/control envelope, commits only that bridge-control agent through Agentgres-backed `writeAgent`, returns the Rust thread projection, and still never dispatches the deleted JS runtime bridge `controlThread` path. | Durable managed-session storage/replay/projection, wallet/cTEE runtime-service authority, durable bridge replay/projection, direct protocol APIs, and command-transport retirement remain non-terminal. |
| Runtime-service lifecycle agent aliases retired | Rust thread-lifecycle start/control planning scrubs retired camel runtime-service identity aliases before returning projections, JS lifecycle normalizers reject any Rust response carrying them, lifecycle/session helpers read only `runtime_profile`, `runtime_session_id`, and `fixture_profile`, and Rust lifecycle/thread-event/workspace-trust replay readers no longer accept those aliases as truth. | Deletion/cancellation replay/projection, wallet/cTEE authority, and stable lifecycle protocol APIs remain non-terminal. |
| Runtime bridge turn submit positive API | Runtime-service turn creation now calls Rust `plan_runtime_bridge_turn_run_state_update`; JS builds only temporary run candidate facts and sends no pre-plan JS `projection` candidate into Rust, while Rust denies the retired request field. The daemon requires the Rust-planned `turn.runtime_bridge.submit` run projection, commits only that run through Agentgres-backed `writeRun`, then returns the Rust turn projection from the committed run, and still never dispatches the deleted JS runtime bridge `submitTurn` path. | Durable managed-session storage/replay/projection, wallet/cTEE runtime-service authority, durable bridge replay/projection, direct protocol APIs, and command-transport retirement remain non-terminal. |
| Public run create positive API | Agent-scoped public run creation now calls Rust `plan_run_create_state_update`; JS gathers agent/provider/model-route/memory/run-candidate facts only after the Rust planner boundary exists, requires a Rust-returned `run.create` projection with complete identity/timestamps, commits only that projection through Agentgres-backed `writeRun`, keeps direct `runs` map mutation retired, ignores retired thread/approval and diagnostics request aliases, and remains fail-closed before lookup/route/memory when the Rust planner is absent. Native agent run-create routes no longer accept or fall back to a route-level `lifecycleAdmissionRunner`; run create passes only `store.contextPolicyCore` into the direct Rust-backed lifecycle function, and the lifecycle helper no longer accepts `runCreateStateUpdateRunner`, `runtimeBridgeTurnRunStateUpdateRunner`, or `store.contextPolicyCore ?? null` as alternate planners. | Run replay/projection, wallet/cTEE policy, and direct Rust lifecycle APIs remain non-terminal beyond the temporary typed context-policy API scaffolding. |
| Public task create positive API | Public task creation now calls Rust `plan_runtime_task_job_create_state_update`; JS requires canonical `agent_id`, gathers the existing agent, model-route, memory, and run candidate only after the Rust planner boundary exists, and requires Rust-authored `task.create` task/job/checklist plus run projections before committing only the returned run through Agentgres-backed `writeRun`. Direct `createAgent`, `createRun`, JS task/job/checklist projection synthesis, retired request aliases, projection mismatch compatibility paths, and the constructor-injected `taskJobCreateRunner` alias remain retired. | Wallet/operator authority, durable task-create replay/projection storage, and direct Rust lifecycle APIs remain non-terminal beyond the typed context-policy daemon-core mount. |
| Public run cancel positive API | Public run cancellation now calls typed `daemonCoreRuntimeControlApi.planRunCancelStateUpdate`; JS supplies only the current run and cancel timestamp, requires a Rust-returned `run.cancel` projection with canceled status, terminal job/run events, runtime task/job/checklist records, receipts, and artifacts, commits only that Rust-authored projection through Agentgres-backed `writeRun`, and keeps direct run-map mutation plus JS runtime task/job/checklist/event/receipt/artifact materialization retired. Missing-planner refusals use typed `planRunCancelAdmissionRequired`; `cancelRun()` no longer reads or falls back to `state.runCancelRunner`, `state.contextPolicyCore`, or `state?.contextPolicyCore`, so both cancellation planning and admission-required refusal shaping resolve only the positive `contextPolicyCore` mount passed by the auxiliary surface or subagent cancellation composition. The generic command transport and old run-cancel command operations are rejected, and the Rust policy child has deleted `RunCancelCommandError`, `RunCancel*BridgeRequest`, `plan_run_cancel_*_response`, and `rust_run_cancel_*_command` source markers so run-cancel cannot preserve command-shaped owner wrappers. | Wallet/operator authority, cancellation replay/projection storage, and direct Rust lifecycle APIs remain non-terminal beyond the thin JS protocol-client surface. |
| Public task/job cancel positive API | Public task and job cancellation now call Rust `plan_runtime_task_job_cancel_state_update`; JS derives only the canonical run id from `task_`/`job_` public ids, supplies the current run and cancel timestamp, requires Rust-returned `task.cancel`/`job.cancel` projections with matching canceled task/job/checklist and run records, terminal events, receipts, and artifacts, then commits only the Rust-authored run through Agentgres-backed `writeRun`. Direct `cancelRun`, public-id fallback, JS task/job/checklist/event/receipt/artifact materialization, projection mismatch compatibility paths, and the constructor-injected `taskJobCancelRunner` alias are retired. | Wallet/operator authority, cancellation replay/projection storage, and direct Rust lifecycle APIs remain non-terminal beyond the typed context-policy daemon-core mount. |
| Public task/job read projection positive API | Public task/job list and get now call Rust `project_runtime_task_job_projection`; JS forwards only runtime `state_dir` plus canonical `agent_id`, `status`, `task_id`, and `job_id` request facts, while Rust replays admitted `runs/*.json` records from Agentgres state before constructing task/job records, filtering, and selecting public ids. The task/job surface no longer receives JS `runtimeTaskRecordForRun`/`runtimeJobRecordForRun` builders, no longer accepts the constructor-injected `taskJobProjectionRunner` alias, no longer sends `runs` candidate transport, retired `agentId` aliases remain ignored, and missing/mismatched Rust projections fail closed instead of falling back to JS record synthesis. | Wallet/operator authority, richer durable task/job-specific replay storage, and direct Rust lifecycle APIs remain non-terminal beyond the typed context-policy daemon-core mount. |
| Public non-runtime thread resume/turn create positive API | Public non-runtime thread resume now calls the direct Rust-backed lifecycle status API and returns the Rust thread projection; public non-runtime turn creation now calls the direct Rust-backed run-create API and returns the Rust turn projection. The thread-turn surface no longer accepts alternate `threadLifecycleRunner` or `threadTurnAdmissionRunner` mounts; status/update, run-create, and admission-required refusal planning resolve only through `contextPolicyCore`. Direct `store.updateAgent()`, `store.createRun()`, mounted `agentRunLifecycleSurface`, JS turn projection composition, and runtime-event append remain retired from the thread-turn surface. | Durable managed-session storage/replay/projection, direct lifecycle protocol APIs, durable replay/projection storage, and command-transport retirement remain non-terminal. |
| Diagnostics-blocked turn creation positive Rust path | Blocking post-edit diagnostics feedback now enters the same direct Rust-backed run-create path as canonical turn creation: JS injects the Rust-projected diagnostics feedback into the canonical turn request, calls the direct lifecycle run-create API with explicit planner/build dependencies, commits only the Rust-planned blocked run through Agentgres-backed `writeRun`, and returns the Rust turn projection. The old `thread_turn_diagnostics_block` / `turn.diagnostics_block` admission-required refusal path is retired and Rust rejects it. | Deletion/cancellation replay/projection, remaining runtime-control materialization/projection APIs, durable diagnostics replay/storage, wallet/cTEE diagnostics authority, stable lifecycle/diagnostics protocol APIs, and broader run lifecycle remain non-terminal. |
| Public agent status-control positive API | Public archive/unarchive/resume/close/reload now call Rust `plan_agent_status_state_update`; JS supplies the current agent and requested status facts, requires a Rust-returned agent projection plus matching operation kind, and commits only that Rust-authored projection through Agentgres-backed `writeAgent`. Missing planner output or mismatched operation kind fails closed before persistence. | Replay, projection, and broader lifecycle APIs remain temporary or fail-closed. |
| Public agent delete positive API | Public permanent delete now calls Rust `plan_agent_delete_state_update`; JS supplies the current agent fact, requires a Rust-returned `agent.delete` tombstone with `status: deleted` and `deletedAt`, commits only that tombstone through Agentgres-backed `writeAgent`, and keeps local removal/duplicate operation append retired. | Wallet/retention authority, deletion replay/projection, durable cache invalidation, and broader lifecycle APIs remain non-terminal. |
| Public thread runtime-control positive API | Rust `plan_thread_control_agent_state_update` now owns public mode/model/thinking and generic runtime-control state planning. The JS surface gathers canonical agent, control, `state_dir`/`event_stream_id`, and model-route facts, fails closed without the Rust planner before lookup, sends no caller `seq`, persists only the Rust-authored agent projection through Agentgres-backed `writeAgent`, and leaves direct control-event append plus JS latest-sequence cache reads retired under `thread-control-js-facade-retired`. Initial/normalized runtime controls, public control state shaping, and the lifecycle fallback seed now emit only canonical `approval_mode` control truth without top-level `approvalMode` or `updatedAt`; runtime-backed turn request shaping scrubs `threadMode` / `approvalMode` aliases before forwarding canonical `thread_mode` / `approval_mode`; and coding-tool approval plus workspace-trust consumers read `controls.approval_mode` rather than the retired alias. | Model route selection still depends on the separate model_mount route authority surface; remaining runtime-control materialization/projection APIs, deeper workspace-trust wallet/cTEE authority enforcement, replay/projection storage, and typed context-policy API scaffolding remains temporary. |
| Workspace-trust warning/ack positive API | Rust `plan_workspace_trust_control_state_update` now owns workspace-trust warning and acknowledgement event envelope planning through typed `daemonCoreWorkspaceTrustApi.planWorkspaceTrustControlStateUpdate`; the old workspace-trust state-update command operation is rejected by Rust. Thread mode updates require the Rust workspace-trust planner before lookup/write, persist only the Rust-authored thread-control agent projection, then admit Rust-authored workspace-trust warning events through `admit_runtime_thread_event`; acknowledgement routes send runtime `state_dir` to Rust, Rust replays admitted `events/*.jsonl` warning truth itself, rejects restored JS `events` candidate transport and caller-supplied `seq`, require Rust-authored acknowledgement events with receipts, and admit only those Rust-authored events. The JS workspace-trust state client no longer calls `runtimeEventsForStream()` or `latestRuntimeEventSeq()`, while old JS warning/ack payload builders and repository-context warning record path remain retired. | Wallet/cTEE workspace authority is represented as Rust-owned receipt/authority refs but still needs deeper direct cTEE/wallet enforcement and stable protocol APIs beyond the replay cache transport. |
| Generic runtime thread-event admission positive API | `admit_runtime_thread_event` is a Rust daemon-core operation; generic runtime event append now sends only the candidate event plus runtime `state_dir`, Rust derives latest sequence/head/state root from admitted `events/*.jsonl`, rejects caller-supplied `latest_seq`, `expected_head`, and `state_root_before`, and requires Rust Agentgres admission with receipt refs, storage admission, payload refs, and projection watermarks before JS can register the Rust-returned event for local replay. | JS still constructs some generic candidate event envelopes and keeps a temporary replay cache; stable public readback APIs still need direct Rust projection/replay protocol ownership. |
| Runtime thread-event projection positive API | `project_runtime_thread_events` is a Rust daemon-core operation; Rust now authors synthetic `thread.started` and run-event projection envelopes from admitted Agentgres `agents/*.json` and `runs/*.json` records under runtime `state_dir`, derives latest sequence/current head/state root/idempotency from admitted Agentgres `events/*.jsonl`, rejects retired projection request aliases plus caller-supplied cache fields (`latest_seq`, `expected_head`, `state_root_before`, and `existing_idempotency_keys`) and retired JS fact fields (`workspace_root`, `agent`, and `runs`), requires receipt refs, admits each projected event through Rust Agentgres admission, and returns only Rust-admitted event records for local replay registration. The thread-event surface no longer receives the JS `ttiEnvelopeForRunEvent` builder or forwards JS agent/run projection facts. | JS still maintains a temporary hydration/registration cache for event materialization; public lifecycle read projections now replay admitted Agentgres state directly through Rust, and stable Rust projection/replay protocol APIs remain non-terminal. |
| Runtime thread-event replay positive API | `project_runtime_thread_event_replay` is a Rust daemon-core operation; public stream/turn/run readback now sends replay kind, cursor, and runtime `state_dir` to Rust, Rust reads admitted Agentgres `events/*.jsonl` records itself, owns replay-kind selection, canonical `since_seq`/`last_event_id` cursor evaluation, required Agentgres admission refs, latest-sequence derivation, resulting state/head/watermark projection, and the returned event list, and rejects caller-supplied replay `events` and `latest_seq` transport. The JS stream/turn helpers no longer call `runtimeCursorSeq()` or filter by stream/turn locally; the JS replay-candidate collector and public `replayFromCanonicalState` alias are deleted. | JS still maintains a temporary hydration/registration cache and the mounted typed API adapter remains until stable Rust projection/replay protocol APIs replace it for IDE/SDK reads. |
| Runtime thread/turn projection positive API | `project_runtime_thread_turn_projection` is a Rust daemon-core operation reached through typed `daemonCoreAgentgresApi.projectRuntimeThreadTurnProjection`; public thread and turn records are Rust-authored projections over canonical agent/run facts and Rust replay events, including seq ranges, input/output item ids, usage envelope placement, runtime identity fields, projection hashes, and retired alias rejection. The JS `threadForAgent()` and `turnForRun()` helpers now fail closed without the Rust projection API instead of composing public records locally, and the old Agentgres command operation is rejected by Rust. | JS still supplies canonical agent/run/usage facts and a temporary replay cache; stable Rust projection/replay protocol APIs remain non-terminal beyond the thin JS protocol client. |
| Public operator turn-control positive API | Public operator interrupt/steer now call typed `daemonCoreRuntimeControlApi.planOperatorInterruptStateUpdate` / `planOperatorSteerStateUpdate`, validate the Rust-planned operator-control run projection, resolve the live run through the mounted run resolver, send `state_dir`/`event_stream_id` instead of caller `seq`, persist only the Rust-planned run through Agentgres-backed `writeRun`, and return the Rust operator-control envelope. Missing-planner refusals use typed `planOperatorTurnControlAdmissionRequired`; the thread-turn surface no longer accepts an alternate `operatorTurnControlAdmissionRunner` mount, so operator control state/admission planning resolves only through `contextPolicyCore`. The old JS runtime-bridge control, JS event append, local run-map mutation, JS latest-sequence cache read, generic command transport, and no-planner success paths remain retired, and Rust rejects the old operator-control command operations. `OperatorControlCommandError`, `DiagnosticsOperatorOverrideStateUpdateBridgeRequest`, `OperatorTurnControlAdmissionRequiredBridgeRequest`, `OperatorInterruptStateUpdateBridgeRequest`, `OperatorSteerStateUpdateBridgeRequest`, `plan_diagnostics_operator_override_state_update_response`, `plan_operator_*_response`, `rust_*_command` source markers, and bridge-shaped owner proof tests are deleted, so operator-control cannot preserve command-shaped Rust owner wrappers. | Wallet/runtime-control authority, durable replay/projection storage, deeper receipt/state-root binding, and stable SDK/IDE operator-control APIs remain non-terminal. |
| Public runtime account/node/tool catalog projection positive API | `project_runtime_tool_catalog` is a typed Rust daemon-core API reached through `daemonCoreRuntimeProjectionApi.projectRuntimeToolCatalog`; public `/v1/account`, `/v1/runtime/nodes`, and `/v1/tools` require the Rust projector through the mounted tool surface, return Rust-authored account/node/tool catalog records, keep canonical snake_case tool catalog entries, and reject projection mismatches. `createRuntimeToolSurface()` no longer accepts `toolCatalogRunner`; daemon startup mounts the positive `contextPolicyCore` API directly and conformance guards the retired alias absent. The old `RuntimeToolCatalogProjectionRequiredCore` path, generic JS `operation: "project_runtime_tool_catalog"` envelope, Rust command operation, dispatch arm, and command-response wrapper are retired; command-protocol deletion guards keep the old operation absent. | JS still supplies temporary request/environment facts; direct Rust catalog storage/replay, broader Agentgres-backed catalog persistence, wallet/network authority on external exposure, receipt/state-root binding, and stable protocol APIs remain non-terminal. |
| Public skill/hook registry projection positive API | `project_skill_hook_registry` is a typed Rust daemon-core API reached through `daemonCoreRuntimeProjectionApi.projectSkillHookRegistry`; public `/v1/skills`, `/v1/hooks`, and doctor skill/hook readiness require the Rust projector through the mounted skill-hook surface, return Rust-authored public catalog records, and reject registry-kind mismatches. `createRuntimeSkillHookSurface()` no longer accepts `skillHookRunner`; daemon startup mounts the positive `contextPolicyCore` API directly and conformance guards the retired alias absent. The old `SkillHookRegistryProjectionRequiredCore` path, generic JS `operation: "project_skill_hook_registry"` envelope, Rust command operation, dispatch arm, and command-response wrapper are retired; command-protocol deletion guards keep the old operation absent. The remaining command-shaped `operation` field is now removed from the skill/hook projection request/result contract; JS sends only `operation_kind`, Rust serializes no `operation`, and the normalizer strips stale `operation` fields instead of preserving compatibility. | JS still supplies temporary workspace/home request facts; direct Rust catalog persistence/replay, broader Agentgres-backed governance storage, wallet authority where applicable, receipt/state-root binding, and stable protocol APIs remain non-terminal. |
| Public repository workflow projection positive API | `project_repository_workflow` is a typed Rust daemon-core API reached through `daemonCoreRuntimeProjectionApi.projectRepositoryWorkflow`; public repository workflow routes require the Rust projector through the mounted repository surface, return Rust-authored repository context, branch policy, GitHub context, issue context, PR attempt, review gate, and GitHub PR create-plan records, and reject projection-kind mismatches. `createRuntimeRepositorySurface()` no longer accepts `repositoryRunner`; daemon startup mounts the positive `contextPolicyCore` API directly and conformance guards the retired alias absent. The old `RepositoryWorkflowProjectionRequiredCore` path, generic JS `operation: "project_repository_workflow"` envelope, Rust command operation, dispatch arm, and command-response wrapper are retired; command-protocol deletion guards keep the old operation absent. | JS still supplies temporary workspace request facts; durable Agentgres-backed repository workflow storage/replay, wallet authority for external exits, receipt/state-root binding, and stable protocol APIs remain non-terminal. |
| Public lifecycle projection positive API | `project_runtime_lifecycle` is a typed Rust daemon-core API reached through `daemonCoreRuntimeProjectionApi.projectRuntimeLifecycle`; public agent, thread, run, agent-run, thread usage, thread turn/detail/event, run wait/conversation/usage/event/replay/trace/inspect/computer-use/scorecard/artifact list/detail routes now send only route identifiers plus runtime `state_dir`, Rust replays admitted `agents/*.json`, `runs/*.json`, and `events/*.jsonl` Agentgres records before deriving projections, and caller-supplied JS lifecycle candidate transport is rejected. The mounted daemon lifecycle surface no longer reads JS agent/run maps, thread/turn helpers, usage helpers, event streams, replay helpers, trace helpers, or artifact resolvers before invoking Rust. `createRuntimeLifecycleProjectionSurface()` no longer accepts `lifecycleRunner`; daemon startup mounts the positive `contextPolicyCore` API directly and conformance guards the retired alias absent. The old `RuntimeLifecycleProjectionRequiredCore` path, generic JS `operation: "project_runtime_lifecycle"` envelope, Rust command operation, dispatch arm, command-response wrapper, JS cache candidate transport, and lifecycle replay-through-`eventsForRun` facade are retired; command-protocol deletion guards keep the old operation absent. | Wallet/cTEE authority on lifecycle exits, receipt/state-root binding for every lifecycle read projection, richer ArtifactRef/PayloadRef-aware artifact projection, and stable IDE/CLI/SDK protocol APIs remain non-terminal beyond the Rust-owned Agentgres replay projector. |
| Public memory projection positive API | `project_runtime_memory_projection` is reached through typed `daemonCoreThreadMemoryApi.projectRuntimeMemoryProjection`; explicit public thread/agent memory list, policy, path, status, and validation routes require the Rust projector through the mounted thread-memory surface, return Rust-selected memory projections, reject projection-kind mismatches, and reject retired JS projection candidate transport. JS now sends only route-owned thread/agent context, filters, and runtime `state_dir`; Rust replays admitted memory record/policy files before any public read truth can return. Slice 1250 retires the old top-level `/v1/memory*` context-query/body route family, daemon-store `memoryProjectionForContext`/`memoryStatus`/`validateMemory` helpers, and SDK global `getMemoryStatus()`/`validateMemory()` clients, with conformance guarding that those bridges cannot return. Slice 1318 makes the mounted thread-memory surface instance-owned by daemon startup, so memory projection runners resolve only the constructor-mounted `contextPolicyCore` and no longer read `store?.contextPolicyCore ?? contextPolicyCore` or `store.contextPolicyCore`. command-protocol deletion guards keep the retired memory projection command operation absent. | Direct Rust memory mutation/admission storage APIs, wallet/policy authority, cTEE private-memory custody, receipt/state-root binding, and stable IDE memory APIs remain non-terminal. |
| Runtime MCP registry/control and serve positive API | MCP registry/control, validation, manager status/catalog/catalog-summary, live-result replay, tool search/fetch, and MCP serve planning/result projection now call typed Rust daemon-core APIs through `RuntimeContextPolicyCore`: `daemonCoreMcpApi.planMcpControlAgentStateUpdate`, `executeRuntimeMcpLiveBackend`, replay/validation/catalog methods, `planRuntimeMcpServeToolCall`, and `projectRuntimeMcpServeToolResult`. Thread-scoped MCP controls send canonical request/server/tool/transport fields plus `agent_id` and runtime `state_dir`, but no caller `seq`; Rust replays admitted `agents/*.json` for agent truth and admitted `events/*.jsonl` for MCP-control event sequence, enforces wallet authority refs, cTEE custody refs, transport containment refs, Agentgres receipt/live-result commits, and live-result replay before `writeAgent()`. Slice 1270 retires the remaining top-level and legacy MCP route/client family: public `/v1/mcp*` handlers, legacy `/api/v1/mcp*` model-mount handlers, SDK global MCP catalog/control clients, CLI live MCP aliases, and status/search/fetch/serve projection strings for top-level MCP routes are gone; clients must use `/v1/threads/{thread_id}/mcp*` protocol APIs. MCP live invoke/discovery exits now materialize deterministic `ioi.runtime.mcp-live-result-payload.v1` protocol payloads/hashes in Rust, bind the hash through the receipt/control/result/replay record, embed `ioi.runtime.mcp-backend-execution.v1` contracts bound to `ioi_drivers::mcp::McpManager`/`McpTransport` with `rust_driver_contract_bound` status, omit retired JS/backend/command/binary-bridge/compatibility fallback proof fields, and JS rejects retired pending transport, missing driver-backend contracts, or stale fallback-proof fields before public truth can return. The live-result commit path now runs `executeRuntimeMcpLiveBackend()` before receipt/result state commits, requires `runtime_mcp_live_backend_rust_driver_executed` evidence and `rust_driver_executed` observation details, binds the actual Rust driver-result hash into the public result payload, recomputes the result payload hash, updates the Rust-returned control and receipt hash bindings, and commits only that Rust-returned receipt/result pair before replay and agent projection, so the planner direct result can no longer be terminal live-result truth. `RuntimeAgentService::execute_runtime_mcp_live_backend()` now backs that API with real Rust `McpManager` process I/O for `tools/call` and admitted live `tools/list`, records actual driver-result hashes without fallback-proof compatibility fields, and conformance guards that `McpTransport` retains the spawned stdio child process instead of dropping a `kill_on_drop` child immediately after pipe extraction. MCP catalog live-discovery now returns Rust materialized declared-row snapshots with `rust_mcp_live_discovery_materialized` and `runtime_mcp_live_discovery_rust_materialized` evidence, while the retired deferred marker stays false instead of being accepted as live-discovery success truth. MCP serve `tools/call` now requires Rust wallet authority grant/receipt refs, cTEE custody refs, and transport containment refs before planning or coding-tool invocation; Rust binds those refs into the StepModule invocation request and the `runtime.mcp_serve` `ioi.runtime.mcp-live-result.v1` record with protocol payload, receipt refs, StepModuleRouter/Rust coding-tool ownership, and no retired JS/command/binary-bridge/compatibility fallback proof fields; JS must reject those retired fields if they reappear, commit that record with `commitRuntimeMcpLiveResultState()`, and return only the result replayed by `projectMcpLiveResultReplay()`. MCP serve planning, result projection, and replay now resolve only through the single positive `contextPolicyCore` mount injected into `createRuntimeMcpServeSurface()`; the surface and focused tests no longer read `store.contextPolicyCore` or the `store.contextPolicyCore ?? contextPolicyCore` fallback. The public daemon now serves only the advertised `/v1/threads/{thread_id}/mcp/serve` protocol route for MCP serve JSON-RPC; top-level `/v1/mcp/serve` and the SDK global `serveMcpRpc()` client are retired, SDK `threadMcpServeRpc()` sends stable `ioi.runtime.mcp-serve-client.v1` protocol bodies carrying wallet authority refs, cTEE custody refs, and containment refs instead of query-string admission, and IDE React Flow MCP serve nodes plus the Rust CLI TUI `/mcp serve` command are canonical daemon protocol clients with body-carried admission refs and no endpoint override. MCP catalog projection, search, fetch, and serve no longer resolve direct JS agent truth, mutate registries, append runtime events, execute JS MCP transports, synthesize served tool-call ids/idempotency/workflow fields, shape result text/event refs, return planner direct result truth, return uncommitted MCP serve projections, read a store-mounted MCP serve planner, or preserve false-valued fallback-proof compatibility fields. The old MCP command-response wrappers, bridge request structs, command-protocol operations, dispatch arms, generic `daemonCoreApi.mcp` compatibility mount, MCP serve response wrappers, `RuntimeMcpServeCommandError`, command-source markers, JS command `operation`/`backend` envelopes, and store-core MCP serve fallback are retired; Rust command-protocol deletion guards keep retired MCP control/catalog/serve operations absent. | Broader non-MCP SDK route-family protocol coverage over Rust replay records remains non-terminal. |
| Model-mount MCP workflow typed API | `planModelMountMcpWorkflow` is a typed `daemonCoreModelMountApi` method backed by Rust `RuntimeKernel::plan_model_mount_mcp_workflow`; model-mount MCP import, ephemeral registration, tool invocation, and workflow-node execution now require Rust-authored workflow/control records plus Rust Agentgres model_mount record-state commit before returning route truth, without a generic command envelope. Rust rejects the old `plan_model_mount_mcp_workflow` command operation and the bridge request/response wrapper is deleted. Tool invocation and workflow-node execution external exits additionally require Rust-enforced `wallet.network.mcp_external_exit` grant refs, authority receipt refs, cTEE custody refs, and transport containment refs, bind them into the authority hash and committed control details, and fail closed without those refs before JS can preserve no-authority/no-custody/no-containment compatibility. MCP server, tool invocation, and workflow-node contracts no longer carry false-valued JS proof or fallback proof fields: Rust returns admitted execution/StepModule dispatch contracts with content receipt refs and materialized result payloads, omits the retired JS/command/binary/compatibility fallback proof fields entirely, and the JS model-mount core rejects stale `rust_required` or fallback-proof MCP workflow responses. Those execution ops now also return Rust-authored `ioi.model_mount.mcp_workflow_receipt.v1` receipts with workflow/authority hashes, custody/containment refs, Agentgres operation refs, state-root binding, StepModuleRouter result binding, materialized protocol result payloads, and `result_payload_hash` binding; JS requires `persistRustAuthoredReceiptWithCommit()` and store guards reject direct JS MCP execution receipt appends without Rust content receipt evidence, Agentgres/state-root fields, and the Rust materialized result binding. `mcp_servers` is a Rust model_mount read-projection kind over runtime `state_dir`, so server list readback replays admitted `mcp-servers` records and filters out legacy or JS-authored MCP truth. | Live external MCP transport backend invocation/discovery and broader runtime containment sandboxing remain non-terminal, along with stable IDE/CLI/SDK protocol APIs; admitted execution-contract result payload materialization and receipt/state-root binding are Rust-owned. |
| Public memory mutation/control positive API | `plan_runtime_memory_control` is reached through typed `daemonCoreThreadMemoryApi.planRuntimeMemoryControl` for public memory write, edit, delete, and policy mutation; public thread/agent memory controls send runtime `state_dir`, Rust replays admitted `memory-records/*.json` for edit/delete current-record truth and `memory-policies/*.json` for policy current truth, rejects JS-supplied `current_record`/`current_policy` transport, commits Rust-authored record/policy payloads through Rust Agentgres memory-state admission, refreshes the temporary memory projection cache, and then returns Rust-projected route output. Direct JS `AgentMemoryStore` `remember`/`updateRecord`/`deleteRecord`/`setPolicy` writers remain retired, missing Rust planning fails closed, direct memory mutation helper remains fail-closed before JS event/state mutation, and Rust rejects the old memory control command operation. Run-memory command resolution now also requires the mounted thread-memory surface, reads policy/path/record truth through Rust public memory projection, and sends chat/API remember, edit, delete, enable, and disable commands through Rust memory-control planning plus Agentgres memory-state commit; missing Rust projection/control fails closed before JS memory cache reads. The daemon-store memory pass-through wrappers are deleted, so migrated memory hot paths can no longer re-enter through store-level remember, list, policy, path, edit/delete, status/validation, or direct memory control-event compatibility handles. Slice 1318 also removes the memory control/event runners' `store?.contextPolicyCore ?? contextPolicyCore` fallback and the agent status/delete helpers' `store.contextPolicyCore ?? null` defaults. | JS still coordinates temporary commit/projection cache refresh; wallet/policy authority, cTEE private-memory custody, durable replay/projection storage, and stable SDK/IDE memory APIs remain non-terminal. |
| Runtime memory status/validation control-event positive API | `recordThreadMemoryStatus()`, `validateThreadMemory()`, and `appendThreadMemoryControlEvent()` now call Rust daemon-core `plan_runtime_memory_control` through typed `daemonCoreThreadMemoryApi.planRuntimeMemoryControl` for event-only `memory.status` and `memory.validate` planning, receive Rust-authored runtime-event payloads, and admit only those events through Rust runtime-event Agentgres admission. The old JS status/validation/direct event authorship facade no longer appends JS events, mutates agents, calls `writeAgent`, synthesizes duplicate receipt/policy evidence when Rust planning is missing, or uses the retired memory command operation. | JS still supplies temporary current memory/projection/request facts and coordinates admission/commit clients; direct memory mutation helper remains fail-closed, and wallet/policy authority, cTEE private-memory custody, durable replay/projection storage, and stable SDK/IDE memory APIs remain non-terminal. |
| Runtime workflow-edit control positive API | `plan_runtime_workflow_edit_control` is a Rust daemon-core operation for public workflow-edit proposal and apply event planning; `proposeWorkflowEdit()` and `applyWorkflowEditProposal()` now require Rust-authored `workflow.edit_proposed`/`workflow.edit.apply` runtime events and admit only those events through Rust runtime-event Agentgres admission. The old JS proposal/apply facade no longer authors event envelopes, persists approval requests, builds proposal/apply truth envelopes, writes workflow JSON, or replays legacy `workflow.edit_applied` as accepted truth when Rust planning is missing. The workflow-edit surface no longer accepts or falls back to a `workflowEditRunner` constructor alias; focused tests mount fake Rust planners only through the constructor-mounted `contextPolicyCore`, and conformance guards the retired alias plus store-core fallback absent. | JS still supplies temporary request/workflow facts and coordinates admission transport; wallet approval authority, workflow mutation custody, durable workflow-edit projection/replay, ArtifactRef/PayloadRef binding where needed, and stable SDK/IDE workflow APIs remain non-terminal. |
| Runtime diagnostics repair decision control positive API | `plan_runtime_diagnostics_repair_control` is a Rust daemon-core operation for diagnostics repair decision execution and direct decision-executed event planning; `executeDiagnosticsRepairDecision()` and `appendDiagnosticsRepairDecisionExecutedEvent()` now require the single daemon-injected `contextPolicyCore`, require Rust-authored diagnostics repair runtime events, and admit only those events through Rust runtime-event Agentgres admission. The old JS decision-execution/event facade no longer resolves accepted repair truth, appends JS runtime events, mutates run state, routes through `diagnosticsRepairControlRunner`, reads `store.contextPolicyCore ?? null`, or persists repair execution truth when Rust planning is missing. | JS still supplies temporary request/gate/snapshot facts and coordinates typed core/admission transport; wallet-governed repair policy authority, durable diagnostics repair projection/replay, receipt/state-root binding, and stable SDK/IDE diagnostics APIs remain non-terminal. |
| Runtime diagnostics repair decision projection positive API | `project_runtime_diagnostics_repair_projection` is a Rust daemon-core operation for diagnostics repair decision resolution; `resolveDiagnosticsRepairDecision()` now sends runtime `state_dir` instead of JS decision candidates, requires the single daemon-injected `contextPolicyCore`, Rust replays admitted Agentgres runtime events for diagnostics repair decisions, validates projection kind/thread/decision/gate identity before returning accepted repair truth, and rejects retired `projection`/`decision`/`decisions`/`repair_decisions` candidate transport. The old fail-closed JS projection-retired facade marker and `diagnosticsRepairProjectionRunner` wrapper are removed instead of preserved as compatibility scaffolding. | JS still coordinates typed API follow-up and supplies the runtime state-dir handle; wallet-governed repair policy authority, broader diagnostics repair policy/replay, receipt/state-root binding, and stable SDK/IDE diagnostics APIs remain non-terminal. |
| Runtime diagnostics operator override state-update/direct-event positive API | Diagnostics operator override execution now requires typed `daemonCoreRuntimeControlApi.planDiagnosticsOperatorOverrideStateUpdate` through the single daemon-injected `contextPolicyCore` to derive operator-override approval state from canonical operator request, decision, and repair-policy context, reject retired JS approval verdict and authority transport, require wallet.network grant plus authority receipt refs for approval-required overrides, bind a Rust-authored diagnostics operator-override authority hash into the operator-control run projection, and then commit only that Rust-planned run through the Rust Agentgres run-state commit path without generic command transport; Rust rejects the old diagnostics operator override command operation and the old Rust command-response wrapper/request/source-marker cluster is deleted. Direct operator-override event append still requires Rust `plan_runtime_diagnostics_repair_control` to author the `diagnostics.operator_override.event` runtime event, apply the same wallet.network authority gate, bind authority evidence into the payload, and admit only that Rust-authored event through Rust runtime-event Agentgres admission. The old JS operator-override facade no longer resolves accepted repair truth, derives approval verdicts or authority, appends operator override events, mutates run maps, routes through `diagnosticsOperatorOverrideStateUpdateRunner`, reads `store.contextPolicyCore ?? null`, or persists run state when Rust planning, wallet authority, or run-state admission is missing. | JS still supplies temporary canonical run/event/decision/authority-ref facts and coordinates commit/admission transport; durable diagnostics repair projection/replay and stable SDK/IDE diagnostics APIs remain non-terminal. |
| Runtime diagnostics repair retry run/event/result positive API | Diagnostics repair retry-turn creation now requires Rust `plan_runtime_diagnostics_repair_retry_run` through the single daemon-injected `contextPolicyCore` before lookup and run creation, so Rust authors the retry run-create request plus retry-event request seed before JS executes the direct Rust-backed run-create lifecycle API with that same core and admits the Rust-authored `diagnostics.repair_retry.created` event through Rust runtime-event Agentgres admission. The final retry result envelope now requires Rust `project_runtime_diagnostics_repair_retry_result` through typed `daemonCoreRuntimeProjectionApi.projectRuntimeDiagnosticsRepairRetryResult` before JS agent lookup/run creation can proceed, and the daemon validates the complete Rust-projected result envelope after event admission, rejects partial or mismatched projections, and no longer fills missing retry result fields locally. Direct retry-event append still requires Rust diagnostics repair retry-event planning and Rust runtime-event admission. The old JS retry facade no longer authors retry prompts/options/diagnostics feedback, calls `store.createRun()` or `agentRunLifecycleSurface`, appends JS retry events, mutates run maps, projects retry/operator result envelopes, routes through diagnostics repair retry/projection runner wrappers, or persists retry truth when Rust planning/projection is missing. `diagnosticsRepairRetryResultFromEvent`, `diagnosticsOperatorOverrideResultFromEvent`, `diagnosticsRepairApplyApprovalKey`, and `diagnosticsRepairExecutionStatus` are deleted, and Rust rejects the retry-result command operation. | JS still supplies temporary agent, request, gate, and snapshot facts and coordinates typed core/admission transport; wallet-governed repair policy authority, durable diagnostics repair projection/replay, and stable SDK/IDE diagnostics APIs remain non-terminal. |
| Public conversation-artifact read/control positive API | `project_runtime_conversation_artifact_projection` and `plan_runtime_conversation_artifact_control` are Rust daemon-core operations; public and thread-scoped conversation-artifact list/get/revision routes require the Rust projector, send runtime `state_dir` instead of artifact candidates, and replay admitted `artifacts/*.json` records in Rust before returning route truth. Create/action/export/promote routes also send runtime `state_dir`; Rust replays existing artifact truth from admitted Agentgres artifact records and rejects retired JS `artifact`/`artifacts` candidate transport before planning. The mounted surface sends only canonical request fields, rejects mismatched Rust plans/projections, commits only the Rust-authored artifact, and never calls direct JS `ConversationArtifactStore` create/action/export/promote writers. | JS coordinates typed runner/commit transport only; durable ArtifactRef/PayloadRef admission, richer Agentgres-backed mutation replay/projection storage, wallet/cTEE authority where needed, and stable SDK/IDE artifact APIs remain non-terminal. |
| Public subagent read projection positive API | `project_runtime_subagent_projection` is a Rust daemon-core operation; public subagent list/get/result routes require the Rust projector through the mounted subagent surface, send runtime `state_dir` instead of subagent/run candidates, replay admitted `subagents/*.json` and `runs/*.json` records in Rust before returning route truth, and reject projection-kind mismatches plus retired JS projection candidate transport. The public read fail-closed JS facade, mounted JS candidate collector, and `subagentProjectionRunner` wrapper are removed; the surface dereferences only the daemon-injected `contextPolicyCore`, while missing Rust projection or missing `state_dir` still fails before JS `subagents`/`runs` map readback can author public truth. | JS coordinates typed projection transport only; direct Rust subagent control admission/storage/replay APIs, StepModuleRouter delegation authority, wallet authority for delegation/cancellation, receipt/state-root binding, and stable SDK/IDE subagent APIs remain non-terminal. |
| Public subagent spawn control positive API | `plan_runtime_subagent_control` now owns `subagent.spawn`; public spawn preflights the Rust subagent control/state planner, composes Rust-owned child-agent creation and Rust-owned child run creation through direct lifecycle APIs, admits the Rust-authored `subagent.spawned` runtime event, requires Rust subagent record state-update planning, and persists only the Rust-planned subagent projection through Agentgres-backed `writeSubagent`. The old spawn fail-closed JS facade is removed without restoring `store.createAgent()`, `store.createRun()`, mounted `agentRunLifecycleSurface`, direct subagent-map mutation, or JS-authored spawn event truth. | JS still supplies temporary parent/thread/run candidate facts and coordinates migration transports; direct Rust subagent delegation/execution admission, StepModuleRouter execution, wallet authority, durable replay/projection storage, receipt/state-root binding, and stable SDK/IDE subagent APIs remain non-terminal. |
| Public subagent wait control positive API | `plan_runtime_subagent_control` is a Rust daemon-core operation for `subagent.wait`; public wait control now requires the Rust control planner for the lifecycle event, Rust runtime-event Agentgres admission for that event, typed `daemonCoreThreadLifecycleApi.planSubagentRecordStateUpdate` record planning, and Agentgres-backed `writeSubagent` persistence before returning the Rust-authored result/projection. The old wait fail-closed JS facade is removed, direct control-event append now enters the Rust-planned event admission path instead of JS event authorship, and Rust rejects the old subagent-record state-update command operation. | JS still supplies temporary current subagent/run facts and coordinates migration transports; direct Rust subagent delegation/execution admission, StepModuleRouter execution, wallet authority, durable replay/projection storage, receipt/state-root binding, and stable SDK/IDE subagent APIs remain non-terminal. |
| Public subagent input/resume control positive API | `plan_runtime_subagent_control` now owns `subagent.input` and `subagent.resume` control-event planning; public input and resume controls require the Rust read projection, Rust-owned child-agent run creation through the direct lifecycle run-create API, Rust-authored runtime-event admission, Rust subagent record state-update planning, and Agentgres-backed `writeSubagent` persistence. The old input/resume fail-closed JS facades are removed without restoring `store.createRun()`, mounted `agentRunLifecycleSurface`, or direct subagent-map mutation. | JS still supplies temporary current subagent/run facts and coordinates migration transports; direct Rust StepModuleRouter delegation/execution, wallet authority, durable replay/projection storage, and stable SDK/IDE subagent APIs remain non-terminal. |
| Public subagent direct assign/cancel control positive API | `plan_runtime_subagent_control` now also owns `subagent.assign` and `subagent.cancel` control-event planning; public assignment and cancellation controls require Rust-authored runtime-event admission, Rust subagent record state-update planning, and Agentgres-backed `writeSubagent` persistence. Public cancellation additionally composes with the Rust run-cancel state-update path before the subagent cancellation projection is committed. The old assign/cancel fail-closed JS facades are removed without restoring JS `store.cancelRun()` or direct subagent-map mutation. | JS still supplies temporary current subagent/run facts and coordinates migration transports; direct Rust StepModuleRouter delegation/execution, wallet authority, durable replay/projection storage, and stable SDK/IDE subagent APIs remain non-terminal. |
| Public subagent cancellation propagation positive API | `plan_runtime_subagent_control` now owns `subagent.cancel.propagate`; public parent cancellation propagation requires Rust subagent read projection for candidate selection, Rust propagated-cancel control planning, Rust run-cancel state planning, Rust runtime-event admission, Rust subagent record state-update planning, and Agentgres-backed `writeSubagent` persistence before returning the propagated/noop envelope. The old propagation fail-closed JS facade is removed without restoring JS `store.cancelRun()`, direct subagent-map mutation, or JS-authored propagation event truth. | JS still supplies temporary current subagent/run facts and coordinates migration transports; direct Rust StepModuleRouter delegation/execution, wallet authority, durable replay/projection storage, and stable SDK/IDE subagent APIs remain non-terminal. |
| Runtime subagent direct control-event positive API | `appendThreadSubagentControlEvent` now calls Rust daemon-core `plan_runtime_subagent_control` and admits only the Rust-authored event through Rust runtime-event Agentgres admission. The old direct append fail-closed JS facade no longer authors event payloads, synthesizes receipts/policy refs, mutates subagent records, falls back to JS event append when Rust planning is missing, or routes through the retired `subagentControlRunner` wrapper; subagent control planning resolves only through the positive `contextPolicyCore` mount. | JS still supplies the temporary current subagent/control request facts and coordinates positive daemon-core/admission transport; direct Rust subagent admission/storage/replay APIs, StepModuleRouter delegation/execution authority, wallet authority, durable replay/projection storage, receipt/state-root binding, and stable SDK/IDE subagent APIs remain non-terminal. |
| JS facade and alias retirement | Daemon-store pass-through wrappers, many compatibility aliases, local event writers, fixture selectors, provider JS drivers, and stale readback helpers have been deleted or fail closed. | Remaining `rust_core_required` surfaces should be collapsed into macro positive Rust APIs instead of producing more helper-level retirements. |

## Remaining Terminal Blockers

- Rust daemon core does not yet own every hot-path positive API. Many JS surfaces
  fail closed correctly, but fail-closed is not terminal ownership.
- StepModuleRouter dispatch, workload invocation construction, Agentgres
  admission, receipt/state-root binding, projection, and replay are not yet one
  Rust-owned execution spine for every route family.
- Approval-required coding-tool execution has Rust satisfaction, block planning,
  result-event admission, public approval request/decision/revoke control with
  `state_dir` replayed target truth, public approval queue/read projection,
  public request-authority issuance, Rust approval-lease authority, public
  decision/revoke wallet authority binding, budget-block planning/admission,
  and coding-tool satisfaction projection, but wallet.network grant issuance
  semantics and approval authority projection/replay
  still need direct Rust authority/admission ownership beyond thin JS protocol-client
  scaffolding.
- Coding-tool invocation now requires Rust daemon-core result envelope/context
  planning before workload dispatch and result-event admission. Patch snapshot
  capture, public workspace snapshot/restore APIs, public restore
  artifact/event admission, workload observation field ownership, post-edit
  diagnostics feedback planning, artifact draft/read projection, diagnostics
  repair decision projection, and diagnostics repair policy projection are
  Rust-owned typed APIs without command transport, but durable diagnostics
  replay/storage and stable protocol APIs still need one Rust-owned execution spine.
- Model_mount provider and instance lifecycle still need direct Rust/Agentgres
  projection/replay APIs beyond the migrated local health/start/stop, public
  load/unload, instance-maintenance planning/commit, provider-inventory
  record/replay, provider-inventory topology/catalog materialization edges, and
  positive runtime-engine preference/profile control/projection records, public
  backend-lifecycle control records, public catalog/download/storage-control
  records, storage/download read projection over admitted records,
  runtime-engine read projection over admitted controls, and catalog-provider
  OAuth read projection over admitted controls, plus receipt-backed
  projection/authority/health/survey/snapshot envelopes over `receipts/*.json`
  replay; backend/server projection, conversation state, hosted/provider
  endpoint discovery/materialization, live external hosted API execution, richer hosted catalog materialization, deeper wallet/cTEE route
  authority, and direct protocol/API surfaces still
  need direct Rust daemon-core ownership beyond migration transport.
- Public agent create/thread create/run create/task create/run cancel/task-job cancel/archive/unarchive/resume/close/reload/delete and thread
  mode/model/thinking runtime controls are now Rust-planned and
  Agentgres-backed at the public surface, while generic runtime thread-event
  append plus synthetic thread-start/run-event projection now use Rust
  Agentgres admission/projection spines. Public stream/turn/run replay readback
  now uses Rust `state_dir` replay over Agentgres-admitted event records with
  caller-supplied replay event transport retired, and public thread/turn
  projection records are Rust-authored over those replay events.
  Public lifecycle read projection now uses Rust `state_dir` replay over
  admitted agent/run/event records with caller-supplied lifecycle candidate
  transport retired. Diagnostics-blocked turn creation now enters Rust-planned
  run creation with Rust turn projection instead of the retired admission-required
  refusal path. Public lifecycle create/thread/run/status/delete routes,
  non-runtime resume/turn creation, diagnostics retry-run creation, and
  subagent spawn/input/resume no longer pass through mounted
  `agentRunLifecycleSurface`; they call direct Rust-backed lifecycle APIs with
  explicit planner/build/projection dependencies. Runtime-service resume and
  turn submission also call the direct Rust lifecycle adapter and commit only
  Rust-planned Agentgres state before returning projection truth.
  Deletion/cancellation replay/projection, direct runtime-control event
  materialization, deeper wallet/cTEE workspace-trust authority enforcement,
  durable task/job replay/projection storage,
  durable managed-session storage/replay/projection, wallet/cTEE
  managed-session authority, durable workspace-change storage/replay/projection,
  wallet/workspace rollback authority, durable thread-fork storage/replay/projection,
  wallet/cTEE fork authority, and broader run lifecycle still need Rust
  daemon-core ownership.
  Subagent spawn/wait/input/resume/assign/cancel and cancellation propagation controls now have Rust control-event planning,
  Rust runtime-event admission, Rust subagent state-update planning, and
  Agentgres-backed persistence; spawn also uses the Rust agent-create and run-create
  state-update paths, input/resume also use the Rust run-create
  state-update path, and cancellation/propagation also use the Rust run-cancel
  state-update path, and direct subagent control-event append now uses Rust
  control-event planning plus Rust runtime-event admission. Public memory
  write/edit/delete/policy controls now use Rust memory-control planning and
  Rust Agentgres memory-state commits, run-memory chat/API memory commands now
  use the same Rust projection/control spine, daemon-store memory pass-through
  wrappers are deleted, and memory status/validation/direct
  control-event append now uses Rust memory-control event planning plus Rust
  runtime-event admission; those memory projection/control calls now go through
  typed `daemonCoreThreadMemoryApi`. The top-level `/v1/memory*` context routes,
  daemon-store memory context helpers, and SDK global memory status/validation
  clients are retired, so memory read/status/validation clients must enter
  through explicit thread/agent protocol routes. Rust rejects the retired
  `project_runtime_memory_projection`, `plan_runtime_memory_control`,
  `plan_memory_manager_status_projection`,
  `plan_memory_manager_validation_projection`, and
  `plan_thread_memory_agent_state_update` command operations. Workflow-edit
  proposal/apply now uses Rust
  workflow-edit event planning plus Rust runtime-event admission, and
  diagnostics repair decision execution/direct decision-event append now use Rust
  diagnostics repair event planning plus Rust runtime-event admission,
  diagnostics repair policy projection and decision resolution now use Rust
  diagnostics repair projection APIs, diagnostics operator override execution now
  uses Rust state-update planning plus Rust run-state
  admission, direct operator-override event append now uses Rust diagnostics
  repair event planning/admission, and diagnostics repair retry-turn
  creation/direct retry-event append now compose Rust run-create and diagnostics
  repair event planning/admission plus Rust retry-result projection with
  missing or partial projection failure and the old JS result helpers deleted,
  while direct memory mutation, diagnostics
  durable diagnostics repair storage/replay, actual external MCP transport
  execution/backend discovery, MCP runtime containment sandboxing,
  and deeper workflow mutation custody still include or need fail-closed
  Rust-owned boundaries beyond the newly Rust-enforced wallet grant,
  authority-receipt, cTEE custody, and transport-containment ref gate for MCP
  external-exit planning.
- cTEE private workspace, worker/service package, L1 settlement,
  governed-improvement, and external capability authority now use mounted core
  APIs instead of JS runners. L1 settlement state-root truth is Rust-derived and
  retired from daemon/SDK/IDE/CLI request clients, but these families still need
  stable direct Rust daemon-core protocol/API bindings beyond the temporary
  command-envelope request builders.
- Remaining IDE/CLI/SDK surfaces still need stable protocol APIs over Rust
  projection and replay records instead of depending on route-family JS adapter
  behavior.
- The temporary command transport must not become the terminal substrate.

## Do Not Recreate

The following paths are retired and must not be restored as compatibility
scaffolding:

- shared JS command-spawn helper:
  `packages/runtime-daemon/src/runtime-daemon-core-command-runner.mjs`
- StepModule command wrapper:
  `packages/runtime-daemon/src/step-module-command-runner.mjs`
- JS coding-tool approval satisfaction gate:
  `codingToolApprovalSatisfaction()`
- JS coding-tool approval satisfaction projection/matcher:
  `codingToolApprovalSatisfactionProjection()`,
  `approvalProjectionContextForThread()`,
  `codingToolApprovalManifestsMatch()`
- JS approval request readback facade:
  `latestApprovalRequestEvent()`
- JS approval block facade:
  `blockCodingToolForApproval()`
- command/env fallback selectors for migrated hot paths:
  `IOI_RUNTIME_DAEMON_CORE_COMMAND`, `IOI_RUNTIME_DAEMON_CORE_COMMAND_ARGS`,
  per-runner `*_COMMAND` envs, constructor `command`, and constructor `args`
- root daemon-core compatibility mount for migrated authority/governed-admission
  hot paths:
  constructor `daemonCoreApi` and nested `daemonCoreApi.*` fallbacks in the
  external capability authority, cTEE, worker/service package, L1 settlement, and
  governed-improvement cores
- root daemon-core compatibility mount for migrated approval, Agentgres,
  workspace restore, and context-policy hot paths:
  constructor `daemonCoreApi` and nested `daemonCoreApi.*` fallbacks in
  coding-tool approval, approval state, runtime Agentgres admission, workspace
  restore/snapshot, and runtime context-policy cores
- runtime auxiliary compositor store-core fallback for migrated managed-session,
  workspace-change, and thread-fork helpers:
  `deps.contextPolicyCore ?? store.contextPolicyCore`,
  `store.contextPolicyCore`, and `store?.contextPolicyCore`
- runtime context-policy surface store-core fallback:
  `store?.contextPolicyCore ?? contextPolicyCore`,
  `store.contextPolicyCore`, and `store?.contextPolicyCore`
- runtime workflow-edit surface store-core fallback:
  `store?.contextPolicyCore ?? null`,
  `store.contextPolicyCore`, and `store?.contextPolicyCore`
- thread-memory surface and lifecycle helper store-core fallbacks:
  `store?.contextPolicyCore ?? contextPolicyCore`,
  `store.contextPolicyCore`,
  `statusStateUpdateRunner = store.contextPolicyCore ?? null`, and
  `deleteStateUpdateRunner = store.contextPolicyCore ?? null`
- run-cancel state-core fallback:
  `state.contextPolicyCore` and `state?.contextPolicyCore`
- coding-tool result/artifact and diagnostics-repair command operations:
  `plan_coding_tool_result_envelope`,
  `plan_runtime_coding_tool_artifact_drafts`,
  `project_runtime_coding_tool_artifact_read`,
  `plan_post_edit_diagnostics_feedback`,
  `plan_runtime_diagnostics_repair_control`,
  `plan_runtime_diagnostics_repair_retry_run`,
  `project_runtime_diagnostics_repair_retry_result`,
  `project_runtime_diagnostics_repair_projection`, and
  `project_runtime_diagnostics_repair_policy`
- JS diagnostics repair public-result helpers:
  `diagnosticsRepairRetryResultFromEvent`,
  `diagnosticsOperatorOverrideResultFromEvent`,
  `diagnosticsRepairApplyApprovalKey`,
  `diagnosticsRepairExecutionStatus`
- diagnostics repair surface runner wrappers and fallback:
  `diagnosticsRepairControlRunner`,
  `diagnosticsRepairRetryRunRunner`,
  `diagnosticsRepairRetryResultProjectionRunner`,
  `diagnosticsRepairProjectionRunner`,
  `diagnosticsOperatorOverrideStateUpdateRunner`,
  `store.contextPolicyCore ?? null`
- mounted agent/run lifecycle facade:
  `agentRunLifecycleSurface`,
  `createRuntimeAgentRunLifecycleSurface`
- runtime agent/run lifecycle helper runner fallbacks:
  `agentCreateStateUpdateRunner`,
  `threadCreateStateUpdateRunner`,
  `runCreateStateUpdateRunner`,
  `runtimeBridgeThreadStartStateUpdateRunner`,
  `runtimeBridgeTurnRunStateUpdateRunner`,
  `runtimeBridgeThreadControlStateUpdateRunner`,
  `store.contextPolicyCore ?? null`
- conversation-artifact surface runner wrappers and fallback:
  `conversationArtifactControlRunner`,
  `conversationArtifactProjectionRunner`,
  `store.contextPolicyCore ?? contextPolicyCore`
- runtime MCP serve store-core fallback:
  `store.contextPolicyCore`,
  `store.contextPolicyCore ?? contextPolicyCore`
- coding-tool artifact surface store-core fallback:
  `store.contextPolicyCore`,
  `store.contextPolicyCore ?? contextPolicyCore`
- coding-tool budget recovery surface store-core fallback:
  `store.contextPolicyCore`,
  `store.contextPolicyCore ?? null`
- runtime task/job surface store-core fallback:
  `store.contextPolicyCore`,
  `store.contextPolicyCore ?? null`
- bridge-local command identity/dispatch wrappers deleted from
  `crates/node/src/bin/ioi_step_module_bridge`
- retired `ioi-step-module-bridge` binary and empty root tombstone:
  `crates/node/src/bin/ioi-step-module-bridge.rs`,
  `crates/node/src/bin/ioi_step_module_bridge/mod.rs`
- daemon-wide generic invoker pass-through:
  `daemonCoreInvoker: options.daemonCoreInvoker`,
  `this.daemonCoreInvoker = options.daemonCoreInvoker`,
  `daemonCoreInvoker: deps.daemonCoreInvoker`
- daemon-store pass-through wrappers that re-enter mounted surfaces as duplicate
  authority
- standalone JS fixture-policy compatibility wrapper:
  `packages/runtime-daemon/src/model-mounting/fixture-policy.mjs`;
  `fixture-policy.mjs` is absent, and disabled-internal-fixture cleanup remains
  private inside `default-discovery.mjs`
- retired internal fixture enable selector:
  `IOI_ENABLE_INTERNAL_FIXTURE_MODELS` no longer enables fixture seeding; the
  only explicit selector is `IOI_EXPOSE_INTERNAL_FIXTURE_MODELS`
- JS event scans, local file cache reads, fixture selectors, provider-map
  readbacks, or compatibility aliases that can author accepted truth

## Conformance Posture

Current conformance is proof that the current tier surface is internally
consistent, not proof that terminal migration is done.

Required checks after every macro cut:

| Check | Purpose |
| --- | --- |
| `node --check` on touched JS/conformance files | syntax and module integrity |
| focused `node --test` or Rust tests | local behavior and negative guards |
| `npm run hypervisor-conformance:docs` | docs/source-of-truth consistency |
| `npm run hypervisor-conformance:bridge` | bridge/direct-invoker/facade guards |
| relevant tier commands | receipts, cTEE, compositor, negative, or ABI as touched |
| `npm run hypervisor-conformance` | full current-tier proof |
| `git diff --check` | whitespace and patch hygiene |

Terminal acceptance still requires the master guide terminal conditions, not
just a green current-tier suite.

## Matrix Maintenance Rule

Do not append one section per implementation slice. Add or update a row only
when a macro authority boundary changes:

- a positive Rust daemon-core API replaces a fail-closed JS facade;
- a route-family truth path moves into Rust with admission, receipts,
  projection, replay, and conformance;
- a temporary command transport or JS facade is deleted for an entire family;
- a terminal blocker changes shape.

If future work needs detailed evidence, cite tests, conformance check IDs, and
git commits. Do not rebuild a slice-by-slice narrative inside this matrix.
