# Canon-to-Code Delta

Status: canonical implementation index.
Canonical owner: this file for the object-level delta between the foundational bounded-DAS, institutional-learning-boundary, collaborative-pursuit, federated-ontology, and embedded wallet-authority canon and what the code durably implements today, and for the deferred application-UX backlog that replaced the former surface-by-surface implementation queue.
Supersedes: the PR68 operational-depth queue as an implementation SEQUENCE (its audit evidence remains valid and referenced); ad hoc "what do we build next" lists in plans.
Superseded by: none.
Last alignment pass: 2026-07-18.
Doctrine status: canonical
Implementation status: mixed (each row carries its own state; most target objects are not started — that is the point of this file)
Last implementation audit: 2026-07-12

## Why this file exists

The estate reached an audited application plane (the operational-depth atlas,
`apps/hypervisor/application-operational-depth.json`) whose remaining queue
ranked SURFACE work. The canon's next leg is not surface work: it begins with
the constitution/deployment/continuity object plane, then the
collaborative-pursuit and federated-ontology object plane. This file records,
object by object, what the canon requires, what the code holds today, and what
crossing turns the delta into durable truth — so application UX resumes only
when an implemented contract pulls it.

Reading rules:

- **Implementation state** is honest: `not started` means the object's
  durable form does not exist in code — even when an adjacent substrate is
  named as an explicitly labeled *implementation precedent* (a precedent is
  never a partial instance). `partial` is reserved for rows where a real
  slice of the object itself persists. Nothing planned is described as
  shipped.
- **Next authority crossing** is the first daemon/Agentgres/wallet boundary the
  object must cross to exist as governed truth (not a UI milestone).
- **Owning application projection** names the product surface that will RENDER
  the object. Projections never own the truth; the daemon/Agentgres contracts
  in the row do.

## The delta table

| Canonical object | Canonical owner | Durable form (target) | Current code anchor | Implementation state | Next authority crossing | Conformance proof | Owning application projection |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `AuthorityScopeRequestEnvelope`, `WalletAuthorityReview`, `AuthorityReviewReceipt` profile, `AuthFactor`, `GuardianSurface`, `AuthorityGrantEnvelope` v3 request-context successor, wallet account/session/recovery state | [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md), [`wallet-network/doctrine.md`](../components/wallet-network/doctrine.md), [`wallet-network/api-authority-scopes.md`](../components/wallet-network/api-authority-scopes.md), [`wallet-network/product-exchange-risk.md`](../components/wallet-network/product-exchange-risk.md), [`events-receipts-delivery-bundles.md`](../components/daemon-runtime/events-receipts-delivery-bundles.md) | provider-neutral federated/passkey access creates a wallet account and binds an externally owned product session without effect authority. A context-bound path separately commits the immutable request, canonical reviewed representation, single-use approval ceremony, and typed authorization subject (`exact_effect`, `batch_manifest`, or `standing_envelope`); presentation-surface evidence and authenticator evidence remain separate, portable v3 commits the admitted chain, and the daemon compares its actual payload by equality, membership, or standing constraints before invocation. Recovery/device transitions never preserve or widen grants | `packages/wallet-protocol` and the Rust wallet service provide adjacent authority-review, capability-lease, receipt, and immutable portable `AuthorityGrantEnvelope` v1/v2 contracts. The checked `authority-review.schema.json` does not bind the full principal/session/origin/request/review-representation/presentation/ceremony/authorization-subject/result context; no provider-neutral factor/passkey ceremony, GuardianSurface enrollment, wallet account-session inventory, recovery state machine, typed review receipt, or v3 final-effect comparison implements this target | exact managed sign-in-to-effect path not started; v1/v2 portable grant mechanism and effect-fence v1 built but intentionally unchanged | register the request/review/receipt successors and AuthorityGrant v3 schema; generate Rust/TypeScript projections and fixtures; implement WebAuthn begin/finish, factor/guardian enrollment, wallet account/session and recovery/device routes; issue the exact context-bound grant; resolve its authorization subject and compare the daemon-computed effect before the invoker; emit WalletReceipt and Agentgres evidence | reject WebAuthn challenge/type/RP/origin/cross-origin/credential/owner/signature/required-UP-or-UV failures; generic authenticator-as-display claims; request/review-representation/presentation-evidence/ceremony/authorization-subject/grant substitution; caller-asserted factor/guardian participation; predecessor edit-and-approve reuse; principal/session/origin replay; exact-effect mismatch; missing batch membership; standing-envelope constraint excess; recovery-session login/effect use; recovery-based authority preservation; stale revocation; effect invocation without exact v3 grant. The undeniable-product proof cannot pass while only the legacy AuthorityReview shape is validated | embedded ioi.ai/Hypervisor sign-in and approval; Wallet security/recovery cockpit; Governance/Provenance drilldowns; calling product retains its own session lifecycle |
| `GoalRunProfile`, `WorkflowTemplate` | [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md), [`default-harness-profile.md`](../components/daemon-runtime/default-harness-profile.md) | immutable, content-addressed pursuit specification plus a separately owned immutable directed-work graph; the Goal Kernel interprets the former and the Workflow Compositor owns the latter | `goalrun_routes.rs` and `runtime_goal_run_admission.rs` implement a narrow durable GoalRun precursor; existing workflow/template-shaped refs are adjacent precedents, not canonical immutable release/admission objects, and GoalRuns do not yet freeze the full selected profile resolution | `GoalRunProfile`: not started; `WorkflowTemplate`: not started | admit immutable releases, resolve declared component requirements under policy, and freeze profile revision/hash, admitted overrides, transitive component-set hash, and resolution receipt into the GoalRun before pursuit begins | two GoalRuns can reuse one profile without shared live state; profile cannot execute, grant authority, or own campaign/domain state; workflow template cannot trigger/own a run; changed profile content requires a successor revision; packages carry refs/requirements but no live leases, gateways, or run IDs | Studio and Packages for composition/distribution; Automations for declared workflow use; Work for GoalRun projection |
| `WorkLifecycleRecord`, `WorkLifecycleProjection`, `CancellationFanoutPlan`, `WorkLifecycleArchiveSegment`, `WorkLifecycleSnapshot` | [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md), [`doctrine.md`](../components/daemon-runtime/doctrine.md), [`api.md`](../components/daemon-runtime/api.md) | one shared content-committed exact-head/idempotent append mechanism over kind-specific lifecycle tables, typed child refs, rebuildable active projections, cancellation fanout, and archive-root/head-bound replay checkpoints without moving owner-domain state into the mechanism | `crates/services/src/agentic/runtime/kernel/work_lifecycle.rs` implements independent table/reference legality parity, declared kind-specific authority-class checks, monotonic time, canonical child-ref typing, cancellation policy/fanout, replay, and snapshot/archive construction; `crates/node/src/bin/hypervisor_daemon_routes/work_lifecycle.rs` implements collision-resistant immutable local records, projection repair, fork/gap/tamper refusal, crash replay, durable archive/snapshot output, and `GET /v1/hypervisor/work-lifecycle/status`. Grant signatures/scope/expiry/revocation are not independently verified by this shared mechanism | partial mechanism; no owner write route is bound, automatic pruning and archive-only resume are absent, and production Agentgres persistence is not claimed | bind each GoalRun/GoalGroundingLoop/WorkRun/AutomationRun/HarnessInvocation/ContextCell/external-handle owner at its own legal mutation boundary, verify authority grants/revocation at that owner PEP, emit owner receipts/events after durable fact commit, then prove archive-only resume before enabling pruning | focused Rust tests prove exhaustive reference-vs-table legality parity, exact-head replay/conflict, owner-specific declared-authority refusal, cancellation drain/fence/timeout/compensation/reconciliation planning and policy requirements, canonical ref/time checks, append-only child replay, crash-before-projection repair, fork/gap/tamper/collision refusal, and receipt-lineage-preserving snapshot/archive; owner-route, real authority verification, and Agentgres integration remain required | Work, Automations, Sessions, Developer Console, and Provenance project their own objects/status; no standalone lifecycle application |
| `ImprovementGovernanceProfile`, `ImprovementAgenda`, `ImprovementCampaign`, `EvaluationEpoch`, `EvaluationExposureLedger`, `ImprovementOrderCutoffReceipt`, `ImprovementEvidenceClaim` | [`bounded-recursive-improvement.md`](../foundations/bounded-recursive-improvement.md), [`improvement.md`](../components/hypervisor/improvement.md), [`evaluations.md`](../components/hypervisor/evaluations.md), [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md) | immutable owner-scoped governance profile and immutable-by-revision Agenda plus optional durable Campaign aggregate coordinating GoalRuns, immutable candidates, frozen evaluation epochs, append-only exposure, adjacent-order evidence cutoffs, proposal lineage, recovery refs, and qualified immutable claims | governed-improvement proposal admission, GoalRun records, evaluation-suite declarations, Foundry experiment candidates, receipts, and release controls are partial ingredients; no governance-profile binding, Campaign aggregate, frozen epoch/exposure ledger, order cutoff, or claim lifecycle exists | not started as the canonical campaign spine | admit Agenda and Campaign against exact owner/governance, target, and profile roots plus System/constitution roots when System-scoped; reserve finite budgets; freeze one order-0 epoch; admit candidates, exposure, findings, reproduction, cutoff, and selection; emit only a target-owner `UpgradeProposal` | candidate cannot control evaluator, meter, authority, or recovery ancestor; search cannot redefine the selecting epoch; exposure and false-promotion budgets do not reset by branch/order; negative evidence persists; stale bases fail; Campaign cannot activate production; direct one-shot proposal path remains valid | Improvement cockpit; Evaluations for judgment; Foundry for experiments; Governance for admission/promotion; Provenance for lineage/claims; Work for GoalRuns; Systems/Packages contextually |
| `SkillManifest`, `SkillEntry`, `ActiveSkillSetSnapshot` | [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md), [`connector-and-tool-contracts.md`](../components/connectors-tools/contracts.md) | immutable procedure/context/assets definition; immutable successor-versioned owner-scope binding revision to one exact manifest revision/hash with mutable registry lifecycle projection; immutable run/harness-bound resolution snapshot with selected/excluded skills, reasons, set hash, compatible tool refs, leases, and receipt | `crates/types/src/app/agentic/market.rs` uses `SkillManifest` for listing/commercial metadata; `execution.rs` makes `AgentMacro`/`SkillRecord` executable; `ioi_intelligence_routes.rs` stores mutable procedure-body SkillEntries; `skill_hook_registry.rs` discovers filesystem skills; `thread_lifecycle.rs` mixes selected skills and executable hooks in `ActiveSkillHookManifest` | not started as the canonical three-way split; incompatible same-name records and adjacent runtime discovery exist | admit/version the manifest, install/admit an owner-scoped entry, then daemon-resolve and receipt one exact active set per run/harness; executable helpers separately cross `RuntimeToolContract` and authority gates | marketplace listing/offer fields never enter the manifest, while source-rights/license refs remain; skill cannot execute or grant authority; entry cannot substitute a mutable manifest body; snapshot cannot become reusable definition; hooks remain separate; packages contain manifest refs but neither entries nor active snapshots | Packages and Developer Console for version/install state; Studio/Work context for selection and exact run snapshot; Improvement for evidence-backed successor proposals |
| `MCPPrimitiveNormalization`, `HypervisorMCPGatewayRequirement`, `HypervisorMCPGatewayProfile` | [`connector-and-tool-contracts.md`](../components/connectors-tools/contracts.md), [`doctrine.md`](../components/connectors-tools/doctrine.md), [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md), [`core-clients-surfaces.md`](../components/hypervisor/core-clients-surfaces.md) | transport-neutral semantic normalization: admitted tools become `RuntimeToolContract`; resources become leased policy-bound views/artifact or memory projections; prompts are untrusted import inputs; elicitation is typed user input with separate authority approval; Tasks are opaque `HarnessInvocation` handles; Apps are sandboxed extension surfaces. Packages declare immutable gateway requirements; live subject-bound gateway profiles are admitted and revocable below System | governed MCP tool execution exists, but `hypervisor-daemon.rs` still mounts top-level `/v1/mcp`, `/servers`, `/tools`, `/resources`, and `/prompts`; resources/prompts are catalog slices; elicitation/tasks/apps are absent; HTTP and stdio transports pin different protocol/session assumptions; the Rust `RuntimeToolContract` shape also drifts from the documented schema | partial tool path; other primitive mappings not started; route and protocol contracts drifted | admit raw descriptors into native IOI contracts; resolve resource/prompt/elicitation/task/app inputs through their owning admission paths; resolve package requirements to a least-privilege gateway profile at activation/invocation; classify or retire top-level compatibility routes | no MCP primitive can mint IOI identity/authority or become runtime truth; prompt import stays inert until admitted; elicitation cannot approve itself; Task handle is not GoalRun; App is tenant/System-isolated extension state; package has no credentials, concrete gateway, subjects, leases, or secrets; protocol-version and route-surface fixtures stay explicit | Developer Console for servers/contracts/gateways; Packages for immutable requirements; Applications for admitted MCP Apps; Work/Developer Workspace for invocation and leased context projections |
| `HypervisorProductSurfaceTaxonomyV2`, `HypervisorCoreWorkspaceRegistration`, `HypervisorApplicationSurfaceRegistration`, `HypervisorRouteAliasRegistration`, `HypervisorSurfaceReleaseRecord`, `HypervisorSurfaceInstallationBinding`, `HypervisorSystemInterfaceBinding`, `HypervisorSurfaceServingBinding`, `HypervisorProductSurfaceProjection` | [`core-clients-surfaces.md`](../components/hypervisor/core-clients-surfaces.md), [`canonical-enums.md`](../foundations/canonical-enums.md) | normalized core-workspace, owner-application, substrate-application, tool-surface, and extension-application definitions plus typed route aliases and separate release, installation, System-interface, and serving bindings, compiled with independent publisher-origin, creation, distribution, availability, admission, installation, package-disposition, enablement, capability-depth, operational-state, authority, route, and context axes | **partial static shadow compiler:** typed first-party registrations compile the five core workspaces, twelve enduring owner applications, two substrate applications, and one planned conditional `owner_application`; an extension-application target contract is exposed with its origin, creation, and distribution axes separated, while its inventory is explicitly `not_connected`. All thirteen certified runtime surfaces attach independently as twelve owner-bound tools and one Work view, with legacy serving routes marked as compatibility aliases and pixel/capture evidence unable to create peer membership. The product API, Applications estate, launcher, and Home consume this static v2 projection. The native permanent rail remains transitional; generated-app management is still separate; daemon-admitted release/installation/System-interface/serving bindings, dynamic extension inventory, and request-scoped policy filtering are absent | partial | extend the shadow compiler from static first-party registrations to daemon-admitted release/installation/System-interface/serving records; policy-filter by authenticated org/user and typed Project/System/work context; migrate the native rail and canonical routes only as their read models become real | `verify-hypervisor-product-surface-catalog.mjs`; `verify-hypervisor-product-surface-live-smoke.mjs`; parity against safe static inventory; certification-toggle non-membership; exact 13-surface placement; one-definition-to-many-bindings; tenant/System cache-isolation; tool-owner; typed-alias uniqueness/collision/context preservation; no orphan route; unavailable/degraded; generated-candidate-not-launchable tests | Applications plus Home/System/Project/Work contextual launch projections; no new truth owner |
| `HypervisorSystemsProjection` | [`core-clients-surfaces.md`](../components/hypervisor/core-clients-surfaces.md), [`governed-autonomous-systems.md`](../foundations/governed-autonomous-systems.md) | policy-filtered inventory/detail read model for admitted System identity, constitution, genesis, desired/observed topology, work, authority, evidence, improvement, and interfaces | no bounded-System inventory or product workspace; adjacent node/deployment/receipt surfaces are precedents only | not started | bounded-System package/genesis/constitution/deployment/membership contracts and rebuildable read projection | no permanent Systems rail before honest read model; direct non-System Session/Project/Goal remains valid | Systems; contextual Studio, Governance, Operations, Provenance, Improvement, Packages |
| `HypervisorWorkSubjectProjection`, `HypervisorLegacyWorkSubjectAlias` | [`core-clients-surfaces.md`](../components/hypervisor/core-clients-surfaces.md) | policy-filtered typed projection across GoalRuns, OutcomeRooms, AutomationRuns, Sessions, WorkItems, WorkRuns, queues, reviews, incidents, and history plus migration-only legacy Mission aliases with review/evidence/receipts | **partial compatibility projection:** the `/__ioi/missions` route is visibly framed as Work and projects real AutomationRun, GoalRun, schedule, failure, and blocker facts without minting a Mission object; certified Issues is registered as the `Work / Incidents` workspace view. Sessions remains a separate current-code workspace, while OutcomeRooms, queues, reviews, heterogeneous history, request-scoped policy filtering, canonical `/work` routes, and alias/migration receipts are absent | canonical contract not started; noncanonical compatibility UI partial | stop new Mission writes; add typed associations and the remaining Work views; deterministically resolve or review legacy records; emit alias/migration receipts; migrate canonical routes without breaking typed context | private-subject non-disclosure, typed deep links, GoalRun continuity across Session replacement, ambiguous-record refusal, alias context preservation | Work / Goals, Sessions, Rooms, Queues, Reviews, Incidents, History |
| `HypervisorPackagesSurface`, admitted extension-application lifecycle | [`core-clients-surfaces.md`](../components/hypervisor/core-clients-surfaces.md) | mandatory local package admission, installed inventory, releases, dependencies, recall/impact, and generated/installed application binding; Marketplace is an optional discovery/publish/exchange mode | current Marketplace UI/package records and a separate generated-app manager are partial precedents; generated apps are not compiled into one launch catalog | not started | package admission/install/binding/serve/disable/recall/revoke records feed the catalog compiler | package works without commerce; recall shows impact without mutating Systems; one interface package binds independently without data/authority/cache leakage | Packages; Packages / Marketplace; Applications; System / Interfaces |
| `AutonomousSystemManifest`, `AutonomousSystemGenesis` | [`governed-autonomous-systems.md`](../foundations/governed-autonomous-systems.md), [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md) | reusable immutable package release plus one-time binding to a new system identity, constitution, initial profiles, governing authority, and sequence-zero commitment/state/receipt roots | package and manifest precedents exist, but no package-to-genesis object or activation path | not started | daemon proposal -> governing decision/authority -> Agentgres genesis/initialize/activate admissions and receipts | package reuse across two independent system IDs; activation refuses missing/mismatched release, constitution, profile, authority, and origin roots (build step 1) | Studio; Governance; Systems; Provenance |
| `AutonomousSystemConstitution`, `OrderingAdmissionFinalityProfile`, `OracleEvidenceProfile`, `LifecycleContinuityProfile`, `IOINetworkEnrollment` | [`governed-autonomous-systems.md`](../foundations/governed-autonomous-systems.md), [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md) | protected, versioned logical-system contract set with proposal/decision/activation and lifecycle receipts | no public object or route family; existing policy/upgrade/receipt planes are precedents only | not started | distinct constitutional, oracle, lifecycle, and enrollment authority paths admitted through daemon/Agentgres | planned negative tests for ordinary-gate amendment, self-authority widening, stale/contradictory oracle evidence, succession widening, and false enrollment assurance (build steps 1 and 14) | Systems; Governance; Provenance |
| `AutonomousSystemDeploymentProfile`, `AutonomousSystemNodeMembership`, `AutonomousSystemFailoverProfile`, `AutonomousSystemWriterEpochTransition`, `LostSuffixRecord`, embedded `ConsequentialEffectFenceContext` | [`domain-kernels.md`](../foundations/domain-kernels.md), [`governed-autonomous-systems.md`](../foundations/governed-autonomous-systems.md), [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md) | desired topology separated from observed membership/readiness, with declared timing assumptions, durable continuity CAS, immutable per-System writer transition, excluded-suffix record, active resource fences, and daemon-generated exact pre-effect evidence | shared transition/effect validator and durable local active-fence projection are built; startup/load replay immutable chains at commit time, refuse forks/gaps/tamper/orphan projections, repair stale heads atomically, and use full-ref commitment filenames rather than lossy sanitized identity. Trusted startup node identity must equal the active writer; caller-authored contexts are refused. Promoted Agentgres live updates derive ownership from the current durable object and require candidate continuity; immutable legacy backfill may use its legacy owner facts, but any unreadable, malformed, fenced, or substrate-refused legacy record now leaves the promoted engine unavailable rather than silently creating incomplete truth. Connector reads derive System/resource from the durable source and authority/expiry from the durable session before credential/network invocation. Agentgres mux epoch remains storage-only; soak is diagnostic shadow, not a PEP. Active transition grant refs are compared but not independently reverified here. Public membership/failover APIs, automatic promotion, external CAS/witness integration, lost-suffix emission, and estate-wide PEP coverage are absent | partial vertical; no public control plane or promotion route | governed membership and transition proposal, external continuity-CAS proof, catch-up/root and displaced-writer fencing, independently verified authority/revocation evidence, durable transition/projection, resource PEP, drain/removal and suffix reconciliation | current tests prove crash-before-projection restart recovery, fork/gap/tamper and filename-collision refusal, positive reload/effect, stale CAS/epoch/membership/root/executing-node/resource/effect/timing/read denial, caller-context refusal, owner-omission/change/self-assignment refusal, promoted-backfill fail-closed behavior, and zero invoker calls; multi-node no-dual-writer and automatic-failover proof remain open (build steps 2 and 10) | Systems (context); Operations (member actions); Provenance |
| `OrderingFinalityRecovery`, `StateTransitionCommitment` | [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md), [`events-receipts-delivery-bundles.md`](../components/daemon-runtime/events-receipts-delivery-bundles.md) | profile-native recovery transition plus operational sequence/predecessor/operation/resulting commitments, admission proof, state root, and receipt root | static single-writer Agentgres transition/root precedents only; no typed profile-native recovery or complete INV-29 receipt | not started | daemon recovery/transition proposal -> active profile proof -> Agentgres CAS admission/receipt | reject stale predecessor, missing proof, fabricated writer epoch, and operational/economic settlement conflation (build steps 1-2) | Operations; Provenance |
| `NetworkServiceInvocation`, `SettlementEnvelope` | [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md), [`ioi-l1-contract-interfaces.md`](../foundations/ioi-l1-contract-interfaces.md) | selected registry/rights/reputation/finality operation separated from its economic obligation/payment rail | contract sketches and marketplace mirrors are precedents; no generic invocation/receipt or profile-neutral settlement adapter | not started | daemon selected-service/enrollment gate -> service adapter -> invocation receipt; independent settlement intent/obligation path | nonselected/expired enrollment refusal; registry operation can pay by invoice; acceptance/adjudication cannot be authored by escrow (build step 14) | Packages / Marketplace; Provenance; Governance |
| `LearningSourceRightsClaim`, `InstitutionalLearningBoundaryProfile`, `LearningEvidenceEligibility`, `LearningEgressReceipt`, `InstitutionalIntelligenceExportBundle`, `ModelSwapContinuityReport` | [`institutional-learning-boundary.md`](../foundations/institutional-learning-boundary.md), [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md), [`events-receipts-delivery-bundles.md`](../components/daemon-runtime/events-receipts-delivery-bundles.md) | source-rights claim plus sovereign-system compiler profile, run/job snapshots, general owner-qualified learning-eligibility decision, attempted-or-admitted egress receipt, policy-filtered capability-exit manifest, and composite provider-removal evaluation projection; `TrainingEvidenceEligibility` remains its model/worker-training profile | memory-vault export/import, route metadata, Agentgres operations, receipt substrate, policy-bound views, training-eligibility envelope, and inert Foundry objects are partial ingredients; no general eligibility object, compiled end-to-end boundary, or deployment-grade proof exists | not started as an integrated contract | system profile proposal/admission -> route/data/Foundry/runtime policy intersection -> `LearningEvidenceEligibility` at the improvement-use boundary -> pre-egress enforcement and receipt when crossing -> Agentgres lineage/impact -> export/import re-admission and provider-removal eval | [`institutional-learning-boundary.md`](../../conformance/hypervisor-core/institutional-learning-boundary.md): confidential canary blocked before network egress; private eligible improvement with transitive rights; incumbent disconnect and declared eval continuity; seller/cross-tenant denial; clean export/import; source revocation impact (build step 8) | Governance primary; contextual Data, Ontology, Models, Foundry, Evaluations, Improvement, Provenance, Environments/Operations; no new application |
| `WorkResult` | [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md) | canonical generic typed result beyond software patches with accountable producer/submitter, operator/affiliation lineage, exact producer-component snapshot/hash/admission receipt and resolver revision/hash for executable results, and required `RoomAdmittedObjectBase` when room-scoped; HarnessInvocation is an invocation binding, never the underlying work subject | generic admission plane implemented in the held stack (build step 3, not yet merged to master); software `implementation_result` remains one profile, but exact producer-component resolution, participant-lease actor binding, and the full room commitment spine are absent | partial precursor; not yet canonical room-result admission | component resolution + room participant lease -> expected room revision/predecessor -> decision/receipt/resulting commitment, then acceptance/challenge authority | existing work-result tests remain evidence for generic profiles; planned exact producer-resolution and producer/lease/CAS/INV-29 room admission proof (build steps 4-5) | Work (typed result detail); Provenance (evidence chain) |
| `OutcomeDelta` | [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md) | declared before/after outcome change bound to a result plus `RoomAdmittedObjectBase` for room-scoped evaluation/admission | held stack admits proposed result-bound deltas and exact room equality, but lacks participant-lease actor, expected predecessor commitment, decision, and resulting room commitment/root | partial precursor | evaluation/admission under room authority with exact lease and CAS transition spine | existing delta-binds-result tests plus planned room-admission transition proof (build steps 4-5) | Work / Rooms (outcome view) |
| `OutcomeRoom` | [`collaborative-outcome-pattern.md`](../domains/ioi-ai/collaborative-outcome-pattern.md), [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md) | reusable OutcomeRoom package whose every durable room genesis creates a bounded-DAS instance; all room children share `RoomAdmittedObjectBase` and cryptographic continuity | hosted aggregate implemented in the held stack (build step 4, not yet merged to master): expected-revision lifecycle and reciprocal GoalRun membership are real precedents, but package/genesis/system/profile binding, participant-lease actor binding, and full transition-commitment/receipt roots are absent | partial; not yet the canonical flagship-DAS proof | package-to-genesis activation, then participant lease + typed child admission under the room system (build steps 4-5) | existing held room tests remain evidence for aggregate concurrency; planned package/genesis and INV-29 room-transition proof is required before build step 9 closes | Systems (room-system context); Work / Rooms; ioi.ai Goal Space |
| `CollaborativeWorkGraph` | [`collaborative-outcome-pattern.md`](../domains/ioi-ai/collaborative-outcome-pattern.md) | canonical object graph: admitted participant/frontier/claim/attempt/finding/evaluation graph of one room | nearest substrate: per-run `adaptive_work_graph` execution strategy (`crates/types/src/app/chat.rs`) — a DIFFERENT, run-internal object | not started (run-internal graph exists; shared room graph absent) | graph heads admitted in Agentgres under the room aggregate; no direct client writes | planned: graph-transition receipt tests (build step 5) | Work / Rooms (workstream board); boards/digests are projections |
| `OutcomeRoomDiscovery` | [`aiip.md`](../foundations/aiip.md) | policy-bound discovery projection an external worker can query for eligible rooms | none | not started | policy-bound read projection with visibility policy enforcement at the daemon boundary | planned: AIIP discovery conformance (build step 12) | Work / Rooms (discovery lens); Developer Console (federation config) |
| `LocalAgentPairingSession` | [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md), [`aiagent/worker-endpoints.md`](../domains/aiagent/worker-endpoints.md) | short-lived challenge/key/origin-bound bootstrap for one `room_guest`, `private_worker`, or `organization_worker` target; exact resolver discriminator/revision/hash keeps the concrete AgentHarnessAdapter separate from its optional exact semantic HarnessProfile; a separate typed composition/participation proposal and least-privilege gateway follow only after admission | none; existing harness adapters and capability-lease gateways are precedents only | not started | pairing create/inspect/claim/complete/cancel/revoke routes; typed room/registry admission before gateway issuance; no bootstrap reuse | planned: exact profile/adapter distinction, replay/attempt-limit/origin mismatch, no-ambient-authority, prompt-only proposal ceiling, revoke, and no-publication tests (build step 5) | ioi.ai Add local contributor; aiagent.xyz My/Organization workers |
| `RoomParticipationRequest` | [`collaborative-outcome-pattern.md`](../domains/ioi-ai/collaborative-outcome-pattern.md) | canonical object: typed admission request (identity, affiliation, eligibility evidence, requested scopes) | none | not started | typed admission decision producing a receipt + (on accept) a `RoomParticipantLease` | planned: admission accept/refuse receipt tests (build step 5) | Work / Rooms (participant admission queue) |
| `ParticipantStateBundle` | [`collaborative-outcome-pattern.md`](../domains/ioi-ai/collaborative-outcome-pattern.md) | canonical object: portable exit bundle of permitted claim/contribution/result/finding/receipt refs with recorded exclusions | none | not started | export crossing: bundle assembly is policy-filtered and receipted; usable without the hosted room DB | planned: portable-exit proof (build step 12) | Work / Rooms (participant detail → export) |
| `RoomParticipantLease` | [`collaborative-outcome-pattern.md`](../domains/ioi-ai/collaborative-outcome-pattern.md) | canonical object: bounded leased participation (TTL, heartbeat, budget/authority leases, lifecycle) | nearest pattern: the CapabilityLease gateway (`crates/node/src/bin/hypervisor_daemon_routes/capability_lease_plan_routes.rs` + daemon lease gateway) — authority-crossing precedent, not the room lease | not started (lease-gateway pattern proven elsewhere) | lease issue/renew/expire/revoke transitions receipted under the room aggregate | planned: lease lifecycle tests incl. revocation releases claims (build step 5) | Work / Rooms (participants panel) |
| `ResourceOffer` | [`collaborative-outcome-pattern.md`](../domains/ioi-ai/collaborative-outcome-pattern.md) | canonical object: typed offer of compute/runtime/data/verification capacity to a room; a profile over existing resource inventory | nearest substrate: provider/candidate planes (`crates/node/src/bin/hypervisor_daemon_routes/provider_routes.rs`, candidate sources) | not started (inventory substrate exists; offer object absent) | offer admission bound to real inventory refs; acceptance creates allocation with spend visibility | planned: offer-accept-allocate receipt chain test (build step 5) | Work / Rooms (offers board); Operations (backing inventory) |
| `CapabilityOffer` | [`collaborative-outcome-pattern.md`](../domains/ioi-ai/collaborative-outcome-pattern.md) | canonical object: typed advertisement of a Worker/service/specialist capability to a room | nearest substrate: marketplace listing plane (`crates/node/src/bin/hypervisor_daemon_routes/marketplace_routes.rs`) | not started | offer admission with eligibility evidence; matching to frontier items receipted | planned: capability-match receipt test (build step 5) | Work / Rooms (offers board); Packages / Marketplace (listing origin) |
| `WorkFrontierItem` | [`collaborative-outcome-pattern.md`](../domains/ioi-ai/collaborative-outcome-pattern.md) | canonical object: claimable unit of room work with readiness/dependency state | none | not started | frontier transitions (open→claimed→resolved/returned) admitted under the room aggregate | planned: frontier state-machine tests + backpressure proof (build step 5) | Work / Rooms (frontier board) |
| `WorkClaimLease` | [`collaborative-outcome-pattern.md`](../domains/ioi-ai/collaborative-outcome-pattern.md) | canonical object: bounded pull-based claim on a frontier item (TTL, renewal, release-on-exit) | nearest pattern: CapabilityLease gateway (same precedent as `RoomParticipantLease`) | not started | claim issue/expiry/release transitions receipted; exit/revocation MUST release claims | planned: claim-release-on-revocation test (build step 5) | Work / Rooms (claims view) |
| `Attempt` | [`collaborative-outcome-pattern.md`](../domains/ioi-ai/collaborative-outcome-pattern.md) | canonical object: one bounded, isolated execution against a claim, run as a GoalRun | implementation precedent (not a partial Attempt): bounded GoalRun records + agent-run transcripts (`goalrun_routes.rs`; `/v1/hypervisor/agent-run-transcripts`) — the bounded-execution substrate an Attempt will run on | not started (no Attempt object; no attempt-to-claim binding) | attempt record binding claim ref + GoalRun ref at admission | planned: attempt-binds-claim invariant test (build step 5) | Work / Rooms (attempt timeline); Provenance (replay) |
| `Finding` | [`collaborative-outcome-pattern.md`](../domains/ioi-ai/collaborative-outcome-pattern.md) | canonical object: positive or negative typed discovery bound to attempts/evidence, valuable independent of success | implementation precedent only: the eval-suite library plane (`crates/node/src/bin/hypervisor_daemon_routes/eval_suite_routes.rs`) is DECLARATION-ONLY by its own contract — no run/execute endpoint, no scoring, no verdict — a precedent for declared assessment gates, not a findings or verdict plane | not started (no verdict or finding plane exists anywhere) | finding admission with evidence refs; negative findings first-class | planned: negative-finding lineage test (build step 5) | Work / Rooms (findings feed); Evaluations (verdict lineage) |
| `VerifierChallenge` | [`collaborative-outcome-pattern.md`](../domains/ioi-ai/collaborative-outcome-pattern.md) | canonical object: typed challenge against a result/finding with resolution lifecycle | nearest substrate: improvement governance gates (`crates/node/src/bin/hypervisor_daemon_routes/*` improvement/governance planes) | not started | challenge admission + resolution transition receipted; unresolved challenges block acceptance | planned: challenge-blocks-acceptance test (build step 5) | Evaluations (challenge queue); Work / Rooms (room detail) |
| `OntologyVersion` | [`domain-ontologies-and-data-recipes.md`](../foundations/domain-ontologies-and-data-recipes.md) | canonical object: immutable `DomainOntologyEnvelope` with `ontology_record_profile: ontology_version` (compatibility/deprecation contract per namespace) | implementation precedent (not a partial OntologyVersion): ODK's MUTABLE single-domain revision counter + `expected_revision` optimistic concurrency + revision history (`crates/node/src/bin/hypervisor_daemon_routes/odk_routes.rs`) — concurrency discipline the immutable version object will reuse | not started (no immutable version object, no cross-namespace identity) | version publication as immutable admitted record with cross-namespace identity | precedent evidence: `odk_tests` (revision conflict = typed, zero mutation); planned: federated version lineage tests (build step 7) | Ontology (Manager history) |
| `OntologyOverlay` | [`domain-ontologies-and-data-recipes.md`](../foundations/domain-ontologies-and-data-recipes.md) | canonical object: `DomainOntologyEnvelope` with `ontology_record_profile: ontology_overlay` + explicit base-version refs | none | not started | overlay admission bound to a base `OntologyVersion` ref | planned: overlay-resolves-against-base test (build step 7) | Ontology (Manager overlays) |
| `OntologyCrosswalk` | [`domain-ontologies-and-data-recipes.md`](../foundations/domain-ontologies-and-data-recipes.md) | canonical object: `OntologyMappingEnvelope` with `mapping_record_profile: ontology_crosswalk` (declared loss, ambiguity, scope, verifier requirements) | nearest substrate: single-domain `ConnectorMapping` (`crates/node/src/bin/hypervisor_daemon_routes/connector_mapping_routes.rs`) — provider→domain, not domain→domain | not started (in-domain mapping exists; cross-domain crosswalk absent) | crosswalk admission binding two version refs; used at AIIP profile negotiation | planned: two-domain profile negotiation proof (build steps 7+12) | Ontology (crosswalk view); Developer Console (federation) |
| `SemanticMappingDecision` | [`domain-ontologies-and-data-recipes.md`](../foundations/domain-ontologies-and-data-recipes.md) | canonical object: applied `OntologyMappingEnvelope` with `mapping_record_profile: semantic_mapping_decision` — challengeable, receipted application of a crosswalk to a concrete handoff | implementation precedent (not a partial decision object): `ConnectorMapping` create/update receipts (`connector_mapping_routes.rs`) — receipted mapping mutations without decision identity | not started (no decision-as-object, no reviewer lineage) | decision admission with reviewer identity + receipt | planned: mapping-decision lineage test (build step 7) | Ontology (mapping review) |
| `DataRecipe`, `ConnectorMapping`, `TransformationRun` | [`domain-ontologies-and-data-recipes.md`](../foundations/domain-ontologies-and-data-recipes.md), [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md) | immutable DataRecipe and successor-versioned ConnectorMapping definitions whose content hashes commit exact semantic-component snapshots; TransformationRun freezes the exact recipe revision/hash and identical semantic tuple while owning concrete outputs, grants, and receipts | mutable connector-mapping routes and adjacent dataset/transformation records exist, but they do not expose the canonical immutable mapping revision/hash and exact semantic-component snapshot contract | partial precursor; exact definition/run split not started | publish immutable recipe/mapping revisions, resolve exact ontology/object-model/schema/view dependencies, then admit TransformationRun against the identical tuple | same signed package/recipe cannot execute against a later semantic head; mapping change requires successor mapping and recipe; run rejects tuple mismatch and emits a transformation receipt | Data and Ontology for definitions/runs; Packages for immutable distribution; Provenance for receipts |
| `ProvenanceAssertion` | [`domain-ontologies-and-data-recipes.md`](../foundations/domain-ontologies-and-data-recipes.md) | canonical object: `OntologyAssertionEnvelope` with `assertion_profile: provenance_assertion` — time/source/confidence/evidence/dispute-bearing claim; admission records it without making it universally true | implementation precedent (not a partial assertion object): the receipt families + proof stream (`/v1/hypervisor/work-ledger` route in `crates/node/src/bin/hypervisor-daemon.rs` — route name is API compatibility; the product surface is Provenance) — evidence substrate an assertion will bind | not started (no assertion-as-object) | assertion admission binding receipt refs; challenge path shared with `VerifierChallenge` | planned: assertion-binds-receipts test (build step 7) | Provenance (assertion graph) |
| `OntologyActionContract` | [`domain-ontologies-and-data-recipes.md`](../foundations/domain-ontologies-and-data-recipes.md) | canonical object: executable semantic action binding target object + typed IO to pre/postconditions, capabilities, authority, risk, idempotency, compensation, verifier, evidence, and receipt obligations | nearest substrate: ODK COM `action_types` (`odk_routes.rs`) + `RuntimeToolContract` split tiers (`crates/types/src/app/runtime_contracts.rs`) | not started (typed action definitions + tool contracts exist separately; the binding contract absent) | contract admission binding action type ref → tool contract ref → `authority_scope_requirements`; execution stays behind wallet/policy | planned: action-contract execution refusal/receipt tests (build step 7) | Ontology (action contracts); Studio (composition wiring) |

## The build sequence this table pulls

The ordered, contract-first sequence lives in
[`execution-horizons.md`](./execution-horizons.md#the-build-sequence-contract-first).
Rows above name which step implements them. Closure is the working proof —
no step is bound to a PR number.

## Deferred application-UX backlog (replaces the PR68 queue)

The operational-depth atlas
(`apps/hypervisor/application-operational-depth.json`, verified by
`apps/hypervisor/scripts/verify-hypervisor-operational-depth.mjs`) remains
valid AUDIT EVIDENCE: its per-surface censuses, daemon cross-checks, and
security notes are the record of what each surface can honestly do today.
What this file retires is its RANKED QUEUE as the primary implementation
sequence. Surface operationalization is now PULLED by contracts — each row
below is deferred and resumes only when pulled by an implemented contract:

| Surface | Status | Resumes when |
| --- | --- | --- |
| Changes (Upgrade Assistant) | deferred | pulled by an implemented improvement/challenge contract (`VerifierChallenge`, governance transitions) |
| Monitors (Automate) | deferred | pulled by automation authority the room/frontier plane exposes |
| Models (Model Catalog) | deferred | pulled by route-binding/metering contracts (Horizon 0 model-route work) |
| Designer (Solution Designer) | deferred | pulled by a real composition-write contract (Studio-owned) |
| Incidents (Issues) | deferred | pulled by an incident/finding contract (`Finding` admission) |
| Machinery | deferred | pulled by state-machine execution authority |
| Evalsuites (AIP Evals) | deferred | pulled by `Finding`/`VerifierChallenge` admission (build step 5) |
| Explorer (Object Explorer) | deferred | pulled by federated ontology versions/overlays (build step 7) |
| Packages / Marketplace (Listings) | deferred | pulled by `CapabilityOffer`/settlement contracts; local package lifecycle does not wait for commerce |

Surfaces implemented in the held stack (Approvals, Ontology Manager,
Pipeline Builder, Sources — PRs #61–#69, open and held for review, NOT yet
merged to master) keep their implementations and verifiers; nothing in this
reclassification discards them. They land per their own review gates; this
backlog governs NEW surface work only.

## Related Canon

- [`implementation-matrix.md`](./implementation-matrix.md) — per-concept durable-form index (the wider matrix).
- [`execution-horizons.md`](./execution-horizons.md) — horizon framing + the contract-first build sequence.
- [`source-of-truth-map.md`](./source-of-truth-map.md) — subject-to-owner map.
- [`../foundations/institutional-learning-boundary.md`](../foundations/institutional-learning-boundary.md) — enterprise-owned learning compiler, portability, and model-independence owner.
- [`../domains/ioi-ai/collaborative-outcome-pattern.md`](../domains/ioi-ai/collaborative-outcome-pattern.md) — the room/pursuit behavior owner.
- [`../foundations/domain-ontologies-and-data-recipes.md`](../foundations/domain-ontologies-and-data-recipes.md) — the federated ontology / assertions / mappings / action-contracts owner.
- [`../foundations/aiip.md`](../foundations/aiip.md) — the inter-domain protocol owner.
