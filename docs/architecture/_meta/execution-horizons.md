# Execution Horizons

Status: canonical architecture note.
Canonical owner: this file for the horizon framing that separates the launch wedge from long-horizon breadth without narrowing canon.
Supersedes: readings of the canon that mistake speculative breadth for current shipped surface, or that treat horizon labels as scope deletion.
Superseded by: none.
Last alignment pass: 2026-07-19.
Doctrine status: canonical
Implementation status: mixed (this note classifies; subject owners carry per-file status)
Last implementation audit: 2026-07-05

## Why this note exists

The canon deliberately specifies more than is built. That breadth is an asset
— it keeps the object model coherent as lanes land — but only if readers can
tell what is shipping from what is designed. This note gives the horizon
frame; the per-file `Implementation status` axis (see
[`doc-classes.md`](./doc-classes.md)) gives the per-subject truth.

**Horizon does not equal deletion.** Nothing here narrows the architecture.
A `speculative` label is a maturity statement, not a scope cut.

## Product-taxonomy migration gate

The target Hypervisor topology is canonical before it is fully shipped, but it
must not be presented as implemented by documentation alone. Before permanent
navigation changes:

- run `HypervisorCoreWorkspaceRegistration` and
  `HypervisorApplicationSurfaceRegistration` through a policy-filtered catalog
  compiler in shadow mode against the current hard-coded inventory;
- distinguish owner applications, substrate applications, tool surfaces, and
  extension applications while keeping publisher, creation method,
  distribution, availability, admission, installation, package disposition,
  enablement, capability depth, and operational state independent;
- stop new generic `HypervisorMission` writes, create typed GoalRun,
  OutcomeRoom, AutomationRun, Session, WorkItem, and WorkRun associations, and
  emit reviewable alias/migration receipts for legacy records;
- do not put Systems in the permanent rail until genesis, constitution,
  deployment, observed membership, and lifecycle read models can render honest
  inventory and detail;
- do not replace the broad Sessions/Missions estate with Work until every row
  has a policy-filtered typed subject and compatibility routes preserve query,
  hash, embed, return-path, Project, System, and backing-subject context.

Packages/generated-interface/Embodied Systems UX remains pulled by its owning
contracts and horizon. The taxonomy gate cannot become a competing feature
sequence or a way to advertise unimplemented system capability.

## Selected minimum-L0 proof profile

Horizon 0 is a contract portfolio, not one aggregate release blocker. IOI must
select one minimum deployment profile that exercises the indispensable
boundaries end to end while leaving conditional breadth honest. This selection
does not create a `MinimumL0` object, redefine a bounded DAS, or make unselected
features optional for every later deployment.

### Required sovereign-local fixture

The selected profile must first pass the
`embedded_single_operator_offline` fixture with every IOI-managed endpoint
denied. Preloaded signed package, tool, fixture, and local-model artifacts are
allowed. The fixture must not require an `ioi.ai` account, hosted
wallet.network login, marketplace, IOI Network enrollment, IOI L1, license
heartbeat, telemetry, update service, or external model provider. Loopback and
explicit local IPC remain available. Optional planes return typed unavailable
or degraded dispositions rather than fabricated success.

The selected first profile is an **OutcomeRoom-backed bounded software change
institution**. One reusable package instantiates one single-node,
single-authority System that can receive a bounded change goal, allocate
independent work and verification, propose a sandboxed repository change,
obtain exact effect authority, apply or refuse the change, and preserve the
complete result/evidence/lifecycle chain. It combines the flagship OutcomeRoom
with a narrow domain whose effects can be reproduced, diffed, tested, reversed,
and adversarially inspected.

Required for this profile:

- one immutable package/release, genesis record, stable `system_id`,
  constitution, protected amendment path, declared `single_authority`
  ordering/finality profile, single-node deployment and observed membership,
  oracle/evidence profile, and lifecycle/terminal policy;
- one source-neutral Studio/ADK/CLI build representation and the
  blank-or-template-to-genesis product journey;
- one admitted `GoalRunProfile`, frozen `GoalRun`, optional directed workflow,
  disclosed Worker/role topology, scoped skills/tools/model routes, isolated
  work environment, generic result, independent deterministic verifier, and
  OutcomeRoom admission/claim/finding lineage;
- deployment-local identity authentication and a low-risk product session for
  the isolated lane, with authentication remaining separate from a locally
  permitted authority provider; exact consequential-action review, scoped
  expiring authority, daemon-boundary revalidation, effect fencing, receipt,
  and revocation remain required. A separately tested connected lane repeats
  the journey with provider-neutral account authentication, passkey-capable
  step-up, and the context-bound portable `AuthorityGrantEnvelope` v3
  successor. The isolated lane does not claim portable delegated authority,
  and hosted wallet.network login is never its prerequisite;
- Agentgres-backed operations, receipts, replay, artifact/source-to-export
  lineage, assertion/evidence admission, and inspectable state roots;
- proposal-mediated package/profile improvement plus target-owner
  approval, activation, rollback/recall, suspension, and dissolution.

Not required to claim this first profile complete:

- multiple runtime nodes, automatic failover, cross-system AIIP, public
  marketplace participation, payments, IOI Network enrollment, IOI L1,
  physical actuation, cTEE, public certification, or generalized recursive
  improvement;
- generic correctness, universal factual truth, autonomous production access,
  hidden provider non-learning, or any capability outside the exact selected
  package and conformance claims.

Those capabilities remain canonical where their profiles require them and
advance through later horizons. The first proof wins by closing one real
vertical, not by simulating the entire roadmap.

## Undeniable-product proof gate

The first profile crosses from architecture demonstration to product proof only
when one operator can complete this visible journey through supported product
surfaces and public contracts:

```text
authenticate through the deployment-local or connected identity lane
  -> choose the bounded software-change template
  -> describe the goal, repository, constraints, authority, and acceptance
  -> validate, preview, and simulate one compiled package/genesis proposal
  -> approve genesis and inspect the stable System
  -> start or admit the GoalRun in its OutcomeRoom-backed work context
  -> observe planning, claimed work, attempts, verification, and blockers
  -> review the exact proposed repository effect
  -> satisfy the lane's exact scoped authority ceremony
  -> let the daemon revalidate and execute or refuse the effect
  -> inspect the diff, tests, evidence admission, receipt chain, state root,
     costs, provider route, and learning eligibility
  -> replay the decision and effect from exported evidence
  -> propose an upgrade, exercise rollback or recall, and retire the System
```

The gate fails if any step requires hidden database edits, a privileged
one-off script unavailable to the product, copied bearer authority, an
uninspectable prompt-only transition, a fabricated success row, or manual
reconstruction of the evidence chain. A compact flow and the advanced
declaration must resolve to the same typed objects and hashes.

The gate runs in two ordered profiles:

1. **Sovereign-local completeness.** With every IOI-managed endpoint blocked,
   the operator uses a fixture repository, embedded Agentgres, local storage,
   deployment-local identity, a locally permitted authority provider, and an
   admitted local-model or deterministic fixture route to complete package
   admission, genesis, governed work, independent verification, exact-effect
   execution or refusal, receipt inspection, restart/replay, backup/restore,
   evidence export and offline verification, upgrade/recall, and retirement.
   Accountless never means unauthenticated, and single-node operation claims
   only its actual durability and failure domain.
2. **Managed optionality.** Beginning from that independently operable System,
   the operator links an account, explicitly enables one managed service,
   executes one named managed operation through its exact data view, lease,
   RuntimeAssignment, authority, and quote or rate contract, inspects the
   binding, custody, usage, charge, and receipt chain, then revokes or
   disconnects it. The gate fails if connection silently moves truth or
   custody, grants authority, changes package or System identity, creates a
   writer, or starts billing; it also fails if detachment prevents work whose
   complete dependency closure remains locally satisfied.

The minimum-L0 claim requires the first profile. A production self-hosted
organization claim additionally requires a headless single-node fixture. A
managed-optionality claim requires both ordered profiles. Multi-node HA,
IOI-managed hosting, identity-preserving migration, AIIP, IOI L1, marketplace,
cTEE, and physical-action claims remain conditional until their own fixtures
pass.

The target cases and claim-scoped fixture requirements live in
[`sovereign-local-completeness.md`](../../conformance/hypervisor-core/sovereign-local-completeness.md).

At minimum the proof records time to first valid preview, time to genesis,
typed-blocker resolution rate, authorized completion rate, verifier
reproducibility, effect-recovery success, receipt/replay completeness,
provider-swap continuity, recovery/revocation behavior, time to a locally valid
System, network-blocked completion, managed-link scope accuracy, disconnect
continuity, export/import portability, and the amount of architecture vocabulary
exposed to a first-time operator. Thresholds and baseline cohorts are
release-program decisions; omitting the measurements is not.

## Horizon 0 — bounded-DAS contract convergence and commercial truth

Make the architecture executable without pretending the product is already
commercially metered:

- implement the constitution, manifest, deployment, observed membership,
  failover, ordering/admission/finality, oracle/evidence, lifecycle transition,
  and optional network-enrollment envelope families before presenting a
  persistent autonomous system as bounded;
- implement protected amendment, node join/role/catch-up/root/fence/drain,
  migration/succession/dissolution, and enrollment transition receipts with
  their distinct authority paths;
- implement the now-canonical `CollaborationTerms`, terms-acceptance receipt,
  `LocalAgentPairingSession`, `OutcomeRoom`, `OutcomeRoomDiscovery`,
  `RoomParticipationRequest`, `ParticipantStateBundle`, room participant leases,
  resource/capability offers, work-frontier items, claim leases, generic
  attempts, findings, verifier challenges, `WorkResult`, and `OutcomeDelta`
  over existing GoalRun, OutcomeRoom, Agentgres, AIIP, evaluation, governance, and
  contribution owners; preserve exact-root acceptance, counteroffer/decline,
  private valuation, non-retroactive amendment, and attribution-not-allocation;
- implement generic Goal Kernel results beyond software patches while retaining
  `ImplementationResultPayload` as the software profile;
- implement the canonical locally owned ontology namespace/version/assertion
  semantics, optional versioned mappings when selected, and executable
  OntologyActionContracts;
- bind every model/provider route to access mode, automation, downstream-use,
  credential-principal, privacy/retention, region, parameter, fallback,
  price-limit, and bidirectional learning rights: provider use of customer
  material and customer retention/evaluation/tuning/distillation/reuse of model
  outputs remain separately explicit;
- implement `InstitutionalLearningBoundaryProfile` as the fail-closed compiler
  across source rights, policy-bound views, consent, custody, route rights,
  `LearningEvidenceEligibility` (including its training compatibility profile),
  retention/export, derivative lineage, and revocation;
  add `LearningEgressReceipt` and the policy-filtered
  `InstitutionalIntelligenceExportBundle` without creating another authority,
  truth, runtime, application, or privacy plane;
- implement the optional bounded-improvement spine:
  `ImprovementGovernanceProfile`, `ImprovementAgenda`, `ImprovementCampaign`,
  `EvaluationEpoch`, `EvaluationExposureLedger`,
  `ImprovementOrderCutoffReceipt`, and `ImprovementEvidenceClaim`, while
  preserving the direct one-shot `UpgradeProposal` path and keeping Search,
  Judgment, and Authority separately attributable;
- replace flat per-receipt model metering with invoice-grade route-attempt,
  price-schedule, supplier-cost, broker-fee, IOI-fee, adjustment, Work Credit,
  and provider-statement reconciliation;
- preserve the assurance ladder from receipt to evidence, verification,
  acceptance, adjudication, and settlement.
- freeze the native Embodied Runtime contract surface before implementation:
  immutable `EmbodiedRuntimeGraphManifest`, composable `micro`/`edge`/`site`
  profiles, isolated local execution strata, `PhysicalStreamContract`,
  `EmbodimentAdapter`, `EmbodiedActionPolicyContract`, non-authoritative
  `EmbodiedActionChunk`, native `LocalControlSupervisor` versus compatibility
  `LocalControlBridge`, `EmbodiedGraphActivationTransaction`,
  `SpacetimeReservationLease`, and deployment-bound assurance-case refs. This
  contract work does not make the compiler, executor, supervisor, adapters, or
  Embodied Systems application implemented;
- implement versioned A2A, MCP, directory/schema, and settlement-adapter
  bindings without making any external standard an authority or assurance
  shortcut.

The BYO/managed provider plane, applicable authority gates, storage/archive
custody, Hypervisor clients and surfaces, and Agentgres receipt spine are
enabling substrate in this horizon. Provider-count expansion is not itself the
product wedge.

## Horizon 1 — single-node flagship proofs

### Horizon 1A — flagship OutcomeRoom bounded DAS

Build ioi.ai's reusable OutcomeRoom package and instantiate each durable room as
a permissioned flagship bounded DAS on one Hypervisor Node and one declared
Agentgres admission domain. The hosted service operates many room systems; it
is not itself their shared logical identity. OutcomeRoom is the reference
implementation because it exercises the workgraph, verification, contribution,
and course-correction stack; it is not the definition of L0.

The first terminal instantiation uses the selected bounded software-change
profile above. Later OutcomeRoom profiles may target research, enterprise
operations, public-interest work, embodied coordination, or other ontologies
without inheriting software-change fields.

This proof must:

- bind the package/release root, room genesis, stable `system_id`, active
  constitution, deployment, single-authority ordering/finality profile,
  oracle/evidence policy, lifecycle/terminal policy, locally compatible
  enrollment posture, and sequence-zero state/receipt/transition roots;

- a disclosed first-party seed fleet of named planner, researcher/builder,
  deterministic verifier, model critic, and synthesizer Worker compositions;
- lawful cognition supply through open/self-hosted weights, provider APIs,
  dedicated endpoints, or expressly permitted user-scoped routes—not pooled
  enterprise chat/workspace seats;
- pull-based claim leases, dynamic participants, capability/resource offers,
  queue backpressure, spend limits, isolated attempts, positive and negative
  findings, verifier challenges, contribution lineage, and replay;
- hostile participant inputs remain tainted until bounded execution, policy,
  verification, and hosted admission accept them;
- Goal Space and Hypervisor Work / Rooms render the frontier,
  active/sleeping/waiting/quarantined
  participants, leases, costs, evidence, blockers, authority, and course
  corrections instead of hiding agents behind a token stream.

Commercial posture: internal telemetry first, then a prepaid capped beta with
no model-quality or upstream-availability SLA. A fixed included Work Credit
allowance waits until provider-invoice reconciliation, p50/p90 COGS, fallback
amplification, abuse/chargebacks, and accepted-outcome-per-dollar are bounded.

### Horizon 1B — first bounded improvement campaign

Prove the optional campaign path on one low-risk, target-order-0 software or
pursuit-method target inside one System. This proof complements rather than
replaces the direct one-shot `UpgradeProposal` path:

- admit one immutable owner-scoped `ImprovementGovernanceProfile`, one
  immutable-by-revision `ImprovementAgenda`, one finite `ImprovementCampaign`,
  one exact target base, and one coordinating `GoalRunProfile` resolution;
- identify Search, Judgment, and Authority separately even when a low-risk
  profile places them under one accountable organization;
- freeze one `EvaluationEpoch` with visible-development and sealed-survival
  suites, a declared statistical method, and fixed resource, wall-clock,
  false-promotion, learning-rights, and evaluation-exposure ceilings;
- run immutable candidate attempts, preserve ancestry plus positive, negative,
  exploit, invalid, and inconclusive findings, and append every protected
  evaluation access to the exposure ledger;
- reproduce the selected result against the unchanged epoch and target base,
  then emit only a target-owner `UpgradeProposal` with evidence and recovery
  references;
- prove that the Campaign cannot alter its selecting evaluator, reset budgets
  by branching, promote itself, or mutate production. Any shadow, canary, or
  activation crosses the target owner's ordinary governance and release path.

This closes only the bounded-campaign vertical slice. It is not evidence of
recursive improvement, ignition, inflection, or a generally capable optimizer.

## Horizon 2 — one logical DAS doing useful work across nodes

Before claiming sovereign federation, prove that one bounded DAS can distribute
both its durable state **and useful work** across admitted nodes without losing
one logical identity, authority boundary, or effect history.

### Horizon 2A — continuity across two failure domains

Run the Horizon 1 system across two declared nodes/failure domains before
claiming sovereign federation:

- preserve one stable `system_id`, constitution, governance owner set, and
  ordering/finality profile;
- provision and attest a candidate, propose membership, assign
  `state_replica`/`hot_standby`, restore a checkpoint, catch up the ordered log,
  verify the current state root, and mark observed readiness;
- prove durability and read/execution distribution without claiming that
  replication is consensus, multi-party independence, or authority expansion;
- inject writer/node/link failure, reconcile active work leases, promote only
  through operator-controlled policy, increment the writer epoch, fence the old
  writer, replay, and prove no dual effects;
- expose desired versus observed topology, failure domains, watermarks,
  catch-up, root, epoch/fence, RPO/RTO, degraded state, drain/removal, and
  continuity receipts through Operations and Provenance;
- leave dynamic membership, leader election, and automated failover labeled
  unbuilt until their own fault-model/conformance proof exists.

This is the minimum credible distributed-system continuity proof. Replication
and failover are necessary, but they do not by themselves prove the substrate
can orchestrate a useful distributed autonomous system.

### Horizon 2B — useful same-system distributed work

Run a non-trivial GoalRun/workload whose complementary roles execute across the admitted
nodes while remaining one system:

- bind GoalRun/RoleTopology work, runtime assignments, capability/resource
  offers, and claim leases to the stable `system_id`, deployment profile, and
  observed node memberships without granting authority by placement;
- prove leased allocation, backpressure, shared-state watermarks, coordination
  epochs, fenced reassignment, partition/degraded behavior, rejoin, rebalance,
  and duplicate/ambiguous-effect reconciliation;
- inject worker, coordinator, node, and link failure during useful work; show
  which work continues, pauses, reassigns, or fails closed, and prove that
  completion or external effects are not silently duplicated;
- expose logical roles, physical placement, active/superseded leases, world or
  task-state freshness, coordination epochs, effect status, and recovery
  through Systems, Work, Operations, and Provenance;
- compile one exact `EmbodiedRuntimeGraphManifest` into at least `edge` and
  `site` footprints, with a `micro` safety/control target represented in HIL or
  an equivalent bounded target; prove that deployment placement changes do not
  change graph semantics, action authority, or safety invariants;
- activate the graph transactionally: prepare components and resources,
  validate physical-stream QoS/time/criticality compatibility and local
  supervisor readiness, then commit or return every local target inactive and
  unarmed. A distributed coordination barrier must not be described as global
  physical atomicity;
- exercise native same-system fleet/swarm coordination through simulation,
  HIL, or shadow mode before live promotion: epoch-fenced mission allocation
  and `SpacetimeReservationLease` may span nodes and units, while autonomy,
  deterministic motion, runtime-assurance veto, heartbeat, recovery, and
  emergency stop remain locally enforceable. `EmbodiedActionChunk` remains a
  fresh bounded proposal rather than an actuator command;
- inject stream deadline/freshness/clock/liveliness failures, autonomy/GPU
  failure, coordinator loss, link partition, conflicting reservation, standby
  controller takeover, stale allocation, and ambiguous physical effect. Prove
  one active actuator writer, safe local degrade/stop, fenced reassignment,
  rejoin reconciliation, and no widened disconnected authority;
- require live physical promotion to pass the separate Physical Action Safety
  and Embodied Runtime gates and bind the graph, hardware/software, operational
  design domain, hazards, timing/fault assumptions, tests, and evidence through
  the deployment assurance case. A simulated, SIL, HIL, or shadow proof is
  evidence at its declared stage, never a completed physical mission or generic
  certification.

This is intra-system coordination, so the members do not negotiate AIIP
collaboration terms with their own system. Different operators, sites, or
failure domains do not by themselves create sovereign peers; if a participant
requires its own constitution, operational truth, admission authority, and
independent exit, model it as a separate DAS and cross AIIP in Horizon 3. This
proof is explicitly unbuilt until the contracts and conformance evidence exist.
Together, Horizon 2A and 2B are the first evidence that L0 is more than an
application wrapper; they are not yet an Internet-of-Intelligence or
multi-party network proof.

## Horizon 3 — two sovereign DASs over AIIP

Where both systems' governed decisions find positive cooperation surplus, run
one OutcomeRoom across two independently governed Hypervisor/Agentgres domains
via an initial AIIP semantic profile and conformance harness. If either system
declines the exact terms root, the conformance proof must preserve local
operation without penalty or implicit admission:

- begin with a customer/data-owner system and an IOI
  coordinator/managed-worker system, then add an independently operated worker
  provider and verifier;
- use no shared raw operational database or raw private room context;
- declare `hosted_admission` or `federated_admission` shared-state ordering,
  merge, conflicts, adjudication, and failover;
- negotiate one `CollaborationTermsEnvelope` root covering scope, roles,
  rights, obligations, disclosure, contribution/reward basis, risk, exit, and
  settlement; prove exact-root accept/counteroffer/decline and non-retroactive
  amendment behavior;
- negotiate ontology/action profiles and exchange only signed, sequenced,
  idempotent, policy-permitted refs and restricted views;
- repeat useful distributed work across the sovereignty boundary without
  collapsing it: each DAS retains its own `system_id`, membership, operational
  truth, coordinator, admission, and exit, while AIIP carries the accepted
  work/handoff/evidence envelopes between them;
- for embodied cooperation, keep each fleet's controller bindings, mission
  envelope, shared-world-state truth, local safety veto, and emergency stop
  local; AIIP may carry mission offers, claims, checkpoints, restricted views,
  evidence, and receipts, but never a direct foreign servo/control stream;
- preserve independent verification, acceptance, contribution lineage, and
  dispute state without requiring IOI L1 for ordinary operation;
- prove that an independently operated external Worker can discover an eligible
  room through a policy-bound projection, negotiate semantic/action profiles,
  submit a typed participation request, receive bounded leases, claim work,
  return a verifiable contribution, preserve credit/dispute lineage, retire or
  be revoked with claims released, and retain a portable participant-state
  bundle without sharing one runtime, database, administrator, or continued
  IOI-host trust.
- make the proof economically non-theatrical: disclose any subsidy; show that
  the sponsor receives measurable value above an eligible same-domain or
  direct-local baseline; and show that every required external participant
  receives its contracted payment, outcome right, reciprocal access, license,
  reputation, learning, or other benefit after declared cost and risk.

This is the minimum credible Internet-of-Intelligence conformance proof. It has
priority over broad application-catalog expansion, generalized cloud-provider
breadth, or public token economics.

## Horizon 4 — Goal Space and open challenge service

Expose the proven substrate through one ioi.ai Goal Space product:

- persistent private goal, organization pursuit, invited cross-org pursuit,
  and open challenge are room policies over one architecture;
- the screenshot-like Add local contributor path uses an expiring
  challenge/key/origin pairing, proposal-only bootstrap, typed room/registry
  admission, and a separate least-privilege gateway; pairing alone grants no
  membership, context, budget, tools, authority, reputation, or payout;
- an open challenge may target IOI protocol/reference-implementation
  improvement or a custom DAS, but admitted results become governed upgrade
  proposals; contributor rank or verification never grants release authority;
- after Horizon 1B passes, a separately admitted target-order-1 recursive-seat
  experiment may test whether an improved pursuit method transfers to fresh
  lower-order portfolios under fixed budgets, cross-play, ablation,
  reproduction, and sealed evaluation. Its output is a qualified
  `ImprovementEvidenceClaim`, never an ignition claim or release authority by
  default;
- same-domain 1–N worker orchestration is included in the target subscription
  contract;
  Network/Open contributors use a separately bounded goal budget;
- the subscription pays for conductor, memory, policy, collaboration, receipts,
  replay, and an observed-cost monthly Work Credit grant;
- `Auto`/1-of-N, `Pinned`, and `Compare`/N-of-N are execution policies, not
  separate node products; heavy work uses explicit top-up, overage, or
  committed-spend drawdown;
- marketplace sourcing, anti-spam/Sybil controls, permissioned visibility,
  public projections, multi-objective leaderboards, worker/service fees, and
  settlement appear only where they add real value.

Managed inference uses a diversified portfolio: direct provider APIs and
negotiated/dedicated capacity for core routes, aggregators as replaceable
breadth/overflow adapters, customer BYOK/BYOA, and local/open-weight capacity.
The durable margin is conductor, governed runtime, verified routing, assurance,
private deployment, and outcome coordination—not opaque token arbitrage.

## Horizon 5 — enterprise and embodied verticals

Apply the proven same-system distribution and cross-system collaboration fabric
to a selectively interoperating enterprise operational domain and a live
embodied mission. This horizon advances a native IOI embodied stack rather than
making a partner runtime the architecture: the admitted graph runs across
composable `micro`, `edge`, and `site` profiles; `EmbodimentAdapter` and
`LocalControlBridge` remain compatibility surfaces; `LocalControlSupervisor`
remains the native deterministic execution and runtime-assurance root. The
higher-level two-speed rule stays intact while the fast side proves separately
isolated autonomy, deterministic-motion, and runtime-assurance/safety strata.

Foundry must run a backend-neutral scenario/graph loop from simulation through
SIL, HIL, shadow, and limited live operation, preserving graph, action-policy,
stream, evaluation, and evidence identities across replaceable simulator,
middleware, hardware-rig, and runtime bindings. Promote only through the target
owner's ordinary transfer, Physical Action Safety, Embodied Runtime, assurance,
and governance gates; Foundry evidence does not activate a graph or authorize an
actuator. The embodied proof matrix must cover compile/admission, transactional
activation and restart-unarmed behavior, stream/time/QoS failure, bounded
scheduling, independent local veto/recovery/e-stop, single-writer fencing and
safe takeover, space-time conflicts, partition/rejoin/effect reconciliation,
adapter non-equivalence, and deployment-bound assurance lineage. Validate
accepted semantic mapping, restricted views, continuous policy obligations,
derived-artifact recall, audit, liability, customer-boundary/private execution,
and same-system L0 coordination; use AIIP only when a separately sovereign
participant accepts exact cross-system terms.

The enterprise proof must make owned learning operational rather than merely
stateful: admit a confidential correction and private eval under one boundary
revision; block a forbidden provider route before network egress; produce a
rights-eligible improvement with transitive lineage; hard-disable the incumbent
model/provider; meet declared eval floors through a replacement route using only
institution-controlled, rights-eligible state; deny seller/cross-tenant access;
export and re-import the eligible capability into a clean customer-operated deployment;
then revoke one source and prove quarantine, reevaluation, recall, or clean
rebuild according to policy. The proof may not claim hidden provider
non-learning, equivalent replacement quality, or weight-level forgetting from
contracts, receipts, or absent canary hits alone.

Provider breadth, cTEE private workspaces, HypervisorOS bare-metal, ecosystem
assurance/certification, and vertical domain packs advance when these scenarios
pull them, not as independent category theses.

## Horizon 6 — public economic commitments

Only after repeated verified demand should IOI attach public reputation roots,
rights, bonds, dispute finality, token/BME economics, or other IOI L1
commitments to selected contributions. Attempts, GoalRuns, tool calls, model
calls, routine receipts, and local autonomous-system transitions remain local;
public consensus is used only when independent ordering, rights, reputation,
dispute, or economic finality creates value.

Prove registry, rights, escrow, verifier/guardian/availability services, bonds,
claims, disputes, enrollment, and exit on a devnet or mature compatible
settlement environment before selecting a sovereign L1. A native asset is
gated behind real public-capacity and risk-bearing demand; it is never Work
Credits, generic model-call currency, or a token per DAS.

## The build sequence (contract-first)

The horizons above frame maturity; this is the ORDER work is pulled in. Each
step is a durable contract with its own conformance proof — application UX
resumes only when one of these contracts pulls it (see
[`canon-to-code-delta.md`](./canon-to-code-delta.md) for the object-level
delta and the deferred UX backlog):

1. Bounded-system constitutional core — reusable package/release manifest,
   live-system genesis, constitution, protected amendment,
   ordering/admission/finality, oracle/evidence, lifecycle, and
   network-enrollment contracts plus negative tests.
2. Deployment and observed membership core — desired role/failure-domain/
   durability/failover profile separated from node identity, admission, lease,
   catch-up, state-root, readiness, epoch, fencing, drain, and removal records.
3. Generic `WorkResult` and `OutcomeDelta` — the result envelope beyond
   software patches, with `ImplementationResultPayload` retained as the
   software profile.
4. Reusable `OutcomeRoom` package plus hosted room-system instantiation — each
   durable room compiles through genesis into the flagship reference DAS with a
   declared admission mode and cryptographic `RoomAdmittedObjectBase`; every
   shared-state transition is admitted, sequenced, committed, and receipted.
5. Local-agent pairing, participants, offers, frontier, claims, attempts,
   findings, and challenges — `LocalAgentPairingSession` provides an
   authenticated, expiring, single-use pre-admission bootstrap; downstream
   `RoomParticipationRequest`, `RoomParticipantLease`, `ResourceOffer`,
   `CapabilityOffer`, `WorkFrontierItem`, `WorkClaimLease`, `Attempt`,
   `Finding`, and `VerifierChallenge` become admitted, leased, or receipted
   objects under their own contracts.
6. Systems, Work, Governance, Operations, and Provenance projections — Systems
   renders logical identity, constitution, lifecycle, and desired/observed
   topology; Work renders typed goal/room/automation/session/frontier state;
   Governance authorizes protected change; Operations performs admitted
   infrastructure/member operations; Provenance renders evidence. They own none
   of the underlying truth.
7. Local ontology versions, assertions, optional accepted mappings, action contracts, and
   external-fact policy — `OntologyVersion`, `OntologyOverlay`,
   `OntologyCrosswalk`, `SemanticMappingDecision`, `ProvenanceAssertion`,
   `OntologyActionContract`, and `OracleEvidenceProfile` as durable profiles.
8. Institutional learning boundary, provider-exit, and bounded-improvement
   contracts — compile
   organization/project defaults into an admitted sovereign-system profile and
   run/job snapshots; bind source rights, bidirectional route rights, custody,
   evidence eligibility, egress receipts, derivative impact, capability export,
   and the model-swap continuity evaluation contract; then land the immutable
   improvement-governance, Agenda, Campaign, epoch, exposure, cutoff, and claim
   objects needed by the Horizon 1B order-0 proof without making them a new
   runtime, authority plane, or mandatory wrapper for direct proposals.
9. Single-node flagship proofs — Horizon 1A's package-to-genesis,
   constitution-bound OutcomeRoom and room-child admission spine, end to end,
   on one node through the selected bounded software-change profile and the
   undeniable-product proof gate above; then Horizon 1B's immutable
   improvement-governance profile,
   Agenda, Campaign, target base, coordinating GoalRunProfile, frozen order-0
   epoch, finite reservations,
   candidate/evaluator separation, exposure ledger, negative-result retention,
   reproduction, and target-owner UpgradeProposal handoff with no
   Campaign-owned production mutation.
10. Same-system two-node continuity proof — Horizon 2's join, catch-up, root,
   fenced promotion, replay, drain, and unchanged-authority demonstration.
11. Useful same-system distributed-work proof — Horizon 2B's typed
    role-to-membership runtime assignments, allocation leases, shared-state
    watermarks, coordination epochs, partition/rejoin/rebalance, fenced
    reassignment, and duplicate/ambiguous-effect reconciliation; include the
    native embodied graph compiler/admission contract, `micro`/`edge`/`site`
    resolution, physical-stream compatibility, proposal-only action chunks,
    transactional activation, LocalControlSupervisor final-veto behavior,
    space-time reservations, and the staged backend-neutral proof matrix while
    keeping deterministic control and safety local. This step remains unbuilt
    until the named conformance evidence exists.
12. AIIP discovery, exact-root collaboration terms negotiation, standards
    bindings, participation, portable exit, and federated admission —
    `CollaborationTerms`, `OutcomeRoomDiscovery`, typed participation,
    `ParticipantStateBundle`, A2A/MCP binding profiles, and
    `hosted_admission`/`federated_admission` across domain boundaries.
13. Two-sovereign-system conformance proof — Horizon 3's minimum credible
    Internet-of-Intelligence demonstration, including positive participant-
    level value rather than connectivity or traffic alone.
14. Connected/secured network-service proof — conformance, enrollment, registry,
    rights, security-service, bond, dispute, settlement, and exit contracts
    before any sovereign-mainnet or native-asset commitment.

**Completion is not forced into an arbitrary PR number. Closure is the working
proof** — a step is done when its conformance evidence exists, and the next
step starts from that evidence.

## The rule that keeps this honest

**Architecture detail follows durable contracts; launch claims follow working
evidence.** Target-state owner docs may be precise enough to prevent later
implementation from painting out a required boundary. Every file still carries
an honest implementation status, and no horizon label turns a planned object,
route, receipt, privacy posture, commercial right, or network proof into a
shipped claim.

## Related Canon

- [`doc-classes.md`](./doc-classes.md) — the status-axis vocabulary.
- [`implementation-matrix.md`](./implementation-matrix.md) — per-concept durable-form status.
- [`canon-to-code-delta.md`](./canon-to-code-delta.md) — the object-level canon-to-code delta and deferred UX backlog.
- [`../foundations/economic-flywheel-and-pricing-boundaries.md`](../foundations/economic-flywheel-and-pricing-boundaries.md) — what monetizes at each layer.
