# Canon Implementation Matrix

Status: canonical implementation index.
Canonical owner: this file for mapping major architecture concepts to canonical owner docs, current durable forms, object/projection status, code anchors, and conformance hooks.
Supersedes: ad hoc implementation-status tables in plans/specs when concept ownership or durable-form status conflicts.
Superseded by: none.
Last alignment pass: 2026-07-18.
Doctrine status: canonical
Implementation status: mixed (rows carry per-concept durable-form status and code anchors)
Last implementation audit: 2026-07-18 (portable contract, current Rust route-behavior, ownership, and production-gate reconciliation)

## Purpose

This matrix answers the implementer question:

> **What is this concept, where is it canonically owned, and what durable form should I build today?**

Do not use this file to invent competing doctrine. If a row conflicts with its
canonical owner, update the owner doc first and then update this matrix.

Current implementation alignment is owned by this matrix together with the
subject doctrine and the object-level canon delta. The historical sequencing
and model_mount blocker narration formerly embedded here is archived at
[`../_archive/change-ledgers/implementation-matrix-sprint-lane-log.md`](../_archive/change-ledgers/implementation-matrix-sprint-lane-log.md).
The rows below are the durable concept → owner → form index. The
object-level delta for the collaborative-pursuit and federated-ontology leg —
what the canon requires next versus what code holds today, plus the deferred
application-UX backlog that replaced the former surface queue — is owned by
[`canon-to-code-delta.md`](./canon-to-code-delta.md).

**Current-evidence rule (2026-07-18 audit).** The Rust `hypervisor-daemon`
and its mounted Rust route/kernel modules are the sole current runtime evidence
in this index. Paths in a "Code anchor when known" cell are existence- and
source-type checked; README/Markdown and arbitrary documentation are not
current implementation evidence. Critical route-family rows are additionally
checked against independently configured owning source files and concrete
handler, mount, persistence, or planner-call markers. Those bounded checks do
not prove arbitrary semantics for every row. Deleted JavaScript-daemon and
process-bridge paths and retired sequencing belong only to archived provenance,
never to a current-evidence cell here.

**Owner-boundary audit (2026-07-18).** The full convergence remap was reviewed
by subject and authority boundary, not by concept-name matching. Model control
remains Model Router subject matter; capability tokens additionally name
wallet.network authority, and vault control additionally names wallet.network
authority plus cTEE custody. Runtime mutation/control rows name daemon runtime
alongside, rather than instead of, their substantive owners. Thread memory
therefore names daemon control, Portable Memory Vault semantics, and Agentgres
truth/durable mutation; managed sessions name daemon control with client
presentation; workflow edits name daemon mutation with client graph/product
shape; and skill/hook registry control preserves common-object Skill semantics,
daemon executable-hook control, and connector/tool ownership. The reviewed
connector/tool, wallet, L1, improvement, model, Agentgres, and client categories
retain their substantive owners unless a row explicitly records one of these
cross-boundary responsibilities.

Status values:

```text
canonical object
  needs stable identity, lifecycle, object heads, query, replay, or conflict handling

Agentgres operation
  should be admitted as operation-backed truth, but may not need its own object

event
  runtime-observable transition; may become receipt-backed or projected

receipt
  proof of an accountable transition, decision, invocation, or verification

projection
  rebuildable view over canonical operations, receipts, refs, or artifacts

profile
  configuration/contract applied by daemon/runtime/package

artifact ref
  Agentgres-governed reference to payload bytes held elsewhere
```

## Runtime and Harness Concepts

| Concept | Canonical owner | Current durable form | Promote or keep | Code anchor when known | Conformance hook |
| --- | --- | --- | --- | --- | --- |
| `GoalRunProfile` | [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md) | **target canonical; release/admission object not started, with adjacent substrate only:** one immutable content-addressed pursuit specification interpreted by the Goal Kernel. Current `GoalRun` admission does not yet provide the canonical release/admission family or freeze a complete profile-resolution receipt into every admitted run | promote the immutable pursuit-composition owner; every newly admitted `GoalRun`, including ad-hoc work through the generic-adaptive revision, freezes the exact profile revision/hash, admitted overrides, resolved transitive component set, and resolution receipt. A profile compiles pursuit but never executes, holds authority, owns live state, replaces a campaign/domain database, or owns directed-work graph shape | `crates/node/src/bin/hypervisor_daemon_routes/goalrun_routes.rs`; `crates/services/src/agentic/runtime/kernel/runtime_goal_run_admission.rs` | reject mutable profile revisions, profile-owned execution/authority/live state, any new GoalRun without a frozen profile-resolution receipt, package-carried live leases/gateways/run IDs, and `GoalMicroharness` as a new canonical object rather than an explicit compatibility/prohibition mention |
| `WorkflowTemplate` | [`core-clients-surfaces.md`](../components/hypervisor/core-clients-surfaces.md) | **target canonical; release/admission object not started, with adjacent substrate only:** one Workflow-Compositor-owned immutable directed-work graph revision and its step contracts. Existing workflow/template-shaped refs do not provide the canonical object | promote the immutable graph-shape owner separately from GoalRunProfile; AutomationSpec or GoalRunProfile may reference an exact revision, but the template never triggers or owns a run, holds authority/live state, or absorbs adaptive-convergence semantics | existing compositor workflow/template-shaped refs are adjacent precedents only | reject mutable template revisions, template-owned triggers, run history, execution, authority, or live state, and any owner map that assigns WorkflowTemplate to the common-object or harness-profile owner |
| `WorkLifecycleRecord`, `WorkLifecycleProjection`, `CancellationFanoutPlan`, `WorkLifecycleArchiveSegment`, `WorkLifecycleSnapshot` | [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md), [`ioi-cli-daemon-runtime.md`](../components/daemon-runtime/doctrine.md), [`ioi-daemon-runtime-api.md`](../components/daemon-runtime/api.md) | **target canonical; shared mechanism not started:** the contract retains independent phase and authority tables for GoalRun, GoalGroundingLoop, WorkRun, AutomationRun, HarnessInvocation, ContextCell, and external handles while defining exact-head/idempotent transition facts, rebuildable projections, policy-bound cancellation planning, and archive/snapshot continuity. Current owner planes retain their own lifecycle and receipt machinery; no shared WorkLifecycle kernel, durable store, or status route exists on this base | keep every domain owner responsible for intent, grant verification, mutation authority, events, effects, and completion receipts. A future shared mechanism must bind each owner route at its legal mutation PEP, move append authority into Agentgres/cross-process transactions, execute and receipt cancellation actions, and prove archive-only resume before automatic pruning | no shared runtime code anchor on the current base; owner-specific lifecycle implementations are precedents only and do not satisfy this cross-owner contract | [`work-lifecycle.md`](../../conformance/hypervisor-core/work-lifecycle.md) is a target conformance profile; reject table/reference drift, universal phase flattening, stale heads, changed-body replay, child mutation through parent refs, projection-as-truth, lost receipt lineage, or cancellation completion inferred from a parent phase |
| `SkillManifest`, `SkillEntry`, `ActiveSkillSetSnapshot` | [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md) | **target canonical; three-way split not started, with incompatible same-name and adjacent records:** immutable instructional `SkillManifest`, immutable successor-versioned owner-scope installation/admission `SkillEntry` binding revisions with mutable registry lifecycle projections, and exact run/harness-bound `ActiveSkillSetSnapshot` are distinct. Rust currently overloads skill across a marketplace `SkillManifest` listing record, executable `AgentMacro`/`SkillRecord`, mutable procedure-body `SkillEntry`, filesystem `SKILL.md` discovery, and an `ActiveSkillHookManifest` that mixes skills with executable hooks | promote the three-way split; treat the current marketplace `SkillManifest` wire name as a legacy listing/offer shape, keep price, commercial-license offers/terms, and ranking outside the manifest while retaining source-rights/license refs, expose executable macros/scripts only behind separately owned `RuntimeToolContract`, and keep runtime hooks separately owned. A package may carry skill-manifest revision refs, never current/live SkillEntry heads or active snapshots | `crates/types/src/app/agentic/market.rs`; `crates/types/src/app/agentic/execution.rs`; `crates/node/src/bin/hypervisor_daemon_routes/ioi_intelligence_routes.rs`; `crates/services/src/agentic/runtime/kernel/skill_hook_registry.rs`; `crates/services/src/agentic/runtime/kernel/policy/thread_lifecycle.rs` | reject marketplace listing/offer fields in `SkillManifest` while preserving source-rights/license refs; reject executable authority in a skill, mutable procedure bodies masquerading as `SkillEntry`, active snapshots used as reusable definitions, package-carried SkillEntries/snapshots, skill/hook conflation, or connector/tool contracts claiming Skill semantics; require exact manifest revision/hash, selection/exclusion reasons, set hash, compatible tool refs, leases, and receipt on each active snapshot |
| `RuntimeToolContract` | [`connector-and-tool-contracts.md`](../components/connectors-tools/contracts.md) | **machine contract built; runtime partial:** each immutable content-addressed revision declares typed IO, risk/effect class, primitive capabilities, authority scopes, evidence, information-flow limits, destinations, timeout, and concurrency policy. The registered v1 schema, fixtures, and deterministic projections are contract substrate; the governed daemon tool path is a partial runtime precedent | keep tool execution behind daemon admission and the exact revision/hash; enforce authority, data-class, destination, evidence, and receipt requirements. RuntimeToolContract remains separate from Skill definitions, HarnessProfile resolution, WorkflowTemplate graph shape, and GoalRunProfile pursuit composition | `docs/architecture/_meta/schemas/runtime-tool-contract.v1.schema.json`; `crates/types/src/app/runtime_contracts.rs`; generated Rust/TypeScript architecture-contract projections | architecture-contract gates prove schema/fixture/projection parity only; runtime conformance must reject mutable releases, missing or mismatched authority/data/destination declarations, direct execution, and any tool/connector owner claiming Skill semantics |
| `MCPPrimitiveNormalization`, `HypervisorMCPGatewayRequirement`, `HypervisorMCPGatewayProfile` | [`connector-and-tool-contracts.md`](../components/connectors-tools/contracts.md), [`doctrine.md`](../components/connectors-tools/doctrine.md), [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md), [`core-clients-surfaces.md`](../components/hypervisor/core-clients-surfaces.md) | **target canonical; implementation partial and drifted:** MCP is a subordinate transport. Tool descriptors are candidates admitted as `RuntimeToolContract`; resources resolve to policy-bound views/artifact or memory projections under `ContextLease`; prompts are untrusted import inputs; elicitation is typed user input and never authority; Tasks are opaque external invocation handles on `HarnessInvocation`; Apps are sandboxed extension surfaces and never runtime truth. The Rust daemon has a governed tool path, but resources/prompts remain catalog-shaped, elicitation/tasks/apps lack normalization, top-level `/v1/mcp*` routes still exist despite older retirement claims, and stdio/HTTP clients pin different protocol versions/session assumptions | promote semantic capability requirements in immutable packages and resolve them at activation/invocation to separately admitted, revocable live gateway profiles; never put credentials, concrete gateway instances, subject/session/run IDs, Context/Capability/Authority leases, or secrets in a package. Retain native tools/connectors as equal implementations so MCP cannot own IOI identity, state, authority, or lifecycle | `crates/node/src/bin/hypervisor-daemon.rs`; `crates/node/src/bin/hypervisor_daemon_routes/lifecycle_routes.rs`; `crates/services/src/agentic/runtime/service/mcp_live_backend.rs`; `crates/drivers/src/mcp/transport.rs`; `crates/types/src/app/runtime_contracts.rs` | require raw-tool admission before invocation; resource-to-view/lease proof; explicit prompt import/admission; elicitation unable to mint authority; Task handle distinct from GoalRun identity/status; App sandbox/extension admission; package requirement/live-profile separation; protocol-version compatibility fixtures; and retirement or explicit compatibility classification of every top-level `/v1/mcp*` route. This fresh row supersedes conflicting present-tense status in the older `RuntimeMcpControl` migration-history row |
| `HypervisorSubstrateMode` | [`core-clients-surfaces.md`](../components/hypervisor/core-clients-surfaces.md), [`hypervisoros.md`](../components/daemon-runtime/hypervisoros.md), [`providers-and-environments.md`](../components/hypervisor/providers-and-environments.md) | three deployment/control postures of one Hypervisor control plane: `type1_hypervisoros` for bare-metal/appliance/cluster substrate; `type2_desktop_workstation` for hosted local VMs, sandboxes, models, tools, agents, connectors, and environments on a normal OS; `type3_autonomy_plane` for sessions, WorkRuns, workers, model routes, tools, authority, receipts, replay, outcomes, and promotion | promote as product/API metadata and roadmap axis; not three products and not a license to make Hypervisor only a VM manager | planned Hypervisor substrate-mode projections; HypervisorOS node APIs; Desktop/Workstation environment APIs; Operations/Environments/Developer Workspace/Provenance views (`Workbench` remains a current-code alias) | Type 1 and Type 2 substrate controls must remain visible enough to satisfy infrastructure users; Type 3 must remain the differentiator; conformance should reject product surfaces that expose only agent orchestration while hiding hosts, local environments, console, checkpoints, networks, storage, devices, tasks, policy, and ledger controls |
| `HypervisorProductSurfaceTaxonomyV2` | [`core-clients-surfaces.md`](../components/hypervisor/core-clients-surfaces.md), [`0016-hypervisor-systems-work-and-application-taxonomy.md`](../../decisions/0016-hypervisor-systems-work-and-application-taxonomy.md) | **target canonical; unified compiler not started:** Core remains the shared substrate and App/Web/CLI-headless remain clients; the five core workspaces are `Home`, `Systems`, `Projects`, `Applications`, and `Work`, with `+ New` actions and one `Open Application` slot. Automations is an owner application with a shell placement, not a duplicate core-workspace registration. The owner-, substrate-, tool-, and extension-application taxonomy is canonical, but the current catalog and surface registries are compatibility-era implementation precedents rather than the typed, policy-filtered compiler and durable registration family described here | promote through a typed policy-filtered compiler; retain Sessions, Missions, Marketplace, and Workbench only as implementation evidence or compatibility aliases. External CLI agents remain adapters, and specialized primitives remain inside Applications/Open Application | current `apps/hypervisor/scripts/app-catalog.mjs` and `apps/hypervisor/scripts/surface-registry.mjs` are adjacent catalog evidence only. Canonical release/installation/System-interface/serving records, dynamic extension inventory, request-scoped policy filtering, Systems/Work durable read models and routes, and route/identity migration remain planned | reject flattening core workspaces, owner applications, substrate applications, tools, and extensions into one kind; reject certification or capture evidence as membership authority; reject old current-code names as target doctrine; preserve one-click New Session and singular Open Application |
| `HypervisorSystemsProjection` | [`core-clients-surfaces.md`](../components/hypervisor/core-clients-surfaces.md), [`governed-autonomous-systems.md`](../foundations/governed-autonomous-systems.md) | target read model and core workspace over admitted `system_id`, constitution/genesis, desired/observed deployment and membership, authority, work, evidence, improvement, and interfaces; it owns none of those truths and direct sessions/projects/goals need not create a System | promote only after bounded-System contracts and honest inventory/detail read models exist | **not started**; nearest substrate is bounded-System envelope canon plus daemon membership/deployment records | reject Systems minting identity, treating a process/node/package as a System, or entering permanent navigation as an empty readiness claim |
| `HypervisorWorkSubjectProjection`, `HypervisorWorkFacetProjection`, `HypervisorWorkQueue` | [`core-clients-surfaces.md`](../components/hypervisor/core-clients-surfaces.md), [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md) | target policy-filtered core workspace/read model across GoalRuns, OutcomeRooms, AutomationRuns, Sessions, WorkQueues, WorkItems, WorkRuns, reviews, incidents, and history; every row has one typed canonical work subject and route. Reviews, incidents, RuntimeAssignments, authority, cost, and evidence are cross-owner facets/pointers, never additional universal subject kinds or truth objects | promote as read-only aggregation after typed associations and privacy filters exist; it is never a universal work object or lifecycle | **canonical contract not started; current compatibility UI only:** current master serves `/__ioi/missions` and its surface registration still declares `owner=Missions`; that is compatibility evidence, not a visible Work reframing. It lacks typed Work / Rooms projections for OutcomeRooms, participants, frontier/claims, attempts, findings, and challenges, plus canonical request-scoped policy filtering, queue/review/history breadth, association and alias objects, and a durable projection contract | reject untyped rows, copied authority or universal status, a review/incident wrapper becoming truth, aggregation before policy filtering, GoalRun continuity inferred solely through a Session, or current Missions labels promoted to target owner canon |
| `HypervisorCoreWorkspaceRegistration`, `HypervisorApplicationSurfaceRegistration`, `HypervisorRouteAliasRegistration`, `HypervisorSurfaceReleaseRecord`, `HypervisorSurfaceInstallationBinding`, `HypervisorSystemInterfaceBinding`, `HypervisorSurfaceServingBinding`, `HypervisorProductSurfaceProjection` | [`core-clients-surfaces.md`](../components/hypervisor/core-clients-surfaces.md), [`canonical-enums.md`](../foundations/canonical-enums.md) | target normalized registration family: core workspaces are separate from `owner_application`, `substrate_application`, `tool_surface`, and `extension_application`; one stable surface definition joins zero or more release, installation, System-interface, and serving bindings while typed route aliases carry one static target or fail-closed resolver. Publisher origin, creation method, distribution channel, availability, admission state, installation state, package disposition, enablement state, capability depth, operational state, contexts, routes/aliases, authority, and evidence obligations remain independent and live on their owning record. One compiler combines static registrations, normalized daemon-admitted release/installation/System-interface/serving records, org/user policy and preferences, and typed launch context | promote in shadow mode before navigation/catalog cutover; keep `OntologySurfaceDescriptor` distinct because semantic UI description alone does not grant product membership or launchability | **partial static shadow compiler:** first-party core/application registrations and certified tool/view placements compile into the v2 product API and launcher with evidence-independent membership; Embodied remains a planned nonlaunchable `owner_application`; extension origin/creation/distribution axes are separated and its inventory is explicitly unconnected. Typed route-alias records, immutable releases, installation/System-interface/serving bindings, dynamic extension inventory, request-scoped policy/preferences/context joins, and tenant/System cache-isolation proof remain not started | reject UX lineage/parity granting membership, one definition absorbing per-install/per-System state, drafts launching as installed apps, tenant/System cache leakage, alias collisions, orphan routes, or a tool without one primary owner |
| `HypervisorPackagesSurface` | [`core-clients-surfaces.md`](../components/hypervisor/core-clients-surfaces.md) | target owner application for local admission, installed inventory, releases, dependencies, recall, and impact; Marketplace is an optional discovery/publishing/exchange mode and compatibility route, never package or installation truth | promote as the mandatory lifecycle surface in private, disconnected, and networked deployments | **not started/rename planned**; current Marketplace surface and package records are partial substrate | reject package lifecycle depending on commerce, recall silently mutating/stopping Systems, or marketplace listing status granting installation/authority |
| `HypervisorLegacyWorkSubjectAlias` | [`core-clients-surfaces.md`](../components/hypervisor/core-clients-surfaces.md) | migration-only mapping from a legacy generic Mission ref to exactly one GoalRun or OutcomeRoom subject, with `resolved`, `needs_review`, or `rejected` decision and evidence/receipt refs; `/missions` and `/sessions` resolve into Work while preserving typed context | keep only for deterministic migration and saved-link compatibility; stop new `HypervisorMission` writes before remapping | **not started**; legacy Mission-shaped UI/projection remains current evidence | reject silent inference, a new Mission lifecycle/authority/budget, alias context loss, or deletion of historical receipt/evidence lineage |
| `AgentOperatingPlane`, `AgentRecord`, `AgentExecution`, `TurnControlInput`, `ModelConfiguration`, `ReasoningEffort`, `ServiceTier` | [`default-harness-profile.md`](../components/daemon-runtime/default-harness-profile.md), [`ioi-daemon-runtime-api.md`](../components/daemon-runtime/api.md), [`model-router/api-byok-mounting.md`](../components/model-router/api-byok-mounting.md), [`vocabulary.md`](./vocabulary.md) | daemon-owned agent/session/turn/work-run control plane: configured product agents compile to worker/package refs, mode, model configuration, reasoning, speed, harness, tools/connectors, memory, authority, budget, eval, receipt, and runtime compatibility; agent sessions may atomically bind environment recipe, selected agent, initial input, and execution posture; agent executions expose typed conversation blocks, turn controls, waiting interests, subagents, runner reconciliation, LLM usage, and exec/security telemetry | promote as the stable agent-platform implementation contract; `Agent` is user-facing and buildable, `Worker` remains the protocol actor, `Model` is product-facing, and `ModelRoute` remains runtime/custody truth | planned daemon Agent Operating Plane APIs, WorkQueue/WorkItem/WorkRun APIs, Thread/Turn APIs, model-router APIs, Hypervisor composer and Agents/Workers surface | conformance should reject hidden client-local agent loops, untyped generic event bags as the durable agent contract, service-local job truth, raw secret/model/provider routing outside wallet and model-router policy, and agent executions without WorkRun/Session/Thread/Agentgres/receipt bindings |
| `HypervisorAutomationSpec` | [`core-clients-surfaces.md`](../components/hypervisor/core-clients-surfaces.md) | durable reusable standing behavior for manual, scheduled, webhook/event-triggered, monitor, approval-flow, queue-worker, service/API, or marketplace-service-recipe activation; stores product version, trigger/review/output contracts, graph/compositor refs, harness hints, authority refs, Agentgres refs, receipt policy, and state root. System/Project scope and foreground/background supervision are orthogonal | promote as Automations API object; it is not a GoalRun, chat transcript, editor document, execution runtime, or generic Mission | planned Hypervisor Automations APIs over Workflow Compositor and daemon runtime APIs | must bind daemon execution, wallet authority when required, Agentgres refs/receipts, and compositor contracts; System binding must not change object kind |
| `HypervisorAutomationRun` | [`core-clients-surfaces.md`](../components/hypervisor/core-clients-surfaces.md), [`events-receipts-delivery-bundles.md`](../components/daemon-runtime/events-receipts-delivery-bundles.md) | one activation freezing the exact WorkflowTemplate, AutomationSpec, and AutomationInstallationBinding revision/hash tuple, with explicit optional System, GoalRun, OutcomeRoom, WorkItem, WorkRun, and Session associations; zero GoalRuns and zero Sessions are valid when the activation completes directly | promote as run-history/replay object for Automations; execution truth remains daemon/Agentgres-owned; generic `mission_ref` is retired | planned automation run projection API | consequential success requires the exact three-owner resolution receipt plus receipts/Agentgres operation refs; associations must remain typed rather than inferred through a Mission wrapper |
| `HypervisorCanvasView` | [`core-clients-surfaces.md`](../components/hypervisor/core-clients-surfaces.md) | visual layout/projection for automation, workflow, Foundry job, or project graph; may carry layout artifact refs and projection refs | keep as UI/editor object; never promote to execution truth | planned Canvas/editor projections inside Hypervisor App/Web | conformance should reject Canvas-owned runtime authority, receipts, or Agentgres truth |
| `IoiAiGoalChatHandoff` | [`ioi-ai-control-plane.md`](../domains/ioi-ai/control-plane.md), [`core-clients-surfaces.md`](../components/hypervisor/core-clients-surfaces.md) | proposal/handoff from ioi.ai Goal Chat into Hypervisor run, AutomationSpec draft, Foundry job draft, marketplace publish flow, or restore/provider action; carries goal summary, constraints, authorized account context, target domain, draft refs, and review requirements | promote as coordination object only; not durable automation truth and not execution owner | planned ioi.ai to Hypervisor handoff API | must require explicit user intent before creating durable automations/services and must route execution through daemon/Core contracts |
| `HypervisorFoundry`, `FoundryProject`, `FoundryJobRequest`, `FoundryScorecard` | [`foundry.md`](../components/hypervisor/foundry.md), [`core-clients-surfaces.md`](../components/hypervisor/core-clients-surfaces.md) | platform-style build surface for model catalog, registry, model routes/mounts, tuning, training, evaluation, executable eval worlds, interactive worlds, gameplay trajectory datasets, scenario curricula, world-model candidates, spatial-temporal policy candidates, transfer gates, tool-call audits, trajectory scorecards, certification-run candidates, datasets, feature views, experiments, pipelines, endpoints, batch inference, metadata, monitoring, simulation training, robotics worlds, worker/package promotion, and ontology-aware package building. For embodied systems it owns a backend-neutral candidate/evidence loop across scenario and graph candidates, simulation, SIL, HIL, shadow, limited-live observation, scorecards, and promotion-bundle proposals; simulator, middleware, hardware-rig, and runtime backends remain replaceable bindings | promote as the canonical surface/object family for building and improving reusable autonomous capability; not direct runtime mutation, live graph truth, evaluator authority, actuator authority, or ioi.ai coordination | planned Foundry APIs and App/Web projections; envelope anchors in `common-objects-and-envelopes.md`, worker-training, physical-safety, embodied-runtime, and domain-ontology docs | Foundry jobs must route consequential execution through daemon/Core, bind Agentgres refs/receipts, respect wallet authority, and gate promotion through executable eval, transfer-gate, quality, policy, replay, and certification-support checks; gameplay, simulator, robotics-training, SIL/HIL, shadow, and limited-live artifacts are evidence inputs and do not grant runtime, promotion, or physical actuator authority; conformance must run the same canonical graph/scenario contracts against at least two replaceable backend bindings before claiming backend neutrality |
| `EmbodiedRuntimeGraphManifest`, `NativeEmbodiedRuntimeProfile`, `EmbodiedRuntimeExecutionStratum`, `PhysicalStreamContract`, `EmbodimentAdapter`, `EmbodiedActionPolicyContract`, `EmbodiedActionChunk`, `EmbodiedRuntimeDomain`, `RobotFleetRecord`, `EmbodiedUnitIdentity`, `RobotControllerBinding`, `LocalControlSupervisor`, `LocalControlBridge`, `EmbodiedGraphActivationTransaction`, `FleetMissionCoordinationRecord`, `FleetMissionAllocationLease`, `SpacetimeReservationLease`, `SensorRegistryEntry`, `ActuatorRegistryEntry`, `EmbodiedResourceGroup`, `WorldModel`, `PhysicalCommandQueue`, `PhysicalTelemetryStream`, `PhysicalReplayBundle`, `SimToRealPromotionGate`, `EmbodiedDeploymentAssuranceCase`, `OperatorHandoff`, `RecoveryPlan`, `FleetPolicy` | [`embodied-runtime.md`](../components/daemon-runtime/embodied-runtime.md), [`physical-action-safety.md`](../foundations/physical-action-safety.md), [`ecosystem-assurance-certification-liability.md`](../foundations/ecosystem-assurance-certification-liability.md) | **target canonical; native compiler/executor/API family not started:** immutable runtime graphs compile to composable `micro`, `edge`, and `site` footprints; local execution separates autonomy, deterministic motion, and independent runtime-assurance/safety strata beneath the slower mission/governance plane; typed streams own identity/authentication/integrity/anti-replay plus time/QoS/criticality semantics independently of transport; action-policy contracts narrow semantic action classes and action chunks remain proposals; `LocalControlSupervisor` is the native deterministic execution/final-veto root while `LocalControlBridge` is compatibility only; activation is local prepare/validate/commit-or-abort and separate from arming; same-system fleet/swarm coordination uses epoch-fenced allocation and space-time leases plus bounded partition/rejoin/effect reconciliation; deployment assurance binds exact graph/hardware/software/ODD/evidence without claiming generic certification | promote the native contract and conformance shape while keeping Physical Action Safety as owner of safety/authority invariants, Foundry as owner of backend-neutral simulation/SIL/HIL/shadow/limited-live candidate and evidence production, events/receipts as receipt-schema owner, ecosystem assurance as certification-claim owner, and AIIP only at independently governed system boundaries. `EmbodimentAdapter` and `LocalControlBridge` are adoption surfaces, not the product center or alternate authorities | **planned:** no native graph compiler, mixed-criticality executor, `micro`/`edge`/`site` runtime, physical-stream admission, action-chunk API, LocalControlSupervisor, transactional activation, space-time lease service, deployment-assurance binding API, or Embodied Systems route is built. Existing runtime nodes, telemetry concepts, ROS/controller bridges, physical-safety doctrine, and Foundry robotics/simulation objects are adjacent ingredients only | proof matrix must cover exact graph compile/admission and restart-unarmed behavior; stream identity/authentication/integrity/anti-replay plus compatibility, deadline/freshness/clock/liveliness/backpressure failure; bounded scheduling/WCET and fault containment; supervisor deny/clip/switch/recovery/e-stop independent of AI/network; one active actuator writer and safely fenced standby takeover; resource-group leaf freezing; allocation/reservation conflict, partition, rejoin, reassignment, and ambiguous-effect reconciliation; adapter semantic non-equivalence; backend-neutral simulation-to-live lineage; and deployment-bound assurance evidence across simulation, SIL, HIL, shadow, limited-live, and applicable independent assessment. Reject group/unit-as-authority, global physical atomicity, internal fleet work as AIIP, required HypervisorOS, bridge-as-certification, action chunk as command, simulation as authority, or cloud/model/wallet/chain-dependent emergency stop |
| `EcosystemAssuranceProfile`, `ConformanceProfile`, `CertificationClaim`, `JurisdictionPolicyPack`, `AssuranceEvidenceBundle`, `AssurancePostureProjection`, `EcosystemAbuseSignal`, `QuarantineAdvisory`, `LiabilityClaimRoute`, `CommercialAssuranceExport` | [`ecosystem-assurance-certification-liability.md`](../foundations/ecosystem-assurance-certification-liability.md) | cross-domain assurance object family for certifiable trust posture: profiles declare expectations, policy packs express jurisdiction/compliance/retention/regulated-action rules, evidence bundles bind receipts/refs/scorecards, projections show current posture, advisories route quarantine, claim routes bind liability evidence, and commercial exports package billing/SLA/audit evidence | promote profiles, policy packs, claim routes, advisories, and exports as stable contracts; keep operational truth with the owning domains and use Agentgres refs/receipts for evidence validity | planned Assurance / Certification / Audit application surface, Hypervisor Govern/Operate views, aiagent worker listing posture, sas service-order posture, wallet authority/risk screens, Agentgres assurance evidence APIs, IOI L1 public certification/advisory/dispute anchors where needed | conformance should reject certification badges granting authority, assurance exports as source of truth, compliance packs as legal advice, quarantine as an unbounded global kill switch, insurance hooks as coverage decisions, private audit data on L1, or assurance bypassing daemon/wallet/Agentgres/marketplace/service owner boundaries |
| Runtime-node attestation assurance and deployment/regulatory evidence projection over `JurisdictionPolicyPack`, `AssuranceEvidenceBundle`, and `AssurancePostureProjection` | [`runtime-nodes-tee-depin.md`](../components/daemon-runtime/runtime-nodes-tee-depin.md), [`hypervisoros.md`](../components/daemon-runtime/hypervisoros.md), [`ecosystem-assurance-certification-liability.md`](../foundations/ecosystem-assurance-certification-liability.md) | **target canonical; structured evaluators not started:** the contract separates RATS Attester, Verifier/Appraiser, and Relying Party roles; binds nonce, workload/build, endorsement/reference values, appraisal windows, lease/revocation, and re-attestation; and keeps deployment incident, reporting, retention, erasure, and legal-conformity projection duties explicit. The existing startup profile validator has only a coarse `security_attestation` gate and is implementation precedent, not the shared evaluator or obligations projection | keep future evaluators as existing-object projection seams, not a second attestation/evidence/compliance registry. Evidence-kind minimums remain exact, fallback never inherits a stronger hardware claim, and crypto-shredding never silently becomes verified erasure | `crates/services/src/agentic/runtime/kernel/profile.rs` and `crates/node/src/bin/ioi-agent.rs` are adjacent startup-gate precedent; the current binary explicitly configures remote attestation as not required. No structured assurance evaluator, deployment-obligations evaluator, hardware quote driver, nonce store, endorsement/revocation service, reporting client, or erasure executor is present | [`attestation-assurance.md`](../../conformance/hypervisor-core/attestation-assurance.md) is a target profile; reject role collapse, replay, wrong workload/build, stale appraisal, evidence-kind substitution, stronger fallback labels, receiptless reports, crypto-shred overclaim, or generated legal-conformity claims |
| `GoalSpaceSubscription`, `WorkCredit`, `NetworkGoalBudget`, `GoalExecutionPolicy`, `ContributorScope`, `ManagedExecutionMode` | [`economic-flywheel-and-pricing-boundaries.md`](../foundations/economic-flywheel-and-pricing-boundaries.md), [`collaborative-outcome-pattern.md`](../domains/ioi-ai/collaborative-outcome-pattern.md), [`identity-access-and-metering.md`](../components/hypervisor/identity-access-and-metering.md), [`canonical-enums.md`](../foundations/canonical-enums.md) | one Goal Space product entitlement with persistent conductor, memory, policy, receipts, replay, collaboration, and a bounded grant of non-transferable Work Credits; Network/Open work uses a separate funded budget, bounty, procurement cap, or service order; Auto/Pinned/Compare, Standard/Private, contributor scope, and placement are orthogonal controls | promote entitlement, budgets, rate-card refs, usage/charge records, and controls as explicit product/domain objects; do not pool or resell provider seats, disguise provider spend, call credits a token, or subsidize unlimited worker burn | public subscription/entitlement, purchase/top-up, and invoice-grade supplier reconciliation are planned; the fixed-point internal managed-work ledger is partially built, while existing flat OCU metering remains only local non-billable telemetry | conformance should reject named-human seat sharing, hidden markups, double-charging customer-borne BYOK costs, Network/Open spend from ordinary seat allowance, missing quote/cap/adjustment/refund evidence, and UI that collapses privacy, contributor scope, placement, or routing policy into one tier |
| `CollaborationTerms`, `OutcomeRoom`, `RoomAdmittedObjectBase`, `CollaborativeWorkGraph`, `OutcomeRoomDiscovery`, `RoomParticipationRequest`, `ParticipantStateBundle`, `RoomParticipantLease`, `ResourceOffer`, `CapabilityOffer`, `WorkFrontierItem`, `WorkClaimLease`, `Attempt`, `Finding`, `VerifierChallenge`, `Contribution` | [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md), [`collaborative-outcome-pattern.md`](../domains/ioi-ai/collaborative-outcome-pattern.md), [`economic-flywheel-and-pricing-boundaries.md`](../foundations/economic-flywheel-and-pricing-boundaries.md), [`domain-ontologies-and-data-recipes.md`](../foundations/domain-ontologies-and-data-recipes.md) | reusable OutcomeRoom package whose each durable room genesis creates one bounded-DAS instance; cross-party activity binds one exact accepted terms root; every consequential child binds exact participant lease or room-system issuer, expected room revision/predecessor commitment, decision, monotonic sequence, admission proof, and resulting transition/state/receipt roots | promote as Agentgres-backed objects/operations with package/genesis/system/profile identity, exact-root terms negotiation, participant-rationality decisions, non-retroactive amendments, TTL/heartbeat, typed CAS admission, visibility, privacy, attribution, claim release, portable exit, and dispute semantics; terms acceptance, participant admission, work award, contribution, acceptance, and payout stay distinct | **partial and merged for hosted admission:** production daemon planes implement the hosted OutcomeRoom aggregate and backlinks, authenticated participation/participant leases, frontier/claims, resource/capability offers and receipted eligibility matching, Attempt/Finding, generic WorkResult/OutcomeDelta, and VerifierChallenge with durable intents, receipts, replay, and aggregate reservations. CollaborationTerms negotiation, canonical package/genesis, discovery, portable participant-state export, Contribution, acceptance/verdict, settlement, execution authority, and federated/AIIP admission remain unavailable or not started | owner-plane Rust tests and isolated real-wallet verifiers cover the merged hosted graph; conformance must still reject presumed cooperation, raw actors bypassing participant leases, attribution-as-allocation, stale predecessors, mutable history, fabricated independence, acceptance inferred from receipts, or federated writes into hosted truth |
| `LocalAgentPairingSessionEnvelope` | [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md), [`aiagent/worker-endpoints.md`](../domains/aiagent/worker-endpoints.md), [`identity-access-and-metering.md`](../components/hypervisor/identity-access-and-metering.md) | short-lived challenge/key/origin-bound bootstrap session for `room_guest`, `private_worker`, or `organization_worker`; its resolver discriminator binds one exact HarnessProfile or AgentHarnessAdapter revision/hash and keeps an adapter's optional semantic HarnessProfile exact and separate; it may submit only the target-specific composition/participation proposal and grants no room membership, context, tool, budget, reputation, payout, or effect authority | promote through planned create/inspect/claim/complete/cancel/revoke pairing routes and a separate least-privilege post-admission gateway; never reuse the bootstrap as a durable bearer token | **not started**; screenshot-like product flow, pairing routes, gateway issuance, and conformance are planned | reject replay, undiscriminated profile/adapter refs, broad org tokens, raw room/database access, implicit marketplace publication, pairing-as-competence, prompt-only execution/effects, or any gateway issued before typed room/registry admission |
| `GoalRun`, `GoalGroundingLoop`, `RoleTopology`, `ContextCell`, `ContextLease`, `ContextHandoff`, `HarnessInvocation`, `HarnessAdapterEvent` | [`default-harness-profile.md`](../components/daemon-runtime/default-harness-profile.md), [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md) | bounded loop-native pursuit below an optional OutcomeRoom: orient, plan, implement, verify, course-correct, continue or close through typed context and authority leases; the daemon already has a narrow software-oriented two-implementer GoalRun create/start/reconcile path with receipts, current-code Workbench/Timeline/Provenance projections, and fail-closed harness/model admission | keep GoalRun generic, durable across zero or many Sessions, and bounded; promote durable role/cell/lease/handoff/invocation state only where replay, continuation, collaboration, conflict, or settlement requires it; target product projections are Work/Goals, Developer Workspace, and Provenance | partial implementation in `crates/node/src/bin/hypervisor_daemon_routes/goalrun_routes.rs`, `crates/services/src/agentic/runtime/kernel/runtime_goal_run_admission.rs`, and `apps/hypervisor/scripts/verify-hypervisor-goalrun-multi-harness.mjs`; the OutcomeRoom owner now binds room membership and the WorkResult owner supplies generic result profiles. Direct WorkRun binding, wider topology, and target Work projections remain planned | focused GoalRun verifier plus architecture conformance; reject raw prompt text as durable handoff, client-local orchestration truth, unbounded context, fake parallelism, Session identity as GoalRun identity, and successful reconciliation without verifier/receipt/state-root evidence |
| `WorkResult`, `OutcomeDelta`, `ImplementationResultPayload`, `IoiAiAttemptSummary` | [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md), [`default-harness-profile.md`](../components/daemon-runtime/default-harness-profile.md), [`collaborative-outcome-pattern.md`](../domains/ioi-ai/collaborative-outcome-pattern.md) | generic cross-domain result and proposed outcome-change seam; executable results freeze the exact producer-component snapshot/hash/admission receipt and exact resolver revision/hash; software implementation is one profile carrying changed files, patches, tests, artifacts, blockers, and receipts, while research, ontology, review, service, incident, and physical work use their own result profiles | promote WorkResult and OutcomeDelta as typed objects/operations when identity, verification, acceptance, replay, dispute, or settlement requires them; retain ImplementationResultPayload and ioi.ai attempt summaries as profiles/projections | **partial and merged:** `crates/node/src/bin/hypervisor_daemon_routes/work_result_routes.rs` admits the canonical result profiles and same-room OutcomeDelta records with exact room/result backlinks; Attempt/Finding and VerifierChallenge consume or extend those owner seams. Verification and acceptance transitions remain unavailable | merged WorkResult/OutcomeDelta and challenge-backlink tests; reject HarnessInvocation as a second work subject, mutable family heads as replay provenance, cross-result deltas, or any receipt treated as automatic correctness or acceptance |
| `GoalAppropriateSearchPolicy`, `MultiModelExecution`, `MultiWorkerExecution`, `MultiNodeExecution`, `MultiPartyCollaborationEnvelope` | [`collaborative-outcome-pattern.md`](../domains/ioi-ai/collaborative-outcome-pattern.md), [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md), [`economic-flywheel-and-pricing-boundaries.md`](../foundations/economic-flywheel-and-pricing-boundaries.md) | policy chooses direct, single-path, multi-model, multi-worker, multi-node, or truly multi-party pursuit only when expected participant-level surplus exceeds latency, cost, privacy, disclosure, semantic, authority, verification, counterparty, dispute, and settlement burdens; plurality dimensions remain explicit because multiple models or nodes do not manufacture independent parties | keep as planning/admission policy plus exact-root terms and typed collaboration envelope when principals are independent; do not force every goal through a swarm, race, message board, leaderboard, market, or federation | planner/envelope expansion planned; current direct-versus-Compare launcher and narrow GoalRun path implement only part of the policy | conformance should prove direct collapse for simple work, accepted terms and bounded claims for external work, positive governed participation decisions, affiliation disclosure, independent-party admission, and no claims of decentralization or network value based only on model, worker, node, message, or receipt count |
| `ModelRouteRightsContract`, model supply portfolio, `Auto` / `Pinned` / `Compare` | [`model-router/doctrine.md`](../components/model-router/doctrine.md), [`model-router/api-byok-mounting.md`](../components/model-router/api-byok-mounting.md), [`economic-flywheel-and-pricing-boundaries.md`](../foundations/economic-flywheel-and-pricing-boundaries.md) | versioned commercial, automation, credential-principal, downstream/OEM, region, endpoint/version, parameter, fallback, price, rights-basis, provider-use-of-customer-material, retention/ZDR, and customer-use-of-output contract over direct APIs, dedicated endpoints, customer BYOK/BYOA, replaceable aggregators, and open/self-hosted weights | promote as a mandatory model-route admission profile and receipt input; missing rights fail closed, aggregator routes remain replaceable, provider fallback is semantic substitution, provider service-delivery permission never implies secondary use, and customer receipt of output never implies retention/training/distillation rights | model registry, route selection, local/Ollama, provider records, wallet/cTEE metadata, and Rust-owned model-mount admission are partial; sealed BYOK/OAuth materialization, hosted transports, full rights enforcement, and invoice-grade routing remain incomplete | model-mount conformance plus commercial-rights fixtures should reject named-user subscription pooling, undisclosed provider fallback, incomplete directional-rights matrices, Compare/synthesis rights laundering, raw API resale without authorization, provider-trust routes labeled Private, and Foundry training from unlicensed outputs |
| `OntologyVersion`, `OntologyOverlay`, `OntologyCrosswalk`, `SemanticMappingDecision`, `ProvenanceAssertion`, `OntologyActionContract`, `DataRecipe`, `ConnectorMapping`, `TransformationRun` | [`domain-ontologies-and-data-recipes.md`](../foundations/domain-ontologies-and-data-recipes.md), [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md), [`aiip.md`](../foundations/aiip.md) | local-first, optionally federated semantic world plane: each domain owns locally canonical ontology versions and assertions; cross-domain work negotiates explicit versioned crosswalks, accepted mapping risk, and receipted, challengeable mapping decisions; DataRecipe and immutable ConnectorMapping revisions commit exact semantic-component snapshots, and TransformationRun reuses the recipe's exact tuple; consequential semantic actions compile to contracts that bind state transition, capability, policy, authority, effects, retry/reconciliation, compensation, evidence, receipts, and safety | promote version, assertion, mapping, action-contract, recipe, transformation, and challenge state as Agentgres-backed domain objects/operations; keep projections rebuildable and preserve both valid time and transaction time | canonical schemas are specified; ODK, Domain App, exact semantic-component snapshot, connector-mapping, and AIIP semantic-profile implementations remain planned | conformance should reject a global universal ontology/database, silent movement to current ontology/mapping/object-model/schema/view heads, mutable ConnectorMapping revisions, silent lossy mappings, federation without accepted terms and mapping risk, operational admission presented as universal truth, ontology names granting capability, or action execution outside policy/authority/daemon gates |
| `AssuranceState`, `Receipt`, `EvidenceBundle`, `VerifiedWorkGraph` | [`events-receipts-delivery-bundles.md`](../components/daemon-runtime/events-receipts-delivery-bundles.md), [`marketplace-neutrality.md`](../domains/marketplace-neutrality.md), [`economic-flywheel-and-pricing-boundaries.md`](../foundations/economic-flywheel-and-pricing-boundaries.md), [`canonical-enums.md`](../foundations/canonical-enums.md) | staged assurance across `attested`, `evidenced`, `verified`, `accepted`, `adjudicated`, and `settled`; receipts record attributable transitions, evidence bundles support claims, and the Verified Work Graph is the cross-domain provenance/economic memory over those stages | keep stages explicit in storage, UI, routing, reputation, reward, dispute, and settlement; a receipt is evidence input, not automatic truth, correctness, verification, acceptance, or payout | receipt substrate is built in several runtime lanes; cross-domain assurance-state enforcement and complete Verified Work Graph projections remain planned | conformance should reject labels such as verified or settled without their owner-domain gate and should preserve negative, inconclusive, invalid, exploit, disputed, superseded, and no-fault information |
| `EffectRecoveryClass`, `AmbiguousEffect`, `ReconciliationRequired` | [`events-receipts-delivery-bundles.md`](../components/daemon-runtime/events-receipts-delivery-bundles.md), [`aiip.md`](../foundations/aiip.md), [`physical-action-safety.md`](../foundations/physical-action-safety.md) | effect-aware recovery classifies work as replayable, checkpointable, compensatable, reconciliation-required, or non-retryable; timeouts after possible external effect become ambiguous until observed/reconciled and are never silently retried | promote recovery class into action, task, AIIP, connector, service, and physical-action profiles; bind idempotency keys, provider operation refs, observation/reconciliation plans, compensation, and receipts | partially expressed in receipts and domain contracts; uniform runtime admission/recovery enforcement is planned | adversarial tests should cover timeout-after-effect, duplicate delivery, stale observation, provider disagreement, compensation failure, and physical-action uncertainty; environment restore must never be reported as outcome reconciliation |
| `PlatformPlane`, `PlatformOperationClass`, `PlaneState`, `PlatformOperabilityDecision`, platform recovery/version/key/projection reference mechanisms | [`platform-operability.md`](../components/daemon-runtime/platform-operability.md) | **target canonical; evaluator and recovery mechanisms not started:** the contract maps owner-produced plane observations and an exact operation class to `available`, `degraded`, or `fail_closed`, retaining reason codes, surviving evidence/recovery obligations, assurance narrowing, cache age/head, replay continuity, version negotiation, key-epoch rotation, and privacy-safe observability requirements. The checked fault matrix is a conformance artifact, not proof of a live evaluator | keep as the cross-plane decision/SRE owner without creating a health, truth, authority, proof, billing, settlement, incident, privacy, or persistence database. Future implementation must consume owner-rooted observations at the effect boundary and integrate with real restore, rotation, mixed-version, reconciliation, capacity, and telemetry paths | no runtime evaluator, recovery kernel, daemon route, authoritative plane-observation service, key distributor, mixed-version orchestrator, or fault injector exists on the current base | [`platform-operability.md`](../../conformance/hypervisor-core/platform-operability.md) and [`platform-fault-matrix.v1.json`](../../conformance/hypervisor-core/platform-fault-matrix.v1.json) define the target bar; reject missing/stale/split-brain required planes, stronger fallback assurance, unknown-effect success, expired cache, recovery gaps/tamper, or protected-content leakage |
| `RateCard`, `ManagedWorkPlan`, `WorkQuote`, `CreditHold`, `UsageRecord`, `OverrunDecision`, `FinalDebit`, `BillingAdjustment`, `ManagedWorkBillingLedgerBundle` | [`economic-flywheel-and-pricing-boundaries.md`](../foundations/economic-flywheel-and-pricing-boundaries.md), [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md), [`identity-access-and-metering.md`](../components/hypervisor/identity-access-and-metering.md) | **machine contract built; ledger implementation not started:** the registered v1 schema, invariants, positive/adversarial fixtures, and generated Rust/TypeScript projections define fixed-point RateCard/Plan/quote/hold/usage/overrun/debit/downward-adjustment bindings. They do not implement append authority, replay, evidence resolution, billing decisions, or money movement | keep Work Credits as non-transferable product budget units and keep money, supplier statements, coarse OCU telemetry, payment, payout, and public settlement with their owners. Future runtime work must add Agentgres transactions, owner-evidence resolution, entitlements, receipts, supplier reconciliation, and payment/refund/payout integration | `docs/architecture/_meta/schemas/managed-work-billing-ledger-bundle.v1.schema.json`; generated `crates/types/src/app/generated/architecture_contracts.rs` and `packages/hypervisor-workbench/src/runtime/generated/architecture-contracts.ts`; no billing kernel or public route exists | [`managed-work-billing.md`](../../conformance/hypervisor-core/managed-work-billing.md) is a target profile; architecture-contract gates cover schema/fixture/projection parity only and do not certify a live ledger |
| `DisputeValueUnitBinding`, `DisputeRailProfileEnvelope`, `DisputeEnvelope`, `DisputeResolutionEnvelope`, `DisputeRailBundle` | [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md), [`economic-flywheel-and-pricing-boundaries.md`](../foundations/economic-flywheel-and-pricing-boundaries.md), [`marketplace-neutrality.md`](../domains/marketplace-neutrality.md), [`aiip.md`](../foundations/aiip.md) | **machine contract built; dispute runtime not started:** the registered v1 schema, invariants, fixtures, and generated Rust/TypeScript projections bind profile/case/resolution shape, one fixed-point asset/unit, windows, declared defaults/remedies, bonds, allocation, and required receipt kinds. They do not implement case heads, replay, evidence adjudication, authority, escrow, remedy execution, or finality | keep the rail kinds separate and perform no implicit conversion. Future runtime work must add Agentgres case/resolution heads, evidence and adjudicator-authority resolution, escrow/bond/remedy execution, receipts, appeal supersession, and explicitly enrolled public settlement | `docs/architecture/_meta/schemas/dispute-rail-bundle.v1.schema.json`; generated Rust/TypeScript architecture-contract projections; no dispute kernel or live route exists | [`dispute-rails.md`](../../conformance/hypervisor-core/dispute-rails.md) is a target profile; architecture-contract gates cover schema/fixture/projection parity only and do not certify adjudication or settlement |
| AIIP collaborative-pursuit profile | [`aiip.md`](../foundations/aiip.md), [`collaborative-outcome-pattern.md`](../domains/ioi-ai/collaborative-outcome-pattern.md), [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md) | signed, sequenced, policy-bound exchange of room invitations, participant/resource/capability offers, claim leases, work handoffs/results, findings, verifier challenges, mapping decisions, assurance state, disputes, and settlement refs while each domain retains local truth | promote as an AIIP profile over the same OutcomeRoom semantics, not a second room graph or global database; hosted and federated admission must converge on equivalent typed contracts | AIIP envelope/channel doctrine exists; collaborative-pursuit transport/router/profile implementation remains planned | conformance should reject raw remote database access, unsigned chat as durable handoff, credential/authority transfer, global sequence assumptions, or federated messages directly mutating another domain's truth |
| `IoiAiGoalDraft`, `IoiAiGoalProjection`, `IoiAiOutcomePlanProjection`, `IoiAiAttemptSummary`, `IoiAiCrossSessionOutcomeGraph` | [`collaborative-outcome-pattern.md`](../domains/ioi-ai/collaborative-outcome-pattern.md), [`ioi-ai-control-plane.md`](../domains/ioi-ai/control-plane.md) | ioi.ai Goal Space draft and read projections over canonical GoalRun, OrchestrationPlan, and optional OutcomeRoom truth, plus attempt/result/evidence, connector, and authority navigation | pre-admission draft uses `intent://`; admitted `goal://` lifecycle remains GoalRun-owned; plan projection binds exact OrchestrationPlan revision/hash and decision receipt; do not create a second lifecycle/selection owner, swarm runtime, authority owner, marketplace, global graph, or generic Mission wrapper | planned ioi.ai Goal Space APIs and Hypervisor handoffs; current Hypervisor Sessions, legacy Missions UI, GoalRun, and the Provenance proof stream provide partial substrate; target Hypervisor projection is Work/Rooms | consequential work must route through policy/authority providers and daemon admission; simple goals should collapse to direct execution; multi-party goals must bind room membership, contribution lineage, assurance, and separate Network/Open budget |
| `HypervisorSessionLaunchRecipe`, `HypervisorSessionLaunchRecipeAdmission`, `HypervisorHarnessSelectionOption`, `HypervisorSessionModelConfiguration`, `HarnessSessionBinding`, `HarnessSessionBindingAdmission`, `HarnessSessionLaunch`, `HarnessAdapterReceipt` | [`core-clients-surfaces.md`](../components/hypervisor/core-clients-surfaces.md), [`default-harness-profile.md`](../components/daemon-runtime/default-harness-profile.md) | **planner-only admissions plus an independent host-spawn lane; canonical chain partial/unmounted:** the recipe and binding endpoints call pure planners that validate/canonicalize caller-provided recipe, binding, authority, receipt, operation, and state-root refs, then return records without daemon persistence. No typed `HarnessSessionLaunch` producer exists. `handle_session_execute` separately loads a session, applies its wallet gate, and calls `run_host_spawn_lane`; it does not consume the recipe/binding admission outputs as one launch chain | do not claim end-to-end Recipe -> Binding -> Launch -> Spawn -> Readiness -> TerminalAttach consumption. Persist and consume each exact predecessor, add a typed Launch producer, then author durable Spawn/Readiness records before terminal attach | `crates/node/src/bin/hypervisor_daemon_routes/lifecycle_routes.rs`; `crates/services/src/agentic/runtime/kernel/runtime_hypervisor_session_launch_recipe_admission.rs`; `crates/services/src/agentic/runtime/kernel/runtime_harness_session_binding_admission.rs` | data-driven route/body checks bind the pure planner handlers, caller-provided proof refs, independent session load/wallet/spawn path, absent typed Launch producer, and absent end-to-end chain consumption |
| `HarnessSessionSpawn`, `HarnessSessionReadiness`, `HarnessSessionTerminalAttach`, `HarnessTerminalTranscriptProjection` | [`core-clients-surfaces.md`](../components/hypervisor/core-clients-surfaces.md), [`providers-and-environments.md`](../components/hypervisor/providers-and-environments.md) | **consumer-only/partial:** the mounted terminal-attachment planner accepts caller-supplied `session_spawn` and `session_readiness`, validates their mutual fields, and projects a client PTY attachment. Current Rust does not author or durably admit those input records, and the independent host-spawn lane's terminal events are not canonical Spawn/Readiness production | retain the validator as a fail-closed consumer, not proof of the owner sequence. Implement daemon-owned typed producers and durable predecessor consumption before allowing terminal-attach success to certify launch-chain truth | `crates/node/src/bin/hypervisor_daemon_routes/lifecycle_routes.rs`; `crates/services/src/agentic/runtime/kernel/runtime_harness_session_terminal_attach_admission.rs` | direct source assertions prove request-field consumption, no durable input admission, and no end-to-end Recipe -> Binding -> Launch -> Spawn -> Readiness -> TerminalAttach consumption |
| `HarnessContainerLanePlan`, `HarnessContainerLaneReceipt` | [`core-clients-surfaces.md`](../components/hypervisor/core-clients-surfaces.md), [`providers-and-environments.md`](../components/hypervisor/providers-and-environments.md) | **not implemented:** the session execution route may probe a requested `container` lane, but no canonical container-lane plan or receipt type is authored, persisted, or admitted | implement image/runtime/network/workspace/custody/authority bindings and a matching receipt before treating container availability or host spawn as a container-lane contract | no current code anchor; the target shape remains in the owner docs | source-backed status checks must reject `current Rust implementation` wording for these absent producers |
| `HypervisorOperationalProfiles`, `HypervisorEnvironmentClass`, `HypervisorEnvironmentOpsProfile`, `HypervisorEnvironmentLifecycleState`, `HypervisorEnvironmentActivitySignal`, `HypervisorSessionAccessLease`, `HypervisorEnvironmentService`, `HypervisorEnvironmentTask`, `HypervisorEnvironmentPort`, `HypervisorScmAuthRequirement` | [`core-clients-surfaces.md`](../components/hypervisor/core-clients-surfaces.md), [`ioi-daemon-runtime-api.md`](../components/daemon-runtime/api.md), [`providers-and-environments.md`](../components/hypervisor/providers-and-environments.md) | `HypervisorProject`, typed GoalRun/AutomationRun/OutcomeRoom/Session/WorkItem/WorkRun associations, `AdapterConnectionProfile`, development-environment recipe/resolution/class/ops/lifecycle/observation/stop/snapshot/backup/activity objects, access leases/tokens, services, tasks, ports, SCM requirements, and browser/support policies for implementable work, adapter, environment, access, log, port, browser, support, SCM, and restore boundaries | profile/object set; promote when development setup, lifecycle, authority, replay, sharing, privacy, provider posture, SCM auth, port exposure, snapshot, backup, archive, restore, or cleanup needs identity; `SessionAccessToken` remains derived material, and generic `HypervisorMission` is not a target object | planned session/environment-ops API package; typed work associations and legacy Mission migration are not started | every target resolves through an adapter connection profile or admitted recipe; background/headless work remains visible without inventing Mission truth; environment changes produce Agentgres refs/receipts when they affect authority, privacy, replay, cost, or restore; snapshots/backups are restore material, archives are policy-bound restore chains, and encrypted blobs are not restore truth |
| `WorkflowCompositor` | [`core-clients-surfaces.md`](../components/hypervisor/core-clients-surfaces.md) | graph projection over Hypervisor Core, typed step contracts, replay/receipt/authority projections | high-level workflow/service shape; not execution truth | `packages/hypervisor-workbench/src/runtime/workflow-node-registry.ts`, workflow/compositor surfaces | compositor conformance; must not bypass daemon gates |
| `HarnessProfile` | [`default-harness-profile.md`](../components/daemon-runtime/default-harness-profile.md) | profile metadata, selected step-resolution path, harness activation/projection records, receipts | daemon-executed or daemon-mediated step-resolution adapter; not a peer runtime | `crates/types/src/app/harness/core.rs`, `crates/types/src/app/harness/receipts.rs` | HP-1 through HP-10 |
| `DefaultHarnessProfile` | [`default-harness-profile.md`](../components/daemon-runtime/default-harness-profile.md) | reference profile metadata, scaffold/fallback activation records, receipts | reference scaffold/fallback HarnessProfile; not the only admissible harness and not a meta-harness | `crates/types/src/app/harness/core.rs`, `crates/types/src/app/harness/receipts.rs` | HP-1 through HP-10 |
| `PersistentWorkspaceIntelligence` | [`agentgres/doctrine.md`](../components/agentgres/doctrine.md), [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md), [`default-harness-profile.md`](../components/daemon-runtime/default-harness-profile.md) | workspace/project/domain skills, Agent Wiki / `ioi-memory`, memory profiles, memory archives, memory projections, ContextMutation operations, receipts, provenance refs | persistent intelligence plane; survives model/adapter swaps when compatibility, policy, provenance, and authority allow; adapter-local memory is cache | memory runtime/API, Agentgres memory/admission records, projection APIs, archive/restore APIs | memory admission invariant; HP-10 |
| `ImprovementProposalPlane` | [`governed-autonomous-systems.md`](../foundations/governed-autonomous-systems.md), [`worker-training-lifecycle.md`](../foundations/worker-training-lifecycle.md) | proposal records for skill/memory/tool/workflow/profile/routing/verifier/context patches plus eval receipts | proposal-mediated improvement; not direct self-mutation and not Foundry by default | planned improvement proposal APIs; Foundry jobs when training/packaging needed | no direct self-modification; approval/eval/Agentgres gates |
| `ImprovementGovernanceProfile`, `ImprovementAgenda`, `ImprovementCampaign`, `EvaluationEpoch`, `EvaluationExposureLedger`, `ImprovementOrderCutoffReceipt`, `ImprovementEvidenceClaim` | [`bounded-recursive-improvement.md`](../foundations/bounded-recursive-improvement.md), [`improvement.md`](../components/hypervisor/improvement.md), [`evaluations.md`](../components/hypervisor/evaluations.md), [`improvement-governance-gates.md`](../components/daemon-runtime/improvement-governance-gates.md) | **target canonical; Campaign family not started:** the envelopes define bounded, proposal-only, evaluator-separated improvement work with immutable governance/profile roots, finite budgets, append-only exposure, negative-result retention, learning eligibility, and target-owner proposal handoff. The current daemon has a narrow direct proposal/simulation path and record-shaped approval/release checks; that is adjacent one-shot implementation precedent, not a Campaign, independent evaluator, trusted authority proof, anti-decomposition policy, or production activation mechanism | promote as the optional durable Campaign spine for adaptive or multi-epoch work; retain a bounded direct path for one-shot proposals. Search, Judgment, and Authority remain separated logical duties rather than new services or planes, and only the target owner may admit a successor into production | `crates/node/src/bin/hypervisor_daemon_routes/ioi_intelligence_routes.rs` and `governance_routes.rs` are adjacent direct-proposal and record-gate precedents. Governance-profile/agenda/Campaign/epoch/exposure/cutoff/claim contracts and lifecycle remain not started | the first implementation must prove one target-order-0 campaign with immutable roots, finite budgets, candidate/evaluator separation, append-only exposure, negative-result retention, target-owner handoff, exact authority, and no campaign-owned production activation |
| Owner-qualified Recipe migration (`WorkspaceBootstrapRecipe` retired) | [`providers-and-environments.md`](../components/hypervisor/providers-and-environments.md), [`core-clients-surfaces.md`](../components/hypervisor/core-clients-surfaces.md), [`default-harness-profile.md`](../components/daemon-runtime/default-harness-profile.md) | **partial:** `HypervisorSessionLaunchRecipe` and admission are built; development-environment setup contracts are planned/partial; historical bootstrap and generic recipe fields remain compatibility debt | semantic transformation is `DataRecipe`; environment construction is `HypervisorDevelopmentEnvironmentRecipe`; session composition is `HypervisorSessionLaunchRecipe`; directed work is `WorkflowTemplate` plus optional `AutomationSpec`; adaptive pursuit is `GoalRunProfile`. No generic RecipeEnvelope or executable `run-recipe:` object | session-launch admission modules and environment APIs; migrate legacy `recipe_ref`, `dev-recipe://`, and bootstrap fields at boundary adapters | reject new ownerless recipes; exact immutable revision/hash, daemon gates, wallet authority, Agentgres receipts, and source-snapshot-derived-export lineage where an output is materialized |
| `StepModuleInvocation` | [`ioi-cli-daemon-runtime.md`](../components/daemon-runtime/doctrine.md) | **current Rust implementation:** the Step/Module invocation, result, and routing boundary is a Rust kernel/workload contract; deleted process-bridge and JavaScript-daemon narration is migration history, not present authority | keep the typed Rust invocation/result/router boundary; remaining workload, authority, receipt, and replay depth belongs to the substantive canon owners and the non-doctrinal migration status matrix | `crates/services/src/agentic/runtime/kernel/step_module.rs`; `crates/services/src/agentic/runtime/kernel/step_router.rs`; `crates/services/src/agentic/runtime/kernel/coding_tool_step_module.rs` | `hypervisor-conformance:abi`, `hypervisor-conformance:bridge` |
| `StepModuleResult` | [`ioi-cli-daemon-runtime.md`](../components/daemon-runtime/doctrine.md) | **current Rust implementation:** the Step/Module invocation, result, and routing boundary is a Rust kernel/workload contract; deleted process-bridge and JavaScript-daemon narration is migration history, not present authority | keep the typed Rust invocation/result/router boundary; remaining workload, authority, receipt, and replay depth belongs to the substantive canon owners and the non-doctrinal migration status matrix | `crates/services/src/agentic/runtime/kernel/step_module.rs`; `crates/services/src/agentic/runtime/kernel/step_router.rs`; `crates/services/src/agentic/runtime/kernel/coding_tool_step_module.rs` | `hypervisor-conformance:abi`, `hypervisor-conformance:receipts` |
| `StepModuleRouter` | [`ioi-cli-daemon-runtime.md`](../components/daemon-runtime/doctrine.md) | **current Rust implementation:** the Step/Module invocation, result, and routing boundary is a Rust kernel/workload contract; deleted process-bridge and JavaScript-daemon narration is migration history, not present authority | keep the typed Rust invocation/result/router boundary; remaining workload, authority, receipt, and replay depth belongs to the substantive canon owners and the non-doctrinal migration status matrix | `crates/services/src/agentic/runtime/kernel/step_module.rs`; `crates/services/src/agentic/runtime/kernel/step_router.rs`; `crates/services/src/agentic/runtime/kernel/coding_tool_step_module.rs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:ctee`, `hypervisor-conformance:negative` |
| `HypervisorKernelSubstrateMigration` | Canonical owner: none; [`hypervisor-kernel-substrate-migration-matrix.md`](./hypervisor-kernel-substrate-migration-matrix.md) and [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md) are archived terminal records | **archived terminal provenance; non-actionable:** the records preserve completed retirements and historical conformance context but own no current status, doctrine, or sequence | route current implementation through this implementation matrix, [`doctrine.md`](../components/daemon-runtime/doctrine.md), and [`canon-to-code-delta.md`](./canon-to-code-delta.md); archived records may not direct work | `scripts/conformance/hypervisor-conformance.mjs`, `package.json` | `hypervisor-conformance` |
| `HypervisorOS` | [`hypervisoros.md`](../components/daemon-runtime/hypervisoros.md) | bare-metal node profile, boot profile, node measurement receipts, daemon-rooted workload policy | profile; not a peer runtime and not a privacy substitute for cTEE | planned | daemon root rule; no unmanaged workload bypass; boot measurement receipt present |
| `HypervisorOSNode` | [`hypervisoros.md`](../components/daemon-runtime/hypervisoros.md) | runtime node object/projection plus boot profile refs, daemon refs, cTEE policy refs, and receipt requirements | promote when node is persistent, billable, restorable, or settlement/dispute eligible | planned | Hypervisor Daemon is node root; forbidden bypasses enforced |
| `HypervisorOSBootProfile` | [`hypervisoros.md`](../components/daemon-runtime/hypervisoros.md) | profile manifest binding image/kernel/initrd/daemon/package/driver hashes and secure-boot/TPM posture | profile; object if managed provider/update/rollback lifecycle needs identity | planned | signed updates and rollback policy checked |
| `HypervisorOSBootReceipt` / `NodeMeasurementReceipt` | [`hypervisoros.md`](../components/daemon-runtime/hypervisoros.md), [`events-receipts-delivery-bundles.md`](../components/daemon-runtime/events-receipts-delivery-bundles.md) | receipt binding boot epoch, image hash, daemon hash, driver/package manifest hashes, measurement method, and privacy claim | receipt; never enough to claim consumer-GPU plaintext privacy without cTEE/TEE | planned | integrity receipt present; privacy claim cannot exceed measurement profile |
| `NodeEnforcementProfile` | [`hypervisoros.md`](../components/daemon-runtime/hypervisoros.md) | daemon gates, sandbox/seccomp/eBPF/LSM hooks where available, executable policy, egress policy, datawall/leakage detection, log/export redaction, cTEE checks, optional hardware attestation hooks, and receipts such as `ExecutableDeniedReceipt`, `EgressDetectionReceipt`, and `DataLeakageIncidentReceipt` | profile/evidence layer; not a privacy substitute for cTEE and not proof that consumer GPUs are confidential | planned HypervisorOS enforcement API | unmanaged executable launch, unscoped egress, datawall detections, and daemon bypass attempts are blocked/detected and receipted |
| `HypervisorProvidersAndEnvironments` | [`providers-and-environments.md`](../components/hypervisor/providers-and-environments.md) | Environments Applications catalog entry, Open Application, session, project, provider, org/admin, and operator-console views for infrastructure posture, direct provider integrations, managed environments, access leases, services, tasks, ports, logs, SCM auth, zero-to-idle, archive, and restore; `Providers / Environments` is the architectural family alias | core Hypervisor capability; not a separate peer product, runtime, authority, truth, or storage layer | planned Environments APIs | Hypervisor manages sessions/environments/providers; daemon executes; required authority providers authorize; Agentgres records truth |
| `ByoProviderPlane` | [`byo-provider-plane.md`](../components/hypervisor/byo-provider-plane.md), [`providers-and-environments.md`](../components/hypervisor/providers-and-environments.md) | `ProviderAccount`, `ProviderCredentialBinding`, `RuntimeNode`, `PlacementDecision`, `SnapshotRef`, `RestoreRef`, `SpendReceipt`, `ProviderOperationReceipt`, provider materials, provider receipts, environment-class provider eligibility, user placement choices (`run local`, `use my infrastructure`, `pick a cloud`, `let Hypervisor choose`), `placement_source: connected | managed | optimized`, `selection_mode: local | user_pinned | policy_pinned | auto | failover`, `runtime_class: VM | microVM | container | devcontainer | GPU runtime | browser | model server | HypervisorOS node`, VM lifecycle verbs (create, start, stop, restart, SSH/console, attach disk/volume/GPU, snapshot, archive, restore, expose ports/IP leases, run agent/session/workbench, record cost/state root/receipts, tear down), and the priority adapter ladder: SSH/bare-metal, simple GPU VM, GPU runtime, GPU marketplace, hyperscaler, cluster/customer-cloud, DePIN compute, and storage custody | canonical object plane when customer-owned compute/storage/provider accounts must be governed, snapshotted, restored, costed, and receipted without hidden provider markup; supports visible adapter/orchestration fees when Hypervisor performs provider lifecycle work for BYO or pinned venues | `apps/hypervisor/scripts/verify-hypervisor-byo-provider-plane.mjs`; daemon `provider_routes.rs`; provider account and receipt APIs | provider state is evidence; daemon/Agentgres state is truth. BYO provider spend is customer-borne. Direct self-managed provider spend has no percentage fee; pinned or BYO venues run through Hypervisor may carry visible adapter/orchestration fees; routing fees require separate visible routing value and a challengeable routing receipt |
| `HypervisorChangePlane` | [`providers-and-environments.md`](../components/hypervisor/providers-and-environments.md), [`core-clients-surfaces.md`](../components/hypervisor/core-clients-surfaces.md) | target-vs-observed state, explicit change plans, release channels, maintenance/suppression windows, canary/adjudication, recall, remediation, deployed-where posture, and supply-chain evidence across sessions, environments, services, workers, model routes, connectors, images, datasets, automations, and providers | default Hypervisor capability; surface through Governance release/change facets, Environments, session, project, Application, and Automation views; not a hidden provider control loop, separate deployment app, authority layer, or truth layer | planned change-plane APIs | change plan visible; gates evaluated; daemon/provider execution only; required authority providers authorize consequential changes; Agentgres records lifecycle refs, state roots, and receipts |
| `HypervisorChangePlan` / `ChangePlanGate` | [`providers-and-environments.md`](../components/hypervisor/providers-and-environments.md), [`core-clients-surfaces.md`](../components/hypervisor/core-clients-surfaces.md) | visible change unit with target/observed refs, affected objects, steps, rollback steps, gate refs, blocked reason, authority refs, spend/risk/privacy posture, maintenance/suppression refs, state roots, and receipts | object when environment, release, model-route, connector, secret, provider placement, remediation, restore repair, or migration change needs execution identity | planned change-plan API | no plan executes until required gates pass; raw provider/scanner state cannot authorize or admit the change |
| `ReleaseChannel` / `MaintenanceWindow` / `SuppressionWindow` | [`providers-and-environments.md`](../components/hypervisor/providers-and-environments.md) | policy objects for eligible releases/model routes/workers/connectors/images/automation versions, allowed execution windows, human holds, failed-rollout holds, incident holds, and automated failure-threshold holds | policy/profile; canonical object when promotion, rollback, rollout timing, hold, or emergency override needs audit/replay | planned policy API | channel subscription is not blind auto-upgrade; human suppression is respected until authorized clearance; emergency override is wallet/policy-gated and receipted |
| `CanaryRun` / `AdjudicationReceipt` / `RecallPolicy` / `RecallReceipt` / `RemediationPlan` | [`providers-and-environments.md`](../components/hypervisor/providers-and-environments.md), [`events-receipts-delivery-bundles.md`](../components/daemon-runtime/events-receipts-delivery-bundles.md) | staged rollout cohorts, health/eval/readiness adjudication, active recall ranges, roll-off/rollback/patched replacement plans, and remediation receipts for bad releases/routes/workers/connectors/images/datasets/placements | canary/receipt/policy objects when promotion, rollback, vulnerability repair, incident response, or placement safety depends on them | planned receipt/change-plane APIs | recalled refs are blocked from promotion/placement until admitted clearance or authorized exception; promotion depends on admitted adjudication, not raw logs |
| `DeployedWhereIndex` / `SupplyChainManifestRef` / `SBOMRef` / `VulnerabilityFinding` / `PatchSlaPolicy` | [`providers-and-environments.md`](../components/hypervisor/providers-and-environments.md), [`artifact-ref-plane.md`](../components/agentgres/artifact-ref-plane.md) | evidence-backed map of what version/image/worker/model route/connector/dataset/automation/package is active where, plus supply-chain manifests, SBOM refs, vulnerability findings, suppressions, and patch SLA policy | projection/evidence by default; canonical refs when change gates, recalls, dispute, compliance, customer reporting, or remediation need identity | planned Agentgres/provider/scanner integrations | deployed-where and vulnerability posture derive from admitted refs and receipts; scanner, registry, and provider output remain evidence until admitted |
| `CloudRoute` | [`providers-and-environments.md`](../components/hypervisor/providers-and-environments.md) | Hypervisor object for routing a workload to compute, storage, GPU, network, confidential compute, DePIN, customer cloud, local, or provider infrastructure with authority refs, privacy posture, budget, attestation requirements, and risk labels | projection by default; canonical object when spend, secret release, provider account use, replay, billing, dispute, migration, or private workspace placement needs identity | planned cloud/provider route adapters | cloud route cannot authorize spend, release secrets, or claim privacy without wallet.network, cTEE/TEE/local/customer-cloud posture, Agentgres refs, and receipts |
| `CloudCandidate` | [`providers-and-environments.md`](../components/hypervisor/providers-and-environments.md) | resource route candidate from direct provider connector, local inventory, customer cloud, DePIN market, decentralized storage network, hyperscaler, cloud GPU provider, enterprise cluster, or user route | keep as candidate/proposal until selected into approved `CloudRoute`; never authority by itself | planned provider connector APIs | cloud candidate is not execution, authority, storage truth, privacy proof, or provider-selection approval |
| `CloudResourceIntent` / `CloudResourceCandidate` | [`cloud.md`](../domains/decentralized/cloud.md), [`providers-and-environments.md`](../components/hypervisor/providers-and-environments.md), [`byo-provider-plane.md`](../components/hypervisor/byo-provider-plane.md) | decentralized.cloud candidate objects for infrastructure capacity: resource classes, runtime class, placement source, custody posture, provider quote refs, spend estimates, custody plans, failover plans, provider reliability evidence, region, availability, interruption risk, and candidate evidence | candidate/proposal only; Hypervisor admits a selected `PlacementDecision` or `CloudRoute` before execution | planned decentralized.cloud candidate-source client; planned optimized-placement API | resource candidates cannot provision, spend, release credentials, expose ingress, claim custody, or restore until selected into Hypervisor placement and authorized by wallet.network or the relevant authority provider |
| `ProviderQuote` / `CustodyPlan` / `FailoverPlan` / `SpendEstimate` | [`cloud.md`](../domains/decentralized/cloud.md), [`byo-provider-plane.md`](../components/hypervisor/byo-provider-plane.md), [`economic-flywheel-and-pricing-boundaries.md`](../foundations/economic-flywheel-and-pricing-boundaries.md) | supporting evidence for optimized placement: price/capacity quote, snapshot/archive/cTEE/provider-trust custody plan, re-placement/failover plan, spend estimate, routing-fee eligibility, cost owner, and uncertainty | evidence/projection by default; canonical object when approval, billing, dispute, failover, private custody, restore, or marketplace procurement requires identity | planned optimized-placement receipts | quote, custody, failover, and spend estimates are not authority, provider truth, or restore validity; paid optimized placement requires visible routing/procurement value and challengeable evidence |
| `AkashProviderLane` | [`providers-and-environments.md`](../components/hypervisor/providers-and-environments.md) | direct DePIN compute/GPU provider integration preserving SDL/deployment, provider-bid, lease, GPU attributes, persistent-volume, IP lease, logs/events, shell/exec, stop/close, and redeploy semantics | provider lane; never execution owner, authority owner, storage truth, or privacy proof by itself | planned Akash provider connector | Akash route cannot receive secrets, spend, or private plaintext without wallet.network, cTEE/TEE/local/customer policy, and receipts |
| `FilecoinStorageLane` | [`providers-and-environments.md`](../components/hypervisor/providers-and-environments.md), [`storage-backends/doctrine.md`](../components/storage-backends/doctrine.md), [`agentgres/artifact-ref-plane.md`](../components/agentgres/artifact-ref-plane.md) | decentralized storage-backend lane for content-addressed encrypted archives, payloads, evidence bundles, retrieval, replication, renewal, and repair posture | storage backend/profile; never Agentgres artifact meaning or restore authority | planned Filecoin storage connector | CIDs/deals/retrieval success do not equal restore validity without Agentgres refs, lifecycle state, receipts, and payload verification |
| `HypervisorProviderNode` | [`providers-and-environments.md`](../components/hypervisor/providers-and-environments.md) | node/provider/runtime inventory projection with authority refs, daemon refs, Agentgres domain refs, status, cTEE posture, storage posture, cost/provider metadata, and receipt refs | projection by default; canonical object when provider lifecycle, billing, restore, or policy requires identity | planned | node status links to daemon heartbeat or provider connector and receipt refs |
| `HypervisorWorkloadPrimitive` | [`providers-and-environments.md`](../components/hypervisor/providers-and-environments.md) | VM/container/microVM/WASM/image/volume/network/snapshot/backup/restore/GPU-pool/node-pool/migration/provider-connector lifecycle projection with authority refs, Agentgres operation refs, and receipts | projection by default; promote when lifecycle, billing, restore, migration, authority, or dispute needs identity | planned | infrastructure lifecycle actions still require authority, daemon/provider boundary, Agentgres refs, and receipts |
| `ClassicalInfraPrimitive` | [`providers-and-environments.md`](../components/hypervisor/providers-and-environments.md), [`hypervisoros.md`](../components/daemon-runtime/hypervisoros.md) | traditional infrastructure object such as VM, container, microVM, WASM workload, image, volume, network, snapshot, backup, restore point, node pool, GPU pool, quota, lease, health/log/metric/cost stream, provider connector, or migration plan | keep as vocabulary/category unless an object has lifecycle/admission needs | planned | cannot split classical infra management from autonomous runtime governance when consequential |
| `HypervisorRuntimeAssignmentView` | [`providers-and-environments.md`](../components/hypervisor/providers-and-environments.md) | projection over runtime assignments, placement reasons, authority refs, Agentgres operation refs, receipt refs, workspace/run refs, and status | projection; promote only if assignment lifecycle needs query/conflict handling beyond source objects | planned | placement cannot bypass wallet.network, daemon execution, or Agentgres admission |
| `HypervisorStoragePosture` | [`providers-and-environments.md`](../components/hypervisor/providers-and-environments.md), [`storage-backends/doctrine.md`](../components/storage-backends/doctrine.md), [`agentgres/artifact-ref-plane.md`](../components/agentgres/artifact-ref-plane.md) | storage-backend availability/posture projection over Agentgres artifact refs and backend metadata | projection; storage backend does not own artifact meaning | planned | storage availability cannot be treated as restore validity without Agentgres refs/receipts |
| `WorkspacePersistenceProfile` | [`providers-and-environments.md`](../components/hypervisor/providers-and-environments.md), [`core-clients-surfaces.md`](../components/hypervisor/core-clients-surfaces.md) | ephemeral/session/zero-to-idle/persistent/archive-only workspace profile with compute idle, provider lease closure, archive/checkpoint requirements, allowed storage backends, retrieval checks, restore policy, authority, cTEE, and receipts | profile/object when workspace lifecycle, cost, archive, restore, or privacy posture needs identity | planned session/provider APIs | zero-to-idle shuts down compute but restore remains Agentgres-backed, receipt-backed, and payload-verified |
| `EnvironmentWarmupProfile` | [`providers-and-environments.md`](../components/hypervisor/providers-and-environments.md), [`core-clients-surfaces.md`](../components/hypervisor/core-clients-surfaces.md) | prebuild/dependency-cache/model-cache/index-warmup/image-pull/provider-warm-pool profile with privacy posture, invalidation triggers, authority refs, and receipts | performance projection; not canonical workspace truth | planned Workbench/provider warmup APIs | warmup caches cannot contain protected plaintext unless policy/cTEE/local posture explicitly permits and receipts it |
| `HypervisorPlacementDecision` | [`providers-and-environments.md`](../components/hypervisor/providers-and-environments.md) | placement projection/object for node/provider/workload/workspace/model/worker/service routing with cost, latency, data locality, cTEE posture, model availability, authority, and policy reasons | projection by default; canonical object when approval, replay, dispute, billing, or migration requires identity | planned | placement cannot become execution or authority; approved actions route through daemon/provider boundary and wallet.network |
| `ProviderConnector` | [`providers-and-environments.md`](../components/hypervisor/providers-and-environments.md), [`connector-and-tool-contracts.md`](../components/connectors-tools/contracts.md) | connector profile for cloud, DePIN, storage, Kubernetes, KubeVirt, VMware, Proxmox, Nutanix, HypervisorOS, or customer environments | connector/profile; canonical object when installed, billed, policy-governed, or dispute-relevant | planned | provider API actions are approved daemon/provider-connector actions with receipts |
| `GpuPool` | [`providers-and-environments.md`](../components/hypervisor/providers-and-environments.md) | accelerator-pool projection over nodes/providers, utilization, model routes, leases, cost, authority, and cTEE posture | projection; canonical object when allocation, billing, or settlement needs identity | planned | GPU placement cannot imply cTEE privacy or model-weight custody guarantees without matching profile receipts |
| `PrivateWorkspaceCtee` | [`private-workspace-ctee.md`](../components/daemon-runtime/private-workspace-ctee.md) | private workspace profile, daemon routing policy, encrypted refs/blobs, receipts | workspace/execution profile; not a hardware TEE claim | planned | private workspace conformance |
| `PrivateWorkspaceNode` | [`private-workspace-ctee.md`](../components/daemon-runtime/private-workspace-ctee.md) | runtime assignment + Agentgres refs + wallet policy | object/projection when node is persistent, billable, or restorable | planned | no-plaintext protected classes |
| `PrivateWorkspaceCapsule` | [`private-workspace-ctee.md`](../components/daemon-runtime/private-workspace-ctee.md) | task/workspace capsule envelope plus artifact refs and receipts | promote when replay/dispute/routing needs identity | planned | private workspace capsule checks |
| `PlaintextFreeRuntimeMounting` | [`private-workspace-ctee.md`](../components/daemon-runtime/private-workspace-ctee.md) | daemon mount discipline for tools, shells, filesystems, model servers, and model calls | profile/discipline; specialize as concrete mount/view objects where audit or replay needs identity | planned | no protected plaintext in untrusted runtime mounts |
| `PlaintextFreeModelMount` | [`private-workspace-ctee.md`](../components/daemon-runtime/private-workspace-ctee.md) | model mount profile/view, encrypted refs, redacted projections, private handles, `ModelMountReceipt` | promote mount/view when replay, dispute, leakage accounting, or model-route audit needs identity | planned | no protected plaintext in model context; mount receipt present |
| `CustodyType` | [`private-workspace-ctee.md`](../components/daemon-runtime/private-workspace-ctee.md) | type discipline for whether plaintext may be public, redacted, sealed, guardian-only, crypto-operator-only, capability-only, or never-remote-plaintext | compiler discipline by default; persist manifest hash in proof-bearing runs | planned | every edge into rented node has admissible custody type |
| `CustodyProof` | [`private-workspace-ctee.md`](../components/daemon-runtime/private-workspace-ctee.md) | verifier-facing proof object binding custody derivation, mount graph, lattice commitments, receipts, leakage budget, and state roots | required when acceptance/dispute/restore/settlement relies on no-plaintext-custody claim | planned | proof verifies against policy, receipts, and state roots |
| `PrivateAgencyTransform` | [`private-workspace-ctee.md`](../components/daemon-runtime/private-workspace-ctee.md) | reduction from secret-bearing agent step to public proposal plus private selection/declassification/authorization | compiler strategy by default | planned | protected task is candidate-selection-reducible or routed away from rented node |
| `CandidateCoverageProfile` | [`private-workspace-ctee.md`](../components/daemon-runtime/private-workspace-ctee.md) | scheduler profile for redundancy mass, redundancy phase, coverage target, candidate trace budget, public token budget, schedule, and fallback | promote when CLPD/CLE claims depend on measured coverage | planned | lattice width/depth chosen from coverage frontier, not guesswork |
| `CounterfactualLatticeExecution` | [`private-workspace-ctee.md`](../components/daemon-runtime/private-workspace-ctee.md) | high-assurance CLPD schedule that expands committed public lattices before private selection feedback | strategy when leakage budget requires lower online selection leakage | planned | lattice committed before private selection feedback |
| `CounterfactualLatticeReceipt` | [`private-workspace-ctee.md`](../components/daemon-runtime/private-workspace-ctee.md), [`events-receipts-delivery-bundles.md`](../components/daemon-runtime/events-receipts-delivery-bundles.md) | receipt binding candidate lattice commitment, width/depth/token budget, generation/dedupe/padding rules, node ref, and state root | receipt when counterfactual selection-leakage claims matter | planned | cannot claim zero online branch-selection leakage without receipt |
| `ManagedExecutionMode` | [`private-workspace-ctee.md`](../components/daemon-runtime/private-workspace-ctee.md), [`model-router/doctrine.md`](../components/model-router/doctrine.md), [`economic-flywheel-and-pricing-boundaries.md`](../foundations/economic-flywheel-and-pricing-boundaries.md) | user-facing selector with exactly `Standard` and `Private`; Standard is cTEE/private-native IOI-managed runtime with provider-trust model routes allowed by disclosure, while Private adds no-provider-trust model routing through open-weight or user-controlled routes in local/BYO/customer-boundary/cTEE/TEE custody | UI/request field and projection; not a replacement for execution posture evidence | planned | do not expose redacted/provider-trust/unsafe as extra product modes; Private requires custody proof, model/API boundary, no-provider-trust route, and receipts |
| `ExecutionPrivacyPosture` | [`private-workspace-ctee.md`](../components/daemon-runtime/private-workspace-ctee.md) | posture label for private-native, redacted-API, provider-trust, or unsafe execution paths | required for workers/services/outcomes that may touch private workspace data | planned | private-native not claimed when sensitive plaintext reaches third-party model API |
| `PrivateManagedExecutionPosture` | [`private-workspace-ctee.md`](../components/daemon-runtime/private-workspace-ctee.md), [`economic-flywheel-and-pricing-boundaries.md`](../foundations/economic-flywheel-and-pricing-boundaries.md), [`connectors-tools-and-authority-registry.md`](../components/connectors-tools/doctrine.md) | product/entitlement posture for managed private execution, cTEE/TEE/customer-boundary placement, BYO private node routing, protected connector processing, audit/replay, or persistent background private work | entitlement/projection when a paid plan, Work Credits, enterprise policy, or BYO node gates managed private runtime | planned | private label requires matching execution privacy posture, custody proof, model/API boundary, and receipts; connecting an app is not a connector tax |
| `CryptographicOperatorPlane` | [`private-workspace-ctee.md`](../components/daemon-runtime/private-workspace-ctee.md) | internal cTEE routing plane for FHE/MPC/garbled/ORAM/local/threshold protected subcomputations | discipline by default; concrete policy/receipt when protected operator execution needs audit | planned | protected work not plaintext on node; authority surface is default second party |
| `CryptographicOperatorPolicy` | [`private-workspace-ctee.md`](../components/daemon-runtime/private-workspace-ctee.md) | policy object with allowed operator families, fallback order, second-party refs, leakage budget, and receipt requirements | promote when workspace or service package relies on private operators | planned | operator policy exists before protected operator routing |
| `PrivateOperatorReceipt` | [`private-workspace-ctee.md`](../components/daemon-runtime/private-workspace-ctee.md), [`events-receipts-delivery-bundles.md`](../components/daemon-runtime/events-receipts-delivery-bundles.md) | receipt binding operator family, protected input commitments, second party, output commitment, leakage profile, and no-plaintext claim | receipt by default; proof object when dispute/replay needs it | planned | protected private-operator result is not admitted without receipt |
| `CandidateLatticePrivateDecoding` | [`private-workspace-ctee.md`](../components/daemon-runtime/private-workspace-ctee.md) | default execution strategy, candidate lattice commitment, leakage budget fields, `PrivateInferenceReceipt` | strategy by default; promote candidate lattice only when replay, dispute, or verification needs identity | planned | no protected plaintext on node; candidate commitment and leakage budget present |
| `DeterrenceDetectionProfile` | [`private-workspace-ctee.md`](../components/daemon-runtime/private-workspace-ctee.md) | profile plus `DeterrenceDetectionReceipt`/`CanaryTripReceipt` evidence | profile/receipt; object when marketplace dispute, slashing, or provider attribution needs lifecycle | planned | synthetic canaries only; detection never authorizes plaintext mounting |
| `AutonomyLease` | [`private-workspace-ctee.md`](../components/daemon-runtime/private-workspace-ctee.md), [`wallet-network/doctrine.md`](../components/wallet-network/doctrine.md) | wallet authority lease plus policy receipt | canonical authority object when offline autonomy is allowed | planned | capability-exit checks |
| `AccessPointBinding` / `StepUpChallenge` | [`wallet-network/doctrine.md`](../components/wallet-network/doctrine.md) | wallet binding for SMS/email/chat/voice/webhook access points plus short-lived challenge pointer | canonical authority object for persistent agent access rails; challenge is event/receipt-backed pointer, not a grant | planned | low-assurance channels cannot decrypt, declassify, hold grants, release secrets, or approve high-risk actions without step-up |
| `IntentContract` | [`default-harness-profile.md`](../components/daemon-runtime/default-harness-profile.md) | run request, resolver receipt, operation payload | promote when multiple components query/rebase it | `crates/services/src/agentic/runtime/service/decision_loop/intent_resolver/instruction_contract.rs` | CIRC, HP-1 |
| `RuntimePlan` | [`default-harness-profile.md`](../components/daemon-runtime/default-harness-profile.md) | plan receipt or projection | promote when plan epochs/replay matter | `crates/types/src/app/harness/core.rs` | HP-1, HP-4 |
| `ContextTopology` | [`default-harness-profile.md`](../components/daemon-runtime/default-harness-profile.md) | planner projection plus receipts | promote when repartition/replay/cross-actor routing needs object identity | planned | HP-4 |
| `ContextCell` | [`default-harness-profile.md`](../components/daemon-runtime/default-harness-profile.md) | scoped runtime state, context events, projections | promote when context-cell state outlives one turn or is shared | runtime state and compaction paths | HP-4 |
| `LoopStep` / `LoopIteration` | [`default-harness-profile.md`](../components/daemon-runtime/default-harness-profile.md) | event, receipt, trace segment | promote when loop-level replay/verification is first-class | `crates/services/src/agentic/runtime/service/decision_loop/mod.rs` | HP-2, HP-3 |
| `ModelPass` | [`default-harness-profile.md`](../components/daemon-runtime/default-harness-profile.md), [`events-receipts-delivery-bundles.md`](../components/daemon-runtime/events-receipts-delivery-bundles.md) | model invocation receipt plus redacted trace | keep as receipt/trace unless lineage query needs object | cognition and model-router paths | CEC, HP-6 |
| `RuntimeModelRouteSelection` | [`model-router-byok-run-to-idle.md`](../components/model-router/doctrine.md) | **mounted direct-handler compatibility behavior; canonical planner unmounted:** `handle_routes_create` writes the caller-shaped route record directly and `handle_route_test` builds selection/decision output directly. Neither handler calls `plan_route_control`; the canonical planner is declared-unmounted contract substrate | retain only the source-observed direct route behavior as current. Mount the canonical route-control planner and its admission/receipt semantics before calling the planner-backed contract current | `crates/node/src/bin/hypervisor-daemon.rs`; declared-unmounted contract `crates/services/src/agentic/runtime/kernel/model_mount/route_control.rs` | source-marker checks bind route mounts, `handle_routes_create`, `handle_route_test`, direct `persist_record`, and absence of a planner call |
| `ModelRouteControl` | [`model-router-byok-run-to-idle.md`](../components/model-router/doctrine.md) | **partial direct-handler implementation; canonical planner unmounted:** route creation persists a direct record and route testing directly composes `route_selection` plus `build_route_decision`; `plan_route_control` is not called by either mounted handler | do not infer planner-backed authority, admission, receipt, or replay semantics from the direct compatibility handlers or the existence of `route_control.rs` | `crates/node/src/bin/hypervisor-daemon.rs`; declared-unmounted contract `crates/services/src/agentic/runtime/kernel/model_mount/route_control.rs` | source-marker checks must reject a fabricated planner-backed route-control claim |
| `ModelTokenizerControl` | [`model-router-byok-run-to-idle.md`](../components/model-router/doctrine.md), [`wallet-network-authority-layer.md`](../components/wallet-network/doctrine.md) | **mounted direct Rust compatibility implementation:** `/v1/model-mount/tokens/tokenize`, `/tokens/count`, and `/context/fit` authorize a wallet-rooted bearer and perform deterministic whitespace counting/shaping directly in `hypervisor-daemon.rs`. The mounted handlers do not call `model_mount/tokenizer.rs`; that planner is unbound substrate, not route evidence | keep the limited direct behavior explicit. Bind a real tokenizer adapter and the Rust planner before claiming model-tokenizer parity; do not infer mounted planner semantics from a similarly named module | `crates/node/src/bin/hypervisor-daemon.rs`; unbound substrate `crates/services/src/agentic/runtime/kernel/model_mount/tokenizer.rs` | source-backed checks must prove the three mounts, direct handler behavior, and absence of a planner call; receipt conformance covers only the direct tokenize/context-fit receipts |
| `ModelInvocationControl` | [`model-router-byok-run-to-idle.md`](../components/model-router/doctrine.md) | **partial mounted provider-execution path; canonical invocation authority/admission unmounted:** native-local calls `admit_provider_execution` and `invoke_provider` / `invoke_provider_stream`, but the daemon calls neither `admit_invocation` nor `plan_invocation_authority` | preserve the real provider-execution path without promoting the uncalled invocation-control planners into current authority/admission semantics | `crates/node/src/bin/hypervisor-daemon.rs`; mounted execution `crates/services/src/agentic/runtime/kernel/model_mount/provider_execution.rs`; declared-unmounted contract `crates/services/src/agentic/runtime/kernel/model_mount/invocation_authority.rs` | source-marker checks bind the mounted provider-execution calls and the absent invocation-authority/admission calls |
| `ModelProviderExecution` | [`model-router-byok-run-to-idle.md`](../components/model-router/doctrine.md) | **current Rust implementation:** model-mount routing, control, projection, custody, and lifecycle behavior is owned by the Rust daemon route and model-mount kernel sources listed in this row; no deleted JavaScript package is current evidence | keep the Rust model-control/projection boundary under the listed model, authority, custody, and truth owners; track unfinished materialization or protocol work in the non-doctrinal migration status matrix | `crates/node/src/bin/hypervisor-daemon.rs`; `crates/services/src/agentic/runtime/kernel/model_mount.rs`; `crates/services/src/agentic/runtime/kernel/model_mount/provider_execution.rs`; `crates/services/src/agentic/runtime/kernel/model_mount/provider_execution/admission.rs`; `crates/services/src/agentic/runtime/kernel/model_mount/provider_execution/invocation.rs`; `crates/services/src/agentic/runtime/kernel/model_mount/provider_execution/stream.rs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:receipts` |
| `ModelProviderInvocation` | [`model-router-byok-run-to-idle.md`](../components/model-router/doctrine.md) | **current Rust implementation:** model-mount routing, control, projection, custody, and lifecycle behavior is owned by the Rust daemon route and model-mount kernel sources listed in this row; no deleted JavaScript package is current evidence | keep the Rust model-control/projection boundary under the listed model, authority, custody, and truth owners; track unfinished materialization or protocol work in the non-doctrinal migration status matrix | `crates/node/src/bin/hypervisor-daemon.rs`; `crates/services/src/agentic/runtime/kernel/model_mount.rs`; `crates/services/src/agentic/runtime/kernel/model_mount/provider_execution.rs`; `crates/services/src/agentic/runtime/kernel/model_mount/provider_execution/admission.rs`; `crates/services/src/agentic/runtime/kernel/model_mount/provider_execution/invocation.rs`; `crates/services/src/agentic/runtime/kernel/model_mount/provider_execution/stream.rs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:receipts` |
| `ModelWeightCustodyProfile` | [`model-router/doctrine.md`](../components/model-router/doctrine.md), [`private-workspace-ctee.md`](../components/daemon-runtime/private-workspace-ctee.md), [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md) | model-weight custody disclosure and admission object separating workspace privacy from proprietary-weight custody; lanes include `public_open_weight`, `user_local_private_weight`, `remote_api_private_weight`, `provider_trust_remote_mount`, `tee_or_customer_cloud_mount`, and `forbidden_plaintext_mount` | model-route admission metadata; not a cTEE guarantee by itself and not a provider trust waiver unless wallet/policy accepts it | docs now; model-mount admission policy implementation should bind this profile into route selection and receipts | rented/root-owned GPU with proprietary plaintext weights must be blocked or labeled provider-trust/TEE/customer-cloud before execution |
| `ModelProviderResult` | [`model-router-byok-run-to-idle.md`](../components/model-router/doctrine.md) | **partial execution output; canonical provider-result admission unmounted:** mounted provider invocation returns execution results, but the daemon never calls `admit_provider_result`; `provider_result.rs` is declared-unmounted contract substrate | do not treat a returned provider-execution result or planner file existence as admitted provider-result truth | `crates/node/src/bin/hypervisor-daemon.rs`; mounted execution `crates/services/src/agentic/runtime/kernel/model_mount/provider_execution.rs`; declared-unmounted contract `crates/services/src/agentic/runtime/kernel/model_mount/provider_result.rs` | source-marker checks bind `invoke_provider` and reject a fabricated `admit_provider_result` mount |
| `ModelProviderControl` | [`model-router-byok-run-to-idle.md`](../components/model-router/doctrine.md), [`wallet-network-authority-layer.md`](../components/wallet-network/doctrine.md), [`private-workspace-ctee.md`](../components/daemon-runtime/private-workspace-ctee.md) | **mounted partial Rust implementation:** `/v1/model-mount/providers` authorizes a wallet-rooted bearer and calls `plan_provider_control`; persisted provider records declare wallet authority and cTEE custody boundaries. The route hashes caller-supplied secret refs and does not prove a live cTEE material adapter or outbound secret injection | retain Model Router subject ownership, wallet authority, and cTEE custody as separate boundaries. Do not equate a declared custody field with live cTEE handling | `crates/node/src/bin/hypervisor-daemon.rs`; `crates/services/src/agentic/runtime/kernel/model_mount/provider_control.rs` | source-backed route/planner-call assertions plus negative authority/custody checks; live cTEE material injection remains absent |
| `ModelProviderLifecycle` | [`model-router-byok-run-to-idle.md`](../components/model-router/doctrine.md) | **mounted partial canonical planner:** native-local instance load calls `plan_provider_lifecycle`, persists its record, and carries the returned lifecycle hash into instance materialization. That mounted planner is implemented in `lifecycle/provider.rs` | keep this claim bounded to the native-local load call graph; do not substitute backend lifecycle/process modules for provider lifecycle ownership | `crates/node/src/bin/hypervisor-daemon.rs`; `crates/services/src/agentic/runtime/kernel/model_mount/lifecycle/provider.rs` | source-marker checks bind `instance_real_load` to `plan_provider_lifecycle` and its persisted record |
| `ModelLifecycleReceiptControl` | [`model-router-byok-run-to-idle.md`](../components/model-router/doctrine.md) | **partial direct receipt behavior; canonical accepted-receipt planner unmounted:** mounted handlers hand-author estimate, invocation, and related receipts, but the daemon calls neither `plan_accepted_receipt_head` nor `plan_accepted_receipt_transition` | keep direct receipts honest and do not promote them to accepted-receipt head/transition semantics | `crates/node/src/bin/hypervisor-daemon.rs`; declared-unmounted contract `crates/services/src/agentic/runtime/kernel/model_mount/accepted_receipt.rs` | source-marker checks bind direct receipt persistence and absence of accepted-receipt planner calls |
| `ModelInstanceLifecycleControl` | [`model-router-byok-run-to-idle.md`](../components/model-router/doctrine.md) | **current Rust implementation:** model-mount routing, control, projection, custody, and lifecycle behavior is owned by the Rust daemon route and model-mount kernel sources listed in this row; no deleted JavaScript package is current evidence | keep the Rust model-control/projection boundary under the listed model, authority, custody, and truth owners; track unfinished materialization or protocol work in the non-doctrinal migration status matrix | `crates/node/src/bin/hypervisor-daemon.rs`; `crates/services/src/agentic/runtime/kernel/model_mount.rs` | `hypervisor-conformance:receipts` |
| `ModelProviderInventory` | [`model-router-byok-run-to-idle.md`](../components/model-router/doctrine.md) | **partial hand-authored seed/projection; canonical inventory planner unmounted and incompatible:** `seed_catalog` directly persists the native fixture inventory because the planner hard-codes and rejects the required caller item refs. The daemon never calls `plan_provider_inventory` | retain the fixture inventory only as hand-authored compatibility truth; repair and mount the canonical planner before claiming planner-backed provider inventory | `crates/node/src/bin/hypervisor-daemon.rs`; declared-unmounted incompatible contract `crates/services/src/agentic/runtime/kernel/model_mount/lifecycle/inventory.rs` | source-marker checks bind the hand-authored `model-provider-inventory` persistence and absent planner call |
| `ModelMountProjectionAdapter` | [`model-router-byok-run-to-idle.md`](../components/model-router/doctrine.md) | **current Rust implementation:** model-mount routing, control, projection, custody, and lifecycle behavior is owned by the Rust daemon route and model-mount kernel sources listed in this row; no deleted JavaScript package is current evidence | keep the Rust model-control/projection boundary under the listed model, authority, custody, and truth owners; track unfinished materialization or protocol work in the non-doctrinal migration status matrix | `crates/node/src/bin/hypervisor-daemon.rs`; `crates/services/src/agentic/runtime/kernel/model_mount.rs`; `crates/services/src/agentic/runtime/kernel/model_mount/read_projection.rs`; `crates/services/src/agentic/runtime/kernel/model_mount/read_projection/common.rs`; `crates/services/src/agentic/runtime/kernel/model_mount/read_projection/adapter_boundary.rs`; `crates/services/src/agentic/runtime/kernel/model_mount/read_projection/status.rs` | `hypervisor-conformance:receipts`, `hypervisor-conformance:compositor` |
| `ModelMountPublicCatalogRoutes` | [`model-router-byok-run-to-idle.md`](../components/model-router/doctrine.md) | **current Rust implementation:** model-mount routing, control, projection, custody, and lifecycle behavior is owned by the Rust daemon route and model-mount kernel sources listed in this row; no deleted JavaScript package is current evidence | keep the Rust model-control/projection boundary under the listed model, authority, custody, and truth owners; track unfinished materialization or protocol work in the non-doctrinal migration status matrix | `crates/node/src/bin/hypervisor-daemon.rs`; `crates/services/src/agentic/runtime/kernel/model_mount.rs` | `hypervisor-conformance:compositor` |
| `ModelServerControl` | [`model-router-byok-run-to-idle.md`](../components/model-router/doctrine.md) | **current Rust implementation:** model-mount routing, control, projection, custody, and lifecycle behavior is owned by the Rust daemon route and model-mount kernel sources listed in this row; no deleted JavaScript package is current evidence | keep the Rust model-control/projection boundary under the listed model, authority, custody, and truth owners; track unfinished materialization or protocol work in the non-doctrinal migration status matrix | `crates/node/src/bin/hypervisor-daemon.rs`; `crates/services/src/agentic/runtime/kernel/model_mount.rs`; `crates/services/src/agentic/runtime/kernel/model_mount/server_control.rs` | `hypervisor-conformance:receipts` |
| `ModelRuntimeEngineControl` | [`model-router-byok-run-to-idle.md`](../components/model-router/doctrine.md) | **current Rust implementation:** model-mount routing, control, projection, custody, and lifecycle behavior is owned by the Rust daemon route and model-mount kernel sources listed in this row; no deleted JavaScript package is current evidence | keep the Rust model-control/projection boundary under the listed model, authority, custody, and truth owners; track unfinished materialization or protocol work in the non-doctrinal migration status matrix | `crates/node/src/bin/hypervisor-daemon.rs`; `crates/services/src/agentic/runtime/kernel/model_mount.rs`; `crates/services/src/agentic/runtime/kernel/model_mount/runtime_engine.rs`; `crates/services/src/agentic/runtime/kernel/model_mount/read_projection/runtime.rs` | `hypervisor-conformance:receipts` |
| `ModelRuntimeSurveyProjection` | [`model-router-byok-run-to-idle.md`](../components/model-router/doctrine.md) | **current Rust implementation:** model-mount routing, control, projection, custody, and lifecycle behavior is owned by the Rust daemon route and model-mount kernel sources listed in this row; no deleted JavaScript package is current evidence | keep the Rust model-control/projection boundary under the listed model, authority, custody, and truth owners; track unfinished materialization or protocol work in the non-doctrinal migration status matrix | `crates/node/src/bin/hypervisor-daemon.rs`; `crates/services/src/agentic/runtime/kernel/model_mount.rs`; `crates/services/src/agentic/runtime/kernel/model_mount/runtime_survey.rs` | `hypervisor-conformance:receipts` |
| `ModelArtifactStorageControl` | [`model-router-byok-run-to-idle.md`](../components/model-router/doctrine.md), [`wallet-network-authority-layer.md`](../components/wallet-network/doctrine.md), [`private-workspace-ctee.md`](../components/daemon-runtime/private-workspace-ctee.md) | **mounted mixed Rust implementation:** artifact import and endpoint mount authorize through wallet-rooted capability tokens and call `plan_artifact_endpoint`; dry-run import and artifact deletion are direct daemon handlers. Artifact records declare wallet/cTEE boundaries, but no live cTEE artifact-custody adapter is proven. `storage_control.rs` is not the implementation of these mounted artifact handlers | keep artifact identity/routing with Model Router, authority with wallet.network, and protected custody with cTEE. Distinguish planner-backed import/mount from direct dry-run/delete behavior | `crates/node/src/bin/hypervisor-daemon.rs`; `crates/services/src/agentic/runtime/kernel/model_mount/artifact_endpoint.rs`; `crates/services/src/agentic/runtime/kernel/model_mount/read_projection/topology.rs`; unbound storage substrate `crates/services/src/agentic/runtime/kernel/model_mount/storage_control.rs` | source-backed assertions must bind each mounted handler to its actual call path and keep live cTEE artifact custody unclaimed |
| `ModelCatalogDownloadControl` | [`model-router-byok-run-to-idle.md`](../components/model-router/doctrine.md), [`wallet-network-authority-layer.md`](../components/wallet-network/doctrine.md), [`private-workspace-ctee.md`](../components/daemon-runtime/private-workspace-ctee.md) | **mounted fixture-only direct Rust implementation:** catalog import, download, cancel, cleanup, and artifact delete run directly in `hypervisor-daemon.rs` after wallet-rooted bearer authorization. They write fixture/local bytes and records directly; they do not call `storage_control.rs`, perform a hosted catalog-provider transfer, or prove cTEE custody. Returning `completed` is valid only for this fixture/local behavior | retain the three owner boundaries and record hosted transport/custody as absent. A future live downloader must bind the storage-control planner, outbound wallet authority, cTEE custody/egress, bounded transport evidence, and durable admission | `crates/node/src/bin/hypervisor-daemon.rs`; unbound target substrate `crates/services/src/agentic/runtime/kernel/model_mount/storage_control.rs` | source-backed handler/call assertions must reject planner, hosted-download, or cTEE claims until those calls are mounted |
| `ModelCatalogProviderControl` | [`model-router-byok-run-to-idle.md`](../components/model-router/doctrine.md), [`wallet-network-authority-layer.md`](../components/wallet-network/doctrine.md), [`private-workspace-ctee.md`](../components/daemon-runtime/private-workspace-ctee.md) | **unmounted Rust substrate:** `catalog_provider_control.rs` and catalog-provider read-projection modules declare Model Router, wallet authority, cTEE custody, and Agentgres record boundaries, but `hypervisor-daemon.rs` mounts no `/v1/model-mount/catalog/providers*` route and calls no catalog-provider planner. SDK and Workbench route builders currently target those absent routes; they are not functioning implementation evidence | mount nothing until the daemon owns config/OAuth/material control with wallet authority, cTEE custody, Agentgres admission/replay, and positive protocol conformance. Do not treat planner definitions or client route strings as a live control plane | `crates/services/src/agentic/runtime/kernel/model_mount/catalog_provider_control.rs`; `crates/services/src/agentic/runtime/kernel/model_mount/read_projection/oauth.rs`; route-absence inventory in `crates/node/src/bin/hypervisor-daemon.rs`; stale clients in `packages/agent-sdk/src/substrate-client.ts` and `packages/hypervisor-workbench/src/runtime/workflow-model-mount-control-nodes.ts` | source-backed absence checks must reject any mounted/client-positive claim until server routes and end-to-end conformance exist |

Implementation note (2026-07-18 audit): deleted JavaScript helpers remain
historical migration evidence only. Current status is determined by the mounted
Rust route and its actual call graph. In particular, catalog-provider control
has Rust planner/projection substrate and stale client route builders, but no
mounted daemon route or positive execution path.

| `ModelCapabilityTokenControl` | [`model-router-byok-run-to-idle.md`](../components/model-router/doctrine.md), [`wallet-network-authority-layer.md`](../components/wallet-network/doctrine.md) | **current Rust implementation:** model-mount routing, control, projection, custody, and lifecycle behavior is owned by the Rust daemon route and model-mount kernel sources listed in this row; no deleted JavaScript package is current evidence | keep the Rust model-control/projection boundary under the listed model, authority, custody, and truth owners; track unfinished materialization or protocol work in the non-doctrinal migration status matrix | `crates/node/src/bin/hypervisor-daemon.rs`; `crates/services/src/agentic/runtime/kernel/model_mount.rs`; `crates/services/src/agentic/runtime/kernel/model_mount/capability_token_control.rs`; `crates/services/src/agentic/runtime/kernel/model_mount/read_projection/custody.rs` | `hypervisor-conformance:receipts`, `hypervisor-conformance:negative` |
| `ModelMcpWorkflowControl` | [`connector-and-tool-contracts.md`](../components/connectors-tools/contracts.md) | **unmounted Rust substrate:** model-mount MCP workflow planning, read projection, and live-backend modules exist, but current master has no public model-mount MCP workflow route and the mounted Rust daemon does not call this workflow executor | retain as subordinate connector/model substrate only; do not claim public execution, admission, authority, receipts, or replay until a real daemon caller and positive conformance are mounted | route-absence inventory in `crates/node/src/bin/hypervisor-daemon.rs`; `crates/services/src/agentic/runtime/kernel/model_mount/mcp_workflow.rs`; `crates/services/src/agentic/runtime/kernel/model_mount/read_projection/mcp.rs`; `crates/services/src/agentic/runtime/service/mcp_live_backend.rs` | source-backed route inventory must keep this row at unmounted-substrate status until a real daemon caller is mounted and complete positive execution/admission conformance exists |
| `ModelReceiptGateControl` | [`model-router-byok-run-to-idle.md`](../components/model-router/doctrine.md) | **mounted direct Rust implementation:** `/v1/model-mount/workflows/receipt-gate` reads daemon receipt records, compares one route id, and writes a gate receipt directly. It does not authorize the request, call `receipt_gate.rs`, or prove the planner's required-receipt/Agentgres semantics | retain only the narrow route-binding check as current. Bind authority, the planner, durable receipt truth, and negative conformance before claiming governed receipt-gate control | `crates/node/src/bin/hypervisor-daemon.rs`; unbound substrate `crates/services/src/agentic/runtime/kernel/model_mount/receipt_gate.rs` | source-backed checks must prove direct behavior and reject planner/admission claims |
| `ModelVaultControl` | [`model-router-byok-run-to-idle.md`](../components/model-router/doctrine.md), [`wallet-network-authority-layer.md`](../components/wallet-network/doctrine.md), [`private-workspace-ctee.md`](../components/daemon-runtime/private-workspace-ctee.md) | **mounted direct Rust compatibility implementation:** vault routes authorize wallet-rooted bearer scopes, hash refs/material in memory, persist hash-only metadata, and require rebinding after restart. They do not call `vault_control.rs` and do not use a live cTEE custody adapter; `rust_daemon_core.vault.in_memory_material` is not cTEE | keep Model Router refs, wallet authority, and cTEE custody distinct. Do not promote the in-memory adapter or the unbound planner into a live cTEE vault claim | `crates/node/src/bin/hypervisor-daemon.rs`; unbound substrate `crates/services/src/agentic/runtime/kernel/model_mount/vault_control.rs`; `crates/services/src/agentic/runtime/kernel/model_mount/read_projection/custody.rs` | source-backed checks must preserve wallet authorization, hash-only persistence, restart rebind, no planner call, and no live cTEE claim |
| `ModelBackendLifecycle` | [`model-router-byok-run-to-idle.md`](../components/model-router/doctrine.md) | **current Rust implementation:** model-mount routing, control, projection, custody, and lifecycle behavior is owned by the Rust daemon route and model-mount kernel sources listed in this row; no deleted JavaScript package is current evidence | keep the Rust model-control/projection boundary under the listed model, authority, custody, and truth owners; track unfinished materialization or protocol work in the non-doctrinal migration status matrix | `crates/node/src/bin/hypervisor-daemon.rs`; `crates/services/src/agentic/runtime/kernel/model_mount.rs`; `crates/services/src/agentic/runtime/kernel/model_mount/backend_process.rs`; `crates/services/src/agentic/runtime/kernel/model_mount/backend_lifecycle.rs` | `hypervisor-conformance:receipts` |
| `RuntimeAgentRunCreateControl` | [`ioi-cli-daemon-runtime.md`](../components/daemon-runtime/doctrine.md) | **current Rust implementation:** this runtime control/projection family is owned by the Rust hypervisor daemon and the listed Rust kernel sources; deleted JavaScript-daemon and Step/Module bridge paths are migration history only | keep the Rust-owned control/projection boundary under the listed subject, mutation, authority, custody, and truth owners; the non-doctrinal migration status matrix tracks unfinished sequencing and historical retirement evidence | `crates/node/src/bin/hypervisor-daemon.rs`; `crates/node/src/bin/hypervisor_daemon_routes/lifecycle_routes.rs`; `crates/services/src/agentic/runtime/kernel/policy.rs`; `crates/services/src/agentic/runtime/kernel/policy/thread_lifecycle.rs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:compositor` |
| `RuntimeLifecycleProjectionControl` | [`ioi-cli-daemon-runtime.md`](../components/daemon-runtime/doctrine.md) | **current Rust implementation:** this runtime control/projection family is owned by the Rust hypervisor daemon and the listed Rust kernel sources; deleted JavaScript-daemon and Step/Module bridge paths are migration history only | keep the Rust-owned control/projection boundary under the listed subject, mutation, authority, custody, and truth owners; the non-doctrinal migration status matrix tracks unfinished sequencing and historical retirement evidence | `crates/node/src/bin/hypervisor-daemon.rs`; `crates/node/src/bin/hypervisor_daemon_routes/lifecycle_routes.rs`; `crates/services/src/agentic/runtime/kernel/runtime_lifecycle.rs`; `crates/services/src/agentic/runtime/kernel/mod.rs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:compositor` |
| `RuntimeToolCatalogProductIdeProtocolClients` | [`connector-and-tool-contracts.md`](../components/connectors-tools/contracts.md) | current Workbench workflow capability binding and repair projections are non-authoritative clients of the Rust `/v1/tools` catalog projection; no deleted product Authority Center module is claimed as current evidence | keep the Workbench surface as a protocol client over Rust daemon projection records; it does not own tool semantics, admission, authority, or catalog truth | `packages/hypervisor-workbench/src/runtime/workflow-tool-connector-capability-binding.ts`; `packages/hypervisor-workbench/src/runtime/workflow-run-capability-receipts.ts`; `packages/hypervisor-workbench/src/runtime/workflow-tool-connector-capability-binding.test.ts`; `packages/hypervisor-workbench/src/runtime/workflow-run-history-model.test.ts` | `hypervisor-conformance:compositor`, `hypervisor-conformance:docs` |
| `RuntimeContextPressureAlertProjectionControl` | [`ioi-cli-daemon-runtime.md`](../components/daemon-runtime/doctrine.md) | **current Rust implementation:** this runtime control/projection family is owned by the Rust hypervisor daemon and the listed Rust kernel sources; deleted JavaScript-daemon and Step/Module bridge paths are migration history only | keep the Rust-owned control/projection boundary under the listed subject, mutation, authority, custody, and truth owners; the non-doctrinal migration status matrix tracks unfinished sequencing and historical retirement evidence | `crates/node/src/bin/hypervisor-daemon.rs`; `crates/node/src/bin/hypervisor_daemon_routes/lifecycle_routes.rs` | `hypervisor-conformance:compositor` |
| `RuntimeDiagnosticsFeedbackProjectionControl` | [`ioi-cli-daemon-runtime.md`](../components/daemon-runtime/doctrine.md) | **current Rust implementation:** this runtime control/projection family is owned by the Rust hypervisor daemon and the listed Rust kernel sources; deleted JavaScript-daemon and Step/Module bridge paths are migration history only | keep the Rust-owned control/projection boundary under the listed subject, mutation, authority, custody, and truth owners; the non-doctrinal migration status matrix tracks unfinished sequencing and historical retirement evidence | `crates/node/src/bin/hypervisor-daemon.rs`; `crates/node/src/bin/hypervisor_daemon_routes/lifecycle_routes.rs`; `crates/services/src/agentic/runtime/kernel/coding_tool_event.rs`; `crates/services/src/agentic/runtime/kernel/runtime_diagnostics_repair_policy.rs` | `hypervisor-conformance:compositor` |
| `RuntimeDiagnosticsRepairPolicyProjectionControl` | [`ioi-cli-daemon-runtime.md`](../components/daemon-runtime/doctrine.md) | **current Rust implementation:** this runtime control/projection family is owned by the Rust hypervisor daemon and the listed Rust kernel sources; deleted JavaScript-daemon and Step/Module bridge paths are migration history only | keep the Rust-owned control/projection boundary under the listed subject, mutation, authority, custody, and truth owners; the non-doctrinal migration status matrix tracks unfinished sequencing and historical retirement evidence | `crates/node/src/bin/hypervisor-daemon.rs`; `crates/node/src/bin/hypervisor_daemon_routes/lifecycle_routes.rs`; `crates/services/src/agentic/runtime/kernel/runtime_diagnostics_repair_policy.rs` | `hypervisor-conformance:compositor` |
| `RuntimeThreadEventAdmission` | [`ioi-cli-daemon-runtime.md`](../components/daemon-runtime/doctrine.md) | **current Rust implementation:** this runtime control/projection family is owned by the Rust hypervisor daemon and the listed Rust kernel sources; deleted JavaScript-daemon and Step/Module bridge paths are migration history only | keep the Rust-owned control/projection boundary under the listed subject, mutation, authority, custody, and truth owners; the non-doctrinal migration status matrix tracks unfinished sequencing and historical retirement evidence | `crates/node/src/bin/hypervisor-daemon.rs`; `crates/node/src/bin/hypervisor_daemon_routes/lifecycle_routes.rs`; `crates/services/src/agentic/runtime/kernel/runtime_thread_event.rs`; `crates/services/src/agentic/runtime/kernel/projection.rs`; `crates/services/src/agentic/runtime/kernel/agentgres_admission.rs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:receipts`, `hypervisor-conformance:compositor` |
| `RuntimeThreadTurnProjection` | [`ioi-cli-daemon-runtime.md`](../components/daemon-runtime/doctrine.md) | **current Rust implementation:** this runtime control/projection family is owned by the Rust hypervisor daemon and the listed Rust kernel sources; deleted JavaScript-daemon and Step/Module bridge paths are migration history only | keep the Rust-owned control/projection boundary under the listed subject, mutation, authority, custody, and truth owners; the non-doctrinal migration status matrix tracks unfinished sequencing and historical retirement evidence | `crates/node/src/bin/hypervisor-daemon.rs`; `crates/node/src/bin/hypervisor_daemon_routes/lifecycle_routes.rs`; `crates/services/src/agentic/runtime/kernel/runtime_thread_event.rs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:compositor` |
| `RuntimeAgentgresStateCacheControl` | [`ioi-cli-daemon-runtime.md`](../components/daemon-runtime/doctrine.md), [`agentgres-state-substrate.md`](../components/agentgres/doctrine.md) | **current Rust runtime/admission boundary:** the daemon owns runtime control and cache/projection use; Agentgres owns admitted durable truth. `agentgres_admission.rs` validates runtime event/state transitions, but no cache may become a second truth owner | keep the concept only as a daemon-controlled rebuildable cache/projection boundary over Agentgres truth; remove the row if no distinct cache contract remains | `crates/node/src/bin/hypervisor-daemon.rs`; `crates/node/src/bin/hypervisor_daemon_routes/lifecycle_routes.rs`; `crates/services/src/agentic/runtime/kernel/agentgres_admission.rs` | reject cache-authored accepted truth, missing Agentgres admission, and daemon mutation bypass |
| `RuntimeAgentDeleteControl` | [`ioi-cli-daemon-runtime.md`](../components/daemon-runtime/doctrine.md) | **current Rust implementation:** this runtime control/projection family is owned by the Rust hypervisor daemon and the listed Rust kernel sources; deleted JavaScript-daemon and Step/Module bridge paths are migration history only | keep the Rust-owned control/projection boundary under the listed subject, mutation, authority, custody, and truth owners; the non-doctrinal migration status matrix tracks unfinished sequencing and historical retirement evidence | `crates/node/src/bin/hypervisor-daemon.rs`; `crates/node/src/bin/hypervisor_daemon_routes/lifecycle_routes.rs`; `crates/services/src/agentic/runtime/kernel/policy.rs`; `crates/services/src/agentic/runtime/kernel/policy/thread_lifecycle.rs`; `crates/services/src/agentic/runtime/kernel/agentgres_admission.rs` | `hypervisor-conformance:receipts`, `hypervisor-conformance:negative` |
| `RuntimeAgentStatusControl` | [`ioi-cli-daemon-runtime.md`](../components/daemon-runtime/doctrine.md) | **current Rust implementation:** this runtime control/projection family is owned by the Rust hypervisor daemon and the listed Rust kernel sources; deleted JavaScript-daemon and Step/Module bridge paths are migration history only | keep the Rust-owned control/projection boundary under the listed subject, mutation, authority, custody, and truth owners; the non-doctrinal migration status matrix tracks unfinished sequencing and historical retirement evidence | `crates/node/src/bin/hypervisor-daemon.rs`; `crates/node/src/bin/hypervisor_daemon_routes/lifecycle_routes.rs`; `crates/services/src/agentic/runtime/kernel/mod.rs`; `crates/services/src/agentic/runtime/kernel/policy.rs`; `crates/services/src/agentic/runtime/kernel/policy/thread_lifecycle.rs`; `crates/services/src/agentic/runtime/kernel/agentgres_admission.rs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:receipts` |
| `RuntimeThreadMemoryControl` | [`ioi-cli-daemon-runtime.md`](../components/daemon-runtime/doctrine.md), [`portable-memory-vault.md`](../components/daemon-runtime/portable-memory-vault.md), [`agentgres-state-substrate.md`](../components/agentgres/doctrine.md) | **current Rust implementation:** this runtime control/projection family is owned by the Rust hypervisor daemon and the listed Rust kernel sources; deleted JavaScript-daemon and Step/Module bridge paths are migration history only | keep the Rust-owned control/projection boundary under the listed subject, mutation, authority, custody, and truth owners; the non-doctrinal migration status matrix tracks unfinished sequencing and historical retirement evidence | `crates/node/src/bin/hypervisor-daemon.rs`; `crates/node/src/bin/hypervisor_daemon_routes/lifecycle_routes.rs`; `crates/services/src/agentic/runtime/kernel/runtime_memory_projection.rs`; `crates/services/src/agentic/runtime/kernel/runtime_memory_control.rs`; `crates/services/src/agentic/runtime/kernel/runtime_memory_command.rs`; `crates/services/src/agentic/runtime/kernel/policy.rs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:compositor`, `hypervisor-conformance:receipts` |
| `RuntimeThreadControl` | [`ioi-cli-daemon-runtime.md`](../components/daemon-runtime/doctrine.md) | **current Rust implementation:** this runtime control/projection family is owned by the Rust hypervisor daemon and the listed Rust kernel sources; deleted JavaScript-daemon and Step/Module bridge paths are migration history only | keep the Rust-owned control/projection boundary under the listed subject, mutation, authority, custody, and truth owners; the non-doctrinal migration status matrix tracks unfinished sequencing and historical retirement evidence | `crates/node/src/bin/hypervisor-daemon.rs`; `crates/node/src/bin/hypervisor_daemon_routes/lifecycle_routes.rs`; `crates/services/src/agentic/runtime/kernel/mod.rs`; `crates/services/src/agentic/runtime/kernel/policy.rs`; `crates/services/src/agentic/runtime/kernel/policy/thread_lifecycle.rs`; `crates/services/src/agentic/runtime/kernel/policy/workspace_trust.rs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:compositor` |
| `RuntimeWorkspaceTrustControl` | [`ioi-cli-daemon-runtime.md`](../components/daemon-runtime/doctrine.md), [`private-workspace-ctee.md`](../components/daemon-runtime/private-workspace-ctee.md) | **current Rust implementation:** this runtime control/projection family is owned by the Rust hypervisor daemon and the listed Rust kernel sources; deleted JavaScript-daemon and Step/Module bridge paths are migration history only | keep the Rust-owned control/projection boundary under the listed subject, mutation, authority, custody, and truth owners; the non-doctrinal migration status matrix tracks unfinished sequencing and historical retirement evidence | `crates/node/src/bin/hypervisor-daemon.rs`; `crates/node/src/bin/hypervisor_daemon_routes/lifecycle_routes.rs`; `crates/services/src/agentic/runtime/kernel/mod.rs`; `crates/services/src/agentic/runtime/kernel/policy/workspace_trust.rs`; `crates/services/src/agentic/runtime/kernel/runtime_thread_event.rs`; `crates/services/src/agentic/runtime/kernel/agentgres_admission.rs` | `hypervisor-conformance:compositor`, `hypervisor-conformance:negative` |
| `RuntimeWorkspaceChangeControl` | [`ioi-cli-daemon-runtime.md`](../components/daemon-runtime/doctrine.md) | **current Rust implementation:** this runtime control/projection family is owned by the Rust hypervisor daemon and the listed Rust kernel sources; deleted JavaScript-daemon and Step/Module bridge paths are migration history only | keep the Rust-owned control/projection boundary under the listed subject, mutation, authority, custody, and truth owners; the non-doctrinal migration status matrix tracks unfinished sequencing and historical retirement evidence | `crates/node/src/bin/hypervisor-daemon.rs`; `crates/node/src/bin/hypervisor_daemon_routes/lifecycle_routes.rs`; `crates/services/src/agentic/runtime/kernel/runtime_workspace_change_control.rs`; `crates/services/src/agentic/runtime/kernel/agentgres_admission.rs` | `hypervisor-conformance:compositor`, `hypervisor-conformance:negative` |
| `RuntimeManagedSessionControl` | [`ioi-cli-daemon-runtime.md`](../components/daemon-runtime/doctrine.md), [`core-clients-surfaces.md`](../components/hypervisor/core-clients-surfaces.md) | **current Rust implementation:** this runtime control/projection family is owned by the Rust hypervisor daemon and the listed Rust kernel sources; deleted JavaScript-daemon and Step/Module bridge paths are migration history only | keep the Rust-owned control/projection boundary under the listed subject, mutation, authority, custody, and truth owners; the non-doctrinal migration status matrix tracks unfinished sequencing and historical retirement evidence | `crates/node/src/bin/hypervisor-daemon.rs`; `crates/node/src/bin/hypervisor_daemon_routes/lifecycle_routes.rs`; `crates/services/src/agentic/runtime/kernel/runtime_managed_session_control.rs`; `crates/services/src/agentic/runtime/kernel/agentgres_admission.rs` | `hypervisor-conformance:compositor`, `hypervisor-conformance:negative` |
| `RuntimeBridgeThreadTurnControl` | [`ioi-cli-daemon-runtime.md`](../components/daemon-runtime/doctrine.md) | **current Rust implementation:** this runtime control/projection family is owned by the Rust hypervisor daemon and the listed Rust kernel sources; deleted JavaScript-daemon and Step/Module bridge paths are migration history only | keep the Rust-owned control/projection boundary under the listed subject, mutation, authority, custody, and truth owners; the non-doctrinal migration status matrix tracks unfinished sequencing and historical retirement evidence | `crates/node/src/bin/hypervisor-daemon.rs`; `crates/node/src/bin/hypervisor_daemon_routes/lifecycle_routes.rs`; `crates/services/src/agentic/runtime/kernel/mod.rs`; `crates/services/src/agentic/runtime/kernel/policy.rs`; `crates/services/src/agentic/runtime/kernel/policy/thread_lifecycle.rs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:compositor` |
| `RuntimeThreadForkControl` | [`ioi-cli-daemon-runtime.md`](../components/daemon-runtime/doctrine.md) | **current Rust implementation:** this runtime control/projection family is owned by the Rust hypervisor daemon and the listed Rust kernel sources; deleted JavaScript-daemon and Step/Module bridge paths are migration history only | keep the Rust-owned control/projection boundary under the listed subject, mutation, authority, custody, and truth owners; the non-doctrinal migration status matrix tracks unfinished sequencing and historical retirement evidence | `crates/node/src/bin/hypervisor-daemon.rs`; `crates/node/src/bin/hypervisor_daemon_routes/lifecycle_routes.rs`; `crates/services/src/agentic/runtime/kernel/runtime_thread_fork_control.rs`; `crates/services/src/agentic/runtime/kernel/agentgres_admission.rs` | `hypervisor-conformance:compositor`, `hypervisor-conformance:bridge` |
| `RuntimeSubagentControl` | [`ioi-cli-daemon-runtime.md`](../components/daemon-runtime/doctrine.md) | **current Rust implementation:** this runtime control/projection family is owned by the Rust hypervisor daemon and the listed Rust kernel sources; deleted JavaScript-daemon and Step/Module bridge paths are migration history only | keep the Rust-owned control/projection boundary under the listed subject, mutation, authority, custody, and truth owners; the non-doctrinal migration status matrix tracks unfinished sequencing and historical retirement evidence | `crates/node/src/bin/hypervisor-daemon.rs`; `crates/node/src/bin/hypervisor_daemon_routes/lifecycle_routes.rs`; `crates/services/src/agentic/runtime/kernel/runtime_subagent_projection.rs`; `crates/services/src/agentic/runtime/kernel/runtime_subagent_control.rs`; `crates/services/src/agentic/runtime/kernel/policy.rs`; `crates/services/src/agentic/runtime/kernel/policy/thread_lifecycle.rs` | `hypervisor-conformance:compositor`, `hypervisor-conformance:bridge` |
| `RuntimeTaskJobControl` | [`ioi-cli-daemon-runtime.md`](../components/daemon-runtime/doctrine.md) | **current Rust implementation:** this runtime control/projection family is owned by the Rust hypervisor daemon and the listed Rust kernel sources; deleted JavaScript-daemon and Step/Module bridge paths are migration history only | keep the Rust-owned control/projection boundary under the listed subject, mutation, authority, custody, and truth owners; the non-doctrinal migration status matrix tracks unfinished sequencing and historical retirement evidence | `crates/node/src/bin/hypervisor-daemon.rs`; `crates/node/src/bin/hypervisor_daemon_routes/lifecycle_routes.rs`; `crates/services/src/agentic/runtime/kernel/policy/task_job.rs`; `crates/services/src/agentic/runtime/kernel/policy/thread_lifecycle.rs`; `crates/services/src/agentic/runtime/kernel/policy/run_cancel.rs` | `hypervisor-conformance:compositor`, `hypervisor-conformance:bridge`, `hypervisor-conformance:docs` |
| `RuntimeRunCancelControl` | [`ioi-cli-daemon-runtime.md`](../components/daemon-runtime/doctrine.md) | **current Rust implementation:** this runtime control/projection family is owned by the Rust hypervisor daemon and the listed Rust kernel sources; deleted JavaScript-daemon and Step/Module bridge paths are migration history only | keep the Rust-owned control/projection boundary under the listed subject, mutation, authority, custody, and truth owners; the non-doctrinal migration status matrix tracks unfinished sequencing and historical retirement evidence | `crates/node/src/bin/hypervisor-daemon.rs`; `crates/node/src/bin/hypervisor_daemon_routes/lifecycle_routes.rs`; `crates/services/src/agentic/runtime/kernel/policy.rs`; `crates/services/src/agentic/runtime/kernel/policy/run_cancel.rs` | `hypervisor-conformance:bridge` |
| `RuntimeSkillHookRegistryControl` | [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md), [`ioi-cli-daemon-runtime.md`](../components/daemon-runtime/doctrine.md), [`connector-and-tool-contracts.md`](../components/connectors-tools/contracts.md) | **current Rust implementation:** this runtime control/projection family is owned by the Rust hypervisor daemon and the listed Rust kernel sources; deleted JavaScript-daemon and Step/Module bridge paths are migration history only | keep the Rust-owned control/projection boundary under the listed subject, mutation, authority, custody, and truth owners; the non-doctrinal migration status matrix tracks unfinished sequencing and historical retirement evidence | `crates/node/src/bin/hypervisor-daemon.rs`; `crates/node/src/bin/hypervisor_daemon_routes/lifecycle_routes.rs`; `crates/services/src/agentic/runtime/kernel/runtime_doctor_report.rs`; `crates/services/src/agentic/runtime/kernel/skill_hook_registry.rs`; `crates/services/src/agentic/runtime/kernel/policy/thread_lifecycle.rs`; `crates/services/src/agentic/runtime/kernel/mod.rs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:compositor` |
| `RuntimeRepositoryWorkflowProjectionControl` | [`ioi-cli-daemon-runtime.md`](../components/daemon-runtime/doctrine.md), [`core-clients-surfaces.md`](../components/hypervisor/core-clients-surfaces.md) | **current Rust implementation:** this runtime control/projection family is owned by the Rust hypervisor daemon and the listed Rust kernel sources; deleted JavaScript-daemon and Step/Module bridge paths are migration history only | keep the Rust-owned control/projection boundary under the listed subject, mutation, authority, custody, and truth owners; the non-doctrinal migration status matrix tracks unfinished sequencing and historical retirement evidence | `crates/node/src/bin/hypervisor-daemon.rs`; `crates/node/src/bin/hypervisor_daemon_routes/lifecycle_routes.rs`; `crates/services/src/agentic/runtime/kernel/repository_workflow.rs`; `crates/services/src/agentic/runtime/kernel/mod.rs` | `hypervisor-conformance:bridge` |
| `RuntimeToolCatalogProjectionControl` | [`ioi-cli-daemon-runtime.md`](../components/daemon-runtime/doctrine.md), [`connector-and-tool-contracts.md`](../components/connectors-tools/contracts.md) | **current Rust implementation:** this runtime control/projection family is owned by the Rust hypervisor daemon and the listed Rust kernel sources; deleted JavaScript-daemon and Step/Module bridge paths are migration history only | keep the Rust-owned control/projection boundary under the listed subject, mutation, authority, custody, and truth owners; the non-doctrinal migration status matrix tracks unfinished sequencing and historical retirement evidence | `crates/node/src/bin/hypervisor-daemon.rs`; `crates/node/src/bin/hypervisor_daemon_routes/lifecycle_routes.rs`; `crates/services/src/agentic/runtime/kernel/runtime_doctor_report.rs`; `crates/services/src/agentic/runtime/kernel/runtime_tool_catalog.rs`; `crates/services/src/agentic/runtime/kernel/mod.rs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:compositor` |
| `RuntimeDoctorReportProjectionControl` | [`ioi-cli-daemon-runtime.md`](../components/daemon-runtime/doctrine.md) | **current Rust implementation:** this runtime control/projection family is owned by the Rust hypervisor daemon and the listed Rust kernel sources; deleted JavaScript-daemon and Step/Module bridge paths are migration history only | keep the Rust-owned control/projection boundary under the listed subject, mutation, authority, custody, and truth owners; the non-doctrinal migration status matrix tracks unfinished sequencing and historical retirement evidence | `crates/node/src/bin/hypervisor-daemon.rs`; `crates/node/src/bin/hypervisor_daemon_routes/lifecycle_routes.rs`; `crates/services/src/agentic/runtime/kernel/runtime_doctor_report.rs`; `crates/services/src/agentic/runtime/kernel/mod.rs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:docs` |
| `RuntimeCodingToolBudgetRecoveryControl` | [`ioi-cli-daemon-runtime.md`](../components/daemon-runtime/doctrine.md) | **current Rust implementation:** this runtime control/projection family is owned by the Rust hypervisor daemon and the listed Rust kernel sources; deleted JavaScript-daemon and Step/Module bridge paths are migration history only | keep the Rust-owned control/projection boundary under the listed subject, mutation, authority, custody, and truth owners; the non-doctrinal migration status matrix tracks unfinished sequencing and historical retirement evidence | `crates/node/src/bin/hypervisor-daemon.rs`; `crates/node/src/bin/hypervisor_daemon_routes/lifecycle_routes.rs`; `crates/services/src/agentic/runtime/kernel/policy.rs`; `crates/services/src/agentic/runtime/kernel/policy/coding_tool_budget_recovery.rs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:compositor` |
| `RuntimeCodingToolGovernanceControl` | [`ioi-cli-daemon-runtime.md`](../components/daemon-runtime/doctrine.md), [`wallet-network-authority-layer.md`](../components/wallet-network/doctrine.md) | **current Rust implementation:** daemon runtime owns coding-tool admission, policy, result/event control, and mutation ordering; wallet.network owns approval/grant authority where the live records require it | preserve the explicit daemon-governance versus wallet-authority split; neither receipt shaping nor client intent may mint approval authority | `crates/node/src/bin/hypervisor-daemon.rs`; `crates/node/src/bin/hypervisor_daemon_routes/lifecycle_routes.rs`; `crates/services/src/agentic/runtime/kernel/approval.rs`; `crates/services/src/agentic/runtime/kernel/coding_tool_event.rs`; `crates/services/src/agentic/runtime/kernel/policy.rs`; `crates/services/src/agentic/runtime/kernel/policy/context_lifecycle.rs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:negative` |
| `RuntimeCodingToolResultEnvelopeControl` | [`ioi-cli-daemon-runtime.md`](../components/daemon-runtime/doctrine.md) | **current Rust implementation:** this runtime control/projection family is owned by the Rust hypervisor daemon and the listed Rust kernel sources; deleted JavaScript-daemon and Step/Module bridge paths are migration history only | keep the Rust-owned control/projection boundary under the listed subject, mutation, authority, custody, and truth owners; the non-doctrinal migration status matrix tracks unfinished sequencing and historical retirement evidence | `crates/node/src/bin/hypervisor-daemon.rs`; `crates/node/src/bin/hypervisor_daemon_routes/lifecycle_routes.rs`; `crates/services/src/agentic/runtime/kernel/coding_tool_event.rs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:receipts` |
| `RuntimeComputerUseInvocationControl` | [`ioi-cli-daemon-runtime.md`](../components/daemon-runtime/doctrine.md), [`wallet-network-authority-layer.md`](../components/wallet-network/doctrine.md) | **current Rust implementation:** daemon runtime owns computer-use invocation admission/execution binding; wallet.network owns the authority grants and approvals required by the live computer-use records | keep invocation mechanics and authority issuance separate, and reject any caller-authored approval or capability fact | `crates/node/src/bin/hypervisor-daemon.rs`; `crates/node/src/bin/hypervisor_daemon_routes/lifecycle_routes.rs`; `crates/services/src/agentic/runtime/kernel/coding_tool_computer_use.rs`; `crates/services/src/agentic/runtime/kernel/coding_tool_step_module.rs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:negative` |
| `RuntimeComputerUseProjectionControl` | [`ioi-cli-daemon-runtime.md`](../components/daemon-runtime/doctrine.md) | **current Rust implementation:** this runtime control/projection family is owned by the Rust hypervisor daemon and the listed Rust kernel sources; deleted JavaScript-daemon and Step/Module bridge paths are migration history only | keep the Rust-owned control/projection boundary under the listed subject, mutation, authority, custody, and truth owners; the non-doctrinal migration status matrix tracks unfinished sequencing and historical retirement evidence | `crates/node/src/bin/hypervisor-daemon.rs`; `crates/node/src/bin/hypervisor_daemon_routes/lifecycle_routes.rs`; `crates/services/src/agentic/runtime/kernel/runtime_computer_use.rs`; `crates/services/src/agentic/runtime/kernel/coding_tool_computer_use.rs`; `crates/services/src/agentic/runtime/kernel/mod.rs` | `hypervisor-conformance:compositor`, `hypervisor-conformance:negative` |
| `RuntimeComputerUseRunMaterializationControl` | [`ioi-cli-daemon-runtime.md`](../components/daemon-runtime/doctrine.md) | **current Rust implementation:** this runtime control/projection family is owned by the Rust hypervisor daemon and the listed Rust kernel sources; deleted JavaScript-daemon and Step/Module bridge paths are migration history only | keep the Rust-owned control/projection boundary under the listed subject, mutation, authority, custody, and truth owners; the non-doctrinal migration status matrix tracks unfinished sequencing and historical retirement evidence | `crates/node/src/bin/hypervisor-daemon.rs`; `crates/node/src/bin/hypervisor_daemon_routes/lifecycle_routes.rs`; `crates/services/src/agentic/runtime/kernel/policy/thread_lifecycle.rs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:docs` |
| `RuntimeCodingToolArtifactMutationControl` | [`ioi-cli-daemon-runtime.md`](../components/daemon-runtime/doctrine.md) | **current Rust implementation:** this runtime control/projection family is owned by the Rust hypervisor daemon and the listed Rust kernel sources; deleted JavaScript-daemon and Step/Module bridge paths are migration history only | keep the Rust-owned control/projection boundary under the listed subject, mutation, authority, custody, and truth owners; the non-doctrinal migration status matrix tracks unfinished sequencing and historical retirement evidence | `crates/node/src/bin/hypervisor-daemon.rs`; `crates/node/src/bin/hypervisor_daemon_routes/lifecycle_routes.rs`; `crates/services/src/agentic/runtime/kernel/coding_tool_artifact.rs`; `crates/services/src/agentic/runtime/kernel/coding_tool_event.rs`; `crates/services/src/agentic/runtime/kernel/coding_tool_step_module.rs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:negative` |
| `RuntimeConversationArtifactControl` | [`ioi-cli-daemon-runtime.md`](../components/daemon-runtime/doctrine.md) | **current Rust implementation:** this runtime control/projection family is owned by the Rust hypervisor daemon and the listed Rust kernel sources; deleted JavaScript-daemon and Step/Module bridge paths are migration history only | keep the Rust-owned control/projection boundary under the listed subject, mutation, authority, custody, and truth owners; the non-doctrinal migration status matrix tracks unfinished sequencing and historical retirement evidence | `crates/node/src/bin/hypervisor-daemon.rs`; `crates/node/src/bin/hypervisor_daemon_routes/lifecycle_routes.rs`; `crates/services/src/agentic/runtime/kernel/runtime_conversation_artifact_control.rs`; `crates/services/src/agentic/runtime/kernel/runtime_conversation_artifact_projection.rs`; `crates/services/src/agentic/runtime/kernel/agentgres_admission.rs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:compositor`, `hypervisor-conformance:receipts` |
| `ModelConversationStateControl` | [`model-router-byok-run-to-idle.md`](../components/model-router/doctrine.md) | **partial direct persistence/projection; canonical conversation planner unmounted:** `store_conversation` directly persists caller-shaped state under `model-conversations` and continuation reads it directly. The daemon never calls `plan_conversation_state` | keep direct continuation storage distinct from the uncalled canonical planner and its accepted-receipt semantics | `crates/node/src/bin/hypervisor-daemon.rs`; declared-unmounted contract `crates/services/src/agentic/runtime/kernel/model_mount/conversation.rs`; mounted read projection `crates/services/src/agentic/runtime/kernel/model_mount/read_projection/conversation.rs` | source-marker checks bind direct `persist_record`, direct load, and absence of `plan_conversation_state` |
| `RuntimeWorkspaceSnapshotRestoreControl` | [`ioi-cli-daemon-runtime.md`](../components/daemon-runtime/doctrine.md) | **current Rust implementation:** this runtime control/projection family is owned by the Rust hypervisor daemon and the listed Rust kernel sources; deleted JavaScript-daemon and Step/Module bridge paths are migration history only | keep the Rust-owned control/projection boundary under the listed subject, mutation, authority, custody, and truth owners; the non-doctrinal migration status matrix tracks unfinished sequencing and historical retirement evidence | `crates/node/src/bin/hypervisor-daemon.rs`; `crates/node/src/bin/hypervisor_daemon_routes/lifecycle_routes.rs`; `crates/services/src/agentic/runtime/kernel/workspace_restore.rs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:receipts`, `hypervisor-conformance:compositor` |
| `RuntimeOperatorTurnControl` | [`ioi-cli-daemon-runtime.md`](../components/daemon-runtime/doctrine.md) | **current Rust implementation:** this runtime control/projection family is owned by the Rust hypervisor daemon and the listed Rust kernel sources; deleted JavaScript-daemon and Step/Module bridge paths are migration history only | keep the Rust-owned control/projection boundary under the listed subject, mutation, authority, custody, and truth owners; the non-doctrinal migration status matrix tracks unfinished sequencing and historical retirement evidence | `crates/node/src/bin/hypervisor-daemon.rs`; `crates/node/src/bin/hypervisor_daemon_routes/lifecycle_routes.rs`; `crates/services/src/agentic/runtime/kernel/mod.rs`; `crates/services/src/agentic/runtime/kernel/policy.rs`; `crates/services/src/agentic/runtime/kernel/policy/operator_control.rs` | `hypervisor-conformance:bridge` |
| `RuntimeApprovalControl` | [`ioi-cli-daemon-runtime.md`](../components/daemon-runtime/doctrine.md), [`wallet-network-authority-layer.md`](../components/wallet-network/doctrine.md) | **current Rust implementation:** this runtime control/projection family is owned by the Rust hypervisor daemon and the listed Rust kernel sources; deleted JavaScript-daemon and Step/Module bridge paths are migration history only | keep the Rust-owned control/projection boundary under the listed subject, mutation, authority, custody, and truth owners; the non-doctrinal migration status matrix tracks unfinished sequencing and historical retirement evidence | `crates/node/src/bin/hypervisor-daemon.rs`; `crates/node/src/bin/hypervisor_daemon_routes/lifecycle_routes.rs`; `crates/services/src/agentic/runtime/kernel/approval.rs`; `crates/services/src/agentic/runtime/kernel/coding_tool_event.rs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:negative` |
| `RuntimeContextPolicyControl` | [`ioi-cli-daemon-runtime.md`](../components/daemon-runtime/doctrine.md) | **current Rust implementation:** this runtime control/projection family is owned by the Rust hypervisor daemon and the listed Rust kernel sources; deleted JavaScript-daemon and Step/Module bridge paths are migration history only | keep the Rust-owned control/projection boundary under the listed subject, mutation, authority, custody, and truth owners; the non-doctrinal migration status matrix tracks unfinished sequencing and historical retirement evidence | `crates/node/src/bin/hypervisor-daemon.rs`; `crates/node/src/bin/hypervisor_daemon_routes/lifecycle_routes.rs`; `crates/services/src/agentic/runtime/kernel/policy.rs`; `crates/services/src/agentic/runtime/kernel/policy/context_lifecycle.rs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:compositor` |
| `RuntimeStudioIntentFrameControl` | [`ioi-cli-daemon-runtime.md`](../components/daemon-runtime/doctrine.md), [`core-clients-surfaces.md`](../components/hypervisor/core-clients-surfaces.md) | **current Rust implementation:** this runtime control/projection family is owned by the Rust hypervisor daemon and the listed Rust kernel sources; deleted JavaScript-daemon and Step/Module bridge paths are migration history only | keep the Rust-owned control/projection boundary under the listed subject, mutation, authority, custody, and truth owners; the non-doctrinal migration status matrix tracks unfinished sequencing and historical retirement evidence | `crates/node/src/bin/hypervisor-daemon.rs`; `crates/node/src/bin/hypervisor_daemon_routes/lifecycle_routes.rs`; `crates/services/src/agentic/runtime/kernel/studio_intent_frame.rs`; `crates/services/src/agentic/runtime/kernel/mod.rs` | `hypervisor-conformance:compositor` |
| `RuntimeDiagnosticsRepairControl` | [`ioi-cli-daemon-runtime.md`](../components/daemon-runtime/doctrine.md) | **current Rust implementation:** this runtime control/projection family is owned by the Rust hypervisor daemon and the listed Rust kernel sources; deleted JavaScript-daemon and Step/Module bridge paths are migration history only | keep the Rust-owned control/projection boundary under the listed subject, mutation, authority, custody, and truth owners; the non-doctrinal migration status matrix tracks unfinished sequencing and historical retirement evidence | `crates/node/src/bin/hypervisor-daemon.rs`; `crates/node/src/bin/hypervisor_daemon_routes/lifecycle_routes.rs`; `crates/services/src/agentic/runtime/kernel/runtime_diagnostics_repair_control.rs`; `crates/services/src/agentic/runtime/kernel/runtime_diagnostics_repair_projection.rs`; `crates/services/src/agentic/runtime/kernel/runtime_diagnostics_repair_policy.rs`; `crates/services/src/agentic/runtime/kernel/policy.rs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:compositor`, `hypervisor-conformance:negative` |
| `RuntimeWorkflowEditControl` | [`ioi-cli-daemon-runtime.md`](../components/daemon-runtime/doctrine.md), [`core-clients-surfaces.md`](../components/hypervisor/core-clients-surfaces.md) | **current Rust implementation:** this runtime control/projection family is owned by the Rust hypervisor daemon and the listed Rust kernel sources; deleted JavaScript-daemon and Step/Module bridge paths are migration history only | keep the Rust-owned control/projection boundary under the listed subject, mutation, authority, custody, and truth owners; the non-doctrinal migration status matrix tracks unfinished sequencing and historical retirement evidence | `crates/node/src/bin/hypervisor-daemon.rs`; `crates/node/src/bin/hypervisor_daemon_routes/lifecycle_routes.rs`; `crates/services/src/agentic/runtime/kernel/runtime_workflow_edit_control.rs`; `crates/services/src/agentic/runtime/kernel/policy.rs`; `crates/services/src/agentic/runtime/kernel/policy/admission_required.rs`; `crates/services/src/agentic/runtime/kernel/approval.rs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:compositor` |
| `RuntimeMcpControl` | [`ioi-cli-daemon-runtime.md`](../components/daemon-runtime/doctrine.md), [`connector-and-tool-contracts.md`](../components/connectors-tools/contracts.md), [`wallet-network-authority-layer.md`](../components/wallet-network/doctrine.md) | current Rust `hypervisor-daemon` mounts non-live thread MCP import, server control, status, validation, and tool-search handlers; compatibility discovery routes `/v1/mcp`, `/v1/mcp/servers`, `/v1/mcp/tools`, `/v1/mcp/resources`, and `/v1/mcp/prompts` remain mounted. It also mounts `/v1/model-mount/mcp/invoke`, but that direct handler performs no MCP transport call: it authorizes, authors a receipt, and falsely returns `result.status: "executed"`. No thread live serve/invoke route exists, and the unmounted workflow/live-backend modules remain substrate | preserve the mounted non-live control/discovery surface, and record the model-mount invoke response as a canon-to-code gap. A later contract cut must either execute through an admitted MCP backend with containment/receipt/replay or return an honest non-executed status; do not infer live execution from the route or receipt | `crates/node/src/bin/hypervisor-daemon.rs`; `crates/node/src/bin/hypervisor_daemon_routes/lifecycle_routes.rs`; `crates/services/src/agentic/runtime/kernel/mod.rs`; `crates/services/src/agentic/runtime/kernel/policy/mcp_memory.rs`; unmounted backend `crates/services/src/agentic/runtime/service/mcp_live_backend.rs` | source-backed assertions must inventory every mount and inspect `handle_mcp_invoke` behavior, including the absence of a backend call and the dishonest current `executed` status |

Implementation note: the current non-live thread control and tool-search
handlers call `RuntimeKernelService` directly from Rust
`lifecycle_routes.rs`; the five top-level discovery routes use the same Rust
catalog projection. No JS MCP surface edge or mounted thread serve route exists.
The unmounted Rust serve/live-backend modules and stale client route strings are
implementation debt, not public server truth.

| `ActionProposal` | [`default-harness-profile.md`](../components/daemon-runtime/default-harness-profile.md) | action request, runtime item, event | promote when approval/replay/policy review needs identity | `crates/types/src/app/runtime/computer_use.rs` | HP-2 |
| `InformationFlowLabel`, `DeclassificationApproval`, and selected Cut 3B1 policy-enforcement points | [`security-privacy-policy-invariants.md`](../foundations/security-privacy-policy-invariants.md), [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md), [`connector-and-tool-contracts.md`](../components/connectors-tools/contracts.md) | **machine contracts built; runtime enforcement not started:** registered closed label and approval schemas, invariants, fixtures, and generated Rust/TypeScript projections define the target derivation, request/effect binding, destination, and declassification shape. Generated projections are data contracts and do not propagate labels or enforce a policy boundary | implement owner-specific PEPs before each real effect and preserve refs through outputs; do not infer estate-wide enforcement from schema availability. Raw external results remain untrusted, and unsupported propagation must fail closed | `docs/architecture/_meta/schemas/information-flow-label.v1.schema.json`, `declassification-approval.v1.schema.json`, and generated architecture-contract projections; no shared IFC kernel or daemon enforcement module exists on the current base | [`information-flow-propagation.md`](../../conformance/hypervisor-core/information-flow-propagation.md) is a target conformance profile. Architecture-contract gates prove schema/fixture/projection parity only; connector, MCP, model, browser, memory, webhook, room, artifact, and estate-wide propagation PEPs remain unimplemented |
| `PhysicalActionIntent` | [`physical-action-safety.md`](../foundations/physical-action-safety.md) | action proposal plus authority, safety, supervision, e-stop, exact pre-action state root, expanded embodied-resource-group leaves, exact command/controller binding, sensor evidence, and expected receipt refs | promote whenever embodied work can affect actuators, devices, facilities, vehicles, drones, robots, or physical safety | **partial validation precedent only:** `crates/services/src/agentic/runtime/kernel/runtime_physical_action_intent_admission.rs` rejects generic-tool posture and checks basic risk, scope, policy, supervision, e-stop, sensor-receipt, and ref shape. It is a pure planner with no daemon route, owner resolution, expanded resource closure, command/controller/idempotency binding, typed adapter call, effect PEP, or durable execution state | physical action cannot execute as generic tool/connector/shell/AIIP traffic; syntactically supplied policy/evidence refs are not proof, and no live execution authority is claimed |
| `PhysicalActionPolicy` / `SafetyEnvelope` | [`physical-action-safety.md`](../foundations/physical-action-safety.md) | policy object plus Agentgres refs and receipt obligations | canonical object when a worker/domain may perform actuator-affecting work | **target canonical:** current intent/package admission code treats policy and safety refs as required inputs but does not resolve deployment, ODD, timing, assured-input, proof-test, restart/writer, teleoperation, resource, or command/controller evidence. No local-supervisor or effect-bound admission gate is built | every `physical_action` requires resolved safety policy and envelope evidence; supplied refs are not self-verifying evidence |
| `EmergencyStopAuthority` / `HumanSupervisionPolicy` | [`physical-action-safety.md`](../foundations/physical-action-safety.md), [`wallet-network/doctrine.md`](../components/wallet-network/doctrine.md) | wallet/daemon policy refs plus receipts | canonical object when harm can propagate or policy requires human oversight | planned wallet/daemon gate | e-stop and supervision requirements checked before command issue |
| `SensorEvidenceReceipt` / `ActuatorCommandReceipt` / `PhysicalActionExecutionReceipt` | [`physical-action-safety.md`](../foundations/physical-action-safety.md), [`events-receipts-delivery-bundles.md`](../components/daemon-runtime/events-receipts-delivery-bundles.md) | registered closed `ReceiptEnvelope` plus physical execution body binding expanded resources, exact command/controller/graph/envelope/evidence/writer/timing refs, before/after roots, dispatch proof, normalized effect, and hash-chain predecessor | receipt required for every approved physical actuator command | **machine contract built; execution mechanism not started:** the registered schema, invariants, fixtures, and generated projections define the receipt shape. No typed physical-execution core, actuator adapter mount, durable idempotency/reconciliation state, hardware-identity verifier, or Agentgres physical-action ledger exists on the current base | architecture-contract gates validate schema/fixture/projection parity. A future execution gate must prove zero calls on denial, idempotent completed replay, changed-body conflict, prepared-state reconciliation, adapter identity, dispatch proof, normalized outcomes, and valid receipt hashing without treating the schema itself as execution evidence |
| `PhysicalActionIncident` | [`physical-action-safety.md`](../foundations/physical-action-safety.md), [`agentgres/doctrine.md`](../components/agentgres/doctrine.md) | Agentgres incident object/event plus remediation/dispute refs | canonical object when safety, supervision, sensor, actuator, policy, or disputed-outcome failure occurs | planned incident/admission path | incidents admitted through Agentgres, not hidden in provider logs |
| `GateResult` | [`default-harness-profile.md`](../components/daemon-runtime/default-harness-profile.md), [`wallet-network/doctrine.md`](../components/wallet-network/doctrine.md) | policy/authority decision receipt | receipt now; object if long-lived approval/blocker | policy and approval handlers | HP-2, CEC phase order |
| `ExecutionResult` | [`events-receipts-delivery-bundles.md`](../components/daemon-runtime/events-receipts-delivery-bundles.md) | workload/tool/worker/service receipt | receipt; artifact refs for large outputs | tool execution processing paths | CEC execution/verification |
| `NormalizedObservation` | [`default-harness-profile.md`](../components/daemon-runtime/default-harness-profile.md) | typed event/projection payload | promote when reused by verifiers/downstream tasks | browser observation and completion receipt paths | HP-3, HP-6 |
| `OutputOwnershipPass` | [`default-harness-profile.md`](../components/daemon-runtime/default-harness-profile.md) | completion receipt plus terminal event | promote for delivery, dispute, replay, marketplace settlement | `crates/services/src/agentic/runtime/service/queue/processing/completion_receipts.rs` | HP-6 |
| `Blocker` | [`default-harness-profile.md`](../components/daemon-runtime/default-harness-profile.md), [`daemon-runtime/api.md`](../components/daemon-runtime/api.md) | event plus task state | object when user/operator action or long wait is needed | approval/blocker runtime handlers | HP-2, HP-4 |
| `AlphaSeal` | [`private-workspace-ctee.md`](../components/daemon-runtime/private-workspace-ctee.md) | encrypted/private strategy capsule artifact ref plus policy refs | object when strategy lifecycle, reuse, or audit matters | planned | private strategy checks |
| `DeclassificationGate` / `DeclassificationReceipt` | [`private-workspace-ctee.md`](../components/daemon-runtime/private-workspace-ctee.md), [`wallet-network/doctrine.md`](../components/wallet-network/doctrine.md) | policy gate plus receipt | receipt by default; object if long-lived policy state is queried | planned | capability-exit checks |
| `PrivateInferenceReceipt` | [`private-workspace-ctee.md`](../components/daemon-runtime/private-workspace-ctee.md), [`events-receipts-delivery-bundles.md`](../components/daemon-runtime/events-receipts-delivery-bundles.md) | receipt | proof object | planned | no plaintext on node claim |

## State, Memory, Artifact, and Receipt Concepts

| Concept | Canonical owner | Current durable form | Promote or keep | Code anchor when known | Conformance hook |
| --- | --- | --- | --- | --- | --- |
| `InstitutionalLearningBoundaryProfile`, `LearningSourceRightsClaim`, `LearningEvidenceEligibility`, `LearningEgressReceipt`, `InstitutionalIntelligenceExportBundle`, `ModelSwapContinuityReport` | [`institutional-learning-boundary.md`](../foundations/institutional-learning-boundary.md), [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md), [`events-receipts-delivery-bundles.md`](../components/daemon-runtime/events-receipts-delivery-bundles.md) | target cross-cutting compiler profile plus source-rights claim, owner-qualified evidence-eligibility decision, egress receipt, policy-filtered export manifest, and composite continuity projection; it intersects source/data policy, consent, custody, route rights, evidence eligibility, retention/export, lineage, and revocation without replacing their owners | promote the profile, eligibility decision, and source/export envelopes as stable target contracts; retain `TrainingEvidenceEligibility` as the model/worker-training compatibility profile; promote `LearningEgressReceipt` as attempted-or-actual boundary-egress evidence; keep the continuity report as a projection over eval/route/promotion/receipt truth; none is authority, operational truth, payload storage, a legal instrument, or another product application | planned; current memory vault export/import, Agentgres operations, route metadata, cTEE doctrine, training-eligibility envelope, Foundry draft plane, and receipt substrate are partial ingredients only | [`institutional-learning-boundary.md`](../../conformance/hypervisor-core/institutional-learning-boundary.md); must prove forbidden canary blocked before network egress, eligible private improvement and transitive lineage, incumbent-provider removal with declared eval continuity, cross-tenant denial, clean export/import with re-admission, and revocation impact; cannot claim provider non-learning, verified unlearning, or equivalent replacement quality without corresponding evidence |
| `Run` | [`agentgres/api-object-model.md`](../components/agentgres/api-object-model.md) | Agentgres runtime object | canonical object for serious runs | runtime thread/run records | HP-1, HP-7 |
| `Task` / `TaskState` | [`agentgres/api-object-model.md`](../components/agentgres/api-object-model.md) | Agentgres runtime object | canonical object for serious runs | runtime state objects | HP-4 |
| `ContextMutation` | [`agentgres/api-object-model.md`](../components/agentgres/api-object-model.md), [`agentgres/doctrine.md`](../components/agentgres/doctrine.md) | Agentgres operation/object-shaped memory admission | canonical when durable behavior-affecting memory changes | memory API and Agentgres object model | memory admission invariant |
| `AgentWiki` | [`agentgres/doctrine.md`](../components/agentgres/doctrine.md) | memory/wiki surface plus projections; durable commits via `ContextMutation` | context-memory plane, not Agentgres truth by itself | memory runtime/API | memory admission invariant |
| `ioi-memory` | [`agentgres/doctrine.md`](../components/agentgres/doctrine.md), [`daemon-runtime/api.md`](../components/daemon-runtime/api.md) | local/runtime memory plane, thread checkpoints, enrichment jobs | keep adjacent to Agentgres; admit durable changes through operations | memory runtime/API | memory admission invariant |
| `ArtifactRef` | [`agentgres/artifact-ref-plane.md`](../components/agentgres/artifact-ref-plane.md) | Agentgres object/ref | canonical ref; bytes live in storage backends | artifact APIs | HP-5, artifact-ref conformance |
| `PayloadRef` | [`agentgres/artifact-ref-plane.md`](../components/agentgres/artifact-ref-plane.md) | operation payload ref | keep as operation/ref field unless lifecycle needs object | artifact APIs | HP-5, artifact-ref conformance |
| `EvidenceBundle` | [`agentgres/artifact-ref-plane.md`](../components/agentgres/artifact-ref-plane.md) | Agentgres-governed bundle ref plus receipt/artifact graph | promote when claims, verification, replay, or dispute need bundle identity | evidence/artifact APIs | HP-5, verification receipt checks |
| `Receipt` | [`events-receipts-delivery-bundles.md`](../components/daemon-runtime/events-receipts-delivery-bundles.md) | attributable evidence object/envelope for a declared operation, observation, decision, authority transition, delivery, or effect; indexed through Agentgres when operational truth is affected | canonical evidence record; never by itself proof of correctness, semantic truth, verification, acceptance, adjudication, or settlement | `crates/types/src/app/harness/receipts.rs` | CEC, HP-3; assurance-state conformance |
| `ArchitectureContractRegistry`; 13 compiled contract revisions spanning receipts/proofs, portable authority, information flow and declassification, runtime tools, managed-work billing, dispute rails, and physical-action execution | [`architecture-contract-registry.v1.json`](./schemas/architecture-contract-registry.v1.json), [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md), [`events-receipts-delivery-bundles.md`](../components/daemon-runtime/events-receipts-delivery-bundles.md), [`connectors-tools/contracts.md`](../components/connectors-tools/contracts.md), [`security-privacy-policy-invariants.md`](../foundations/security-privacy-policy-invariants.md), [`economic-flywheel-and-pricing-boundaries.md`](../foundations/economic-flywheel-and-pricing-boundaries.md), [`physical-action-safety.md`](../foundations/physical-action-safety.md) | **built machine-contract substrate:** strict JSON Schema 2020-12, cross-field invariant metadata, read-only compatibility aliases, positive/adversarial fixtures, and deterministic TypeScript/Rust projections for 13 registered revisions. The named `ajv-2020-12-plus-portable-invariants-and-canonical-rfc3339` oracle constrains every JSON integer to the portable unsigned domain `0..9007199254740991` (integral JSON decimals such as `1.0` remain integers) and constrains date-time text to uppercase `T`/`Z` or a colonized `±HH:MM` offset while retaining Ajv calendar/leap-second semantics. These artifacts define and validate machine shape and projection parity; they do not provide cryptographic authority verification, receipt accumulation/proof verification, offline CLI verification, runtime admission, or owner-plane effects | keep registry/schema beneath semantic owners; mint successor IDs for breaking changes and never hand-edit generated projections. Registry `generated_targets` must equal the canonical TypeScript and Rust consumer path/kind/symbol map; redirected copies, escaping paths, and symlink escapes fail before reads or writes. Receipt checkpoint v1 remains an explicitly linear target contract, not a Merkle/RFC 6962 claim. Verifiers, trusted-key/time inputs, production emission, network discovery, transparency, and witness defenses require separate implementation | `docs/architecture/_meta/schemas/`; `scripts/generate-architecture-contracts.mjs`; `scripts/check-architecture-contracts.mjs`; generated `crates/types/src/app/generated/architecture_contracts.rs` and `packages/hypervisor-workbench/src/runtime/generated/architecture-contracts.ts` | `check:architecture-contracts`, live Ajv-to-Rust differential projection tests (including direct nested deserialization), `ioi-types` architecture-contract tests, and the architecture-doc checker; no portable-proof verifier, fixture-generator CLI, or offline verification tier is claimed |
| `TraceRef` | [`events-receipts-delivery-bundles.md`](../components/daemon-runtime/events-receipts-delivery-bundles.md) | trace ref plus storage payload | ref/projection; payload in storage backend | runtime trace exports | HP-3 |
| `AgentStateArchive` | [`agentgres/artifact-ref-plane.md`](../components/agentgres/artifact-ref-plane.md), [`agentgres/api-object-model.md`](../components/agentgres/api-object-model.md) | Agentgres archive ref plus encrypted payload bytes | canonical ref/object; payload in storage backend | planned archive/restore APIs | restore invariant |
| `AgentExecutionTrace`, `AgentExecutionBranch`, `StagedEffect`, `BranchCheckpoint`, `BranchMergePlan` | [`agentgres/doctrine.md`](../components/agentgres/doctrine.md), [`agentgres/api-object-model.md`](../components/agentgres/api-object-model.md) | planned durable branch/staged-effect objects over existing substrate: `thread.forked` events, run replay, counterfactual what-if replay, workspace snapshot/restore custody, GoalRun continuation state; ref schemes `execution_branch://`, `staged_effect://`, `branch_checkpoint://`, `branch_merge://` | promote to canonical Agentgres objects; canonical heads advance only through expected-head merge/admission, and admission re-validates staged-effect authority against current revocation epoch/expiry/policy hash (INV-1/INV-5) with the epoch bound into the decision receipt; merge resolution classifies heads as exclusive_owner / declared_commutative / adjudicated (never text merge) | `crates/services/src/agentic/runtime/kernel/workspace_restore.rs` (snapshot/restore substrate); run replay + what-if simulation routes | replay/restore invariants; expected-head admission checks |
| `AgentgresSubstrateContract`, `SqlWriteIntentBridge` | [`agentgres/doctrine.md`](../components/agentgres/doctrine.md) (Substrate Contract Doctrine), [`agentgres/postgres-bridge-and-readiness-contract.md`](../components/agentgres/postgres-bridge-and-readiness-contract.md) (SQL Writes as Intents) | substrate contract: ENGINE V0 BUILT (`crates/agentgres` — five-verb trait, append-only op log, deterministic single-writer group commit, batch rooting over operation ranges, O(1) fork/checkpoint, recovery-by-replay; `#![forbid(unsafe_code)]`, zero clock/randomness in engine). Baseline (dev box, fsync-honest): 8.6k admissions/s/domain @64 in-flight (5k target met), fork 0.4ms (<1s target met), p99 blocked by the box's ~5.4ms fdatasync floor (engine ceiling 261k ops/s @ p99 0.10ms non-durable). SESSION 2: mux combined-flush log (`mux.rs` — one file/one fsync across domains, per-domain head/seq/root chains independent of interleaving) lifts single-box aggregate 8.8k → 20.8k ops/s (2.4×, 88% of group-commit bound); `substrate-shadow` carried real daemon truth read-only — all 4,287 persisted provider receipts, complete coverage, deterministic double-run, recovery verified. Determinism audit of kernel cores: zero randomness, 33 clock uses across 8 files backlogged (`crates/agentgres/DETERMINISM-AUDIT.md`). SESSION 3 CUT-OVER: `provider-receipts` truth IS the engine (first promoted family; no downstream users, split brain avoided by decision) — `persist_record` admits fail-closed into the mux log at `<data_dir>/substrate/` (no legacy JSON written), `read_record_dir` serves writer-thread last-write-wins projection, one-time idempotent backfill in `(at, record_id)` order on first open (legacy files inert history), WAL torn-tail truncation on recovery; all 1 write + 3 read sites covered via the two shared helpers; `GET /v1/hypervisor/substrate/status`; PROVEN LIVE by daemon smoke (boot → backfill → engine-served receipts → restart recovery with stable root); dual-write soak lane + `substrate-parity` promotion bar retained for FUTURE families. SESSION 4′ REPLICATION-AS-DURABILITY: durability classes on every ack (buffered | device_flush | replicated_same_host | quorum_replicated — owner canonical-enums.md, INV-14 in protocol); replicate-then-ack writer ships exact batch bytes to a log-shipping replica (byte-identical logs → identical roots by construction), device flush = background hygiene on independent fds BOTH sides, honest degrade on link failure, quorum label impossible same-host; MEASURED (dev box, loopback, replicated_same_host): 220k adm/s @ p99 0.75ms single domain, 8-domain aggregate 221.7k/s @ p99 1.1ms — CPU-bound, flush floor OFF the ack path; ALL THREE performance-contract metrics met under replicated semantics. SESSION 5 HA-WITHOUT-CONSENSUS BUILT: mux/storage-writer epochs as log frames (fencing primitive; ship byte-identically; outside root chains), protocol AGRS2 = epoch+offset handshake → deposed primaries fenced at handshake or mid-stream NACK, late replicas catch up by offset streaming, ahead replicas refuse overwrite; receipted operator promotion (`ioi.agentgres.writer-promotion.v1`, epoch+1); multi-replica static-membership fan-out with ack quorum; quorum_replicated GATED on declared failure-independence (same-host 2/2 quorum still caps at replicated_same_host — measured 200k adm/s p99 0.89ms); daemon opt-in via IOI_SUBSTRATE_REPLICA_ADDRS/ASYNC_FLUSH/ACK_QUORUM with loud per-batch-sync fail-safe. These mux epochs are explicitly not per-System authority; the separate System transition/fence kernel and effect PEPs remain target work in the autonomous-system row. Bridge SQL-writes-as-intents still planned | consensus tier (leader election, view changes, dynamic membership, automated failover) stays H2 behind TWO named gates: real multi-node customer with failover SLAs AND running deterministic-simulation harness; then VSR-under-DST vs proven-library-control-plane over this data plane (epochs feed either); extend `PROMOTED_DOMAINS` per family after soak + parity; Postgres remains projection/durability host, never admission owner (INV-10) | `crates/agentgres/` (engine, mux, replica, bench/shadow/parity/replica bins); `crates/node/src/bin/hypervisor_daemon_routes/substrate_store.rs` (promoted write/read + backfill + replication env + status route); `crates/services/src/agentic/runtime/` (kernel cores to migrate) | `verify-agentgres-substrate-bench.mjs` (36/36, 17-test unit battery incl. catch-up resync, promotion fencing, quorum same-host cap); replay/restore invariants; deterministic-simulation harness (planned, named gate) |
| `ProjectionCheckpoint` | [`agentgres/api-object-model.md`](../components/agentgres/api-object-model.md) | projection state/checkpoint ref | projection, invalidatable/rebuildable | Agentgres projection APIs | projection rebuild checks |
| `DeliveryBundle` | [`agentgres/artifact-ref-plane.md`](../components/agentgres/artifact-ref-plane.md), [`events-receipts-delivery-bundles.md`](../components/daemon-runtime/events-receipts-delivery-bundles.md) | delivery artifact refs + receipt bundle + settlement state | object when marketplace/dispute acceptance applies | delivery APIs | HP-6, HP-7 |
| `ServiceCompositionReceiptBundle` | [`events-receipts-delivery-bundles.md`](../components/daemon-runtime/events-receipts-delivery-bundles.md), [`sas/service-marketplace.md`](../domains/sas/service-marketplace.md), [`sas/service-endpoints.md`](../domains/sas/service-endpoints.md) | receipt bundle profile binding composition graph, routing receipts, contribution receipts, verifier refs, policy receipts, private-data posture, dispute evidence refs, Agentgres operation refs, and state root | required evidence profile for nested service outcomes; not a runtime, authority layer, or storage backend | planned SAS delivery APIs and daemon receipt paths | `npm run check:service-composition-evidence`; composed deliveries cannot settle or dispute from raw blobs/provider logs alone |
| `ArtifactAvailabilityIncident` / `ArtifactAvailabilityIncidentAgentgresOperation` | [`agentgres/artifact-ref-plane.md`](../components/agentgres/artifact-ref-plane.md), [`storage-backends/doctrine.md`](../components/storage-backends/doctrine.md) | **current Rust implementation:** the Rust daemon mounts artifact-availability incident admission and delegates canonicalization to the Rust kernel admission core | keep the Rust-owned control/projection boundary under the listed subject, mutation, authority, custody, and truth owners; the non-doctrinal migration status matrix tracks unfinished sequencing and historical retirement evidence | `crates/node/src/bin/hypervisor-daemon.rs`; `crates/node/src/bin/hypervisor_daemon_routes/lifecycle_routes.rs`; `crates/services/src/agentic/runtime/kernel/runtime_artifact_availability_incident_admission.rs` | `npm run check:artifact-availability-incident`; repair must be operation-backed through Agentgres |
| `PrivateUserAppStateRef` | [`agentgres/artifact-ref-plane.md`](../components/agentgres/artifact-ref-plane.md) | Agentgres-governed encrypted payload ref plus policy, receipt, authority, and state-root linkage | ref by default; promote when lifecycle, query, restore, dispute, or cross-app portability needs object identity | planned app-state/artifact APIs | private-state split invariant |
| `StorageBackend` | [`storage-backends/doctrine.md`](../components/storage-backends/doctrine.md) | payload byte store profile/config | keep as backend profile unless domain policy needs object identity | storage/artifact APIs | storage backend conformance |
| `FilecoinCASBackend` | [`storage-backends/filecoin-cas.md`](../components/storage-backends/filecoin-cas.md) | storage backend profile | byte availability only; not authority layer | artifact APIs | storage backend conformance |
| Restore/import flow | [`agentgres/artifact-ref-plane.md`](../components/agentgres/artifact-ref-plane.md) | Agentgres operations plus archive payload refs and receipts | operation-backed flow; never silent file mutation | planned archive/restore APIs | restore invariant |
| `PlacementDecision`, `PlacementDecisionReceipt`, `FailoverPlan`, `FailoverRun`, `FailoverEvent` | [`decentralized/cloud.md`](../domains/decentralized/cloud.md), [`hypervisor/byo-provider-plane.md`](../components/hypervisor/byo-provider-plane.md) | BUILT durable records (`placement-decision://pld_*`, `placement-decision-receipt://pdr_*`, `failover-plan://fpl_*`, `failover-run://for_*` with embedded events) + routes `/v1/hypervisor/placement/decisions*`, `/v1/hypervisor/failover/{plans,run,runs}`; decisions cite selected+alternatives+rejected w/ reason codes, fee_object_minted:false always this cut, eligible_future only when ≥2 real candidates; failover = resumable wallet-gated orchestration reusing provider-op/storage gates IN-PROCESS (no second mutation lane), 8 named failure conditions fail-closed, restore only after state_root validation (custody → archive 5-gate fallback), old exposure closes/warns + new opens; ledger facets placement_decision + failover (incl. triggered_by); Ops `#ops-failover` (+auto-trigger posture), Env `#env-placement-decisions` (+trigger pill), NS preview decision_mode. AUTO-TRIGGER BUILT: arm/disarm/evaluate + opt-in background evaluator (`IOI_FAILOVER_AUTO_EVALUATE_SECS`); evidence-mapped detection (declared coverage: credential_revoked/capacity_eviction/host_unreachable/storage_unavailable, uncovered conditions honestly noted), triggers cite record refs, arming fail-closed without restore material, single-shot armed→triggered (explicit re-arm), triggered runs PARK at the wallet gate — automatic detection never automatic authority; done-bar `verify-hypervisor-auto-failover-trigger.mjs` 20/20 | promote RoutingDecisionReceipt-with-fee only when routed-for-payment exists (canon); decisions remain evidence never authority (INV-7) | `crates/node/src/bin/hypervisor_daemon_routes/placement_failover_routes.rs` | `verify-hypervisor-cross-provider-failover.mjs` (27/27); provider gates green (cloud 33/33, akash 28/28, vast 26/26, byo 32/32, filecoin 27/27 fresh-state) |

## Authority, Interop, Marketplace, and Settlement Concepts

| Concept | Canonical owner | Current durable form | Promote or keep | Code anchor when known | Conformance hook |
| --- | --- | --- | --- | --- | --- |
| `AuthorityGrant` | [`wallet-network/doctrine.md`](../components/wallet-network/doctrine.md), [`wallet-network/api-authority-scopes.md`](../components/wallet-network/api-authority-scopes.md) | grant/lease/approval record | canonical authority object outside Agentgres truth | authority API and approval handlers | HP-2 |
| `PrincipalAuthorityBindingProofV1` / `PrincipalAuthorityResolutionV1` | [`wallet-network/doctrine.md`](../components/wallet-network/doctrine.md), [`wallet-network/api-authority-scopes.md`](../components/wallet-network/api-authority-scopes.md) | wallet control-root-signed immutable binding versions plus immutable principal-hash/version indexes, a CAS-updated current head, and request-correlated operation-scoped resolution/proof receipts; exact complete `ApprovalAuthority` snapshot, matched scope, snapshot hash, predecessor, status, expiry, and mutation audit commitment are bound | canonical wallet.network identity-to-authority resolver; keep proofs and version indexes append-only, reserve the final chain slot for revocation, use POST receipt-producing lookup, and require exact optional ref/version/hash pins; protocol/SDK consumers recompute the Rust-compatible JCS/SHA-256 authority snapshot hash before accepting scope | `crates/types/src/app/wallet_network/principal_authority.rs`, `crates/services/src/wallet_network/handlers/principal_authority.rs`, `packages/wallet-protocol/src/principal-authority-hash.ts`, `packages/wallet-protocol/schemas/principal-authority-*.schema.json`, `packages/wallet-sdk/src/client.ts` | wallet service tests plus `npm run check:wallet-protocol`, `npm run test:wallet-protocol`, and `npm run test:wallet-sdk`; frozen-hash scope/expiry/revocation tampering, coupled mutable-head rollback, empty/foreign scope, path/ref mismatch, local login, org roles, session identity, caller fields, copied grant fields, and TOFU never resolve authority |
| `AuthorityGrantEnvelope` v1/v2 and target v3 request-context successor | [`wallet-network/doctrine.md`](../components/wallet-network/doctrine.md), [`wallet-network/api-authority-scopes.md`](../components/wallet-network/api-authority-scopes.md), [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md) | v1 and portable signed/attenuable v2 are built registered machine contracts with shared structural fixtures and generated projections. No portable v1/v2 signature, attenuation, revocation, or freshness verifier is included in this cut. Target v3 retains v2 and binds the exact authority-request body hash, principal, externally owned product-session ref, origin, factor/guardian refs, authority-review receipt, and approval-evidence root | preserve immutable v1/v2 schemas; add v3 under a new registered ID rather than changing their field sets. Deployment-local production grants continue to use their existing owner-plane validators; a portable verifier and v3 are separate future work | built v1/v2 schemas, invariant metadata, fixtures, and generated Rust/TypeScript projections; portable verifier/CLI and v3 schema/projections/issuance/admission are not started | architecture-contract gates cover shape and projection parity only; a future portable verifier must reject payload, principal, session, origin, subject, factor/guardian, resource/destination, budget, policy, risk, holder/audience, time, revocation, parent, and signature substitution |
| wallet account identity/session, provider-neutral `AuthFactor`, passkey/WebAuthn lifecycle, binding to an externally owned product session, `GuardianSurface`, account recovery, and device lifecycle | [`wallet-network/doctrine.md`](../components/wallet-network/doctrine.md), [`wallet-network/api-authority-scopes.md`](../components/wallet-network/api-authority-scopes.md), [`wallet-network/product-exchange-risk.md`](../components/wallet-network/product-exchange-risk.md) | **target canonical; exact end-to-end contract not started:** federated login and passkeys authenticate a native wallet account and low-risk product session without conveying effect authority; WebAuthn registration/authentication binds a single-use challenge and ceremony context to type, RP ID/hash, origin/cross-origin posture, credential/public key, owner/user handle, required UP/UV, transports, discoverability, BE/BS signals, attestation policy, revocation and replacement. A passkey becomes a GuardianSurface only through explicit receipted high-assurance enrollment. Recovery and device replacement revoke or quarantine affected sessions/factors/grants and never reconstruct, preserve, or widen authority | promote as one embedded sign-in-to-effect experience over existing identity, externally owned product-session, factor, guardian, authority-review, grant/lease, daemon-admission, receipt, and Agentgres owners; keep provider brands as UI/provider refs over `federated_identity`, keep biometric samples/templates device-local, and keep authentication/recovery separate from authority | current wallet Rust/service/protocol packages are adjacent authority substrate; provider-neutral factor schema, WebAuthn begin/finish routes, wallet account/session/device inventory, recovery state machine, exact AuthorityReview successor, v3 request-context grant, and one managed-product journey remain planned | reject challenge/type/RP-hash/origin/cross-origin/credential/principal/session/userHandle/signature/required-UP-or-UV mismatch; score counter regression and BE/BS anomalies under policy rather than treating them as universal clone proof; reject factor-as-grant, unenrolled passkey-as-guardian, email-only high-risk recovery, recovery-session login/effect use, account merge by email alone, recovery-preserved grants, device replacement without revocation/quarantine, or review/result replay under another origin/session |
| `WalletAuthorityReview` | [`wallet-network/product-exchange-risk.md`](../components/wallet-network/product-exchange-risk.md), [`wallet-network/api-authority-scopes.md`](../components/wallet-network/api-authority-scopes.md) | reusable review object for Wallet app, embedded approval card, mobile sheet, CLI prompt, and advanced console; target successor additionally binds the exact request-body hash, authenticated principal, externally owned product session, origin, acting subject, resources/destination, budget, policy, risk, expiry, factor/guardian posture, and result | canonical product/API object for authority review; not a UI-only object. The checked legacy wallet-protocol schema is adjacent v1 implementation and cannot satisfy the context-bound product proof | legacy `packages/wallet-protocol/schemas/authority-review.schema.json`; successor schema/API/projections planned | review must bind intent, simulation, risk, eligibility, policy explanation, approval modes, exact context, and receipt refs; principal/session/origin/request substitution must fail before v3 issuance and effect |
| `ApprovalMode` | [`wallet-network/product-exchange-risk.md`](../components/wallet-network/product-exchange-risk.md), [`wallet-network/api-authority-scopes.md`](../components/wallet-network/api-authority-scopes.md) | enum-like policy result: one-shot, session envelope, batch, silent-within-policy, after-the-fact receipt, step-up, denied | canonical policy-derived mode; apps may request but wallet decides | planned approval API and SDK helpers | approval mode cannot widen authority beyond policy |
| `WalletPresentationProfile` | [`wallet-network/product-exchange-risk.md`](../components/wallet-network/product-exchange-risk.md), [`vocabulary.md`](./vocabulary.md) | UI profile over the same authority review contract | keep as product/API presentation hint, not authority by itself | planned Wallet SDK/render-profile helpers | presentation profile must not change policy result or receipt semantics |
| `CapabilityLease` / `CapabilityLeaseRevocation` | [`wallet-network/api-authority-scopes.md`](../components/wallet-network/api-authority-scopes.md), [`wallet-network/product-exchange-risk.md`](../components/wallet-network/product-exchange-risk.md) | scoped, expiring lease over a capability such as connector use, broker action, cloud deploy, or app permission; revocation object binds lease, initiator, holder, scope, policy hash, revocation epoch, timestamp, and receipt refs | canonical authority object family for credential/capability use without durable secret custody | `packages/wallet-protocol/src/types.ts`, `packages/wallet-protocol/schemas/capability-lease*.schema.json`, `packages/wallet-sdk/src/capabilities.ts`, `packages/wallet-sdk/src/client.ts` | agents never receive long-lived credentials by default; every use is receipted or aggregated under policy; revocation is typed and epoch-bound, not silent local mutation |
| `RiskCoverageState` | [`wallet-network/product-exchange-risk.md`](../components/wallet-network/product-exchange-risk.md), [`vocabulary.md`](./vocabulary.md) | label metadata for assessed/unknown/unassessed/stale/partially-covered/conflicting-source risk or eligibility claims | canonical metadata attached to risk/eligibility labels | planned risk registry and approval review schemas | absence of risk label is never safe; unknown/unassessed/stale must surface as caution |
| `ExchangeIntent` | [`wallet-network/product-exchange-risk.md`](../components/wallet-network/product-exchange-risk.md), [`exchange.md`](../domains/decentralized/exchange.md), [`wallet-network/api-authority-scopes.md`](../components/wallet-network/api-authority-scopes.md) | wallet authority object binding route, calldata, slippage, simulation, policy, risk labels, grant/lease, revocation epoch, economics, and exact `TxIntent` records | canonical object for wallet exchange approval/execution | planned exchange authority APIs | exchange intent binding |
| `CandidateEvidence` | [`exchange.md`](../domains/decentralized/exchange.md), [`trade.md`](../domains/decentralized/trade.md), [`cloud.md`](../domains/decentralized/cloud.md), [`wallet-network/product-exchange-risk.md`](../components/wallet-network/product-exchange-risk.md), [`@ioi/wallet-protocol`](./wallet-protocol-sdk-packaging-plan.md) | evidence envelope binding candidate id, source, adapter id, observed timestamp, expiry, coverage state, evidence refs, risk labels, eligibility labels, and claims | canonical evidence metadata for route, venue, market, cloud-resource, provider, connector, and app candidates; not authority by itself | `packages/wallet-protocol/src/types.ts`, `packages/wallet-protocol/schemas/authority-review.schema.json`, `packages/wallet-sdk/src/route-sources.ts`; planned decentralized.cloud candidate-source client | `npm run check:candidate-evidence`; stale/unknown/unassessed/conflicting candidates cannot execute silently |
| `RouteCandidate` | [`exchange.md`](../domains/decentralized/exchange.md), [`wallet-network/product-exchange-risk.md`](../components/wallet-network/product-exchange-risk.md) | candidate route from decentralized.exchange route-intelligence, direct pools, DEX routers, bridge routers, solvers, quote APIs, or user routes, carrying `CandidateEvidence` | keep as candidate/proposal until selected into `ExchangeIntent`; never authority by itself | `createDecentralizedExchangeCandidateSourceClient()` in `packages/wallet-sdk/src/route-sources.ts`; live route service remains a deployment concern | route source is not authority; missing/expired/stale candidate evidence blocks silent execution |
| `TradeIntent` | [`wallet-network/product-exchange-risk.md`](../components/wallet-network/product-exchange-risk.md), [`trade.md`](../domains/decentralized/trade.md) | wallet authority object binding venue, market, side, collateral, leverage, margin mode, order type, liquidation/funding assumptions, simulation, policy, risk labels, grant/lease, revocation epoch, and exact venue/order/TxIntent records | canonical object for advanced trade approval/execution | `createDecentralizedTradeCandidateSourceClient()` in `packages/wallet-sdk/src/route-sources.ts` for venue candidates; planned trade authority APIs remain Wallet-owned | trade intent binding; perps/margin/position actions are high-risk and agent-denied by default; prediction markets use `PredictionIntent` |
| `PositionReceipt` | [`trade.md`](../domains/decentralized/trade.md), [`wallet-network/product-exchange-risk.md`](../components/wallet-network/product-exchange-risk.md), [`events-receipts-delivery-bundles.md`](../components/daemon-runtime/events-receipts-delivery-bundles.md) | receipt over position state, risk status, policy status, liquidation/funding snapshots, PnL, close conditions, and venue metadata | receipt by default; promote to object when long-lived monitoring, dispute, portfolio risk, or agent policy needs query/conflict handling | planned trade/position APIs | position risk changes are receipted and cannot be hidden behind swap history |
| `PredictionIntent` | [`trade.md`](../domains/decentralized/trade.md), [`wallet-network/product-exchange-risk.md`](../components/wallet-network/product-exchange-risk.md) | wallet authority object for prediction markets/event contracts binding venue, market question, outcome, side, price limit, shares, max loss, max payout, resolution source, market rules, liquidity snapshot, policy, risk labels, grant/lease, revocation epoch, and venue/order records | canonical object for event exposure approval/execution | planned prediction/event-market authority APIs | prediction markets are event exposure, not swaps; agents denied live prediction authority by default |
| `PredictionReceipt` | [`trade.md`](../domains/decentralized/trade.md), [`wallet-network/product-exchange-risk.md`](../components/wallet-network/product-exchange-risk.md), [`events-receipts-delivery-bundles.md`](../components/daemon-runtime/events-receipts-delivery-bundles.md) | receipt over prediction/event-market order, risk, policy, resolution source, market rules, max loss/payout, execution, and resolution refs | receipt by default; promote to object when long-lived monitoring, resolution dispute, compliance, portfolio risk, or agent policy needs query/conflict handling | planned prediction/event-market receipt APIs | prediction risk and resolution changes are receipted and cannot be hidden behind swap or position history |
| `TxIntent` | [`wallet-network/doctrine.md`](../components/wallet-network/doctrine.md), [`wallet-network/product-exchange-risk.md`](../components/wallet-network/product-exchange-risk.md) | exact chain transaction intent bound to policy, grant/lease, revocation epoch, calldata, slippage, and simulation hash | canonical executable object for chain actions; contained by `ExchangeIntent` for swaps | planned wallet execution APIs | exact transaction approval binding |
| `WalletReceipt` / `ExchangeReceipt` | [`wallet-network/product-exchange-risk.md`](../components/wallet-network/product-exchange-risk.md), [`events-receipts-delivery-bundles.md`](../components/daemon-runtime/events-receipts-delivery-bundles.md) | human-readable and machine-verifiable binding of the declared wallet action, intent, policy, authority, and execution evidence; indexed through Agentgres when operational truth is affected | canonical action/evidence record, not automatic proof of economic correctness, finality, or user acceptance | planned wallet activity/receipt APIs | wallet receipt binding plus settlement/finality checks |
| `AssetExposureRecord` | [`wallet-network/product-exchange-risk.md`](../components/wallet-network/product-exchange-risk.md) | exposure record over asset/account cryptography, public-key exposure, bridge/admin/oracle dependency, approval exposure, agent access, policy level, and recommendations | object when displayed, queried, or used for protection policy | planned risk/exposure APIs | risk label accuracy |
| `ProtectionAction` | [`wallet-network/product-exchange-risk.md`](../components/wallet-network/product-exchange-risk.md) | proposed/approved action such as revoke approval, reduce allowance, move funds, isolate agent funds, or require step-up | receipt-backed object when consequential | planned protection APIs | protection receipt binding |
| `ApprovalInboxItem` | [`wallet-network/product-exchange-risk.md`](../components/wallet-network/product-exchange-risk.md), [`wallet-network/api-authority-scopes.md`](../components/wallet-network/api-authority-scopes.md) | pending wallet authority decision with initiator, requested action, risk classes/labels, affected assets/secrets, policy diff, simulation, expiry, and approve/edit/deny actions | product object backed by authority request/receipt | approval inbox APIs | step-up and denial receipts |
| `@ioi/wallet-protocol` | [`wallet-network/doctrine.md`](../components/wallet-network/doctrine.md), [`wallet-network/api-authority-scopes.md`](../components/wallet-network/api-authority-scopes.md), [`wallet-protocol-sdk-packaging-plan.md`](./wallet-protocol-sdk-packaging-plan.md) | checked-in package exporting wallet protocol TypeScript objects, method registry, JSON Schema, OpenAPI, fixtures, and package tests. Rust `ioi_types::app::wallet_network`, `WalletNetworkService`, wallet service tests, session-channel E2E tests, and architecture API doctrine remain the lower anchors | export from IOI-owned contracts; product repos consume it, never author it | `packages/wallet-protocol`, `crates/types/src/app/wallet_network`, `crates/services/src/wallet_network`, `crates/services/src/wallet_network/tests`, `crates/cli/tests/wallet_network_session_channel_e2e`, `scripts/check-wallet-protocol-generated.mjs` | `npm run check:wallet-protocol`; `npm run test:wallet-protocol`; protocol package must match Rust/service/conformance fixtures |
| `@ioi/wallet-sdk` | [`wallet-network/doctrine.md`](../components/wallet-network/doctrine.md), [`wallet-network/api-authority-scopes.md`](../components/wallet-network/api-authority-scopes.md), [`wallet-protocol-sdk-packaging-plan.md`](./wallet-protocol-sdk-packaging-plan.md) | checked-in typed client/helper package over `@ioi/wallet-protocol` for authority reviews, capability leases, receipts, and protocol method calls | build as a client over `@ioi/wallet-protocol`; product repos import it | `packages/wallet-sdk` | `npm run test:wallet-sdk`; SDK helpers must not invent scopes, receipts, or authority semantics outside protocol fixtures |
| `prim:*` | [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md) | primitive execution capability vocabulary | keep as vocabulary, not authority grant | runtime contracts | CIRC capability gates |
| `scope:*` | [`wallet-network/api-authority-scopes.md`](../components/wallet-network/api-authority-scopes.md) | wallet/provider authority scope vocabulary | keep as authority vocabulary | authority scope APIs | HP-2 |
| `DigitalWorkerOntology` | [`aiagent/digital-worker-ontology.md`](../domains/aiagent/digital-worker-ontology.md), [`aiagent/worker-marketplace.md`](../domains/aiagent/worker-marketplace.md) | base ontology profile for worker packages, capabilities, task/action classes, integration surfaces, policy/evidence/receipt/runtime/memory/persistence primitives, and embodied-worker safety bindings | canonical aiagent ontology profile; not a runtime, wallet, state store, or settlement layer | worker manifests, aiagent listing metadata, vertical pack refs | verticals extend ontology instead of forking daemon/runtime/authority |
| `VerticalOntologyPack` | [`aiagent/vertical-ontology-packs.md`](../domains/aiagent/vertical-ontology-packs.md), [`domain-ontologies-and-data-recipes.md`](../foundations/domain-ontologies-and-data-recipes.md) | installable versioned domain extension declaring objects, action vocabulary, connector mappings, policy profiles, risk mappings, receipt schemas, evidence requirements, benchmarks, forbidden actions, UI projection hints, and safety bindings | aiagent domain extension; does not own execution or authority | planned pack manifests and marketplace indexes | pack actions map to risk classes, scopes, receipts, and evidence coverage |
| `OntologyDevelopmentKitManifest` | [`domain-ontologies-and-data-recipes.md`](../foundations/domain-ontologies-and-data-recipes.md), [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md), [`core-clients-surfaces.md`](../components/hypervisor/core-clients-surfaces.md) | builder/conformance manifest packaging ontology refs, object models, data recipes, connector mappings, policy-bound views, surface descriptors, workflow schemas, eval refs, operator/MCP contracts, package refs, and conformance expectations | builder-kit manifest; not semantic truth, runtime truth, permission truth, marketplace truth, or settlement | planned ODK package, ODK facets, generated Domain App descriptors | generated surfaces must still register as Hypervisor Applications and bind daemon APIs, Agentgres refs, policy-bound views, authority requirements, receipts, replay, and conformance |
| `OntologySurfaceDescriptor` | [`domain-ontologies-and-data-recipes.md`](../foundations/domain-ontologies-and-data-recipes.md), [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md), [`core-clients-surfaces.md`](../components/hypervisor/core-clients-surfaces.md) | object-aware surface descriptor for views, editors, graphs, forms, review queues, consoles, dashboards, data-recipe builders, connector-mapping editors, and generated domain apps over ontology projections | descriptor/projection contract; not a local UI truth store or authority layer | planned generated surface registry, Application catalog descriptors, operator/MCP contract manifests | descriptor must declare ontology/object/recipe/policy/projection refs, daemon/API dependencies, effect boundaries, receipt obligations, and conformance profile refs |
| `IntegrationSurface` | [`aiagent/integration-surface-taxonomy.md`](../domains/aiagent/integration-surface-taxonomy.md) | policy/evidence profile for environments such as chat/community, games, SaaS, code, commerce, finance/trading, local computer-use, enterprise, webhook/API, voice/SMS, robotics, embodied systems, vehicles, field service, education, creative/media, and support/ops | integration profile; not a credential or authority grant | worker manifests, connector mappings, vertical packs | voice/SMS cannot carry durable authority; physical surfaces require safety envelopes |
| `ManagedWorkerInstanceLifecycle` | [`aiagent/managed-worker-instance-lifecycle.md`](../domains/aiagent/managed-worker-instance-lifecycle.md), [`aiagent/worker-endpoints.md`](../domains/aiagent/worker-endpoints.md) | lifecycle object over install, initialize, grant authority, bind memory profile/projection/archive refs, assign runtime, active, idle, zero-to-idle, suspend, payment past due, archive, restore, migrate, export, delete, and forget | Agentgres-backed managed-instance lifecycle truth with authority gates for restore/export/delete/decryption when required | aiagent instance endpoints, runtime assignments, Agentgres archive refs, memory archive refs | payment lapse or node failure cannot silently delete accepted user context; restore is Agentgres-operation backed |
| `ManagedAgentConsole` | [`aiagent/managed-agent-console-contract.md`](../domains/aiagent/managed-agent-console-contract.md), [`aiagent/worker-endpoints.md`](../domains/aiagent/worker-endpoints.md) | web projection over a managed worker instance for chat, sessions, approvals, receipts, usage, memory, runtime, archive/restore, and control proposals | presentation/projection only; does not execute work or own truth | aiagent managed console, daemon thread/run APIs | console controls must pass wallet/daemon gates and refresh from Agentgres projections |
| `WorkerPackage` | [`aiagent/worker-marketplace.md`](../domains/aiagent/worker-marketplace.md), [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md) | manifest/package artifact ref plus registry/listing state, harness/model/runtime support, memory profile compatibility, and projection requirements | object/listing where installed, published, or routed; package declares memory shape but managed instances own concrete memory | worker manifests, harness bindings, memory profile declarations | marketplace neutrality; no seller-owned buyer memory by default |
| `ServicePackage` | [`sas/service-marketplace.md`](../domains/sas/service-marketplace.md), [`default-harness-profile.md`](../components/daemon-runtime/default-harness-profile.md) | service manifest/package artifact ref plus optional marketplace contract | object when ordered, installed, or delivered | service APIs | HP-8 |
| `ServiceEngine` | [`default-harness-profile.md`](../components/daemon-runtime/default-harness-profile.md), [`sas/service-marketplace.md`](../domains/sas/service-marketplace.md) | runtime invocation state plus service receipts | object when long-lived or contract-bound | planned service runtime | HP-2, HP-6 |
| `MoWRoutingDecision` | [`mixture-of-workers.md`](../foundations/mixture-of-workers.md), [`marketplace-neutrality.md`](../domains/marketplace-neutrality.md) | routing receipt plus Agentgres state | object when payment/reputation/dispute applies | routing receipts | HP-8 |
| `AutonomousSystemManifestEnvelope`, `AutonomousSystemGenesisEnvelope` | [`governed-autonomous-systems.md`](../foundations/governed-autonomous-systems.md), [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md) | reusable immutable package release separated from the one-time live-system binding of release, system identity, constitution, initial profiles, governing authority, and sequence-zero commitment/state/receipt roots | **machine contract and pure proposal compiler built:** registered schemas/invariants/positive and adversarial fixtures plus generated Rust/TypeScript projections are current; `ioi-types` compiles one supplied immutable release and explicit proposed instantiation into deterministic RFC 8785 JCS bytes/root or a bounded blocker report. No package-to-genesis route, persistence, authority verification, admission, activation, or live System plane exists | `docs/architecture/_meta/schemas/architecture-contract-registry.v1.json`, generated architecture projections, and `crates/types/src/app/system_genesis.rs`; autonomous-system route and Agentgres object classes remain not started | one release produces stable distinct roots for two explicit candidate identities while package bytes remain unchanged; these are proposals only, not M1.6's two admitted stable Systems. Activation still requires future admitted genesis/lifecycle receipts |
| `AutonomousSystemConstitutionEnvelope`, `AutonomousSystemConstitutionAmendmentEnvelope` | [`governed-autonomous-systems.md`](../foundations/governed-autonomous-systems.md), [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md) | protected purpose, principals/affected parties, ontology/action roots, prohibitions, ceilings, amendment, emergency, oracle, lifecycle, shutdown, and activation refs plus an exact-root predecessor/successor amendment proposal | **machine contracts built only:** registered schemas/invariants/fixtures and generated projections exist; no public object/route family, authority verification, amendment decision admission, constitution mutation, transition, activation, or persistence exists | architecture-contract registry and generated projections; autonomous-system control APIs and Agentgres object class remain not started | ordinary upgrade must fail closed on protected target; an amendment proposal never self-authorizes or mutates a constitution; constitutional decision/activation receipts remain future work |
| `AutonomousSystemDeploymentProfileEnvelope`, `AutonomousSystemNodeMembershipEnvelope`, `AutonomousSystemFailoverProfileEnvelope`, `AutonomousSystemWriterEpochTransitionEnvelope`, `LostSuffixRecordEnvelope`, embedded `ConsequentialEffectFenceContext` | [`domain-kernels.md`](../foundations/domain-kernels.md), [`governed-autonomous-systems.md`](../foundations/governed-autonomous-systems.md), [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md) | desired topology/profile separated from observed membership/readiness; failover timing assumptions and durable continuity CAS; immutable per-System writer transitions, explicit excluded-suffix custody, rebuildable active-fence projection, and owner-derived pre-effect context | **target canonical; System authority/fencing mechanism not started:** no public System membership/control routes, per-System transition/effect validator, active-fence projection, continuity witness, lost-suffix handling, or consequential-resource PEP exists on the current base. Agentgres storage-writer epochs and replication are adjacent durability substrate and never substitute for per-System authority | no System-authority code anchor on the current base; `crates/agentgres/` supplies separate storage-writer replication/epoch precedent only | future conformance must cover transition/reload/effect proof, stale CAS/epoch, foreign membership/root/resource/effect, trusted-node derivation, caller-authored context refusal, lost-suffix handling, and estate-wide PEP coverage before any distributed-fencing implementation claim |
| `RuntimeAssignmentEnvelope` | [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md), [`domain-kernels.md`](../foundations/domain-kernels.md), [`governed-autonomous-systems.md`](../foundations/governed-autonomous-systems.md) | canonical bridge from GoalRun/task/run/claim/role/Context Cell or embodied-unit work to system, deployment, admitted member-node role, runtime/compute session, optional embodied-unit/controller/fleet-allocation bindings, state watermark, locality/partition posture, assignment lease, reassignment/fencing, duplicate-effect control, reconciliation, authority/evidence, receipts, and economics | canonical object; **planned**. Existing runtime-placement and work-run records are partial implementation precedent, not the complete same-system contract | planned runtime-assignment admission/query/reconcile API and Horizon 2B conformance | assignment never creates membership or authority; stale/partitioned placement follows policy; reassignment fences predecessors and reconciles ambiguous/non-retryable effects; fleet-of-one needs no coordination or allocation state unless a mission spans multiple units or execution-node memberships; internal assignment is native L0, while each AIIP peer admits its own local assignment |
| `OrderingAdmissionFinalityProfileEnvelope`, `OrderingFinalityRecoveryEnvelope` | [`canonical-enums.md`](../foundations/canonical-enums.md), [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md) | declared ordering/admission/finality and cryptographic-continuity contract plus typed profile-native threshold/BFT/membership/external-finality recovery transition | `OrderingAdmissionFinalityProfileEnvelope` **machine contract built only**; profile registry, admission/finality authority, recovery transition, receipt, and conformance harness are not started. Existing Agentgres is single-writer/static replication, not threshold/BFT | architecture-contract registry/generated projections; planned profile registry, recovery API, receipt, and conformance harness | replication/node count never upgrades authority/finality; recovery compare-and-swaps predecessor commitments and cannot invent authority or writer epochs |
| `OracleEvidenceProfileEnvelope`, extended `OntologyAssertionEnvelope`, `OracleEvidenceAdmissionReceipt`, `OntologyAssertionAdmissionReceipt` | [`governed-autonomous-systems.md`](../foundations/governed-autonomous-systems.md), [`domain-ontologies-and-data-recipes.md`](../foundations/domain-ontologies-and-data-recipes.md), [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md), [`events-receipts-delivery-bundles.md`](../components/daemon-runtime/events-receipts-delivery-bundles.md) | fact-class, source, dependency/correlation, freshness, aggregation, uncertainty, contradiction, verifier-path, challenge/adjudication, consequence-scope, validity, degraded/unknown, and replacement policy over attributed evidence and assertions. The oracle receipt freezes the evaluator's exact evidence/dependency graph, decision, scope, validity, authority requirement, and exact-head predecessor/result; the assertion-admission receipt separately freezes the Agentgres/domain admission, matching oracle-decision ref and scopes, and assertion head/operation | `OracleEvidenceProfileEnvelope` **machine contract built only**; evaluator, assertion/receipt contracts, admission, challenge/reversal, effect revalidation, persistence, and policy-filtered projection are not started. A generated profile projection is not external truth | architecture-contract registry/generated projections; planned Agentgres profile/assertion admission and exact-head oracle/assertion-decision chains, deterministic evaluator, evidence-dependency analysis, challenge/reversal path, effect-boundary revalidation, both receipt emissions, and policy-filtered assertion projection | reject signature/receipt/vote/consensus as sufficient external truth; reject correlated or affiliation-unknown sources counted as independent, stale evidence silently admitted, unknown/conflicting evidence projected as true, assertion/evaluator/domain-admission commitment or scope mismatch, forked decision heads, effect use of an expired/revoked/superseded decision, consequence-scope widening, later acceptance/adjudication/settlement described as higher factual accuracy, or admitted determinations without expiry/reversal |
| `LifecycleContinuityProfileEnvelope`, `LifecycleTransitionEnvelope` | [`governed-autonomous-systems.md`](../foundations/governed-autonomous-systems.md), [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md) | recovery, succession, migration/fork/adoption, authority rotation, dissolution, disposition, export, exit, and terminal decommission | **machine contracts built only:** schemas/invariants/fixtures and generated projections exist; lifecycle proposal/admission/execution APIs, authority verification, persistence, and effects are not started | architecture-contract registry/generated projections; planned lifecycle proposal/transition APIs | creator absence cannot widen purpose; every future admitted transition must bind evidence, decision, authority, state roots, disposition, and receipts |
| `IOINetworkEnrollmentEnvelope` | [`ioi-l1-mainnet.md`](../foundations/ioi-l1-mainnet.md), [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md) | stateful `ioi_compatible`, `ioi_connected`, or `ioi_secured` relationship with selected service, terms, assurance, bond/fee, renewal, suspension, obligations, and exit | **machine contract built only:** schema/invariants/fixtures and generated projections exist; no enrollment authority, persistence, activation/exit effect, deployed registry, or network service exists | architecture-contract registry/generated projections; planned `NetworkEnrollmentRegistry` and daemon transition API | compatible has no L1/fee/assurance; connected claims only selected services; secured requires current Standard DAS and named coverage |
| `StandardDASProfile` and shared-security service/bond agreements | [`ecosystem-assurance-certification-liability.md`](../foundations/ecosystem-assurance-certification-liability.md), [`ioi-l1-mainnet.md`](../foundations/ioi-l1-mainnet.md), [`ioi-l1-contract-interfaces.md`](../foundations/ioi-l1-contract-interfaces.md) | versioned conformance profile plus verifier/guardian/availability/relayer/arbitrator/ordering/finality terms, evidence, bonds, claims, and exit | profiles and contract sketches **speculative**; no L1 deployment | planned conformance harness, registries, and receipt families | service-specific assurance only; never generic verified/safe/correct/legal claim |
| `AIIPEnvelope` | [`aiip.md`](../foundations/aiip.md), [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md) | signed/sequenced packet envelope | canonical interop envelope | planned AIIP channel/router code | AIIP profile conformance |
| `AIIPChannel` | [`aiip.md`](../foundations/aiip.md) | bilaterally admitted channel/profile state between two distinct independently governed System identities | object when cross-system interop persists; same-System local/installed routing remains L0 even over a local transport | planned AIIP registry/router | AIIP profile conformance must reject equal endpoint System identities, missing endpoint enrollment, or transport-as-sovereignty inference |
| `AIIPExternalProtocolBindingEnvelope` | [`aiip.md`](../foundations/aiip.md), [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md) | versioned native/A2A/MCP/HTTP-RPC/OASF/ERC adapter mapping with explicit assurance non-equivalences | canonical profile; **planned** | planned AIIP adapter registry and conformance fixtures | remote completion/tool response/registry/evaluator state cannot silently become IOI authority, verification, acceptance, adjudication, or settlement |
| `StateTransitionCommitmentEnvelope` | [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md), [`events-receipts-delivery-bundles.md`](../components/daemon-runtime/events-receipts-delivery-bundles.md) | operational transition sequence, predecessor/operation/resulting commitments, admission proof, state/receipt roots, acting membership, and ordering/finality proof | canonical target contract; legacy local-settlement-shaped records are partial implementation precedent | planned Agentgres transition-commitment object and receipt migration | operational finalization is not economic settlement; INV-29 fields required for intelligent-blockchain classification |
| `SettlementEnvelope`, `settlement_mode` | [`canonical-enums.md`](../foundations/canonical-enums.md), [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md), [`ioi-l1-mainnet.md`](../foundations/ioi-l1-mainnet.md) | economic obligation/payment/bond record over local, bilateral, invoice, escrow, external-chain, or enrolled IOI L1 rails plus policy, enrollment, asset/account, decision, intent, and receipt refs | canonical v2 contract; broad runtime/marketplace migration **planned** | planned settlement adapter/profile registry and fail-closed enrollment checks | local domain is default; Work Credits are not payout assets; `ioi_l1` requires active selected enrollment; acceptance/adjudication remain separate |
| `NetworkServiceInvocationEnvelope` | [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md), [`ioi-l1-contract-interfaces.md`](../foundations/ioi-l1-contract-interfaces.md) | selected registry, rights/license, reputation, or finality service operation with exact service/terms/enrollment, predecessor/resulting commitments, decision, authority, and receipt | canonical target contract; **planned** | planned daemon invocation route and `NetworkServiceInvocationReceipt` | service selection is independent of fee rail; IOI invocation requires active matching enrollment; registry/reputation is not settlement |
| Marketplace listing | [`aiagent/worker-marketplace.md`](../domains/aiagent/worker-marketplace.md), [`sas/service-marketplace.md`](../domains/sas/service-marketplace.md) | listing state, manifest refs, license/contract refs | canonical marketplace object | marketplace APIs | marketplace neutrality |
| ContributionReceipt | [`marketplace-neutrality.md`](../domains/marketplace-neutrality.md), [`events-receipts-delivery-bundles.md`](../components/daemon-runtime/events-receipts-delivery-bundles.md) | receipt plus attribution graph/projection | object when rewards/reputation/disputes apply | routing/receipt paths | HP-8 |

## Build Rule

When unsure whether a concept should become a canonical object:

```text
If it only needs observability, emit an event.
If it proves an accountable transition, emit a receipt.
If it changes admitted truth, create an Agentgres operation.
If it is a rebuildable view, make it a projection.
If it points to bytes, create an ArtifactRef/PayloadRef.
If it needs lifecycle, query, replay, conflict handling, or settlement, promote it to a canonical object.
```
