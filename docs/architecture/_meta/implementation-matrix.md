# Canon Implementation Matrix

Status: canonical implementation index.
Canonical owner: this file for mapping major architecture concepts to canonical owner docs, current durable forms, object/projection status, code anchors, and conformance hooks.
Supersedes: ad hoc implementation-status tables in plans/specs when concept ownership or durable-form status conflicts.
Superseded by: none.
Last alignment pass: 2026-06-15.

## Purpose

This matrix answers the implementer question:

> **What is this concept, where is it canonically owned, and what durable form should I build today?**

Do not use this file to invent competing doctrine. If a row conflicts with its
canonical owner, update the owner doc first and then update this matrix.

Current sprint lane: typed Rust daemon-core APIs are the migration substrate;
generic command invokers and binary bridge transports are not terminal
architecture. `AgentgresRuntimeStateStore` now passes typed daemon-core APIs into
mounted hot-path cores, and StepModule execution uses
`daemonCoreWorkloadApi.runCodingToolStepModule` instead of a daemon-core command
envelope. The shared JS-authored `mockResult` command fallback is retired, and
the shared JS daemon-core command-spawn helper is deleted. L1 settlement,
external capability authority, governed improvement, cTEE Private Workspace,
worker/service package, coding-tool approval, workspace restore, runtime
Agentgres admission, approval-state, model_mount, context-policy/state-update,
and StepModule execution are mounted typed Rust-client surfaces, so command-env
compatibility can no longer authorize, admit, plan, or execute those truth
paths. The coding-tool approval-satisfaction JS event/lease gate is also
retired, and the coding-tool budget recovery/operator-control/run-cancel
command-shaped Rust owner wrappers are deleted: those runtime-control families
now expose typed runtime-control APIs plus Rust policy cores instead of bridge
request/response wrappers. Approval-required coding-tool execution
now asks Rust
`project_coding_tool_approval_satisfaction` for request/decision/lease
projection and then Rust `plan_coding_tool_approval_satisfaction` for a
positive satisfaction record. The JS store projection callback and exported JS
manifest matcher are retired; unsatisfied approval otherwise fails closed at
the governance block surface until direct Rust approval/admission projection
owns block materialization end to end. Once real Rust daemon-core APIs are wired
and verified, retire remaining JS facade scaffolding instead of preserving it as
compatibility wrappers.

Status values:

```text
canonical object
  needs stable identity, lifecycle, object heads, query, replay, or conflict handling

Agentgres operation
  should be admitted as operation-backed truth, but may not need its own object

event
  runtime-observable transition; may become receipt-backed or projected

receipt
  proof of an accountable transition, decision, invocation, or verification

projection
  rebuildable view over canonical operations, receipts, refs, or artifacts

profile
  configuration/contract applied by daemon/runtime/package

artifact ref
  Agentgres-governed reference to payload bytes held elsewhere
```

## Runtime and Harness Concepts

| Concept | Canonical owner | Current durable form | Promote or keep | Code anchor when known | Conformance hook |
| --- | --- | --- | --- | --- | --- |
| `RuntimeDaemonCoreModelMountArtifactEndpointPositiveApi` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md), [`hypervisor-kernel-substrate-migration-matrix.md`](./hypervisor-kernel-substrate-migration-matrix.md) | public model artifact import and endpoint mount/unmount now call typed `daemonCoreModelMountApi.planModelMountArtifactEndpoint`, backed by Rust `RuntimeKernelService::plan_model_mount_artifact_endpoint`, receive Rust-authored `model-artifacts` or `model-endpoints` records with artifact/endpoint hashes, authority hashes, wallet/cTEE boundary facts, and Agentgres truth evidence, and require Rust Agentgres model_mount record-state commit before returning public artifact or endpoint truth; the old `plan_model_mount_artifact_endpoint` command operation, command-dispatch arm, bridge request/response wrapper, backend marker, and JS command-envelope builder are retired; public artifact and endpoint lists now call Rust read-projection over runtime `state_dir`, replay admitted `model-artifacts` and `model-endpoints` artifact-endpoint records, merge provider-inventory/route materializations, apply Rust endpoint unmount records as removal, and filter JS-authored artifact/endpoint truth; the fail-closed JS artifact/endpoint facade, JS artifact/endpoint map mutation/readback, JS lifecycle receipt synthesis, `writeMap("model-artifacts")`, `writeMap("model-endpoints")`, local materialization, no-commit planner success, and JS-authored list truth stay retired | projection/replay cleanup only; keep JS only as protocol/client scaffolding while hosted/provider endpoint materialization, deeper binding, and stable SDK/IDE APIs move over the Rust-owned records | `crates/services/src/agentic/runtime/kernel/mod.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/artifact_endpoint.rs`, `crates/services/src/agentic/runtime/kernel/command_protocol.rs`, `crates/services/src/agentic/runtime/kernel/command_dispatch.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/read_projection/topology.rs`, `packages/runtime-daemon/src/model-mounting.mjs`, `packages/runtime-daemon/src/model-mounting/model-mount-core.mjs`, `packages/runtime-daemon/src/model-mounting/artifact-endpoint-operations.test.mjs`, `packages/runtime-daemon/src/runtime-daemon-core-direct-invoker-service.test.mjs`, `packages/runtime-daemon/src/model-mounting/read-projection-facade.test.mjs`, `scripts/conformance/hypervisor-conformance.mjs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:docs` |
| `RuntimeDaemonCoreModelMountStorageControlPositiveApi` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md), [`hypervisor-kernel-substrate-migration-matrix.md`](./hypervisor-kernel-substrate-migration-matrix.md) | public catalog import URL, model download queue/cancel, artifact delete, and storage cleanup now call typed `daemonCoreModelMountApi.planModelMountStorageControl`, backed by Rust `RuntimeKernel::plan_model_mount_storage_control`, receive Rust-authored `model-catalog-imports`, `model-downloads`, or `model-storage-controls` records with storage/download evidence, authority hashes, wallet/cTEE boundary facts, and Agentgres truth evidence, and require Rust Agentgres model_mount record-state commit before returning public storage/download truth; the fail-closed catalog/download/storage JS facade, command-envelope request builder, model_mount storage-control command operation/dispatch arm, bridge response wrapper, `daemonCoreApi` compatibility mount, JS lifecycle receipt synthesis, JS download/artifact map mutation, direct `writeMap()` storage truth, fixture/live network materialization, filesystem mutation, and no-commit planner success stay retired | projection/replay cleanup only; keep JS only as protocol/client scaffolding while richer filesystem custody, hosted catalog/download materialization, and stable SDK/IDE storage/download APIs move over the Rust-owned records | `crates/services/src/agentic/runtime/kernel/model_mount/storage_control.rs`, `crates/services/src/agentic/runtime/kernel/command_protocol.rs`, `crates/services/src/agentic/runtime/kernel/command_dispatch.rs`, `packages/runtime-daemon/src/model-mounting.mjs`, `packages/runtime-daemon/src/model-mounting/model-mount-core.mjs`, `packages/runtime-daemon/src/model-mounting/catalog-download-operations.test.mjs`, `packages/runtime-daemon/src/model-mounting/storage-operations.test.mjs`, `scripts/conformance/hypervisor-conformance.mjs` | `hypervisor-conformance:receipts`, `hypervisor-conformance:docs` |
| `RuntimeDaemonCoreModelMountProviderControlPositiveApi` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md), [`hypervisor-kernel-substrate-migration-matrix.md`](./hypervisor-kernel-substrate-migration-matrix.md) | public provider upsert now calls Rust daemon-core `plan_model_mount_provider_control`, receives a Rust-authored `model-providers` record with provider-control and authority hashes, wallet.network/cTEE no-plaintext custody facts, and Agentgres provider-control truth evidence, and requires Rust Agentgres model_mount record-state commit before returning public provider truth; Rust read-projection kind `providers` now replays admitted `model-providers` provider-control records and the mounted provider accessor consumes that projection instead of `state.providers` map truth; the fail-closed JS provider-upsert facade, provider-map mutation/lookup truth, JS vault-material resolution, `writeMap("model-providers")`, plaintext material readback, and no-commit planner success stay retired | projection/replay cleanup only; provider-control command transport is retired, and JS remains only protocol/client scaffolding over the typed Rust daemon-core provider-control API while hosted/provider materialization and stable SDK/IDE APIs move over Rust records | `crates/services/src/agentic/runtime/kernel/model_mount/provider_control.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/read_projection/topology.rs`, `crates/services/src/agentic/runtime/kernel/command_protocol.rs`, `crates/services/src/agentic/runtime/kernel/command_dispatch.rs`, `packages/runtime-daemon/src/model-mounting.mjs`, `packages/runtime-daemon/src/model-mounting/state-accessors.mjs`, `packages/runtime-daemon/src/model-mounting/model-mount-core.mjs`, `packages/runtime-daemon/src/model-mounting/provider-operations.test.mjs`, `scripts/conformance/hypervisor-conformance.mjs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:docs` |
| `RuntimeDaemonCoreModelMountCoreApi` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md), [`hypervisor-kernel-substrate-migration-matrix.md`](./hypervisor-kernel-substrate-migration-matrix.md) | model_mount route-decision, invocation admission, provider-execution admission, provider invocation/stream execution, provider lifecycle/inventory planning, instance lifecycle planning, provider-result admission, artifact-endpoint planning, storage control, route-control planning, tokenizer/required-control planning, conversation/stream planning, MCP workflow planning, server-control planning, runtime-engine planning, runtime-survey planning, read-projection planning, catalog-provider control planning, provider control planning, capability-token control planning, vault control planning, receipt-gate planning, accepted-receipt head/transition planning, and invocation receipt-binding now call typed `daemonCoreModelMountApi` methods without command envelopes; Rust `command_protocol.rs` rejects the retired command operations, `command_dispatch.rs` is deleted, the Rust bridge request/response wrappers are deleted, and conformance requires the typed API path plus absence of command `operation`/`backend` envelope fields on those calls. Backend-process, backend-lifecycle, and read-projection planning now use typed `daemonCoreModelMountApi` methods; remaining model_mount cleanup is local materialization, backend supervision/execution materialization, hosted/provider transport, OAuth/materialization, invocation authority, and stable protocol API work rather than those retired command transports. Read-projection now calls typed `daemonCoreModelMountApi.planModelMountReadProjection`, backed by Rust `RuntimeKernelService::plan_model_mount_read_projection`, and the old read-projection command operation, dispatch arm, bridge request/response wrapper, source/backend markers, and JS command-envelope builder are retired. Catalog-provider control, provider control, capability-token control, vault control, receipt-gate planning, tokenizer-required, route-control-required, tokenizer planning, conversation-state planning, stream-completion planning, and stream-cancel planning now call typed `daemonCoreModelMountApi` methods backed by Rust `RuntimeKernelService`, and the old command operations, dispatch arms, bridge wrappers, backend markers, and JS command builders are retired. The JS admission runner facade/test, command/env fallback, runner option, and JS in-flight model invocation coalescing map/receipt path are deleted; the core rejects retired command compatibility options, requires `daemonCoreModelMountApi` for migrated typed APIs, rejects generic `daemonCoreInvoker`, and no longer stores a direct-invoker compatibility shim, and preserves Rust-owned receipt refs, evidence refs, process supervision/spawn booleans, process args, inventory fields, expected heads, binding records, and projection evidence without JS synthesis. | materialization/protocol cleanup only; do not restore generic invokers, command-env fallback, binary bridge fallback, JS command-envelope request builders, command schema markers, or compatibility shims for migrated model_mount hot paths | `packages/runtime-daemon/src/model-mounting/model-mount-core.mjs`, `packages/runtime-daemon/src/model-mounting/model-mount-core.test.mjs`, `packages/runtime-daemon/src/runtime-daemon-core-direct-invoker-service.test.mjs`, `packages/runtime-daemon/src/model-mounting.mjs`, `crates/services/src/agentic/runtime/kernel/command_protocol.rs`, `crates/services/src/agentic/runtime/kernel/command_dispatch.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/{admission,artifact_endpoint,lifecycle,provider_execution,provider_result,route_control,mcp_workflow,server_control,conversation}.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/read_projection.rs`, `crates/services/src/agentic/runtime/kernel/model_mount_receipt.rs`, `scripts/conformance/hypervisor-conformance.mjs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:receipts` |
| `RuntimeDaemonCoreModelMountGenericInvokerRetired` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md), [`hypervisor-kernel-substrate-migration-matrix.md`](./hypervisor-kernel-substrate-migration-matrix.md) | model_mount core construction is typed-API-only: `daemonCoreInvoker` is rejected as a retired compatibility option, `invokeDaemonCore()` and generic invoker storage are deleted, the mounted core no longer exports the daemon-core command schema marker, route-decision defaults to `rust_model_mount_api`, and `ModelMountingState` forwards only `daemonCoreModelMountApi`. | materialization/protocol cleanup only; do not restore generic invokers, command/env fallback, binary bridge fallback, JS command-envelope request builders, command schema markers, or compatibility shims for migrated model_mount hot paths | `packages/runtime-daemon/src/model-mounting/model-mount-core.mjs`, `packages/runtime-daemon/src/model-mounting/model-mount-core.test.mjs`, `packages/runtime-daemon/src/model-mounting.mjs`, `packages/runtime-daemon/src/index.mjs`, `scripts/conformance/hypervisor-conformance.mjs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:docs` |
| `RuntimeDaemonCoreTopLevelGenericInvokerRetired` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md), [`hypervisor-kernel-substrate-migration-matrix.md`](./hypervisor-kernel-substrate-migration-matrix.md) | daemon startup and store construction no longer accept a daemon-wide generic `daemonCoreInvoker` pass-through: `startRuntimeDaemonServiceWithStore()` rejects the top-level option, `AgentgresRuntimeStateStore` rejects it if constructed directly, and the coding-tool approval policy default core is built from typed `daemonCoreApprovalApi` instead of forwarding `deps.daemonCoreInvoker`. | top-level generic invoker handle retired; do not restore `daemonCoreInvoker: options.daemonCoreInvoker`, `this.daemonCoreInvoker = options.daemonCoreInvoker`, or `daemonCoreInvoker: deps.daemonCoreInvoker` as split-brain compatibility scaffolding | `packages/runtime-daemon/src/service/runtime-daemon-service.mjs`, `packages/runtime-daemon/src/index.mjs`, `packages/runtime-daemon/src/runtime-coding-tool-approval.mjs`, `packages/runtime-daemon/src/runtime-daemon-top-level-generic-invoker.test.mjs`, `scripts/conformance/hypervisor-conformance.mjs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:docs` |
| `RuntimeDaemonCoreCodingToolApprovalCoreApi` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md), [`hypervisor-kernel-substrate-migration-matrix.md`](./hypervisor-kernel-substrate-migration-matrix.md) | coding-tool approval now mounts `codingToolApprovalCore` directly on the daemon store; the JS runner facade, runner tests, command/env selection, spawn hooks, and JS bridge response normalizers are deleted. The core calls typed `daemonCoreApprovalApi.planCodingToolApprovalManifest`, `projectCodingToolApprovalSatisfaction`, `planCodingToolApprovalSatisfaction`, and `planCodingToolApprovalBlock` methods with canonical Rust daemon-core approval requests, rejects generic `daemonCoreInvoker`, retired command compatibility options, and request aliases, and returns Rust `approval.rs` envelopes as-is without synthesizing source/backend fields, approval booleans, manifest/default plan fields, satisfaction refs, or block result/event records. The Rust approval command-protocol operations for this family are retired. The coding-tool approval policy is now a Rust-client adapter over that mounted core; satisfaction projection sends runtime `state_dir` to Rust, Rust replays admitted `agents/*.json`/`runs/*.json` truth and rejects JS `run`/`agent` candidates, while the JS store satisfaction projection callback, projection-context helper, and exported JS manifest matcher remain retired. | projection/replay cleanup only; do not restore the JS runner, generic command invoker, Rust command operations, Node bridge fallback, command-env compatibility, JS approval projection callback, JS run/agent candidate transport, JS manifest-matching truth, or JS response normalizer for authority manifest, projection, satisfaction, or block planning | `packages/runtime-daemon/src/runtime-coding-tool-approval-core.mjs`, `packages/runtime-daemon/src/runtime-coding-tool-approval-core.test.mjs`, `packages/runtime-daemon/src/runtime-coding-tool-approval-core-store.test.mjs`, `packages/runtime-daemon/src/runtime-coding-tool-approval.mjs`, `crates/services/src/agentic/runtime/kernel/approval.rs`, `crates/services/src/agentic/runtime/kernel/command_protocol.rs`, `scripts/conformance/hypervisor-conformance.mjs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:negative` |
| `RuntimeDaemonCoreApprovalStateRunnerRetired` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md), [`hypervisor-kernel-substrate-migration-matrix.md`](./hypervisor-kernel-substrate-migration-matrix.md) | approval-state public control/read now mounts `approvalStateCore` directly on the daemon store; the JS runner facade, runner tests, command/env fallback, and JS bridge response normalizers are deleted. The core calls typed `daemonCoreApprovalApi.authorizeApprovalRequest`, `planApprovalRequestStateUpdate`, `authorizeApprovalDecision`, `planApprovalDecisionStateUpdate`, `planApprovalRevokeStateUpdate`, and `projectApprovalQueue` methods with canonical Rust daemon-core approval requests, fails closed without that API, rejects generic `daemonCoreInvoker` plus retired aliases/options before Rust invocation, validates only the Rust operation kind, and returns the Rust `approval.rs` authority/projection/state-update envelope as-is without synthesizing source, backend, queue counts, authority refs, or state-update defaults. The Rust approval command-protocol operations for this family are retired, including `authorize_approval_request`. Public approval request now requires Rust request-authority issuance before state planning, and the Rust request state planner rejects missing request-authority record/hash/authority-receipt binding. Public approval request/decision/revoke state updates and queue reads now send runtime `state_dir`; Rust replays admitted `agents/*.json` and `runs/*.json` Agentgres projections, resolves target run/agent truth in daemon core, and rejects JS `agent`/`run`/`runs` candidate transport before state-update or queue truth can return. The public daemon approval surface calls the mounted core for request/decision/revoke/queue projection, no longer performs JS target lookup through `agentForThread`, `getRun`, `listRuns`, or `runs.get`, and persists only Rust-authored run/agent projections through Agentgres-gated state commits. | projection/replay cleanup only; do not restore the JS runner, generic command invoker, Rust command operations, Node bridge fallback, command-env compatibility, JS approval event append, JS caller receipt authority, JS readback truth, JS target/candidate transport, JS queue candidate transport, one-call request state planning without Rust request authority, or JS response normalizer for approval state control/read APIs | `packages/runtime-daemon/src/runtime-approval-state-core.mjs`, `packages/runtime-daemon/src/runtime-approval-state-core.test.mjs`, `packages/runtime-daemon/src/runtime-approval-surface.mjs`, `packages/runtime-daemon/src/runtime-approval-control-facade.test.mjs`, `crates/services/src/agentic/runtime/kernel/approval.rs`, `crates/services/src/agentic/runtime/kernel/command_protocol.rs`, `scripts/conformance/hypervisor-conformance.mjs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:negative` |
| `RuntimeDaemonCoreCteeRunnerRetired` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md), [`hypervisor-kernel-substrate-migration-matrix.md`](./hypervisor-kernel-substrate-migration-matrix.md) | cTEE Private Workspace now mounts `cteePrivateWorkspaceCore` directly on the daemon store; the JS runner facade, runner tests, store runner option, command/env fallback, JS receipt/evidence normalizer, generic `daemonCoreInvoker` command-envelope request builder, and Rust `execute_private_workspace_ctee_action` command operation are deleted or retired for this family. The core requires typed `daemonCoreCteeApi.executePrivateWorkspaceCteeAction`, rejects retired request aliases/options plus `daemonCoreInvoker` before Rust invocation, and returns the Rust `governed_receipt.rs` cTEE custody protocol envelope as-is without synthesizing receipt refs, evidence refs, source, or backend truth. | cTEE projection/replay cleanup only; move richer Agentgres receipt/state-root binding and cTEE projection/replay records out of JS adapter scaffolding; do not restore the JS runner, Node bridge fallback, command-env compatibility, generic command invoker, Rust command operation, or JS response normalizer for Private Workspace custody work | `packages/runtime-daemon/src/runtime-ctee-private-workspace-core.mjs`, `packages/runtime-daemon/src/runtime-ctee-private-workspace-core.test.mjs`, `packages/runtime-daemon/src/runtime-ctee-private-workspace-core-store.test.mjs`, `crates/services/src/agentic/runtime/kernel/governed_receipt.rs`, `crates/services/src/agentic/runtime/kernel/command_protocol.rs`, `scripts/conformance/hypervisor-conformance.mjs` | `hypervisor-conformance:ctee`, `hypervisor-conformance:negative` |
| `RuntimeDaemonCoreWorkerServicePackageRunnerRetired` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md), [`hypervisor-kernel-substrate-migration-matrix.md`](./hypervisor-kernel-substrate-migration-matrix.md) | worker/service package invocation admission now mounts `workerServicePackageCore` directly on the daemon store; the JS runner facade, runner tests, store runner option, command/env fallback, JS ref/envelope normalizer, generic `daemonCoreInvoker` command-envelope request builder, and Rust `admit_worker_service_package_invocation` command operation are deleted or retired for this family. The core requires typed `daemonCoreWorkerServiceApi.admitWorkerServicePackageInvocation`, fails closed without that API, rejects retired request aliases/options plus `daemonCoreInvoker`, and returns the Rust `governed_receipt.rs` admission protocol envelope as-is without synthesizing receipt refs, artifact refs, payload refs, authority refs, source, or backend truth. | package projection/replay cleanup only; move richer Agentgres receipt/state-root binding and stable IDE/CLI/SDK package admission read APIs out of JS adapter scaffolding; do not restore the JS runner, Node bridge fallback, command-env compatibility, generic command invoker, Rust command operation, or JS response normalizer for package invocation admission | `packages/runtime-daemon/src/runtime-worker-service-package-core.mjs`, `packages/runtime-daemon/src/runtime-worker-service-package-core.test.mjs`, `packages/runtime-daemon/src/runtime-worker-service-package-core-store.test.mjs`, `crates/services/src/agentic/runtime/kernel/governed_receipt.rs`, `crates/services/src/agentic/runtime/kernel/command_protocol.rs`, `scripts/conformance/hypervisor-conformance.mjs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:receipts`, `hypervisor-conformance:negative` |
| `RuntimeDaemonCoreL1SettlementRunnerRetired` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md), [`hypervisor-kernel-substrate-migration-matrix.md`](./hypervisor-kernel-substrate-migration-matrix.md) | L1 settlement admission now mounts `l1SettlementCore` directly on the daemon store; the JS runner facade, runner tests, store runner option, command/env fallback, JS ref/envelope normalizer, generic `daemonCoreInvoker` command-envelope request builder, and Rust `admit_l1_settlement_attempt` command operation are deleted or retired for this family. The core requires typed `daemonCoreGovernedAdmissionApi.admitL1SettlementAttempt`, fails closed without that API, rejects retired request aliases/options plus caller-supplied `state_root_ref`/`stateRootRef` truth and `daemonCoreInvoker` before Rust invocation, and returns the Rust `governed_admission.rs` admission protocol envelope as-is without synthesizing trigger refs, receipt refs, settlement refs, state-root refs, admission hashes, source, or backend truth. Rust `settlement.rs` derives admitted `state_root_ref` from canonical settlement/domain/trigger/receipt facts. | L1 settlement projection/replay cleanup only; move richer Agentgres receipt/state-root binding and stable IDE/CLI/SDK settlement read APIs out of JS adapter scaffolding; do not restore the JS runner, Node bridge fallback, command-env compatibility, generic command invoker, Rust command operation, JS state-root request truth, or JS response normalizer for settlement admission | `packages/runtime-daemon/src/runtime-l1-settlement-core.mjs`, `packages/runtime-daemon/src/runtime-l1-settlement-core.test.mjs`, `packages/runtime-daemon/src/runtime-l1-settlement-core-store.test.mjs`, `crates/services/src/agentic/runtime/kernel/settlement.rs`, `crates/services/src/agentic/runtime/kernel/governed_admission.rs`, `crates/services/src/agentic/runtime/kernel/command_protocol.rs`, `scripts/conformance/hypervisor-conformance.mjs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:negative` |
| `RuntimeDaemonCoreGovernedImprovementRunnerRetired` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md), [`hypervisor-kernel-substrate-migration-matrix.md`](./hypervisor-kernel-substrate-migration-matrix.md) | governed-improvement proposal admission now mounts `governedImprovementCore` directly on the daemon store; the JS runner facade, runner tests, store runner option, command/env fallback, JS response normalizer, generic `daemonCoreInvoker` command-envelope request builder, and Rust `admit_governed_runtime_improvement_proposal` command operation are deleted or retired for this family. The core requires typed `daemonCoreGovernedAdmissionApi.admitGovernedRuntimeImprovementProposal`, fails closed without that API, rejects retired proposal aliases/truth fields/options plus `daemonCoreInvoker`, and returns the Rust `governed_admission.rs` admission protocol envelope as-is without synthesizing expected heads, eval/verifier receipt refs, proposal refs, admission hashes, Agentgres operation refs, state roots, approval refs, rollback refs, source, or backend truth. | governed-improvement projection/replay cleanup only; move richer Agentgres receipt/state-root binding and stable IDE/CLI/SDK governed-improvement read APIs out of JS adapter scaffolding; do not restore the JS runner, Node bridge fallback, command-env compatibility, generic command invoker, Rust command operation, or JS response normalizer for admitted proposal truth | `packages/runtime-daemon/src/runtime-governed-improvement-core.mjs`, `packages/runtime-daemon/src/runtime-governed-improvement-core.test.mjs`, `packages/runtime-daemon/src/runtime-governed-improvement-core-store.test.mjs`, `crates/services/src/agentic/runtime/kernel/governed_admission.rs`, `crates/services/src/agentic/runtime/kernel/command_protocol.rs`, `scripts/conformance/hypervisor-conformance.mjs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:receipts`, `hypervisor-conformance:negative` |
| `RuntimeDaemonCoreAgentgresAdmissionRunnerRetired` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md), [`hypervisor-kernel-substrate-migration-matrix.md`](./hypervisor-kernel-substrate-migration-matrix.md) | runtime Agentgres admission/commit/projection/replay now mounts `runtimeAgentgresAdmissionCore` directly on the daemon store through typed `daemonCoreAgentgresApi` methods; the JS runner facade, runner tests, command/env selection, spawn hooks, generic `daemonCoreInvoker` option, JS response normalizers, and Rust Agentgres command-protocol operations are retired for this route family. The core sends typed Rust daemon-core Agentgres API requests for coding-tool result-event admission, command-stream admission, runtime thread-event admission/projection/replay, thread/turn projection, storage-write admission, and runtime run/agent/memory/subagent/artifact/model_mount state commits, rejects retired compatibility options, and returns Rust `agentgres_admission.rs`, `agentgres_protocol.rs`, `runtime_thread_event.rs`, and `coding_tool_event.rs` envelopes as-is without synthesizing refs, records, source/backend fields, storage writes, or projection truth. Runtime thread-event replay now sends runtime `state_dir` instead of JS replay event candidates, and Rust loads admitted `events/*.jsonl` Agentgres records itself. | remaining cache/protocol cleanup only; do not restore the JS runner, generic command invoker, Node bridge fallback, command-env compatibility, Agentgres command-protocol operations, JS response normalizers, JS replay-candidate transport, or JS accepted-truth mutation path for admitted Agentgres truth | `packages/runtime-daemon/src/runtime-agentgres-admission-core.mjs`, `packages/runtime-daemon/src/runtime-agentgres-admission-core.test.mjs`, `packages/runtime-daemon/src/runtime-daemon-core-direct-invoker-service.test.mjs`, `packages/runtime-daemon/src/threads/thread-persistence.mjs`, `packages/runtime-daemon/src/threads/thread-persistence.test.mjs`, `crates/services/src/agentic/runtime/kernel/agentgres_admission.rs`, `crates/services/src/agentic/runtime/kernel/agentgres_protocol.rs`, `crates/services/src/agentic/runtime/kernel/runtime_thread_event.rs`, `crates/services/src/agentic/runtime/kernel/coding_tool_event.rs`, `crates/services/src/agentic/runtime/kernel/command_protocol.rs`, `crates/services/src/agentic/runtime/kernel/command_dispatch.rs`, `scripts/conformance/hypervisor-conformance.mjs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:receipts`, `hypervisor-conformance:compositor`, `hypervisor-conformance:negative` |
| `RuntimeDaemonCoreWorkspaceRestoreCoreApi` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md), [`hypervisor-kernel-substrate-migration-matrix.md`](./hypervisor-kernel-substrate-migration-matrix.md) | workspace snapshot/restore now mounts `workspaceRestoreCore` directly on the daemon store; the JS runner facade, runner tests, command/env selection, spawn hooks, and JS response normalizers are deleted. The core calls typed `daemonCoreWorkspaceRestoreApi.planWorkspaceRestoreApplyPolicy`, `previewWorkspaceRestoreOperations`, `applyWorkspaceRestoreOperations`, `captureWorkspaceSnapshotFiles`, `projectWorkspaceSnapshotList`, `projectWorkspaceSnapshotContentPackage`, `previewWorkspaceSnapshotRestore`, and `applyWorkspaceSnapshotRestore` methods with canonical Rust daemon-core protocol requests, rejects generic `daemonCoreInvoker`, retired command compatibility options, and request aliases, and returns Rust `workspace_restore.rs` envelopes as-is without synthesizing source/backend fields, receipt refs, artifact refs, projections, restore previews, or restore apply results. The Rust workspace restore command-protocol operations for this family are retired. The workspace-snapshot surface consumes only Rust `projection`, `restore_preview`, and `restore_apply` envelopes, commits only Rust-authored restore/snapshot artifact records through Rust Agentgres artifact-state admission, and admits only Rust-authored restore/snapshot runtime events through Rust Agentgres runtime-event admission before returning public truth. | projection/replay cleanup only; do not restore the JS runner, generic command invoker, Rust command operations, Node bridge fallback, command-env compatibility, JS projection/readback truth, or JS response normalizer for workspace snapshot/restore | `packages/runtime-daemon/src/runtime-workspace-restore-core.mjs`, `packages/runtime-daemon/src/runtime-workspace-restore-core.test.mjs`, `packages/runtime-daemon/src/runtime-workspace-restore-core-store.test.mjs`, `packages/runtime-daemon/src/runtime-workspace-snapshot-surface.mjs`, `crates/services/src/agentic/runtime/kernel/workspace_restore.rs`, `crates/services/src/agentic/runtime/kernel/command_protocol.rs`, `scripts/conformance/hypervisor-conformance.mjs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:docs`, `hypervisor-conformance:negative` |
| `DefaultHarnessProfile` | [`default-harness-profile.md`](../components/daemon-runtime/default-harness-profile.md) | profile metadata, harness activation/projection records, receipts | profile; not a peer runtime | `crates/types/src/app/harness/core.rs`, `crates/types/src/app/harness/receipts.rs` | DHP-1 through DHP-8 |
| `StepModuleInvocation` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md) | shared daemon-to-kernel invocation ABI; migrated coding-tool invocation envelopes are now constructed by Rust daemon-core from canonical request facts rather than by the JS StepModule ABI helper | canonical object for every consequential step/module invocation | `crates/services/src/agentic/runtime/kernel/step_module.rs`, `crates/services/src/agentic/runtime/kernel/coding_tool_step_module.rs`, `packages/runtime-daemon/src/step-module-abi.mjs`, `packages/runtime-daemon/src/step-module-runner.mjs` | `hypervisor-conformance:abi`, `hypervisor-conformance:bridge` |
| `StepModuleResult` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md) | shared execution result, observation, receipt/ref, state-root, and projection ABI; coding-tool JS helpers no longer synthesize projection receipt refs for accepted results, so success/partial results must carry explicit Rust-owned receipt refs or fail closed through StepModuleResult validation | canonical result object for admitted module execution | `crates/services/src/agentic/runtime/kernel/step_module.rs`, `packages/runtime-daemon/src/step-module-abi.mjs`, `packages/runtime-daemon/src/runtime-event-envelopes.mjs` | `hypervisor-conformance:abi`, `hypervisor-conformance:receipts` |
| `StepModuleRouter` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md) | daemon-owned route from approved loop step to backend execution; runtime-daemon StepModule runner is `rust_workload_live` by construction, retired command/backend env selectors are absent from the runner, constructor backend/command/argv options fail closed, the dedicated `step-module-command-runner.mjs` wrapper and shared `runtime-daemon-core-command-runner.mjs` command-spawn helper are deleted, and the runner now requires typed `daemonCoreWorkloadApi.runCodingToolStepModule`; explicit constructor backend, command, command-argv, generic `daemonCoreInvoker`, command `operation`, command `backend`, or JS-supplied `invocation` selections are rejected before dispatch; the runner now sends only canonical coding-tool request facts to the Rust workload API, preserves omitted Rust results as `null`, and fails closed instead of falling back to JS-authored StepModuleResult projection; the public thread-tool HTTP route and post-edit diagnostics feedback now call the mounted coding-tool invocation surface directly, and daemon-store `invokeThreadTool()` plus retired `invokeThreadToolAsync` dispatch wrappers are gone; coding-tool invocation now also calls mounted governance, artifact, workspace-snapshot, and diagnostics-feedback surfaces directly instead of store helper pass-through wrappers; admission routes for governed improvement, external capability exits, worker/service packages, cTEE private workspace actions, and L1 settlement call their mounted Rust-backed/fail-closed surfaces directly instead of daemon-store pass-through wrappers; workflow-edit apply now calls the mounted workflow-edit surface backed by Rust workflow-edit control planning and runtime-event admission, diagnostics repair decision execution now calls the mounted diagnostics repair surface backed by Rust diagnostics repair control planning and runtime-event admission, while workspace snapshot list and workspace restore preview/apply routes still call mounted fail-closed surfaces directly instead of daemon-store compatibility wrappers | current sprint lane: finish Rust core owner boundary and retire JS facade behavior; not a peer runtime | migration boundary in `packages/runtime-daemon/src/step-module-runner.mjs`; target Rust `step_router`; remaining JS helpers must be non-authoritative protocol/API clients after route-family migration, and the deleted command-spawn helper must not be restored while direct Rust daemon-core APIs replace the current request-building facade | `hypervisor-conformance:bridge`, `hypervisor-conformance:ctee`, `hypervisor-conformance:negative` |
| `RuntimeDaemonCoreCodingToolBridgeFacadeRetirement` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md), [`hypervisor-kernel-substrate-migration-matrix.md`](./hypervisor-kernel-substrate-migration-matrix.md) | the temporary Rust Node bridge no longer defines local coding-tool response wrapper functions, `core_*` alias wrappers, bridge-local `CodingToolStepModuleCommandError` remapping, service-owner proof imports, workload-client proof imports, or a `proof_tests.rs` semantic proof module; coding-tool StepModule file patch, artifact read, result retrieval, and computer-use request-lease proof coverage now lives in Rust owner tests in `coding_tool_step_module.rs` and `coding_tool_workspace.rs`; the retired `ioi-step-module-bridge` binary and empty `ioi_step_module_bridge/mod.rs` tombstone are deleted and conformance requires both to stay absent | retired bridge cleanup only; do not restore the Node bridge, tombstone module, binary fallback, or command-transport response path | `crates/services/src/agentic/runtime/kernel/coding_tool_step_module.rs`, `crates/services/src/agentic/runtime/kernel/coding_tool_workspace.rs`, `scripts/conformance/hypervisor-conformance.mjs` | `hypervisor-conformance:bridge` |
| `RuntimeDaemonCoreBridgeDispatchTransportScope` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md), [`hypervisor-kernel-substrate-migration-matrix.md`](./hypervisor-kernel-substrate-migration-matrix.md) | the retired stdin/JSON command transport is deleted instead of preserved as a fail-closed guard; `crates/services/src/agentic/runtime/kernel/command_dispatch.rs`, `CommandTransportError`, raw stdin helpers, response framing, the old temporary bridge binary, and the empty root tombstone are absent, while `command_protocol.rs` keeps only the empty retired-operation catalog | retired command-transport cleanup only; do not restore the Node bridge, binary fallback, root tombstone, command-env path, command-dispatch module, or compatibility process transport | `crates/services/src/agentic/runtime/kernel/command_protocol.rs`, `scripts/conformance/hypervisor-conformance.mjs` | `hypervisor-conformance:bridge` |
| `RuntimeDaemonCoreThreadLifecycleWrapperRetired` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md), [`hypervisor-kernel-substrate-migration-matrix.md`](./hypervisor-kernel-substrate-migration-matrix.md) | thread control, runtime bridge thread start/control/turn, subagent record updates, and agent/thread/run create/status/delete now expose only direct Rust daemon-core request/record APIs in `policy/thread_lifecycle.rs`; `ThreadLifecycleCommandError`, lifecycle `*BridgeRequest` structs, `plan_*_state_update_response` wrappers, `rust_*_state_update_command` source markers, facade exports, and bridge-shaped owner tests are deleted. `RuntimeContextPolicyCore` normalizes these as typed API results with `rust_*_state_update_api` source defaults, while conformance guards the absence of the retired command-shaped wrapper layer. | wrapper-retirement cleanup only; do not restore command-shaped Rust owner wrappers, command/env fallbacks, binary bridge fallbacks, bridge request structs, `_command` source markers, or bridge-shaped lifecycle proof tests | `packages/runtime-daemon/src/runtime-context-policy-core.mjs`, `packages/runtime-daemon/src/runtime-context-policy-core.test.mjs`, `packages/runtime-daemon/src/runtime-agent-run-lifecycle.test.mjs`, `packages/runtime-daemon/src/runtime-subagent-surface.test.mjs`, `packages/runtime-daemon/src/runtime-thread-control-surface.test.mjs`, `packages/runtime-daemon/src/threads/thread-store.test.mjs`, `crates/services/src/agentic/runtime/kernel/policy.rs`, `crates/services/src/agentic/runtime/kernel/policy/thread_lifecycle.rs`, `scripts/conformance/hypervisor-conformance.mjs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:compositor`, `hypervisor-conformance:negative` |
| `RuntimeDaemonCoreCommandTransport` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md) | fixed migration transport for Rust daemon-core admission/control surfaces; model_mount schema/error/helper foundation now lives in Rust `model_mount/common.rs`, route-decision and invocation-admission gates plus their Rust proof tests now live in Rust `model_mount/admission.rs`, tokenizer and route-control required-record planners plus their Rust proof tests live in Rust `model_mount/required/{tokenizer,route_control}.rs`, backend-lifecycle, server-control, runtime-engine positive control, and runtime-survey capture planners plus their Rust proof tests live in Rust `model_mount/{backend_lifecycle,server_control,runtime_engine,runtime_survey}.rs`, while tokenizer/required-control direct API proof tests now live in Rust `model_mount/required.rs` and `model_mount/tokenizer.rs`, backend-process planning plus its Rust proof fixtures/tests and bridge response-envelope shaping/proof tests now live in Rust `model_mount/backend_process.rs`, accepted-receipt head/transition planning plus its Rust direct API proof fixtures/tests now live in Rust `model_mount/accepted_receipt.rs` and `model_mount_receipt.rs`, provider lifecycle, provider inventory, and instance lifecycle planning now live in Rust `model_mount/lifecycle/{provider,inventory,instance}.rs`, while their bridge request/response-envelope shaping and Rust owner proof tests now live in Rust `model_mount/lifecycle.rs`, provider execution admission plus Rust proof tests now lives in Rust `model_mount/provider_execution/admission.rs` behind the facade-only `model_mount/provider_execution.rs`, non-stream provider invocation execution plus Rust proof tests now lives in Rust `model_mount/provider_execution/invocation.rs`, native-local stream invocation planning plus Rust proof tests now lives in Rust `model_mount/provider_execution/stream.rs`, model-mount backend-process and backend-lifecycle command-response proof coverage plus tokenizer/required-control, conversation/stream, accepted-receipt head/transition, and invocation receipt-binding direct API proof coverage now lives in Rust `model_mount/admission.rs`, `model_mount/provider_execution.rs`, `model_mount/lifecycle.rs`, `model_mount/backend_process.rs`, `model_mount/required.rs`, `model_mount/conversation.rs`, `model_mount/provider_result.rs`, and `model_mount_receipt.rs` owner tests while route-decision, tokenizer/required-control, and conversation/stream command transport is retired by `model_mount_route_decision_command_transport_is_retired`, `model_mount_tokenizer_required_control_command_transport_is_retired`, and `model_mount_conversation_stream_command_transport_is_retired`; the bridge proof surface no longer imports those request types, response aliases, or bridge-named tests, provider-result admission plus its Rust proof tests now live in Rust `model_mount/provider_result.rs`, read-projection shared helper ownership plus Rust proof tests now live in Rust `model_mount/read_projection/common.rs`, read-projection adapter/workflow authorship plus its Rust proof tests now live in Rust `model_mount/read_projection/adapter_boundary.rs`, read-projection server/catalog status plus authority-snapshot authorship and Rust proof tests now live in Rust `model_mount/read_projection/status.rs` and `model_mount/read_projection/authority.rs`, receipt summary/replay read projections plus Rust proof tests now live in Rust `model_mount/read_projection/receipt.rs`, route-decision read projection plus Rust proof tests now lives in Rust `model_mount/read_projection/route_decision.rs`, health/runtime-survey receipt projections plus Rust proof tests now live in Rust `model_mount/read_projection/health.rs`, aggregate snapshot/projection envelope authorship plus Rust proof tests now lives in Rust `model_mount/read_projection/aggregate.rs`, runtime-engine read-projection replay plus Rust proof tests now live in Rust `model_mount/read_projection/runtime.rs`, topology/product-catalog default read projections plus Rust proof tests now live in Rust `model_mount/read_projection/topology.rs`, and public catalog-status readback now returns through Rust `model_mount/read_projection/status.rs` while catalog-provider OAuth session/state replay projection plus Rust proof tests now live in Rust `model_mount/read_projection/oauth.rs`, behind a facade-only `ModelMountCore` instead of in the broad model-mount kernel file; workflow-edit and diagnostics-repair admission-required direct record planning now lives in Rust `policy/admission_required.rs`, their command-response bridge wrappers are deleted, typed `daemonCoreRuntimeControlApi` methods replace the old command operations, and the bridge proof surface no longer imports those policy admission request types or carries bridge-named admission-required tests; the former temporary `ioi_step_module_bridge/policy_command.rs` delegate is deleted, projection-required command request/response shaping for skill-hook, repository-workflow, runtime-tool-catalog, and runtime-lifecycle is retired after those public projection families moved to positive Rust owner modules (`skill_hook_registry.rs`, `repository_workflow.rs`, `runtime_tool_catalog.rs`, and `runtime_lifecycle.rs`); the bridge proof surface no longer imports those projection-required request types or carries bridge-named projection-required tests; the former temporary `ioi_step_module_bridge/projection_command.rs` delegate is deleted, coding-tool approval plus approval request/decision/revoke command request/response shaping now lives in Rust `approval.rs`, and the approval command-response proof family now lives in Rust `approval.rs` owner tests while the bridge proof surface no longer imports approval request types or carries bridge-named approval tests; the former temporary `ioi_step_module_bridge/approval_command.rs` delegate is deleted, external capability authority direct protocol response shaping now lives in Rust `authority.rs`, `authorize_external_capability_exit` is removed from the command catalog/dispatch path, and wallet.network/protocol negative proof coverage now lives in Rust `authority.rs` plus `command_protocol.rs` owner tests while the bridge proof surface no longer imports authority request types or carries bridge-named authority tests; the former temporary `ioi_step_module_bridge/authority_command.rs` delegate is deleted, context-budget/coding-tool-budget/compaction/context-compaction command request/response shaping now lives in Rust `policy/context_lifecycle.rs`, and command-response proof coverage now lives in Rust `policy/context_lifecycle.rs` owner tests while the bridge proof surface no longer imports those context lifecycle request types or carries bridge-named context lifecycle tests; the former temporary `ioi_step_module_bridge/context_policy_command.rs` delegate is deleted, thread lifecycle command-shaped Rust owner wrappers are retired: `policy/thread_lifecycle.rs` exposes direct request/record APIs only, lifecycle bridge request/response wrappers and `_command` source markers are deleted, and the bridge proof surface carries only absence guards; the former temporary `ioi_step_module_bridge/thread_lifecycle_command.rs` delegate is deleted, MCP/memory command request/response shaping and command-response proof coverage now live in Rust `policy/mcp_memory.rs` owner tests while the bridge proof surface no longer imports those MCP/memory request types or carries bridge-named MCP/memory tests; the former temporary `ioi_step_module_bridge/mcp_memory_command.rs` delegate is deleted, workspace-restore protocol request/response shaping now lives in Rust `workspace_restore.rs`, and workspace snapshot capture, restore operations, restore apply-policy, public snapshot projection, content-package projection, and restore preview/apply proof coverage now lives in Rust `workspace_restore.rs` owner tests while typed `daemonCoreWorkspaceRestoreApi` calls bypass the command catalog/dispatch path; the old workspace restore command operations and former temporary `ioi_step_module_bridge/workspace_restore_command.rs` delegate are deleted, runtime-control command request/response shaping now lives in Rust `policy/coding_tool_budget_recovery.rs`, `policy/operator_control.rs`, and `policy/run_cancel.rs`, and command-response proof coverage now lives in those Rust owner tests while the bridge proof surface no longer imports those runtime-control request types or carries bridge-named runtime-control tests; the former temporary `ioi_step_module_bridge/runtime_control_command.rs` delegate is deleted, L1 settlement and governed-improvement response shaping now live in Rust `governed_admission.rs`, and their command-response proof coverage now lives in Rust `governed_admission.rs` owner tests while the bridge proof surface no longer imports governed-admission request types or carries bridge-named L1/governed-improvement tests; the former temporary `ioi_step_module_bridge/governed_admission_command.rs` delegate is deleted, and governed receipt response shaping and accepted-receipt append for cTEE private workspace and worker/service package invocation now live in Rust `governed_receipt.rs`, and their command-response proof coverage now lives in Rust `governed_receipt.rs` owner tests while the bridge proof surface no longer imports governed-receipt request types or carries bridge-named cTEE/worker-service tests; the former temporary `ioi_step_module_bridge/governed_receipt_command.rs` delegate is deleted; daemon-core command operation catalogs, typed operation identity, and command-envelope wire shape now live in Rust `command_protocol.rs`; `run_coding_tool_step_module` is retired from the daemon-core command operation catalog, the live JS StepModule runner calls typed `daemonCoreWorkloadApi.runCodingToolStepModule` without a command envelope, the dead `STEP_MODULE_OPERATIONS`, `CommandFamily`, `command_family()`, `is_step_module_operation()`, and `is_daemon_core_operation()` API is removed, and `CommandEnvelope` owns canonical `schema_version` parsing while command-protocol owner tests reject retired `schemaVersion` aliases and all live operation use of the retired StepModule command schema; Rust owner tests now carry semantic command/proof coverage and the deleted bridge proof module no longer carries per-surface `*_rejects_step_module_command_schema` duplicate tests, `CommandOperation` maps every temporary operation to Rust-owned identity before dispatch, `ValidatedCommandEnvelope` returns that typed operation, unknown operations and schema-family mismatches are rejected by Rust before bridge dispatch, the adapter-only `ioi_step_module_bridge/command_envelope.rs` wrapper and bridge-local `BridgeEnvelope` are retired, retired operation dispatch no longer has a service-kernel transport module; the deleted `ioi_step_module_bridge/command_dispatch.rs` and `ioi_step_module_bridge/bridge_dispatch.rs` paths must not be recreated; no Rust stdin/JSON transport framing, response wrapper, or typed operation dispatch module remains; worker/service package, L1 settlement, governed-improvement, external capability authority, coding-tool approval, and model_mount cores reject retired command compatibility options, while runtime context-policy/state-update core and workflow-edit admission-required refusal reject retired command compatibility and the StepModule runner no longer reads command-env selectors, rejects constructor `args`, and removes per-runner argv parsers; runtime Agentgres admission and model_mount mounted cores reject the remaining command compatibility options before Rust invocation, while coding-tool approval and workspace restore reject generic `daemonCoreInvoker` and use typed Rust daemon-core APIs; runtime context-policy core no longer imports the shared JS command invoker, rejects command/env compatibility selection, requires typed daemon-core APIs directly, rejects generic `daemonCoreInvoker`, and fails closed when the typed API is absent; the StepModule runner no longer imports the shared JS command invoker, reads no command-env compatibility selector, and requires typed `daemonCoreWorkloadApi`; runtime Agentgres admission, coding-tool approval, workspace restore, and model_mount are mounted cores without JS runner facades; the shared `runtime-daemon-core-command-runner.mjs` command-spawn helper is deleted and must not be recreated | temporary migration transport only; replace with direct Rust daemon-core protocol/API once verified | `packages/runtime-daemon/src/runtime-worker-service-package-core.mjs`, `packages/runtime-daemon/src/runtime-l1-settlement-core.mjs`, `packages/runtime-daemon/src/runtime-ctee-private-workspace-core.mjs`, `packages/runtime-daemon/src/runtime-external-capability-authority-core.mjs`, `packages/runtime-daemon/src/runtime-governed-improvement-core.mjs`, `packages/runtime-daemon/src/runtime-coding-tool-approval-core.mjs`, `packages/runtime-daemon/src/runtime-approval-state-core.mjs`, `packages/runtime-daemon/src/runtime-context-policy-core.mjs`, `packages/runtime-daemon/src/runtime-agentgres-admission-core.mjs`, `packages/runtime-daemon/src/runtime-workspace-restore-core.mjs`, `packages/runtime-daemon/src/model-mounting/model-mount-core.mjs`, `crates/services/src/agentic/runtime/kernel/model_mount/common.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/admission.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/required.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/backend_lifecycle.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/required/tokenizer.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/required/route_control.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/backend_process.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/accepted_receipt.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/lifecycle.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/lifecycle/provider.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/lifecycle/inventory.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/lifecycle/instance.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/provider_execution.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/provider_execution/admission.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/provider_execution/invocation.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/provider_execution/stream.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/provider_result.rs`, `crates/services/src/agentic/runtime/kernel/model_mount_receipt.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/read_projection/common.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/read_projection/adapter_boundary.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/read_projection/status.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/read_projection/authority.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/read_projection/receipt.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/read_projection/route_decision.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/read_projection/health.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/read_projection/aggregate.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/read_projection/runtime.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/read_projection/topology.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/read_projection/oauth.rs`, `crates/node/src/bin/ioi_step_module_bridge/mod.rs`, `crates/services/src/agentic/runtime/kernel/command_protocol.rs`, `crates/services/src/agentic/runtime/kernel/approval.rs`, `crates/services/src/agentic/runtime/kernel/governed_admission.rs`, `crates/services/src/agentic/runtime/kernel/governed_receipt.rs`, `crates/services/src/agentic/runtime/kernel/command_dispatch.rs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:ctee`, `hypervisor-conformance:receipts`, `hypervisor-conformance:negative` |
| `RuntimeDaemonCoreRuntimeCompositorCommandTransportRetired` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md), [`hypervisor-kernel-substrate-migration-matrix.md`](./hypervisor-kernel-substrate-migration-matrix.md) | task/job create/cancel state planning, task/job read projection, workflow-edit control, managed-session projection/control, workspace-change projection/control, thread-fork control, conversation-artifact projection/control, and subagent projection/control now call typed `daemonCoreRuntimeControlApi` or `daemonCoreRuntimeProjectionApi` methods backed by direct `RuntimeKernelService` APIs. The old command operations, `CommandOperation` variants, dispatch arms, command-response wrappers, command-source markers, and JS `operation`/`backend` command envelopes are retired for this hot-path family, and Rust `command_protocol.rs` proves those operation names are unknown. | command transport retired for this runtime compositor family; do not restore generic command invokers, command/env fallback, binary bridge fallback, JS response wrappers, or duplicate JS authority paths for these routes | `packages/runtime-daemon/src/runtime-context-policy-core.mjs`, `packages/runtime-daemon/src/runtime-context-policy-core.test.mjs`, `crates/services/src/agentic/runtime/kernel/mod.rs`, `crates/services/src/agentic/runtime/kernel/command_protocol.rs`, `crates/services/src/agentic/runtime/kernel/command_dispatch.rs`, `crates/services/src/agentic/runtime/kernel/policy/task_job.rs`, `crates/services/src/agentic/runtime/kernel/runtime_workflow_edit_control.rs`, `crates/services/src/agentic/runtime/kernel/runtime_managed_session_control.rs`, `crates/services/src/agentic/runtime/kernel/runtime_workspace_change_control.rs`, `crates/services/src/agentic/runtime/kernel/runtime_thread_fork_control.rs`, `crates/services/src/agentic/runtime/kernel/runtime_conversation_artifact_control.rs`, `crates/services/src/agentic/runtime/kernel/runtime_conversation_artifact_projection.rs`, `crates/services/src/agentic/runtime/kernel/runtime_subagent_control.rs`, `crates/services/src/agentic/runtime/kernel/runtime_subagent_projection.rs`, `scripts/conformance/hypervisor-conformance.mjs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:compositor`, `hypervisor-conformance:negative` |
| `RuntimeDaemonCoreCodingDiagnosticsCommandTransportRetired` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md), [`hypervisor-kernel-substrate-migration-matrix.md`](./hypervisor-kernel-substrate-migration-matrix.md) | coding-tool result envelope planning, coding-tool artifact draft planning, coding-tool artifact read projection, post-edit diagnostics feedback planning, diagnostics-repair control, diagnostics-repair retry-run planning, diagnostics-repair retry-result projection, diagnostics-repair decision projection, and diagnostics repair policy projection now call typed `daemonCoreRuntimeControlApi` or `daemonCoreRuntimeProjectionApi` methods backed by direct `RuntimeKernelService` APIs. `RuntimeContextPolicyCore` rejects generic `daemonCoreInvoker` for this family and sends no command `operation`/`backend` envelope fields; Rust `command_protocol.rs` keeps only `run_coding_tool_step_module`, rejects these retired operations as unknown, and `command_dispatch.rs` has no dispatch arms, error conversions, or response wrappers for them. The JS diagnostics repair retry/operator result helpers and stale daemon constructor wiring are deleted, so retry result truth cannot fall back to JS envelope authorship. | command transport and JS result-helper facade retired for this coding/artifact/diagnostics family; do not restore generic command invokers, command/env fallback, binary bridge fallback, JS response wrappers, command source markers, JS retry/operator result helpers, or duplicate JS authority paths for these routes | `packages/runtime-daemon/src/runtime-context-policy-core.mjs`, `packages/runtime-daemon/src/runtime-context-policy-core.test.mjs`, `packages/runtime-daemon/src/index.mjs`, `packages/runtime-daemon/src/runtime-coding-tool-invocation-surface.mjs`, `packages/runtime-daemon/src/runtime-diagnostics-repair-surface.mjs`, `packages/runtime-daemon/src/diagnostics-repair-execution.mjs`, `crates/services/src/agentic/runtime/kernel/mod.rs`, `crates/services/src/agentic/runtime/kernel/coding_tool_artifact.rs`, `crates/services/src/agentic/runtime/kernel/coding_tool_event.rs`, `crates/services/src/agentic/runtime/kernel/runtime_diagnostics_repair_control.rs`, `crates/services/src/agentic/runtime/kernel/runtime_diagnostics_repair_projection.rs`, `crates/services/src/agentic/runtime/kernel/runtime_diagnostics_repair_policy.rs`, `crates/services/src/agentic/runtime/kernel/command_protocol.rs`, `crates/services/src/agentic/runtime/kernel/command_dispatch.rs`, `scripts/conformance/hypervisor-conformance.mjs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:compositor`, `hypervisor-conformance:negative` |
| `RuntimeDiagnosticsRepairProtocolClientAliasesRetired` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md), [`hypervisor-kernel-substrate-migration-matrix.md`](./hypervisor-kernel-substrate-migration-matrix.md) | IDE diagnostics repair nodes now emit only canonical snake_case daemon protocol bodies for decision execution; SDK diagnostics restore already sends canonical request fields; and the public diagnostics repair surface rejects retired camelCase request aliases before Rust planning or runtime-event admission while forwarding the canonical protocol body into Rust diagnostics repair control planning. | diagnostics repair IDE/SDK clients are protocol clients, not alternate truth authors; do not restore camelCase decision/approval/conflict/idempotency request compatibility, JS decision truth, or local retry/operator result projection | `packages/agent-ide/src/runtime/workflow-runtime-control-nodes.ts`, `packages/agent-ide/src/runtime/workflow-runtime-control-nodes.test.ts`, `packages/runtime-daemon/src/runtime-diagnostics-repair-surface.mjs`, `packages/runtime-daemon/src/runtime-diagnostics-repair-surface.test.mjs`, `packages/agent-sdk/src/substrate-client.ts`, `packages/agent-sdk/test/sdk.test.mjs`, `scripts/conformance/hypervisor-conformance.mjs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:compositor`, `hypervisor-conformance:docs` |
| `RuntimeDaemonCoreCommandEnvelopeChildWrappers` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md), [`hypervisor-kernel-substrate-migration-matrix.md`](./hypervisor-kernel-substrate-migration-matrix.md) | approval policy child wrappers no longer own command-envelope identity or their body response shaping; admission-required policy child wrappers are retired for workflow-edit and diagnostics-repair now that typed `daemonCoreRuntimeControlApi` calls return Rust direct records and Rust rejects the old command operations; projection-required child wrappers are retired for migrated public projection families after Rust `CommandEnvelope` validation and typed `CommandOperation` dispatch; Rust `authority.rs` owns external capability direct protocol request/response shaping, `authorize_external_capability_exit` is removed from `CommandOperation`, and the former `authority_command.rs` delegate is deleted; Rust `approval.rs` owns coding-tool approval and approval state-update request/response shaping and the former `approval_command.rs` delegate is deleted, while Rust `policy/admission_required.rs` owns workflow-edit and diagnostics-repair admission-required request validation/record planning with the former `policy_command.rs` delegate deleted | temporary migration cleanup only; replace the remaining child wrappers and command transport with direct Rust daemon-core protocol/API once verified | `crates/services/src/agentic/runtime/kernel/command_protocol.rs`, `crates/services/src/agentic/runtime/kernel/authority.rs`, `crates/services/src/agentic/runtime/kernel/approval.rs`, `crates/services/src/agentic/runtime/kernel/policy/admission_required.rs`, `crates/node/src/bin/ioi_step_module_bridge/mod.rs`, `scripts/conformance/hypervisor-conformance.mjs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:negative` |
| `RuntimeDaemonCoreGovernedAdmissionCommandEnvelopeWrappers` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md), [`hypervisor-kernel-substrate-migration-matrix.md`](./hypervisor-kernel-substrate-migration-matrix.md) | governed command wrappers no longer own command-envelope identity after Rust `CommandEnvelope` validation and typed `CommandOperation` dispatch; Rust `governed_admission.rs` owns L1 settlement admission and governed runtime-improvement proposal request structs, trigger/proposal admission wrapping, response shaping, and bridge error codes with the former `ioi_step_module_bridge/governed_admission_command.rs` delegate deleted, while Rust `governed_receipt.rs` owns receipt-bearing cTEE private workspace execution and worker/service package invocation response shaping, accepted-receipt append, and receipt-bound cTEE/marketplace admission with the former `ioi_step_module_bridge/governed_receipt_command.rs` delegate deleted | temporary migration cleanup only; replace the remaining child wrappers and command transport with direct Rust daemon-core protocol/API once verified | `crates/services/src/agentic/runtime/kernel/command_protocol.rs`, `crates/services/src/agentic/runtime/kernel/approval.rs`, `crates/services/src/agentic/runtime/kernel/governed_admission.rs`, `crates/services/src/agentic/runtime/kernel/governed_receipt.rs`, `crates/node/src/bin/ioi_step_module_bridge/mod.rs`, `scripts/conformance/hypervisor-conformance.mjs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:ctee`, `hypervisor-conformance:receipts`, `hypervisor-conformance:negative` |
| `RuntimeDaemonCoreL1SettlementProductRouteEnvelope` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md), [`hypervisor-kernel-substrate-migration-matrix.md`](./hypervisor-kernel-substrate-migration-matrix.md) | L1 settlement product-route admission envelope authorship now lives in Rust `governed_admission.rs`; the Rust request carries optional `thread_id` and `agent_id`, Rust derives the admitted state-root ref, and the JS product surface only extracts canonical `attempt`, rejects retired request aliases plus caller-supplied state-root truth, resolves the thread agent, and forwards context to mounted `l1SettlementCore` instead of minting `schema_version`, `settlement_admitted`, settlement refs, trigger refs, receipt refs, state-root refs, admission hash, source, or backend locally | temporary migration cleanup only; replace the remaining JS request extractor and command-envelope core with direct Rust daemon-core settlement protocol/API once verified | `crates/services/src/agentic/runtime/kernel/settlement.rs`, `crates/services/src/agentic/runtime/kernel/governed_admission.rs`, `packages/runtime-daemon/src/runtime-l1-settlement-core.mjs`, `packages/runtime-daemon/src/runtime-l1-settlement-surface.mjs`, `scripts/conformance/hypervisor-conformance.mjs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:negative` |
| `RuntimeDaemonCoreGovernedImprovementProductRouteEnvelope` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md), [`hypervisor-kernel-substrate-migration-matrix.md`](./hypervisor-kernel-substrate-migration-matrix.md) | governed runtime-improvement product-route admission envelope authorship now lives in Rust `governed_admission.rs`; the Rust request carries optional `thread_id` and `agent_id`, and the JS product surface only extracts canonical `proposal`, rejects retired request/truth/payload alias fields, resolves the thread agent, and forwards context to mounted `governedImprovementCore` instead of minting `schema_version`, `proposal_admitted`, `mutation_executed`, proposal refs, admission hash, Agentgres operation/state roots, resulting head, approval ref, rollback ref, source, or backend locally | temporary migration cleanup only; replace the remaining JS request extractor and command-envelope core with direct Rust daemon-core governed-improvement protocol/API once verified | `crates/services/src/agentic/runtime/kernel/governed_admission.rs`, `packages/runtime-daemon/src/runtime-governed-improvement-core.mjs`, `packages/runtime-daemon/src/runtime-governed-improvement-surface.mjs`, `scripts/conformance/hypervisor-conformance.mjs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:receipts`, `hypervisor-conformance:negative` |
| `RuntimeDaemonCoreExternalCapabilityAuthorityProductRouteEnvelope` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md), [`hypervisor-kernel-substrate-migration-matrix.md`](./hypervisor-kernel-substrate-migration-matrix.md) | external capability authority product-route response envelope authorship now lives in Rust `authority.rs`; the Rust protocol request carries optional `thread_id` and `agent_id`, and the JS product surface only extracts canonical `request`, rejects retired aliases, resolves the thread agent, and forwards context to the mounted Rust core instead of minting `schema_version`, `status`, `exit_authorized`, `direct_truth_write_allowed`, authority refs, wallet.network grant refs, authority receipt refs, or authority hash locally; the JS runner normalizer is deleted, so absent Rust-authored `schema_version`, `object`, `status`, `exit_authorized`, `direct_truth_write_allowed`, wallet grant refs, authority receipts, or authority hashes remain absent rather than becoming JS compatibility truth; the daemon external capability authority core now requires typed `daemonCoreAuthorityApi.authorizeExternalCapabilityExit`, rejects retired request aliases, Rust-owned truth fields, command compatibility options, and generic `daemonCoreInvoker` before Rust invocation, and the old runner/env/command-envelope fallback remains absent | authority protocol/replay cleanup only; move richer Agentgres receipt/state-root binding and authority projection/replay records out of JS adapter scaffolding; do not restore the JS runner, Node bridge, command-env compatibility fallback, generic command invoker, Rust command operation, or JS normalizer for external effects | `crates/services/src/agentic/runtime/kernel/authority.rs`, `packages/runtime-daemon/src/runtime-external-capability-authority-core.mjs`, `packages/runtime-daemon/src/runtime-external-capability-authority-surface.mjs`, `scripts/conformance/hypervisor-conformance.mjs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:negative` |
| `RuntimeDaemonCoreWorkerServicePackageProductRouteEnvelope` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md), [`hypervisor-kernel-substrate-migration-matrix.md`](./hypervisor-kernel-substrate-migration-matrix.md) | worker/service package product-route admission envelope authorship now lives in Rust `governed_receipt.rs`; the Rust protocol request carries optional `thread_id` and `agent_id`, and the JS product surface only extracts canonical `invocation`, rejects retired request/truth fields, resolves the thread agent, and forwards context to mounted `workerServicePackageCore` instead of minting `schema_version`, `invocation_admitted`, package refs, router admission, receipt binding, accepted receipt append, Agentgres admission, projection record, receipt refs, artifact refs, payload refs, authority grant refs, source, or backend locally. The mounted core now calls typed `daemonCoreWorkerServiceApi.admitWorkerServicePackageInvocation` and the old command operation is absent from Rust `command_protocol.rs`. | projection/replay cleanup only; replace the remaining JS request extractor with stable Rust daemon protocol/API clients once package admission projection and replay APIs are verified | `crates/services/src/agentic/runtime/kernel/governed_receipt.rs`, `crates/services/src/agentic/runtime/kernel/command_protocol.rs`, `packages/runtime-daemon/src/runtime-worker-service-package-core.mjs`, `packages/runtime-daemon/src/runtime-worker-service-package-surface.mjs`, `scripts/conformance/hypervisor-conformance.mjs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:receipts`, `hypervisor-conformance:negative` |
| `RuntimeDaemonCoreCteeProductRouteEnvelope` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md), [`hypervisor-kernel-substrate-migration-matrix.md`](./hypervisor-kernel-substrate-migration-matrix.md) | cTEE Private Workspace product-route admission envelope authorship lives in Rust `governed_receipt.rs`; the Rust protocol request carries optional `thread_id` and `agent_id`, and the JS product surface only extracts canonical `action`, rejects retired request/truth fields, resolves the thread agent, and forwards context to mounted `cteePrivateWorkspaceCore` instead of minting `schema_version`, `action_executed`, invocation/receipt refs, receipt binding, accepted receipt append, Agentgres admission, projection record, receipt refs, evidence refs, source, or backend locally. The mounted core now calls typed `daemonCoreCteeApi.executePrivateWorkspaceCteeAction` and the old command operation is absent from Rust `command_protocol.rs`. | projection/replay cleanup only; replace the remaining JS request extractor with stable Rust daemon protocol/API clients once cTEE projection and replay APIs are verified | `crates/services/src/agentic/runtime/kernel/governed_receipt.rs`, `crates/services/src/agentic/runtime/kernel/command_protocol.rs`, `packages/runtime-daemon/src/runtime-ctee-private-workspace-core.mjs`, `packages/runtime-daemon/src/runtime-ctee-private-workspace-surface.mjs`, `scripts/conformance/hypervisor-conformance.mjs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:ctee`, `hypervisor-conformance:negative` |
| `RuntimeDaemonCoreApprovalWorkspaceCommandEnvelopeWrappers` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md), [`hypervisor-kernel-substrate-migration-matrix.md`](./hypervisor-kernel-substrate-migration-matrix.md) | approval and workspace-restore command wrappers no longer own command-envelope identity after Rust `CommandEnvelope` validation and typed `CommandOperation` dispatch; Rust `approval.rs` owns approval protocol request/response shaping with the old approval command operations retired, while Rust `workspace_restore.rs` now owns workspace-restore protocol request structs, response envelopes, protocol source markers, and protocol-facing error codes with the old workspace restore command operations retired and the former `workspace_restore_command.rs` delegate deleted | typed API cleanup only; do not restore child wrappers, generic command transport, or bridge-local response shaping for migrated approval or workspace restore families | `crates/services/src/agentic/runtime/kernel/command_protocol.rs`, `crates/services/src/agentic/runtime/kernel/approval.rs`, `crates/services/src/agentic/runtime/kernel/workspace_restore.rs`, `crates/node/src/bin/ioi_step_module_bridge/mod.rs`, `scripts/conformance/hypervisor-conformance.mjs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:compositor`, `hypervisor-conformance:negative` |
| `RuntimeDaemonCorePolicyLifecycleMemoryCommandEnvelopeWrappers` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md), [`hypervisor-kernel-substrate-migration-matrix.md`](./hypervisor-kernel-substrate-migration-matrix.md) | context-policy, runtime-control, thread-lifecycle, workspace-trust, MCP control/catalog, MCP serve, and thread-memory wrappers no longer own command-envelope identity after Rust `CommandEnvelope` validation and typed `CommandOperation` dispatch. Typed `daemonCoreContextLifecycleApi`, `daemonCoreRuntimeControlApi`, `daemonCoreThreadLifecycleApi`, `daemonCoreWorkspaceTrustApi`, `daemonCoreMcpApi`, and `daemonCoreThreadMemoryApi` replace the retired command operations for migrated state-update, admission-required, MCP control/catalog/serve, and memory projection/control families. Rust `command_protocol.rs` rejects the retired operations, and `command_dispatch.rs` is deleted, MCP serve response wrappers/`RuntimeMcpServeCommandError`/command-source markers are deleted, and conformance guards that JS sends canonical direct API requests without command `operation`/`backend` envelopes for those migrated hot paths. | temporary migration cleanup only; keep retired command operations deleted and move remaining non-migrated command transports plus deeper MCP/memory custody, transport, replay, projection, and stable protocol APIs to direct Rust daemon-core surfaces | `crates/services/src/agentic/runtime/kernel/command_protocol.rs`, `crates/services/src/agentic/runtime/kernel/command_dispatch.rs`, `crates/services/src/agentic/runtime/kernel/runtime_mcp_serve.rs`, `crates/services/src/agentic/runtime/kernel/policy/context_lifecycle.rs`, `crates/services/src/agentic/runtime/kernel/policy/thread_lifecycle.rs`, `crates/services/src/agentic/runtime/kernel/policy/workspace_trust.rs`, `crates/services/src/agentic/runtime/kernel/policy/mcp_memory.rs`, `packages/runtime-daemon/src/runtime-context-policy-core.mjs`, `scripts/conformance/hypervisor-conformance.mjs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:compositor`, `hypervisor-conformance:negative` |
| `RuntimeDaemonCoreAgentgresProtocolApi` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md), [`hypervisor-kernel-substrate-migration-matrix.md`](./hypervisor-kernel-substrate-migration-matrix.md) | Agentgres storage-write admission, coding-tool event admission, runtime-thread admission/projection/replay, thread/turn projection, and runtime-state commit response shaping now live in Rust protocol APIs, not generic command-envelope identity. Rust `agentgres_protocol.rs` owns body-specific protocol request structs with retired command-envelope fields denied, hard-coded Rust backend/source envelopes, storage-write admission wrapping, per-record persistence helper calls, and persisted runtime-state commit wrapping through `AgentgresAdmissionCore`; `coding_tool_event.rs` and `runtime_thread_event.rs` deny caller-supplied command/backend fields for the migrated Agentgres admission/projection protocol structs. The former Node `ioi_step_module_bridge/agentgres_command.rs` delegate stays deleted. | Agentgres command-envelope wrapper retired for this family; keep only typed Rust protocol/API callers and do not restore child wrappers, command operations, command-env compatibility, or JS command callers | `crates/services/src/agentic/runtime/kernel/command_protocol.rs`, `crates/services/src/agentic/runtime/kernel/command_dispatch.rs`, `crates/services/src/agentic/runtime/kernel/agentgres_admission.rs`, `crates/services/src/agentic/runtime/kernel/agentgres_protocol.rs`, `crates/services/src/agentic/runtime/kernel/runtime_thread_event.rs`, `crates/services/src/agentic/runtime/kernel/coding_tool_event.rs`, `crates/services/src/agentic/runtime/kernel/mod.rs`, `crates/node/src/bin/ioi_step_module_bridge/mod.rs`, `scripts/conformance/hypervisor-conformance.mjs` | `hypervisor-conformance:receipts`, `hypervisor-conformance:negative` |
| `RuntimeDaemonCoreModelMountCommandEnvelopeWrapper` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md), [`hypervisor-kernel-substrate-migration-matrix.md`](./hypervisor-kernel-substrate-migration-matrix.md) | model_mount command wrappers no longer own command-envelope identity after Rust `CommandEnvelope` validation and typed `CommandOperation` dispatch; route-decision, tokenizer-required, route-control-required, tokenizer, conversation-state, stream-completion, stream-cancel, backend-process planning, and backend-lifecycle planning command-envelope request/response shaping are retired and Rust `command_protocol.rs` now rejects `admit_model_mount_route_decision`, `plan_model_mount_tokenizer_required`, `plan_model_mount_route_control_required`, `plan_model_mount_tokenizer`, `plan_model_mount_conversation_state`, `plan_model_mount_stream_completion`, `plan_model_mount_stream_cancel`, `plan_model_mount_backend_process`, and `plan_model_mount_backend_lifecycle`; read-projection now uses typed `daemonCoreModelMountApi.planModelMountReadProjection` and its command operation is retired; accepted-receipt head/transition planning plus invocation receipt binding now call typed `daemonCoreModelMountApi` methods backed by Rust `model_mount_receipt.rs`, where the Rust receipt boundary owns StepModuleRouter admission, ReceiptBinder binding/accepted append, Agentgres admission, projection binding, and accepted-receipt transition mismatch rejection; backend-process and backend-lifecycle planning now call typed `daemonCoreModelMountApi.planModelMountBackendProcess` and `planModelMountBackendLifecycle`, backed by Rust `RuntimeKernelService::plan_model_mount_backend_process` and `plan_model_mount_backend_lifecycle`; the former `ioi_step_module_bridge/model_mount_command.rs` and `ioi_step_module_bridge/model_mount_receipt_command.rs` delegates are deleted, and `ioi_step_module_bridge/mod.rs` imports only remaining non-migrated Rust response functions directly as temporary proof/transport surface | route-decision, read-projection, accepted-receipt, receipt-binding, tokenizer, required-control, conversation/stream, backend-process, and backend-lifecycle planning command transport retired; replace remaining command transport, JS command callers, and model-mount JS facades with direct Rust daemon-core model_mount protocol/API once verified | `crates/services/src/agentic/runtime/kernel/command_protocol.rs`, `crates/services/src/agentic/runtime/kernel/model_mount.rs`, `crates/services/src/agentic/runtime/kernel/model_mount_receipt.rs`, `crates/node/src/bin/ioi_step_module_bridge/mod.rs`, `scripts/conformance/hypervisor-conformance.mjs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:receipts`, `hypervisor-conformance:negative` |
| `RuntimeDaemonCoreCodingToolCommandEnvelopeWrapper` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md), [`hypervisor-kernel-substrate-migration-matrix.md`](./hypervisor-kernel-substrate-migration-matrix.md) | coding-tool StepModule command transport is retired after Rust `CommandEnvelope` validation and typed `CommandOperation` dispatch were collapsed for this family; Rust `coding_tool_step_module.rs` now owns the direct `CodingToolStepModuleRunRequest`, deny-unknown canonical request shape, per-tool response wrappers, and workload observation shaping; the former `ioi_step_module_bridge/coding_tool_command.rs` delegate is deleted, the retired `ioi_step_module_bridge/mod.rs` tombstone is deleted, `command_dispatch.rs` is deleted, and `command_protocol.rs` rejects `run_coding_tool_step_module` as unknown; bounded subprocess execution, sanitized subprocess environments, timeout enforcement, and read-only git command execution now live in Rust `coding_tool_execution.rs`, and the former `ioi_step_module_bridge/coding_tool_helpers.rs` helper is deleted instead of remaining as proof plumbing; `file.apply_patch` workspace mutation validation, file read/write execution, diff preview hashing, workspace snapshot draft construction, and Agentgres-style transition derivation now live in Rust `coding_tool_workspace.rs`; `file.inspect` workspace path canonicalization, escape rejection, metadata, directory listing, preview reads, and preview hash derivation also now live in Rust `coding_tool_workspace.rs`; `workspace.status` and `git.diff` status/diff command planning, path containment, output hashing, git-unavailable response shaping, changed-file counting, diff preview truncation, and stat projection now live in Rust `coding_tool_workspace.rs`; `test.run` command allowlisting, command mapping, cwd/path containment, sanitized environment filtering, timeout/output bounding, output hashing, and pass/fail response shaping also now live in Rust `coding_tool_workspace.rs`; `lsp.diagnostics` command allowlisting, backend selection, node/TypeScript diagnostic execution, project context, diagnostic parsing, timeout/output bounding, output hashing, and response shaping also now live in Rust `coding_tool_workspace.rs`; migrated coding-tool workload observations emitted from `coding_tool_workspace.rs` are canonical snake_case at the Rust source, and the JS Rust-live wrapper now strips retired camelCase observation keys rather than translating them; workload dispatch, StepModuleRouter admission, receipt binding, Agentgres admission, projection binding, backend-to-projection status selection, shared successful-result construction, artifact data-plane binding, and computer-use lease receipt/evidence binding now live in Rust `coding_tool_step_module.rs`; the old `ioi_step_module_bridge/coding_tool_receipt_command.rs` and `ioi_step_module_bridge/computer_use.rs` shims are deleted instead of remaining as compatibility wrappers | command transport retired for this family; keep only typed Rust daemon-core/workload protocol/API callers and do not restore StepModule command helpers, JS command callers, or coding-tool JS authority facades | `crates/services/src/agentic/runtime/kernel/command_protocol.rs`, `crates/services/src/agentic/runtime/kernel/coding_tool_execution.rs`, `crates/services/src/agentic/runtime/kernel/coding_tool_workspace.rs`, `crates/services/src/agentic/runtime/kernel/coding_tool_step_module.rs`, `crates/client/src/workload_client/mod.rs`, `crates/node/src/bin/ioi_step_module_bridge/mod.rs`, `crates/services/src/agentic/runtime/kernel/command_dispatch.rs`, `scripts/conformance/hypervisor-conformance.mjs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:receipts`, `hypervisor-conformance:negative` |
| `RuntimeDaemonCoreModelMountCommandTransport` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md), [`hypervisor-kernel-substrate-migration-matrix.md`](./hypervisor-kernel-substrate-migration-matrix.md) | model_mount route-decision, tokenizer-required, route-control-required, tokenizer, conversation-state, stream-completion, stream-cancel, backend-process planning, and backend-lifecycle planning no longer enter command transport: JS calls typed `daemonCoreModelMountApi` methods, Rust `command_protocol.rs` rejects `admit_model_mount_route_decision`, `plan_model_mount_tokenizer_required`, `plan_model_mount_route_control_required`, `plan_model_mount_tokenizer`, `plan_model_mount_conversation_state`, `plan_model_mount_stream_completion`, `plan_model_mount_stream_cancel`, `plan_model_mount_backend_process`, and `plan_model_mount_backend_lifecycle`, and `command_dispatch.rs` is deleted. The former `ioi_step_module_bridge/model_mount_command.rs` and `ioi_step_module_bridge/model_mount_receipt_command.rs` child delegates are deleted, so receipt/state-root binding, Agentgres admission, accepted-receipt append, projection binding, transition mismatch rejection, backend-process planning, and backend-lifecycle planning cannot drift back into child Node wrappers; the service-kernel command-dispatch transport is deleted; remaining proofs stay in `command_protocol.rs` and typed Rust owner tests | route-decision, read-projection, accepted-receipt, receipt-binding, tokenizer, required-control, conversation/stream, backend-process, and backend-lifecycle planning command transport retired; replace the remaining model_mount transport with direct Rust daemon-core model_mount protocol/API once verified | `crates/node/src/bin/ioi_step_module_bridge/mod.rs`, `crates/services/src/agentic/runtime/kernel/command_dispatch.rs`, `crates/services/src/agentic/runtime/kernel/model_mount.rs`, `crates/services/src/agentic/runtime/kernel/model_mount_receipt.rs`, `packages/runtime-daemon/src/model-mounting/model-mount-core.mjs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:receipts` |
| `RuntimeDaemonCoreCodingToolCommandTransport` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md), [`hypervisor-kernel-substrate-migration-matrix.md`](./hypervisor-kernel-substrate-migration-matrix.md) | coding-tool StepModule direct API request, per-tool response wrappers, workload dispatch planning, StepModuleRouter admission, receipt binding, Agentgres admission, projection binding, backend-to-projection status selection, artifact data-plane binding, computer-use lease binding, and shared successful-result construction live in Rust `coding_tool_step_module.rs`; the former `ioi_step_module_bridge/coding_tool_command.rs` transport delegate is deleted, the retired `ioi_step_module_bridge/mod.rs` tombstone is deleted, `command_dispatch.rs` is deleted, `command_protocol.rs` rejects `run_coding_tool_step_module`, and the old bridge binary is deleted; bounded subprocess/git execution lives in Rust `coding_tool_execution.rs`, `file.apply_patch` mutation/write/transition ownership plus `file.inspect`, `workspace.status`, `git.diff`, `test.run`, and `lsp.diagnostics` workspace observation/execution ownership live in Rust `coding_tool_workspace.rs`; computer-use request-lease planning and provider registry/selection live in Rust `coding_tool_computer_use.rs`, and the old `ioi_step_module_bridge/computer_use.rs` plus `computer_use_provider.rs` bridge modules are deleted; the dedicated JS StepModule command runner and the shared JS daemon-core command runner are deleted, and the JS StepModule runner now requires typed `daemonCoreWorkloadApi.runCodingToolStepModule` directly and rejects generic `daemonCoreInvoker` instead of owning or sharing a child-process wrapper; Rust `command_protocol.rs` now has an empty daemon-core operation catalog while Rust execution ownership remains targeted at daemon-core/workload modules | command transport retired for coding-tool StepModule; keep direct Rust daemon-core coding-tool execution/admission APIs and Rust/WASM workload module execution as the only admissible path | `crates/services/src/agentic/runtime/kernel/coding_tool_execution.rs`, `crates/services/src/agentic/runtime/kernel/coding_tool_workspace.rs`, `crates/services/src/agentic/runtime/kernel/coding_tool_computer_use.rs`, `crates/services/src/agentic/runtime/kernel/coding_tool_step_module.rs`, `crates/services/src/agentic/runtime/kernel/command_dispatch.rs`, `crates/node/src/bin/ioi_step_module_bridge/mod.rs`, `packages/runtime-daemon/src/runtime-coding-tool-invocation-surface.mjs`, `packages/runtime-daemon/src/step-module-runner.mjs`, `crates/client/src/workload_client/mod.rs`, `crates/services/src/agentic/runtime/kernel/step_router.rs`, `crates/services/src/agentic/runtime/kernel/receipt_binder.rs`, `crates/services/src/agentic/runtime/kernel/agentgres_admission.rs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:receipts`, `hypervisor-conformance:negative` |
| `RuntimeDaemonCoreAuthorityCommandTransport` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md), [`hypervisor-kernel-substrate-migration-matrix.md`](./hypervisor-kernel-substrate-migration-matrix.md) | external capability exit authority request/response shaping now lives in Rust `authority.rs` as a direct protocol response; the former `ioi_step_module_bridge/authority_command.rs` delegate is deleted, JS `externalCapabilityAuthorityCore` requires typed `daemonCoreAuthorityApi.authorizeExternalCapabilityExit`, and `authorize_external_capability_exit` is removed from `CommandOperation`, `DAEMON_CORE_OPERATIONS`, and `command_dispatch.rs` with Rust owner tests proving it has no command schema. | authority command transport retired for this family; keep remaining authority projection/replay, richer Agentgres receipt/state-root binding, and stable IDE/CLI/SDK authority read APIs on the Rust roadmap without restoring the command operation | `crates/services/src/agentic/runtime/kernel/authority.rs`, `crates/services/src/agentic/runtime/kernel/command_protocol.rs`, `crates/services/src/agentic/runtime/kernel/command_dispatch.rs`, `packages/runtime-daemon/src/runtime-external-capability-authority-core.mjs`, `packages/runtime-daemon/src/runtime-external-capability-authority-core-store.test.mjs`, `scripts/conformance/hypervisor-conformance.mjs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:negative` |
| `RuntimeDaemonCoreAgentgresCommandTransportRetired` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md), [`hypervisor-kernel-substrate-migration-matrix.md`](./hypervisor-kernel-substrate-migration-matrix.md) | Agentgres storage-write admission, runtime-state commits, coding-tool event admission, command-stream admission, runtime-thread event admission, runtime-thread event projection/replay, and thread/turn projection are removed from `DAEMON_CORE_OPERATIONS`, `CommandOperation`, and `command_dispatch.rs`; Rust owner tests prove those operation names now return no command schema, and JS `runtimeAgentgresAdmissionCore` rejects generic `daemonCoreInvoker` while requiring typed `daemonCoreAgentgresApi`. | command transport retired for migrated Agentgres hot paths; do not restore Rust command operations, broad bridge dispatch, generic JS command invoker, or command-env fallback | `crates/services/src/agentic/runtime/kernel/command_protocol.rs`, `crates/services/src/agentic/runtime/kernel/command_dispatch.rs`, `crates/services/src/agentic/runtime/kernel/agentgres_protocol.rs`, `packages/runtime-daemon/src/runtime-agentgres-admission-core.mjs`, `scripts/conformance/hypervisor-conformance.mjs` | `hypervisor-conformance:receipts`, `hypervisor-conformance:negative` |
| `RuntimeAgentServiceCommandTransport` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md) | retired JS RuntimeAgentService command adapter, `RuntimeApiBridge` adapter class/factory export, `ioi-runtime-bridge` Cargo bin, bridge env policy overrides, deleted runtime-service helper, and retired runtime-service proof scripts/tests are deleted; daemon startup now rejects `runtimeBridge`, and runtime-service bridge projection builders plus derived-event injectors remain absent | retired JS command/binary bridge substrate; replace any future runtime-service execution and replay with stable Rust daemon-core protocol/API instead of reviving Node command transport, env bridge fallback, binary bridge fallback, or JS bridge readback projection | `packages/runtime-daemon/src/runtime-api-bridge.mjs`, `packages/runtime-daemon/src/index.mjs`, `packages/runtime-daemon/src/service/runtime-daemon-service.mjs`, `crates/node/Cargo.toml`, `scripts/lib/autopilot-runtime-agent-service-inference.mjs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:compositor` |
| `HypervisorKernelSubstrateMigration` | [`hypervisor-kernel-substrate-migration-matrix.md`](./hypervisor-kernel-substrate-migration-matrix.md) | route-family owner map, current sprint lane, remaining terminal blockers, and conformance command state | migration matrix until terminal conformance, then historical implementation record | `scripts/conformance/hypervisor-conformance.mjs`, `package.json` | `hypervisor-conformance` |
| `HypervisorOS` | [`hypervisoros.md`](../components/daemon-runtime/hypervisoros.md) | bare-metal node profile, boot profile, node measurement receipts, daemon-rooted workload policy | profile; not a peer runtime and not a privacy substitute for cTEE | planned | daemon root rule; no unmanaged workload bypass; boot measurement receipt present |
| `HypervisorOSNode` | [`hypervisoros.md`](../components/daemon-runtime/hypervisoros.md) | runtime node object/projection plus boot profile refs, daemon refs, cTEE policy refs, and receipt requirements | promote when node is persistent, billable, restorable, or settlement/dispute eligible | planned | Hypervisor Daemon is node root; forbidden bypasses enforced |
| `HypervisorOSBootProfile` | [`hypervisoros.md`](../components/daemon-runtime/hypervisoros.md) | profile manifest binding image/kernel/initrd/daemon/package/driver hashes and secure-boot/TPM posture | profile; object if managed fleet/update/rollback lifecycle needs identity | planned | signed updates and rollback policy checked |
| `HypervisorOSBootReceipt` / `NodeMeasurementReceipt` | [`hypervisoros.md`](../components/daemon-runtime/hypervisoros.md), [`events-receipts-delivery-bundles.md`](../components/daemon-runtime/events-receipts-delivery-bundles.md) | receipt binding boot epoch, image hash, daemon hash, driver/package manifest hashes, measurement method, and privacy claim | receipt; never enough to claim consumer-GPU plaintext privacy without cTEE/TEE | planned | integrity receipt present; privacy claim cannot exceed measurement profile |
| `HypervisorFleet` | [`fleet.md`](../components/hypervisor/fleet.md) | general infrastructure manager substrate plus IDE and console.ioi.ai projections, with autonomous systems as first-class workload | substrate/control-plane projection; not runtime, authority, truth, or storage | planned | Fleet manages infrastructure posture; daemon executes; wallet.network authorizes; Agentgres records truth |
| `CloudRoute` | [`fleet.md`](../components/hypervisor/fleet.md), [`cloud-parked-future.md`](../domains/decentralized/cloud-parked-future.md) | Hypervisor/Fleet object for routing a workload to compute, storage, GPU, network, confidential compute, DePIN, customer cloud, local, or provider infrastructure with authority refs, privacy posture, budget, attestation requirements, and risk labels | projection by default; canonical object when spend, secret release, provider account use, replay, billing, dispute, migration, or private workspace placement needs identity | planned cloud/provider route adapters | cloud route cannot authorize spend, release secrets, or claim privacy without wallet.network, cTEE/TEE/local/customer-cloud posture, Agentgres refs, and receipts |
| `CloudCandidate` | [`fleet.md`](../components/hypervisor/fleet.md), [`cloud-parked-future.md`](../domains/decentralized/cloud-parked-future.md) | resource route candidate from direct provider connector, local inventory, customer cloud, DePIN market, decentralized storage network, hyperscaler, cloud GPU provider, enterprise cluster, or user route | keep as candidate/proposal until selected into approved `CloudRoute`; never authority by itself | planned provider connector APIs | cloud candidate is not execution, authority, storage truth, privacy proof, or provider-selection approval |
| `FleetNode` | [`fleet.md`](../components/hypervisor/fleet.md) | node/provider/runtime inventory projection with authority refs, daemon refs, Agentgres domain refs, status, cTEE posture, storage posture, cost/provider metadata, and receipt refs | projection by default; canonical object when fleet lifecycle, billing, restore, or policy requires identity | planned | node status links to daemon heartbeat or provider connector and receipt refs |
| `FleetWorkloadPrimitive` | [`fleet.md`](../components/hypervisor/fleet.md) | VM/container/microVM/WASM/image/volume/network/snapshot/backup/restore/GPU-pool/node-pool/migration/provider-connector lifecycle projection with authority refs, Agentgres operation refs, and receipts | projection by default; promote when lifecycle, billing, restore, migration, authority, or dispute needs identity | planned | infrastructure lifecycle actions still require authority, daemon/provider boundary, Agentgres refs, and receipts |
| `ClassicalInfraPrimitive` | [`fleet.md`](../components/hypervisor/fleet.md), [`hypervisoros.md`](../components/daemon-runtime/hypervisoros.md) | traditional infrastructure object such as VM, container, microVM, WASM workload, image, volume, network, snapshot, backup, restore point, node pool, GPU pool, quota, lease, health/log/metric/cost stream, provider connector, or migration plan | keep as vocabulary/category unless an object has lifecycle/admission needs | planned | cannot split classical infra management from autonomous runtime governance when consequential |
| `FleetRuntimeAssignmentView` | [`fleet.md`](../components/hypervisor/fleet.md) | projection over runtime assignments, placement reasons, authority refs, Agentgres operation refs, receipt refs, workspace/run refs, and status | projection; promote only if assignment lifecycle needs query/conflict handling beyond source objects | planned | placement cannot bypass wallet.network, daemon execution, or Agentgres admission |
| `FleetStoragePosture` | [`fleet.md`](../components/hypervisor/fleet.md), [`storage-backends/doctrine.md`](../components/storage-backends/doctrine.md), [`agentgres/artifact-ref-plane.md`](../components/agentgres/artifact-ref-plane.md) | storage-backend availability/posture projection over Agentgres artifact refs and backend metadata | projection; storage backend does not own artifact meaning | planned | storage availability cannot be treated as restore validity without Agentgres refs/receipts |
| `FleetPlacementDecision` | [`fleet.md`](../components/hypervisor/fleet.md) | placement projection/object for node/provider/workload/workspace/model/worker/service routing with cost, latency, data locality, cTEE posture, model availability, authority, and policy reasons | projection by default; canonical object when approval, replay, dispute, billing, or migration requires identity | planned | placement cannot become execution or authority; approved actions route through daemon/provider boundary and wallet.network |
| `ProviderConnector` | [`fleet.md`](../components/hypervisor/fleet.md), [`connector-and-tool-contracts.md`](../components/connectors-tools/contracts.md) | connector profile for cloud, DePIN, storage, Kubernetes, KubeVirt, VMware, Proxmox, Nutanix, HypervisorOS, or customer environments | connector/profile; canonical object when installed, billed, policy-governed, or dispute-relevant | planned | provider API actions are approved daemon/provider-connector actions with receipts |
| `GpuPool` | [`fleet.md`](../components/hypervisor/fleet.md) | accelerator-pool projection over nodes/providers, utilization, model routes, leases, cost, authority, and cTEE posture | projection; canonical object when allocation, billing, or settlement needs identity | planned | GPU placement cannot imply cTEE privacy or model-weight custody guarantees without matching profile receipts |
| `PrivateWorkspaceCtee` | [`private-workspace-ctee.md`](../components/daemon-runtime/private-workspace-ctee.md) | private workspace profile, daemon routing policy, encrypted refs/blobs, receipts | workspace/execution profile; not a hardware TEE claim | planned | private workspace conformance |
| `PrivateWorkspaceNode` | [`private-workspace-ctee.md`](../components/daemon-runtime/private-workspace-ctee.md) | runtime assignment + Agentgres refs + wallet policy | object/projection when node is persistent, billable, or restorable | planned | no-plaintext protected classes |
| `PrivateWorkspaceCapsule` | [`private-workspace-ctee.md`](../components/daemon-runtime/private-workspace-ctee.md) | task/workspace capsule envelope plus artifact refs and receipts | promote when replay/dispute/routing needs identity | planned | private workspace capsule checks |
| `PlaintextFreeRuntimeMounting` | [`private-workspace-ctee.md`](../components/daemon-runtime/private-workspace-ctee.md) | daemon mount discipline for tools, shells, filesystems, model servers, and model calls | profile/discipline; specialize as concrete mount/view objects where audit or replay needs identity | planned | no protected plaintext in untrusted runtime mounts |
| `PlaintextFreeModelMount` | [`private-workspace-ctee.md`](../components/daemon-runtime/private-workspace-ctee.md) | model mount profile/view, encrypted refs, redacted projections, private handles, `ModelMountReceipt` | promote mount/view when replay, dispute, leakage accounting, or model-route audit needs identity | planned | no protected plaintext in model context; mount receipt present |
| `CustodyType` | [`private-workspace-ctee.md`](../components/daemon-runtime/private-workspace-ctee.md) | type discipline for whether plaintext may be public, redacted, sealed, guardian-only, crypto-operator-only, capability-only, or never-remote-plaintext | compiler discipline by default; persist manifest hash in proof-bearing runs | planned | every edge into rented node has admissible custody type |
| `CustodyProof` | [`private-workspace-ctee.md`](../components/daemon-runtime/private-workspace-ctee.md) | verifier-facing proof object binding custody derivation, mount graph, lattice commitments, receipts, leakage budget, and state roots | required when acceptance/dispute/restore/settlement relies on no-plaintext-custody claim | planned | proof verifies against policy, receipts, and state roots |
| `PrivateAgencyTransform` | [`private-workspace-ctee.md`](../components/daemon-runtime/private-workspace-ctee.md) | reduction from secret-bearing agent step to public proposal plus private selection/declassification/authorization | compiler strategy by default | planned | protected task is candidate-selection-reducible or routed away from rented node |
| `CandidateCoverageProfile` | [`private-workspace-ctee.md`](../components/daemon-runtime/private-workspace-ctee.md) | scheduler profile for redundancy mass, redundancy phase, coverage target, candidate trace budget, public token budget, schedule, and fallback | promote when CLPD/CLE claims depend on measured coverage | planned | lattice width/depth chosen from coverage frontier, not guesswork |
| `CounterfactualLatticeExecution` | [`private-workspace-ctee.md`](../components/daemon-runtime/private-workspace-ctee.md) | high-assurance CLPD schedule that expands committed public lattices before private selection feedback | strategy when leakage budget requires lower online selection leakage | planned | lattice committed before private selection feedback |
| `CounterfactualLatticeReceipt` | [`private-workspace-ctee.md`](../components/daemon-runtime/private-workspace-ctee.md), [`events-receipts-delivery-bundles.md`](../components/daemon-runtime/events-receipts-delivery-bundles.md) | receipt binding candidate lattice commitment, width/depth/token budget, generation/dedupe/padding rules, node ref, and state root | receipt when counterfactual selection-leakage claims matter | planned | cannot claim zero online branch-selection leakage without receipt |
| `ExecutionPrivacyPosture` | [`private-workspace-ctee.md`](../components/daemon-runtime/private-workspace-ctee.md) | posture label for private-native, redacted-API, provider-trust, or unsafe execution paths | required for workers/services/outcomes that may touch private workspace data | planned | private-native not claimed when sensitive plaintext reaches third-party model API |
| `CryptographicOperatorPlane` | [`private-workspace-ctee.md`](../components/daemon-runtime/private-workspace-ctee.md) | internal cTEE routing plane for FHE/MPC/garbled/ORAM/local/threshold protected subcomputations | discipline by default; concrete policy/receipt when protected operator execution needs audit | planned | protected work not plaintext on node; authority surface is default second party |
| `CryptographicOperatorPolicy` | [`private-workspace-ctee.md`](../components/daemon-runtime/private-workspace-ctee.md) | policy object with allowed operator families, fallback order, second-party refs, leakage budget, and receipt requirements | promote when workspace or service package relies on private operators | planned | operator policy exists before protected operator routing |
| `PrivateOperatorReceipt` | [`private-workspace-ctee.md`](../components/daemon-runtime/private-workspace-ctee.md), [`events-receipts-delivery-bundles.md`](../components/daemon-runtime/events-receipts-delivery-bundles.md) | receipt binding operator family, protected input commitments, second party, output commitment, leakage profile, and no-plaintext claim | receipt by default; proof object when dispute/replay needs it | planned | protected private-operator result is not admitted without receipt |
| `CandidateLatticePrivateDecoding` | [`private-workspace-ctee.md`](../components/daemon-runtime/private-workspace-ctee.md) | default execution strategy, candidate lattice commitment, leakage budget fields, `PrivateInferenceReceipt` | strategy by default; promote candidate lattice only when replay, dispute, or verification needs identity | planned | no protected plaintext on node; candidate commitment and leakage budget present |
| `DeterrenceDetectionProfile` | [`private-workspace-ctee.md`](../components/daemon-runtime/private-workspace-ctee.md) | profile plus `DeterrenceDetectionReceipt`/`CanaryTripReceipt` evidence | profile/receipt; object when marketplace dispute, slashing, or provider attribution needs lifecycle | planned | synthetic canaries only; detection never authorizes plaintext mounting |
| `AutonomyLease` | [`private-workspace-ctee.md`](../components/daemon-runtime/private-workspace-ctee.md), [`wallet-network/doctrine.md`](../components/wallet-network/doctrine.md) | wallet authority lease plus policy receipt | canonical authority object when offline autonomy is allowed | planned | capability-exit checks |
| `AccessPointBinding` / `StepUpChallenge` | [`wallet-network/doctrine.md`](../components/wallet-network/doctrine.md) | wallet binding for SMS/email/chat/voice/webhook access points plus short-lived challenge pointer | canonical authority object for persistent agent access rails; challenge is event/receipt-backed pointer, not a grant | planned | low-assurance channels cannot decrypt, declassify, hold grants, release secrets, or approve high-risk actions without step-up |
| `IntentContract` | [`default-harness-profile.md`](../components/daemon-runtime/default-harness-profile.md) | run request, resolver receipt, operation payload | promote when multiple components query/rebase it | `crates/services/src/agentic/runtime/service/decision_loop/intent_resolver/instruction_contract.rs` | CIRC, DHP-1 |
| `RuntimePlan` | [`default-harness-profile.md`](../components/daemon-runtime/default-harness-profile.md) | plan receipt or projection | promote when plan epochs/replay matter | `crates/types/src/app/harness/core.rs` | DHP-1, DHP-4 |
| `ContextTopology` | [`default-harness-profile.md`](../components/daemon-runtime/default-harness-profile.md) | planner projection plus receipts | promote when repartition/replay/cross-actor routing needs object identity | planned | DHP-4 |
| `ContextChamber` | [`default-harness-profile.md`](../components/daemon-runtime/default-harness-profile.md) | scoped runtime state, context events, projections | promote when chamber state outlives one turn or is shared | runtime state and compaction paths | DHP-4 |
| `LoopStep` / `LoopIteration` | [`default-harness-profile.md`](../components/daemon-runtime/default-harness-profile.md) | event, receipt, trace segment | promote when loop-level replay/verification is first-class | `crates/services/src/agentic/runtime/service/decision_loop/mod.rs` | DHP-2, DHP-3 |
| `ModelPass` | [`default-harness-profile.md`](../components/daemon-runtime/default-harness-profile.md), [`events-receipts-delivery-bundles.md`](../components/daemon-runtime/events-receipts-delivery-bundles.md) | model invocation receipt plus redacted trace | keep as receipt/trace unless lineage query needs object | cognition and model-router paths | CEC, DHP-6 |
| `RuntimeModelRouteSelection` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md) | explicit runtime model-route selection and run-request model override now call the mounted Rust-owned `model_mount.route.select` route-control client, return only the Rust-authored route decision, accepted route-selection receipt, route-control record refs, and canonical runtime model-route fields, and still fail closed when the Rust route-control client is unavailable; JS route-selection receipt creation, fallback receipt minting, fallback route selection, and retired camelCase request aliases remain absent, while persisted agent route readback remains projection-only | runtime route-selection facade is a protocol/client adapter over Rust `model_mount` route-control truth; durable Agentgres-backed route-selection replay and stable protocol APIs remain non-terminal | `packages/runtime-daemon/src/threads/model-route-selection.mjs`, `packages/runtime-daemon/src/threads/model-route-selection.test.mjs`, `packages/runtime-daemon/src/model-mounting.mjs`, `crates/services/src/agentic/runtime/kernel/model_mount/route_control.rs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:receipts` |
| `ModelRouteControl` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md) | public route upsert/test, mounted route-selection, explicit-model endpoint resolution, and runtime explicit/run-override model-route selection now call typed `daemonCoreModelMountApi.planModelMountRouteControl`, backed by Rust `RuntimeKernelService::plan_model_mount_route_control`, receive Rust-authored route, route-test, route-selection, or endpoint-resolution records with receipt refs, commit only those records through Rust Agentgres `commit_runtime_model_mount_record_state`, and return committed Rust route-control truth; mounted/runtime route selection passes current route/provider/endpoint facts to Rust, Rust performs endpoint selection, calls typed `daemonCoreModelMountApi.admitModelMountRouteDecision` for the Rust `admit_model_mount_route_decision` API plus the Rust-authored `accepted_receipt_record`, and JS persists or forwards only that Rust-authored selection/receipt truth without route-map mutation, endpoint mounting, JS policy evaluation, candidate scoring, JS-created route-control receipts, JS fallback route receipts, route-decision command transport, or duplicate route-selection truth; the retired route-selection receipt helper path now requests a Rust-authored `model_mount.route_control` required record through typed `daemonCoreModelMountApi.planModelMountRouteControlRequired`, backed by Rust `RuntimeKernelService::plan_model_mount_route_control_required`, with the old `plan_model_mount_route_control_required` command operation, dispatch arm, bridge request/response wrapper, backend marker, and JS command-envelope builder retired; JS `createModelRouteDecision()` authoring and its local policy/rationale/hash helper tree are retired, the final `route-decision.mjs` compatibility module plus provider request-shaping helper/test path are deleted, and the remaining JS route-control record/route-selection receipt builder facades (`upsertRouteRecord()`, `routeSelectionReceipt()`, `routeSelectionReceiptForState()`, `modelMountRouteDecisionRequestForSelection()`, and `persistModelRouteSelectionState()`) are absent rather than preserved as request-building shims; public model-capability contracts now emit canonical snake_case protocol fields (`schema_version`, `route_id`, `model_role`, `authority_scope_requirements`, `fallback_policy`, `credential_readiness`, `receipt_behavior`, `workflow_availability`, `agent_availability`) without SDK camelCase output aliases, and the orphan JS daemon-side `model-capability.mjs` materializer/test module is deleted | Rust daemon-core route-control/projection object; public route write/test/selection records are Rust-planned and Agentgres-committed, route-decision, route-control, and route-control-required command transports are retired, and public route-list replay/projection is Rust-owned over persisted `model-routes` records, while model invocation provider execution/request shaping, route-derived capability projections, route-selection/endpoint-resolution replay, stable protocol APIs, and remaining command transport for adjacent model_mount helpers must still move fully to Rust/Agentgres truth | `packages/runtime-daemon/src/model-mounting/routes.mjs`, `packages/runtime-daemon/src/model-mounting/routes.test.mjs`, `packages/runtime-daemon/src/model-mounting/model-mount-core.mjs`, `packages/runtime-daemon/src/model-mounting/model-mount-core.test.mjs`, `packages/runtime-daemon/src/threads/model-route-selection.mjs`, `packages/runtime-daemon/src/threads/thread-runtime-controls.mjs`, `packages/runtime-daemon/src/model-mounting/model-invocation-operations.mjs`, `packages/runtime-daemon/src/model-mounting.mjs`, `packages/agent-sdk/src/model-mounts.ts`, `crates/services/src/agentic/runtime/kernel/model_mount.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/route_control.rs`, `crates/node/src/bin/ioi_step_module_bridge/mod.rs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:receipts` |
| `ModelTokenizerControl` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md) | public tokenize/count/context-fit utility facades now call typed `daemonCoreModelMountApi.planModelMountTokenizer`, backed by Rust `RuntimeKernelService::plan_model_mount_tokenizer`, bind the Rust-authored route-selection record/receipt returned by `model_mount.route.select`, receive Rust tokenizer/context-fit records with receipt refs, commit only those records through Rust Agentgres `commit_runtime_model_mount_record_state`, and return committed Rust tokenizer truth; tokenizer-required refusal records now call typed `daemonCoreModelMountApi.planModelMountTokenizerRequired`, backed by Rust `RuntimeKernelService::plan_model_mount_tokenizer_required`; the old `plan_model_mount_tokenizer`, `plan_model_mount_tokenizer_required`, and `plan_model_mount_route_control_required` command operations, dispatch arms, bridge request/response wrappers, backend markers, and JS command-envelope builders are retired; the old `model_mount.tokenizer` required-record shim, `modelTokenizerRustCoreRequiredError`, mounted `tokenizerRequired()`, JS context-window fallback helper, JS tokenizer receipts, JS route receipts, route-state mutation, truncation, and response-envelope shaping are absent from the hot path, while mounted public methods still reject retired tokenizer request aliases before route selection or Rust planning | Rust daemon-core model_mount tokenizer/context-fit control object backed by admitted route-selection truth, Agentgres record-state admission, replay, and projection; JS state methods are protocol clients over the typed Rust daemon-core tokenizer/context-fit APIs, not command-transport truth authors | `packages/runtime-daemon/src/model-mounting.mjs`, `packages/runtime-daemon/src/model-mounting/tokenizer-control.mjs`, `packages/runtime-daemon/src/model-mounting/model-mount-core.mjs`, `packages/runtime-daemon/src/model-mounting/model-mount-core.test.mjs`, `packages/runtime-daemon/src/model-mounting/tokenizer-operations.test.mjs`, `crates/services/src/agentic/runtime/kernel/model_mount.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/tokenizer.rs`, `crates/services/src/agentic/runtime/kernel/command_protocol.rs`, `crates/services/src/agentic/runtime/kernel/command_dispatch.rs` | `hypervisor-conformance:receipts` |
| `ModelInvocationControl` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md) | public fixture/local-folder non-stream invocation and native-local stream invocation now call Rust-owned route selection records/receipts, provider-execution admission, provider invocation/stream execution, provider-result admission, accepted-receipt transition planning, receipt binding, StepModule projection, Agentgres operation/state-root binding, and Rust-authored receipt persistence through `model_mount_invocation_positive_rust_path`; OpenAI-compatible stream cancellation now calls typed `daemonCoreModelMountApi.planModelMountStreamCancel`, backed by Rust `RuntimeKernelService::plan_model_mount_stream_cancel`, commits the Rust-authored canceled conversation projection through Agentgres model_mount record-state admission, persists only the Rust-authored `model_invocation_stream_canceled` receipt, and the old stream-cancel command operation/dispatch/wrapper/backend path is retired; the migrated public path no longer uses JS authorization facades, JS provider drivers, JS invocation receipts, JS stream-cancel receipts, route-state persistence, record-state commits, provider-native request-shaping hooks, or stream downgrade fallback, while unsupported hosted/provider paths still fail closed at the Rust backend boundary | Rust daemon-core model invocation API owns route decision, provider execution, receipt binding, Agentgres admission, replay, projection, stream lifecycle, and stream cancellation admission; JS may retain only protocol/API adapter scaffolding until hosted providers, model loading/instance lifecycle, conversation projection, durable replay, and direct Rust APIs replace the remaining temporary transport | `packages/runtime-daemon/src/model-mounting/model-invocation-operations.mjs`, `packages/runtime-daemon/src/model-mounting.mjs`, `packages/runtime-daemon/src/openai-compat-routes.mjs`, `crates/services/src/agentic/runtime/kernel/model_mount.rs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:receipts` |
| `ModelProviderExecution` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md) | Rust `model_mount` provider-execution admission record binding route decision, route receipt, policy/input hashes, wallet refs, cTEE custody refs, backend evidence, and stream status before provider execution, with non-stream provider invocation now owned by `model_mount/provider_execution/invocation.rs` and native-local stream planning now owned by `model_mount/provider_execution/stream.rs`; the mounted JS provider-driver factory is retired, `provider-driver-factory.mjs` is deleted, and `ModelMountingState.driverForProvider()` fails closed before allocating fixture, native-local, OpenAI-compatible, Ollama, LM Studio, vLLM, or llama.cpp JS driver objects; the hosted/nonlocal JS driver modules for OpenAI-compatible, vLLM/llama.cpp, Ollama, and LM Studio are deleted rather than preserved as fail-closed edge adapters; provider driver-kind inference helpers are retired and the final `provider-driver-helpers.mjs` compatibility module is deleted, so JS no longer maps provider `kind` to implicit driver names after driver deletion | Rust daemon-core provider execution/control object; direct Rust provider transports, lifecycle, inventory, projection, stable protocol APIs, and command transport must still move fully to Rust/Agentgres truth | `crates/services/src/agentic/runtime/kernel/model_mount.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/provider_execution.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/provider_execution/admission.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/provider_execution/invocation.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/provider_execution/stream.rs`, `crates/node/src/bin/ioi_step_module_bridge/mod.rs`, `packages/runtime-daemon/src/model-mounting/model-invocation-operations.mjs`, `packages/runtime-daemon/src/model-mounting.mjs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:receipts` |
| `ModelProviderInvocation` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md) | Rust `model_mount` provider-invocation result for migrated provider backends; fixture/local-folder and native-local non-stream execution return output text, token counts, backend refs, evidence refs, and invocation hash from Rust; hosted/non-migrated provider invocation helpers now report Rust-required, unsupported provider request builders fail closed before JS-compatible execution, provider invocation requests carry only explicit provider/endpoint `driver` fields, hosted providers without an admitted explicit driver report `provider_driver: null`, the final standalone provider-driver helper module is deleted instead of preserving coalescing or backend-inference compatibility scaffolding, hosted/nonlocal JS driver modules are deleted instead of retaining `invoke`/`streamInvoke`, HTTP request-shaping, backend-process, or public-CLI transport bodies, and the leftover JS provider invocation-retirement, HTTP transport, and transport-policy wrappers are deleted rather than preserved as fail-closed compatibility surfaces; the provider protocol helper surface is deleted, provider-result admission-request assembly requires Rust/provider result `token_count`, missing or inconsistent token counts fail closed instead of using JS estimation, and OpenAI/native embedding protocol responses now require Rust/provider-authored embedding vectors instead of synthesizing deterministic vectors in JS | execution result; all provider invocation and provider command paths require Rust model_mount ownership, with unsupported backends fail-closed until direct Rust transports exist and direct Rust provider-result envelopes own token accounting and embedding vectors | `crates/services/src/agentic/runtime/kernel/model_mount.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/provider_execution.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/provider_execution/admission.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/provider_execution/invocation.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/provider_execution/stream.rs`, `crates/node/src/bin/ioi_step_module_bridge/mod.rs`, `packages/runtime-daemon/src/model-mounting/model-invocation-operations.mjs`, `packages/runtime-daemon/src/model-mounting/protocol-responses.mjs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:receipts` |
| `ModelProviderResult` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md) | Rust `model_mount` provider-result admission record now lives in the dedicated Rust `model_mount/provider_result.rs` boundary for Rust-executed provider outputs and native stream starts, binding provider-execution ref/hash, request hash, output hash, token counts, stream status, backend refs, and evidence before accepted invocation receipts; hosted/non-migrated JS provider execution now fails closed, provider-invocation helpers no longer return a non-Rust-required predicate for hosted or stream selections, provider-result helpers reject unsupported hosted/non-migrated output observations before admission request assembly, Rust core validation rejects the retired `js_provider_driver_observation` backend in favor of Rust-owned fixture/native-local provider result backends, and embedding responses fail closed unless admitted provider results carry native embedding vectors | admission record for Rust-owned provider outputs only; promote concrete hosted/provider transports only after direct Rust `workload_client`/`model_mount` execution exists, including provider-native embedding result envelopes and direct provider-result protocol APIs | `crates/services/src/agentic/runtime/kernel/model_mount.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/provider_result.rs`, `crates/node/src/bin/ioi_step_module_bridge/mod.rs`, `packages/runtime-daemon/src/model-mounting/model-invocation-operations.mjs`, `packages/runtime-daemon/src/model-mounting/protocol-responses.mjs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:receipts` |
| `ModelProviderControl` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md), [`wallet-network/doctrine.md`](../components/wallet-network/doctrine.md) | public provider upsert still fails closed before JS vault-ref resolution, provider record-state commits, provider-map mutation, JS-created provider-control receipts, or projection writes; migrated native-local provider start/stop now route through Rust `plan_model_mount_provider_lifecycle` load/unload planning without JS driver execution, provider-control receipt creation, provider-map writes, or projection writes, while unsupported hosted/custom start/stop remains fail-closed before JS driver execution; the fail-closed `provider-operations.mjs` helper module is deleted, and mounted public `ModelMountingState` provider methods now own provider upsert alias rejection, vault-ref normalization, migrated lifecycle planning, and `model_mount.provider_control` Rust-core-required errors directly; the provider-registry dependency-injection binding wrapper and test are deleted, mounted model_mount imports only the non-projection canonical provider registry helpers directly, and the remaining provider-registry `publicProvider()` projection helper/test expectations are deleted so JS no longer preserves provider public/vault-boundary redaction as a registry helper; JS provider auth header materialization is also retired (`providerAuthHeaders()`, `providerAuthorizationHeaderValue()`, `assertProviderVaultBoundary()`, `providerHasVaultRef()`, `normalizeProviderAuthScheme()`, and `normalizeProviderAuthHeaderName()` are absent), leaving only plaintext-secret rejection, canonical vault-ref request parsing, and provider-kind vault-ref preflight at the JS edge until Rust daemon-core wallet/cTEE provider auth APIs own the direct path | Rust daemon-core provider-control object for provider configuration, wallet/cTEE vault authority, lifecycle control, record-state admission, projection, and outbound provider auth materialization | `packages/runtime-daemon/src/model-mounting.mjs`, `packages/runtime-daemon/src/model-mounting/provider-auth.mjs`, `packages/runtime-daemon/src/model-mounting/provider-registry.mjs`, `crates/services/src/agentic/runtime/kernel/model_mount.rs` | `hypervisor-conformance:receipts`, `hypervisor-conformance:negative` |
| `ModelProviderLifecycle` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md) | public fixture/native-local and hosted/custom metadata provider health/start/stop now route through Rust `plan_model_mount_provider_lifecycle`, receive Rust-authored `model-provider-lifecycle-controls` records with lifecycle hash, evidence, action, status, operation kind, and Rust-contained hosted metadata transport contracts where applicable, require Rust Agentgres model_mount record-state commit before returning public lifecycle truth, and still avoid JS driver execution, JS lifecycle receipt creation, provider-map mutation, projection writes, or JS-hosted transport execution; hosted/custom lifecycle records now carry `rust_hosted_provider_metadata_transport_materialized`, cTEE no-plaintext custody evidence, wallet.network transport-authority evidence, `rust_materialized` transport status, and explicit JS/command/binary-bridge/compatibility fallback false facts; model load/unload public facades now route through Rust provider lifecycle and Rust instance lifecycle planning, commit only the Rust-authored model-instance record through Rust Agentgres, and return after commit without JS provider driver execution, JS lifecycle receipts, direct model-instance map writes, or projection writes; canonical estimate-only model load now skips provider lifecycle execution, asks Rust instance lifecycle for `model_mount.instance.estimate`, commits the Rust-authored estimate record through Rust Agentgres, and returns no JS sizing or driver truth; mounted public `ModelMountingState` provider methods now own migrated lifecycle request shaping plus `model_mount.provider_health` / `model_mount.provider_control` Rust-core-required errors directly after deleting `provider-operations.mjs`; mounted public `ModelMountingState` model-loading methods now own load/unload alias rejection plus Rust instance-lifecycle estimate/load/unload planning and commit client behavior directly after deleting `model-loading-operations.mjs`; hosted/nonlocal provider lifecycle driver modules, the generic JS provider transport wrapper, the final provider transport-policy wrapper, and the final local provider driver adapter module are deleted, the JS backend-process supervisor path is retired, backend lifecycle helpers fail closed with `model_mount_backend_process_supervisor_retired`, native-local lifecycle planning and command response shaping now live in Rust lifecycle core and no longer keep JS driver wrapper classes for health/load/unload, the backend-process planner now lives in Rust `model_mount/backend_process.rs`, and the final LM Studio `lmStudioLoadOptionArgs()` public-CLI argument helper is deleted from load-policy rather than preserving `lms` flag shaping | Rust daemon-core lifecycle/control object; JS may remain only as protocol/read adapter after direct Rust control/projection APIs are verified, with backend process supervision and provider HTTP/native-local lifecycle owned by direct Rust daemon-core lifecycle APIs rather than MJS spawn/HTTP/driver helpers | `packages/runtime-daemon/src/model-mounting.mjs`, `packages/runtime-daemon/src/model-mounting/load-policy.mjs`, `crates/services/src/agentic/runtime/kernel/model_mount.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/backend_process.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/backend_lifecycle.rs` | `hypervisor-conformance:receipts` |
| `ModelLifecycleReceiptControl` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md) | direct `model_lifecycle` receipt helper and generic model_mount JS receipt creation now fail closed before JS receipt construction, store writes, or projection refresh; legacy subject aliases still fail before the Rust-core-required boundary; JS may persist only Rust-authored receipt records that carry Rust model_mount evidence through the Agentgres receipt-state commit gate; model_mount accepted-receipt head/transition planning now lives in Rust `model_mount/accepted_receipt.rs`; the fail-closed `receipt-operations.mjs` helper module is deleted, and mounted public `ModelMountingState` receipt methods now own receipt list/get store adapters, lifecycle subject alias rejection, `model_mount.lifecycle_receipt` Rust-core-required errors, generic JS receipt-creation retirement, and Rust-authored receipt persistence validation directly | Rust daemon-core model_mount receipt authoring/admission/binding/projection object; JS may only read already admitted lifecycle projections or persist Rust-authored records during migration transport until direct Rust daemon-core receipt APIs replace the wrapper | `packages/runtime-daemon/src/model-mounting.mjs`, `packages/runtime-daemon/src/model-mounting/store.mjs`, `crates/services/src/agentic/runtime/kernel/model_mount.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/accepted_receipt.rs`, `crates/node/src/bin/ioi_step_module_bridge/mod.rs` | `hypervisor-conformance:receipts` |
| `ModelInstanceLifecycleControl` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md) | public load/unload now asks Rust provider lifecycle for the provider lifecycle hash, asks Rust `plan_model_mount_instance_lifecycle` for the canonical model-instance transition record, commits that Rust-authored record through Rust Agentgres `commit_runtime_model_mount_record_state`, and returns public instance truth only after the commit; canonical estimate-only now asks Rust `plan_model_mount_instance_lifecycle` for a `model_mount.instance.estimate` record, receives Rust-authored `load_estimate` truth with provider lifecycle execution, JS sizing, and JS driver execution all false, commits that record through Rust Agentgres without mutating the daemon instance cache, and fails closed if the Rust Agentgres estimate commit is unavailable; loaded-instance idle eviction, duplicate coalescing, and explicit supersede now ask Rust `plan_model_mount_instance_lifecycle` for canonical evict/supersede transition records, require Rust-authored `reason` plus `superseded_by` binding for supersede, commit those Rust-authored records through Rust Agentgres before any local projection-cache update, and do not create JS lifecycle receipts, call supersede mutation helpers, write `model-instances` maps, or author compatibility `supersededBy` metadata; the fail-closed `model-loading-operations.mjs` helper module is deleted, and mounted public `ModelMountingState` model-loading methods now own canonical load request alias rejection, endpoint/instance lookup, Rust instance-lifecycle estimate/load/unload planning, and Rust Agentgres model-instance commit client behavior directly; the remaining JS estimate-only projection path is retired: `ModelMountingState.loadEstimate()` and helper-level `estimateNativeLocalResources()` are absent, the public `/api/v1/models/estimate-load` route is no longer routed, and the retired `estimateOnly`, `gpuOffload`, `contextLength`, `ttlSeconds`, `idleTtlSeconds`, `instanceIdentifier`, `modelPath`, `tensorParallelSize`, `gpuMemoryUtilization`, and `maxModelLen` load-option aliases plus load-policy `ttlSeconds`, `idleTtlSeconds`, `autoEvict`, and `memoryPressureEvict` aliases are ignored/stripped before provider normalization, so the retired estimate facade, GPU placement, context-window steering, lifecycle TTL steering, idle-eviction steering, memory-pressure steering, instance identity, model-path, tensor-parallel, GPU-memory, and model-length steering cannot survive as JS compatibility endpoints or camelCase request selectors before Rust-core ownership; helper-level `planModelMountInstanceLifecycleForMigratedProvider()`/`modelMountInstanceLifecycleRequiresRust()`/`modelMountInstanceLifecycleFields()` planning wrappers are deleted, and the later one-purpose `model-instance-lifecycle.mjs` guard module is also absent, while mounted `ModelMountingState.planModelMountInstanceLifecycle()` is only a temporary protocol client over `ModelMountCore.planInstanceLifecycle()` rather than an authority-bearing JS planner | Rust daemon-core instance lifecycle/projection object backed by Agentgres-admitted model-instance truth; public instance-list replay/projection is Rust-owned, while deeper receipt/state-root binding, richer load sizing/materialization, and stable protocol APIs remain required | `packages/runtime-daemon/src/model-mounting.mjs`, `packages/runtime-daemon/src/model-mounting/load-policy.mjs`, `packages/runtime-daemon/src/model-mounting/loaded-instances.mjs`, `packages/runtime-daemon/src/model-mounting/receipt-write-guards.mjs`, `packages/runtime-daemon/src/runtime-route-handlers.mjs`, `crates/services/src/agentic/runtime/kernel/model_mount.rs` | `hypervisor-conformance:receipts` |
| `ModelProviderInventory` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md) | public fixture/local-folder, native-local, and hosted metadata provider model/list-loaded facades now call Rust `plan_model_mount_provider_inventory`, require Rust inventory hash/evidence/item_refs/item_count/action/status envelopes, and return Rust-owned inventory results without JS driver execution, JS inventory lifecycle receipts, local artifact/instance fallback reads, JS artifact record-state commits, artifact/instance map mutation, or projection writes; hosted/nonlocal provider inventory now uses Rust `rust_model_mount_hosted_provider_inventory`, records canonical provider metadata/item refs, carries a Rust-contained metadata transport contract with `rust_materialized` execution status, cTEE no-plaintext custody evidence, wallet.network transport-authority evidence, and explicit JS/command/binary-bridge/compatibility fallback false facts, and no longer returns through JS driver execution; mounted public `ModelMountingState` provider methods now own migrated provider-inventory request shaping plus `model_mount.provider_inventory` Rust-core-required errors directly after deleting `provider-operations.mjs`; Rust-backed fixture/native-local/hosted-metadata inventory planner plumbing and command response shaping live in Rust lifecycle core as the only migrated provider-inventory API rather than as backend-only substrate, and no JS driver wrapper classes remain for local provider inventory; default LM Studio public discovery no longer runs public CLI status/list probes, no longer mints `lmstudio.detected` artifact fallback truth, no longer prunes LM Studio artifact/endpoint/instance projection maps from JS, and default seeding no longer calls `state.discoverLmStudioProvider(checkedAt)` or inserts `provider.lmstudio` into `state.providers`; the retired LM Studio default-discovery exports, mounted pass-through methods, local-system public-CLI parsers/artifact projector, and `lmStudioPublicCliEnabled()` toggle are deleted rather than preserved as inert compatibility helpers; the standalone `fixture-policy.mjs` wrapper is also deleted, remaining disabled-internal-fixture cleanup predicates live privately inside `default-discovery.mjs` instead of a reusable dependency-injected policy surface, and native-local fixture seeding no longer writes `native-fixture` model files, hashes those files, parses local metadata, or emits `artifactPath` as storage truth; hosted/nonlocal provider inventory driver modules, the final local provider driver adapter module, and generic JS provider transport wrapper are deleted, so OpenAI-compatible/Ollama/vLLM/llama.cpp/LM Studio/native-local/fixture inventory can no longer retain HTTP, public-CLI, loaded-instance projection, catalog bridge helper bodies, local driver readback bodies, or JS fixture file materialization in JS | Rust daemon-core inventory/projection object backed by Agentgres-admitted provider metadata, artifact, and model-instance truth; JS may remain only as protocol/read adapter after direct Rust projection APIs own provider inventory reads and catalog/provider transport | `packages/runtime-daemon/src/model-mounting.mjs`, `packages/runtime-daemon/src/model-mounting/model-mount-core.mjs`, `packages/runtime-daemon/src/model-mounting/default-discovery.mjs`, `packages/runtime-daemon/src/model-mounting/state-seeding.mjs`, `packages/runtime-daemon/src/model-mounting/default-records.mjs`, `crates/services/src/agentic/runtime/kernel/model_mount.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/lifecycle/inventory.rs` | `hypervisor-conformance:receipts` |
| `ModelMountProjectionAdapter` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md) | state accessor reads remain adapters, but provider-direct artifact creation and loaded-instance touch/eviction mutations now fail closed or reuse existing projections without JS artifact/instance record-state commits or map writes; store-level `AgentgresModelMountingStore.writeMap()` also fails closed with `model_mount_store_map_write_retired`; public model_mount projection and model_mount snapshot envelope authoring now route through `ModelMountCore::plan_read_projection`, whose dispatcher lives in Rust `model_mount/read_projection.rs`, whose shared helper ownership now lives in Rust `model_mount/read_projection/common.rs`, whose adapter-boundary/workflow-binding authorship now lives in Rust `model_mount/read_projection/adapter_boundary.rs`, whose server/catalog status plus authority-snapshot authorship now lives in Rust `model_mount/read_projection/status.rs` and `model_mount/read_projection/authority.rs`, whose receipt summary and replay authorship now lives in Rust `model_mount/read_projection/receipt.rs`, whose route-decision readback authorship now lives in Rust `model_mount/read_projection/route_decision.rs`, whose provider-lifecycle health and runtime-survey readback authorship now lives in Rust `model_mount/read_projection/health.rs`, whose runtime-engine read-projection replay now lives in Rust `model_mount/read_projection/runtime.rs`, and whose aggregate snapshot/projection envelope authorship now lives in Rust `model_mount/read_projection/aggregate.rs`, and whose topology/product-catalog default read projections now live in Rust `model_mount/read_projection/topology.rs`, public catalog-status readback now returns through Rust `model_mount/read_projection/status.rs`, and whose catalog-provider OAuth session/state replay projection now lives in Rust `model_mount/read_projection/oauth.rs`, via typed `daemonCoreModelMountApi.planModelMountReadProjection` backed by Rust `RuntimeKernelService::plan_model_mount_read_projection`; the old read-projection command operation, dispatch arm, bridge request/response wrapper, source/backend markers, JS command-envelope builder, and child delegate are retired; snapshot summary and adapter-boundary fields no longer rebuild the full projection internally, and projection-summary, route-decision projection, receipt-replay, wallet authority snapshot, provider-health list/latest, and latest vault-health reads now use slim Rust read-projection inputs instead of broad snapshot/projection state; receipt-replay now sends empty request state plus receipt_id and runtime state_dir, and Rust replays admitted receipts/*.json records, returns null route/endpoint/instance/provider enrichments until direct Agentgres topology lookup owns those joins, and the JS topology imports plus Rust projection_lookup helper are removed; canonical `model-mounting-canonical` projection persistence now requires `canonicalProjectionWritePlan()` Rust evidence, direct JS projection writes fail closed with `model_mount_projection_direct_write_forbidden`, direct projection-cache reads fail closed with `model_mount_projection_cache_read_retired`, and the store adapter identifies as `rust_plan_gated_receipt_projection_adapter`; public projection-field list surfaces (`listArtifacts()`, `listProviders()`, `listEndpoints()`, `listInstances()`, `listRoutes()`, `listModelCapabilities()`, `listDownloads()`, and `listProviderHealth()`) now call dedicated Rust read-projection kinds instead of unwrapping the broad `projection` envelope, with dedicated topology-list surfaces sending empty request state, provider-health sending empty request state plus runtime `state_dir` for provider-lifecycle record replay, and `instances` plus `routes` additionally sending runtime `state_dir` for direct Agentgres replay; public runtime-engine list/detail/default-load/preference/profile reads now call dedicated Rust read-projection kinds with empty JS request state plus runtime `state_dir`, and Rust replays admitted `runtime-engine-controls/*.json` records while filtering out JS-authored controls, so JS runtime-engine maps/preferences cannot become public projection truth through the dedicated runtime-engine surfaces; public `workflowNodeBindings()` now calls dedicated Rust read-projection kind `workflow_bindings` with no JS state payload instead of unwrapping the broad `projection` envelope, public `adapterBoundaries()` now calls dedicated Rust read-projection kind `adapter_boundaries` with empty request state instead of unwrapping the broad `projection` envelope, echoing JS `wallet`/`vault`/`agentgres_store` adapter status inputs, or calling JS `buildAdapterBoundaries()`, public `modelCapabilities` list readback now replays Agentgres-backed route, endpoint-resolution, provider-inventory, artifact, and instance truth through Rust `model_capabilities` with runtime `state_dir`, filters JS-authored records, and emits canonical `ioi.model-capability.v1` readiness contracts, public product-safe `listProductArtifacts()`, `runtimeModelCatalogList()`, and `openAiModelList()` now use dedicated Rust read-projection kinds that send empty request state and return Rust default empty product/catalog lists instead of JS `productArtifactList`, `runtimeModelCatalogList`, or `openAiModelList` helpers, caller-supplied artifact arrays, or fixture policy, `listProviderHealth()`/`latestProviderHealth()`/`latestVaultHealth()` success and not-found decisions now route through Rust `provider_health`/`latest_provider_health`/`latest_vault_health` read-projection kinds, with provider-health list/latest using empty request state plus runtime state_dir provider-lifecycle record replay instead of JS provider records, provider-health records, stale provider_health receipts, or local provider-health files, public `catalogStatus()` now routes through Rust `catalog_status` read-projection kind with empty request state plus runtime `state_dir`, replays admitted `model-provider-inventory/*.json` records into Rust-authored catalog provider status, storage status, last-search summary, and result rows, deletes the old JS edge refusal translator, and no longer imports the dead JS catalog-status helper; direct Rust `catalog_status` ignores caller `catalog_status_input` while catalog status replays admitted provider-inventory truth; broad snapshot/projection requests also no longer send `backends: state.listBackends()` or `backend_processes: state.listBackendProcesses()`, so JS backend registry/process maps cannot become public projection truth while dedicated `backends` replays admitted `model-backend-lifecycle-controls/*.json` with runtime `state_dir`; broad snapshot/projection requests also no longer send `runtime_engines: state.listRuntimeEngines()`, `runtime_engine_profiles: state.listRuntimeEngineProfiles()`, or `runtime_preference: state.runtimePreference()`, so JS runtime-engine maps/preferences cannot become public projection truth through the broad envelope; broad snapshot/projection requests also no longer send `mcp_servers: state.listMcpServers()` or `conversation_states: state.listConversations()`, so JS MCP/conversation maps cannot become public projection truth while direct Rust projection remains pending; broad snapshot/projection requests also no longer send `grants: state.listTokens()`, `vault_refs: state.listVaultRefs()`, `agentgres_store: state.store.adapterStatus()`, `wallet: state.walletAuthority.adapterStatus()`, or `vault: state.vaultStatus()`, so JS wallet/vault/Agentgres adapter state cannot become public authority or adapter-boundary truth through the broad envelope while dedicated authority and adapter-boundary read projections remain narrow Rust-planned inputs; broad snapshot/projection requests also no longer send `provider_health: providerHealthList(...)` or `runtime_survey_input: latestRuntimeSurveyProjectionInput(...)`, so JS provider-health files and runtime-survey probe summaries cannot become public telemetry truth through the broad envelope; dedicated `provider_health` now sends empty request state plus runtime state_dir, Rust replays admitted `model-provider-lifecycle-controls/*.json` records into `agentgres_provider_lifecycle_health` list entries with `receipt: null` and lifecycle replay watermarks, the Rust bridge `provider_health` arm ignores caller-supplied provider-health records and no longer returns a default empty list, dedicated `latestProviderHealth()` also selects the latest admitted lifecycle health record instead of stale `provider_health` receipts, dedicated `latestRuntimeSurvey()` also sends empty request state plus runtime state_dir, Rust ignores local provider-health records plus `runtime_survey_input`, and the dead JS runtime-survey projection-input helper is deleted so JS telemetry fallback cannot become public provider-health or runtime-survey truth; broad snapshot/projection requests also no longer send `artifacts`, `endpoints`, `instances`, `providers`, `routes`, `downloads`, or `product_artifact_policy`, so JS model topology maps cannot become public projection truth through the broad envelope; dedicated `artifacts`, `providers`, `endpoints`, `instances`, `routes`, `model_capabilities`, and `downloads` read projections now send empty request state; `instances`, `routes`, and `model_capabilities` also send runtime `state_dir`, and Rust bridge replays persisted `model-instances/*.json` Agentgres records while filtering to Rust-authored instance lifecycle evidence, persisted `model-routes/*.json` Agentgres records while filtering to Rust-authored route-control evidence, and route/endpoint/provider/artifact/instance records into Rust-authored model-capability readiness contracts, so JS model instance and route maps cannot become public topology truth through those dedicated list surfaces; the remaining non-replay topology direct arms still return empty/default lists instead of caller-supplied topology arrays, so remaining JS model topology maps cannot become public topology truth through those dedicated list surfaces; dedicated `product_artifacts`, `runtime_model_catalog`, and `open_ai_model_list` read projections also send empty request state, and Rust bridge direct arms return empty/default product catalog lists instead of caller-supplied artifact arrays, so JS artifact maps and fixture policy cannot become public product catalog or OpenAI-compatible model-list truth; broad Rust `snapshot` and `projection` envelopes now also return empty/default topology, runtime-engine, MCP/conversation, and product-catalog fields instead of honoring direct caller arrays, and the dead caller-supplied product-artifact/runtime-catalog/OpenAI-list/fixture-filter helper tree plus the JS fixture mirror were deleted, while model-capability derivation now exists only as Rust Agentgres replay; internal fixture seeding now honors only the explicit `IOI_EXPOSE_INTERNAL_FIXTURE_MODELS` toggle and ignores the retired `IOI_ENABLE_INTERNAL_FIXTURE_MODELS` alias, so fixture exposure cannot survive as a hidden compatibility selector beside Rust-owned provider/projection paths; broad snapshot/projection requests also no longer send `server_status_input: serverStatusProjectionInput(...)`, so JS volatile server-control state cannot become public server truth through the broad envelope; dedicated `server_status` now sends empty request state plus request-level `base_url` and runtime `state_dir`, the JS `serverStatusProjectionInput()` helper is deleted, and Rust replays admitted `model-server-controls/*.json` to author public server status, last-operation, receipt, topology-count, and backend-state truth; dedicated `authoritySnapshot()` now sends empty request state plus runtime state_dir and no longer sends JS `server_status_input`, grants, vault refs, wallet status, vault status, or caller-supplied receipt arrays, so JS authority adapter state cannot become public authority truth through the authority envelope; provider read-projection input no longer calls JS `publicProvider()`, `providerHasVaultRef()`, or `state.vault.vaultRefMetadata()`, no longer synthesizes `vaultMetadata` as JS readback authority, and the canonical provider registry no longer exports a `publicProvider()` fallback helper; OAuth session/state readback now routes through Rust `oauth_sessions`/`oauth_states` read-projection kinds with empty JS request state plus runtime `state_dir`; Rust replays admitted `model-catalog-provider-controls/*.json` records, filters JS-authored OAuth truth, returns redacted wallet/cTEE custody rows, and the JS edge no longer translates retired OAuth refusal or reads local OAuth maps, while broad snapshot/projection transport plus direct Rust broad envelopes keep `oauth_sessions` and `oauth_states` as schema-stable empty arrays until the public envelope adopts the direct Rust OAuth projection, the dead broad JS projection helper exports (`modelMountingSnapshot()`, `workflowNodeBindings()`, `buildModelMountingProjection()`, `buildAuthoritySnapshot()`, `buildProjectionSummary()`, `buildAdapterBoundaries()`, and `buildReceiptReplay()`) have been retired after their callers moved to Rust read-projection kinds, and the final one-helper `projections.mjs` compatibility module plus later `route-decision.mjs` compatibility module were deleted once their public readback callers were Rust-planned or owner-local; the orphaned JS `read-model.mjs` projection helper module and test were deleted after public list/catalog/health readbacks moved to Rust read-projection kinds, so JS no longer carries fallback list builders for artifacts, providers, endpoints, instances, routes, capabilities, downloads, provider health, product artifacts, runtime catalog, or OpenAI-compatible model lists; JS only prepares current state input, edge Rust-error-to-public-404 translation, and local materialization as migration transport | Rust daemon-core model_mount projection object backed by Agentgres-admitted artifact/instance/provider-lifecycle/runtime-engine/backend-lifecycle/server-control truth; direct Rust projection APIs over remaining Agentgres-backed state still need to replace JS state materialization, local map/projection-file materialization, and edge error-envelope translation | `packages/runtime-daemon/src/model-mounting/state-accessors.mjs`, `packages/runtime-daemon/src/model-mounting/read-projection-facade.mjs`, `packages/runtime-daemon/src/model-mounting/model-mount-core.mjs`, `packages/runtime-daemon/src/model-mounting/store.mjs`, `packages/runtime-daemon/src/model-mounting/environment.mjs`, `packages/runtime-daemon/src/threads/thread-runtime-controls.mjs`, `crates/node/src/bin/ioi_step_module_bridge/mod.rs`, `crates/services/src/agentic/runtime/kernel/model_mount.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/read_projection.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/read_projection/common.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/read_projection/adapter_boundary.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/read_projection/status.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/read_projection/authority.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/read_projection/receipt.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/read_projection/route_decision.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/read_projection/health.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/read_projection/aggregate.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/read_projection/runtime.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/read_projection/topology.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/read_projection/oauth.rs` | `hypervisor-conformance:receipts`, `hypervisor-conformance:compositor` |
| `ModelMountPublicCatalogRoutes` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md) | public `/v1/models` and `/v1/model-capabilities` routes now call mounted `modelMounting.runtimeModelCatalogList()` and `modelMounting.listModelCapabilities()` read-projection surfaces directly instead of daemon-store `listModels()` / `listModelCapabilities()` compatibility wrappers; route tests poison the retired wrappers, conformance bans the public route layer from re-entering them, and the dead store-level wrappers are deleted | Direct Rust daemon-core model catalog and capability projection over Agentgres-admitted model/catalog truth; command-transport retirement, receipt/state-root binding, replay, wallet authority where exposure exits local trust, and stable protocol APIs remain required | `packages/runtime-daemon/src/http/public-runtime-routes.mjs`, `packages/runtime-daemon/src/http/public-runtime-routes.test.mjs`, `packages/runtime-daemon/src/model-mounting/read-projection-facade.mjs`, `packages/runtime-daemon/src/model-mounting.mjs`, `scripts/conformance/hypervisor-conformance.mjs` | `hypervisor-conformance:compositor` |
| `ModelServerControl` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md) | public server start/stop/restart/write, operation recording, and log append now call typed `daemonCoreModelMountApi.planModelMountServerControl`, backed by Rust `RuntimeKernel::plan_model_mount_server_control`, receive Rust-authored `model-server-controls` records with the `model_mount.server_control` boundary, commit only those records through Rust Agentgres `commit_runtime_model_mount_record_state`, and return Rust public responses with `js_state_write: false`, `js_log_write: false`, and `js_transport_execution: false`; the old `plan_model_mount_server_control` command operation, command-dispatch arm, bridge request/response wrapper, backend marker, and JS command-envelope builder are retired, and server-control request bodies forward only canonical snake_case fields and strip retired authorization/camelCase state, while a missing Rust planner still fails closed before any JS state/log writes; public server-status readback now routes through Rust `plan_model_mount_read_projection` kind `server_status` with empty JS request state, request-level `base_url`, and runtime `state_dir`; Rust replays admitted `model-server-controls/*.json`, filters out JS-authored controls, and authors the public server-status envelope plus nested snapshot/authority-snapshot `server` object from Rust server-control truth instead of echoing a JS-built public envelope or reading `server_status_input`, and the deleted JS `serverStatusProjectionInput()` helper can no longer summarize provider status from `state.providers.values()` or backend status from `state.listBackends()`; public server logs/events/log-record readback now routes through Rust `plan_model_mount_read_projection` kinds `server_logs`, `server_events`, and `server_log_records` with canonical `server_log_query` plus runtime `state_dir`, and Rust filters JS-authored controls plus retired read-as-mutation `logs_read`/`events_read`/`log_projection` records before returning redacted log/event projections; the standalone `server-control.mjs` helper/test module remains deleted and `recordServerOperation()` now commits Rust server-control truth instead of delegating to a JS helper or required-record facade | Rust daemon-core server-control/state/log/event/projection object; actual process supervision/transport execution and stable SDK/IDE server-control APIs remain non-terminal | `packages/runtime-daemon/src/model-mounting.mjs`, `packages/runtime-daemon/src/model-mounting/model-mount-core.mjs`, `packages/runtime-daemon/src/model-mounting/read-projection-facade.mjs`, `packages/runtime-daemon/src/model-mounting/server-control-mounted.test.mjs`, `packages/runtime-daemon/src/runtime-route-handlers.mjs`, `crates/node/src/bin/ioi_step_module_bridge/mod.rs`, `crates/services/src/agentic/runtime/kernel/model_mount.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/server_control.rs` | `hypervisor-conformance:receipts` |
| `ModelRuntimeEngineControl` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md) | public runtime-engine selection/profile/remove mutations now call typed `daemonCoreModelMountApi.planModelMountRuntimeEngine`, backed by Rust `RuntimeKernelService::plan_model_mount_runtime_engine`, receive Rust-authored `runtime-engine-controls` records with the `model_mount.runtime_engine` boundary, commit only those records through Rust Agentgres `commit_runtime_model_mount_record_state`, and return Rust public responses with `js_preference_write: false`, `js_profile_write: false`, and `js_projection_write: false`; runtime-engine request bodies forward only canonical snake_case fields and strip retired camelCase state, while a missing Rust planner still fails closed before any JS runtime preference/profile/projection writes; the old `plan_model_mount_runtime_engine_required` required-record facade and deleted `required/runtime_engine.rs` path must not return, and the old `plan_model_mount_runtime_engine` command operation, dispatch arm, bridge response wrapper, backend marker, and JS command-envelope builder are retired; `runtime-engines.mjs` and `runtime-engines.test.mjs` remain deleted, so JS no longer preserves helper-level runtime-engine list/detail/preference/profile/default-load builders or latest-receipt enrichers as fallback projection truth; list/detail/default-load/preference/profile reads now route through Rust `plan_model_mount_read_projection` runtime-engine projection kinds with empty JS request state plus runtime `state_dir`, and Rust replays admitted `runtime-engine-controls/*.json` records for runtime-engine projection truth while filtering out JS-authored controls; the local runtime-engine helper tail no longer exports `llamaCppGpuLayersArg()` or `backendBindAddress()` | Rust daemon-core runtime-engine preference/profile/projection object backed by Agentgres-admitted `runtime-engine-controls` truth; local runtime materialization and stable SDK/IDE runtime-engine APIs remain non-terminal | `packages/runtime-daemon/src/model-mounting.mjs`, `packages/runtime-daemon/src/model-mounting/model-mount-core.mjs`, `packages/runtime-daemon/src/model-mounting/model-mount-core.test.mjs`, `packages/runtime-daemon/src/model-mounting/runtime-engine-mounted.test.mjs`, `packages/runtime-daemon/src/model-mounting/local-runtime-engines.mjs`, `packages/runtime-daemon/src/model-mounting/read-projection-facade.mjs`, `crates/node/src/bin/ioi_step_module_bridge/mod.rs`, `crates/services/src/agentic/runtime/kernel/model_mount.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/runtime_engine.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/read_projection/runtime.rs` | `hypervisor-conformance:receipts` |
| `ModelRuntimeSurveyProjection` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md) | public runtime-survey capture now calls typed `daemonCoreModelMountApi.planModelMountRuntimeSurvey`, backed by Rust `RuntimeKernelService::plan_model_mount_runtime_survey`, replays runtime-engine state from Agentgres `state_dir`, captures Rust hardware facts without LM Studio command execution, authors a Rust `runtime_survey` receipt, and requires Rust Agentgres model_mount receipt-state commit before public truth returns; latest runtime-survey readback now routes through Rust `plan_model_mount_read_projection` kind `latest_runtime_survey` with empty request state plus runtime state_dir receipt replay, no `latestRuntimeSurveyProjectionInput()` import, and no JS hardware/probe/preference fallback; Rust now authors not-checked readback as zero/null/default and checked readback only from admitted `runtime_survey` receipt details; broad snapshot/projection requests no longer send runtime-survey probe summaries; hidden LM Studio runtime helper probes no longer run `lms runtime ls` or `lms runtime survey`, runtime-specific public CLI parsers/env toggle are removed, and the dead JS `latestRuntimeSurveyProjectionInput()`, `lmStudioRuntimeEngines()`, `lmStudioRuntimeSurvey()`, and standalone runtime-engine projection helper exports are deleted instead of preserved as compatibility scaffolding; the old `plan_model_mount_runtime_survey` command operation, dispatch arm, bridge response wrapper, backend marker, and JS command-envelope builder are retired; the one-function JS `runtime-survey.mjs` helper module remains deleted, and mounted public `ModelMountingState.runtimeSurvey()` is now a Rust-planned, Agentgres receipt-state committed protocol edge instead of an edge refusal or dependency-injected JS probe facade | Rust daemon-core runtime-survey/projection object backed by Agentgres-admitted survey truth; JS remains only protocol transport while richer runtime materialization and stable SDK/IDE runtime-survey APIs remain non-terminal | `packages/runtime-daemon/src/model-mounting/local-system-probes.mjs`, `packages/runtime-daemon/src/model-mounting/environment.mjs`, `packages/runtime-daemon/src/model-mounting/read-projection-facade.mjs`, `packages/runtime-daemon/src/model-mounting.mjs`, `packages/runtime-daemon/src/model-mounting/model-mount-core.mjs`, `crates/node/src/bin/ioi_step_module_bridge/mod.rs`, `crates/services/src/agentic/runtime/kernel/model_mount.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/runtime_survey.rs` | `hypervisor-conformance:receipts` |
| `ModelArtifactStorageControl` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md) | public import/mount/unmount now call typed `daemonCoreModelMountApi.planModelMountArtifactEndpoint`, backed by Rust `RuntimeKernelService::plan_model_mount_artifact_endpoint`, while download-cancel, artifact-delete, and storage-cleanup now call typed Rust storage-control planning; both paths receive Rust-authored records and require Rust Agentgres `commit_runtime_model_mount_record_state` before public truth returns. `storageSummary()`, `listDownloads()`, and `downloadStatus()` now call Rust read-projection kinds over runtime `state_dir`, replay admitted `model-catalog-imports`, `model-downloads`, and `model-storage-controls` records, and filter out JS-authored storage/download truth. The fail-closed `storage-operations.mjs` and `artifact-endpoint-operations.mjs` helper modules are deleted, JS model artifact/storage lifecycle receipts, local filesystem mutation/scanning, artifact/endpoint/download map mutation/readback, direct `writeMap()` storage truth, the artifact-endpoint command-envelope builder/bridge wrapper/backend marker, and no-commit planner success remain retired. | Rust daemon-core artifact, endpoint, download, storage, filesystem, receipt, record-state, and projection object; JS may remain only as protocol adapter until richer filesystem custody and stable APIs are Rust-owned | `packages/runtime-daemon/src/model-mounting.mjs`, `packages/runtime-daemon/src/model-mounting/read-projection-facade.mjs`, `packages/runtime-daemon/src/model-mounting/catalog-helpers.mjs`, `packages/runtime-daemon/src/model-mounting/model-mount-core.mjs`, `packages/runtime-daemon/src/model-mounting/read-projection-facade.test.mjs`, `crates/services/src/agentic/runtime/kernel/model_mount.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/artifact_endpoint.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/storage_control.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/read_projection/topology.rs` | `hypervisor-conformance:receipts` |
| `ModelCatalogDownloadControl` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md) | public catalog import URL and direct model download mutations now call Rust daemon-core `plan_model_mount_storage_control`, receive Rust-authored `model-catalog-imports` or `model-downloads` records, and require Rust Agentgres model_mount record-state commit before public truth returns; canonical catalog import URL, download identity, download control, and download metadata request alias rejection remain at the mounted protocol edge, while the fail-closed `catalog-download-operations.mjs` helper module, JS catalog/download lifecycle receipts, fixture/live materialization, local file writes, artifact/download map mutation, direct catalog/download record-state commits, and no-commit planner success remain retired. | Rust daemon-core catalog/download/filesystem/admission object; JS may remain only as protocol/read adapter until Rust projection owns the full catalog/download surface | `packages/runtime-daemon/src/model-mounting.mjs`, `packages/runtime-daemon/src/model-mounting/model-mount-core.mjs`, `packages/runtime-daemon/src/model-mounting/catalog-download-operations.test.mjs`, `crates/services/src/agentic/runtime/kernel/model_mount.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/storage_control.rs` | `hypervisor-conformance:receipts` |
| `ModelCatalogProviderControl` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md), [`wallet-network/doctrine.md`](../components/wallet-network/doctrine.md), [`private-workspace-ctee.md`](../components/daemon-runtime/private-workspace-ctee.md) | public catalog-provider configuration list/get/write, private config/material resolution, and OAuth start/callback/exchange/refresh/revoke now call Rust daemon-core `plan_model_mount_catalog_provider_control`, receive Rust-authored `model-catalog-provider-controls` records, commit those records through Agentgres model_mount record-state admission, and do not create JS catalog configuration/OAuth receipts, execute JS OAuth credential refresh, mutate provider config/runtime-material/session/state maps, write vault refs, or write projections from JS; mounted OAuth start/callback/exchange/refresh/revoke facades no longer inject `publicCatalogProviderConfig()`, `catalogProviderStatus()`, `oauthBoundaryForSession()`, `publicOAuthSession()`, or `stableHash()` compatibility helpers into the fail-closed OAuth operations, the fail-closed `catalog-provider-oauth.mjs` helper module is deleted, and the mounted public `ModelMountingState` OAuth methods now call Rust catalog-provider-control planning while preserving callback `state` validation; public catalog-provider config list/get/write and private config/runtime-material readback now use mounted `ModelMountingState` protocol clients to call Rust catalog-provider-control planning and commit Rust-authored records, the fail-closed `catalog-provider-configuration-operations.mjs` helper module is deleted, and JS no longer owns provider configurability, configurable-provider counts, request-field counts, local runtime-material status summaries, or Rust-core-required error truth for those public edges; OAuth callback preflight now accepts only `state`, not retired `oauth_state`/`oauthState` aliases; the hidden `catalogProviderConfigUpdate` JS writer has been removed from the mounted runtime and config module, so it can no longer bind source material, write vault refs, or synthesize catalog-provider configuration records; external Hugging Face-compatible and custom HTTP catalog searches now fail closed with `model_catalog_live_http_search_retired` before JS auth resolution, `/api/models` or `/catalog/search` URL shaping, catalog HTTP timeout handling, or `fetchWithTimeout()` transport, and the dead Hugging Face JS search helper module is deleted; the private `OAuthCredentialProvider` helper is no longer mounted and its retired helper module is deleted; catalog source runtime-material lookup now routes through Rust daemon-core `model_mount.catalog_provider_runtime_material.resolve` before JS vault resolution, source-material parsing, catalog-provider config-map reads, cached bound/missing/failed runtime-material readback, vault-ref writes, or runtime-material caching; private catalog-provider config readback now routes through Rust daemon-core `model_mount.catalog_provider_configuration.read_private` before JS config reads, auth vault hash/scheme/header projection, OAuth session hash projection, vault resolution, vault-ref writes, or plaintext header shaping; public catalog search orchestration now fails closed with `model_catalog_search_js_orchestrator_retired` before JS provider iteration, entry enrichment, result aggregation, or `lastCatalogSearch` writes, the fail-closed `catalog-operations.mjs` helper module is deleted, and mounted `ModelMountingState` catalog methods now own storage-summary readback, catalog-search refusal, and catalog-variant enrichment refusal directly; public catalog-provider configuration list/get now route through Rust daemon-core `model_mount.catalog_provider_configuration.list`/`get` before JS map reads, runtime-material resolution, provider-port iteration, public config projection, or provider-status attachment, and broad snapshot/projection transport no longer sends `catalog_provider_configs` or emits `catalogProviderConfigs` compatibility fields; public catalog-status readback now routes to the Rust `catalog_status` read-projection boundary with runtime `state_dir`, replays admitted `model-provider-inventory/*.json` records into Rust-authored catalog provider status, storage status, last-search summary, and result rows, deletes the old JS edge refusal translator, and no longer preserves JS `catalogStatus()` or `catalogStatusProjectionInput()` helpers; direct Rust `catalog_status` ignores `catalog_status_input` while catalog status replays admitted provider-inventory truth before JS provider-port iteration, provider-status attachment, storage summary, `lastCatalogSearch` reads, result materialization, or catalog-status transport; non-search catalog variant enrichment now fails closed with `model_catalog_variant_enrichment_js_retired` before JS storage summary reads, artifact map materialization, fixture variant lookup, backend/download/recommendation synthesis, or selection receipt-field generation, and the retired `catalog-entries.mjs` materializer/test module is deleted so JS no longer preserves fixture/local-manifest/Hugging Face/Ollama catalog entry shaping helpers; the orphan `catalog-projections.mjs` product/config/auth projection helper/test module is deleted, the catalog-provider port/registry helper surface is deleted, and the remaining catalog-provider source/auth shaping plus OAuth boundary helper surface is also deleted: `catalog-provider-config.mjs` now contains the catalog-provider-control Rust client helper, Agentgres record-state commit adapter, and defensive missing-planner error while `catalog-provider-config.test.mjs`, `oauth-boundary.mjs`, and `oauth-boundary.test.mjs` are absent, so JS no longer preserves source-material parsers, auth config shapers, auth-header materialization, OAuth token transport, PKCE helpers, OAuth vault-ref helpers, public OAuth session/state projection, or OAuth boundary projection compatibility helpers | Rust daemon-core catalog-provider control/search/status/custody object for provider configuration, OAuth state/session custody, wallet/cTEE vault binding, auth-header resolution, external/local/fixture catalog search transport, catalog status and variant projection, receipts, record-state, and projection; the catalog-provider-control command transport is retired, while direct Rust search/custody/materialization APIs and stable protocol APIs must still replace remaining JS state materialization | `packages/runtime-daemon/src/model-mounting.mjs`, `packages/runtime-daemon/src/model-mounting/catalog-provider-config.mjs`, `packages/runtime-daemon/src/model-mounting/read-projection-facade.mjs`, `crates/node/src/bin/ioi_step_module_bridge/mod.rs`, `crates/services/src/agentic/runtime/kernel/model_mount.rs` | `hypervisor-conformance:receipts`, `hypervisor-conformance:ctee` |

Implementation note: mounted model_mount topology accessors and loaded-instance maintenance selectors now consume Rust read-projection lists for endpoints, providers, routes, instances, and artifacts; load/unload and maintenance transitions no longer refresh `state.instances` after the Rust Agentgres commit, and the `state accessor reads remain adapters` caveat above now refers to temporary protocol/materialization transport, not JS topology-map truth.

Implementation note: dead catalog download policy/risk/recommendation helpers are deleted from `catalog-helpers.mjs`; JS no longer preserves `catalogDownloadRisk()`, `normalizeDownloadPolicy()`, or `catalogApprovalDecision()` as a parallel catalog/download policy layer.

Implementation note: the orphan `download-helpers.mjs` HTTP/partial-file catalog download materializer is deleted; JS no longer preserves fixture/live download, resume metadata, retry, checksum, or failed-download cleanup execution helpers for catalog/download facades.

Implementation note: the dormant catalog import materializer helper tail is deleted from `catalog-helpers.mjs`; JS no longer preserves `normalizeImportMode()`, `importTargetPath()`, `materializeImportArtifact()`, `inferModelArchitecture()`, or `inferParameterCount()` as local artifact import execution or catalog metadata-shaping helpers.

Implementation note: the catalog-provider port and registry helper surface is deleted; JS no longer preserves `catalog-provider-ports.mjs`, `catalog-provider-ports.test.mjs`, `catalog-registry.mjs`, `catalog-registry.test.mjs`, mounted `catalogProviderPorts()`, provider-port ordering, health-status merge, `operations: ["search", ...]` metadata, or `catalogProviderStatus()` compatibility helpers.

Implementation note: the catalog-provider source/auth shaping and OAuth boundary helper surface is deleted; JS no longer preserves `catalog-provider-config.test.mjs`, `oauth-boundary.mjs`, `oauth-boundary.test.mjs`, `catalogProviderRuntimeMaterialFromBody()`, `catalogProviderAuthConfig()`, `catalogProviderAuthHeaders()`, `fetchOAuthToken()`, `parseOAuthTokenResponse()`, PKCE helpers, OAuth vault-ref helpers, or public OAuth projection helpers as compatibility scaffolding.

Implementation note: `ModelCatalogProviderControl` now has a positive Rust daemon-core control path. Public catalog-provider config list/get/write, private config/material resolution, and OAuth start/callback/exchange/refresh/revoke call `plan_model_mount_catalog_provider_control`, commit only Rust-authored `model-catalog-provider-controls` records through Agentgres model_mount record-state admission, and keep JS from executing OAuth credential helpers, resolving provider vault refs, reading provider config/runtime-material maps as accepted truth, writing OAuth/session/provider maps, or returning plaintext catalog-provider material. JS-side `catalog-provider-config.mjs` now contains the catalog-provider-control Rust client helper, Agentgres record-state commit adapter, and defensive missing-planner error, not an execution truth path. The remaining non-terminal work is auth-header materialization, hosted catalog transport/materialization, and stable protocol APIs; the catalog-provider-control command transport is retired.

| `ModelCapabilityTokenControl` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md), [`wallet-network/doctrine.md`](../components/wallet-network/doctrine.md) | public capability-token create/list/authorize/revoke now call Rust daemon-core `plan_model_mount_capability_token_control`, receive Rust-authored `capability-tokens` records with wallet.network authority evidence, commit only those records through Rust Agentgres model_mount record-state admission, and return committed Rust response envelopes; Rust replays admitted `capability-tokens/*.json` records for list/authorize/revoke plus broad `projection`, `snapshot`, and `authoritySnapshot()` grants/tokens through the Rust custody read projection, committed records persist token hashes and authority facts without plaintext token material, and the fail-closed `capability-token-operations.mjs` helper module plus orphaned `publicToken()` formatter remain deleted | Deeper Rust wallet authority policy, revocation epochs, and stable SDK/IDE capability-token APIs remain non-terminal; the capability-token command transport is retired, and JS remains only as protocol/Bearer preflight and Rust record-commit adapter for this migrated boundary | `packages/runtime-daemon/src/model-mounting/capability-token-control.mjs`, `packages/runtime-daemon/src/model-mounting.mjs`, `packages/runtime-daemon/src/model-mounting/model-mount-core.mjs`, `crates/services/src/agentic/runtime/kernel/model_mount/capability_token_control.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/read_projection/custody.rs` | `hypervisor-conformance:receipts`, `hypervisor-conformance:negative` |
| `ModelMcpWorkflowControl` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md), [`hypervisor-kernel-substrate-migration-matrix.md`](./hypervisor-kernel-substrate-migration-matrix.md) | public MCP import, ephemeral MCP registration, MCP tool invocation, and workflow-node execution now call typed `daemonCoreModelMountApi.planModelMountMcpWorkflow`, backed by Rust `RuntimeKernel::plan_model_mount_mcp_workflow`, receive Rust-authored `mcp-servers` or `mcp-workflow-controls` records with workflow hashes, authority hashes, receipt refs, custody facts, no-JS-execution flags, and Agentgres truth evidence, then require Rust Agentgres model_mount record-state commit before returning public truth; MCP tool invocation and workflow-node external exits now require Rust-enforced `wallet.network.mcp_external_exit` grant refs, authority receipt refs, cTEE custody refs, and transport containment refs, bind them into the Rust authority hash and committed control details, and reject no-authority/no-custody/no-containment requests before JS transport or compatibility behavior can run; MCP tool invocation now returns a Rust-admitted transport execution contract with `transport_execution_status: "rust_admitted"`, content receipt refs, StepModuleRouter owner, materialized protocol result payload and hash, and explicit no-JS/no-command/no-binary-bridge/no-compatibility fallback flags, workflow-node execution returns the matching `execution_status: "rust_admitted"` StepModule dispatch contract with the same materialized result binding, and the JS model-mount core rejects stale `rust_required` or pending-materialization MCP workflow execution responses instead of normalizing them into public truth; those execution ops now also return Rust-authored `ioi.model_mount.mcp_workflow_receipt.v1` receipts with workflow/authority hashes, custody/containment refs, Agentgres operation refs, state-root binding, StepModuleRouter result binding, materialized result payloads, and `result_payload_hash` binding, and JS requires `persistRustAuthoredReceiptWithCommit()` plus the materialized result binding before public execution truth returns; public `listMcpServers()` now calls Rust read-projection kind `mcp_servers` with runtime `state_dir`, where Rust replays persisted `mcp-servers/*.json`, filters to Rust-authored MCP workflow records, and returns only redacted MCP server projections; the model_mount MCP workflow command operation/dispatch arm, bridge request/response wrapper, command-envelope builder, direct JS receipt synthesis, MCP server record-state commits, MCP server map mutation/readback, authorization, fixture tool execution, route tests, receipt-gate dispatch, model invocation, `publicMcpServer()` shaping, the old projection-required refusal shim, the fail-closed `mcp-workflow-operations.mjs` helper module, and the orphan `workflow-node.mjs` response helper/test module remain retired | Rust daemon-core model_mount MCP/workflow planning and read-projection object backed by wallet authority, StepModuleRouter dispatch planning, receipt_binder, Agentgres record-state admission, materialized result payload binding, projection, and replay; live external MCP transport backend invocation/discovery, broader runtime containment sandboxing, and stable SDK/IDE APIs remain non-terminal | `packages/runtime-daemon/src/model-mounting.mjs`, `packages/runtime-daemon/src/model-mounting/model-mount-core.mjs`, `packages/runtime-daemon/src/model-mounting/read-projection-facade.mjs`, `packages/runtime-daemon/src/model-mounting/mcp-workflow-operations.test.mjs`, `crates/services/src/agentic/runtime/kernel/model_mount.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/mcp_workflow.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/read_projection/mcp.rs`, `crates/services/src/agentic/runtime/kernel/command_protocol.rs`, `crates/services/src/agentic/runtime/kernel/command_dispatch.rs`, `scripts/conformance/hypervisor-conformance.mjs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:receipts` |
| `ModelReceiptGateControl` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md) | direct receipt-gate validation now calls Rust daemon-core `plan_model_mount_receipt_gate`, passes canonical receipt facts and required tool receipt ids to Rust, receives a Rust-authored `workflow_receipt_gate` or `workflow_receipt_gate_blocked` receipt with receipt-binder and Agentgres gate evidence, and persists only that receipt through Rust Agentgres model_mount receipt-state admission; JS no longer computes gate failures, creates gate receipts, or accepts receipt-gate truth without the Rust planner | Broader MCP/workflow execution, workflow policy composition, and stable SDK/IDE receipt-gate APIs remain non-terminal; the receipt-gate command transport is retired | `packages/runtime-daemon/src/model-mounting/validation.mjs`, `packages/runtime-daemon/src/model-mounting.mjs`, `packages/runtime-daemon/src/model-mounting/model-mount-core.mjs`, `crates/services/src/agentic/runtime/kernel/model_mount/receipt_gate.rs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:receipts` |
| `ModelVaultControl` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md), [`wallet-network/doctrine.md`](../components/wallet-network/doctrine.md), [`private-workspace-ctee.md`](../components/daemon-runtime/private-workspace-ctee.md) | public vault bind/list/metadata/status/health/remove now call Rust daemon-core `plan_model_mount_vault_control`, receive Rust-authored `vault-refs` records with wallet.network authority and cTEE custody evidence, commit only those records through Rust Agentgres model_mount record-state admission, and return committed Rust response envelopes; Rust replays admitted `vault-refs/*.json` bind/remove records for list/metadata/status/health plus broad `projection`, `snapshot`, and `authoritySnapshot()` vault refs through the Rust custody read projection, committed records persist vault_ref_hash, material_hash, custody facts, and authority facts without plaintext material, and JS vault-port metadata/write helpers no longer author accepted vault truth; the fail-closed `vault-operations.mjs` helper module and orphaned `publicVaultRefs()` formatter remain deleted, while mounted public `ModelMountingState` vault methods only own canonical request alias rejection, required `vault_ref`/`material` preflight, material hashing, Rust planning, Rust record commit, and committed-response forwarding | Deeper cTEE material storage, provider auth materialization, and stable SDK/IDE vault APIs remain non-terminal; the vault-control command transport is retired, and JS vault-port internals may remain only as non-authoritative migration custody plumbing until direct Rust wallet/cTEE vault APIs are verified | `packages/runtime-daemon/src/model-mounting/vault-control.mjs`, `packages/runtime-daemon/src/model-mounting.mjs`, `packages/runtime-daemon/src/model-mounting/model-mount-core.mjs`, `crates/services/src/agentic/runtime/kernel/model_mount/vault_control.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/read_projection/custody.rs` | `hypervisor-conformance:receipts`, `hypervisor-conformance:ctee` |
| `ModelBackendLifecycle` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md) | public backend health/start/stop/log APIs now call Rust model_mount through `plan_model_mount_backend_lifecycle`, receive Rust-authored `model-backend-lifecycle-controls` records, commit only those records through Rust Agentgres model_mount record-state admission, and return Rust public responses that mark JS backend-registry reads, process control, and log reads/writes false before any JS lifecycle receipt, backend mutation, local log read, or backend registry truth can run; the fail-closed `backend-lifecycle.mjs` helper module is deleted, the old backend-lifecycle required-record facade and command operation are retired, typed `daemonCoreModelMountApi.planModelMountBackendProcess` / `planModelMountBackendLifecycle` replace the backend-process/backend-lifecycle command envelopes, and Rust `command_protocol.rs` rejects both retired planning operations; mounted public `ModelMountingState` backend methods pass only the requested `backend_id` plus canonical operation body without local backend kind inference, backend-process ensure/touch/start/spawn/stop refusals remain fail-closed with `model_mount_backend_process_supervisor_retired`, backend-process planning now lives in Rust `model_mount/backend_process.rs`, and `model_mount.backend_lifecycle` metadata is owned by the Rust positive control record; the leftover `backend-processes.mjs` lookup/snapshot helper module and test are deleted, and mounted `ModelMountingState` now owns missing-backend lookup metadata plus backend-process snapshot normalization directly; the private backend registry log helper no longer writes `backend-logs/*.jsonl` and returns only redacted non-persistent telemetry, and derived backend registry records no longer read `state.providers` or provider-map records for LM Studio, OpenAI-compatible, Ollama, or vLLM status, base URL, or public-CLI binary path projection; the exported JS `backendRegistry()` overlay that merged derived backend defaults, `state.backends`, and process snapshots is deleted, and mounted `ModelMountingState.backendRegistry()` plus `backend()` now consume the Rust `backends` read projection instead of deriving from `state.backends`, so lower-level JS process supervision and provider-map projection cannot create duplicate local backend truth paths; public backend list readback now routes through the Rust model-mount read-projection API as projection kind `backends`, sends empty JS request state plus runtime `state_dir`, replays admitted `model-backend-lifecycle-controls/*.json`, filters out JS-authored lifecycle controls, and returns Rust backend projection records through `/api/v1/backends` or `/api/v1/models/backends`; Rust-backed native fixture artifact/endpoint materialization no longer embeds `backendRegistry` arrays from JS seeding; default model-mount seeding no longer writes derived backend records into `state.backends`, and `seedBackends()` fails closed with `model_mount_backend_projection_rust_core_required`, so backend inventory/projection cannot re-enter as fixture-carried or default-seeded JS truth | Rust daemon-core backend lifecycle/control/projection object; backend-process/backend-lifecycle planning command transport is retired; process control, provider lifecycle execution, richer backend process-state materialization, and stable protocol APIs remain required | `packages/runtime-daemon/src/model-mounting.mjs`, `packages/runtime-daemon/src/model-mounting/model-mount-core.mjs`, `packages/runtime-daemon/src/model-mounting/backend-registry-state.mjs`, `packages/runtime-daemon/src/model-mounting/default-discovery.mjs`, `packages/runtime-daemon/src/model-mounting/default-records.mjs`, `packages/runtime-daemon/src/model-mounting/state-seeding.mjs`, `packages/runtime-daemon/src/runtime-route-handlers.mjs`, `crates/node/src/bin/ioi_step_module_bridge/mod.rs`, `crates/services/src/agentic/runtime/kernel/model_mount.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/backend_process.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/backend_lifecycle.rs` | `hypervisor-conformance:receipts` |
| `RuntimeAgentRunCreateControl` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md) | public agent creation now requires Rust `plan_agent_create_state_update` before provider/model-route/MCP/runtime-control candidate facts can persist, rejects missing Rust projection, mismatched operation kind, or incomplete identity/timestamps, commits only the Rust-returned `agent.create` projection through Agentgres-backed `writeAgent`, and keeps direct `agents` map mutation retired; top-level thread creation now requires Rust `plan_thread_create_state_update` before JS route/provider/MCP/runtime-control candidate facts can persist, rejects missing Rust agent/thread projections, mismatched operation kind, incomplete identity/timestamps, or agent/thread identity mismatch, commits only the Rust-returned `thread.create` agent projection through Agentgres-backed `writeAgent`, emits thread-start projection through the Rust thread-event surface, returns the Rust-authored thread/turn projection, and remains fail-closed before route planning when the Rust planner is absent; agent-scoped run creation now requires Rust `plan_run_create_state_update` before JS agent lookup, provider checks, run model route/memory resolution, canonical run candidate construction, usage envelope assembly, or persistence can proceed, rejects missing Rust projection, mismatched operation kind, or incomplete identity/timestamps, commits only the Rust-returned `run.create` projection through Agentgres-backed `writeRun`, keeps direct `runs` map mutation retired, and ignores retired thread/approval plus diagnostics request aliases; public agent create, top-level thread create, and agent-scoped run create routes call the mounted agent/run lifecycle surface directly, and `RuntimeDaemonStore.createAgent()` / `RuntimeDaemonStore.createThread()` / `RuntimeDaemonStore.createRun()` compatibility pass-throughs are removed so route-level creation cannot re-enter through daemon-store lifecycle shims; agent/run/thread creation required-boundary envelopes are authored by Rust `LifecycleAdmissionRequiredCore` through `plan_lifecycle_admission_required`; Rust agent/thread/run create/status policy ownership, validation, and focused tests live in `policy/thread_lifecycle.rs` behind the policy facade; lifecycle command response functions import directly from Rust policy in `ioi_step_module_bridge/mod.rs` instead of a Node child delegate | Rust daemon-core agent/thread/run admission, persistence, projection, wallet/cTEE policy, and Agentgres expected-head/state-root binding object | `packages/runtime-daemon/src/runtime-agent-run-lifecycle.mjs`, `packages/runtime-daemon/src/runtime-context-policy-core.mjs`, `packages/runtime-daemon/src/runtime-route-handlers.mjs`, `packages/runtime-daemon/src/http/public-runtime-routes.mjs`, `crates/services/src/agentic/runtime/kernel/policy.rs`, `crates/services/src/agentic/runtime/kernel/policy/thread_lifecycle.rs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:compositor` |
| `RuntimeLifecycleProjectionControl` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md) | public agent, thread, run, agent-run, thread usage, thread turns/detail, thread events, run wait, run conversation, run usage, run events, run replay, run trace/inspect, run computer-use trace/trajectory, run scorecard, run artifact list/detail, top-level usage, and authority-evidence projections now route through the mounted lifecycle projection surface before route-facing JS agent/thread/run maps, usage telemetry, authority-evidence helpers, turn projection, event/replay streams, or trace/artifact helpers can author lifecycle truth; mounted daemon lifecycle projection surface calls typed `daemonCoreRuntimeProjectionApi.projectRuntimeLifecycle`, backed by Rust `RuntimeKernelService::project_runtime_lifecycle`, so `/v1/agents`, `/v1/agents/:id`, `/v1/agents/:id/runs`, `/v1/threads`, `/v1/threads/:id`, `/v1/threads/:id/usage`, `/v1/threads/:id/turns`, `/v1/threads/:id/turns/:turn_id`, `/v1/threads/:id/events`, `/v1/threads/:id/events/stream`, `/v1/runs`, `/v1/runs/:id`, `/v1/runs/:id/wait`, `/v1/runs/:id/conversation`, `/v1/runs/:id/usage`, `/v1/runs/:id/events`, `/v1/runs/:id/replay`, `/v1/runs/:id/trace`, `/v1/runs/:id/inspect`, `/v1/runs/:id/computer-use/trace`, `/v1/runs/:id/computer-use/trajectory`, `/v1/runs/:id/scorecard`, `/v1/runs/:id/artifacts`, `/v1/runs/:id/artifacts/:artifact_ref`, `/v1/usage`, `/v1/authority-evidence`, and `/v1/workflow-capability-preflights` send only route identifiers plus runtime `state_dir`, Rust replays admitted `agents/*.json`, `runs/*.json`, and `events/*.jsonl` Agentgres records before deriving projections, and retired JS lifecycle candidate transport fields (`agents`, `runs`, `events`, `replay`, `usage`, `trace`, `artifacts`, `artifact`, and related singular candidates) are rejected; the native `/api/v1/authority-evidence`, `/api/v1/authority-evidence-summaries`, `/api/v1/workflow-capability-preflight-evidence`, and `/api/v1/workflow-capability-preflight` compatibility aliases are retired so this migrated authority-evidence/preflight read family has one canonical protocol path; the daemon lifecycle surface no longer reads JS agent/run maps, thread/turn helpers, usage helpers, authority-evidence event helpers, event streams, replay helpers, trace helpers, or artifact resolvers before Rust, run replay no longer travels through `eventsForRun` or `replayFromCanonicalState`, the run-read surface no longer exposes `listUsage` or `authorityEvidenceSummary`, and the old `authority-evidence-summary.mjs` helper/test are deleted; the old `RuntimeLifecycleProjectionRequiredCore` path, JS generic `project_runtime_lifecycle` operation envelope, Rust command operation/dispatch arm, command-response wrapper, JS cache candidate transport, duplicate run replay facade, JS authority-evidence facade, top-level usage/evidence run-read facade, and migrated native authority-evidence compatibility aliases are retired | Wallet/cTEE authority where lifecycle access exits local trust, receipt/state-root binding for every lifecycle read projection, richer ArtifactRef/PayloadRef-aware run artifact projection, and stable IDE/CLI/SDK protocol APIs over Rust Agentgres replay records | `packages/runtime-daemon/src/runtime-lifecycle-projection-surface.mjs`, `packages/runtime-daemon/src/runtime-lifecycle-projection-surface.test.mjs`, `packages/runtime-daemon/src/runtime-run-read-surface.mjs`, `packages/runtime-daemon/src/http/public-runtime-routes.mjs`, `packages/runtime-daemon/src/runtime-route-handlers.mjs`, `packages/runtime-daemon/src/runtime-context-policy-core.mjs`, `packages/runtime-daemon/src/runtime-daemon-core-direct-invoker-service.test.mjs`, `crates/services/src/agentic/runtime/kernel/runtime_lifecycle.rs`, `crates/services/src/agentic/runtime/kernel/mod.rs`, `crates/services/src/agentic/runtime/kernel/command_protocol.rs`, `scripts/conformance/hypervisor-conformance.mjs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:compositor` |
| `RuntimeContextPressureAlertProjectionControl` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md) | runtime context-pressure alert projection no longer synthesizes JS `receipt_context_pressure...` or `policy_context_pressure...` refs; advisory `context.pressure_alert` rows keep empty `receipt_refs` and `policy_decision_refs` until Rust receipt/policy admission supplies admitted refs | Rust daemon-core usage telemetry, context-budget policy, receipt/policy admission, Agentgres projection, replay, and stable protocol APIs | `packages/runtime-daemon/src/runtime-usage-events.mjs`, `packages/runtime-daemon/src/runtime-usage-events.test.mjs`, `scripts/conformance/hypervisor-conformance.mjs` | `hypervisor-conformance:compositor` |
| `RuntimeDiagnosticsFeedbackProjectionControl` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md) | diagnostics feedback projection no longer synthesizes JS `receipt_lsp_diagnostics...`, blocking-gate receipt IDs, or JS blocking-gate policy-decision refs; advisory diagnostics injection and gate rows pass through admitted diagnostic event refs plus Rust diagnostics repair policy projection refs, with local `receipt_id`, `policy_decision_id`, and `policy_decision_refs` left empty until Rust diagnostics-repair admission supplies admitted refs; post-edit diagnostics feedback planning routes through Rust `plan_post_edit_diagnostics_feedback`, and pending feedback policy projection routes through Rust `project_runtime_diagnostics_repair_policy`, so JS only forwards canonical diagnostics facts, repair contexts, and Rust-authored request/policy envelopes | Rust daemon-core diagnostics repair admission, durable Agentgres projection/replay, wallet-governed override authority, receipt/state-root binding, and stable protocol APIs | `packages/runtime-daemon/src/diagnostics-feedback.mjs`, `packages/runtime-daemon/src/diagnostics-feedback.test.mjs`, `packages/runtime-daemon/src/runtime-diagnostics-feedback-surface.mjs`, `packages/runtime-daemon/src/runtime-diagnostics-feedback-surface.test.mjs`, `packages/runtime-daemon/src/runtime-context-policy-core.mjs`, `crates/services/src/agentic/runtime/kernel/coding_tool_event.rs`, `crates/services/src/agentic/runtime/kernel/runtime_diagnostics_repair_policy.rs`, `scripts/conformance/hypervisor-conformance.mjs` | `hypervisor-conformance:compositor` |
| `RuntimeDiagnosticsRepairPolicyProjectionControl` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md) | diagnostics rollback repair policy projection is now Rust-owned through `project_runtime_diagnostics_repair_policy`; Rust emits canonical snake_case rollback repair policy/config/decision refs and projection receipts, pending diagnostics feedback fails closed without that Rust projector, and the JS `diagnosticsRollbackRepairPolicy`, default-decision helper, and context policy aggregation facade are retired | Rust daemon-core diagnostics repair policy projection exists; wallet-governed override authority, durable Agentgres expected-head/state-root binding, receipt/policy binding, replay, and stable IDE/CLI/SDK protocol APIs remain pending | `crates/services/src/agentic/runtime/kernel/runtime_diagnostics_repair_policy.rs`, `crates/services/src/agentic/runtime/kernel/command_protocol.rs`, `crates/services/src/agentic/runtime/kernel/command_dispatch.rs`, `packages/runtime-daemon/src/runtime-context-policy-core.mjs`, `packages/runtime-daemon/src/runtime-context-policy-core.test.mjs`, `packages/runtime-daemon/src/diagnostics-repair-policy.mjs`, `packages/runtime-daemon/src/diagnostics-repair-policy.test.mjs`, `packages/runtime-daemon/src/diagnostics-feedback.mjs`, `packages/runtime-daemon/src/diagnostics-feedback.test.mjs`, `scripts/conformance/hypervisor-conformance.mjs` | `hypervisor-conformance:compositor` |
| `RuntimeThreadEventAdmission` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md) | generic daemon runtime thread-event append now calls Rust `admit_runtime_thread_event` through the mounted Agentgres core, requires receipt refs, expected heads, state roots, storage admission, payload refs, and projection watermarks, and registers only the Rust-returned event in the temporary local replay cache; synthetic `thread.started` and legacy run-event-to-runtime-event projection now call Rust `project_runtime_thread_events`, which authors the projection envelopes from canonical agent/run facts, rejects retired projection aliases, skips known idempotency keys, admits each projected event through Rust Agentgres admission, and returns only Rust-admitted events for local replay registration; public stream/turn/run readback now calls Rust `project_runtime_thread_event_replay` with replay kind, cursor, latest seq, and runtime `state_dir`, so Rust loads admitted `events/*.jsonl` Agentgres records, owns stream/turn selection, canonical cursor evaluation, required Agentgres admission refs, state/head/watermark projection, and the returned event list, and rejects caller-supplied replay `events` transport; the JS replay-candidate collector and public `replayFromCanonicalState` facade are deleted, the thread-event surface no longer receives the JS `ttiEnvelopeForRunEvent` builder, and JS replay helpers no longer call `runtimeCursorSeq()` or filter by stream/turn locally | Rust daemon-core thread-event admission/projection/replay object backed by Agentgres expected heads/state roots, receipt/event materialization, replay watermarks, and stable IDE/CLI/SDK event APIs | `packages/runtime-daemon/src/threads/thread-replay.mjs`, `packages/runtime-daemon/src/runtime-agentgres-admission-core.mjs`, `packages/runtime-daemon/src/runtime-thread-event-surface.mjs`, `crates/services/src/agentic/runtime/kernel/runtime_thread_event.rs`, `crates/services/src/agentic/runtime/kernel/projection.rs`, `crates/services/src/agentic/runtime/kernel/agentgres_admission.rs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:receipts`, `hypervisor-conformance:compositor` |
| `RuntimeThreadTurnProjection` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md) | public thread and turn projection records now call Rust `project_runtime_thread_turn_projection`, so Rust authors the public record shape, runtime identity fields, projection hashes, seq ranges, and input/output item ids over canonical agent/run facts plus Rust replay events; JS `threadForAgent()` and `turnForRun()` only gather canonical facts and fail closed without the Rust projection API instead of composing public records locally | Rust daemon-core thread/turn projection object backed by Agentgres-admitted runtime events, replay watermarks, canonical run facts, and stable IDE/CLI/SDK projection APIs | `packages/runtime-daemon/src/threads/thread-turn-projection.mjs`, `packages/runtime-daemon/src/runtime-agentgres-admission-core.mjs`, `packages/runtime-daemon/src/index.mjs`, `crates/services/src/agentic/runtime/kernel/runtime_thread_event.rs`, `crates/services/src/agentic/runtime/kernel/command_protocol.rs`, `crates/services/src/agentic/runtime/kernel/command_dispatch.rs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:compositor` |
| `RuntimeAgentgresStateCacheControl` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md), [`agentgres/doctrine.md`](../components/agentgres/doctrine.md) | runtime agent and subagent persistence now commits through Rust Agentgres state admission before refreshing JS in-memory cache maps, and focused tests prove rejected Rust commits leave `agents`/`subagents` maps untouched; cache hydration remains restart/readback scaffolding rather than accepted truth | Rust daemon-core Agentgres admission/projection object owns expected heads, state roots, storage admissions, durable writes, and projection cache invalidation; JS maps are removable read-through caches once direct Rust lifecycle/projection APIs own the read side | `packages/runtime-daemon/src/threads/thread-persistence.mjs`, `packages/runtime-daemon/src/runtime-agentgres-admission-core.mjs`, `crates/services/src/agentic/runtime/kernel/agentgres_admission.rs` | `hypervisor-conformance:receipts`, `hypervisor-conformance:negative` |
| `RuntimeAgentDeleteControl` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md) | permanent agent deletion now calls Rust `plan_agent_delete_state_update`; the JS store supplies only the current agent fact, rejects missing Rust tombstone output, mismatched operation kind, or incomplete tombstones, commits only the Rust-returned `agent.delete` tombstone through Agentgres-backed `writeAgent`, and still avoids JS run-count retention checks, `agents` map deletion, local JSON removal, or duplicate operation append | Rust daemon-core agent deletion admission/projection object backed by wallet/retention authority, Agentgres expected-head/state-root binding, receipt/event materialization, replay, and projection | `packages/runtime-daemon/src/threads/thread-store.mjs`, `packages/runtime-daemon/src/runtime-context-policy-core.mjs`, `crates/services/src/agentic/runtime/kernel/policy.rs`, `crates/services/src/agentic/runtime/kernel/policy/thread_lifecycle.rs`, `crates/services/src/agentic/runtime/kernel/agentgres_admission.rs` | `hypervisor-conformance:receipts`, `hypervisor-conformance:negative` |
| `RuntimeAgentStatusControl` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md) | archive, unarchive, resume, close, reload, and generic status-control now call Rust `plan_agent_status_state_update`; the JS store supplies only current agent/requested status facts, rejects missing Rust planner output or mismatched operation kind, and commits only the Rust-returned agent projection through Agentgres-backed `writeAgent`; public agent status/delete routes call the mounted agent/run lifecycle surface directly instead of daemon-store pass-through wrappers; Rust agent status/delete policy ownership, validation, and focused tests live in `policy/thread_lifecycle.rs`, the JS context-policy core reaches them through typed `daemonCoreThreadLifecycleApi` methods, and Rust rejects the old agent status/delete state-update command operations | Rust daemon-core agent lifecycle/status admission/projection object backed by wallet/retention authority, Agentgres expected-head/state-root binding, receipt/event materialization, replay, and projection | `packages/runtime-daemon/src/threads/thread-store.mjs`, `packages/runtime-daemon/src/runtime-agent-run-lifecycle.mjs`, `packages/runtime-daemon/src/runtime-context-policy-core.mjs`, `packages/runtime-daemon/src/runtime-route-handlers.mjs`, `packages/runtime-daemon/src/index.mjs`, `crates/services/src/agentic/runtime/kernel/mod.rs`, `crates/services/src/agentic/runtime/kernel/policy.rs`, `crates/services/src/agentic/runtime/kernel/policy/thread_lifecycle.rs`, `crates/services/src/agentic/runtime/kernel/agentgres_admission.rs`, `crates/services/src/agentic/runtime/kernel/command_protocol.rs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:receipts` |
| `RuntimeThreadMemoryControl` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md), [`agentgres/doctrine.md`](../components/agentgres/doctrine.md) | public thread and agent memory write/edit/delete plus policy mutation now call Rust daemon-core `plan_runtime_memory_control` through typed `daemonCoreThreadMemoryApi.planRuntimeMemoryControl`, send runtime `state_dir`, reject JS `current_record`/`current_policy` candidate transport, replay admitted `memory-records/*.json` for edit/delete current-record truth and `memory-policies/*.json` for policy current truth in Rust, commit only Rust-planned record/policy payloads through Rust Agentgres `commitRuntimeMemoryState`, refresh the temporary memory projection cache, and return Rust-projected route output; missing Rust planning, missing state_dir replay, missing current record for edit/delete, or missing Rust memory-state commit fails closed before direct JS memory-store mutation; status-record, validation-record, and direct memory control-event append now call Rust daemon-core `plan_runtime_memory_control` through the same typed API for event-only `memory.status`/`memory.validate` payloads and admit only the Rust-authored runtime events through Rust Agentgres event admission; direct memory mutation helper still fails closed before JS runtime-event append, Rust planner fallback, `agents` map mutation, `writeAgent`, or duplicate receipt/policy evidence synthesis; explicit public thread/agent memory list/policy/path/status/validation route projections now call Rust daemon-core `project_runtime_memory_projection` through typed `daemonCoreThreadMemoryApi.projectRuntimeMemoryProjection`, so JS supplies only route-owned thread/agent context, filters, and runtime `state_dir`, while Rust replays admitted `memory-records/*.json` and `memory-policies/*.json`, filters active canonical records, synthesizes effective policy/path/status/validation truth, rejects retired JS projection candidate transport, and fails closed when the Rust projector is missing or returns a mismatched projection kind before JS `AgentMemoryStore` projection, effective-policy, path, status, or validation helpers can author public memory truth; top-level `/v1/memory`, `/v1/memory/records`, `/v1/memory/policy`, `/v1/memory/path`, and `/v1/memory/validate` context-query/body routes are retired, daemon-store `memoryProjectionForContext`/`memoryStatus`/`validateMemory` helpers are deleted, SDK global `getMemoryStatus()`/`validateMemory()` clients and context-query input types are deleted, and conformance guards that memory read/status/validation enters only through explicit thread/agent protocol routes; agent/thread memory mutation, policy, status, and validation routes now call the mounted thread-memory surface directly instead of daemon-store pass-through wrappers, so route-level memory control cannot re-enter through `store.rememberForThread()`, `store.rememberForAgentId()`, policy, edit/delete, status, or validation compatibility shims; direct `AgentMemoryStore` record/policy/state writers and run-memory write/policy command paths also fail closed before JS record-map mutation, policy-map mutation, file deletion/write, receipt synthesis, or injected memory-state commits; the Rust projection/control boundary plus SDK memory output contracts now accept only canonical memory projection envelope/policy/path/record fields (`schema_version`, `thread_id`, `agent_id`, `total_matches`, `injection_enabled`, `read_only`, `write_requires_approval`, `subagent_inheritance`, `records_path`, `policies_path`, `effective_policy_id`, `memory_key`, `fact_hash`) instead of retired camelCase aliases; Rust runtime-memory route projection lives in `runtime_memory_projection.rs`, public memory write/edit/delete/policy plus status/validation event planning lives in `runtime_memory_control.rs`, Rust memory-manager validation/status projection and thread-memory state-update policy ownership live in `policy/mcp_memory.rs`, the JS policy core sends canonical request bodies without generic command `operation`/`backend`, and Rust `command_protocol.rs`/`command_dispatch.rs` reject the old memory projection/control, memory-manager status/validation, and thread-memory agent state-update command operations; JS memory helpers remain protocol/commit-client scaffolding for mutation/write coordination, but public memory read projections and mutation current truth no longer accept JS candidates | Rust daemon-core memory admission/projection object backed by wallet/policy authority, Agentgres expected-head/state-root binding, receipt/event materialization, ArtifactRef/PayloadRef where needed, replay, and projection | `packages/runtime-daemon/src/threads/thread-memory-state.mjs`, `packages/runtime-daemon/src/threads/run-memory-resolution.mjs`, `packages/runtime-daemon/src/memory-store.mjs`, `packages/runtime-daemon/src/runtime-context-policy-core.mjs`, `packages/runtime-daemon/src/index.mjs`, `packages/runtime-daemon/src/runtime-route-handlers.mjs`, `packages/runtime-daemon/src/http/public-runtime-routes.mjs`, `packages/agent-sdk/src/substrate-client.ts`, `packages/agent-sdk/src/messages.ts`, `crates/services/src/agentic/runtime/kernel/runtime_memory_projection.rs`, `crates/services/src/agentic/runtime/kernel/runtime_memory_control.rs`, `crates/services/src/agentic/runtime/kernel/command_protocol.rs`, `crates/services/src/agentic/runtime/kernel/command_dispatch.rs`, `crates/services/src/agentic/runtime/kernel/policy.rs`, `crates/services/src/agentic/runtime/kernel/policy/mcp_memory.rs`, `crates/services/src/agentic/runtime/kernel/agentgres_admission.rs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:compositor`, `hypervisor-conformance:receipts` |
| `RuntimeThreadControl` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md) | public thread mode, model, thinking, and generic runtime-control facades now call Rust `plan_thread_control_agent_state_update`; the JS surface only gathers canonical agent/control/event-sequence facts, performs canonical snake_case model-route resolution for model/thinking, fails closed without the Rust planner before agent lookup, and persists only the Rust-authored agent projection through Agentgres-backed `writeAgent`; direct `appendThreadRuntimeControlEvent()` remains retired/fail-closed before JS runtime-event append, workspace-trust warning append from the mode path now calls the Rust workspace-trust planner through typed `daemonCoreWorkspaceTrustApi` and admits only Rust-authored warning events, and daemon-store `updateThreadRuntimeControls()` plus `appendThreadRuntimeControlEvent()` compatibility delegates are retired; thread runtime-control model records now emit canonical `route_id`, `selected_model`, `endpoint_id`, `provider_id`, `receipt_id`, `reasoning_effort`, `max_cost_usd`, `workflow_graph_id`, `workflow_node_id`, and `updated_at` fields without retired camelCase projection aliases, and normalized model-control readers ignore those retired aliases as persisted truth; Rust thread-control policy ownership, model-route alias rejection, receipt-ref binding, validation, and focused tests now live in `policy/thread_lifecycle.rs`; the JS context-policy core reaches thread-control state planning through typed `daemonCoreThreadLifecycleApi.planThreadControlAgentStateUpdate` without a generic command envelope, and Rust rejects the old thread-control state-update command operation; the Rust thread-control state-update request boundary now accepts only canonical `model_route` input, so retired `modelRoute` request aliases no longer satisfy Rust planning | Rust daemon-core thread-control admission/projection object backed by wallet authority, model-route authority, Agentgres expected-head/state-root binding, receipt/event materialization, workspace-trust policy integration, replay, and projection | `packages/runtime-daemon/src/runtime-thread-control-surface.mjs`, `packages/runtime-daemon/src/runtime-context-policy-core.mjs`, `packages/runtime-daemon/src/threads/thread-runtime-controls.mjs`, `packages/runtime-daemon/src/threads/workspace-trust-state.mjs`, `crates/services/src/agentic/runtime/kernel/mod.rs`, `crates/services/src/agentic/runtime/kernel/policy.rs`, `crates/services/src/agentic/runtime/kernel/policy/thread_lifecycle.rs`, `crates/services/src/agentic/runtime/kernel/policy/workspace_trust.rs`, `crates/services/src/agentic/runtime/kernel/agentgres_admission.rs`, `crates/services/src/agentic/runtime/kernel/command_protocol.rs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:compositor` |
| `RuntimeWorkspaceTrustControl` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md), [`private-workspace-ctee.md`](../components/daemon-runtime/private-workspace-ctee.md) | workspace-trust warning and acknowledgement controls now call Rust `plan_workspace_trust_control_state_update` through typed `daemonCoreWorkspaceTrustApi.planWorkspaceTrustControlStateUpdate`; the JS policy core sends canonical request bodies without generic command `operation`/`backend`, and Rust `command_protocol.rs`/`command_dispatch.rs` reject the old workspace-trust command operation. Thread mode updates require the Rust workspace-trust planner before lookup/write, commit only the Rust thread-control agent projection, and admit Rust-authored warning events through the Rust runtime-event Agentgres path, while acknowledgement routes replay admitted warning truth through Rust replay, require Rust-authored acknowledgement events with receipts, and admit only those events. The public acknowledgement route still calls `RuntimeThreadControl` directly, the daemon-store pass-through wrappers remain deleted, and the old JS repository-context warning/ack payload builders remain retired. | Rust daemon-core workspace-trust admission/projection object backed by deeper wallet/cTEE workspace authority, direct Agentgres expected-head/state-root binding, receipt/event materialization, replay, and stable projection APIs beyond migration transport | `packages/runtime-daemon/src/threads/workspace-trust-state.mjs`, `packages/runtime-daemon/src/runtime-thread-control-surface.mjs`, `packages/runtime-daemon/src/runtime-route-handlers.mjs`, `packages/runtime-daemon/src/runtime-context-policy-core.mjs`, `crates/services/src/agentic/runtime/kernel/mod.rs`, `crates/services/src/agentic/runtime/kernel/policy/workspace_trust.rs`, `crates/services/src/agentic/runtime/kernel/runtime_thread_event.rs`, `crates/services/src/agentic/runtime/kernel/command_protocol.rs`, `crates/services/src/agentic/runtime/kernel/agentgres_admission.rs` | `hypervisor-conformance:compositor`, `hypervisor-conformance:negative` |
| `RuntimeWorkspaceChangeControl` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md) | workspace-change inspection now calls Rust daemon-core `project_runtime_workspace_change_projection` with runtime `state_dir`; Rust replays admitted `events/*.jsonl`, derives review cards, and rejects JS-supplied projection/change candidates. Workspace-change accept/reject/rollback control now calls Rust daemon-core `plan_runtime_workspace_change_control` with runtime `state_dir`; Rust replays the selected current change before transition validation, rejects JS-supplied control candidate transport, and the route admits only the Rust-authored `workspace_change.controlled` runtime event. JS supplies request facts, validates Rust event identity, ignores retired control aliases, and no longer performs JS agent lookup, runtime-bridge inspect/control dispatch, request-hash synthesis, receipt-ref synthesis, hunk-preview/result-envelope construction, fixture fallback projection, JS current-change lookup, or bridge result normalization; thread workspace-change review and managed-session/fork/cancel routes still enter through the mounted auxiliary surface instead of daemon-store pass-through wrappers | Rust daemon-core workspace-change control/projection object backed by wallet/workspace authority, Agentgres expected-head/state-root binding, receipt/event materialization, rollback authority, replay, and projection | `packages/runtime-daemon/src/threads/workspace-change-state.mjs`, `packages/runtime-daemon/src/runtime-context-policy-core.mjs`, `packages/runtime-daemon/src/runtime-thread-auxiliary-surface.mjs`, `packages/runtime-daemon/src/runtime-route-handlers.mjs`, `crates/services/src/agentic/runtime/kernel/runtime_workspace_change_control.rs`, `crates/services/src/agentic/runtime/kernel/command_protocol.rs`, `crates/services/src/agentic/runtime/kernel/command_dispatch.rs`, `crates/services/src/agentic/runtime/kernel/agentgres_admission.rs` | `hypervisor-conformance:compositor`, `hypervisor-conformance:negative` |
| `RuntimeManagedSessionControl` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md), [`private-workspace-ctee.md`](../components/daemon-runtime/private-workspace-ctee.md) | managed-session inspection now calls Rust daemon-core `project_runtime_managed_session_projection` with runtime `state_dir`; Rust replays admitted `events/*.jsonl`, derives session cards, and rejects JS-supplied projection/session candidates. Managed-session control now calls Rust daemon-core `plan_runtime_managed_session_control` with runtime `state_dir`; Rust replays the selected current session before planning, rejects JS-supplied control candidate transport, and the route admits only the Rust-authored `managed_session.controlled` runtime event through the mounted runtime-event/Agentgres admission path. JS supplies request facts, validates the returned Rust event, ignores retired control aliases, and no longer performs JS agent lookup, runtime-bridge inspect/control dispatch, request-hash synthesis, action normalization, fixture fallback projection, JS current-session lookup, or bridge result envelope construction | Rust daemon-core managed-session control/projection object backed by wallet/cTEE session authority, Agentgres expected-head/state-root binding, receipt/event materialization, replay, and projection | `packages/runtime-daemon/src/threads/managed-session-state.mjs`, `packages/runtime-daemon/src/runtime-context-policy-core.mjs`, `packages/runtime-daemon/src/runtime-thread-auxiliary-surface.mjs`, `packages/runtime-daemon/src/runtime-route-handlers.mjs`, `crates/services/src/agentic/runtime/kernel/runtime_managed_session_control.rs`, `crates/services/src/agentic/runtime/kernel/command_protocol.rs`, `crates/services/src/agentic/runtime/kernel/command_dispatch.rs`, `crates/services/src/agentic/runtime/kernel/agentgres_admission.rs` | `hypervisor-conformance:compositor`, `hypervisor-conformance:negative` |
| `RuntimeBridgeThreadTurnControl` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md) | runtime-service thread start now requires Rust `plan_runtime_bridge_thread_start_agent_state_update`, commits only the Rust-planned bridge agent through Agentgres `writeAgent`, and returns the Rust thread projection; runtime-service thread control now requires Rust `plan_runtime_bridge_thread_control_agent_state_update`, validates the Rust-planned bridge-control agent/control envelope, commits only the Rust-planned `thread.runtime_bridge.control` agent through Agentgres `writeAgent`, returns the Rust thread projection, and never dispatches the deleted JS bridge `controlThread` path; runtime-service turn-submit now requires Rust `plan_runtime_bridge_turn_run_state_update`, validates the candidate run against the Rust turn projection, commits only the Rust-planned `turn.runtime_bridge.submit` run through Agentgres `writeRun`, returns the Rust turn projection, and never dispatches the deleted JS bridge `submitTurn` path; these state-update planners now call typed `daemonCoreThreadLifecycleApi` methods without generic command `operation`/`backend` envelopes, and Rust `command_protocol.rs` rejects the old runtime-bridge state-update command operations; the standalone `runtime-bridge-thread.mjs` fail-closed compatibility module and its focused test are deleted, and daemon-store `createRuntimeBridgeThread()` / `createRuntimeBridgeTurn()` pass-through wrappers are gone; runtime-service thread start, control, and turn-submit now have positive Rust-planned paths through `RuntimeAgentRunLifecycle`, while `RuntimeThreadTurn` delegates runtime resume to that mounted lifecycle boundary; public thread resume, turn create, interrupt, and steer routes call the mounted `RuntimeThreadTurn` surface directly instead of daemon-store route pass-through wrappers, and daemon-store `resumeThread()`, `createTurn()`, `interruptTurn()`, and `steerTurn()` compatibility delegates are retired; public non-runtime resume now enters the mounted Rust-planned agent status-control path and returns the Rust thread projection, public non-runtime turn creation now enters the mounted Rust-planned run-create path and returns the Rust turn projection, and diagnostics-blocked non-runtime turn creation now injects Rust-projected diagnostics feedback into the canonical turn request, enters the same Rust run-create path, commits only the Rust-planned blocked run through Agentgres-backed `writeRun`, and returns the Rust turn projection while Rust rejects the retired diagnostics-block admission-required operation; public operator interrupt/steer required-boundary envelopes are authored by Rust `OperatorTurnControlAdmissionRequiredCore` through `plan_operator_turn_control_admission_required`; direct `store.updateAgent()`, `store.createRun()`, JS turn projection composition, event append, runtime bridge dispatch, and daemon-store pass-through wrappers stay retired from the thread-turn surface; the old `runtime-thread-control.test.mjs` live JS runtime-service proof has been converted into evidence that route seeding uses Rust route-control planning plus Agentgres model_mount record-state commit, runtime-service thread creation uses Rust bridge-start planning before JS bridge start dispatch, and store-level lifecycle/thread-control/subagent wrappers stay absent; the obsolete Stage 5 stop/cancel/recover and Stage 7 delegation live-GUI proof scripts are deleted so stale JS model-mount/runtime-service bridge success proofs cannot be reused as current evidence; Rust runtime-bridge thread-start/control/turn policy ownership, validation, and focused tests now live in `policy/thread_lifecycle.rs` with direct positive kernel methods, the remaining JS bridge result and live-event normalizers plus daemon index pass-throughs are retired rather than preserved as migration projection plumbing, and conformance now guards the dedicated Rust-owned thread-control boundary instead of the old fail-closed control proof | Rust daemon-core runtime bridge thread/turn/control admission, execution dispatch, persistence, projection, replay, wallet/cTEE policy, and Agentgres expected-head/state-root binding object | `packages/runtime-daemon/src/runtime-agent-run-lifecycle.mjs`, `packages/runtime-daemon/src/runtime-thread-turn-surface.mjs`, `packages/runtime-daemon/src/runtime-thread-control.test.mjs`, `packages/runtime-daemon/src/runtime-context-policy-core.mjs`, `crates/services/src/agentic/runtime/kernel/mod.rs`, `crates/services/src/agentic/runtime/kernel/policy.rs`, `crates/services/src/agentic/runtime/kernel/policy/thread_lifecycle.rs`, `crates/services/src/agentic/runtime/kernel/command_protocol.rs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:compositor` |
| `RuntimeServiceLifecycleAgentAliasRetirement` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md), [`hypervisor-kernel-substrate-migration-matrix.md`](./hypervisor-kernel-substrate-migration-matrix.md) | runtime-service thread start/control Rust planners scrub `runtimeProfile`, `runtimeSessionId`, `runtimeBridgeId`, `runtimeBridgeStatus`, `runtimeBridgeSource`, and `fixtureProfile` from returned agent projections, emit only canonical snake_case runtime-service fields, and Rust lifecycle/thread-event/workspace-trust readers no longer accept those aliases as replay truth; JS runtime-service lifecycle normalizers reject any aliased Rust response, while daemon identity/session helpers read only `runtime_profile`, `runtime_session_id`, and `fixture_profile` | compatibility-truth retirement only; durable lifecycle deletion/cancellation replay/projection, wallet/cTEE runtime-service authority, and stable protocol APIs remain non-terminal | `crates/services/src/agentic/runtime/kernel/policy/thread_lifecycle.rs`, `crates/services/src/agentic/runtime/kernel/runtime_lifecycle.rs`, `crates/services/src/agentic/runtime/kernel/runtime_thread_event.rs`, `crates/services/src/agentic/runtime/kernel/policy/workspace_trust.rs`, `packages/runtime-daemon/src/runtime-context-policy-core.mjs`, `packages/runtime-daemon/src/runtime-identifiers.mjs`, `scripts/conformance/hypervisor-conformance.mjs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:docs` |
| `RuntimeThreadForkControl` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md) | thread fork now calls Rust daemon-core `plan_runtime_thread_fork_control`, requires a Rust-authored forked agent, forked thread projection, and `thread.forked` runtime event, commits only the Rust-authored forked agent through Agentgres-backed `writeAgent`, projects the forked thread through the Rust thread/turn projection boundary, and admits only the Rust-authored fork event through Rust runtime-event admission; the JS state module now remains a protocol commit client that supplies canonical source thread/agent facts and request fields, ignores retired camelCase request aliases, fails closed before source lookup when the Rust planner or Agentgres/event/projection commit surfaces are absent, and no longer calls `createAgent`, runtime-event idempotency lookup, JS event synthesis, or JS fork receipt/policy evidence paths | Rust daemon-core thread-fork control/projection object backed by StepModuleRouter dispatch where applicable, wallet authority for fork control, Agentgres expected-head/state-root binding, receipt/event materialization, replay, and projection | `packages/runtime-daemon/src/threads/thread-fork-state.mjs`, `packages/runtime-daemon/src/runtime-thread-auxiliary-surface.mjs`, `packages/runtime-daemon/src/runtime-route-handlers.mjs`, `packages/runtime-daemon/src/runtime-context-policy-core.mjs`, `crates/services/src/agentic/runtime/kernel/runtime_thread_fork_control.rs`, `crates/services/src/agentic/runtime/kernel/command_protocol.rs`, `crates/services/src/agentic/runtime/kernel/command_dispatch.rs`, `crates/services/src/agentic/runtime/kernel/agentgres_admission.rs` | `hypervisor-conformance:compositor`, `hypervisor-conformance:bridge` |
| `RuntimeSubagentControl` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md) | direct subagent control-event append now calls Rust daemon-core `plan_runtime_subagent_control` and admits only the Rust-authored event through Rust runtime-event Agentgres admission, without JS runtime-event authorship, JS planner fallback, `writeSubagent`, or duplicate receipt/policy evidence synthesis; public spawn, wait, input, resume, assign, cancel, and cancellation propagation lifecycle controls also call Rust daemon-core `plan_runtime_subagent_control` for their control events, admit Rust-authored events through Rust runtime-event Agentgres admission, call Rust subagent record state-update planning, and persist only Rust-planned projections through Agentgres-backed `writeSubagent`; public spawn composes with Rust-owned child-agent creation and child-run creation through the mounted agent/run lifecycle surface and does not restore JS `store.createAgent()`/`store.createRun()` truth; public input/resume additionally compose with the Rust run-create state-update path through the mounted agent/run lifecycle surface and do not restore JS `store.createRun()` truth, while public cancel and cancellation propagation compose with the Rust run-cancel state-update path and do not restore JS `store.cancelRun()` truth; public list/get/result projections now call Rust daemon-core `project_runtime_subagent_projection` through the mounted subagent surface, so JS supplies only temporary subagent/run candidates and fails closed when the Rust projector is missing or returns a mismatched projection kind before JS subagent/run map readback can author public truth; public subagent routes call `RuntimeSubagentControl` directly instead of daemon-store pass-through wrappers, and the old JS mutation bodies, stale readback bodies, spawn/wait/input/resume/assign/cancel/propagation/direct-append fail-closed bodies, and stale success-path tests have been deleted; Rust subagent record policy ownership, parent-thread mismatch rejection, validation, and focused tests now live in `policy/thread_lifecycle.rs`, while the read projection lives in `runtime_subagent_projection.rs`, spawn/wait/input/resume/assign/cancel/propagated-cancel control planning lives in `runtime_subagent_control.rs`, and both are dispatched through `command_protocol.rs`/`command_dispatch.rs` | Rust daemon-core subagent admission/projection object backed by StepModuleRouter dispatch, wallet authority for delegation/cancellation, Agentgres expected-head/state-root binding, receipt/event materialization, and replay | `packages/runtime-daemon/src/runtime-subagent-surface.mjs`, `packages/runtime-daemon/src/runtime-context-policy-core.mjs`, `packages/runtime-daemon/src/runtime-agent-run-lifecycle.mjs`, `packages/runtime-daemon/src/runtime-run-cancellation.mjs`, `crates/services/src/agentic/runtime/kernel/runtime_subagent_projection.rs`, `crates/services/src/agentic/runtime/kernel/runtime_subagent_control.rs`, `crates/services/src/agentic/runtime/kernel/command_protocol.rs`, `crates/services/src/agentic/runtime/kernel/command_dispatch.rs`, `crates/services/src/agentic/runtime/kernel/policy.rs`, `crates/services/src/agentic/runtime/kernel/policy/thread_lifecycle.rs` | `hypervisor-conformance:compositor`, `hypervisor-conformance:bridge` |
| `RuntimeTaskJobControl` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md) | public task create now calls Rust `plan_runtime_task_job_create_state_update`, requires canonical `agent_id`, gathers only existing-agent/model-route/memory/run-candidate facts after the Rust planner boundary exists, requires Rust-authored `task.create` task/job/checklist plus run projections, and persists only the returned run through Agentgres-backed `writeRun`; public task/job cancel now calls Rust `plan_runtime_task_job_cancel_state_update`, derives only canonical `task_`/`job_` public-id run ids in JS, requires Rust-authored `task.cancel`/`job.cancel` projections with canceled task/job/checklist and run records, receipts, artifacts, and terminal events, and persists only the Rust-authored run through Agentgres-backed `writeRun`; public task/job list/get now calls Rust `project_runtime_task_job_projection`, JS supplies only runtime `state_dir` plus canonical filter/public-id facts, and Rust replays admitted `runs/*.json` Agentgres state before task/job record construction, filtering, and public-id selection; the task/job surface no longer receives JS task/job record builders or `runs` candidate transport, retired `agentId` aliases stay ignored, missing/mismatched Rust projections fail closed, and direct `createAgent`, `createRun`, JS projection synthesis, stale readback bodies, and cancel-through-`cancelRun` compatibility paths are deleted | Rust daemon-core task/job admission/projection object backed by StepModuleRouter dispatch, wallet authority for lifecycle control, Agentgres expected-head/state-root binding, receipt/event materialization, and replay | `packages/runtime-daemon/src/runtime-task-job-surface.mjs`, `packages/runtime-daemon/src/runtime-task-job-surface.test.mjs`, `packages/runtime-daemon/src/runtime-context-policy-core.mjs`, `packages/runtime-daemon/src/runtime-context-policy-core.test.mjs`, `crates/services/src/agentic/runtime/kernel/policy/task_job.rs`, `crates/services/src/agentic/runtime/kernel/policy/run_cancel.rs`, `crates/services/src/agentic/runtime/kernel/command_protocol.rs`, `crates/services/src/agentic/runtime/kernel/command_dispatch.rs` | `hypervisor-conformance:compositor`, `hypervisor-conformance:bridge` |
| `RuntimeRunCancelControl` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md) | public run cancellation now calls Rust `plan_run_cancel_state_update`, requires the Rust-authored `run.cancel` projection with canceled status, terminal job/run events, runtime task/job/checklist records, receipts, and artifacts, and persists only that projection through Agentgres-backed `writeRun`; missing state planner still fails closed through `RunCancelAdmissionRequiredCore`, missing/mismatched Rust projections fail before persistence, and JS run-map mutation plus runtime task/job/checklist/event/receipt/artifact materialization stay retired; run cancel routes call the mounted auxiliary surface directly instead of daemon-store pass-through wrappers; Rust run-cancel policy ownership, helper planning, validation, and focused proof tests live in `policy/run_cancel.rs` behind the policy facade, while `RunCancelCommandError`, `RunCancel*BridgeRequest`, `plan_run_cancel_*_response`, and `rust_run_cancel_*_command` source markers are deleted; the former temporary `ioi_step_module_bridge/runtime_control_command.rs` delegate is deleted; direct Rust daemon-core lifecycle APIs remain the terminal target beyond migration command transport | Rust daemon-core run-cancel admission/persistence/projection object backed by Agentgres expected-head/state-root binding | `packages/runtime-daemon/src/runtime-run-cancellation.mjs`, `packages/runtime-daemon/src/runtime-run-cancellation.test.mjs`, `packages/runtime-daemon/src/runtime-thread-auxiliary-surface.mjs`, `packages/runtime-daemon/src/runtime-context-policy-core.mjs`, `crates/services/src/agentic/runtime/kernel/policy.rs`, `crates/services/src/agentic/runtime/kernel/policy/run_cancel.rs`, `crates/node/src/bin/ioi_step_module_bridge/mod.rs` | `hypervisor-conformance:bridge` |
| `RuntimeSkillHookRegistryControl` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md) | public skill/hook registry catalog, `/v1/skills`, and `/v1/hooks` projections now call typed `daemonCoreRuntimeProjectionApi.projectSkillHookRegistry`, backed by Rust `RuntimeKernelService::project_skill_hook_registry`, before public registry truth can return; Rust owns source discovery, skill/hook record construction, redacted hook command metadata, active set hashes, validation counts, and route-specific skill/hook projection records while JS forwards only workspace/home request facts and validates the returned registry kind; public `/v1/skills` and `/v1/hooks` routes call the mounted skill-hook surface directly instead of daemon-store `listSkills()` / `listHooks()` compatibility wrappers; the doctor report calls the mounted skill-hook surface directly and consumes the Rust-authored catalog instead of rediscovering registry truth or re-entering the daemon-store `skillHookCatalog()` wrapper; the dead store-level `skillHookCatalog()` / `listSkills()` / `listHooks()` delegates remain deleted; the old `SkillHookRegistryProjectionRequiredCore` path, JS generic `project_skill_hook_registry` operation envelope, Rust command operation/dispatch arm, and command-response wrapper are retired; `skill-hook-catalog.mjs` remains non-authoritative internal governance helper scaffolding until direct Rust governance/catalog persistence APIs own those shapes | Rust daemon-core skill/hook registry projection object backed by Agentgres-admitted governance/catalog truth, wallet authority where applicable, receipt/state-root binding, replay, and stable protocol APIs | `packages/runtime-daemon/src/runtime-skill-hook-surface.mjs`, `packages/runtime-daemon/src/runtime-context-policy-core.mjs`, `packages/runtime-daemon/src/runtime-doctor-report.mjs`, `packages/runtime-daemon/src/http/public-runtime-routes.mjs`, `crates/services/src/agentic/runtime/kernel/skill_hook_registry.rs`, `crates/services/src/agentic/runtime/kernel/mod.rs`, `crates/services/src/agentic/runtime/kernel/command_protocol.rs`, `crates/services/src/agentic/runtime/kernel/command_dispatch.rs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:compositor` |
| `RuntimeRepositoryWorkflowProjectionControl` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md) | public repository workflow routes now call typed `daemonCoreRuntimeProjectionApi.projectRepositoryWorkflow`, backed by Rust `RuntimeKernelService::project_repository_workflow`, before `/v1/repositories`, `/v1/repository-context`, `/v1/branch-policy`, `/v1/github-context`, `/v1/pr-attempts`, `/v1/issue-context`, `/v1/review-gate`, or `/v1/github/pr-create-plan` can return public truth; Rust owns read-only local Git discovery, sanitized GitHub remote metadata, branch-policy decisions, issue-context defaults, PR preview artifacts, review gate decisions, and dry-run GitHub PR create plans while JS forwards only workspace request facts and validates the returned projection kind; public repository workflow routes still call the mounted repository surface directly instead of daemon-store compatibility wrappers; the old `RepositoryWorkflowProjectionRequiredCore` path, JS generic `project_repository_workflow` operation envelope, Rust command operation/dispatch arm, and command-response wrapper are retired | Agentgres-backed repository workflow persistence/replay, wallet.network authority for external capability exits, receipt/state-root binding, and stable protocol APIs | `packages/runtime-daemon/src/runtime-repository-surface.mjs`, `packages/runtime-daemon/src/http/public-runtime-routes.mjs`, `packages/runtime-daemon/src/runtime-context-policy-core.mjs`, `crates/services/src/agentic/runtime/kernel/repository_workflow.rs`, `crates/services/src/agentic/runtime/kernel/mod.rs`, `crates/services/src/agentic/runtime/kernel/command_protocol.rs`, `crates/services/src/agentic/runtime/kernel/command_dispatch.rs` | `hypervisor-conformance:bridge` |
| `RuntimeToolCatalogProjectionControl` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md) | public runtime account, runtime-node, and tool catalog projections now call typed `daemonCoreRuntimeProjectionApi.projectRuntimeToolCatalog`, backed by Rust `RuntimeKernelService::project_runtime_tool_catalog`, before `/v1/account`, `/v1/runtime/nodes`, or `/v1/tools` can return public truth; JS forwards only canonical request/environment facts through the mounted tool surface, validates the returned projection kind, and returns Rust-authored account/node/tool catalog records with canonical snake_case tool entries; public runtime account/node/tool routes call the mounted tool surface directly instead of daemon-store compatibility wrappers; the doctor report calls mounted tool-surface `listTools()` and `listRuntimeNodes()` directly instead of daemon-store `listTools()` / `listRuntimeNodes()` wrappers; the dead store-level `getAccount()` / `listRuntimeNodes()` / `listTools()` delegates are deleted; the old `RuntimeToolCatalogProjectionRequiredCore` path, JS generic `project_runtime_tool_catalog` operation envelope, Rust command operation/dispatch arm, and command-response wrapper are retired; `runtime-tool-catalog.mjs` remains only non-authoritative internal descriptor/governance helper scaffolding until direct Rust projection/catalog APIs own those shapes | Rust daemon-core runtime account/node/tool catalog projection object backed by Agentgres-admitted runtime catalog truth, wallet/network authority where external capability exposure applies, receipt/state-root binding, replay, and stable IDE/CLI/SDK protocol APIs | `packages/runtime-daemon/src/runtime-tool-surface.mjs`, `packages/runtime-daemon/src/http/public-runtime-routes.mjs`, `packages/runtime-daemon/src/runtime-tool-catalog.mjs`, `packages/runtime-daemon/src/runtime-doctor-report.mjs`, `packages/runtime-daemon/src/runtime-context-policy-core.mjs`, `crates/services/src/agentic/runtime/kernel/runtime_tool_catalog.rs`, `crates/services/src/agentic/runtime/kernel/mod.rs`, `crates/services/src/agentic/runtime/kernel/command_protocol.rs`, `crates/services/src/agentic/runtime/kernel/command_dispatch.rs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:compositor` |
| `RuntimeDoctorReportProjectionControl` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md) | public `/v1/doctor` now calls the mounted `RuntimeDoctorReport` aggregate directly instead of a daemon-store `doctorReport()` route wrapper; the store wrapper method is deleted, route tests poison the retired wrapper, and doctor aggregate tests poison the retired store-level tool/runtime-node/skill-hook catalog wrappers while requiring mounted tool and skill-hook surface reads | Rust daemon-core diagnostic/readiness projection over the admitted runtime graph, Agentgres truth, wallet/cTEE authority where needed, receipt/state-root binding, replay, and stable IDE/CLI/SDK protocol APIs; JS may remain only as non-authoritative edge formatting until direct Rust diagnostic projection APIs own the surface | `packages/runtime-daemon/src/runtime-doctor-report.mjs`, `packages/runtime-daemon/src/runtime-doctor-report.test.mjs`, `packages/runtime-daemon/src/http/public-runtime-routes.mjs`, `packages/runtime-daemon/src/http/public-runtime-routes.test.mjs`, `packages/runtime-daemon/src/index.mjs`, `scripts/conformance/hypervisor-conformance.mjs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:docs` |
| `RuntimeCodingToolBudgetRecoveryControl` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md) | public coding-tool budget recovery `retry_approved` now requires Rust `plan_coding_tool_budget_recovery_state_update`, while `request_approval` and `approve_override` require Rust `plan_coding_tool_budget_recovery_control`; the public route validates the Rust-authored operator control and run projection, commits only that projection through Agentgres-backed `writeRun`, returns a canonical snake_case recovery envelope, and requires wallet.network grant plus authority receipt refs before override authority can bind a Rust authority hash into the run projection; the old admission-required budget-recovery command, stale blocked-event projection facade `latestCodingToolBudgetBlockedEventForRun()`, standalone JS `runtime-coding-tool-budget-recovery.mjs` policy/result helper plus its test, approval request/decision calls, JS runtime-event append, direct run-map mutation, and JS retry/control result synthesis remain absent | Rust daemon-core coding-tool budget recovery control/projection object backed by wallet authority, Agentgres expected-head/state-root binding, policy receipts, retry-event materialization, replay, projection, and direct stable protocol APIs | `packages/runtime-daemon/src/runtime-coding-tool-budget-recovery-surface.mjs`, `packages/runtime-daemon/src/runtime-context-policy-core.mjs`, `crates/services/src/agentic/runtime/kernel/policy.rs`, `crates/services/src/agentic/runtime/kernel/policy/coding_tool_budget_recovery.rs`, `crates/services/src/agentic/runtime/kernel/command_protocol.rs`, `crates/services/src/agentic/runtime/kernel/command_dispatch.rs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:compositor` |
| `RuntimeCodingToolGovernanceControl` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md), [`wallet-network/doctrine.md`](../components/wallet-network/doctrine.md) | public coding-tool budget-block control now calls Rust `plan_coding_tool_budget_block` through `runtime-context-policy-core`, forwards only canonical request facts, strips retired budget-policy aliases, and fails closed if the Rust planner is absent; the invocation hot path admits the Rust-authored blocked result event through `admit_coding_tool_result_event` before returning the policy error, so JS no longer appends budget-block runtime events or constructs budget-block response truth; the JS approval-block facade is deleted, approval-required invocation now asks Rust `plan_coding_tool_approval_satisfaction` for a satisfied record before execution or Rust `plan_coding_tool_approval_block` for the blocked coding-tool result/event envelope when unsatisfied, and blocked coding-tool result events are persisted only after Rust `admit_coding_tool_result_event` returns the Agentgres/state-root-bound event; the mounted governance surface no longer exports `codingToolApprovalSatisfaction()` or `blockCodingToolForApproval()`, no longer scans approval request events, decision event streams, or lease-state helpers to authorize coding-tool execution, and no longer receives those helpers from daemon composition | Rust daemon-core coding-tool governance admission object backed by wallet approval authority, Agentgres expected-head/state-root binding, policy receipts, budget receipts, approval/block event persistence, and projection | `packages/runtime-daemon/src/index.mjs`, `packages/runtime-daemon/src/runtime-coding-tool-governance-surface.mjs`, `packages/runtime-daemon/src/runtime-coding-tool-invocation-surface.mjs`, `packages/runtime-daemon/src/runtime-context-policy-core.mjs`, `packages/runtime-daemon/src/runtime-agentgres-admission-core.mjs`, `crates/services/src/agentic/runtime/kernel/approval.rs`, `crates/services/src/agentic/runtime/kernel/coding_tool_event.rs`, `crates/services/src/agentic/runtime/kernel/policy.rs`, `crates/services/src/agentic/runtime/kernel/policy/context_lifecycle.rs` | `hypervisor-conformance:bridge` |
| `RuntimeCodingToolResultEnvelopeControl` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md), [`hypervisor-kernel-substrate-migration-matrix.md`](./hypervisor-kernel-substrate-migration-matrix.md) | normal successful/failed coding-tool execution now calls Rust daemon-core `plan_coding_tool_result_envelope` through the context-policy runner before workload dispatch for the StepModule context and again after Rust workload observation for the result `payload_summary` plus runtime-event candidate; the invocation surface fails closed before runner execution if the Rust envelope planner is absent, forwards only canonical request/result facts, then admits only the Rust-planned result event through `admit_coding_tool_result_event`; JS-authored StepModule context, source-event kind selection, payload-summary construction, and candidate result-event truth are retired for the migrated normal path | Rust daemon-core coding-tool result envelope/admission object backed by StepModuleRouter admission, workload observations, Agentgres expected-head/state-root binding, receipt refs, payload refs, replay, and projection | `packages/runtime-daemon/src/runtime-coding-tool-invocation-surface.mjs`, `packages/runtime-daemon/src/runtime-context-policy-core.mjs`, `packages/runtime-daemon/src/index.mjs`, `crates/services/src/agentic/runtime/kernel/coding_tool_event.rs`, `crates/services/src/agentic/runtime/kernel/command_protocol.rs`, `crates/services/src/agentic/runtime/kernel/command_dispatch.rs`, `scripts/conformance/hypervisor-conformance.mjs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:receipts` |
| `RuntimeComputerUseInvocationControl` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md), [`wallet-network/doctrine.md`](../components/wallet-network/doctrine.md) | public native browser discovery/control, native browser action, visual GUI action, sandboxed-hosted action, and visual GUI observe daemon methods now route through the Rust-live `computer_use.request_lease` StepModule via the mounted coding-tool invocation surface; Rust `coding_tool_computer_use.rs` authors lease requests, provider registry selection, wallet.network authority boundaries, receipt refs, fail-closed provider availability, and canonical result fields, while the JS adapter only maps the public tool family to canonical lease input and never restores local browser discovery sync, controlled native-browser launch, CDP execution, visual GUI capture/execution, sandbox/session metadata helpers, projection construction, runtime-event append, or direct computer-use event admission; direct event append remains fail-closed until Rust event materialization owns it | concrete provider execution, direct Rust daemon-core computer-use event admission/materialization, cTEE custody for private workspaces, Agentgres expected-head/state-root binding across durable replay, projection, and stable IDE/CLI/SDK APIs | `packages/runtime-daemon/src/index.mjs`, `packages/runtime-daemon/src/runtime-computer-use-invocation-store.test.mjs`, `packages/runtime-daemon/src/runtime-coding-tool-invocation-surface.test.mjs`, `crates/services/src/agentic/runtime/kernel/coding_tool_computer_use.rs`, `crates/services/src/agentic/runtime/kernel/coding_tool_step_module.rs`, `scripts/conformance/hypervisor-conformance.mjs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:negative` |
| `RuntimeCodingToolArtifactMutationControl` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md) | coding-tool artifact draft materialization and computer-use visual observation artifact materialization still fail closed before JS artifact record construction, local visual artifact file reads, Agentgres artifact-state commit calls from JS, `codingArtifacts` map mutation, or `appendRuntimeEvent`; production-default coding-tool result-event admission is Rust-owned by `admit_coding_tool_result_event`, command-stream persistence is Rust-owned by `admit_coding_tool_command_stream_events`, and JS only registers Rust-returned projection events; migrated coding-tool StepModule invocation construction is Rust-owned by `RuntimeKernelService::run_coding_tool_step_module`, whose typed workload API request accepts canonical request facts and rejects command `operation`, command `backend`, and JS-supplied `StepModuleInvocation` envelopes; visual observation mutation metadata now considers only canonical `screenshot_path`, `som_path`, and `ax_path`, ignoring retired path aliases; visual GUI local fixture capture/execution no longer reads `IOI_RUNTIME_ENABLE_VISUAL_CAPTURE_FIXTURE` or `IOI_RUNTIME_ENABLE_VISUAL_EXECUTOR_FIXTURE`, so fixture providers can only be selected by canonical request fields and payloads rather than hidden daemon env selectors; coding-tool invocation calls the mounted artifact surface directly for artifact draft materialization, command-stream admission, artifact reads, result retrieval, and Rust result-event admission instead of daemon-store helper wrappers, and those wrappers are deleted; artifact read/retrieve data-plane normalization for migrated Rust-live tools now lives in Rust `coding_tool_artifact.rs`, accepts only canonical `rust_workload_data_plane`, strips retired result aliases before StepModule response shaping, and feeds Rust `coding_tool_step_module.rs` directly after the `coding_tool_command.rs` bridge delegate was deleted; temporary JS artifact read/retrieve adapters no longer synthesize local `receipt_artifact_read...` refs and only pass through admitted artifact `receipt_refs` supplied by Rust/Agentgres | Rust daemon-core coding-tool artifact, command-stream, result-event, and StepModule invocation object backed by Agentgres expected-head/state-root binding, ArtifactRef/PayloadRef admission, receipt binding, and projection | `packages/runtime-daemon/src/runtime-coding-tool-artifact-surface.mjs`, `packages/runtime-daemon/src/runtime-coding-tool-invocation-surface.mjs`, `packages/runtime-daemon/src/runtime-agentgres-admission-core.mjs`, `packages/runtime-daemon/src/runtime-coding-tool-results.mjs`, `packages/runtime-daemon/src/step-module-runner.mjs`, `packages/runtime-daemon/src/visual-gui-local-capture.mjs`, `packages/runtime-daemon/src/visual-gui-local-executor.mjs`, `crates/services/src/agentic/runtime/kernel/coding_tool_artifact.rs`, `crates/services/src/agentic/runtime/kernel/coding_tool_event.rs`, `crates/services/src/agentic/runtime/kernel/coding_tool_step_module.rs`, `crates/node/src/bin/ioi_step_module_bridge/mod.rs`, `scripts/conformance/hypervisor-conformance.mjs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:negative` |
| `RuntimeConversationArtifactControl` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md) | public conversation artifact create/action/export/promote now call Rust daemon-core `plan_runtime_conversation_artifact_control`, validate the Rust-authored artifact/control plan, and commit only that artifact through Rust Agentgres artifact-state admission before returning route truth; public and thread-scoped conversation-artifact list/get/revision route projections call Rust daemon-core `project_runtime_conversation_artifact_projection` through the mounted conversation-artifact surface with runtime `state_dir`, so Rust replays admitted `artifacts/*.json` records, filters active canonical `ioi.conversation_artifact` records, and rejects retired JS artifact candidate transport before route truth can return; the mounted surface sends only canonical request fields, supplies temporary artifact candidates only for control planning, and never calls direct `ConversationArtifactStore` create/action/export/promote writers, which remain retired before JS artifact class selection, revision/file materialization, action policy checks, export/promote projection, JS receipt construction, record writes, or file writes; public and thread-scoped routes call the mounted conversation-artifact surface directly instead of daemon-store pass-through wrappers, and those store wrappers remain deleted | Rust daemon-core conversation-artifact admission/projection object backed by Agentgres expected-head/state-root binding, receipt_binder, ArtifactRef/PayloadRef admission, replay, and projection | `packages/runtime-daemon/src/runtime-conversation-artifact-surface.mjs`, `packages/runtime-daemon/src/runtime-context-policy-core.mjs`, `packages/runtime-daemon/src/conversation-artifacts.mjs`, `packages/runtime-daemon/src/runtime-route-handlers.mjs`, `packages/runtime-daemon/src/http/public-runtime-routes.mjs`, `packages/runtime-daemon/src/index.mjs`, `crates/services/src/agentic/runtime/kernel/runtime_conversation_artifact_control.rs`, `crates/services/src/agentic/runtime/kernel/runtime_conversation_artifact_projection.rs`, `crates/services/src/agentic/runtime/kernel/command_protocol.rs`, `crates/services/src/agentic/runtime/kernel/command_dispatch.rs`, `crates/services/src/agentic/runtime/kernel/agentgres_admission.rs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:compositor`, `hypervisor-conformance:receipts` |
| `ModelConversationStateControl` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md) | model-mount conversation-state writes now call typed `daemonCoreModelMountApi.planModelMountConversationState`, backed by Rust `RuntimeKernelService::plan_model_mount_conversation_state`, commit only Rust-authored `model-conversations` records through Rust Agentgres model_mount record-state admission, and update the local continuation cache only after commit; native stream-completion finalization now calls typed `daemonCoreModelMountApi.planModelMountStreamCompletion`, backed by Rust `RuntimeKernelService::plan_model_mount_stream_completion`, commits only the Rust-authored conversation record, and persists only the Rust-authored `model_invocation_stream_completed` receipt carrying receipt_binder, accepted-receipt append, StepModule result, Agentgres operation/state-root/resulting-head bindings, stream-completion hash, and conversation-state hash; stream cancellation now calls typed `daemonCoreModelMountApi.planModelMountStreamCancel`, backed by Rust `RuntimeKernelService::plan_model_mount_stream_cancel`, and the old conversation/stream command operations, dispatch arms, bridge wrappers, backend markers, and JS command-envelope builders are retired; public conversation list projection now calls Rust read-projection kind `model_conversation_states` with the runtime `state_dir`, and Rust replays persisted `model-conversations/*.json` records before filtering to Rust-authored conversation records with conversation hashes, Rust conversation/stream evidence, and Agentgres conversation-truth evidence; JS `model-conversations` record synthesis, JS stream-completion receipt synthesis, JS receipt-binding construction, direct `state.receipt("model_invocation_stream_completed")`, `writeMap("model-conversations")`, public-list `conversation_states` request input, and the old fail-closed JS list facade remain retired. | Rust daemon-core model conversation/stream-completion admission and projection object backed by model_mount, receipt_binder, Agentgres expected-head/state-root binding, direct Agentgres conversation replay for public list projection, and Rust-owned projection; JS may only read specific already admitted conversation records for continuation preflight and no longer passes public-list conversation record candidates into Rust projection | `packages/runtime-daemon/src/model-mounting.mjs`, `packages/runtime-daemon/src/model-mounting/read-projection-facade.mjs`, `packages/runtime-daemon/src/model-mounting/model-mount-core.mjs`, `packages/runtime-daemon/src/model-mounting/conversation-operations.test.mjs`, `packages/runtime-daemon/src/model-mounting/read-projection-facade.test.mjs`, `scripts/conformance/hypervisor-conformance.mjs`, `crates/services/src/agentic/runtime/kernel/model_mount.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/conversation.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/read_projection.rs`, `crates/services/src/agentic/runtime/kernel/model_mount/read_projection/conversation.rs`, `crates/services/src/agentic/runtime/kernel/command_protocol.rs`, `crates/services/src/agentic/runtime/kernel/command_dispatch.rs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:receipts` |
| `RuntimeWorkspaceSnapshotRestoreControl` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md) | public snapshot artifact materialization and snapshot event append now require Rust `workspace_restore.rs` snapshot capture output: the surface commits only Rust-authored snapshot artifacts through Rust Agentgres artifact-state admission and admits only Rust-authored snapshot events through Rust Agentgres runtime-event admission before `file.apply_patch` can return snapshot truth; missing or malformed Rust snapshot record/artifact/event records fail closed instead of restoring JS materializers, direct artifact records, runtime-event stream reads, or `appendRuntimeEvent`; public restore preview/apply, snapshot list, and snapshot content-package now call Rust `workspace_restore.rs` through the mounted `workspaceRestoreCore`; restore preview/apply responses now include Rust-authored restore artifact and runtime-event records, and the JS facade can only commit/admit those records through Rust Agentgres artifact-state and runtime-event admission before returning public restore truth; missing or malformed Rust restore artifact/event records fail closed instead of restoring JS materializers; Rust-live `file.apply_patch` calls the mounted workspace-snapshot surface for patch snapshot capture, consumes Rust `workspace_restore.rs` `snapshot_record`/`snapshot_artifact`/`snapshot_event` output through the mounted `workspaceRestoreCore`, and no longer calls the retired JS snapshot-event appender or completes successfully when Rust snapshot capture admission is incomplete; snapshot/restore/content-package store wrappers are deleted; workspace restore apply-policy, preview/apply operations, restore artifact/event record shaping, snapshot artifact/event record shaping, and snapshot capture protocol request/response shaping now live in Rust `workspace_restore.rs`; the former temporary `ioi_step_module_bridge/workspace_restore_command.rs` delegate, Rust workspace restore command operations, generic JS command invoker path, and JS workspace restore runner are deleted or retired | Rust daemon-core workspace snapshot/restore admission and projection object backed by receipt binding, Agentgres expected-head/state-root binding, ArtifactRef/PayloadRef admission, restore policy/approval, and replay | `packages/runtime-daemon/src/runtime-workspace-snapshot-surface.mjs`, `packages/runtime-daemon/src/runtime-coding-tool-invocation-surface.mjs`, `packages/runtime-daemon/src/runtime-workspace-restore-core.mjs`, `crates/services/src/agentic/runtime/kernel/workspace_restore.rs`, `scripts/conformance/hypervisor-conformance.mjs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:receipts`, `hypervisor-conformance:compositor` |
| `RuntimeOperatorTurnControl` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md) | public operator interrupt/steer now call typed `daemonCoreRuntimeControlApi.planOperatorInterruptStateUpdate` / `planOperatorSteerStateUpdate`, validate the Rust-planned operator-control envelope and run projection, resolve the current run through `resolveRunForThreadTurn`, persist only the Rust-planned run through Agentgres-backed `writeRun`, and return the Rust operator-control envelope; missing Rust state-update planning fails closed through typed `planOperatorTurnControlAdmissionRequired`; the old JS runtime-bridge control, JS runtime-event append, direct run-map mutation, generic command transport, command/env fallback, and no-planner success paths remain retired; Rust operator-control policy ownership, helper planning, validation, focused proof tests, and direct daemon-core methods live in `policy/operator_control.rs`/`kernel/mod.rs`; `OperatorControlCommandError`, operator-control bridge request types, `plan_operator_*_response`, `plan_diagnostics_operator_override_state_update_response`, `_command` source markers, and bridge-shaped owner proof tests are deleted, and `command_protocol.rs` rejects the old operator-control command operations | Rust daemon-core operator-control admission/projection object backed by wallet authority, runtime control custody, Agentgres expected-head/state-root binding, receipt/event materialization, replay/projection storage, and stable protocol APIs | `packages/runtime-daemon/src/runtime-thread-turn-surface.mjs`, `packages/runtime-daemon/src/runtime-context-policy-core.mjs`, `packages/runtime-daemon/src/runtime-operator-turn-control-facade.test.mjs`, `crates/services/src/agentic/runtime/kernel/mod.rs`, `crates/services/src/agentic/runtime/kernel/policy.rs`, `crates/services/src/agentic/runtime/kernel/policy/operator_control.rs`, `crates/services/src/agentic/runtime/kernel/command_protocol.rs` | `hypervisor-conformance:bridge` |
| `RuntimeApprovalControl` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md), [`wallet-network/doctrine.md`](../components/wallet-network/doctrine.md) | public approval request/decision/revoke now fail closed before JS runtime-event append, run/agent map mutation, or `writeRun`/`writeAgent` persistence; public request issuance calls Rust `authorize_approval_request` before state planning and the Rust request planner rejects missing request-authority record/hash/authority-receipt binding; public state-update planning sends runtime `state_dir` to Rust, Rust replays the target run/agent from admitted Agentgres projections, rejects retired JS `run`/`agent` candidate transport, and the public approval surface no longer performs JS target lookup through `agentForThread`, `getRun`, `listRuns`, or `runs.get`; public approval routes call the mounted approval surface directly instead of daemon-store request/decision/revoke pass-through wrappers; the unused `latestApprovalDecisionEvent()` and `latestApprovalRequestEvent()` JS readback facades are deleted from the approval surface and daemon store so approval truth cannot re-enter as daemon-local event scanning; coding-tool approval satisfaction is retired as a JS event/lease read gate and replaced by Rust `project_coding_tool_approval_satisfaction` plus Rust `plan_coding_tool_approval_satisfaction`; the JS store satisfaction projection callback, projection-context run/agent candidate helper, and exported JS manifest matcher are retired; unsatisfied coding-tool approval block materialization is replaced by Rust `plan_coding_tool_approval_block`, and the resulting blocked event is persisted through Rust `admit_coding_tool_result_event` before JS projection registration; coding-tool approval, approval request authority, approval satisfaction projection, approval satisfaction, approval block, result-event admission, and approval request/decision/revoke command request/response shaping now lives in Rust `approval.rs` and `coding_tool_event.rs`; the former temporary `ioi_step_module_bridge/approval_command.rs` delegate is deleted instead of the broad bridge module owning those envelopes; the public approval surface now mounts `approvalStateCore`; the retired approval-state runner/env facade and JS response normalizer stay deleted, and Rust approval state planners return through a positive daemon-core API only | Rust daemon-core approval authority/admission/projection object backed by wallet.network grants, Agentgres expected-head/state-root binding, cTEE custody policy where relevant, and receipt/event materialization | `packages/runtime-daemon/src/runtime-approval-surface.mjs`, `packages/runtime-daemon/src/runtime-approval-state-core.mjs`, `packages/runtime-daemon/src/runtime-coding-tool-governance-surface.mjs`, `packages/runtime-daemon/src/runtime-coding-tool-invocation-surface.mjs`, `crates/services/src/agentic/runtime/kernel/approval.rs`, `crates/services/src/agentic/runtime/kernel/coding_tool_event.rs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:negative` |
| `RuntimeContextPolicyControl` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md) | public `compactThread` now calls Rust `planContextCompaction` through typed `daemonCoreContextLifecycleApi` for the `context.compacted` event plan, admits only that Rust-planned event through the mounted Rust Agentgres runtime-event path, calls Rust `planContextCompactionStateUpdate` through the same typed API with the admitted event id/seq, and persists only the Rust-planned run or agent projection through Agentgres-backed `writeRun`/`writeAgent`; missing Rust planning, missing admitted agent/run, incomplete event admission, malformed Rust state update, or absent typed context lifecycle API now fail closed before projection persistence; public thread/run context-budget evaluation now requires typed Rust `evaluateContextBudgetPolicy`, validates the Rust policy-event identity, and admits the Rust-planned `context_budget` runtime event through the mounted Rust Agentgres runtime-event path before returning route truth; public thread compaction-policy evaluation now requires typed Rust `evaluateCompactionPolicy`, validates and admits the Rust-planned `compaction_policy` runtime event, and composes Rust-approved execution through the Rust-owned `compactThread` path instead of a JS event/persistence facade; coding-tool budget policy and budget-block planning also use the typed context lifecycle API; workflow-only context-budget evaluation remains projection-only and does not append accepted thread truth; public workflow-only context-budget, thread context-budget, thread compaction-policy, thread compact, and run context-budget routes call the mounted context-policy surface directly instead of daemon-store pass-through wrappers; Rust context-budget, coding-tool budget/block, compaction-policy, context-compaction plan, and context-compaction state-update ownership, validation, helper planning, response envelopes, canonical source markers, bridge-facing error codes, and focused tests live in `policy/context_lifecycle.rs` behind the policy facade; command-response proof coverage remains in Rust owner tests, the former temporary `ioi_step_module_bridge/context_policy_command.rs` delegate is deleted, and Rust `command_protocol.rs`/`command_dispatch.rs` reject the retired context lifecycle command operations instead of carrying them as terminal transport | Rust daemon-core context-policy admission/projection object backed by Agentgres expected-head/state-root binding, policy receipts, context compaction event materialization, and run/agent persistence | `packages/runtime-daemon/src/runtime-context-policy-surface.mjs`, `packages/runtime-daemon/src/runtime-context-policy-core.mjs`, `crates/services/src/agentic/runtime/kernel/policy.rs`, `crates/services/src/agentic/runtime/kernel/policy/context_lifecycle.rs`, `crates/services/src/agentic/runtime/kernel/command_protocol.rs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:compositor` |
| `RuntimeStudioIntentFrameControl` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md) | public `/v1/studio/intent-frame` now calls the injected studio intent resolver dependency directly instead of a daemon-store `resolveStudioIntentFrame()` route wrapper; the store wrapper method is deleted, route tests poison the retired wrapper, and conformance keeps canonical `execution_mode` input handling tied to the route-wrapper retirement guard | Rust daemon-core intent/admission authority for consequential routing decisions, wallet.network authority where an intent exits local trust, Agentgres truth, receipt/state-root binding, replay, and stable IDE/CLI/SDK protocol APIs; JS may remain only as non-authoritative product/protocol framing glue | `packages/runtime-daemon/src/studio-intent-frame.mjs`, `packages/runtime-daemon/src/http/public-runtime-routes.mjs`, `packages/runtime-daemon/src/http/public-runtime-routes.test.mjs`, `packages/runtime-daemon/src/index.mjs`, `scripts/conformance/hypervisor-conformance.mjs` | `hypervisor-conformance:compositor` |
| `RuntimeDiagnosticsRepairControl` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md) | public diagnostics repair decision execution and direct decision-executed event append now call Rust daemon-core `plan_runtime_diagnostics_repair_control`, require Rust-authored diagnostics repair runtime events, and admit only those events through Rust Agentgres runtime-event admission; diagnostics operator override execution now calls Rust `plan_diagnostics_operator_override_state_update`, requires the Rust-planned operator-control run projection, and persists only through the Rust Agentgres run-state commit path; direct operator-override event append now calls Rust `plan_runtime_diagnostics_repair_control`, requires a Rust-authored `diagnostics.operator_override.event`, and admits only that event through Rust runtime-event admission; diagnostics repair retry-turn creation now requires Rust `plan_runtime_diagnostics_repair_retry_run` before lookup, validates the Rust-authored retry `run_request` and `retry_event_request`, requires Rust `project_runtime_diagnostics_repair_retry_result` before JS agent lookup/run creation, executes only that run request through the mounted Rust-owned run-create lifecycle surface, admits a Rust-authored `diagnostics.repair_retry.created` event through Rust runtime-event admission, validates the complete Rust-projected retry result after admission, rejects partial or mismatched retry result projections, and returns only the Rust-projected retry result envelope without local JS field synthesis; direct retry-event append still requires Rust diagnostics repair retry-event planning and runtime-event admission; the route-facing JS surface no longer resolves accepted repair truth, authors retry prompts/options/diagnostics feedback, appends JS runtime events/operator-override/retry events, projects retry/operator result envelopes, calls `store.createRun`, mutates run maps, or persists decision/override/retry truth when Rust planning/projection is missing; repair policy projection now calls Rust daemon-core `project_runtime_diagnostics_repair_policy`; repair decision resolution now calls Rust daemon-core `project_runtime_diagnostics_repair_projection`, requires Rust-authored decision projection records, validates projection kind/thread/decision/gate identity, and fails closed before JS accepted-truth resolution, JS state-planner execution, run-map mutation, or non-admitted persistence when Rust projection is missing or mismatched; mounted daemon diagnostics repair refusal remains available for the unmigrated helper through `DiagnosticsRepairAdmissionRequiredCore`; diagnostics-repair routes and helpers call the mounted diagnostics repair surface directly, and daemon-store operator-override, repair-retry, repair-decision resolution, and event-append pass-through wrappers remain deleted | Rust daemon-core diagnostics-repair admission/projection object backed by Agentgres expected-head/state-root binding, repair/operator authority receipts, retry-run request planning/admission, retry-result projection, state-root binding, and event materialization | `packages/runtime-daemon/src/runtime-diagnostics-repair-surface.mjs`, `packages/runtime-daemon/src/runtime-context-policy-core.mjs`, `packages/runtime-daemon/src/diagnostics-repair-execution.mjs`, `crates/services/src/agentic/runtime/kernel/runtime_diagnostics_repair_control.rs`, `crates/services/src/agentic/runtime/kernel/runtime_diagnostics_repair_projection.rs`, `crates/services/src/agentic/runtime/kernel/runtime_diagnostics_repair_policy.rs`, `crates/services/src/agentic/runtime/kernel/command_protocol.rs`, `crates/services/src/agentic/runtime/kernel/command_dispatch.rs`, `crates/services/src/agentic/runtime/kernel/policy.rs`, `crates/services/src/agentic/runtime/kernel/policy/admission_required.rs`, `crates/services/src/agentic/runtime/kernel/policy/operator_control.rs`, `crates/services/src/agentic/runtime/kernel/agentgres_admission.rs`, `crates/node/src/bin/ioi_step_module_bridge/mod.rs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:compositor`, `hypervisor-conformance:negative` |
| `RuntimeWorkflowEditControl` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md), [`wallet-network/doctrine.md`](../components/wallet-network/doctrine.md) | public workflow-edit proposal and apply controls now call Rust daemon-core `plan_runtime_workflow_edit_control`, require Rust-authored `workflow.edit_proposed` and `workflow.edit.apply` runtime events, and admit only those events through Rust Agentgres runtime-event admission; the route-facing JS surface no longer authors event envelopes, persists approval requests, constructs proposal/apply truth envelopes, writes workflow JSON, or replays legacy `workflow.edit_applied` as accepted truth when Rust planning is missing; apply validation now requires canonical `proposal_id` before the Rust boundary and the Rust planner also rejects direct apply requests without `proposal_id`; unused workflow-edit target/context helper facades remain deleted from both the daemon store and mounted workflow-edit surface; the former admission-required refusal owner remains as migration proof in `WorkflowEditAdmissionRequiredCore`, but route-facing workflow-edit proposal/apply control no longer calls it as the truth path | Rust daemon-core workflow-edit admission/projection object backed by wallet approval authority, Agentgres expected-head/state-root binding, proposal/apply receipts, workflow mutation custody, replay, and event materialization | `packages/runtime-daemon/src/runtime-workflow-edit-surface.mjs`, `packages/runtime-daemon/src/runtime-context-policy-core.mjs`, `crates/services/src/agentic/runtime/kernel/runtime_workflow_edit_control.rs`, `crates/services/src/agentic/runtime/kernel/command_protocol.rs`, `crates/services/src/agentic/runtime/kernel/command_dispatch.rs`, `crates/services/src/agentic/runtime/kernel/policy.rs`, `crates/services/src/agentic/runtime/kernel/policy/admission_required.rs`, `crates/services/src/agentic/runtime/kernel/approval.rs`, `crates/services/src/agentic/runtime/kernel/projection.rs` | `hypervisor-conformance:bridge` |
| `RuntimeContextLifecycleFallbackRetirement` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md), [`hypervisor-kernel-substrate-migration-matrix.md`](./hypervisor-kernel-substrate-migration-matrix.md) | context-budget policy, compaction-policy, context-compaction plan, and context-compaction state-update normalizers no longer synthesize public `object`, `status`, mode/action, event identity, payload schema, compaction policy, boolean decision, or target fields when Rust omits them; missing Rust-owned context lifecycle fields now remain `null`, and conformance guards that the JS command caller cannot become a compatibility context lifecycle projection author | temporary migration cleanup only; replace the remaining JS facade and Node bridge transport with direct Rust daemon-core context lifecycle policy/projection APIs once verified | `packages/runtime-daemon/src/runtime-context-policy-core.mjs`, `crates/services/src/agentic/runtime/kernel/policy/context_lifecycle.rs`, `scripts/conformance/hypervisor-conformance.mjs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:compositor` |
| `RuntimeStateUpdateFallbackRetirement` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md), [`hypervisor-kernel-substrate-migration-matrix.md`](./hypervisor-kernel-substrate-migration-matrix.md) | coding-tool budget recovery, diagnostics operator override, operator interrupt/steer, run cancel, thread control, MCP control, thread memory, runtime bridge thread start/control/turn submit, subagent record, agent create/status, and run create state-update normalizers no longer synthesize public `object` or `status: "planned"` when Rust omits them; missing Rust-owned state-update envelope fields now remain `null`, with focused core tests and conformance guarding the JS command facade against becoming a state-update compatibility envelope author; coding-tool budget recovery, diagnostics operator override, operator turn-control, operator interrupt/steer, and run-cancel command-response wrappers, bridge request types, `_command` source markers, and bridge-shaped owner tests are deleted from their Rust policy children | temporary migration cleanup only; replace the remaining JS facade and Node bridge transport with direct Rust daemon-core state-update/admission/projection APIs once verified | `packages/runtime-daemon/src/runtime-context-policy-core.mjs`, `crates/services/src/agentic/runtime/kernel/policy/coding_tool_budget_recovery.rs`, `crates/services/src/agentic/runtime/kernel/policy/operator_control.rs`, `crates/services/src/agentic/runtime/kernel/policy/thread_lifecycle.rs`, `crates/services/src/agentic/runtime/kernel/policy/mcp_memory.rs`, `scripts/conformance/hypervisor-conformance.mjs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:compositor` |
| `RuntimeMcpMemoryProjectionFallbackRetirement` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md), [`hypervisor-kernel-substrate-migration-matrix.md`](./hypervisor-kernel-substrate-migration-matrix.md) | MCP manager status, validation, catalog, catalog summary, memory manager status, and memory validation projection normalizers no longer synthesize public `object`, `status`, count, readiness, route, or policy/default fields when Rust omits them; missing Rust-owned projection fields now remain `null`, with focused core tests and conformance guarding that the JS command caller cannot become a compatibility projection author | temporary migration cleanup only; replace the remaining JS facade and Node bridge transport with direct Rust daemon-core MCP/memory projection APIs once verified | `packages/runtime-daemon/src/runtime-context-policy-core.mjs`, `crates/services/src/agentic/runtime/kernel/policy/mcp_memory.rs`, `scripts/conformance/hypervisor-conformance.mjs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:compositor` |
| `RuntimeMcpControl` | [`hypervisor-kernel-substrate-unification-master-guide.md`](./hypervisor-kernel-substrate-unification-master-guide.md), [`connector-and-tool-contracts.md`](../components/connectors-tools/contracts.md), [`wallet-network/doctrine.md`](../components/wallet-network/doctrine.md) | public and thread-scoped MCP control, validation, status/catalog/catalog-summary, live-result replay, tool search/fetch, MCP live backend execution, and MCP serve planning/result projection now call typed Rust daemon-core APIs through `RuntimeContextPolicyCore`. MCP live invoke/discovery exits require wallet authority refs, cTEE custody refs, transport containment refs, Rust live-exit receipts, Rust Agentgres live-result state admission, and Rust replay before `writeAgent()`; Rust now materializes deterministic `ioi.runtime.mcp-live-result-payload.v1` protocol payloads/hashes for those live exits, binds the hash through receipt/control/result/replay, embeds `ioi.runtime.mcp-backend-execution.v1` contracts bound to `ioi_drivers::mcp::McpManager`/`McpTransport` with `rust_driver_contract_bound` status, and JS rejects retired pending transport or missing driver-backend contracts instead of treating them as valid truth. The live-result path now calls `daemonCoreMcpApi.executeRuntimeMcpLiveBackend` before receipt/result state commits, requires `runtime_mcp_live_backend_rust_driver_executed` evidence plus `rust_driver_executed` observation details, binds the actual Rust driver-result hash into the public result payload, recomputes the payload hash, returns Rust-owned control/receipt/result truth, and fails closed before any receipt/result commit/replay/agent write if backend execution is missing or unbound, so the planner direct result cannot remain terminal result truth. `RuntimeAgentService::execute_runtime_mcp_live_backend()` now backs that API with real Rust `McpManager` process I/O for `tools/call` and admitted live `tools/list`; `McpTransport` retains the spawned stdio child so `kill_on_drop` no longer kills the server immediately after pipe extraction. MCP catalog live-discovery now returns Rust materialized declared-row snapshots with `rust_mcp_live_discovery_materialized` and `runtime_mcp_live_discovery_rust_materialized` evidence, while the retired `rust_mcp_live_discovery_deferred` marker stays false instead of being accepted as live-discovery success truth. MCP serve `tools/call` now requires Rust wallet authority grant/receipt refs, cTEE custody refs, and transport containment refs before planning or coding-tool invocation; Rust binds those refs into the StepModule invocation request and into the materialized `runtime.mcp_serve` `ioi.runtime.mcp-live-result.v1` record for served tool results, JS must commit that record through Rust Agentgres live-result state admission, and public JSON-RPC returns only the Rust replayed protocol payload. The public daemon now serves only the advertised `/v1/threads/{thread_id}/mcp/serve` route for MCP serve JSON-RPC; top-level `/v1/mcp/serve` and the SDK global `serveMcpRpc()` client are retired, SDK `threadMcpServeRpc()` sends stable `ioi.runtime.mcp-serve-client.v1` protocol bodies with wallet authority refs, cTEE custody refs, and containment refs rather than query-string admission, and IDE MCP serve nodes plus the Rust CLI TUI `/mcp serve` command emit that same canonical daemon protocol body without an endpoint override or duplicate camelCase body fields. Direct JS agent lookup, registry mutation, JS MCP transport execution, local catalog/search/fetch shaping, served tool-call id/idempotency/workflow derivation, MCP result text/event-ref synthesis, uncommitted MCP serve projection return, generic MCP command invoker handoff, `daemonCoreApi.mcp` compatibility mount, MCP serve response wrappers, `RuntimeMcpServeCommandError`, command-source markers, and JS command `operation`/`backend` envelopes remain retired; Rust rejects the retired MCP control/catalog/serve command operations. | Rust daemon-core MCP control/admission/replay/projection object backed by wallet authority for external exits and served tool calls, cTEE custody refs, Agentgres expected-head/state-root binding, MCP control receipts/results, registry truth, transport containment, validation, materialized result-payload binding, Rust driver-backend execution contracts, required live backend execution observation, actual `McpManager` live process I/O, driver-result payload hash and receipt binding, materialized catalog live-discovery snapshots, SDK, IDE, and CLI MCP serve protocol bodies, top-level MCP serve route retirement, and replay; broader non-MCP SDK route-family protocol coverage over Rust replay records remains non-terminal | `packages/runtime-daemon/src/runtime-mcp-control-surface.mjs`, `packages/runtime-daemon/src/runtime-mcp-catalog-surface.mjs`, `packages/runtime-daemon/src/runtime-mcp-serve-surface.mjs`, `packages/runtime-daemon/src/runtime-context-policy-core.mjs`, `packages/runtime-daemon/src/http/public-runtime-routes.mjs`, `packages/runtime-daemon/src/http/public-runtime-routes.test.mjs`, `packages/agent-sdk/src/substrate-client.ts`, `packages/agent-sdk/src/index.ts`, `packages/agent-sdk/test/sdk.test.mjs`, `crates/services/src/agentic/runtime/service/mcp_live_backend.rs`, `crates/drivers/src/mcp/mod.rs`, `crates/drivers/src/mcp/transport.rs`, `crates/services/src/agentic/runtime/kernel/runtime_mcp_serve.rs`, `crates/services/src/agentic/runtime/kernel/policy/mcp_memory.rs`, `crates/services/src/agentic/runtime/kernel/command_protocol.rs`, `crates/services/src/agentic/runtime/kernel/command_dispatch.rs`, `packages/agent-ide/src/runtime/workflow-runtime-mcp-control-nodes.ts`, `crates/cli/src/commands/agent_tui.rs`, `crates/cli/src/commands/agent_tui_loop.rs`, `scripts/conformance/hypervisor-conformance.mjs` | `hypervisor-conformance:bridge`, `hypervisor-conformance:compositor` |

Implementation note: Slice 964 retired the route-facing daemon-store MCP
pass-through wrappers. Public and thread MCP routes now call mounted MCP
catalog/control/serve surfaces directly; this remains migration scaffolding
until direct Rust daemon-core MCP admission/projection and Agentgres-backed
truth replace the JS surface edge. Slice 1098 deletes the remaining unused
daemon-store MCP helper delegates (`applyThreadMcpServerMutation()`,
`mcpStatusWithLiveDiscovery()`, `appendThreadMcpControlEvent()`, and
`mcpServersForContext()`) so mounted surfaces cannot be bypassed through stale
store wrappers.

| `ActionProposal` | [`default-harness-profile.md`](../components/daemon-runtime/default-harness-profile.md) | action request, runtime item, event | promote when approval/replay/policy review needs identity | `crates/types/src/app/runtime/computer_use.rs` | DHP-2 |
| `GateResult` | [`default-harness-profile.md`](../components/daemon-runtime/default-harness-profile.md), [`wallet-network/doctrine.md`](../components/wallet-network/doctrine.md) | policy/authority decision receipt | receipt now; object if long-lived approval/blocker | policy and approval handlers | DHP-2, CEC phase order |
| `ExecutionResult` | [`events-receipts-delivery-bundles.md`](../components/daemon-runtime/events-receipts-delivery-bundles.md) | workload/tool/worker/service receipt | receipt; artifact refs for large outputs | tool execution processing paths | CEC execution/verification |
| `NormalizedObservation` | [`default-harness-profile.md`](../components/daemon-runtime/default-harness-profile.md) | typed event/projection payload | promote when reused by verifiers/downstream tasks | browser observation and completion receipt paths | DHP-3, DHP-6 |
| `OutputOwnershipPass` | [`default-harness-profile.md`](../components/daemon-runtime/default-harness-profile.md) | completion receipt plus terminal event | promote for delivery, dispute, replay, marketplace settlement | `crates/services/src/agentic/runtime/service/queue/processing/completion_receipts.rs` | DHP-6 |
| `Blocker` | [`default-harness-profile.md`](../components/daemon-runtime/default-harness-profile.md), [`daemon-runtime/api.md`](../components/daemon-runtime/api.md) | event plus task state | object when user/operator action or long wait is needed | approval/blocker runtime handlers | DHP-2, DHP-4 |
| `AlphaSeal` | [`private-workspace-ctee.md`](../components/daemon-runtime/private-workspace-ctee.md) | encrypted/private strategy capsule artifact ref plus policy refs | object when strategy lifecycle, reuse, or audit matters | planned | private strategy checks |
| `DeclassificationGate` / `DeclassificationReceipt` | [`private-workspace-ctee.md`](../components/daemon-runtime/private-workspace-ctee.md), [`wallet-network/doctrine.md`](../components/wallet-network/doctrine.md) | policy gate plus receipt | receipt by default; object if long-lived policy state is queried | planned | capability-exit checks |
| `PrivateInferenceReceipt` | [`private-workspace-ctee.md`](../components/daemon-runtime/private-workspace-ctee.md), [`events-receipts-delivery-bundles.md`](../components/daemon-runtime/events-receipts-delivery-bundles.md) | receipt | proof object | planned | no plaintext on node claim |

## State, Memory, Artifact, and Receipt Concepts

| Concept | Canonical owner | Current durable form | Promote or keep | Code anchor when known | Conformance hook |
| --- | --- | --- | --- | --- | --- |
| `Run` | [`agentgres/api-object-model.md`](../components/agentgres/api-object-model.md) | Agentgres runtime object | canonical object for serious runs | runtime thread/run records | DHP-1, DHP-7 |
| `Task` / `TaskState` | [`agentgres/api-object-model.md`](../components/agentgres/api-object-model.md) | Agentgres runtime object | canonical object for serious runs | runtime state objects | DHP-4 |
| `ContextMutation` | [`agentgres/api-object-model.md`](../components/agentgres/api-object-model.md), [`agentgres/doctrine.md`](../components/agentgres/doctrine.md) | Agentgres operation/object-shaped memory admission | canonical when durable behavior-affecting memory changes | memory API and Agentgres object model | memory admission invariant |
| `AgentWiki` | [`agentgres/doctrine.md`](../components/agentgres/doctrine.md) | memory/wiki surface plus projections; durable commits via `ContextMutation` | context-memory plane, not Agentgres truth by itself | memory runtime/API | memory admission invariant |
| `ioi-memory` | [`agentgres/doctrine.md`](../components/agentgres/doctrine.md), [`daemon-runtime/api.md`](../components/daemon-runtime/api.md) | local/runtime memory plane, thread checkpoints, enrichment jobs | keep adjacent to Agentgres; admit durable changes through operations | memory runtime/API | memory admission invariant |
| `ArtifactRef` | [`agentgres/artifact-ref-plane.md`](../components/agentgres/artifact-ref-plane.md) | Agentgres object/ref | canonical ref; bytes live in storage backends | artifact APIs | DHP-5, artifact-ref conformance |
| `PayloadRef` | [`agentgres/artifact-ref-plane.md`](../components/agentgres/artifact-ref-plane.md) | operation payload ref | keep as operation/ref field unless lifecycle needs object | artifact APIs | DHP-5, artifact-ref conformance |
| `EvidenceBundle` | [`agentgres/artifact-ref-plane.md`](../components/agentgres/artifact-ref-plane.md) | Agentgres-governed bundle ref plus receipt/artifact graph | promote when claims, verification, replay, or dispute need bundle identity | evidence/artifact APIs | DHP-5, verification receipt checks |
| `Receipt` | [`events-receipts-delivery-bundles.md`](../components/daemon-runtime/events-receipts-delivery-bundles.md) | receipt object/envelope, indexed through Agentgres | canonical proof object | `crates/types/src/app/harness/receipts.rs` | CEC, DHP-3 |
| `TraceRef` | [`events-receipts-delivery-bundles.md`](../components/daemon-runtime/events-receipts-delivery-bundles.md) | trace ref plus storage payload | ref/projection; payload in storage backend | runtime trace exports | DHP-3 |
| `AgentStateArchive` | [`agentgres/artifact-ref-plane.md`](../components/agentgres/artifact-ref-plane.md), [`agentgres/api-object-model.md`](../components/agentgres/api-object-model.md) | Agentgres archive ref plus encrypted payload bytes | canonical ref/object; payload in storage backend | planned archive/restore APIs | restore invariant |
| `ProjectionCheckpoint` | [`agentgres/api-object-model.md`](../components/agentgres/api-object-model.md) | projection state/checkpoint ref | projection, invalidatable/rebuildable | Agentgres projection APIs | projection rebuild checks |
| `DeliveryBundle` | [`agentgres/artifact-ref-plane.md`](../components/agentgres/artifact-ref-plane.md), [`events-receipts-delivery-bundles.md`](../components/daemon-runtime/events-receipts-delivery-bundles.md) | delivery artifact refs + receipt bundle + settlement state | object when marketplace/dispute acceptance applies | delivery APIs | DHP-6, DHP-7 |
| `PrivateUserAppStateRef` | [`agentgres/artifact-ref-plane.md`](../components/agentgres/artifact-ref-plane.md) | Agentgres-governed encrypted payload ref plus policy, receipt, authority, and state-root linkage | ref by default; promote when lifecycle, query, restore, dispute, or cross-app portability needs object identity | planned app-state/artifact APIs | private-state split invariant |
| `StorageBackend` | [`storage-backends/doctrine.md`](../components/storage-backends/doctrine.md) | payload byte store profile/config | keep as backend profile unless domain policy needs object identity | storage/artifact APIs | storage backend conformance |
| `FilecoinCASBackend` | [`storage-backends/filecoin-cas.md`](../components/storage-backends/filecoin-cas.md) | storage backend profile | byte availability only; not authority layer | artifact APIs | storage backend conformance |
| Restore/import flow | [`agentgres/artifact-ref-plane.md`](../components/agentgres/artifact-ref-plane.md) | Agentgres operations plus archive payload refs and receipts | operation-backed flow; never silent file mutation | planned archive/restore APIs | restore invariant |

## Authority, Interop, Marketplace, and Settlement Concepts

| Concept | Canonical owner | Current durable form | Promote or keep | Code anchor when known | Conformance hook |
| --- | --- | --- | --- | --- | --- |
| `AuthorityGrant` | [`wallet-network/doctrine.md`](../components/wallet-network/doctrine.md), [`wallet-network/api-authority-scopes.md`](../components/wallet-network/api-authority-scopes.md) | grant/lease/approval record | canonical authority object outside Agentgres truth | authority API and approval handlers | DHP-2 |
| `WalletAuthorityReview` | [`wallet-network/product-exchange-risk.md`](../components/wallet-network/product-exchange-risk.md), [`wallet-network/api-authority-scopes.md`](../components/wallet-network/api-authority-scopes.md) | reusable review object for Wallet app, embedded approval card, mobile sheet, CLI prompt, and advanced console | canonical product/API object for authority review; not a UI-only object | planned authority review APIs | review must bind intent, simulation, risk, eligibility, policy explanation, approval modes, and receipt refs |
| `ApprovalMode` | [`wallet-network/product-exchange-risk.md`](../components/wallet-network/product-exchange-risk.md), [`wallet-network/api-authority-scopes.md`](../components/wallet-network/api-authority-scopes.md) | enum-like policy result: one-shot, session envelope, batch, silent-within-policy, after-the-fact receipt, step-up, denied | canonical policy-derived mode; apps may request but wallet decides | planned approval API and SDK helpers | approval mode cannot widen authority beyond policy |
| `WalletPresentationProfile` | [`wallet-network/product-exchange-risk.md`](../components/wallet-network/product-exchange-risk.md), [`vocabulary.md`](./vocabulary.md) | UI profile over the same authority review contract | keep as product/API presentation hint, not authority by itself | planned Wallet SDK/render-profile helpers | presentation profile must not change policy result or receipt semantics |
| `CapabilityLease` | [`wallet-network/api-authority-scopes.md`](../components/wallet-network/api-authority-scopes.md), [`wallet-network/product-exchange-risk.md`](../components/wallet-network/product-exchange-risk.md) | scoped, expiring lease over a capability such as connector use, broker action, cloud deploy, or app permission | canonical authority object for credential/capability use without durable secret custody | planned capability lease APIs | agents never receive long-lived credentials by default; every use is receipted or aggregated under policy |
| `RiskCoverageState` | [`wallet-network/product-exchange-risk.md`](../components/wallet-network/product-exchange-risk.md), [`vocabulary.md`](./vocabulary.md) | label metadata for assessed/unknown/unassessed/stale/partially-covered/conflicting-source risk or eligibility claims | canonical metadata attached to risk/eligibility labels | planned risk registry and approval review schemas | absence of risk label is never safe; unknown/unassessed/stale must surface as caution |
| `ExchangeIntent` | [`wallet-network/product-exchange-risk.md`](../components/wallet-network/product-exchange-risk.md), [`exchange.md`](../domains/decentralized/exchange.md), [`wallet-network/api-authority-scopes.md`](../components/wallet-network/api-authority-scopes.md) | wallet authority object binding route, calldata, slippage, simulation, policy, risk labels, grant/lease, revocation epoch, economics, and exact `TxIntent` records | canonical object for wallet exchange approval/execution | planned exchange authority APIs | exchange intent binding |
| `RouteCandidate` | [`exchange.md`](../domains/decentralized/exchange.md), [`wallet-network/product-exchange-risk.md`](../components/wallet-network/product-exchange-risk.md) | candidate route from decentralized.exchange route-intelligence, direct pools, DEX routers, bridge routers, solvers, quote APIs, or user routes | keep as candidate/proposal until selected into `ExchangeIntent`; never authority by itself | planned route-source adapters | route source is not authority |
| `TradeIntent` | [`wallet-network/product-exchange-risk.md`](../components/wallet-network/product-exchange-risk.md), [`trade.md`](../domains/decentralized/trade.md) | wallet authority object binding venue, market, side, collateral, leverage, margin mode, order type, liquidation/funding assumptions, simulation, policy, risk labels, grant/lease, revocation epoch, and exact venue/order/TxIntent records | canonical object for advanced trade approval/execution | planned trade authority APIs | trade intent binding; perps/margin/position actions are high-risk and agent-denied by default; prediction markets use `PredictionIntent` |
| `PositionReceipt` | [`trade.md`](../domains/decentralized/trade.md), [`wallet-network/product-exchange-risk.md`](../components/wallet-network/product-exchange-risk.md), [`events-receipts-delivery-bundles.md`](../components/daemon-runtime/events-receipts-delivery-bundles.md) | receipt over position state, risk status, policy status, liquidation/funding snapshots, PnL, close conditions, and venue metadata | receipt by default; promote to object when long-lived monitoring, dispute, portfolio risk, or agent policy needs query/conflict handling | planned trade/position APIs | position risk changes are receipted and cannot be hidden behind swap history |
| `PredictionIntent` | [`trade.md`](../domains/decentralized/trade.md), [`wallet-network/product-exchange-risk.md`](../components/wallet-network/product-exchange-risk.md) | wallet authority object for prediction markets/event contracts binding venue, market question, outcome, side, price limit, shares, max loss, max payout, resolution source, market rules, liquidity snapshot, policy, risk labels, grant/lease, revocation epoch, and venue/order records | canonical object for event exposure approval/execution | planned prediction/event-market authority APIs | prediction markets are event exposure, not swaps; agents denied live prediction authority by default |
| `PredictionReceipt` | [`trade.md`](../domains/decentralized/trade.md), [`wallet-network/product-exchange-risk.md`](../components/wallet-network/product-exchange-risk.md), [`events-receipts-delivery-bundles.md`](../components/daemon-runtime/events-receipts-delivery-bundles.md) | receipt over prediction/event-market order, risk, policy, resolution source, market rules, max loss/payout, execution, and resolution refs | receipt by default; promote to object when long-lived monitoring, resolution dispute, compliance, portfolio risk, or agent policy needs query/conflict handling | planned prediction/event-market receipt APIs | prediction risk and resolution changes are receipted and cannot be hidden behind swap or position history |
| `TxIntent` | [`wallet-network/doctrine.md`](../components/wallet-network/doctrine.md), [`wallet-network/product-exchange-risk.md`](../components/wallet-network/product-exchange-risk.md) | exact chain transaction intent bound to policy, grant/lease, revocation epoch, calldata, slippage, and simulation hash | canonical executable object for chain actions; contained by `ExchangeIntent` for swaps | planned wallet execution APIs | exact transaction approval binding |
| `WalletReceipt` / `ExchangeReceipt` | [`wallet-network/product-exchange-risk.md`](../components/wallet-network/product-exchange-risk.md), [`events-receipts-delivery-bundles.md`](../components/daemon-runtime/events-receipts-delivery-bundles.md) | human-readable and machine-verifiable wallet receipt; indexed through Agentgres when operational truth is affected | canonical proof object for wallet actions | planned wallet activity/receipt APIs | wallet receipt binding |
| `AssetExposureRecord` | [`wallet-network/product-exchange-risk.md`](../components/wallet-network/product-exchange-risk.md) | exposure record over asset/account cryptography, public-key exposure, bridge/admin/oracle dependency, approval exposure, agent access, policy level, and recommendations | object when displayed, queried, or used for protection policy | planned risk/exposure APIs | risk label accuracy |
| `ProtectionAction` | [`wallet-network/product-exchange-risk.md`](../components/wallet-network/product-exchange-risk.md) | proposed/approved action such as revoke approval, reduce allowance, move funds, isolate agent funds, or require step-up | receipt-backed object when consequential | planned protection APIs | protection receipt binding |
| `ApprovalInboxItem` | [`wallet-network/product-exchange-risk.md`](../components/wallet-network/product-exchange-risk.md), [`wallet-network/api-authority-scopes.md`](../components/wallet-network/api-authority-scopes.md) | pending wallet authority decision with initiator, requested action, risk classes/labels, affected assets/secrets, policy diff, simulation, expiry, and approve/edit/deny actions | product object backed by authority request/receipt | approval inbox APIs | step-up and denial receipts |
| `@ioi/wallet-protocol` | [`wallet-network/doctrine.md`](../components/wallet-network/doctrine.md), [`wallet-network/api-authority-scopes.md`](../components/wallet-network/api-authority-scopes.md), [`wallet-protocol-sdk-packaging-plan.md`](./wallet-protocol-sdk-packaging-plan.md) | target generated package; not present as a package yet. Current anchors are Rust `ioi_types::app::wallet_network`, `WalletNetworkService`, wallet service tests, session-channel E2E tests, and architecture API doctrine | generate from IOI-owned contracts; product repos consume it, never author it | `crates/types/src/app/wallet_network`, `crates/services/src/wallet_network`, `crates/services/src/wallet_network/tests`, `crates/cli/tests/wallet_network_session_channel_e2e` | generated protocol package must match Rust/service/conformance fixtures |
| `@ioi/wallet-sdk` | [`wallet-network/doctrine.md`](../components/wallet-network/doctrine.md), [`wallet-network/api-authority-scopes.md`](../components/wallet-network/api-authority-scopes.md), [`wallet-protocol-sdk-packaging-plan.md`](./wallet-protocol-sdk-packaging-plan.md) | target typed client package; not present as a package yet. Should wrap wallet.network APIs, grants, leases, receipts, exchange/trade intents, capability exits, and revocation | generate or build as a client over `@ioi/wallet-protocol`; product repos import it | planned `packages/wallet-sdk` or equivalent generated client package | SDK helpers must not invent scopes, receipts, or authority semantics outside protocol fixtures |
| `prim:*` | [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md) | primitive execution capability vocabulary | keep as vocabulary, not authority grant | runtime contracts | CIRC capability gates |
| `scope:*` | [`wallet-network/api-authority-scopes.md`](../components/wallet-network/api-authority-scopes.md) | wallet/provider authority scope vocabulary | keep as authority vocabulary | authority scope APIs | DHP-2 |
| `WorkerPackage` | [`aiagent/worker-marketplace.md`](../domains/aiagent/worker-marketplace.md), [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md) | manifest/package artifact ref plus registry/listing state | object/listing where installed, published, or routed | worker manifests and harness bindings | marketplace neutrality |
| `ServicePackage` | [`sas/service-marketplace.md`](../domains/sas/service-marketplace.md), [`default-harness-profile.md`](../components/daemon-runtime/default-harness-profile.md) | service manifest/package artifact ref plus optional marketplace contract | object when ordered, installed, or delivered | service APIs | DHP-8 |
| `ServiceEngine` | [`default-harness-profile.md`](../components/daemon-runtime/default-harness-profile.md), [`sas/service-marketplace.md`](../domains/sas/service-marketplace.md) | runtime invocation state plus service receipts | object when long-lived or contract-bound | planned service runtime | DHP-2, DHP-6 |
| `MoWRoutingDecision` | [`mixture-of-workers.md`](../foundations/mixture-of-workers.md), [`marketplace-neutrality.md`](../domains/marketplace-neutrality.md) | routing receipt plus Agentgres state | object when payment/reputation/dispute applies | routing receipts | DHP-8 |
| `AIIPEnvelope` | [`aiip.md`](../foundations/aiip.md), [`common-objects-and-envelopes.md`](../foundations/common-objects-and-envelopes.md) | signed/sequenced packet envelope | canonical interop envelope | planned AIIP channel/router code | AIIP profile conformance |
| `AIIPChannel` | [`aiip.md`](../foundations/aiip.md) | channel/profile registration and policy state | object when cross-domain interop persists | planned AIIP registry/router | AIIP profile conformance |
| L1 settlement trigger | [`ioi-l1-mainnet.md`](../foundations/ioi-l1-mainnet.md), [`default-harness-profile.md`](../components/daemon-runtime/default-harness-profile.md) | settlement intent, local settlement receipt, optional L1 commitment | trigger-based commitment, not default runtime event | L1 contract interfaces | DHP-7 |
| Marketplace listing | [`aiagent/worker-marketplace.md`](../domains/aiagent/worker-marketplace.md), [`sas/service-marketplace.md`](../domains/sas/service-marketplace.md) | listing state, manifest refs, license/contract refs | canonical marketplace object | marketplace APIs | marketplace neutrality |
| ContributionReceipt | [`marketplace-neutrality.md`](../domains/marketplace-neutrality.md), [`events-receipts-delivery-bundles.md`](../components/daemon-runtime/events-receipts-delivery-bundles.md) | receipt plus attribution graph/projection | object when rewards/reputation/disputes apply | routing/receipt paths | DHP-8 |

## Build Rule

When unsure whether a concept should become a canonical object:

```text
If it only needs observability, emit an event.
If it proves an accountable transition, emit a receipt.
If it changes admitted truth, create an Agentgres operation.
If it is a rebuildable view, make it a projection.
If it points to bytes, create an ArtifactRef/PayloadRef.
If it needs lifecycle, query, replay, conflict handling, or settlement, promote it to a canonical object.
```
