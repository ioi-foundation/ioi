# Deprecated Path: CIRC

Status: deprecated compatibility stub.
Superseded by:
[`../hypervisor-core/intent-resolution.md`](../hypervisor-core/intent-resolution.md).

`CIRC` remains a stable conformance label, but the active contract is no longer
scoped to a single `agentic-runtime` directory. Use the Hypervisor Core intent
resolution contract for current architecture, tests, and references.
