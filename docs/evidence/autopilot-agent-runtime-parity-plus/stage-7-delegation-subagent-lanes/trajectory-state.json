{
  "schemaVersion": "ioi.agent-runtime-parity-plus.trajectory-state.v1",
  "stage": 7,
  "proof": "delegation snapshot consumes persisted trajectory presence for child sessions and emits replay_cursor_step from parent AgentState.step_count",
  "unitAssertions": [
    "child trajectory present is true when get_agent_trajectory_step_key(child, child.step_count) exists",
    "child brain present is true when get_agent_brain_key(child) exists",
    "parent playbook active_child_session_id is recovered when persisted parent playbook run exists"
  ]
}
