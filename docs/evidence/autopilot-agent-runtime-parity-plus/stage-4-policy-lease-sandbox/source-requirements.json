{
  "schemaVersion": "ioi.agent-runtime-parity-plus.source-requirements.v1",
  "stage": 4,
  "requirements": [
    {
      "id": "ARP-P0-008",
      "requirement": "daemon-owned policy leases for default permissions, auto-review, full access, allow-once, deny, revoke, and expiry",
      "coveredNow": [
        "Default permissions, auto-review, and full access are classified from persisted daemon policy.",
        "Pending approval gate state is derived from canonical pending action state.",
        "Persisted approval grants project allow-once, expiry, and authority revocation/missing/expired status."
      ],
      "remaining": [
        "Live GUI proof for allow-once, deny, revoke, and expiry across a real Agent action."
      ]
    },
    {
      "id": "ARP-P0-009",
      "requirement": "hardened sandbox semantics: workspace boundary, symlink/sibling escape rejection, ignored-file protection, default network deny, env/log redaction, timeout and output caps",
      "coveredNow": [
        "Workspace filesystem boundary helper remains green for absolute escape and repo-scoped read/write rules.",
        "Runtime inspect exposes workspace-scoped filesystem, reject-escape symlink posture, ignored-file protection, redacted env/log posture, bounded action timeout, and bounded/redacted output posture."
      ],
      "remaining": [
        "Live effect probes for symlink escape, sibling-prefix escape, ignored file, network deny, env/log redaction, timeout, and output caps."
      ]
    }
  ]
}
