IOI_MEASURED_COMMIT=f6c2bcebd4139d2315cea95a45ea9717bb4050d5
PASS the comment-stripped opener check is not vacuous — the raw source DOES match (its own doc comment names the openers), so a raw grep would be prose matching prose
PASS lifted core has no handle-acquisition path in SHIPPED code — 0 opener call sites in the part that ships, comments stripped
PASS the only opener use is inside #[cfg(test)] and is therefore compiled out — the exception is named and gated, not waved through
PASS the lifted core is the only admission discipline — the binary-module implementation was deleted, not duplicated
PASS no caller-side idempotency scan survives in the daemon — dedup is inherited from admission, not re-implemented
PASS no caller-side idempotency scan survives in the runtime bridge — dedup is inherited from admission, not re-implemented
PASS the capability surface is exactly four operations — methods=admit_event,read_head,admit_lease_transition,advance_checkpoint
PASS the four operations are the ruled ones — a fifth requires a filed record
IOI_ISOLATED_LISTENER_OWNERSHIP role=daemon pid=1099321 port=46691 inode=213870467 owned=true
PASS the isolated plane authenticates an owner — login=200
PASS a stream is declared once, inside an expected-absent genesis operation — status=200 seq=0
PASS redeclaration is refused by the substrate's own compare-and-swap — status=409 code=event_stream_already_declared
PASS a class on BOTH sides of the event-class line refuses at declaration — status=422 code=event_class_on_both_sides
PASS an append to an UNDECLARED stream refuses rather than defaulting a side — status=422 code=event_stream_undeclared
PASS a request-supplied declaration CANNOT reclassify an admitted class (reviewer repro) — delivery=admitted (request claimed ephemeral; stream truth says admitted)
PASS boot injection is LIVE: a real admission succeeds against a booted daemon — status=200 seq=2
PASS an un-injected build would be visible here, not silent — the capability-absent refusal is the failure this assertion would show
PASS the thread plane is READABLE — the instrument can see it at all — status=200 seq=2
PASS the instrument reads NON-ZERO movement when the thread plane IS traversed — seq 2 -> 3
PASS the zero-traversal assertion has a PROVEN instrument (fails closed otherwise) — movement observed before zero is claimed
PASS a second, unrelated owner namespace admits through the same path — status=200
PASS both namespaces produce structurally identical admitted facts — keys=admitted_head,agentgres_sequence,class_id,delivery,owner_namespace,payload_schema_ref,replayed,stream_id
PASS each namespace brands with its OWN namespace and no other — scheduler stream=event-stream://automation-scheduler/s1
PASS the two namespaces are separate object identities, not one shared chain — distinct operation refs
PASS driving automation-scheduler produced ZERO thread-plane traversals — thread seq 3 -> 3
PASS the stream walks forward under expected-head CAS — statuses=200,200,200,200
PASS a duplicate key FIVE events back replays the original, live — replayed=true seq=1 original=1
PASS same key with different bytes refuses by its own name — status=422 code=event_stream_same_key_different_bytes
PASS an ephemeral class mints no sequence, head, root, or receipt — delivery=ephemeral
PASS ephemeral delivery advanced NOTHING: the head is still the genesis — genesis seq=0 head seq=0
PASS an undeclared class refuses rather than defaulting to either side — status=422 code=event_class_undeclared
PASS a stale expected head is refused, not silently accepted — status=409 code=event_stream_expected_head_conflict
PASS a non-canonical owner namespace refuses — status=422
PASS the create response carries every REQUIRED field of the declared lease schema — status=200 missing=none
PASS a lease is READABLE as admitted truth, not adapter-local state — status=200 state=active
PASS delivery serves the stream's admitted events through the lease — status=200 events=3 pending=5
PASS lag resolves to a TYPED OUTCOME bounded by the declared window, never a silent drop — outcome=bounded_by_backpressure_window window=3 pending=5
PASS checkpoint substitution refuses: an unadmitted sequence cannot be acknowledged — status=422 code=subscription_checkpoint_not_admitted
PASS a checkpoint advance is an ADMITTED transition, not a scalar the adapter holds — status=200 ack=1 op=agentgres://operation/subscription-lease/sub_1/s
PASS a checkpoint cannot REWIND: re-delivering acknowledged events refuses — status=409 code=subscription_checkpoint_would_rewind
PASS delivery RESUMES FROM THE DURABLE CHECKPOINT, not from the beginning — from=1 first=2
PASS revoke is an admitted lease transition — status=200 state=revoked
PASS delivery under a REVOKED lease refuses by name rather than returning empty — status=409 code=subscription_lease_revoked
PASS delivery past EXPIRY refuses by name — status=409 code=subscription_lease_expired
PASS a broadcast lag ADMITS a typed gap instead of dropping silently — the silent-drop warn is gone and the gap is admitted onto the stream
PASS the delivery gap is a first-class admitted EVENT, not metadata — it occupies a sequence and is replayed like any other event
PASS the SSE body states its resume contract rather than requiring inference — resume point and source are read FROM the response, not reconstructed from it
PASS admitted-append response keys are pinned byte-for-byte — keys=admitted_head,agentgres_sequence,class_id,delivery,owner_namespace,payload_schema_ref,replayed,stream_id
PASS admitted_head sub-object keys are pinned — keys=admission_receipt_ref,admission_root_ref,operation_ref,resulting_head_ref
PASS lease response keys are pinned byte-for-byte — keys=acknowledged_checkpoint,admitted_lease_transition,backpressure,delivery_adapter_kind,expires_at_ref,lease_id,lease_state,nonclaim,projection_binding,schema_version,stream_id,subscriber_ref
PASS delivery response keys are pinned byte-for-byte — keys=backpressure_window,delivered_from_checkpoint,delivery_outcome,events,lease_id,nonclaim,pending_total,resume_after_seq
PASS a refusal body is byte-identical to its pinned bytes — match=true
PASS every refusal body carries exactly {error:{code,message}} and nothing else — uniform refusal envelope across six distinct refusals
IOI_ISOLATED_LISTENER_OWNERSHIP role=daemon pid=1099964 port=46467 inode=214885461 owned=true
PASS a lease SURVIVES RESTART with its admitted state intact — state=revoked ack=1
PASS the stream reconstructs EXACT accepted history across restart — head unchanged=true
PASS the traversal instrument is READABLE — walks=0 admits=17
PASS the instrument reads NON-ZERO when an admitted append DOES traverse — admits 17 -> 18
PASS an ephemeral append performs ZERO history walks — walks 0 -> 0
PASS an ephemeral append performs ZERO admissions — admits 18 -> 18
PASS the ephemeral path resolved its class from steward-held state, not the log — cache hits 15 -> 16
IOI_ISOLATED_LISTENER_OWNERSHIP role=daemon pid=1100516 port=37021 inode=214646048 owned=true
PASS an ephemeral append on a COLD (restarted) daemon performs ZERO history walks — walks 0 -> 0 (Codex measured 0 -> 1 before the fix)
PASS an ephemeral append on a COLD daemon performs ZERO admissions — admits 0 -> 0
PASS the declaration was hydrated at STEWARD OPEN, not on the delivery path — open-time fills=5, unchanged by delivery
PASS the assertion count is pinned — ran=62 expected=62
63/63 passed
M5 event-substrate genericity: PASS (comment-stripped opener check, two-namespace traversal, positive-detection zero-thread-plane, live boot injection)
