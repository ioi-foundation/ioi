// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.2
//   protoc               v3.21.12
// source: control/v1/control.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientUnaryCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";

export const protobufPackage = "ioi.control.v1";

/** Path: crates/ipc/proto/control/v1/control.proto */

export interface LoadModelRequest {
  modelId: string;
  /** Identifier for the shared memory region (e.g., shm_open name) */
  shmemRegionId: string;
}

export interface LoadModelResponse {
  success: boolean;
  /** Bytes allocated in shared memory for this context */
  memoryUsageBytes: number;
}

export interface ExecuteJobRequest {
  jobId: number;
  /** Offset in the shared memory region where the input (AgentContext) begins */
  inputOffset: number;
  /** Length of the input data */
  inputLength: number;
  /** The session ID used for key derivation (32 bytes) */
  sessionId: Uint8Array;
}

export interface ExecuteJobResponse {
  success: boolean;
  /** Offset in shared memory where the output (InferenceOutput) was written */
  outputOffset: number;
  outputLength: number;
  gasUsed: number;
  errorMessage: string;
}

export interface HealthCheckRequest {
}

export interface HealthCheckResponse {
  ready: boolean;
  status: string;
}

export interface SecureEgressRequest {
  domain: string;
  path: string;
  method: string;
  body: Uint8Array;
  /**
   * The identifier of the secret to use (e.g., "openai_primary")
   * This maps to a file in the Guardian's secure keystore.
   */
  secretId: string;
  /**
   * [NEW] JSON path for payload injection (e.g. "payment.handlers[0].token")
   * If present, the Guardian parses the body as JSON and injects the secret here
   * instead of (or in addition to) the Authorization header.
   */
  jsonPatchPath: string;
  /** Required finality tier for this egress. */
  requiredFinalityTier: number;
  /** Canonical SCALE-encoded SealedFinalityProof when sealed finality is required. */
  sealedFinalityProof: Uint8Array;
  /** Canonical SCALE-encoded SealObject when proof-carrying sealed effects are used. */
  sealObject: Uint8Array;
  /** Canonical SCALE-encoded CanonicalCollapseObject when sealed finality is required. */
  canonicalCollapseObject: Uint8Array;
}

export interface SecureEgressResponse {
  body: Uint8Array;
  /** The hash of the TLS certificate from the remote server. */
  certHash: Uint8Array;
  /** The Guardian's signature attesting to this network call. */
  guardianSignature: Uint8Array;
  /** Canonical guardianized receipt envelope, SCALE encoded. */
  receipt: Uint8Array;
  /** Finality tier actually satisfied by the Guardian for this effect. */
  finalityTier: number;
}

export interface SignConsensusRequest {
  payloadHash: Uint8Array;
  subject: Uint8Array;
  expectedCounter: number;
  expectedTraceHash: Uint8Array;
  measurementRoot: Uint8Array;
  policyHash: Uint8Array;
  manifestHash: Uint8Array;
  height: number;
  view: number;
  witnessManifestHash: Uint8Array;
  witnessReassignmentDepth: number;
  recoveryCapsuleHash: Uint8Array;
  shareCommitmentHash: Uint8Array;
}

export interface SignConsensusResponse {
  signature: Uint8Array;
  counter: number;
  traceHash: Uint8Array;
  /** Canonical guardian certificate, SCALE encoded when present. */
  guardianCertificate: Uint8Array;
}

export interface SealConsensusRequest {
  payloadHash: Uint8Array;
  subject: Uint8Array;
  expectedCounter: number;
  expectedTraceHash: Uint8Array;
  measurementRoot: Uint8Array;
  policyHash: Uint8Array;
  manifestHash: Uint8Array;
  height: number;
  view: number;
  witnessManifestHashes: Uint8Array[];
  witnessReassignmentDepth: number;
  observerPlanEntries: Uint8Array[];
  /** Canonical SCALE-encoded AsymptotePolicy payload for the target slot. */
  asymptotePolicy: Uint8Array;
  /** Canonical SCALE-encoded GuardianWitnessRecoveryBindingAssignment payloads. */
  witnessRecoveryBindings: Uint8Array[];
  /** Canonical SCALE-encoded AssignedRecoveryShareEnvelopeV1 payloads. */
  witnessRecoveryShareEnvelopes: Uint8Array[];
}

export interface SealConsensusResponse {
  /** Canonical guardian certificate, SCALE encoded when present. */
  guardianCertificate: Uint8Array;
  /** Canonical sealed finality proof, SCALE encoded when present. */
  sealedFinalityProof: Uint8Array;
  counter: number;
  traceHash: Uint8Array;
}

export interface ObserveAsymptoteRequest {
  /** Canonical SCALE-encoded AsymptoteObserverObservationRequest payload. */
  observationRequest: Uint8Array;
  manifestHash: Uint8Array;
}

export interface ObserveAsymptoteResponse {
  /** Canonical SCALE-encoded AsymptoteObserverObservation payload. */
  observation: Uint8Array;
}

export interface SignCommitteeDecisionRequest {
  /** Canonical SCALE-encoded GuardianDecision payload. */
  decision: Uint8Array;
  manifestHash: Uint8Array;
  height: number;
  view: number;
}

export interface GuardianMemberSignature {
  memberIndex: number;
  signature: Uint8Array;
}

export interface SignCommitteeDecisionResponse {
  manifestHash: Uint8Array;
  decisionHash: Uint8Array;
  partialSignatures: GuardianMemberSignature[];
}

export interface SignWitnessStatementRequest {
  /** Canonical SCALE-encoded GuardianWitnessStatement payload. */
  statement: Uint8Array;
  manifestHash: Uint8Array;
  /** Canonical SCALE-encoded AssignedRecoveryShareEnvelopeV1 payload when present. */
  recoveryShareEnvelope: Uint8Array;
}

export interface SignWitnessStatementResponse {
  manifestHash: Uint8Array;
  statementHash: Uint8Array;
  partialSignatures: GuardianMemberSignature[];
}

export interface LoadAssignedRecoveryShareRequest {
  height: number;
  manifestHash: Uint8Array;
  recoveryCapsuleHash: Uint8Array;
  shareCommitmentHash: Uint8Array;
}

export interface LoadAssignedRecoveryShareResponse {
  /** Canonical SCALE-encoded RecoveryShareMaterial when present. */
  recoveryShareMaterial: Uint8Array;
}

export interface ReportWitnessFaultRequest {
  /** Canonical SCALE-encoded GuardianWitnessFaultEvidence payload. */
  evidence: Uint8Array;
}

export interface ReportWitnessFaultResponse {
  /** Canonical SCALE-encoded GuardianLogCheckpoint payload when persisted. */
  checkpoint: Uint8Array;
}

function createBaseLoadModelRequest(): LoadModelRequest {
  return { modelId: "", shmemRegionId: "" };
}

export const LoadModelRequest: MessageFns<LoadModelRequest> = {
  encode(message: LoadModelRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.modelId !== "") {
      writer.uint32(10).string(message.modelId);
    }
    if (message.shmemRegionId !== "") {
      writer.uint32(18).string(message.shmemRegionId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LoadModelRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLoadModelRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.modelId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.shmemRegionId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LoadModelRequest {
    return {
      modelId: isSet(object.modelId)
        ? globalThis.String(object.modelId)
        : isSet(object.model_id)
        ? globalThis.String(object.model_id)
        : "",
      shmemRegionId: isSet(object.shmemRegionId)
        ? globalThis.String(object.shmemRegionId)
        : isSet(object.shmem_region_id)
        ? globalThis.String(object.shmem_region_id)
        : "",
    };
  },

  toJSON(message: LoadModelRequest): unknown {
    const obj: any = {};
    if (message.modelId !== "") {
      obj.modelId = message.modelId;
    }
    if (message.shmemRegionId !== "") {
      obj.shmemRegionId = message.shmemRegionId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LoadModelRequest>, I>>(base?: I): LoadModelRequest {
    return LoadModelRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LoadModelRequest>, I>>(object: I): LoadModelRequest {
    const message = createBaseLoadModelRequest();
    message.modelId = object.modelId ?? "";
    message.shmemRegionId = object.shmemRegionId ?? "";
    return message;
  },
};

function createBaseLoadModelResponse(): LoadModelResponse {
  return { success: false, memoryUsageBytes: 0 };
}

export const LoadModelResponse: MessageFns<LoadModelResponse> = {
  encode(message: LoadModelResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.memoryUsageBytes !== 0) {
      writer.uint32(16).uint64(message.memoryUsageBytes);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LoadModelResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLoadModelResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.memoryUsageBytes = longToNumber(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LoadModelResponse {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      memoryUsageBytes: isSet(object.memoryUsageBytes)
        ? globalThis.Number(object.memoryUsageBytes)
        : isSet(object.memory_usage_bytes)
        ? globalThis.Number(object.memory_usage_bytes)
        : 0,
    };
  },

  toJSON(message: LoadModelResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.memoryUsageBytes !== 0) {
      obj.memoryUsageBytes = Math.round(message.memoryUsageBytes);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LoadModelResponse>, I>>(base?: I): LoadModelResponse {
    return LoadModelResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LoadModelResponse>, I>>(object: I): LoadModelResponse {
    const message = createBaseLoadModelResponse();
    message.success = object.success ?? false;
    message.memoryUsageBytes = object.memoryUsageBytes ?? 0;
    return message;
  },
};

function createBaseExecuteJobRequest(): ExecuteJobRequest {
  return { jobId: 0, inputOffset: 0, inputLength: 0, sessionId: new Uint8Array(0) };
}

export const ExecuteJobRequest: MessageFns<ExecuteJobRequest> = {
  encode(message: ExecuteJobRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.jobId !== 0) {
      writer.uint32(8).uint64(message.jobId);
    }
    if (message.inputOffset !== 0) {
      writer.uint32(16).uint64(message.inputOffset);
    }
    if (message.inputLength !== 0) {
      writer.uint32(24).uint64(message.inputLength);
    }
    if (message.sessionId.length !== 0) {
      writer.uint32(34).bytes(message.sessionId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExecuteJobRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExecuteJobRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.jobId = longToNumber(reader.uint64());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.inputOffset = longToNumber(reader.uint64());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.inputLength = longToNumber(reader.uint64());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.sessionId = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExecuteJobRequest {
    return {
      jobId: isSet(object.jobId)
        ? globalThis.Number(object.jobId)
        : isSet(object.job_id)
        ? globalThis.Number(object.job_id)
        : 0,
      inputOffset: isSet(object.inputOffset)
        ? globalThis.Number(object.inputOffset)
        : isSet(object.input_offset)
        ? globalThis.Number(object.input_offset)
        : 0,
      inputLength: isSet(object.inputLength)
        ? globalThis.Number(object.inputLength)
        : isSet(object.input_length)
        ? globalThis.Number(object.input_length)
        : 0,
      sessionId: isSet(object.sessionId)
        ? bytesFromBase64(object.sessionId)
        : isSet(object.session_id)
        ? bytesFromBase64(object.session_id)
        : new Uint8Array(0),
    };
  },

  toJSON(message: ExecuteJobRequest): unknown {
    const obj: any = {};
    if (message.jobId !== 0) {
      obj.jobId = Math.round(message.jobId);
    }
    if (message.inputOffset !== 0) {
      obj.inputOffset = Math.round(message.inputOffset);
    }
    if (message.inputLength !== 0) {
      obj.inputLength = Math.round(message.inputLength);
    }
    if (message.sessionId.length !== 0) {
      obj.sessionId = base64FromBytes(message.sessionId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ExecuteJobRequest>, I>>(base?: I): ExecuteJobRequest {
    return ExecuteJobRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ExecuteJobRequest>, I>>(object: I): ExecuteJobRequest {
    const message = createBaseExecuteJobRequest();
    message.jobId = object.jobId ?? 0;
    message.inputOffset = object.inputOffset ?? 0;
    message.inputLength = object.inputLength ?? 0;
    message.sessionId = object.sessionId ?? new Uint8Array(0);
    return message;
  },
};

function createBaseExecuteJobResponse(): ExecuteJobResponse {
  return { success: false, outputOffset: 0, outputLength: 0, gasUsed: 0, errorMessage: "" };
}

export const ExecuteJobResponse: MessageFns<ExecuteJobResponse> = {
  encode(message: ExecuteJobResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.outputOffset !== 0) {
      writer.uint32(16).uint64(message.outputOffset);
    }
    if (message.outputLength !== 0) {
      writer.uint32(24).uint64(message.outputLength);
    }
    if (message.gasUsed !== 0) {
      writer.uint32(32).uint64(message.gasUsed);
    }
    if (message.errorMessage !== "") {
      writer.uint32(42).string(message.errorMessage);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExecuteJobResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExecuteJobResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.outputOffset = longToNumber(reader.uint64());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.outputLength = longToNumber(reader.uint64());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.gasUsed = longToNumber(reader.uint64());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.errorMessage = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExecuteJobResponse {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      outputOffset: isSet(object.outputOffset)
        ? globalThis.Number(object.outputOffset)
        : isSet(object.output_offset)
        ? globalThis.Number(object.output_offset)
        : 0,
      outputLength: isSet(object.outputLength)
        ? globalThis.Number(object.outputLength)
        : isSet(object.output_length)
        ? globalThis.Number(object.output_length)
        : 0,
      gasUsed: isSet(object.gasUsed)
        ? globalThis.Number(object.gasUsed)
        : isSet(object.gas_used)
        ? globalThis.Number(object.gas_used)
        : 0,
      errorMessage: isSet(object.errorMessage)
        ? globalThis.String(object.errorMessage)
        : isSet(object.error_message)
        ? globalThis.String(object.error_message)
        : "",
    };
  },

  toJSON(message: ExecuteJobResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.outputOffset !== 0) {
      obj.outputOffset = Math.round(message.outputOffset);
    }
    if (message.outputLength !== 0) {
      obj.outputLength = Math.round(message.outputLength);
    }
    if (message.gasUsed !== 0) {
      obj.gasUsed = Math.round(message.gasUsed);
    }
    if (message.errorMessage !== "") {
      obj.errorMessage = message.errorMessage;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ExecuteJobResponse>, I>>(base?: I): ExecuteJobResponse {
    return ExecuteJobResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ExecuteJobResponse>, I>>(object: I): ExecuteJobResponse {
    const message = createBaseExecuteJobResponse();
    message.success = object.success ?? false;
    message.outputOffset = object.outputOffset ?? 0;
    message.outputLength = object.outputLength ?? 0;
    message.gasUsed = object.gasUsed ?? 0;
    message.errorMessage = object.errorMessage ?? "";
    return message;
  },
};

function createBaseHealthCheckRequest(): HealthCheckRequest {
  return {};
}

export const HealthCheckRequest: MessageFns<HealthCheckRequest> = {
  encode(_: HealthCheckRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HealthCheckRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHealthCheckRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): HealthCheckRequest {
    return {};
  },

  toJSON(_: HealthCheckRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<HealthCheckRequest>, I>>(base?: I): HealthCheckRequest {
    return HealthCheckRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HealthCheckRequest>, I>>(_: I): HealthCheckRequest {
    const message = createBaseHealthCheckRequest();
    return message;
  },
};

function createBaseHealthCheckResponse(): HealthCheckResponse {
  return { ready: false, status: "" };
}

export const HealthCheckResponse: MessageFns<HealthCheckResponse> = {
  encode(message: HealthCheckResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ready !== false) {
      writer.uint32(8).bool(message.ready);
    }
    if (message.status !== "") {
      writer.uint32(18).string(message.status);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HealthCheckResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHealthCheckResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.ready = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.status = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HealthCheckResponse {
    return {
      ready: isSet(object.ready) ? globalThis.Boolean(object.ready) : false,
      status: isSet(object.status) ? globalThis.String(object.status) : "",
    };
  },

  toJSON(message: HealthCheckResponse): unknown {
    const obj: any = {};
    if (message.ready !== false) {
      obj.ready = message.ready;
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HealthCheckResponse>, I>>(base?: I): HealthCheckResponse {
    return HealthCheckResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HealthCheckResponse>, I>>(object: I): HealthCheckResponse {
    const message = createBaseHealthCheckResponse();
    message.ready = object.ready ?? false;
    message.status = object.status ?? "";
    return message;
  },
};

function createBaseSecureEgressRequest(): SecureEgressRequest {
  return {
    domain: "",
    path: "",
    method: "",
    body: new Uint8Array(0),
    secretId: "",
    jsonPatchPath: "",
    requiredFinalityTier: 0,
    sealedFinalityProof: new Uint8Array(0),
    sealObject: new Uint8Array(0),
    canonicalCollapseObject: new Uint8Array(0),
  };
}

export const SecureEgressRequest: MessageFns<SecureEgressRequest> = {
  encode(message: SecureEgressRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.domain !== "") {
      writer.uint32(10).string(message.domain);
    }
    if (message.path !== "") {
      writer.uint32(18).string(message.path);
    }
    if (message.method !== "") {
      writer.uint32(26).string(message.method);
    }
    if (message.body.length !== 0) {
      writer.uint32(34).bytes(message.body);
    }
    if (message.secretId !== "") {
      writer.uint32(42).string(message.secretId);
    }
    if (message.jsonPatchPath !== "") {
      writer.uint32(50).string(message.jsonPatchPath);
    }
    if (message.requiredFinalityTier !== 0) {
      writer.uint32(56).uint32(message.requiredFinalityTier);
    }
    if (message.sealedFinalityProof.length !== 0) {
      writer.uint32(66).bytes(message.sealedFinalityProof);
    }
    if (message.sealObject.length !== 0) {
      writer.uint32(74).bytes(message.sealObject);
    }
    if (message.canonicalCollapseObject.length !== 0) {
      writer.uint32(82).bytes(message.canonicalCollapseObject);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SecureEgressRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSecureEgressRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.domain = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.method = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.body = reader.bytes();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.secretId = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.jsonPatchPath = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.requiredFinalityTier = reader.uint32();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.sealedFinalityProof = reader.bytes();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.sealObject = reader.bytes();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.canonicalCollapseObject = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SecureEgressRequest {
    return {
      domain: isSet(object.domain) ? globalThis.String(object.domain) : "",
      path: isSet(object.path) ? globalThis.String(object.path) : "",
      method: isSet(object.method) ? globalThis.String(object.method) : "",
      body: isSet(object.body) ? bytesFromBase64(object.body) : new Uint8Array(0),
      secretId: isSet(object.secretId)
        ? globalThis.String(object.secretId)
        : isSet(object.secret_id)
        ? globalThis.String(object.secret_id)
        : "",
      jsonPatchPath: isSet(object.jsonPatchPath)
        ? globalThis.String(object.jsonPatchPath)
        : isSet(object.json_patch_path)
        ? globalThis.String(object.json_patch_path)
        : "",
      requiredFinalityTier: isSet(object.requiredFinalityTier)
        ? globalThis.Number(object.requiredFinalityTier)
        : isSet(object.required_finality_tier)
        ? globalThis.Number(object.required_finality_tier)
        : 0,
      sealedFinalityProof: isSet(object.sealedFinalityProof)
        ? bytesFromBase64(object.sealedFinalityProof)
        : isSet(object.sealed_finality_proof)
        ? bytesFromBase64(object.sealed_finality_proof)
        : new Uint8Array(0),
      sealObject: isSet(object.sealObject)
        ? bytesFromBase64(object.sealObject)
        : isSet(object.seal_object)
        ? bytesFromBase64(object.seal_object)
        : new Uint8Array(0),
      canonicalCollapseObject: isSet(object.canonicalCollapseObject)
        ? bytesFromBase64(object.canonicalCollapseObject)
        : isSet(object.canonical_collapse_object)
        ? bytesFromBase64(object.canonical_collapse_object)
        : new Uint8Array(0),
    };
  },

  toJSON(message: SecureEgressRequest): unknown {
    const obj: any = {};
    if (message.domain !== "") {
      obj.domain = message.domain;
    }
    if (message.path !== "") {
      obj.path = message.path;
    }
    if (message.method !== "") {
      obj.method = message.method;
    }
    if (message.body.length !== 0) {
      obj.body = base64FromBytes(message.body);
    }
    if (message.secretId !== "") {
      obj.secretId = message.secretId;
    }
    if (message.jsonPatchPath !== "") {
      obj.jsonPatchPath = message.jsonPatchPath;
    }
    if (message.requiredFinalityTier !== 0) {
      obj.requiredFinalityTier = Math.round(message.requiredFinalityTier);
    }
    if (message.sealedFinalityProof.length !== 0) {
      obj.sealedFinalityProof = base64FromBytes(message.sealedFinalityProof);
    }
    if (message.sealObject.length !== 0) {
      obj.sealObject = base64FromBytes(message.sealObject);
    }
    if (message.canonicalCollapseObject.length !== 0) {
      obj.canonicalCollapseObject = base64FromBytes(message.canonicalCollapseObject);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SecureEgressRequest>, I>>(base?: I): SecureEgressRequest {
    return SecureEgressRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SecureEgressRequest>, I>>(object: I): SecureEgressRequest {
    const message = createBaseSecureEgressRequest();
    message.domain = object.domain ?? "";
    message.path = object.path ?? "";
    message.method = object.method ?? "";
    message.body = object.body ?? new Uint8Array(0);
    message.secretId = object.secretId ?? "";
    message.jsonPatchPath = object.jsonPatchPath ?? "";
    message.requiredFinalityTier = object.requiredFinalityTier ?? 0;
    message.sealedFinalityProof = object.sealedFinalityProof ?? new Uint8Array(0);
    message.sealObject = object.sealObject ?? new Uint8Array(0);
    message.canonicalCollapseObject = object.canonicalCollapseObject ?? new Uint8Array(0);
    return message;
  },
};

function createBaseSecureEgressResponse(): SecureEgressResponse {
  return {
    body: new Uint8Array(0),
    certHash: new Uint8Array(0),
    guardianSignature: new Uint8Array(0),
    receipt: new Uint8Array(0),
    finalityTier: 0,
  };
}

export const SecureEgressResponse: MessageFns<SecureEgressResponse> = {
  encode(message: SecureEgressResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.body.length !== 0) {
      writer.uint32(10).bytes(message.body);
    }
    if (message.certHash.length !== 0) {
      writer.uint32(18).bytes(message.certHash);
    }
    if (message.guardianSignature.length !== 0) {
      writer.uint32(26).bytes(message.guardianSignature);
    }
    if (message.receipt.length !== 0) {
      writer.uint32(34).bytes(message.receipt);
    }
    if (message.finalityTier !== 0) {
      writer.uint32(40).uint32(message.finalityTier);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SecureEgressResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSecureEgressResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.body = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.certHash = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.guardianSignature = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.receipt = reader.bytes();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.finalityTier = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SecureEgressResponse {
    return {
      body: isSet(object.body) ? bytesFromBase64(object.body) : new Uint8Array(0),
      certHash: isSet(object.certHash)
        ? bytesFromBase64(object.certHash)
        : isSet(object.cert_hash)
        ? bytesFromBase64(object.cert_hash)
        : new Uint8Array(0),
      guardianSignature: isSet(object.guardianSignature)
        ? bytesFromBase64(object.guardianSignature)
        : isSet(object.guardian_signature)
        ? bytesFromBase64(object.guardian_signature)
        : new Uint8Array(0),
      receipt: isSet(object.receipt) ? bytesFromBase64(object.receipt) : new Uint8Array(0),
      finalityTier: isSet(object.finalityTier)
        ? globalThis.Number(object.finalityTier)
        : isSet(object.finality_tier)
        ? globalThis.Number(object.finality_tier)
        : 0,
    };
  },

  toJSON(message: SecureEgressResponse): unknown {
    const obj: any = {};
    if (message.body.length !== 0) {
      obj.body = base64FromBytes(message.body);
    }
    if (message.certHash.length !== 0) {
      obj.certHash = base64FromBytes(message.certHash);
    }
    if (message.guardianSignature.length !== 0) {
      obj.guardianSignature = base64FromBytes(message.guardianSignature);
    }
    if (message.receipt.length !== 0) {
      obj.receipt = base64FromBytes(message.receipt);
    }
    if (message.finalityTier !== 0) {
      obj.finalityTier = Math.round(message.finalityTier);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SecureEgressResponse>, I>>(base?: I): SecureEgressResponse {
    return SecureEgressResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SecureEgressResponse>, I>>(object: I): SecureEgressResponse {
    const message = createBaseSecureEgressResponse();
    message.body = object.body ?? new Uint8Array(0);
    message.certHash = object.certHash ?? new Uint8Array(0);
    message.guardianSignature = object.guardianSignature ?? new Uint8Array(0);
    message.receipt = object.receipt ?? new Uint8Array(0);
    message.finalityTier = object.finalityTier ?? 0;
    return message;
  },
};

function createBaseSignConsensusRequest(): SignConsensusRequest {
  return {
    payloadHash: new Uint8Array(0),
    subject: new Uint8Array(0),
    expectedCounter: 0,
    expectedTraceHash: new Uint8Array(0),
    measurementRoot: new Uint8Array(0),
    policyHash: new Uint8Array(0),
    manifestHash: new Uint8Array(0),
    height: 0,
    view: 0,
    witnessManifestHash: new Uint8Array(0),
    witnessReassignmentDepth: 0,
    recoveryCapsuleHash: new Uint8Array(0),
    shareCommitmentHash: new Uint8Array(0),
  };
}

export const SignConsensusRequest: MessageFns<SignConsensusRequest> = {
  encode(message: SignConsensusRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.payloadHash.length !== 0) {
      writer.uint32(10).bytes(message.payloadHash);
    }
    if (message.subject.length !== 0) {
      writer.uint32(18).bytes(message.subject);
    }
    if (message.expectedCounter !== 0) {
      writer.uint32(24).uint64(message.expectedCounter);
    }
    if (message.expectedTraceHash.length !== 0) {
      writer.uint32(34).bytes(message.expectedTraceHash);
    }
    if (message.measurementRoot.length !== 0) {
      writer.uint32(42).bytes(message.measurementRoot);
    }
    if (message.policyHash.length !== 0) {
      writer.uint32(50).bytes(message.policyHash);
    }
    if (message.manifestHash.length !== 0) {
      writer.uint32(58).bytes(message.manifestHash);
    }
    if (message.height !== 0) {
      writer.uint32(64).uint64(message.height);
    }
    if (message.view !== 0) {
      writer.uint32(72).uint64(message.view);
    }
    if (message.witnessManifestHash.length !== 0) {
      writer.uint32(82).bytes(message.witnessManifestHash);
    }
    if (message.witnessReassignmentDepth !== 0) {
      writer.uint32(88).uint32(message.witnessReassignmentDepth);
    }
    if (message.recoveryCapsuleHash.length !== 0) {
      writer.uint32(98).bytes(message.recoveryCapsuleHash);
    }
    if (message.shareCommitmentHash.length !== 0) {
      writer.uint32(106).bytes(message.shareCommitmentHash);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SignConsensusRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSignConsensusRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.payloadHash = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.subject = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.expectedCounter = longToNumber(reader.uint64());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.expectedTraceHash = reader.bytes();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.measurementRoot = reader.bytes();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.policyHash = reader.bytes();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.manifestHash = reader.bytes();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.height = longToNumber(reader.uint64());
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.view = longToNumber(reader.uint64());
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.witnessManifestHash = reader.bytes();
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.witnessReassignmentDepth = reader.uint32();
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.recoveryCapsuleHash = reader.bytes();
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.shareCommitmentHash = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SignConsensusRequest {
    return {
      payloadHash: isSet(object.payloadHash)
        ? bytesFromBase64(object.payloadHash)
        : isSet(object.payload_hash)
        ? bytesFromBase64(object.payload_hash)
        : new Uint8Array(0),
      subject: isSet(object.subject) ? bytesFromBase64(object.subject) : new Uint8Array(0),
      expectedCounter: isSet(object.expectedCounter)
        ? globalThis.Number(object.expectedCounter)
        : isSet(object.expected_counter)
        ? globalThis.Number(object.expected_counter)
        : 0,
      expectedTraceHash: isSet(object.expectedTraceHash)
        ? bytesFromBase64(object.expectedTraceHash)
        : isSet(object.expected_trace_hash)
        ? bytesFromBase64(object.expected_trace_hash)
        : new Uint8Array(0),
      measurementRoot: isSet(object.measurementRoot)
        ? bytesFromBase64(object.measurementRoot)
        : isSet(object.measurement_root)
        ? bytesFromBase64(object.measurement_root)
        : new Uint8Array(0),
      policyHash: isSet(object.policyHash)
        ? bytesFromBase64(object.policyHash)
        : isSet(object.policy_hash)
        ? bytesFromBase64(object.policy_hash)
        : new Uint8Array(0),
      manifestHash: isSet(object.manifestHash)
        ? bytesFromBase64(object.manifestHash)
        : isSet(object.manifest_hash)
        ? bytesFromBase64(object.manifest_hash)
        : new Uint8Array(0),
      height: isSet(object.height) ? globalThis.Number(object.height) : 0,
      view: isSet(object.view) ? globalThis.Number(object.view) : 0,
      witnessManifestHash: isSet(object.witnessManifestHash)
        ? bytesFromBase64(object.witnessManifestHash)
        : isSet(object.witness_manifest_hash)
        ? bytesFromBase64(object.witness_manifest_hash)
        : new Uint8Array(0),
      witnessReassignmentDepth: isSet(object.witnessReassignmentDepth)
        ? globalThis.Number(object.witnessReassignmentDepth)
        : isSet(object.witness_reassignment_depth)
        ? globalThis.Number(object.witness_reassignment_depth)
        : 0,
      recoveryCapsuleHash: isSet(object.recoveryCapsuleHash)
        ? bytesFromBase64(object.recoveryCapsuleHash)
        : isSet(object.recovery_capsule_hash)
        ? bytesFromBase64(object.recovery_capsule_hash)
        : new Uint8Array(0),
      shareCommitmentHash: isSet(object.shareCommitmentHash)
        ? bytesFromBase64(object.shareCommitmentHash)
        : isSet(object.share_commitment_hash)
        ? bytesFromBase64(object.share_commitment_hash)
        : new Uint8Array(0),
    };
  },

  toJSON(message: SignConsensusRequest): unknown {
    const obj: any = {};
    if (message.payloadHash.length !== 0) {
      obj.payloadHash = base64FromBytes(message.payloadHash);
    }
    if (message.subject.length !== 0) {
      obj.subject = base64FromBytes(message.subject);
    }
    if (message.expectedCounter !== 0) {
      obj.expectedCounter = Math.round(message.expectedCounter);
    }
    if (message.expectedTraceHash.length !== 0) {
      obj.expectedTraceHash = base64FromBytes(message.expectedTraceHash);
    }
    if (message.measurementRoot.length !== 0) {
      obj.measurementRoot = base64FromBytes(message.measurementRoot);
    }
    if (message.policyHash.length !== 0) {
      obj.policyHash = base64FromBytes(message.policyHash);
    }
    if (message.manifestHash.length !== 0) {
      obj.manifestHash = base64FromBytes(message.manifestHash);
    }
    if (message.height !== 0) {
      obj.height = Math.round(message.height);
    }
    if (message.view !== 0) {
      obj.view = Math.round(message.view);
    }
    if (message.witnessManifestHash.length !== 0) {
      obj.witnessManifestHash = base64FromBytes(message.witnessManifestHash);
    }
    if (message.witnessReassignmentDepth !== 0) {
      obj.witnessReassignmentDepth = Math.round(message.witnessReassignmentDepth);
    }
    if (message.recoveryCapsuleHash.length !== 0) {
      obj.recoveryCapsuleHash = base64FromBytes(message.recoveryCapsuleHash);
    }
    if (message.shareCommitmentHash.length !== 0) {
      obj.shareCommitmentHash = base64FromBytes(message.shareCommitmentHash);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SignConsensusRequest>, I>>(base?: I): SignConsensusRequest {
    return SignConsensusRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SignConsensusRequest>, I>>(object: I): SignConsensusRequest {
    const message = createBaseSignConsensusRequest();
    message.payloadHash = object.payloadHash ?? new Uint8Array(0);
    message.subject = object.subject ?? new Uint8Array(0);
    message.expectedCounter = object.expectedCounter ?? 0;
    message.expectedTraceHash = object.expectedTraceHash ?? new Uint8Array(0);
    message.measurementRoot = object.measurementRoot ?? new Uint8Array(0);
    message.policyHash = object.policyHash ?? new Uint8Array(0);
    message.manifestHash = object.manifestHash ?? new Uint8Array(0);
    message.height = object.height ?? 0;
    message.view = object.view ?? 0;
    message.witnessManifestHash = object.witnessManifestHash ?? new Uint8Array(0);
    message.witnessReassignmentDepth = object.witnessReassignmentDepth ?? 0;
    message.recoveryCapsuleHash = object.recoveryCapsuleHash ?? new Uint8Array(0);
    message.shareCommitmentHash = object.shareCommitmentHash ?? new Uint8Array(0);
    return message;
  },
};

function createBaseSignConsensusResponse(): SignConsensusResponse {
  return {
    signature: new Uint8Array(0),
    counter: 0,
    traceHash: new Uint8Array(0),
    guardianCertificate: new Uint8Array(0),
  };
}

export const SignConsensusResponse: MessageFns<SignConsensusResponse> = {
  encode(message: SignConsensusResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.signature.length !== 0) {
      writer.uint32(10).bytes(message.signature);
    }
    if (message.counter !== 0) {
      writer.uint32(16).uint64(message.counter);
    }
    if (message.traceHash.length !== 0) {
      writer.uint32(26).bytes(message.traceHash);
    }
    if (message.guardianCertificate.length !== 0) {
      writer.uint32(34).bytes(message.guardianCertificate);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SignConsensusResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSignConsensusResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.signature = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.counter = longToNumber(reader.uint64());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.traceHash = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.guardianCertificate = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SignConsensusResponse {
    return {
      signature: isSet(object.signature) ? bytesFromBase64(object.signature) : new Uint8Array(0),
      counter: isSet(object.counter) ? globalThis.Number(object.counter) : 0,
      traceHash: isSet(object.traceHash)
        ? bytesFromBase64(object.traceHash)
        : isSet(object.trace_hash)
        ? bytesFromBase64(object.trace_hash)
        : new Uint8Array(0),
      guardianCertificate: isSet(object.guardianCertificate)
        ? bytesFromBase64(object.guardianCertificate)
        : isSet(object.guardian_certificate)
        ? bytesFromBase64(object.guardian_certificate)
        : new Uint8Array(0),
    };
  },

  toJSON(message: SignConsensusResponse): unknown {
    const obj: any = {};
    if (message.signature.length !== 0) {
      obj.signature = base64FromBytes(message.signature);
    }
    if (message.counter !== 0) {
      obj.counter = Math.round(message.counter);
    }
    if (message.traceHash.length !== 0) {
      obj.traceHash = base64FromBytes(message.traceHash);
    }
    if (message.guardianCertificate.length !== 0) {
      obj.guardianCertificate = base64FromBytes(message.guardianCertificate);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SignConsensusResponse>, I>>(base?: I): SignConsensusResponse {
    return SignConsensusResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SignConsensusResponse>, I>>(object: I): SignConsensusResponse {
    const message = createBaseSignConsensusResponse();
    message.signature = object.signature ?? new Uint8Array(0);
    message.counter = object.counter ?? 0;
    message.traceHash = object.traceHash ?? new Uint8Array(0);
    message.guardianCertificate = object.guardianCertificate ?? new Uint8Array(0);
    return message;
  },
};

function createBaseSealConsensusRequest(): SealConsensusRequest {
  return {
    payloadHash: new Uint8Array(0),
    subject: new Uint8Array(0),
    expectedCounter: 0,
    expectedTraceHash: new Uint8Array(0),
    measurementRoot: new Uint8Array(0),
    policyHash: new Uint8Array(0),
    manifestHash: new Uint8Array(0),
    height: 0,
    view: 0,
    witnessManifestHashes: [],
    witnessReassignmentDepth: 0,
    observerPlanEntries: [],
    asymptotePolicy: new Uint8Array(0),
    witnessRecoveryBindings: [],
    witnessRecoveryShareEnvelopes: [],
  };
}

export const SealConsensusRequest: MessageFns<SealConsensusRequest> = {
  encode(message: SealConsensusRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.payloadHash.length !== 0) {
      writer.uint32(10).bytes(message.payloadHash);
    }
    if (message.subject.length !== 0) {
      writer.uint32(18).bytes(message.subject);
    }
    if (message.expectedCounter !== 0) {
      writer.uint32(24).uint64(message.expectedCounter);
    }
    if (message.expectedTraceHash.length !== 0) {
      writer.uint32(34).bytes(message.expectedTraceHash);
    }
    if (message.measurementRoot.length !== 0) {
      writer.uint32(42).bytes(message.measurementRoot);
    }
    if (message.policyHash.length !== 0) {
      writer.uint32(50).bytes(message.policyHash);
    }
    if (message.manifestHash.length !== 0) {
      writer.uint32(58).bytes(message.manifestHash);
    }
    if (message.height !== 0) {
      writer.uint32(64).uint64(message.height);
    }
    if (message.view !== 0) {
      writer.uint32(72).uint64(message.view);
    }
    for (const v of message.witnessManifestHashes) {
      writer.uint32(82).bytes(v!);
    }
    if (message.witnessReassignmentDepth !== 0) {
      writer.uint32(88).uint32(message.witnessReassignmentDepth);
    }
    for (const v of message.observerPlanEntries) {
      writer.uint32(98).bytes(v!);
    }
    if (message.asymptotePolicy.length !== 0) {
      writer.uint32(106).bytes(message.asymptotePolicy);
    }
    for (const v of message.witnessRecoveryBindings) {
      writer.uint32(114).bytes(v!);
    }
    for (const v of message.witnessRecoveryShareEnvelopes) {
      writer.uint32(122).bytes(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SealConsensusRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSealConsensusRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.payloadHash = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.subject = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.expectedCounter = longToNumber(reader.uint64());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.expectedTraceHash = reader.bytes();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.measurementRoot = reader.bytes();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.policyHash = reader.bytes();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.manifestHash = reader.bytes();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.height = longToNumber(reader.uint64());
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.view = longToNumber(reader.uint64());
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.witnessManifestHashes.push(reader.bytes());
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.witnessReassignmentDepth = reader.uint32();
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.observerPlanEntries.push(reader.bytes());
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.asymptotePolicy = reader.bytes();
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.witnessRecoveryBindings.push(reader.bytes());
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }

          message.witnessRecoveryShareEnvelopes.push(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SealConsensusRequest {
    return {
      payloadHash: isSet(object.payloadHash)
        ? bytesFromBase64(object.payloadHash)
        : isSet(object.payload_hash)
        ? bytesFromBase64(object.payload_hash)
        : new Uint8Array(0),
      subject: isSet(object.subject) ? bytesFromBase64(object.subject) : new Uint8Array(0),
      expectedCounter: isSet(object.expectedCounter)
        ? globalThis.Number(object.expectedCounter)
        : isSet(object.expected_counter)
        ? globalThis.Number(object.expected_counter)
        : 0,
      expectedTraceHash: isSet(object.expectedTraceHash)
        ? bytesFromBase64(object.expectedTraceHash)
        : isSet(object.expected_trace_hash)
        ? bytesFromBase64(object.expected_trace_hash)
        : new Uint8Array(0),
      measurementRoot: isSet(object.measurementRoot)
        ? bytesFromBase64(object.measurementRoot)
        : isSet(object.measurement_root)
        ? bytesFromBase64(object.measurement_root)
        : new Uint8Array(0),
      policyHash: isSet(object.policyHash)
        ? bytesFromBase64(object.policyHash)
        : isSet(object.policy_hash)
        ? bytesFromBase64(object.policy_hash)
        : new Uint8Array(0),
      manifestHash: isSet(object.manifestHash)
        ? bytesFromBase64(object.manifestHash)
        : isSet(object.manifest_hash)
        ? bytesFromBase64(object.manifest_hash)
        : new Uint8Array(0),
      height: isSet(object.height) ? globalThis.Number(object.height) : 0,
      view: isSet(object.view) ? globalThis.Number(object.view) : 0,
      witnessManifestHashes: globalThis.Array.isArray(object?.witnessManifestHashes)
        ? object.witnessManifestHashes.map((e: any) => bytesFromBase64(e))
        : globalThis.Array.isArray(object?.witness_manifest_hashes)
        ? object.witness_manifest_hashes.map((e: any) => bytesFromBase64(e))
        : [],
      witnessReassignmentDepth: isSet(object.witnessReassignmentDepth)
        ? globalThis.Number(object.witnessReassignmentDepth)
        : isSet(object.witness_reassignment_depth)
        ? globalThis.Number(object.witness_reassignment_depth)
        : 0,
      observerPlanEntries: globalThis.Array.isArray(object?.observerPlanEntries)
        ? object.observerPlanEntries.map((e: any) => bytesFromBase64(e))
        : globalThis.Array.isArray(object?.observer_plan_entries)
        ? object.observer_plan_entries.map((e: any) => bytesFromBase64(e))
        : [],
      asymptotePolicy: isSet(object.asymptotePolicy)
        ? bytesFromBase64(object.asymptotePolicy)
        : isSet(object.asymptote_policy)
        ? bytesFromBase64(object.asymptote_policy)
        : new Uint8Array(0),
      witnessRecoveryBindings: globalThis.Array.isArray(object?.witnessRecoveryBindings)
        ? object.witnessRecoveryBindings.map((e: any) => bytesFromBase64(e))
        : globalThis.Array.isArray(object?.witness_recovery_bindings)
        ? object.witness_recovery_bindings.map((e: any) => bytesFromBase64(e))
        : [],
      witnessRecoveryShareEnvelopes: globalThis.Array.isArray(object?.witnessRecoveryShareEnvelopes)
        ? object.witnessRecoveryShareEnvelopes.map((e: any) => bytesFromBase64(e))
        : globalThis.Array.isArray(object?.witness_recovery_share_envelopes)
        ? object.witness_recovery_share_envelopes.map((e: any) => bytesFromBase64(e))
        : [],
    };
  },

  toJSON(message: SealConsensusRequest): unknown {
    const obj: any = {};
    if (message.payloadHash.length !== 0) {
      obj.payloadHash = base64FromBytes(message.payloadHash);
    }
    if (message.subject.length !== 0) {
      obj.subject = base64FromBytes(message.subject);
    }
    if (message.expectedCounter !== 0) {
      obj.expectedCounter = Math.round(message.expectedCounter);
    }
    if (message.expectedTraceHash.length !== 0) {
      obj.expectedTraceHash = base64FromBytes(message.expectedTraceHash);
    }
    if (message.measurementRoot.length !== 0) {
      obj.measurementRoot = base64FromBytes(message.measurementRoot);
    }
    if (message.policyHash.length !== 0) {
      obj.policyHash = base64FromBytes(message.policyHash);
    }
    if (message.manifestHash.length !== 0) {
      obj.manifestHash = base64FromBytes(message.manifestHash);
    }
    if (message.height !== 0) {
      obj.height = Math.round(message.height);
    }
    if (message.view !== 0) {
      obj.view = Math.round(message.view);
    }
    if (message.witnessManifestHashes?.length) {
      obj.witnessManifestHashes = message.witnessManifestHashes.map((e) => base64FromBytes(e));
    }
    if (message.witnessReassignmentDepth !== 0) {
      obj.witnessReassignmentDepth = Math.round(message.witnessReassignmentDepth);
    }
    if (message.observerPlanEntries?.length) {
      obj.observerPlanEntries = message.observerPlanEntries.map((e) => base64FromBytes(e));
    }
    if (message.asymptotePolicy.length !== 0) {
      obj.asymptotePolicy = base64FromBytes(message.asymptotePolicy);
    }
    if (message.witnessRecoveryBindings?.length) {
      obj.witnessRecoveryBindings = message.witnessRecoveryBindings.map((e) => base64FromBytes(e));
    }
    if (message.witnessRecoveryShareEnvelopes?.length) {
      obj.witnessRecoveryShareEnvelopes = message.witnessRecoveryShareEnvelopes.map((e) => base64FromBytes(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SealConsensusRequest>, I>>(base?: I): SealConsensusRequest {
    return SealConsensusRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SealConsensusRequest>, I>>(object: I): SealConsensusRequest {
    const message = createBaseSealConsensusRequest();
    message.payloadHash = object.payloadHash ?? new Uint8Array(0);
    message.subject = object.subject ?? new Uint8Array(0);
    message.expectedCounter = object.expectedCounter ?? 0;
    message.expectedTraceHash = object.expectedTraceHash ?? new Uint8Array(0);
    message.measurementRoot = object.measurementRoot ?? new Uint8Array(0);
    message.policyHash = object.policyHash ?? new Uint8Array(0);
    message.manifestHash = object.manifestHash ?? new Uint8Array(0);
    message.height = object.height ?? 0;
    message.view = object.view ?? 0;
    message.witnessManifestHashes = object.witnessManifestHashes?.map((e) => e) || [];
    message.witnessReassignmentDepth = object.witnessReassignmentDepth ?? 0;
    message.observerPlanEntries = object.observerPlanEntries?.map((e) => e) || [];
    message.asymptotePolicy = object.asymptotePolicy ?? new Uint8Array(0);
    message.witnessRecoveryBindings = object.witnessRecoveryBindings?.map((e) => e) || [];
    message.witnessRecoveryShareEnvelopes = object.witnessRecoveryShareEnvelopes?.map((e) => e) || [];
    return message;
  },
};

function createBaseSealConsensusResponse(): SealConsensusResponse {
  return {
    guardianCertificate: new Uint8Array(0),
    sealedFinalityProof: new Uint8Array(0),
    counter: 0,
    traceHash: new Uint8Array(0),
  };
}

export const SealConsensusResponse: MessageFns<SealConsensusResponse> = {
  encode(message: SealConsensusResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.guardianCertificate.length !== 0) {
      writer.uint32(10).bytes(message.guardianCertificate);
    }
    if (message.sealedFinalityProof.length !== 0) {
      writer.uint32(18).bytes(message.sealedFinalityProof);
    }
    if (message.counter !== 0) {
      writer.uint32(24).uint64(message.counter);
    }
    if (message.traceHash.length !== 0) {
      writer.uint32(34).bytes(message.traceHash);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SealConsensusResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSealConsensusResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.guardianCertificate = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sealedFinalityProof = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.counter = longToNumber(reader.uint64());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.traceHash = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SealConsensusResponse {
    return {
      guardianCertificate: isSet(object.guardianCertificate)
        ? bytesFromBase64(object.guardianCertificate)
        : isSet(object.guardian_certificate)
        ? bytesFromBase64(object.guardian_certificate)
        : new Uint8Array(0),
      sealedFinalityProof: isSet(object.sealedFinalityProof)
        ? bytesFromBase64(object.sealedFinalityProof)
        : isSet(object.sealed_finality_proof)
        ? bytesFromBase64(object.sealed_finality_proof)
        : new Uint8Array(0),
      counter: isSet(object.counter) ? globalThis.Number(object.counter) : 0,
      traceHash: isSet(object.traceHash)
        ? bytesFromBase64(object.traceHash)
        : isSet(object.trace_hash)
        ? bytesFromBase64(object.trace_hash)
        : new Uint8Array(0),
    };
  },

  toJSON(message: SealConsensusResponse): unknown {
    const obj: any = {};
    if (message.guardianCertificate.length !== 0) {
      obj.guardianCertificate = base64FromBytes(message.guardianCertificate);
    }
    if (message.sealedFinalityProof.length !== 0) {
      obj.sealedFinalityProof = base64FromBytes(message.sealedFinalityProof);
    }
    if (message.counter !== 0) {
      obj.counter = Math.round(message.counter);
    }
    if (message.traceHash.length !== 0) {
      obj.traceHash = base64FromBytes(message.traceHash);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SealConsensusResponse>, I>>(base?: I): SealConsensusResponse {
    return SealConsensusResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SealConsensusResponse>, I>>(object: I): SealConsensusResponse {
    const message = createBaseSealConsensusResponse();
    message.guardianCertificate = object.guardianCertificate ?? new Uint8Array(0);
    message.sealedFinalityProof = object.sealedFinalityProof ?? new Uint8Array(0);
    message.counter = object.counter ?? 0;
    message.traceHash = object.traceHash ?? new Uint8Array(0);
    return message;
  },
};

function createBaseObserveAsymptoteRequest(): ObserveAsymptoteRequest {
  return { observationRequest: new Uint8Array(0), manifestHash: new Uint8Array(0) };
}

export const ObserveAsymptoteRequest: MessageFns<ObserveAsymptoteRequest> = {
  encode(message: ObserveAsymptoteRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.observationRequest.length !== 0) {
      writer.uint32(10).bytes(message.observationRequest);
    }
    if (message.manifestHash.length !== 0) {
      writer.uint32(18).bytes(message.manifestHash);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ObserveAsymptoteRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseObserveAsymptoteRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.observationRequest = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.manifestHash = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ObserveAsymptoteRequest {
    return {
      observationRequest: isSet(object.observationRequest)
        ? bytesFromBase64(object.observationRequest)
        : isSet(object.observation_request)
        ? bytesFromBase64(object.observation_request)
        : new Uint8Array(0),
      manifestHash: isSet(object.manifestHash)
        ? bytesFromBase64(object.manifestHash)
        : isSet(object.manifest_hash)
        ? bytesFromBase64(object.manifest_hash)
        : new Uint8Array(0),
    };
  },

  toJSON(message: ObserveAsymptoteRequest): unknown {
    const obj: any = {};
    if (message.observationRequest.length !== 0) {
      obj.observationRequest = base64FromBytes(message.observationRequest);
    }
    if (message.manifestHash.length !== 0) {
      obj.manifestHash = base64FromBytes(message.manifestHash);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ObserveAsymptoteRequest>, I>>(base?: I): ObserveAsymptoteRequest {
    return ObserveAsymptoteRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ObserveAsymptoteRequest>, I>>(object: I): ObserveAsymptoteRequest {
    const message = createBaseObserveAsymptoteRequest();
    message.observationRequest = object.observationRequest ?? new Uint8Array(0);
    message.manifestHash = object.manifestHash ?? new Uint8Array(0);
    return message;
  },
};

function createBaseObserveAsymptoteResponse(): ObserveAsymptoteResponse {
  return { observation: new Uint8Array(0) };
}

export const ObserveAsymptoteResponse: MessageFns<ObserveAsymptoteResponse> = {
  encode(message: ObserveAsymptoteResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.observation.length !== 0) {
      writer.uint32(10).bytes(message.observation);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ObserveAsymptoteResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseObserveAsymptoteResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.observation = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ObserveAsymptoteResponse {
    return { observation: isSet(object.observation) ? bytesFromBase64(object.observation) : new Uint8Array(0) };
  },

  toJSON(message: ObserveAsymptoteResponse): unknown {
    const obj: any = {};
    if (message.observation.length !== 0) {
      obj.observation = base64FromBytes(message.observation);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ObserveAsymptoteResponse>, I>>(base?: I): ObserveAsymptoteResponse {
    return ObserveAsymptoteResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ObserveAsymptoteResponse>, I>>(object: I): ObserveAsymptoteResponse {
    const message = createBaseObserveAsymptoteResponse();
    message.observation = object.observation ?? new Uint8Array(0);
    return message;
  },
};

function createBaseSignCommitteeDecisionRequest(): SignCommitteeDecisionRequest {
  return { decision: new Uint8Array(0), manifestHash: new Uint8Array(0), height: 0, view: 0 };
}

export const SignCommitteeDecisionRequest: MessageFns<SignCommitteeDecisionRequest> = {
  encode(message: SignCommitteeDecisionRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.decision.length !== 0) {
      writer.uint32(10).bytes(message.decision);
    }
    if (message.manifestHash.length !== 0) {
      writer.uint32(18).bytes(message.manifestHash);
    }
    if (message.height !== 0) {
      writer.uint32(24).uint64(message.height);
    }
    if (message.view !== 0) {
      writer.uint32(32).uint64(message.view);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SignCommitteeDecisionRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSignCommitteeDecisionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.decision = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.manifestHash = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.height = longToNumber(reader.uint64());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.view = longToNumber(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SignCommitteeDecisionRequest {
    return {
      decision: isSet(object.decision) ? bytesFromBase64(object.decision) : new Uint8Array(0),
      manifestHash: isSet(object.manifestHash)
        ? bytesFromBase64(object.manifestHash)
        : isSet(object.manifest_hash)
        ? bytesFromBase64(object.manifest_hash)
        : new Uint8Array(0),
      height: isSet(object.height) ? globalThis.Number(object.height) : 0,
      view: isSet(object.view) ? globalThis.Number(object.view) : 0,
    };
  },

  toJSON(message: SignCommitteeDecisionRequest): unknown {
    const obj: any = {};
    if (message.decision.length !== 0) {
      obj.decision = base64FromBytes(message.decision);
    }
    if (message.manifestHash.length !== 0) {
      obj.manifestHash = base64FromBytes(message.manifestHash);
    }
    if (message.height !== 0) {
      obj.height = Math.round(message.height);
    }
    if (message.view !== 0) {
      obj.view = Math.round(message.view);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SignCommitteeDecisionRequest>, I>>(base?: I): SignCommitteeDecisionRequest {
    return SignCommitteeDecisionRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SignCommitteeDecisionRequest>, I>>(object: I): SignCommitteeDecisionRequest {
    const message = createBaseSignCommitteeDecisionRequest();
    message.decision = object.decision ?? new Uint8Array(0);
    message.manifestHash = object.manifestHash ?? new Uint8Array(0);
    message.height = object.height ?? 0;
    message.view = object.view ?? 0;
    return message;
  },
};

function createBaseGuardianMemberSignature(): GuardianMemberSignature {
  return { memberIndex: 0, signature: new Uint8Array(0) };
}

export const GuardianMemberSignature: MessageFns<GuardianMemberSignature> = {
  encode(message: GuardianMemberSignature, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.memberIndex !== 0) {
      writer.uint32(8).uint32(message.memberIndex);
    }
    if (message.signature.length !== 0) {
      writer.uint32(18).bytes(message.signature);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GuardianMemberSignature {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGuardianMemberSignature();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.memberIndex = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.signature = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GuardianMemberSignature {
    return {
      memberIndex: isSet(object.memberIndex)
        ? globalThis.Number(object.memberIndex)
        : isSet(object.member_index)
        ? globalThis.Number(object.member_index)
        : 0,
      signature: isSet(object.signature) ? bytesFromBase64(object.signature) : new Uint8Array(0),
    };
  },

  toJSON(message: GuardianMemberSignature): unknown {
    const obj: any = {};
    if (message.memberIndex !== 0) {
      obj.memberIndex = Math.round(message.memberIndex);
    }
    if (message.signature.length !== 0) {
      obj.signature = base64FromBytes(message.signature);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GuardianMemberSignature>, I>>(base?: I): GuardianMemberSignature {
    return GuardianMemberSignature.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GuardianMemberSignature>, I>>(object: I): GuardianMemberSignature {
    const message = createBaseGuardianMemberSignature();
    message.memberIndex = object.memberIndex ?? 0;
    message.signature = object.signature ?? new Uint8Array(0);
    return message;
  },
};

function createBaseSignCommitteeDecisionResponse(): SignCommitteeDecisionResponse {
  return { manifestHash: new Uint8Array(0), decisionHash: new Uint8Array(0), partialSignatures: [] };
}

export const SignCommitteeDecisionResponse: MessageFns<SignCommitteeDecisionResponse> = {
  encode(message: SignCommitteeDecisionResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.manifestHash.length !== 0) {
      writer.uint32(10).bytes(message.manifestHash);
    }
    if (message.decisionHash.length !== 0) {
      writer.uint32(18).bytes(message.decisionHash);
    }
    for (const v of message.partialSignatures) {
      GuardianMemberSignature.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SignCommitteeDecisionResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSignCommitteeDecisionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.manifestHash = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.decisionHash = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.partialSignatures.push(GuardianMemberSignature.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SignCommitteeDecisionResponse {
    return {
      manifestHash: isSet(object.manifestHash)
        ? bytesFromBase64(object.manifestHash)
        : isSet(object.manifest_hash)
        ? bytesFromBase64(object.manifest_hash)
        : new Uint8Array(0),
      decisionHash: isSet(object.decisionHash)
        ? bytesFromBase64(object.decisionHash)
        : isSet(object.decision_hash)
        ? bytesFromBase64(object.decision_hash)
        : new Uint8Array(0),
      partialSignatures: globalThis.Array.isArray(object?.partialSignatures)
        ? object.partialSignatures.map((e: any) => GuardianMemberSignature.fromJSON(e))
        : globalThis.Array.isArray(object?.partial_signatures)
        ? object.partial_signatures.map((e: any) => GuardianMemberSignature.fromJSON(e))
        : [],
    };
  },

  toJSON(message: SignCommitteeDecisionResponse): unknown {
    const obj: any = {};
    if (message.manifestHash.length !== 0) {
      obj.manifestHash = base64FromBytes(message.manifestHash);
    }
    if (message.decisionHash.length !== 0) {
      obj.decisionHash = base64FromBytes(message.decisionHash);
    }
    if (message.partialSignatures?.length) {
      obj.partialSignatures = message.partialSignatures.map((e) => GuardianMemberSignature.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SignCommitteeDecisionResponse>, I>>(base?: I): SignCommitteeDecisionResponse {
    return SignCommitteeDecisionResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SignCommitteeDecisionResponse>, I>>(
    object: I,
  ): SignCommitteeDecisionResponse {
    const message = createBaseSignCommitteeDecisionResponse();
    message.manifestHash = object.manifestHash ?? new Uint8Array(0);
    message.decisionHash = object.decisionHash ?? new Uint8Array(0);
    message.partialSignatures = object.partialSignatures?.map((e) => GuardianMemberSignature.fromPartial(e)) || [];
    return message;
  },
};

function createBaseSignWitnessStatementRequest(): SignWitnessStatementRequest {
  return { statement: new Uint8Array(0), manifestHash: new Uint8Array(0), recoveryShareEnvelope: new Uint8Array(0) };
}

export const SignWitnessStatementRequest: MessageFns<SignWitnessStatementRequest> = {
  encode(message: SignWitnessStatementRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.statement.length !== 0) {
      writer.uint32(10).bytes(message.statement);
    }
    if (message.manifestHash.length !== 0) {
      writer.uint32(18).bytes(message.manifestHash);
    }
    if (message.recoveryShareEnvelope.length !== 0) {
      writer.uint32(26).bytes(message.recoveryShareEnvelope);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SignWitnessStatementRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSignWitnessStatementRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.statement = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.manifestHash = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.recoveryShareEnvelope = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SignWitnessStatementRequest {
    return {
      statement: isSet(object.statement) ? bytesFromBase64(object.statement) : new Uint8Array(0),
      manifestHash: isSet(object.manifestHash)
        ? bytesFromBase64(object.manifestHash)
        : isSet(object.manifest_hash)
        ? bytesFromBase64(object.manifest_hash)
        : new Uint8Array(0),
      recoveryShareEnvelope: isSet(object.recoveryShareEnvelope)
        ? bytesFromBase64(object.recoveryShareEnvelope)
        : isSet(object.recovery_share_envelope)
        ? bytesFromBase64(object.recovery_share_envelope)
        : new Uint8Array(0),
    };
  },

  toJSON(message: SignWitnessStatementRequest): unknown {
    const obj: any = {};
    if (message.statement.length !== 0) {
      obj.statement = base64FromBytes(message.statement);
    }
    if (message.manifestHash.length !== 0) {
      obj.manifestHash = base64FromBytes(message.manifestHash);
    }
    if (message.recoveryShareEnvelope.length !== 0) {
      obj.recoveryShareEnvelope = base64FromBytes(message.recoveryShareEnvelope);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SignWitnessStatementRequest>, I>>(base?: I): SignWitnessStatementRequest {
    return SignWitnessStatementRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SignWitnessStatementRequest>, I>>(object: I): SignWitnessStatementRequest {
    const message = createBaseSignWitnessStatementRequest();
    message.statement = object.statement ?? new Uint8Array(0);
    message.manifestHash = object.manifestHash ?? new Uint8Array(0);
    message.recoveryShareEnvelope = object.recoveryShareEnvelope ?? new Uint8Array(0);
    return message;
  },
};

function createBaseSignWitnessStatementResponse(): SignWitnessStatementResponse {
  return { manifestHash: new Uint8Array(0), statementHash: new Uint8Array(0), partialSignatures: [] };
}

export const SignWitnessStatementResponse: MessageFns<SignWitnessStatementResponse> = {
  encode(message: SignWitnessStatementResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.manifestHash.length !== 0) {
      writer.uint32(10).bytes(message.manifestHash);
    }
    if (message.statementHash.length !== 0) {
      writer.uint32(18).bytes(message.statementHash);
    }
    for (const v of message.partialSignatures) {
      GuardianMemberSignature.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SignWitnessStatementResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSignWitnessStatementResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.manifestHash = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.statementHash = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.partialSignatures.push(GuardianMemberSignature.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SignWitnessStatementResponse {
    return {
      manifestHash: isSet(object.manifestHash)
        ? bytesFromBase64(object.manifestHash)
        : isSet(object.manifest_hash)
        ? bytesFromBase64(object.manifest_hash)
        : new Uint8Array(0),
      statementHash: isSet(object.statementHash)
        ? bytesFromBase64(object.statementHash)
        : isSet(object.statement_hash)
        ? bytesFromBase64(object.statement_hash)
        : new Uint8Array(0),
      partialSignatures: globalThis.Array.isArray(object?.partialSignatures)
        ? object.partialSignatures.map((e: any) => GuardianMemberSignature.fromJSON(e))
        : globalThis.Array.isArray(object?.partial_signatures)
        ? object.partial_signatures.map((e: any) => GuardianMemberSignature.fromJSON(e))
        : [],
    };
  },

  toJSON(message: SignWitnessStatementResponse): unknown {
    const obj: any = {};
    if (message.manifestHash.length !== 0) {
      obj.manifestHash = base64FromBytes(message.manifestHash);
    }
    if (message.statementHash.length !== 0) {
      obj.statementHash = base64FromBytes(message.statementHash);
    }
    if (message.partialSignatures?.length) {
      obj.partialSignatures = message.partialSignatures.map((e) => GuardianMemberSignature.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SignWitnessStatementResponse>, I>>(base?: I): SignWitnessStatementResponse {
    return SignWitnessStatementResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SignWitnessStatementResponse>, I>>(object: I): SignWitnessStatementResponse {
    const message = createBaseSignWitnessStatementResponse();
    message.manifestHash = object.manifestHash ?? new Uint8Array(0);
    message.statementHash = object.statementHash ?? new Uint8Array(0);
    message.partialSignatures = object.partialSignatures?.map((e) => GuardianMemberSignature.fromPartial(e)) || [];
    return message;
  },
};

function createBaseLoadAssignedRecoveryShareRequest(): LoadAssignedRecoveryShareRequest {
  return {
    height: 0,
    manifestHash: new Uint8Array(0),
    recoveryCapsuleHash: new Uint8Array(0),
    shareCommitmentHash: new Uint8Array(0),
  };
}

export const LoadAssignedRecoveryShareRequest: MessageFns<LoadAssignedRecoveryShareRequest> = {
  encode(message: LoadAssignedRecoveryShareRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.height !== 0) {
      writer.uint32(8).uint64(message.height);
    }
    if (message.manifestHash.length !== 0) {
      writer.uint32(18).bytes(message.manifestHash);
    }
    if (message.recoveryCapsuleHash.length !== 0) {
      writer.uint32(26).bytes(message.recoveryCapsuleHash);
    }
    if (message.shareCommitmentHash.length !== 0) {
      writer.uint32(34).bytes(message.shareCommitmentHash);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LoadAssignedRecoveryShareRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLoadAssignedRecoveryShareRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.height = longToNumber(reader.uint64());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.manifestHash = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.recoveryCapsuleHash = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.shareCommitmentHash = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LoadAssignedRecoveryShareRequest {
    return {
      height: isSet(object.height) ? globalThis.Number(object.height) : 0,
      manifestHash: isSet(object.manifestHash)
        ? bytesFromBase64(object.manifestHash)
        : isSet(object.manifest_hash)
        ? bytesFromBase64(object.manifest_hash)
        : new Uint8Array(0),
      recoveryCapsuleHash: isSet(object.recoveryCapsuleHash)
        ? bytesFromBase64(object.recoveryCapsuleHash)
        : isSet(object.recovery_capsule_hash)
        ? bytesFromBase64(object.recovery_capsule_hash)
        : new Uint8Array(0),
      shareCommitmentHash: isSet(object.shareCommitmentHash)
        ? bytesFromBase64(object.shareCommitmentHash)
        : isSet(object.share_commitment_hash)
        ? bytesFromBase64(object.share_commitment_hash)
        : new Uint8Array(0),
    };
  },

  toJSON(message: LoadAssignedRecoveryShareRequest): unknown {
    const obj: any = {};
    if (message.height !== 0) {
      obj.height = Math.round(message.height);
    }
    if (message.manifestHash.length !== 0) {
      obj.manifestHash = base64FromBytes(message.manifestHash);
    }
    if (message.recoveryCapsuleHash.length !== 0) {
      obj.recoveryCapsuleHash = base64FromBytes(message.recoveryCapsuleHash);
    }
    if (message.shareCommitmentHash.length !== 0) {
      obj.shareCommitmentHash = base64FromBytes(message.shareCommitmentHash);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LoadAssignedRecoveryShareRequest>, I>>(
    base?: I,
  ): LoadAssignedRecoveryShareRequest {
    return LoadAssignedRecoveryShareRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LoadAssignedRecoveryShareRequest>, I>>(
    object: I,
  ): LoadAssignedRecoveryShareRequest {
    const message = createBaseLoadAssignedRecoveryShareRequest();
    message.height = object.height ?? 0;
    message.manifestHash = object.manifestHash ?? new Uint8Array(0);
    message.recoveryCapsuleHash = object.recoveryCapsuleHash ?? new Uint8Array(0);
    message.shareCommitmentHash = object.shareCommitmentHash ?? new Uint8Array(0);
    return message;
  },
};

function createBaseLoadAssignedRecoveryShareResponse(): LoadAssignedRecoveryShareResponse {
  return { recoveryShareMaterial: new Uint8Array(0) };
}

export const LoadAssignedRecoveryShareResponse: MessageFns<LoadAssignedRecoveryShareResponse> = {
  encode(message: LoadAssignedRecoveryShareResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.recoveryShareMaterial.length !== 0) {
      writer.uint32(10).bytes(message.recoveryShareMaterial);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LoadAssignedRecoveryShareResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLoadAssignedRecoveryShareResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.recoveryShareMaterial = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LoadAssignedRecoveryShareResponse {
    return {
      recoveryShareMaterial: isSet(object.recoveryShareMaterial)
        ? bytesFromBase64(object.recoveryShareMaterial)
        : isSet(object.recovery_share_material)
        ? bytesFromBase64(object.recovery_share_material)
        : new Uint8Array(0),
    };
  },

  toJSON(message: LoadAssignedRecoveryShareResponse): unknown {
    const obj: any = {};
    if (message.recoveryShareMaterial.length !== 0) {
      obj.recoveryShareMaterial = base64FromBytes(message.recoveryShareMaterial);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LoadAssignedRecoveryShareResponse>, I>>(
    base?: I,
  ): LoadAssignedRecoveryShareResponse {
    return LoadAssignedRecoveryShareResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LoadAssignedRecoveryShareResponse>, I>>(
    object: I,
  ): LoadAssignedRecoveryShareResponse {
    const message = createBaseLoadAssignedRecoveryShareResponse();
    message.recoveryShareMaterial = object.recoveryShareMaterial ?? new Uint8Array(0);
    return message;
  },
};

function createBaseReportWitnessFaultRequest(): ReportWitnessFaultRequest {
  return { evidence: new Uint8Array(0) };
}

export const ReportWitnessFaultRequest: MessageFns<ReportWitnessFaultRequest> = {
  encode(message: ReportWitnessFaultRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.evidence.length !== 0) {
      writer.uint32(10).bytes(message.evidence);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReportWitnessFaultRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReportWitnessFaultRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.evidence = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReportWitnessFaultRequest {
    return { evidence: isSet(object.evidence) ? bytesFromBase64(object.evidence) : new Uint8Array(0) };
  },

  toJSON(message: ReportWitnessFaultRequest): unknown {
    const obj: any = {};
    if (message.evidence.length !== 0) {
      obj.evidence = base64FromBytes(message.evidence);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReportWitnessFaultRequest>, I>>(base?: I): ReportWitnessFaultRequest {
    return ReportWitnessFaultRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReportWitnessFaultRequest>, I>>(object: I): ReportWitnessFaultRequest {
    const message = createBaseReportWitnessFaultRequest();
    message.evidence = object.evidence ?? new Uint8Array(0);
    return message;
  },
};

function createBaseReportWitnessFaultResponse(): ReportWitnessFaultResponse {
  return { checkpoint: new Uint8Array(0) };
}

export const ReportWitnessFaultResponse: MessageFns<ReportWitnessFaultResponse> = {
  encode(message: ReportWitnessFaultResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.checkpoint.length !== 0) {
      writer.uint32(10).bytes(message.checkpoint);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReportWitnessFaultResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReportWitnessFaultResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.checkpoint = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReportWitnessFaultResponse {
    return { checkpoint: isSet(object.checkpoint) ? bytesFromBase64(object.checkpoint) : new Uint8Array(0) };
  },

  toJSON(message: ReportWitnessFaultResponse): unknown {
    const obj: any = {};
    if (message.checkpoint.length !== 0) {
      obj.checkpoint = base64FromBytes(message.checkpoint);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReportWitnessFaultResponse>, I>>(base?: I): ReportWitnessFaultResponse {
    return ReportWitnessFaultResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReportWitnessFaultResponse>, I>>(object: I): ReportWitnessFaultResponse {
    const message = createBaseReportWitnessFaultResponse();
    message.checkpoint = object.checkpoint ?? new Uint8Array(0);
    return message;
  },
};

/**
 * The service provided by the Workload container to the Orchestrator.
 * Used for high-frequency, low-latency signaling.
 */
export type WorkloadControlService = typeof WorkloadControlService;
export const WorkloadControlService = {
  /** Prepares a model or execution context in memory. */
  loadModel: {
    path: "/ioi.control.v1.WorkloadControl/LoadModel",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: LoadModelRequest): Buffer => Buffer.from(LoadModelRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): LoadModelRequest => LoadModelRequest.decode(value),
    responseSerialize: (value: LoadModelResponse): Buffer => Buffer.from(LoadModelResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): LoadModelResponse => LoadModelResponse.decode(value),
  },
  /**
   * Triggers execution of a job.
   * Inputs/Outputs are read/written directly from Shared Memory via the Data Plane.
   */
  executeJob: {
    path: "/ioi.control.v1.WorkloadControl/ExecuteJob",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: ExecuteJobRequest): Buffer => Buffer.from(ExecuteJobRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ExecuteJobRequest => ExecuteJobRequest.decode(value),
    responseSerialize: (value: ExecuteJobResponse): Buffer => Buffer.from(ExecuteJobResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ExecuteJobResponse => ExecuteJobResponse.decode(value),
  },
  /** Liveness probe. */
  healthCheck: {
    path: "/ioi.control.v1.WorkloadControl/HealthCheck",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: HealthCheckRequest): Buffer => Buffer.from(HealthCheckRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): HealthCheckRequest => HealthCheckRequest.decode(value),
    responseSerialize: (value: HealthCheckResponse): Buffer => Buffer.from(HealthCheckResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): HealthCheckResponse => HealthCheckResponse.decode(value),
  },
} as const;

export interface WorkloadControlServer extends UntypedServiceImplementation {
  /** Prepares a model or execution context in memory. */
  loadModel: handleUnaryCall<LoadModelRequest, LoadModelResponse>;
  /**
   * Triggers execution of a job.
   * Inputs/Outputs are read/written directly from Shared Memory via the Data Plane.
   */
  executeJob: handleUnaryCall<ExecuteJobRequest, ExecuteJobResponse>;
  /** Liveness probe. */
  healthCheck: handleUnaryCall<HealthCheckRequest, HealthCheckResponse>;
}

export interface WorkloadControlClient extends Client {
  /** Prepares a model or execution context in memory. */
  loadModel(
    request: LoadModelRequest,
    callback: (error: ServiceError | null, response: LoadModelResponse) => void,
  ): ClientUnaryCall;
  loadModel(
    request: LoadModelRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: LoadModelResponse) => void,
  ): ClientUnaryCall;
  loadModel(
    request: LoadModelRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: LoadModelResponse) => void,
  ): ClientUnaryCall;
  /**
   * Triggers execution of a job.
   * Inputs/Outputs are read/written directly from Shared Memory via the Data Plane.
   */
  executeJob(
    request: ExecuteJobRequest,
    callback: (error: ServiceError | null, response: ExecuteJobResponse) => void,
  ): ClientUnaryCall;
  executeJob(
    request: ExecuteJobRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ExecuteJobResponse) => void,
  ): ClientUnaryCall;
  executeJob(
    request: ExecuteJobRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ExecuteJobResponse) => void,
  ): ClientUnaryCall;
  /** Liveness probe. */
  healthCheck(
    request: HealthCheckRequest,
    callback: (error: ServiceError | null, response: HealthCheckResponse) => void,
  ): ClientUnaryCall;
  healthCheck(
    request: HealthCheckRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: HealthCheckResponse) => void,
  ): ClientUnaryCall;
  healthCheck(
    request: HealthCheckRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: HealthCheckResponse) => void,
  ): ClientUnaryCall;
}

export const WorkloadControlClient = makeGenericClientConstructor(
  WorkloadControlService,
  "ioi.control.v1.WorkloadControl",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): WorkloadControlClient;
  service: typeof WorkloadControlService;
  serviceName: string;
};

/**
 * Service exposed by the Guardian container to the Workload.
 * Used for secure egress and key management.
 */
export type GuardianControlService = typeof GuardianControlService;
export const GuardianControlService = {
  /** Execute an HTTP call using a secret securely held by the Guardian. */
  secureEgress: {
    path: "/ioi.control.v1.GuardianControl/SecureEgress",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: SecureEgressRequest): Buffer => Buffer.from(SecureEgressRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): SecureEgressRequest => SecureEgressRequest.decode(value),
    responseSerialize: (value: SecureEgressResponse): Buffer =>
      Buffer.from(SecureEgressResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): SecureEgressResponse => SecureEgressResponse.decode(value),
  },
  /** Request a guardianized certificate for a consensus payload. */
  signConsensus: {
    path: "/ioi.control.v1.GuardianControl/SignConsensus",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: SignConsensusRequest): Buffer => Buffer.from(SignConsensusRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): SignConsensusRequest => SignConsensusRequest.decode(value),
    responseSerialize: (value: SignConsensusResponse): Buffer =>
      Buffer.from(SignConsensusResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): SignConsensusResponse => SignConsensusResponse.decode(value),
  },
  /** Request witness-backed sealed finality proof material for a consensus payload. */
  sealConsensus: {
    path: "/ioi.control.v1.GuardianControl/SealConsensus",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: SealConsensusRequest): Buffer => Buffer.from(SealConsensusRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): SealConsensusRequest => SealConsensusRequest.decode(value),
    responseSerialize: (value: SealConsensusResponse): Buffer =>
      Buffer.from(SealConsensusResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): SealConsensusResponse => SealConsensusResponse.decode(value),
  },
  /** Request an equal-authority asymptote observer certificate for a sampled slot. */
  observeAsymptote: {
    path: "/ioi.control.v1.GuardianControl/ObserveAsymptote",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: ObserveAsymptoteRequest): Buffer =>
      Buffer.from(ObserveAsymptoteRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ObserveAsymptoteRequest => ObserveAsymptoteRequest.decode(value),
    responseSerialize: (value: ObserveAsymptoteResponse): Buffer =>
      Buffer.from(ObserveAsymptoteResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ObserveAsymptoteResponse => ObserveAsymptoteResponse.decode(value),
  },
  /** Request raw committee member signatures over a canonical guardian decision. */
  signCommitteeDecision: {
    path: "/ioi.control.v1.GuardianControl/SignCommitteeDecision",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: SignCommitteeDecisionRequest): Buffer =>
      Buffer.from(SignCommitteeDecisionRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): SignCommitteeDecisionRequest => SignCommitteeDecisionRequest.decode(value),
    responseSerialize: (value: SignCommitteeDecisionResponse): Buffer =>
      Buffer.from(SignCommitteeDecisionResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): SignCommitteeDecisionResponse => SignCommitteeDecisionResponse.decode(value),
  },
  /** Request raw witness committee member signatures over a canonical witness statement. */
  signWitnessStatement: {
    path: "/ioi.control.v1.GuardianControl/SignWitnessStatement",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: SignWitnessStatementRequest): Buffer =>
      Buffer.from(SignWitnessStatementRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): SignWitnessStatementRequest => SignWitnessStatementRequest.decode(value),
    responseSerialize: (value: SignWitnessStatementResponse): Buffer =>
      Buffer.from(SignWitnessStatementResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): SignWitnessStatementResponse => SignWitnessStatementResponse.decode(value),
  },
  /** Load a previously stored assigned recovery share by signed witness binding. */
  loadAssignedRecoveryShare: {
    path: "/ioi.control.v1.GuardianControl/LoadAssignedRecoveryShare",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: LoadAssignedRecoveryShareRequest): Buffer =>
      Buffer.from(LoadAssignedRecoveryShareRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): LoadAssignedRecoveryShareRequest =>
      LoadAssignedRecoveryShareRequest.decode(value),
    responseSerialize: (value: LoadAssignedRecoveryShareResponse): Buffer =>
      Buffer.from(LoadAssignedRecoveryShareResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): LoadAssignedRecoveryShareResponse =>
      LoadAssignedRecoveryShareResponse.decode(value),
  },
  /** Persist witness-fault evidence in the guardian witness log. */
  reportWitnessFault: {
    path: "/ioi.control.v1.GuardianControl/ReportWitnessFault",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: ReportWitnessFaultRequest): Buffer =>
      Buffer.from(ReportWitnessFaultRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ReportWitnessFaultRequest => ReportWitnessFaultRequest.decode(value),
    responseSerialize: (value: ReportWitnessFaultResponse): Buffer =>
      Buffer.from(ReportWitnessFaultResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ReportWitnessFaultResponse => ReportWitnessFaultResponse.decode(value),
  },
} as const;

export interface GuardianControlServer extends UntypedServiceImplementation {
  /** Execute an HTTP call using a secret securely held by the Guardian. */
  secureEgress: handleUnaryCall<SecureEgressRequest, SecureEgressResponse>;
  /** Request a guardianized certificate for a consensus payload. */
  signConsensus: handleUnaryCall<SignConsensusRequest, SignConsensusResponse>;
  /** Request witness-backed sealed finality proof material for a consensus payload. */
  sealConsensus: handleUnaryCall<SealConsensusRequest, SealConsensusResponse>;
  /** Request an equal-authority asymptote observer certificate for a sampled slot. */
  observeAsymptote: handleUnaryCall<ObserveAsymptoteRequest, ObserveAsymptoteResponse>;
  /** Request raw committee member signatures over a canonical guardian decision. */
  signCommitteeDecision: handleUnaryCall<SignCommitteeDecisionRequest, SignCommitteeDecisionResponse>;
  /** Request raw witness committee member signatures over a canonical witness statement. */
  signWitnessStatement: handleUnaryCall<SignWitnessStatementRequest, SignWitnessStatementResponse>;
  /** Load a previously stored assigned recovery share by signed witness binding. */
  loadAssignedRecoveryShare: handleUnaryCall<LoadAssignedRecoveryShareRequest, LoadAssignedRecoveryShareResponse>;
  /** Persist witness-fault evidence in the guardian witness log. */
  reportWitnessFault: handleUnaryCall<ReportWitnessFaultRequest, ReportWitnessFaultResponse>;
}

export interface GuardianControlClient extends Client {
  /** Execute an HTTP call using a secret securely held by the Guardian. */
  secureEgress(
    request: SecureEgressRequest,
    callback: (error: ServiceError | null, response: SecureEgressResponse) => void,
  ): ClientUnaryCall;
  secureEgress(
    request: SecureEgressRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: SecureEgressResponse) => void,
  ): ClientUnaryCall;
  secureEgress(
    request: SecureEgressRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: SecureEgressResponse) => void,
  ): ClientUnaryCall;
  /** Request a guardianized certificate for a consensus payload. */
  signConsensus(
    request: SignConsensusRequest,
    callback: (error: ServiceError | null, response: SignConsensusResponse) => void,
  ): ClientUnaryCall;
  signConsensus(
    request: SignConsensusRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: SignConsensusResponse) => void,
  ): ClientUnaryCall;
  signConsensus(
    request: SignConsensusRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: SignConsensusResponse) => void,
  ): ClientUnaryCall;
  /** Request witness-backed sealed finality proof material for a consensus payload. */
  sealConsensus(
    request: SealConsensusRequest,
    callback: (error: ServiceError | null, response: SealConsensusResponse) => void,
  ): ClientUnaryCall;
  sealConsensus(
    request: SealConsensusRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: SealConsensusResponse) => void,
  ): ClientUnaryCall;
  sealConsensus(
    request: SealConsensusRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: SealConsensusResponse) => void,
  ): ClientUnaryCall;
  /** Request an equal-authority asymptote observer certificate for a sampled slot. */
  observeAsymptote(
    request: ObserveAsymptoteRequest,
    callback: (error: ServiceError | null, response: ObserveAsymptoteResponse) => void,
  ): ClientUnaryCall;
  observeAsymptote(
    request: ObserveAsymptoteRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ObserveAsymptoteResponse) => void,
  ): ClientUnaryCall;
  observeAsymptote(
    request: ObserveAsymptoteRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ObserveAsymptoteResponse) => void,
  ): ClientUnaryCall;
  /** Request raw committee member signatures over a canonical guardian decision. */
  signCommitteeDecision(
    request: SignCommitteeDecisionRequest,
    callback: (error: ServiceError | null, response: SignCommitteeDecisionResponse) => void,
  ): ClientUnaryCall;
  signCommitteeDecision(
    request: SignCommitteeDecisionRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: SignCommitteeDecisionResponse) => void,
  ): ClientUnaryCall;
  signCommitteeDecision(
    request: SignCommitteeDecisionRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: SignCommitteeDecisionResponse) => void,
  ): ClientUnaryCall;
  /** Request raw witness committee member signatures over a canonical witness statement. */
  signWitnessStatement(
    request: SignWitnessStatementRequest,
    callback: (error: ServiceError | null, response: SignWitnessStatementResponse) => void,
  ): ClientUnaryCall;
  signWitnessStatement(
    request: SignWitnessStatementRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: SignWitnessStatementResponse) => void,
  ): ClientUnaryCall;
  signWitnessStatement(
    request: SignWitnessStatementRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: SignWitnessStatementResponse) => void,
  ): ClientUnaryCall;
  /** Load a previously stored assigned recovery share by signed witness binding. */
  loadAssignedRecoveryShare(
    request: LoadAssignedRecoveryShareRequest,
    callback: (error: ServiceError | null, response: LoadAssignedRecoveryShareResponse) => void,
  ): ClientUnaryCall;
  loadAssignedRecoveryShare(
    request: LoadAssignedRecoveryShareRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: LoadAssignedRecoveryShareResponse) => void,
  ): ClientUnaryCall;
  loadAssignedRecoveryShare(
    request: LoadAssignedRecoveryShareRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: LoadAssignedRecoveryShareResponse) => void,
  ): ClientUnaryCall;
  /** Persist witness-fault evidence in the guardian witness log. */
  reportWitnessFault(
    request: ReportWitnessFaultRequest,
    callback: (error: ServiceError | null, response: ReportWitnessFaultResponse) => void,
  ): ClientUnaryCall;
  reportWitnessFault(
    request: ReportWitnessFaultRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ReportWitnessFaultResponse) => void,
  ): ClientUnaryCall;
  reportWitnessFault(
    request: ReportWitnessFaultRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ReportWitnessFaultResponse) => void,
  ): ClientUnaryCall;
}

export const GuardianControlClient = makeGenericClientConstructor(
  GuardianControlService,
  "ioi.control.v1.GuardianControl",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): GuardianControlClient;
  service: typeof GuardianControlService;
  serviceName: string;
};

function bytesFromBase64(b64: string): Uint8Array {
  const bin = globalThis.atob(b64);
  const arr = new Uint8Array(bin.length);
  for (let i = 0; i < bin.length; ++i) {
    arr[i] = bin.charCodeAt(i);
  }
  return arr;
}

function base64FromBytes(arr: Uint8Array): string {
  const bin: string[] = [];
  arr.forEach((byte) => {
    bin.push(globalThis.String.fromCharCode(byte));
  });
  return globalThis.btoa(bin.join(""));
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
