// Workflow harness checkpoint helpers live in `core` for now because they share
// private runtime evidence builders with session persistence. This module marks
// the domain boundary for follow-on extraction without changing behavior.
